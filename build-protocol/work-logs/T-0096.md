# T-0096 Work Log

## 2026-08-03 — Task frame

- Started from pushed and post-merge-verified `main@0e06800b` in the isolated
  `task/T-0096-compose-kubernetes` worktree.
- Classified high-risk because the slice proves multi-replica persistence,
  authentication, failover, shutdown, and deployment topology contracts.
- The accepted Wave 5 F1 split is complete and binding, so no duplicate deep
  planning pass is needed. Recorded one implementation owner with explicit
  expected `gpt-5.6-terra` / medium metadata before dispatch.
- Recorded all four canonical review concerns before implementation. Final
  security and repository-wide release verification remain T-0097 scope.
- Next: push this durable task frame, dispatch the implementation owner, and
  begin with Docker/Compose capability evidence and RED topology acceptance.

## 2026-08-03 — Compose reference checkpoint

- Docker client/server `29.6.2`, Compose `v5.3.1`, and the Linux `overlay2`
  storage driver were available before topology work began.
- RED evidence: `node --test examples/message-board/deploy/compose/topology.test.mjs`
  failed because neither required Compose reference existed.
- Added deterministic combined and standalone references. The combined reference
  has one MessageBoard gateway/application process, one simple delivery server,
  a named durable registry namespace, the Datastore emulator selected by
  MessageBoard configuration, and Envoy. The standalone reference has two
  separately addressed MessageBoard applications and two standalone gateways
  that share one registry namespace, session configuration, and Datastore
  installation; Envoy resolves both gateway replicas through one DNS alias.
- Focused topology declarations passed, both references passed `docker compose
config --quiet`, and the real combined topology reached `MessageBoard
combined server ready`. The first emulator command incorrectly used the
  unsupported `--data-dir` option; it was removed after its bounded diagnostic
  evidence, and the rerun succeeded.
- Next: add repeatable Compose lifecycle/failover acceptance before Kubernetes
  references and specialist review.

## 2026-08-03 — Remote delivery configuration checkpoint

- The first Compose draft started a delivery-server container without configuring
  MessageBoard's environment-owned remote delivery facility. The correction is
  intentionally MessageBoard-owned: production configuration now requires
  `DELIVERY_SERVER_URL`, connects `RemoteDelivery`, and supplies a durable
  MessageBoard quarantine from the same application-selected storage factory.
- The quarantine is namespace-isolated, requires atomic compare-and-set, limits
  entries to 100 and serialized state to 64 KiB, limits an ID to 256 UTF-8
  bytes, requires the expected SHA-256 fingerprint shape, retries conflicts at
  most 32 times, and closes only its storage handle. It introduces no generic
  provider API.
- RED evidence: deployment configuration had no `RemoteDelivery.connectTo`.
  GREEN evidence: focused deployment and quarantine suites passed 8/8,
  including read/replace/delete, concurrent independently opened handles,
  corrupt state, full capacity, and close behavior. A 20-writer burst initially
  exhausted the eight-attempt bound; the bounded retry limit was raised to 32
  and the same contention test passed.
- `pnpm proto:generate` and `pnpm typecheck:build:generated` passed. Local
  images rebuilt successfully. Real combined Compose startup and real standalone
  two-application/two-gateway startup both reached every readiness log after
  the delivery-server HTTP/2 health check; the standalone topology has exactly
  one delivery container.
- Next: encode this real lifecycle evidence into the repeatable Compose
  acceptance harness, then add Kubernetes references.

## 2026-08-03 — Kubernetes reference checkpoint

- RED evidence: the three-manifest policy test failed because neither approved
  Kubernetes reference existed.
- Added storage-neutral combined and standalone reference manifests. Both
  configure application-owned storage only through runtime configuration and a
  Secret reference; neither declares a storage provider resource. The standalone
  reference has two application replicas and two separate gateway replicas,
  while the combined reference remains one application/gateway replica.
- Both references include Envoy and exactly one in-memory simple delivery server,
  configuration/Secret references, ten-second termination grace, TCP startup
  and readiness probes, and no liveness probe. They are deliberately reference
  manifests, not a Helm chart, operator, or deployment CLI.
- GREEN evidence: `node --test .../kubernetes/manifests.test.mjs` passed 3/3;
  both Compose references still passed `docker compose config --quiet`.
- Next: build the repeatable full Compose lifecycle/failover acceptance harness
  and focused deployment guidance before review.

## 2026-08-03 — Session and Envoy corrective scope

- A post-checkpoint inspection found that the initial Kubernetes references had
  decorative Envoy deployments and that production MessageBoard processes still
  used the local fixed bearer resolver. This does not meet F1's shared session
  signing, validation, and revocation requirement.
- The bounded correction is MessageBoard-owned. Production reads one shared
  ES256 private key, key ID, issuer, and audience from deployment configuration;
  each process creates `SignedSessions` over a namespace-isolated, bounded,
  compare-and-set revocation record in application-selected storage. Local
  development continues to use `LocalBoardSession` only. No generic storage or
  authentication-provider API is introduced.
- Acceptance will issue through one configured resolver, resolve through a
  second one, revoke through the first, and prove the second rejects it. The
  Kubernetes references will mount a concrete Envoy configuration and TLS
  material, and expose real services from Envoy to the appropriate combined or
  standalone backend.

## 2026-08-03 — Corrective runtime evidence

- Production session tests now prove that a token issued by one configured
  MessageBoard resolver validates in another process with the same namespace,
  and that revocation is immediately visible there. Expired records are swept
  during each compare-and-set mutation; storage closure fails subsequent
  validation closed.
- A fresh generated build was required before image construction because the
  local builder packages compiled artifacts. The rebuilt MessageBoard image
  contains the new session-revocation artifact. The targeted container test
  issued a signed browser token, completed subscription registry operations,
  and confirmed Node PID 1 exits cleanly for TERM and INT.
- The repeatable standalone Compose acceptance started both applications and
  both gateways, terminated one gateway, confirmed the other stayed running,
  restored the terminated gateway, and removed all containers, network, and
  volumes. It passed in 16 seconds.

## 2026-08-03 — Remaining acceptance reassignment

- Coordinator pre-review reconciliation found that process-state failover did
  not yet satisfy F1's public Envoy Post, authoritative Query,
  Subscribe/Activate/Cancel, restart preservation, reconnect/re-query, and
  graceful termination acceptance.
- The initial implementation owner accepted the finding but completed three
  resumed turns after analysis or mechanical checks without implementing the
  bounded combined-Envoy checkpoint. No uncommitted work was left and no
  architectural or environmental blocker was established.
- Reassign the remaining harness-only acceptance to a fresh existing
  `implementer`, expected and explicitly dispatched as `gpt-5.6-terra` /
  medium. Ownership is limited to MessageBoard Compose acceptance helpers/tests
  and the T-0096 log; it must reuse existing interop/model-registry client
  seams, commit and push each bounded checkpoint, and not alter accepted
  framework contracts or protected files.

## 2026-08-03 — Public deployment convergence

- The first signed gRPC-Web command reached the combined application but its
  Projection query stayed empty. Durable diagnostics established two causes:
  context inbox writes still used local storage after the environment worker
  switched to remote delivery ports, and the remote adapter accepted only an
  already-packed wire target ID while repository handoff uses a plain stable
  entity ID.
- The environment handoff now transfers one matching inbox/work-registry pair
  to context writes and workers. The delivery adapter preserves plain framework
  IDs by carrying them as `google.protobuf.StringValue` in the frozen wire
  `EntityId`, while retaining already-packed IDs. The generated build and 149
  focused server tests passed; 58 focused delivery-client/server tests passed,
  including the new exact wire round trip.
- Envoy now forwards the browser gateway's HTTP/1 gRPC-Web endpoint without an
  incorrect HTTP/2 upstream transform. Standalone routing uses strict DNS and
  consistent hashing of the authenticated session header so one connected
  subscription remains on one gateway while reconnect can select a surviving
  replica.
- Combined acceptance passed in 19.2 seconds: signed Post, authoritative Query,
  Subscribe/Activate/update/Cancel, graceful `SIGTERM` with exit code zero, and
  no leaked Compose resources.
- Standalone acceptance passed in 24.0 seconds: the same public behavior through
  Envoy, subscription creation on gateway 1, forced gateway-1 loss, durable
  cancellation through gateway 2, restart without activation resurrection,
  reconnect/re-query, graceful gateway shutdown, and no leaked Compose
  resources. Both Compose references also pass `docker compose config --quiet`.
- No Spine JVM build or JVM-source mutation was used.
- Next: push this checkpoint, run the complete deterministic T-0096 mechanical
  gate, then dispatch the four recorded specialist review concerns.
