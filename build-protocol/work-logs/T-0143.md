# T-0143 Work Log

## Start

- Base: integrated Wave 8 train commit `8372af0e` on
  `task/T-0143-documentation-convergence`.
- Classification: standard documentation convergence.
- Initial broad scan returned 375 matches. Historical plans, reports, handoffs,
  decisions, release evidence, JVM evidence, and completed task/review/work logs
  are evidence rather than active guidance and will not be rewritten.
- Confirmed current correction areas: governing validation package references;
  delivery claim/attempt prose; architecture/API diagrams and inventories;
  root/reference fingerprint language; and active provider/storage guidance.

## Model And Role Record

- Documentation implementation owner: existing `implementer` role, explicitly
  dispatched as `gpt-5.6-terra` / `medium`; ownership is limited to active
  T-0143 documentation, deterministic documentation checks, and this task's
  protocol records. The owner must not spawn subagents.
- Planned documentation reviewer: immutable `gpt-5.6-luna` / `medium`.
- Planned TypeScript/API reviewer: `gpt-5.6-terra` / `high`.
- Planned performance/reliability reviewer: `gpt-5.6-terra` / `high`.
- Runtime metadata will be recorded when exposed; immutable configured profiles
  satisfy the gate when introspection is unavailable.

## Checkpoint 1

- Corrected the active validation runtime claim to
  `@spine-event-engine/validation` `2.0.0-snapshot.7` in governing and public
  entry-point documentation.
- Corrected the two USER_GUIDE remote-inbox examples: `RemoteInbox` now has no
  quarantine argument. Remote removal rereads the exact pending row and calls
  `removeOne()` directly, with no local removal state.
- Replaced the active API-reference failure-policy description with the
  delivered `DeliveryMonitor` behavior: default failed reception is marked
  delivered, independent targets continue, lost acknowledgements can redeliver,
  and downstream effects must be idempotent.
- Updated the task ownership record to name the serialized `implementer` as the
  sole documentation writer, preserving one-writer semantics.
- Evidence: `git diff --check` and `pnpm verify:task -- --no-tests` both exited
  0 on this checkpoint. The latter ran the task documentation/Markdown gate and
  the repository TypeScript build gate. Remaining delivery-attempt prose in the
  two governing runtime/API documents is the next correction batch.

## Checkpoint 2

- Removed the active architecture-guide attempt, retained-claim, and
  exhaustion narrative. The guide now matches the server reference's direct
  Inbox rows, shard-only exclusion, delivered-row deduplication, lost-ack
  redelivery, downstream idempotency, and `DeliveryMonitor` policy.
- Focused stale scan is clean for active public docs except the two remaining
  governing legacy sections in `DEVELOPER_API.md` and
  `RUNTIME_ARCHITECTURE.md`; `docs/spine-ts-extra-concepts-vs-core-jvm.md` is
  historical reduction evidence and intentionally excluded from active claims.
- `git diff --check` exited 0 after this correction.

## Governing Delivery Convergence

- Replaced the remaining `DEVELOPER_API.md` and `RUNTIME_ARCHITECTURE.md`
  attempt/exhaustion/claim narratives with the current direct-Inbox model:
  direct pending/delivered rows, shard-only ownership exclusion, delivered-row
  deduplication, non-transactional handler effects and acknowledgement,
  redelivery after a lost acknowledgement, and downstream idempotency.
- Recorded the delivered `DeliveryMonitor` contract: the default marks failed
  reception delivered and continues independent targets; a custom monitor may
  repeat immediately; a durable acknowledgement failure leaves the row pending,
  blocks later same-target rows for that run, continues independent targets,
  releases ownership, and permits a later run. No attempt history, exhaustion,
  quarantine, receipt, marker, timer, backoff, dead-letter storage, scheduler
  persistence, or policy is documented as runtime behavior.
- Deterministic evidence: `node scripts/check-doc-audience.mjs`,
  `node scripts/check-release-readiness.mjs` (312 relative Markdown links),
  `pnpm exec prettier --check` for changed files, and `git diff --check` each
  exited 0. `pnpm exec vitest run scripts/check-typescript-snippets.test.mjs
scripts/package-metadata.test.mjs` passed 2 files / 14 tests.
- The focused current-claims scan has one intentional match:
  `docs/spine-ts-extra-concepts-vs-core-jvm.md:70` names delivery-attempt
  summaries in a historical reduction inventory. It is evidence, not current
  guidance, and remains unchanged under this task's historical-record rule.
- Final `verify:task` is deliberately deferred to the parent after review
  convergence, per the current assignment.

## Review Correction Batch

- Resolved the aggregated API, architecture, and governing-document findings:
  retired `DeliveryPage`, summary, guard-record, fixed dedup-window,
  epoch/claim/`PAUSED`, doubled-sweep, two-drain, and timer-renewal claims are
  removed from active guidance.
- Current claims describe direct bounded pending-row reads, complete-`WorkerId`
  fencing, same-worker unexpired convergence, different-worker exclusion until
  expiry, stale-release fencing, delivered-row deduplication, and the
  `DeliveryMonitor` default/failure-isolation behavior.
- Storage documentation now names `createRecordStorage(context, spec, group?)`
  and in-memory physical identity by context, source type, and optional
  `StorageGroup`; compatible distinct specs share backing.
- Focused evidence: stale-source scan, audience/readiness/link checks,
  formatting, and diff checks. Final `verify:task` remains deferred to the
  parent after targeted re-review convergence.

## Final Targeted Corrections

- Removed the stale separate-dedup-record wording from the malformed-record
  failure path; active guidance now names only inbox and shard-session records.
- Cross-checked `packages/server/src/delivery/inbox-storage.ts` and
  `packages/server/src/repository/repository.ts`: lower-level `keepUntil` is
  optional, while repository handoffs set it to `whenReceived + 30 seconds`.
  Delivered rows suppress matching duplicates through that deadline, or
  indefinitely when the lower-level field is absent.
- Documentation, TypeScript/API, and performance/reliability targeted
  re-reviews converged CLEAN. Style remains N/A for prose-only work; security
  remains N/A because no trust-boundary claim changed.

## Final Verification

- `pnpm verify:task -- --no-tests` exited 0 after review convergence.
- The final profile passed the repository TypeScript build, tooling typecheck,
  cleanup enforcement, TSDoc, formatting, documentation audience, and release
  readiness gates.
- Release readiness verified 80 package imports, 51 package assets, and 312
  relative Markdown links.
- T-0143 is complete. The next Wave 8 milestone is the final invention audit
  and repository release verification.
