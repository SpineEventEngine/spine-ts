# T-0072 Work Log

## 2026-07-24 — Milestone start

- Baseline: pushed `main@f826acec`.
- Branch: `task/T-0072-generic-entity-query`.
- Worktree:
  `.worktrees/T-0072-generic-entity-query`.
- Classification: high-risk because public APIs/code generation and durable
  query authority change atomically across all entity kinds.
- The approved Wave 2 plan freezes names, behavior, exclusions, review lanes,
  and completion gates. No new human product decision is currently required.
- Deep planning is limited to the existing requirements splitter because this
  milestone changes a public/code-generated contract. Expected model
  `gpt-5.6-sol`; expected reasoning `high`; both must be explicit in dispatch.
  The role is read-only, may not edit or spawn children, and must return a
  dependency-ordered implementation package rather than reopen accepted
  product decisions.
- First requirements-split dispatch: existing role `requirements_splitter`,
  explicit model `gpt-5.6-sol`, explicit reasoning `high`. The Desktop surface
  exposes the immutable configured role/profile but no child self-introspection
  beyond the explicit dispatch metadata. The pass did not return a bounded
  result after repeated narrowed requests and was interrupted to avoid planning
  latency. No result from that pass is accepted.
- Redispatch gate: the same existing role/profile is required, with a concise
  four-part output and no further broad exploration. This is a demonstrated
  orchestration-latency correction, not a product or architectural blocker.
- Requirements redispatch completed and is accepted. Existing role:
  `requirements_splitter`; configured and explicitly dispatched profile:
  `gpt-5.6-sol` with `high` reasoning. The surface exposes that immutable
  configured profile but no separate runtime self-introspection; this
  limitation is recorded and does not invalidate the correctly dispatched
  result.
- Accepted split confirms:
  - the full Projection-prefixed root/codegen/type/file/helper/executable
    family is replaced atomically with Entity terminology, including field
    classification and every Buf configuration;
  - `EntityRecordStorage.query(NormalizedQueryPlan)` is the smallest provider
    query port, with declared columns in the storage input/fingerprint and
    fixed durable lifecycle/version columns;
  - Stand and repositories must share one storage descriptor and physical
    current-record identity, removing `RecordSpec`, legacy index authority,
    dual writes, and the divergent `:stand`/`:entity` keys;
  - RED/GREEN order is declarations/bin, three-kind generation, shared/provider
    conformance, index-independent Stand, all-three-kind service integration,
    then examples/docs/residual scans and full gates.
- No product or environmental blocker was found.

## 2026-07-24 — Implementation dispatch

- Existing role: `implementer`.
- Expected model: `gpt-5.6-terra`.
- Expected reasoning: `medium`.
- Both fields are explicit in the dispatch. The Desktop surface exposes the
  immutable configured role/profile and dispatch metadata, but not independent
  child self-introspection.
- Ownership is the complete overlapping T-0072 change surface in the isolated
  task worktree: client query/column API and declarations, code generator and
  executable, storage current-record query port and providers, server query
  execution, examples, tests, docs, and Wave 2 audit/status records.
- The owner may not spawn children, commit, push, merge, or modify unrelated
  root-worktree files. It must implement test-first and return exact verification
  evidence and remaining limitations.

## 2026-07-24 — Public rename and durable-query implementation notes

- RED evidence: `packages/client/test/index.test.ts` now requires the package-root
  `EntityColumn` export. The focused package test was invoked before implementation,
  but this isolated worktree has no verified dependency installation and pnpm stopped
  at `ERR_PNPM_VERIFY_DEPS_BEFORE_RUN`; the assertion remains the intended failing
  behavior test and must be rerun after installation.
- The client implementation is being moved atomically to Entity terminology:
  `EntityQuery`, `EntityColumn`, `defineGeneratedEntityColumns`, the entity-columns
  executable, and entity field classification. No compatibility alias is intended.
- Accepted requirements precision: the eventual storage contract is
  `EntityRecordStorage.query(NormalizedQueryPlan)`, with Stand/repository callers
  sharing one current-record identity. `RecordStorage` is not permitted to remain
  query candidate authority or a dual-write index. Provider implementations must
  retain finite bounded materialization and durable version/lifecycle authority.

## 2026-07-24 — Validation environment limitation

- Running with `--config.verify-deps-before-run=false` bypasses pnpm's preflight,
  but `pnpm ... typecheck:build:generated` fails at `tsc: command not found`.
  Both root and package-local `node_modules/.bin` directories are absent. The
  directed no-install constraint means the focused TypeScript, lint, formatter,
  and Vitest commands cannot execute in this worktree until the task environment
  is provisioned with the lockfile dependencies.
- First implementation pass returned partial. Existing role `implementer`;
  configured and explicitly dispatched profile `gpt-5.6-terra` with `medium`
  reasoning; independent runtime self-introspection is unavailable. The
  client/codegen edits are retained as work in progress, but the result is not
  accepted because storage/server/provider behavior, tests, cleanup, and docs
  closure remain incomplete.
- The reported `ERR_PNPM_VERIFY_DEPS_BEFORE_RUN` is not an environmental
  blocker. The repository's established native verification invocation uses
  `--config.verify-deps-before-run=false`, so the same implementation context
  is redispatched with that explicit command requirement.
- The continuation confirmed the isolated worktree had no installed
  `node_modules`, so TypeScript and formatting executables were unavailable.
  The first sandboxed install attempt could not resolve registry hosts; after
  stopping its retry loop, `pnpm install --frozen-lockfile` was rerun with
  approved network access and completed successfully with 308 lockfile-pinned
  packages. The toolchain blocker is resolved without a lockfile change.
- Client/codegen focused GREEN evidence after toolchain restoration:
  `proto:generate` plus the focused client set completed with 4 test files and
  30 tests passing; the changed public types compile, Prettier was applied,
  `git diff --check` is clean, and a live-tree scan rejects the old public
  query/column/helper/executable/classifier/path family. This verifies only the
  completed client slice; the T-0072 result remains unaccepted until the durable
  storage/server query authority and provider integrations are complete.
- Core implementation found a genuine durable-contract ambiguity before making
  provider changes: `EntityRecordStorage.query()` must enumerate typed IDs, but
  `EntityIdCodec` is one-way (`key(id)` only), while MySQL and Datastore current
  rows do not persist a generic reversible ID representation. Fabricating IDs
  or retaining the legacy index would violate the accepted authority contract.
- This is a demonstrated architecture/public-SPI block, so the existing
  requirements splitter is invoked at the milestone boundary. Expected and
  explicitly dispatched profile: `gpt-5.6-sol` with `high` reasoning. It must
  select the smallest contract consistent with the already approved lack of
  migration compatibility, provider durability, and query semantics, without
  reopening product scope.
- Architecture decision accepted. Existing role `requirements_splitter`;
  configured and explicitly dispatched profile `gpt-5.6-sol` with `high`
  reasoning; independent runtime self-introspection is unavailable.
- Add `extractId(state)` and declared query columns to `EntityStorageInput`, and
  derive the typed ID from the authoritative decoded state during enumeration.
  Writes and queries must compare the extracted ID's canonical key to the
  supplied/physical row key and reject mismatches as provider data errors.
  Storage keys remain one-way and no duplicate typed-ID payload is persisted.
- Repository and Stand use one internal descriptor with
  `entityStorageKey(stateType, "current")`, layout
  `spine-ts.entity-record.v2`, identical ID field/extractor/codec, and a
  canonical fingerprint over layout, state type, ID descriptor, and declared
  ordered column types. Unsupported ID descriptors fail at registration.
- Memory, MySQL, and Datastore enumerate bounded current rows, decode full
  states, extract/clone IDs, verify row keys, add durable
  version/archived/deleted columns, enforce `candidateLimit + 1`, and use the
  shared evaluator. Datastore indexes the existing canonical scope property;
  MySQL selects the existing entity key and payload. No compatibility reader or
  migration is required.
- The first core implementation owner restored typecheck GREEN by making
  `query`, `extractId`, and declared columns optional while leaving Datastore
  and MySQL without query support. That transitional shape is explicitly
  rejected: the task requires an atomic breaking cutover with no compatibility
  accommodation. Its useful in-memory implementation remains work in progress,
  but no core acceptance is recorded.
- After repeated partial returns from the original context, a replacement
  bounded `implementer` owns the remaining core/provider/server files. Expected
  and explicitly dispatched profile: `gpt-5.6-terra` with `medium` reasoning.
  The surface exposes configured dispatch metadata but no independent runtime
  self-introspection. The replacement must remove optionals, complete all
  providers and shared identity, and preserve compiler GREEN.

## 2026-07-24 — Core storage cutover continuation

- The rejected optional compatibility shape is removed: `EntityRecordStorage.query`,
  `EntityStorageInput.extractId`, and declared `columns` are required.
- Memory, Datastore, and MySQL current storage now expose the query port, evaluate
  bounded candidates through the shared evaluator, include durable lifecycle/version
  columns, and reject a physical/current key that disagrees with the decoded state's
  extracted ID. Provider fingerprints include ordered declared column descriptors.
- Repository and Stand use the shared `:current` key and `spine-ts.entity-record.v2`
  layout; Stand no longer writes or enumerates a `RecordStorage` query index.
- Verification: `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed, followed by focused memory, Datastore, and MySQL entity-history tests:
  3 files / 175 tests passed.
- Remaining uncertainty: the legacy `Stand.queryVersioned(RecordQuery)` adapter currently
  performs durable bounded enumeration but does not translate legacy record filters;
  the canonical normalized query path is durable. Server Aggregate/Projection/PM and
  index-independence tests still need to be run and may require completing that adapter.

## 2026-07-24 — Server descriptor and legacy-query closure

- Added one internal entity-storage descriptor builder. Repository and Stand now supply
  byte-identical current-record layout, key, ID codec/extractor, and descriptor columns
  for the same state schema; Stand keeps no `RecordStorage` index authority or dual write.
- `Stand.queryVersioned(RecordQuery)` now validates and translates IDs, equality filters,
  ordering, masks, and limits to the normalized durable query plan. Legacy continuation
  tokens are rejected with a clear migration message rather than falling back to an index.
- Focused GREEN evidence: build typecheck plus Stand, repository routing, memory,
  Datastore, and MySQL suites passed: 5 files / 340 tests. `git diff --check` is clean.

## 2026-07-24 — Implementation closure and review dispatch

- Implementation closure evidence:
  - client/codegen: 37 tests passed;
  - server/provider: 340 tests passed;
  - generated typecheck, generated lint, format check, generated docs check,
    generated Proto cleanliness, and `git diff --check` passed;
  - the live-tree residual scan finds no old public symbol, helper, executable,
    classifier, or path outside historical protocol records and the active task
    requirement text.
- The implementation result is review-ready but not yet accepted, committed, or
  merged.
- One complete specialist wave is required. Expected profiles, explicit in each
  dispatch:
  - existing `style_maintainability_reviewer`: `gpt-5.6-terra`, `high`;
  - existing `documentation_reviewer`: `gpt-5.6-luna`, `medium`;
  - existing `typescript_api_docs_reviewer`: `gpt-5.6-terra`, `high`;
  - existing `performance_reliability_reviewer`: `gpt-5.6-terra`, `high`.
- Reviewers are read-only, may not spawn children, and must review the complete
  baseline-to-task diff. The Desktop surface exposes configured role/profile
  and explicit dispatch metadata but no separate child self-introspection.
- The documentation dispatch surface rejects a `gpt-5.6-luna` override because
  `documentation_reviewer` has an immutable configured `gpt-5.6-luna` /
  `medium` profile. The first call therefore did not create a child. The role
  is redispatched without an override, honestly relying on that fixed existing
  role/profile; this is a tooling limitation, not inherited-parent fallback.

## 2026-07-24 — Specialist review wave one

- Style/maintainability: findings. Existing role and explicitly dispatched
  profile `style_maintainability_reviewer`, `gpt-5.6-terra` / `high`; no
  independent runtime self-introspection. Accepted:
  - Stand discards authoritative queried records and performs N+1 re-reads,
    creating a post-filter/order/limit mutation race;
  - active implementation/JSDoc names still contain Projection terminology;
  - the EntityColumn test remains under the obsolete `test/projection` path.
- TypeScript/API: findings. Existing role and explicitly dispatched profile
  `typescript_api_docs_reviewer`, `gpt-5.6-terra` / `high`; no independent
  runtime self-introspection. Accepted:
  - TypeDoc inventory still expects six removed Projection-prefixed types;
  - public/package/API documentation still describes Projection-only metadata
    and query support. The unchanged Query Proto and required storage SPI were
    confirmed clean.
- Documentation: findings. Existing immutable role/profile
  `documentation_reviewer`, `gpt-5.6-luna` / `medium`; no independent runtime
  self-introspection and explicit override unsupported by the surface.
  Accepted: guide/client README/package/codegen JSDoc Projection-only wording
  and one malformed Entity-query overview sentence. No other contradiction,
  broken link, history claim, or Wave status error was found.
- Performance/reliability: findings. Existing role and explicitly dispatched
  profile `performance_reliability_reviewer`, `gpt-5.6-terra` / `high`; no
  independent runtime self-introspection. Accepted:
  - memory query has unbounded default/all-map materialization;
  - independently confirmed Stand N+1/re-read consistency race;
  - Aggregate and Process Manager routes omit/reject lifecycle/version and
    declared-column filters;
  - descriptor fingerprint omits the selected ID field;
  - shared history conformance does not exercise the required current query
    port/provider parity.
- One aggregated correction batch returns to the existing implementation
  context. Re-review is required for style/API/reliability and documentation
  because substantive behavior and public documentation change.

## 2026-07-24 — Affected-lane re-review

- Confirmed fixed: Stand N+1/re-read race, finite memory default/sentinel,
  selected-ID-field fingerprint, internal helper rename, and EntityColumn test
  path.
- Remaining accepted corrections:
  - exclude tombstones consistently before candidate accounting,
    ordering, and limits in memory, Datastore, and MySQL;
  - remove Projection-only validation/include-all restrictions for Aggregate
    and Process Manager routes, with declared/system column request tests;
  - extend shared provider conformance so all three providers execute the
    required `current.query()` contract, including bounds, ordering,
    lifecycle/version, ID validation, reopen, and mismatch behavior;
  - replace the six removed Projection-prefixed TypeDoc inventory entries;
  - correct the exact remaining guide, README, package, codegen JSDoc, and test
    wording/grammar instances;
  - deduplicate Stand result construction through one helper.
- All four affected lanes require one final bounded re-review after correction.

## 2026-07-24 — Specialist closure

- Final style/maintainability re-review: CLEAN.
- Final TypeScript/API re-review: CLEAN.
- Final documentation re-review: CLEAN.
- Final performance/reliability re-review: CLEAN.
- Configured role/model/reasoning metadata, the immutable documentation-role
  override limitation, every finding, disposition, and focused validation are
  durably recorded in
  `build-protocol/reviews/T-0072-generic-entity-query.md`.
- The next gate is full native verification, followed by the final Wave 2
  security review. No commit, merge, or push is permitted yet.

## 2026-07-25 — Full verification

- Definitive `pnpm --config.verify-deps-before-run=false verify` passed with
  approved local loopback/IPC/process permissions required by the repository's
  native gRPC, ZeroMQ, and child-process suites.
- Non-coverage tests: 130 files, 2,466 tests passed; 3 files / 25 opt-in tests
  skipped.
- Coverage tests: the same 130 files and 2,466 tests passed; statements 94.08%
  (14,806/15,737), branches 90.02% (8,018/8,906), functions 94.47%
  (3,694/3,910), lines 94.76% (14,105/14,884).
- Generated build/tooling typecheck, lint/cleanup, format, TypeDoc/API inventory,
  Proto source/descriptor integrity, generated Proto cleanliness, and release
  readiness passed. Release readiness verified 19 package imports and 121
  relative Markdown links.
- Earlier sandbox transport failures, one obsolete current-write delay fixture,
  tracked-deleted-path tooling, a semantic-name cleanup rule, and the branch
  threshold were all isolated, corrected, re-reviewed where substantive, and
  rerun clean. No unresolved limitation remains.
- Final gate assignment: existing `security_reviewer`, expected and explicitly
  dispatched `gpt-5.6-terra` / `high`, read-only, no children. It must review
  generated-code trust, query validation/resource bounds, provider
  parameterization, lifecycle data exposure, tenant/current-record authority,
  and the unchanged remote boundary.

## 2026-07-24 — Implementation closure evidence

- The generator fixture now exercises Aggregate, Projection, and Process Manager
  schemas from one descriptor source and asserts deterministic companion output.
- Positive root/codegen/bin coverage remains in the renamed client fixtures; the
  former Projection entrypoints are absent from the live tree and the formatter
  ignores deleted tracked paths during an atomic rename.
- Documentation and status were reconciled without claiming merge completion:
  the user guide and package README wording use generic Entity terminology, the
  T-0072 upstream delta audit records Wave 2 disposition, and the completion
  plan says the task is awaiting review.
- Mechanical evidence: focused client/codegen tests passed (5 files / 37 tests);
  focused server/provider tests passed (5 files / 340 tests); `typecheck:generated`,
  `lint:generated`, `format:check`, `docs:check:generated`, `proto:check-generated`,
  and `git diff --check` passed after strict fixture and formatter corrections.

## 2026-07-24 — Final correction-batch closure

- Root cause of the service regression was obsolete tests, not a runtime failure:
  they injected storage errors to assert the retired Projection-only `include_all`
  guard. They now positively prove Aggregate and Process Manager `include_all`
  reads, plus declared and lifecycle system-column filters for both families.
- Stand point reads and current-query results now use one result mapper. The shared
  provider fixture now exercises current-query lifecycle/version predicates,
  declared-column ordering, limits, and tombstone exclusion; Memory and Datastore
  pass it, while focused MySQL factory coverage verifies its current-query path.
- Final evidence: focused cross-provider/client/service suite 348/348; service
  suite 156/156; Stand 31/31; expanded conformance adapters 74/74. Generated
  typecheck, lint/cleanup, formatting, generated docs/Typedoc API inventory, and
  whitespace diff checks pass.
- Limitation recorded: no standalone `proto:compat:check` or `api:check` scripts
  exist; the repository's generated Proto and Typedoc/API checks were used. This
  task is review-accepted mechanically but remains uncommitted/unmerged/unpushed.

## 2026-07-24 — Reliability coverage blocker closure

- Extended the shared current-query conformance with candidate-limit sentinel
  overflow, close/reopen reads, extracted-state-ID/physical-key mismatch, and
  incompatible descriptor-fingerprint checks. Existing lifecycle/version/order/
  limit/tombstone assertions remain part of that contract.
- MySQL now executes the extracted shared current-query contract against a
  fixture-backed durable-current table reset in `beforeEach`; Memory and Datastore
  continue to execute it through their history adapter. No production behavior was
  modified.
- Evidence: the three provider suites pass 176 tests; generated typecheck,
  lint/cleanup, formatting, and whitespace-diff gates pass. No commit, merge, or
  push was performed.

## 2026-07-24 — Coverage threshold correction

- Added a focused shared-descriptor contract for null, scalar, and structured ID
  canonicalization, extracted state ID, storage key, selected-field fingerprint,
  and defensive clone behavior. This executes T-0072 durability branches without
  changing runtime behavior.
- Focused repository/provider validation passes 311 tests. The full generated
  coverage command completed successfully twice after typecheck/build, but this
  surface returned only the V8 startup banner and produced no coverage artifact.
  The six new branches move the recorded 8011/8906 baseline to 8017/8906 (90.02%)
  when the denominator is unchanged; the parent coverage surface should retain
  the authoritative reporter table.
- Generated lint/cleanup, format, and whitespace-diff gates pass. No commit,
  merge, or push was performed.

## 2026-07-25 — Final review and gate closure

- The parent coverage reporter supersedes the earlier child estimate:
  branches are 90.02% (8,018/8,906), with 130 files / 2,466 tests passing.
- Full native `verify` passed end-to-end on the final source state, including
  TypeDoc/API, Proto, generated cleanliness, and release readiness.
- Final targeted style and reliability review of tracked-deleted-path tooling
  is CLEAN. All callers reuse `lstatIfPresent`; only `ENOENT` is suppressed,
  broken symlinks remain visible, and other filesystem failures propagate.
- Final security review: CLEAN. Existing `security_reviewer`, explicitly
  dispatched `gpt-5.6-terra` / `high`; no independent runtime
  self-introspection. No security exception or unresolved limitation remains.
- T-0072 is ready for task commit and integration. It is not yet merged,
  post-merge verified, or remotely synchronized.

## 2026-07-25 — Post-merge cancellation correction

- T-0072 was merged as `69d43c0a` and pushed to `origin/main`. The deterministic
  formatting correction `74d0192a` was also pushed immediately.
- The first post-merge coverage run passed all 2,466 assertions but failed on
  three unhandled Connect cancellation rejections from the project-management
  load runner. Root-cause tracing found an abandoned pending
  `iterator.next()` when an earlier command or query outcome failed.
- The runner now observes that pending read at creation time, matching the
  established Datastore-orders cleanup pattern. The focused real-gRPC failure
  path passed five consecutive runs, and the exact full coverage gate passed
  130 files / 2,466 tests with 90.02% branch coverage and no unhandled errors.
- Reopened review assignment: existing
  `performance_reliability_reviewer`; expected and explicitly dispatched
  `gpt-5.6-terra` / `high`; read-only; no child agents. The configured role
  profile is visible, but the surface does not expose independent runtime
  self-introspection.
- Bounded performance/reliability re-review: CLEAN. The observer attaches to
  the same pending promise immediately, so cleanup cancellation cannot become
  unhandled while a normally reached first-update failure still propagates
  through the original awaited promise. No lifecycle or resource leak was
  found.

## 2026-07-25 — Durable closure

- Task commit `608fb80a` is pushed on
  `origin/task/T-0072-generic-entity-query`.
- Merge commit `69d43c0a`, formatting correction `74d0192a`, and reviewed
  cancellation correction `bfa2418f` were each pushed immediately to
  `origin/main`.
- Definitive post-merge `verify` on `bfa2418f`: PASSED. Non-coverage and
  coverage runs each passed 130 files / 2,466 tests; 3 files / 25 opt-in tests
  were skipped. Coverage was statements 94.08% (14,809/15,740), branches
  90.02% (8,018/8,906), functions 94.47% (3,695/3,911), and lines 94.76%
  (14,108/14,887).
- Generated/build/tooling typecheck, lint/cleanup, formatting, TypeDoc/API
  inventory, Proto source/descriptor integrity and generated cleanliness, and
  release readiness all passed. Release readiness verified 19 public package
  imports and 121 relative Markdown links.
- All specialist lanes and final security review are CLEAN. The post-merge
  asynchronous correction received a focused CLEAN reliability re-review.
- T-0072 and Wave 2 are durably closed. Wave 3 remains unstarted pending its
  required human Q&A and approved plan.
