# T-0158A Work Log

## 2026-08-11 - Startup

- Opened from `origin/main@e3bd0dfa` as the bounded prerequisite for T-0158.
- JVM evidence fixes the contract: `InboxId.entity_id` contains typed
  `EntityId(Any)`; core `Identifiers` supplies string, int32, int64, and message
  packing. Existing TS `targetId: string` / forced `StringValue` is not
  interoperable and cannot support typed replay.
- Implementation assignment: existing implementer, explicit
  `gpt-5.6-terra` / medium, no subagents. Runtime metadata is recorded when
  exposed; otherwise the immutable configured profile is evidence.
- Requirements decision: existing requirements splitter, explicit
  `gpt-5.6-sol` / high, no subagents; runtime metadata unavailable.
- Verification profile: bounded `verify:task` after one converged specialist
  review wave.

## 2026-08-11 - Implementation Convergence

- `InboxId.targetId` now preserves the complete generated `Any`. The direct
  record converter, local handoff keys, durable deduplication, delivery-client
  wire codec, and default shard strategy compare or encode the complete typed
  identity instead of forcing a `StringValue`.
- Golden coverage preserves exact string, int32, int64, and message identifier
  type URLs and bytes. The same printable value remains distinct across kinds.
  Existing string targets keep their previous shard input; every kind retains
  its shard after direct record persistence and under a fresh strategy.
- Caller mutation cannot change the stored target snapshot. Default targets,
  malformed primitive wrapper bytes, and malformed delivery-client targets
  fail explicitly. Remote acknowledgement compares cloned `Any` values by
  type and bytes, restoring exact pending-row acknowledgement without weakening
  the CAS boundary.
- Memory behavior, Datastore custom-family construction and round-trip, and
  MySQL direct-family construction use the unchanged `InboxMessage` record
  family. No provider production, DDL, kind, index, Proto, or dependency change
  was introduced.
- Fresh affected execution passes 13 files / 525 tests. Generated build,
  tooling typecheck, changed-file ESLint, cleanup rules, TSDoc, API docs,
  Prettier, and `git diff --check` pass.
- Fresh LCOV at `/tmp/t0158a-cov-final2/lcov.info`, intersected with executable
  production lines added against `origin/main`, reports statements/lines 42/45
  (93.33%), branches 46/51 (90.20%), and functions 12/12 (100%). Whole-file
  threshold output is not the changed-range task result.
- The retired-assumption scan finds no string-only `targetId` declaration in
  production. Remaining `targetId: string` matches are test input helpers that
  immediately pack readable fixture IDs; the only production `StringValue`
  branch preserves the required legacy string shard assignment.
- Implementation owner profile: existing implementer, explicitly dispatched
  as `gpt-5.6-terra` / medium with no subagents. Runtime self-introspection was
  unavailable; the configured role/profile is the evidence.

Status: mechanically converged and ready for one specialist review wave.

## 2026-08-11 - Specialist Review And Correction

- Documentation/TSDoc review was clean. TypeScript/API, reliability, and style
  reviews identified one cohesive boundary batch: remote snapshots and custom
  strategies received mutable target bytes; valid zero-valued numeric and legal
  empty message IDs were rejected remotely; blank string IDs remained valid in
  direct storage; repository target equality duplicated `InboxTargets.equal()`;
  and public TSDoc did not fully describe the packed typed identity.
- Remote snapshots now deep-copy target `Any` values. Both custom strategy
  wrappers receive a clone, so application code cannot mutate the identity that
  is subsequently persisted. Regression tests mutate both source/snapshot bytes
  and custom-strategy arguments.
- Shared client validation accepts empty bytes for legal message IDs and the
  default-encoded numeric zero, validates primitive wrapper bytes, and rejects
  blank `StringValue` IDs. Direct write/read/shard validation follows the same
  string rule while retaining int32/int64 zero.
- Repository replay uses canonical `InboxTargets.equal()`.
- Fresh correction execution passes 13 files / 531 tests. LCOV at
  `/tmp/t0158a-cov-final-clean/lcov.info` reports changed-range statements/lines
  56/59 (94.92%), branches 46/51 (90.20%), and functions 14/14 (100%).

Status: correction-complete and ready for targeted API, reliability, and style
re-review. Documentation is not reopened because its lane was clean and the
TSDoc correction is deterministic.

## 2026-08-11 - Targeted Re-review Correction

- TypeScript/API re-review was clean and confirmed that the typed identity
  boundary and public TSDoc are correct. Reliability re-review was clean after
  an independent 6-file / 310-test run.
- Style re-review found one P2 test-only expansion of `repositoryAccess` used to
  reach numeric packing branches. The accessor and fabricated metadata test
  were removed. A real default-identity clone validation test keeps the exact
  changed-range branch metric above 90% without widening an internal facade.

Status: targeted style re-review required only for the accessor removal.
