# T-0191 Work Log

## 2026-08-15 - Start

- Accepted existing `implementer` assignment with explicit `gpt-5.6-terra` /
  medium profile. The Desktop surface exposes no independent runtime telemetry;
  configured role/profile is recorded as the available evidence.
- Classified as high-risk because a public optional persistence port crosses
  exact destructive mutation, shard fencing, provider transactions, and
  compatibility.
- Exclusive initial ownership is delivery ports/Inbox/lifecycle, new adjacent
  cleanup seams, memory cleanup, and direct Inbox/fencing/retention tests.
  Existing MySQL and Datastore `record-storage.ts` remain exclusively owned by
  T-0190 pending a recorded handoff.
- Planned TDD order: establish production-provider persistence RED, prove the
  former validate/delete ownership-transfer gap, then add only the minimum
  provider-owned atomic seam.

## 2026-08-15 - Persistence RED and provider handoff boundary

- Frozen dependencies installed successfully. Clean worktree generated Proto
  outputs were required before package exports could resolve; `pnpm
proto:generate && pnpm typecheck:build:generated` completed with the frozen
  47-source / 52-descriptor digest.
- RED: `pnpm exec vitest run packages/server/test/delivery/inbox.test.ts -t
'removes an eligible exact delivered snapshot'` failed exactly because
  `inbox.removeDelivered is not a function`. The test uses real `InboxStorage`,
  `ShardedWorkRegistry`, and one `InMemoryStorageFactory`, acquires a real
  leased session, persists and marks the row delivered, then requires exact
  removal and physical absence.
- Current provider facts: Inbox rows and shard-session rows are separate direct
  RecordStorage families. Independent `read`/`validateOwnership` plus Inbox
  CAS would retain the forbidden validate/delete gap. A correct shared-provider
  operation needs access to both resolved families in one memory critical
  section, Datastore transaction, and MySQL transaction/shared ownership fence.
- Required T-0190 ownership handoff files are
  `packages/storage-rdbms/src/mysql/record-storage.ts` and
  `packages/storage-datastore/src/datastore/record-storage.ts`, plus any
  provider-internal adjacent transaction adapter those owners select. This
  stream will not edit either owned file. It can continue the optional port,
  direct/memory seam, and lifecycle REDs independently; provider completion,
  live MySQL, and Datastore evidence remain blocked on explicit handoff.

## 2026-08-15 - Memory coordinator GREEN

- Added framework-only `DeliveryCleanupStorageFactories`, modeled on the
  existing Entity commit capability. It is an internal storage subpath, not a
  StorageFactory transaction API.
- The in-memory provider registers a handle that obtains the factory-owned
  Inbox and shard-session `TenantRecords` slices. Its operation synchronously
  checks the exact session snapshot, current worker/shard and expiry, then
  performs exact Inbox compare-and-delete before returning a resolved promise.
  This is the shared critical region with registry CAS operations; no
  cleanup-only mutex was introduced.
- GREEN: `pnpm typecheck:build:generated` and the focused real direct Inbox
  removal test passed. Ownership-transfer interleaving and non-memory provider
  coordinators remain pending.

## 2026-08-15 - Reviewed provider ownership handoff

- T-0190 is accepted and pushed at `3081dcc0` after all required specialist
  lanes, canonical deterministic verification, >=90% changed coverage, and
  separate live MySQL/Datastore containment evidence. Its provider writer is
  inactive.
- The orchestrator explicitly transfers the existing MySQL and Datastore
  provider transaction/factory boundary to this stream. The accepted Query
  branch is merged into this published branch without rewriting history before
  provider cleanup work resumes.
- The existing implementer remains explicitly `gpt-5.6-terra` / medium with no
  subagents and unavailable Desktop runtime telemetry. Its bounded ownership is
  the narrow two-record-family atomic cleanup coordinator, adjacent provider
  registration/helpers, provider conformance/live proof, and T-0192 lifecycle;
  every accepted normalized-query behavior must remain unchanged.

## 2026-08-15 - Provider implementation ownership continuation

- The prior memory/lifecycle implementers are inactive and retain no file
  ownership. A replacement existing `implementer` receives the same bounded
  context in this worktree, explicitly `gpt-5.6-terra` / medium, with no
  subagents. Desktop runtime model/token/latency telemetry is unavailable; the
  immutable dispatch profile is the evidence.
- Its ownership is limited to the provider-atomic MySQL/Datastore coordinator
  and registration, the minimum shared-fence helpers now handed off from
  T-0190, T-0191/T-0192 tests and records, and final coverage/mechanical proof.
  It must not broaden `StorageFactory`, add retention configuration, or alter
  accepted normalized-query behavior.

## 2026-08-15 - Mechanical convergence ownership continuation

- The provider implementation owner completed and pushed runtime/coverage
  behavior through `0e517d11` and is inactive. It retains no file ownership.
- A replacement existing `implementer`, explicitly `gpt-5.6-terra` / medium
  with no subagents and unavailable Desktop runtime telemetry, owns only the
  canonical TSDoc findings, evidence-record convergence, and final task-profile
  verification. Provider/runtime behavior is frozen; no API, persistence, or
  lifecycle change is authorized by this transfer.

## 2026-08-15 - Provider coordinator checkpoint

- Existing `implementer` continued under the explicit `gpt-5.6-terra` /
  medium profile with no subagents; Desktop runtime telemetry remains
  unavailable, so the immutable configured profile is the available evidence.
- RED: dedicated MySQL and Datastore coordinator tests failed because their
  provider-owned modules did not exist. GREEN adds only the internal factory
  registrations and exact two-record-family coordinators. MySQL prepares both
  resolved tables and uses one coordinator-owned transactional connection;
  non-transactional tables fail closed because their ordinary ownership CAS is
  not protected by the coordinator's advisory lock. Datastore reads the exact
  session and inbox records and deletes inside one namespace-scoped transaction.
- Both coordinators reject a missing/replaced session snapshot, an expired or
  otherwise non-current session, and a missing/replaced Inbox snapshot before
  deletion. The direct `InboxStorage` eligibility guard remains unchanged:
  only `DELIVERED` rows with absent or `keepUntil <= now` reach a provider.
  No general `StorageFactory` transaction method or retention setting was
  introduced; accepted normalized-query files were not changed.
- Added conditional end-to-end MySQL/Datastore direct-Inbox tests. They use real
  providers when `SPINE_TS_MYSQL_URL` or `DATASTORE_EMULATOR_HOST` is supplied;
  they are not counted as live evidence yet. An explicit exclusive MySQL window
  and then Datastore window was requested from the orchestrator before running
  them.
- Focused deterministic evidence: `pnpm typecheck:build:generated` succeeded;
  focused direct Inbox/worker/provider and provider-storage suites passed 82
  tests, with the two deliberately service-gated live tests skipped; changed
  files pass Prettier and `git diff --check`. Changed coverage, live service
  proof, and canonical task verification remain open.

## 2026-08-15 - Mechanical TSDoc and evidence convergence

- The replacement existing `implementer` continues under explicit
  `gpt-5.6-terra` / medium configuration with no subagents. Runtime telemetry
  remains unavailable, so the immutable configured profile is the available
  metadata evidence.
- `node scripts/check-tsdoc.mjs` cleared all 42 findings across the changed
  delivery cleanup surfaces. Changes are documentation-only: they add current
  summaries and parameter/return contracts, without modifying persistence,
  lifecycle, or public API behavior.
- The six focused paths passed with 40 deterministic tests. The two conditional
  live direct-Inbox provider tests were skipped because `SPINE_TS_MYSQL_URL`
  and `DATASTORE_EMULATOR_HOST` are unset; no live MySQL or Datastore execution
  is claimed. Current-source coverage remains 112/115 lines (97.39%) and 69/75
  branches (92.00%).
- Next: complete the specified cleanup, type, lint, ESLint, and final
  no-coverage task profile, then leave the canonical lanes review-ready.

## 2026-08-15 - Final task-profile limitation

- `pnpm verify:task -- --no-coverage` reached its terminal deterministic gates:
  Node version, frozen Proto generation/checksum, build, tooling typecheck,
  cleanup, and TSDoc all passed. It then failed at `check-copyright.mjs` before
  formatting or focused-test dispatch.
- The scan reports seven T-0191/T-0192-owned header defects in the
  MySQL/Datastore/memory cleanup modules and their associated provider/direct-
  Inbox test files. The reported scope classification was incorrect; repair is
  limited to the exact `copyrightHeader(2026)` template and changes no runtime
  or test behavior.
- The branch is review-ready for the resolved TSDoc batch, with the canonical
  task profile pending the in-scope header correction and fresh rerun.

## 2026-08-15 - Header correction and format convergence

- Corrected all seven T-0191/T-0192-owned provider/test headers to the exact
  `copyrightHeader(2026)` template. Copyright, TSDoc, cleanup, focused format,
  diff, and the six focused paths pass; focused tests report 40 passes and two
  endpoint-gated skips.
- The first rerun then reached the shared repository format gate and exposed
  one behavior-neutral comma formatting mismatch in the changed
  `sharded-work-registry.ts` slice. The source was normalized with Prettier;
  the full format check now passes. A fresh canonical profile remains pending.

## 2026-08-15 - Canonical task-profile completion

- The exact six-path `pnpm verify:task -- --no-coverage` profile completed
  successfully. Node/Proto, build, tooling, cleanup, TSDoc, copyright, log
  containment, full formatting, documentation/API, Buf, generated-output, and
  release-readiness gates passed. Focused Vitest reports 40 passes and two
  expected live-provider skips.
- The header correction is template-only and the shard-registry correction is
  a Prettier comma normalization; no persistence, lifecycle, or public API
  behavior changed. T-0191 is clean and review-ready; live provider evidence
  remains unavailable because both endpoint variables are unset.

## 2026-08-15 - Accepted review-correction RED/GREEN

- Existing `implementer` continues under explicit `gpt-5.6-terra` / medium
  configuration with no subagents; runtime telemetry remains unavailable.
- RED: focused direct Inbox/worker tests proved cancellation still deleted an
  eligible row, cleanup stopped before an eligible row behind one full protected
  page, and a refused deletion could return `DRAINED` after ownership loss.
- GREEN: direct removal rejects aborted/expired preflight and propagates its
  operation into the provider seam; provider coordinators recheck it before
  destructive work. Cleanup examines at most one continuation page after a full
  no-removal page, and revalidates ownership after every refused deletion.
- Focused deterministic evidence: five suites pass 43/43. Live two-handle and
  physical-row-count proof is pending separately requested serialized provider
  windows.

## 2026-08-15 - MySQL exclusive-provider evidence

- With the orchestrator-granted exclusive MySQL 8.4.10 window, `SPINE_TS_MYSQL_URL`
  targeted only `packages/server/test/delivery/inbox-provider-cleanup.test.ts -t
'MySQL Inbox cleanup'`. It passed 2/2 with the Datastore block skipped.
- The new acceptance opens two independent `MysqlStorageFactory` instances,
  proves stale-owner deletion returns false while a direct durable row remains
  readable from the second factory, then proves the replacement owner removes
  that exact row and its direct lookup becomes absent. Each temporary session
  is released; no broad deletion ran.

## 2026-08-15 - Datastore exclusive-provider evidence

- With the separately granted Datastore emulator window,
  `DATASTORE_EMULATOR_HOST=127.0.0.1:8081 DATASTORE_PROJECT_ID=spine-wave12`
  targeted the Datastore cleanup block only. It passed 2/2 with the two MySQL
  cases skipped.
- The acceptance mirrors MySQL: independently opened factories, stale-owner
  refusal, direct durable-row presence through the replacement factory, and
  exact replacement-owner deletion. Neither provider ran concurrently and no
  emulator-wide clear occurred.

## 2026-08-15 - Final native-count provider evidence

- After admission-relative deadline and MySQL rollback corrections, the
  exclusive MySQL 8.4.10 command
  `SPINE_TS_MYSQL_URL=... pnpm exec vitest run
packages/server/test/delivery/inbox-provider-cleanup.test.ts -t 'MySQL Inbox
cleanup'` passed both MySQL cases. Native SQL counts remained `1` after stale
  refusal and `0` after current-owner deletion; the window was released.
- The separate fallback-project command
  `env -u DATASTORE_PROJECT_ID DATASTORE_EMULATOR_HOST=127.0.0.1:8081 pnpm exec
vitest run packages/server/test/delivery/inbox-provider-cleanup.test.ts -t
'Datastore Inbox cleanup'` passed both Datastore cases. Provider-native key
  counts were `1` then `0`, proving the factories and count helper use the same
  fallback project. MySQL remained idle, no emulator-wide clear ran, and the
  Datastore window was released.
