# T-0191 Work Log

## 2026-08-15 - Start

- Accepted existing `implementer` assignment with explicit `gpt-5.6-terra` /
  medium profile. The Desktop surface exposes no independent runtime telemetry;
  configured role/profile is recorded as the available evidence.
- Classified as high-risk because a public optional persistence port crosses
  exact destructive mutation, shard fencing, provider transactions, and
  compatibility.
- Exclusive initial ownership is delivery ports/Inbox/lifecycle, new adjacent
  cleanup seams, memory cleanup, and direct Inbox/fencing/retention tests.
  Existing MySQL and Datastore `record-storage.ts` remain exclusively owned by
  T-0190 pending a recorded handoff.
- Planned TDD order: establish production-provider persistence RED, prove the
  former validate/delete ownership-transfer gap, then add only the minimum
  provider-owned atomic seam.

## 2026-08-15 - Persistence RED and provider handoff boundary

- Frozen dependencies installed successfully. Clean worktree generated Proto
  outputs were required before package exports could resolve; `pnpm
  proto:generate && pnpm typecheck:build:generated` completed with the frozen
  47-source / 52-descriptor digest.
- RED: `pnpm exec vitest run packages/server/test/delivery/inbox.test.ts -t
  'removes an eligible exact delivered snapshot'` failed exactly because
  `inbox.removeDelivered is not a function`. The test uses real `InboxStorage`,
  `ShardedWorkRegistry`, and one `InMemoryStorageFactory`, acquires a real
  leased session, persists and marks the row delivered, then requires exact
  removal and physical absence.
- Current provider facts: Inbox rows and shard-session rows are separate direct
  RecordStorage families. Independent `read`/`validateOwnership` plus Inbox
  CAS would retain the forbidden validate/delete gap. A correct shared-provider
  operation needs access to both resolved families in one memory critical
  section, Datastore transaction, and MySQL transaction/shared ownership fence.
- Required T-0190 ownership handoff files are
  `packages/storage-rdbms/src/mysql/record-storage.ts` and
  `packages/storage-datastore/src/datastore/record-storage.ts`, plus any
  provider-internal adjacent transaction adapter those owners select. This
  stream will not edit either owned file. It can continue the optional port,
  direct/memory seam, and lifecycle REDs independently; provider completion,
  live MySQL, and Datastore evidence remain blocked on explicit handoff.

## 2026-08-15 - Memory coordinator GREEN

- Added framework-only `DeliveryCleanupStorageFactories`, modeled on the
  existing Entity commit capability. It is an internal storage subpath, not a
  StorageFactory transaction API.
- The in-memory provider registers a handle that obtains the factory-owned
  Inbox and shard-session `TenantRecords` slices. Its operation synchronously
  checks the exact session snapshot, current worker/shard and expiry, then
  performs exact Inbox compare-and-delete before returning a resolved promise.
  This is the shared critical region with registry CAS operations; no
  cleanup-only mutex was introduced.
- GREEN: `pnpm typecheck:build:generated` and the focused real direct Inbox
  removal test passed. Ownership-transfer interleaving and non-memory provider
  coordinators remain pending.

## 2026-08-15 - Reviewed provider ownership handoff

- T-0190 is accepted and pushed at `3081dcc0` after all required specialist
  lanes, canonical deterministic verification, >=90% changed coverage, and
  separate live MySQL/Datastore containment evidence. Its provider writer is
  inactive.
- The orchestrator explicitly transfers the existing MySQL and Datastore
  provider transaction/factory boundary to this stream. The accepted Query
  branch is merged into this published branch without rewriting history before
  provider cleanup work resumes.
- The existing implementer remains explicitly `gpt-5.6-terra` / medium with no
  subagents and unavailable Desktop runtime telemetry. Its bounded ownership is
  the narrow two-record-family atomic cleanup coordinator, adjacent provider
  registration/helpers, provider conformance/live proof, and T-0192 lifecycle;
  every accepted normalized-query behavior must remain unchanged.
