# T-0022a Work Log

Status: in progress

Branch: `task/T-0022a-projection-inbox-handoff`

Worktree:
`.worktrees/T-0022a-projection-inbox-handoff`

Started: `2026-07-09T21:21:23Z`

Baseline commit: `7799d88`

## Entries

- `2026-07-09T21:21:23Z`: Started T-0022a after T-0021 reconciliation and
  post-merge format verification. Root `main` was clean except for the
  user-owned untracked `human-review-1-jul.md`.
- `2026-07-09T21:21:23Z`: Requirements-splitting sub-agent
  `019f48bf-2d8c-7fc0-addb-850438166a03` reported no blockers and selected
  live projection subscriber inbox handoff as the first non-blocked event
  delivery slice.
- `2026-07-09T21:21:23Z`: Created branch
  `task/T-0022a-projection-inbox-handoff` and worktree
  `.worktrees/T-0022a-projection-inbox-handoff`. The sandboxed attempt failed
  on Git branch ref locking under `.git/refs`; the escalated retry succeeded
  under the user's standing permission for per-task worktrees.
- `2026-07-09T21:21:23Z`: Inspected current Spine JVM source fetched into
  `/private/tmp/spine-ts-core-jvm-t0022`: `Repository.java`,
  `ProjectionRepository.java`, `ProcessManagerRepository.java`, `Inbox.java`,
  and `InboxOfEvents.java`.
- `2026-07-09T21:21:23Z`: Recorded D-0072 to start with
  `ProjectionRepository.toSubscriber()` parity instead of a generic repository
  event delivery engine.
- `2026-07-09T21:36:58Z`: Skill applicability check selected `implement`
  (`/Users/armiol/.agents/skills/implement/SKILL.md`), `tdd`
  (`/Users/armiol/.agents/skills/tdd/SKILL.md`), and
  `verification-before-completion`
  (`/Users/armiol/.agents/skills/verification-before-completion/SKILL.md`).
  Skipped reviewer sub-agent execution despite the task review plan because the
  current implementation prompt explicitly said not to spawn sub-agents.
- `2026-07-09T21:36:58Z`: Implemented live projection subscriber durable inbox
  handoff. Built contexts now register a local projection inbox target for
  projection repositories, live projection event dispatch writes
  `UPDATE_SUBSCRIBER` `TO_DELIVER` rows with the original `Event` envelope as
  payload, original event ID as `signalId`, projection state type URL plus
  routed projection ID as `inboxId`, `ShardIndex.single()`, and the same
  30-second dedup retention window as process-manager command handoff.
- `2026-07-09T21:36:58Z`: Kept read-side catch-up out of the inbox path by
  adding a framework-only direct projection dispatch hook for
  `BoundedContext.catchUpReadSide()`. Local projection inbox replay now invokes
  only the exact row target before running the existing projection transaction
  and `Stand.update()` behavior.
- `2026-07-09T21:36:58Z`: Updated docs to record the implemented projection
  subscriber handoff and the deferred process-manager event reactor inbox routing, aggregate
  event reactor/importer, projection catch-up, generic delivery engine,
  scheduler/retry, retained-attempt-history, and transport-worker behavior.
- `2026-07-09T21:50:43Z`: Addressed first-round review findings for T-0022a.
  Added red-first mixed-backlog regressions showing broad local shard drains
  replayed unrelated same-label rows in both projection and process-manager
  handoffs. Introduced exact-row local replay with `Delivery.drainMessage()`,
  switched projection and process-manager handoffs to use it, clarified
  retention-window dedup wording, replaced the local `Any` clone helper with
  a generated `AnySchema` binary round-trip, added the projection-subscriber
  metadata alias, and updated review/runtime docs.
- `2026-07-09T21:52:52Z`: Completed first-round fix verification and recorded
  results below. No edits were made to the user-owned `human-review-1-jul.md`.
- `2026-07-09T21:57:27Z`: Applied the tiny T-0022a cleanup requested after
  style review: projection inbox event replay now uses
  `clone(AnySchema, message.signal)`, and the now-unused generated binary
  helpers were removed from the repository imports. No edits were made to the
  user-owned `human-review-1-jul.md`.
- `2026-07-09T22:01:00Z`: Reverted the tiny cleanup after focused verification
  showed plain `clone(AnySchema, message.signal)` preserved Buffer-shaped bytes
  inside a nested stored `Any`. Projection inbox event replay now uses the
  generated `toBinary`/`fromBinary` path directly, without the removed ad-hoc
  clone helper, so stored bytes normalize back to generated-message shapes.
- `2026-07-09T22:09:25Z`: Addressed second re-review findings for T-0022a.
  Added a `Delivery.drainMessage()` trust-boundary guard that rejects
  structural `InboxMessage` snapshots whose `message.id.shard` does not match
  `message.shard` before shard pickup, introduced
  `DeliveryMessageDrainOptions` so exact-message drains accept only `node` and
  `onMessage`, added focused delivery regression/type coverage, and updated
  API/developer docs plus the review log. No edits were made to the
  user-owned `human-review-1-jul.md`.
- `2026-07-09T22:20:00Z`: Fixed the verification ESLint optional-chain finding
  in `Delivery.drainMessage()` by replacing the explicit undefined/status
  disjunction with an equivalent optional-chain status check. No behavior
  change and no edits were made to the user-owned `human-review-1-jul.md`.
- `2026-07-09T22:25:19Z`: Addressed the remaining T-0022a review blockers.
  `Delivery.drainMessage()` now derives exact-row shard identity from
  `index`/`ofTotal`, leases the normalized ID shard instead of trusting a
  caller-supplied `key()`, and rechecks the leased/session and pending-row
  shards before replay. Local projection and process-manager inbox handoffs now
  share a narrow exact-row drain helper that waits briefly only for duplicate
  `TO_DELIVER` rows skipped because another local drain owns the shard. Added
  regressions for a forged shard whose `key()` lies plus concurrent duplicate
  projection and process-manager handoffs. Corrected docs so
  process-manager event reactors are described as direct local `EventBus`
  execution, not deferred, and as not yet routed through durable inbox storage.
  Fixed the stray `- and` README bullet and renamed the overlong semantic
  helper. No edits were made to the user-owned `human-review-1-jul.md`.
- `2026-07-09T22:36:26Z`: Addressed final T-0022a reliability/docs/coverage
  findings. Replaced the duplicate local inbox handoff's arbitrary
  `20 * 5ms` delivered-row polling with a framework-local in-flight drain map
  keyed by inbox message ID and scoped per `Delivery` instance. Duplicate
  callers now await the original exact-row drain promise instead of failing
  while normal async replay is still in progress. Extended projection and
  process-manager duplicate tests so the original replay completes after the
  old polling window, corrected D-0072 and task acceptance wording to say
  process-manager event reactors are direct local `EventBus` execution while
  durable inbox routing for those reactors is deferred, and renamed the stale
  process-manager backlog test. No edits were made to the user-owned
  `human-review-1-jul.md`.
- `2026-07-09T22:45:57Z`: Added focused delivery branch coverage after full
  coverage remained below threshold. Covered exact-message drain malformed-shard
  and marker-failure paths plus public `InboxRecords`/`DedupRecords` corrupted
  record decode guards. The final escalated coverage run passed with branch
  coverage at `90.03%`. No edits were made to the user-owned
  `human-review-1-jul.md`.
- `2026-07-09T23:00:41Z`: Addressed the production duplicate-race blocker.
  The prior in-flight duplicate coordination was scoped per ephemeral
  `Delivery`, while repository projection/process-manager handoff creates a
  fresh `Delivery` per call. Moved coordination to the long-lived local inbox
  instance, keyed by tenant plus inbox target/signal/shard identity, so
  concurrent duplicate projection handoffs join the original write/drain
  promise even across fresh `Delivery` objects. Added a repository-level
  concurrent duplicate projection regression with the real repository event
  dispatcher path and a delayed subscriber held beyond 150ms. Added projection
  context failure coverage proving a fresh-delivery duplicate rejects with the
  original replay failure and leaves the row pending. Also renamed the stale
  duplicate test names, clarified the `DeliveryLoop` API-doc sentence, and
  corrected the earlier work-log wording to say deferred process-manager event
  reactor inbox routing. No edits were made to the user-owned
  `human-review-1-jul.md`.

## Verification

- `pnpm install`: passed after approval; installed workspace dependencies from
  the lockfile with no downloads.
- `pnpm proto:generate`: passed; verified 25 copied Spine proto source
  checksums and regenerated ignored protobuf output.
- `pnpm typecheck:build:generated`: passed; `tsc -b`.
- `./node_modules/.bin/vitest run packages/server/test/repository/repository-routing.test.ts`:
  passed; 120 tests.
- `./node_modules/.bin/vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/context/process-manager-handoff.test.ts`:
  passed; 10 tests.
- `./node_modules/.bin/vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/context/process-manager-handoff.test.ts`
  during first-round fix pass: first red run failed the new mixed-backlog
  regressions because unrelated rows replayed; after `Delivery.drainMessage()`
  and test expectation updates, passed; 12 tests.
- `./node_modules/.bin/vitest run packages/server/test/repository/repository-routing.test.ts`
  during first-round fix pass: first run exposed that direct
  `clone(AnySchema, ...)` preserved a Buffer-shaped nested `Any.value`; after
  switching to generated `toBinary`/`fromBinary` normalization, passed; 120
  tests.
- `pnpm typecheck:build:generated`: passed; `tsc -b`.
- `pnpm format`: passed; formatted changed files before final checks.
- `pnpm format:check`: passed; all matched files use Prettier style.
- `git diff --check`: passed; no whitespace errors.
- `pnpm format:check`: passed after running `pnpm format`.
- `pnpm lint:generated`: passed; `tsc -b`, ESLint, and cleanup enforcement.
- `pnpm docs:check:generated`: passed; TypeDoc emitted the existing warning
  that git remote `origin` is not valid for source links, then API export
  checks passed.
- A mistaken `pnpm test:generated -- packages/server/test/repository/repository-routing.test.ts`
  invocation expanded to the full Vitest suite after typecheck and was stopped.
  Before interruption it showed known native/gRPC/ZeroMQ-style failures outside
  this slice; focused tests above were rerun and passed.
- `./node_modules/.bin/vitest run packages/server/test/repository/repository-routing.test.ts`
  during tiny cleanup verification: failed; 119 of 120 tests passed. The
  failing generated-registry two-argument projection subscriber assertion shows
  that direct `clone(AnySchema, message.signal)` preserves a Buffer-shaped
  nested `Any.value` in `EventContext.origin`, where the expected generated
  message shape carries a `Uint8Array`.
- `git diff --check` during tiny cleanup verification: passed; no whitespace
  errors.
- `./node_modules/.bin/vitest run packages/server/test/repository/repository-routing.test.ts`
  after restoring generated binary normalization: passed; 120 tests.
- `./node_modules/.bin/vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/context/process-manager-handoff.test.ts`
  after restoring generated binary normalization: passed; 12 tests.
- `pnpm typecheck:build:generated` after restoring generated binary
  normalization: passed; `tsc -b`.
- `pnpm format:check` after restoring generated binary normalization: passed.
- `git diff --check` after restoring generated binary normalization: passed.
- `./node_modules/.bin/vitest run packages/server/test/delivery/delivery-worker.test.ts -t "rejects exact-message drains with mismatched message and id shards"`
  during second re-review fix pass: first red run failed because
  `drainMessage()` delivered the row after leasing the forged
  `message.shard`; after the guard and narrow options type, passed.
- `./node_modules/.bin/vitest run packages/server/test/delivery/delivery-worker.test.ts`
  after formatting: passed; 18 tests.
- `./node_modules/.bin/vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/context/process-manager-handoff.test.ts`
  after formatting: passed; 12 tests.
- `./node_modules/.bin/vitest run packages/server/test/repository/repository-routing.test.ts`
  after formatting: passed; 120 tests.
- `pnpm typecheck:build:generated` during second re-review fix pass: passed;
  `tsc -b`.
- `pnpm docs:check:generated` during second re-review fix pass: passed; TypeDoc
  emitted the existing warning that git remote `origin` is not valid for source
  links, then API export checks passed.
- `pnpm format:check` during second re-review fix pass: passed; all matched
  files use Prettier style.
- `git diff --check` during second re-review fix pass: passed; no whitespace
  errors.
- `pnpm --config.verify-deps-before-run=false lint:generated` after the
  optional-chain lint fix: `tsc -b` and ESLint completed, then cleanup
  enforcement failed on the pre-existing semantic-name rule for
  `packages/server/src/delivery/delivery.ts:189 assertMessageShardMatchesId`.
- `pnpm --config.verify-deps-before-run=false exec eslint packages/server/src/delivery/delivery.ts`
  after the optional-chain lint fix: passed; no ESLint findings remained in
  the touched file.
- `git diff --check` after the optional-chain lint fix: passed; no whitespace
  errors.
- `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/delivery/delivery-worker.test.ts packages/server/test/context/projection-handoff.test.ts packages/server/test/context/process-manager-handoff.test.ts`
  during remaining-findings fix pass: passed; 3 test files, 33 tests.
- `pnpm --config.verify-deps-before-run=false lint:generated` initial
  remaining-findings run: failed after `tsc -b` on two ESLint
  `@typescript-eslint/no-unsafe-assignment` findings in the new shard
  normalizer. Replaced `Reflect.get()` assignment with typed structural field
  reads.
- `pnpm --config.verify-deps-before-run=false lint:generated` after the shard
  normalizer cleanup: passed; `tsc -b`, ESLint, and cleanup enforcement passed.
- `pnpm --config.verify-deps-before-run=false docs:check:generated` during
  remaining-findings fix pass: passed; TypeDoc emitted the existing warning
  that git remote `origin` is not valid for source links, then API export
  checks passed.
- `pnpm --config.verify-deps-before-run=false format:check` during
  remaining-findings fix pass: passed; all matched files use Prettier style.
- `git diff --check` during remaining-findings fix pass: passed; no whitespace
  errors.
- `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/context/process-manager-handoff.test.ts`
  during final reliability fix pass: first red run failed the extended
  duplicate tests after the old `20 * 5ms` wait while the original replay was
  still pending; after replacing polling with in-flight drain tracking, passed;
  2 test files, 14 tests.
- `pnpm --config.verify-deps-before-run=false lint:generated` during final
  reliability fix pass: first run failed on two
  `@typescript-eslint/restrict-template-expressions` findings in the new inbox
  message key helper; after explicit numeric string conversion, passed with
  `tsc -b`, ESLint, and cleanup enforcement.
- `pnpm --config.verify-deps-before-run=false test:coverage:generated` in the
  sandbox: failed because native localhost/ZeroMQ IPC tests hit `EPERM`
  (`listen EPERM: operation not permitted 127.0.0.1` and ZeroMQ
  `Operation not permitted`).
- Escalated `pnpm --config.verify-deps-before-run=false test:coverage:generated`
  before extra coverage tests: all 58 files and 1104 tests passed, but branch
  coverage failed at `89.55%`.
- Escalated `pnpm --config.verify-deps-before-run=false test:coverage:generated`
  after exact-message delivery tests: all 58 files and 1108 tests passed, but
  branch coverage still failed at `89.61%`.
- Escalated `pnpm --config.verify-deps-before-run=false test:coverage:generated`
  after delivery record decode coverage: passed; 58 files, 1110 tests,
  statements `94.97%`, branches `90.03%`, functions `98.24%`, lines `94.97%`.
- `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts packages/server/test/delivery/delivery-worker.test.ts`
  after all final fixes: passed; 4 test files, 159 tests.
- `pnpm --config.verify-deps-before-run=false lint:generated` after all final
  fixes: passed; `tsc -b`, ESLint, and cleanup enforcement passed.
- `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  during the production duplicate-race fix pass: red run failed as expected.
  The fresh-delivery context duplicate rejected with skipped-delivery instead
  of the original replay failure, and the repository duplicate handoff rejected
  with skipped-delivery while the original replay was still running.
- `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  after moving coordination to the local inbox instance: passed; 2 test files,
  126 tests.
- `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/projection-handoff.test.ts packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts packages/server/test/delivery/delivery-worker.test.ts`
  after formatting: passed; 4 test files, 161 tests.
- `pnpm --config.verify-deps-before-run=false lint:generated` during the
  production duplicate-race fix pass: first run failed cleanup enforcement on
  one 121-character test-name line and one five-component type name; after
  shortening both, rerun passed with `tsc -b`, ESLint, and cleanup enforcement.
- `pnpm --config.verify-deps-before-run=false format:check`: first run failed
  on `packages/server/src/context/local-inbox-handoff.ts`; after
  `pnpm --config.verify-deps-before-run=false format`, rerun passed.
- `git diff --check`: passed; no whitespace errors.
- `pnpm --config.verify-deps-before-run=false test:coverage:generated` in the
  sandbox: failed on known native IPC restrictions (`listen EPERM: operation
not permitted 127.0.0.1` and ZeroMQ `Operation not permitted`), with 52 test
  files passing and 6 transport/server files failing.
- Escalated `pnpm --config.verify-deps-before-run=false test:coverage:generated`:
  passed; 58 files, 1112 tests, statements `94.96%`, branches `90.03%`,
  functions `98.24%`, lines `94.97%`.
