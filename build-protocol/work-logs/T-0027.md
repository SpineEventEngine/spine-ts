# T-0027 Work Log

Status: implemented; coordinator verified; review pending

Branch: `task/T-0027-post-t0026-status-reconciliation`

Worktree: `.worktrees/T-0027-post-t0026-status-reconciliation`

Started: `2026-07-11T04:42:44Z`

Baseline commit: `efbf379a`

## Entries

- `2026-07-11T04:42:44Z`: Started T-0027 from root `main` after T-0026 was
  merged and post-merge verified. Root `main` only had the user-owned untracked
  `human-review-1-jul.md`.
- `2026-07-11T04:42:44Z`: Requirements splitter
  `019f4f79-71d3-7a92-8ac3-7e8555bf5980` recommended
  `T-0027: Post-T-0026 Runtime Status Reconciliation` as the next smallest
  non-blocked slice. The splitter found that active docs still contain broad or
  stale remaining-gap wording after T-0026 landed a narrow framework-owned
  delivery worker/loop boundary.
- `2026-07-11T04:42:44Z`: Created branch
  `task/T-0027-post-t0026-status-reconciliation` and worktree
  `.worktrees/T-0027-post-t0026-status-reconciliation`. The sandboxed Git
  attempt failed on ref locking; the escalated retry succeeded.
- `2026-07-11T04:42:44Z`: Performed the canonical skill applicability check.
  Selected workflow skills already active in this session are
  `subagent-driven-development`, `using-git-worktrees`,
  `requesting-code-review`, and `verification-before-completion`. Additional
  selected skills for this docs/status task are `doc-coauthoring` and
  `architecture-decision-records`; both were read before creating the T-0027
  docs. The repo expected-skill manifest, user-installed skill entrypoints, and
  targeted installed-skill lock entries were checked.
- `2026-07-11T04:42:44Z`: Created the T-0027 task brief, work log, review log,
  and decision D-0078 before public-doc edits. T-0027 is docs/status-only and
  must not change runtime source, tests, generated output, or
  `human-review-1-jul.md`.
- `2026-07-11T04:45:00Z`: Implementation worker started in the T-0027 worktree.
  Read the task brief, work log, review log, active delivery sections in
  `build-protocol/RUNTIME_ARCHITECTURE.md` and
  `build-protocol/DEVELOPER_API.md`, and the delivery/runtime-gap sections in
  `docs/architecture/README.md`, `docs/USER_GUIDE.md`, and
  `packages/server/README.md`. The first stale-phrase scan found overbroad
  wording around production worker execution and supported catch-up work in the
  active public docs, while the build-protocol delivery API wording already
  matched the T-0026 contract.
- `2026-07-11T04:49:00Z`: Updated `docs/architecture/README.md`,
  `docs/USER_GUIDE.md`, and `packages/server/README.md` only. The docs now
  state that durable inbox rows, shard leases, per-message claims, bounded
  validated local replay, and the framework-owned local delivery worker/loop
  boundary for supported labels exist after T-0026. Remaining gaps stay explicit:
  retry monitors/workers, retained attempt history, transport topology,
  broker/process supervision, production delivery policy, durable catch-up
  storage/projection catch-up through inbox storage, production storage adapters,
  and broader runtime hardening. The edits preserve `CATCH_UP` as valid durable
  label data that remains pending/skipped and preserve `IMPORT_EVENT` as
  unsupported for new writes with legacy stored rows failing closed.
- `2026-07-11T04:51:00Z`: Verification notes: the first
  `pnpm --config.verify-deps-before-run=false docs:check` failed before
  validation because this fresh worktree lacked `node_modules` and `buf`.
  Offline install against the repo-local pnpm store then failed because
  `@bufbuild/protoc-gen-es` was missing from the store. Ran
  `pnpm install --store-dir /Users/armiol/development/experiments/spine-ts/.pnpm-store`
  with approval so verification dependencies could be downloaded.
- `2026-07-11T04:52:00Z`: Verification completed. Targeted stale-worker and
  import-roadmap `rg` checks exited `1` with no matches; the `CATCH_UP`/
  `IMPORT_EVENT` confirmation `rg` exited `0` with expected matches.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  exited `0` as a prerequisite for TypeDoc package declarations. Retried
  `pnpm --config.verify-deps-before-run=false docs:check` exited `0` with
  TypeDoc reporting `0` errors and one invalid-origin source-link warning.
  `pnpm --config.verify-deps-before-run=false format:check` exited `0`.
  `git diff --check` exited `0`. `git status --short` showed no tracked
  generated-output changes after `docs:check`.
- `2026-07-11T04:53:48Z`: Coordinator verification repeated the stale-worker
  and import-roadmap `rg` checks, both exiting `1` with no matches; repeated the
  `CATCH_UP`/`IMPORT_EVENT` confirmation `rg`, which exited `0` with expected
  matches; and reran `docs:check`, `format:check`, and `git diff --check`, all
  passing. `git status --short` showed no tracked generated-output or
  dependency churn.
