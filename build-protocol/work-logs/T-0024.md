# T-0024 Work Log

Status: complete

Branch: `task/T-0024-roadmap-spec-reconciliation`

Worktree:
`.worktrees/T-0024-roadmap-spec-reconciliation`

Started: `2026-07-10T02:33:16Z`

Baseline commit: `a964cc9a`

## Entries

- `2026-07-10T02:33:16Z`: Started T-0024 docs/spec reconciliation after
  upstream Spine JVM ADR 0001 confirmed event import is dropped and aggregate
  `@Apply` is retained only to fail unsupported models.
- `2026-07-10T02:33:16Z`: Created the T-0024 task brief before substantive
  active-doc edits, with objective, scope, ADR/JVM inspection, acceptance
  criteria, verification plan, and review plan.
- `2026-07-10T02:33:16Z`: Fetched upstream Spine JVM ADR 0001 from the raw
  GitHub URL after sandbox DNS failed. Confirmed D1 drops event import,
  deprecates `IMPORT_EVENT` only for wire compatibility, and D2 keeps
  aggregate `@Apply` only to detect and fail unsupported models.
- `2026-07-10T02:33:16Z`: Updated active architecture, developer API, user
  guide, API README, package README, and decision log wording so aggregate
  import/importer work is removed from the active plan rather than deferred.
- `2026-07-10T02:33:16Z`: Checked that changes remain docs/spec/log only. No
  source, test, generated API reference, runtime `IMPORT_EVENT`, or runtime
  `CATCH_UP` files were edited.
- `2026-07-10T02:43:59Z`: Fixed review findings in
  `build-protocol/TECHNICAL_SPEC.md`: removed active aggregate
  storage wording that treated events as replay input and replaced event worker
  importer wording with ADR 0001-aligned subscribers/reactors plus supported
  inbox delivery language. Left `IMPORT_EVENT` label cleanup as a later
  compatibility contract task, not an active worker/importer feature.
- `2026-07-10T02:48:51Z`: Fixed round-two review findings in
  `build-protocol/DECISION_LOG.md`: added local supersession notes to D-0047
  for the old aggregate "snapshots plus events" roadmap wording and to D-0072
  for the aggregate import/importer part of the
  `reactors/importers` deferral. Preserved still-real deferred work for
  supported reactor delivery, projection catch-up, schedulers, retries,
  transport workers, and retained attempt history.

## Verification Evidence

- Scoped stale-deferral scan over active docs/specs, T-0022b records, and the
  decision log found no matches for stale aggregate import/importer deferral
  wording.
- Positive evidence scan found the replacement wording in active docs:
  aggregate importers are removed from the active plan, aggregate `@React`
  handlers are ordinary generated reactor handlers, and `IMPORT_EVENT` cleanup
  is a later compatibility task.
- `pnpm --config.verify-deps-before-run=false format:check`: failed only
  because dependencies are missing in this isolated worktree. The command
  reported `Failed to start Prettier: spawnSync prettier ENOENT` and
  `Local package.json exists, but node_modules missing`.
- `git diff --check`: passed.
- Added-line line-length scan found no new line over 120 characters. Existing
  long lines in older decision-log content were left untouched.
- Review-fix stale active wording scan over the requested active docs/spec/task
  and log paths found no matches.
- Review-fix `git diff --check`: passed.
- Review-fix `pnpm --config.verify-deps-before-run=false format:check`: failed
  only because dependencies are missing in this isolated worktree. The command
  reported `Failed to start Prettier: spawnSync prettier ENOENT` and
  `Local package.json exists, but node_modules missing`.
- Round-two stale historical wording scan over the requested paths reported
  only historical phrases with explicit supersession/removed wording:
  `build-protocol/DECISION_LOG.md:248` keeps the old D-0047 "snapshots plus
  events" roadmap item, superseded locally in D-0047 consequences;
  `build-protocol/DECISION_LOG.md:2386` keeps the old D-0072
  `reactors/importers` deferral, superseded locally in D-0072 consequences;
  `build-protocol/DECISION_LOG.md:2409` is the new explicit supersession note;
  `build-protocol/work-logs/T-0024.md:40`,
  `build-protocol/work-logs/T-0024.md:42`, and
  `build-protocol/work-logs/T-0024.md:74` record this round-two fix/evidence.
- Round-two `git diff --check`: passed.
- Round-two `pnpm --config.verify-deps-before-run=false format:check`: failed
  only because dependencies are missing in this isolated worktree. The command
  reported `Failed to start Prettier: spawnSync prettier ENOENT` and
  `Local package.json exists, but node_modules missing`.
