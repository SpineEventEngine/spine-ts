# T-0145 Work Log

## Start

- Base: completed Wave 8 commit `1abb4217` on
  `task/T-0145-wave8-guide-correction`.
- Trigger: the active framework USER_GUIDE still presents removed delivery
  observer/batch APIs even though the runtime, server README, and server
  REFERENCE expose the new `DeliveryMonitor` contract.
- Classification: standard documentation correction; no production or public
  declaration change is authorized.

## Model And Role Record

- Sole documentation writer: existing `implementer`, explicitly dispatched as
  `gpt-5.6-terra` / `medium`; it owns active guide/reference edits and focused
  documentation regression tests and must not spawn subagents.
- Mechanical scans and deterministic checks remain orchestrator functions.
- Documentation review uses the existing immutable
  `documentation_reviewer`, `gpt-5.6-luna` / `medium`; TypeScript/API review
  uses the existing `typescript_api_docs_reviewer`, explicitly
  `gpt-5.6-terra` / `high`.
- A fresh-context reader test is required by the documentation co-authoring
  workflow. Runtime metadata will be recorded when exposed; otherwise the
  configured role/profile and metadata limitation are recorded.

## Implementation Checkpoint (2026-08-08)

- Writer assignment accepted as the existing `implementer`, explicitly
  configured `gpt-5.6-terra` / `medium`; this execution surface exposes no
  additional runtime-profile metadata. No child work was dispatched.
- Inventory covered all active Markdown under `docs/`, package `README.md` and
  `REFERENCE.md` files, and example guides. Exact retired-delivery scan covered
  `onPage(`, `withBatchSize(`, `onStarted(`, `onSkipped(`, and `onCompleted(`.
  The only active findings were the `docs/USER_GUIDE.md` local-delivery example
  and its following old page-summary lifecycle explanation. The same scan is
  now empty outside its checker regression fixture.
- Kept current negative-contract prose in API, architecture, server, storage,
  delivery-client, and auth documentation: these say that the framework does
  _not_ persist attempts, quarantine, receipts, markers, claims, or
  fingerprints. They are not stale positive API claims.
- Corrected `docs/USER_GUIDE.md` to use `DeliveryMonitor`,
  `FailedReception`, `pageSize`, terminal statuses, and immutable delivery
  statistics. The guide now explains, in beginner language, ungrouped and
  grouped source/record naming for MySQL tables and Datastore kinds, factory
  layout mappings, `RecordColumn`/`RecordQuery`, and generated Entity
  `(column)` values for filtering and sorting.
- Added deterministic coverage in `scripts/check-release-readiness.test.mjs`
  and matching reader-facing Markdown patterns in
  `scripts/check-release-readiness.mjs`. Reintroducing any of the five retired
  delivery method names now fails release-readiness documentation validation.
- Focused evidence passed:
  - `pnpm vitest run scripts/check-release-readiness.test.mjs` — 1 file,
    19 tests passed.
  - `pnpm prettier --check docs/USER_GUIDE.md scripts/check-release-readiness.mjs scripts/check-release-readiness.test.mjs` — passed.
  - `node scripts/check-release-readiness.mjs` — passed (80 package imports,
    51 package assets, 389 relative Markdown links).
  - `pnpm docs:audience:check` — passed.
  - `git diff --check` — passed.
- Additional focused documentation gates passed:
  - `pnpm docs:api:check` — passed and confirmed expected public export counts;
    TypeDoc emitted one pre-existing zero-error `@inheritDoc` warning in the
    Datastore factory.
  - `pnpm format:check` — passed.
  - `pnpm vitest run scripts/check-typescript-snippets.test.mjs --reporter=dot`
    — 1 file, 5 tests passed.

## Follow-up Correction Batch (2026-08-08)

- Parent inspection confirmed that the original checker only matched a retired
  name when followed by `(`. It would have missed the exact old reader prose
  ``onStarted` follows`` and ``onCompleted` with``. The checker now rejects
  retired names anywhere in reader-facing Markdown: `onPage`, `withBatchSize`,
  `onStarted`, `onSkipped`, `onFailure`, `onCompleted`, `DeliveryPage`, and
  `DeliveryInboxWork`. The focused fixture uses ordinary prose and inline code,
  not calls, for every name.
- Replaced the guide's anonymous monitor subclass with the small named
  `MarkFailedReceptionDelivered` example, preserving the existing default
  `markDelivered()` behavior in beginner-readable form.
- Added source-backed Message Board guidance: the real
  `spine.examples.messageboard.BoardMessageView` Proto declares `board`,
  `author`, and `posted_at` columns; its ungrouped default family is
  `spine_examples_messageboard_BoardMessageView` in MySQL and
  `spine.examples.messageboard.BoardMessageView` in Datastore. The guide now
  distinguishes materialized marked values from ordinary payload-only
  `username` and `text`, and describes the actual QueryService/current
  `EntityRecord` validation and provider query path without asserting the
  example uses MySQL.
- Classified `docs/firestore-storage-extension-analysis.md` as a historical
  analysis, not an active reader guide: its title and analysis/remediation-plan
  content preserve earlier Datastore facts as evidence. It remains outside the
  active-guide correction set rather than being silently rewritten.
- `PROJECT_COMPLETION_PLAN.md` is an active completion-sequence record. Its
  Wave-8 overview no longer says to upgrade the retired external
  `validation-ts` package; it now describes the completed validation-package
  migration. Its `PAUSED` statements are inside archived pre-Wave-8 execution
  packets, so a nearby scope note now makes their historical status and lack of
  current public-contract force explicit.
- Follow-up focused evidence passed:
  - `pnpm vitest run scripts/check-release-readiness.test.mjs` — 1 file,
    19 tests passed, including prose/backtick retired-name coverage.
  - `node scripts/check-release-readiness.mjs` — passed (80 package imports,
    51 assets, 389 relative Markdown links); active `docs/`, package, and
    example Markdown has no positive retired-name scan hit.
  - `pnpm docs:audience:check`, focused Prettier, and `git diff --check` —
    passed.
  - `pnpm vitest run scripts/check-typescript-snippets.test.mjs --reporter=dot`
    — 1 file, 5 tests passed.
  - `pnpm docs:api:check` — passed with the existing zero-error TypeDoc
    `@inheritDoc` warning in Datastore storage-factory documentation.

## Specialist Review Dispatch (2026-08-08)

- Documentation review: existing `documentation_reviewer`, immutable
  `gpt-5.6-luna` / `medium`; concern is beginner readability, guide
  completeness, examples, links, and preservation of each README's established
  look and feel.
- TypeScript/API documentation review: existing
  `typescript_api_docs_reviewer`, explicitly `gpt-5.6-terra` / `high`; concern
  is accuracy of the `DeliveryMonitor`, storage naming, column, and query
  contracts.
- Style/maintainability review: existing `style_maintainability_reviewer`,
  explicitly `gpt-5.6-terra` / `high`; concern is the release-readiness
  checker's scope, false-positive risk, fixtures, and maintainability.
- Performance/reliability review: existing `performance_reliability_reviewer`,
  explicitly `gpt-5.6-terra` / `high`; concern is whether provider pushdown,
  bounded Datastore reconciliation, delivery guarantees, and limits are stated
  truthfully.
- Fresh-context reader test: read-only documentation function, explicitly
  `gpt-5.6-luna` / `medium`; it receives only the changed beginner guide and a
  fixed comprehension questionnaire. This is separate from specialist review.
- Security review is N/A: documentation and deterministic reader-doc checks do
  not change a trust boundary, authentication, authorization, secret handling,
  or runtime behavior.
- Every dispatched agent is instructed not to spawn subagents. Runtime metadata
  will be recorded when exposed; otherwise the immutable configured profile and
  metadata limitation are the evidence.

## Specialist Review Wave (2026-08-08)

- Documentation review, configured immutable `documentation_reviewer`,
  `gpt-5.6-luna` / `medium` (runtime metadata unavailable): one P1. An adjacent
  guide paragraph still says ordinary failed callbacks may remain pending and
  that no monitor handles them, contradicting the default `markDelivered()`
  policy. README look and feel is unaffected and all other active-guide claims
  were clean.
- TypeScript/API documentation review, explicitly
  `typescript_api_docs_reviewer`, `gpt-5.6-terra` / `high` (runtime metadata
  unavailable): one P1. The Datastore three-argument source/record mapping also
  matches default Entity history when its group name equals the state source;
  it cannot provide the documented current-only rename. MySQL's record-only
  and grouped mapping semantics differ and must be explained separately.
- Style/maintainability review, explicitly
  `style_maintainability_reviewer`, `gpt-5.6-terra` / `high` (runtime metadata
  unavailable): one P2. The reader-doc scanner rejects truthful historical or
  migration prose containing a retired identifier, so its active-guidance
  boundary needs an explicit tested scope.
- Performance/reliability review, explicitly
  `performance_reliability_reviewer`, `gpt-5.6-terra` / `high` (runtime
  metadata unavailable): CLEAN. Delivery guarantees, page limits, provider
  pushdown, bounded Datastore reconciliation, and lost-acknowledgement behavior
  match runtime code.
- Fresh-context reader test ran as an explicitly configured read-only
  documentation function with `gpt-5.6-terra` / `medium`, because the dispatch
  surface does not expose `gpt-5.6-luna` for the default function role; runtime
  metadata was unavailable. The reader correctly explained naming, grouping,
  columns, Query translation, default failure handling, and at-least-once
  delivery. It found beginner clarity gaps: no compact identity diagram,
  unclear framework columns, no direct Message Board link, undefined “legal”
  pushdown, and no named distributed example.

## Aggregated Correction Batch

- Rewrite the stale failed-callback paragraph to distinguish default
  `markDelivered()`, the one immediate `repeatDispatching()` action, and
  redelivery after acknowledgement/CAS loss.
- Correct Datastore customization semantics and the later current-only example;
  contrast them with MySQL's separate record-only and grouped mappings.
- Keep strict retired-name detection for active reader guidance while allowing
  explicitly scoped historical/migration documents; add boundary tests.
- Add a small beginner storage-identity diagram or equivalent formula, explain
  generated framework columns, define legal provider plans, link the Message
  Board query source directly, and name/link the distributed Message Board
  example where the guide discusses multi-machine delivery.
- Preserve README structure and prose style; no README rewrite is requested.
- Return the complete batch to the same explicitly configured existing
  `implementer`, `gpt-5.6-terra` / `medium`; it must not spawn subagents.

## Aggregated Correction Implementation (2026-08-09)

- Replaced the stale failed-callback paragraph in `docs/USER_GUIDE.md` with the
  actual `DeliveryMonitor` behavior: default `markDelivered()`, one immediate
  `repeatDispatching()` action when selected, pending target behavior when both
  acknowledgement paths fail, and lost-acknowledgement/CAS redelivery that
  requires idempotent callback effects.
- Rechecked factory resolution before writing the Datastore correction.
  `organizeRecords(source, record, layout)` keys default Entity current storage
  and the same-source default history together because history group names are
  the source name. The guide no longer promises a current-only kind rename;
  record-only fallback, source/record matching, and the coherent
  `useEntityStorage(...)` boundary are now explicit. MySQL's separate
  ungrouped and grouped `setTableName(...)` registrations are contrasted.
- Added the requested beginner aids without changing README structure: a compact
  storage identity formula, the Proto `(column)` to framework `EntityRecord` /
  `RecordColumn` distinction, a definition of a legal provider plan, a direct
  Message Board Query-source link, and the named Distributed Message Board
  multi-machine example link.
- Scoped retired-name enforcement to active reader guidance. A document starts
  with `<!-- release-readiness: historical-or-migration -->` only when it is
  historical or migration evidence; it continues to receive execution-history
  checks but is exempt from retired-name patterns. The Firestore extension
  analysis is explicitly marked. The deterministic fixture proves an active
  README still fails for `onPage` while the marker permits a migration record
  to name `onPage` and `DeliveryPage` truthfully.
- Focused evidence:
  - `pnpm vitest run scripts/check-release-readiness.test.mjs` — 1 file,
    20 tests passed.
  - `node scripts/check-release-readiness.mjs` — passed (80 package imports,
    51 package assets, 391 relative Markdown links).
  - `pnpm docs:audience:check`, focused Prettier, and `git diff --check` —
    passed.
  - `pnpm vitest run scripts/check-typescript-snippets.test.mjs --reporter=dot`
    — 1 file, 5 tests passed.
  - `pnpm docs:api:check` — passed with the existing zero-error TypeDoc
    `@inheritDoc` warning in Datastore storage-factory documentation.

## Targeted Re-review Dispatch (2026-08-09)

- Documentation re-review: existing immutable `documentation_reviewer`,
  `gpt-5.6-luna` / `medium`; verify the failure-policy correction and beginner
  aids, including the named example links and README look and feel.
- TypeScript/API documentation re-review: existing
  `typescript_api_docs_reviewer`, explicitly `gpt-5.6-terra` / `high`; verify
  the corrected MySQL/Datastore mapping semantics and delivery wording.
- Style/maintainability re-review: existing
  `style_maintainability_reviewer`, explicitly `gpt-5.6-terra` / `high`; verify
  the historical/migration marker boundary and its regression test.
- Performance/reliability remains closed from the clean full review: the
  correction clarifies already-verified behavior and changes no runtime,
  provider limit, or execution policy. Security remains N/A for the previously
  recorded reason.
- Each re-review is read-only and must not spawn subagents. Runtime metadata is
  recorded if exposed; otherwise the configured profile is the evidence.

## Targeted Re-review Results And Final Correction Batch (2026-08-09)

- Documentation re-review, immutable `documentation_reviewer`,
  `gpt-5.6-luna` / `medium` (runtime metadata unavailable): CLEAN. The prior
  delivery contradiction, beginner aids, direct links, mapping explanation,
  and README preservation all passed.
- TypeScript/API documentation re-review, explicitly
  `typescript_api_docs_reviewer`, `gpt-5.6-terra` / `high` (runtime metadata
  unavailable): one P1. MySQL's two-argument `setTableName(...)` resolves an
  ungrouped family's source type, not necessarily its stored record type. For
  Entity current storage this is the state schema; the guide and public builder
  TSDoc must say so and contrast current/state-history registrations.
- Style/maintainability re-review, explicitly
  `style_maintainability_reviewer`, `gpt-5.6-terra` / `high` (runtime metadata
  unavailable): two P1 findings. A file-local marker can self-authorize an
  active guide to bypass stale checks, and the marked Firestore analysis has no
  visible warning that it is historical and not an implementation guide.
- Final correction batch for the same existing `implementer`, explicitly
  `gpt-5.6-terra` / `medium`: replace self-authorizing marker behavior with a
  small reviewed path allowlist; prove a marked README remains strict; add a
  visible historical/superseded notice and current User Guide link; correct the
  MySQL guide example and both public builder TSDoc declarations to say source
  type, including Entity current/history examples. No runtime behavior change
  is authorized. Re-review only TypeScript/API docs and style afterward.
