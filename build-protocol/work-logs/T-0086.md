# T-0086 Work Log

Task: `build-protocol/tasks/T-0086-message-board/TASK.md`

## 2026-07-31 — Framing

- Created the isolated task branch/worktree from verified `main` at
  `b0ad6681`.
- Classified the work as high-risk because it changes serialized Proto
  contracts and observable validation/ordering behavior.
- Recorded the complete human requirements ledger and selected skills.
- Next: inspect current Shadcn guidance and the existing Chat model/runtime,
  then dispatch the required requirements splitter.

## 2026-07-31 — Accepted plan

- Dispatched the existing `requirements_splitter` role with explicit
  `gpt-5.6-sol` / high reasoning. Runtime self-metadata was unavailable; the
  immutable configured role/profile is the accepted metadata evidence.
- The splitter confirmed the existing end-to-end path from Proto
  `(if_missing).error_msg`, through packed server `ValidationError` details,
  to the web client's error outcome. It also traced `ResponseFormat.order_by`
  and the full example rename surface.
- The splitter continued exploring after the bounded answer was established.
  The orchestrator interrupted it after two stop requests and multiple bounded
  waits, then produced the dependency plan from its confirmed trace. This is a
  process limitation, not a product blocker.
- Accepted `build-protocol/planning/T-0086_MESSAGE_BOARD_PLAN.md`.
- Next: dispatch one implementation owner for the complete test-driven slice.

## 2026-07-31 — Implementation: RED setup

- Added the first focused browser behavior test at
  `examples/chat/web/test/relative-time.test.ts`. It specifies the required
  Message Board relative-age boundaries: under one minute is `just now`, then
  singular/plural minutes, hours, and days from an injected clock.
- RED command: `pnpm exec vitest run examples/chat/web/test/relative-time.test.ts`.
  It could not load Vitest because this isolated worktree has no installed
  lockfile-pinned dependencies: `ERR_PNPM_VERIFY_DEPS_BEFORE_RUN: Cannot check
whether dependencies are outdated`. This is an environment setup failure,
  not yet the expected missing-module assertion.
- Next: install the existing lockfile-pinned workspace dependencies, rerun the
  same test to capture the behavioral RED result, then add the smallest helper.

## 2026-07-31 — Implementation: relative age GREEN

- The initial dependency install required approved network access because the
  isolated worktree cache lacked browser-test packages. `pnpm install
--frozen-lockfile` then completed using the unchanged lockfile; it reported
  pre-existing `spine-proto` bin-link warnings because generated proto-tools
  output has not been built in this worktree.
- Behavioral RED evidence: `pnpm exec vitest run
examples/chat/web/test/relative-time.test.ts` failed with `Cannot find module
'../src/relative-time.js'`, the intended missing-feature failure.
- Added `examples/chat/web/src/relative-time.ts` using
  `Intl.RelativeTimeFormat` and injected `now`; sub-minute ages are `just now`
  and later ages round down by minute, hour, then day.
- GREEN evidence: the same command passed, `1 passed` test file and `1 passed`
  test. The Jenv permission-warning prefix is environment noise and did not
  affect execution.
- Next: add the contract/domain RED tests for Message Board username propagation
  and chronological Projection ordering before moving or changing production
  sources.

## 2026-07-31 — Implementation continuation

- The first implementation dispatch used the existing `implementer` role with
  explicit `gpt-5.6-terra` / medium reasoning. Runtime self-metadata was not
  exposed; the immutable configured role/profile is the accepted metadata
  evidence.
- The owner returned after only the first TDD slice even though the dispatch
  assigned the complete task. The relative-time RED/GREEN result is retained,
  but task completion was rejected and the full remaining batch was returned
  to the same context.
- The continuation must also group the formatter in a documented semantic
  object, because production standalone functions are a last resort under the
  repository's code-quality rules.

## 2026-07-31 — Message Board implementation and cheap preflight

- Renamed the complete `examples/chat` family, workspace packages, Proto
  package, type URLs, generated imports, tooling paths, and current public
  documentation to Message Board vocabulary. The single model package now owns
  `BoardId`, `MessageId`, `UserId`, commands, events, rejections, Aggregate
  state, and Projection state.
- Added Proto-required `username` and `text` fields with the respective
  `Enter a username.` and `Enter a message.` messages. Both values propagate
  through the command, Aggregate, event, Projection, query result, and UI.
- Replaced the browser page with owned Shadcn-style Button, Card, Input, Label,
  Textarea, Alert, Avatar, Tailwind theme, and responsive layout. The page
  queries oldest-first, applies a stable timestamp/ID tie-break, places new
  messages last, and formats injected-clock relative time.
- Added server validation detail decoding in `PostFeedback`. Real-browser
  diagnosis found that `client-web` previously invoked Proto validation while
  packing the command and therefore prevented invalid values from reaching the
  server. The client command path now packs with `validate: false`; focused
  regression coverage proves the server remains the validation authority.
- Corrected two startup/acceptance defects exposed by the live stack: the local
  bearer string now matches between app and browser, and the Envoy harness
  forwards to a private native Spine endpoint rather than the public browser
  gateway. The Playwright command now builds once before launching both
  already-built processes.
- Deterministic checks pass: changed-file formatting, `git diff --check`,
  cleanup rules, TSDoc, documentation audience/snippets, Proto quality,
  generated freshness, TypeScript build, and ESLint.
- Focused tests pass: 13 files / 146 model, app, web, startup, and tooling tests;
  2 files / 75 client validation-boundary tests; the local bearer regression;
  one Vitest interoperability seam; and 6 Node lifecycle/Envoy tests.
- Real Playwright acceptance passes in Chromium, Firefox, and WebKit. It proves
  the server-authored field messages, username/message post, relative time, and
  newest-last placement through the live application. The final form also
  proves error focus and `aria-invalid` state, and the HTML document declares a
  doctype, English language, viewport, description, and title.
- Live desktop and iPhone screenshots were inspected. Both layouts retain clear
  hierarchy, readable controls, a visible textarea, an empty state, and a
  stable action area without clipping or horizontal overflow.
- The production Vite bundle builds in 149 ms: 22.73 kB CSS / 4.90 kB gzip and
  616.11 kB JavaScript / 179.13 kB gzip. Vite's 500 kB uncompressed warning is
  recorded for performance review; speculative route splitting is not added to
  this single-page example.
- Next: checkpoint and push the verified implementation, then run the complete
  specialist review wave.

## 2026-07-31 — Review correction preflight

- The checkpoint implementation was committed as `cb6c2184` and immediately
  pushed to `origin/task/T-0086-message-board` before review.
- Wave 1 returned two P1 and four deduplicated P2 findings. The existing
  implementation context completed the raw-value, refresh, documentation,
  registry, dead-file, and exact baseline-mapper corrections but did not finish
  the required component split. After the context ended, the orchestrator
  retained sole writer ownership and completed that bounded correction rather
  than redispatching discovery.
- The browser page now delegates query/subscription synchronization,
  deterministic row ordering, message rendering, and posting to focused
  modules. Shadcn wrappers remain thin React components without framework-user
  startup boilerplate.
- Added regressions for raw whitespace preservation and authoritative refresh
  after success without a subscription hint.
- Focused evidence passes: web 25/25, Proto-tools 87/87, and cleanup tooling
  108/108. Generated TypeScript and tooling typechecks, ESLint, cleanup and
  TSDoc enforcement, docs/API, Buf lint, generated cleanliness, formatting,
  and `git diff --check` pass.
- Real Playwright acceptance passes in Chromium, Firefox, and WebKit after the
  correction, proving the full server/gateway/UI behavior remains intact.
- Next: run targeted wave 2 review for the three substantively affected
  concerns, then run the single selected release profile.

## 2026-07-31 — Review convergence

- Targeted style re-review is clean. Targeted API re-review closed the raw
  value P1 and produced one accepted registry-summary P2; a claimed count
  mismatch was rejected because 25/25 covers the component and relative-time
  web files together.
- Targeted reliability re-review found one remaining P1 interleaving. A RED
  regression proved an earlier rejected hint refresh could discard a queued
  post-success refresh. The coalescing loop now keeps that later request; the
  regression is GREEN and final targeted reliability review is clean.
- Registry generation now describes the application type registry as composed
  from declared model packages. The complete Proto-tools suite passes 87/87.
- The complete correction preflight passes: web 26/26, cleanup tooling 108/108,
  generated TypeScript and tooling typechecks, ESLint, cleanup and TSDoc
  enforcement, docs/API, Buf lint, generated cleanliness, formatting, and
  `git diff --check`.
- All four canonical concerns are converged and security remains a concrete
  N/A. Next: run the single selected `verify:release` profile.

## 2026-07-31 — Release-gate correction

- The first `verify:release` attempt completed 3,260 tests and exposed two
  stale black-box expectations. Both expected invalid client commands to reject
  locally, while T-0086 intentionally sends them to the server and resolves a
  structured `COMMAND_VALIDATION_ERROR` outcome.
- Systematic diagnosis traced both failures to the new `client-web` packing
  boundary and its focused contract test. The returned server validation
  details were correct; no runtime defect or concurrency failure was present.
- Updated the to-do and Message Board tests to assert the Spine error type,
  unpacked invalid field, and their existing no-handler/no-state-change
  guarantees. This deterministic test correction does not reopen review.
- The complete cheap preflight was rerun after correction and is clean: five
  affected files / 152 tests, cleanup tooling 108/108, generated TypeScript and
  tooling typechecks, ESLint, cleanup and TSDoc enforcement, docs/API, Buf
  lint, generated cleanliness, formatting, and `git diff --check`.
- Next: rerun `verify:release` against the corrected converged tree.

## 2026-07-31 — Coverage correction

- The corrected release run passed all 3,262 tests but reported 89.94% global
  branch coverage, seven covered branches below the required 90% threshold.
  The newly split Message Board modules exposed branches that the former single
  component had not reported separately.
- Added focused `PostFeedback` behavior coverage for known and unknown template
  placeholders, unrelated fields, missing/wrong packed details, wrong error
  types, and violations without useful messages. No exclusions or threshold
  changes were introduced.
- The focused module gate passes 5/5 with 100% statements, branches (20/20),
  functions, and lines.
- The complete cheap preflight was rerun again and is clean: six affected files
  / 157 tests, cleanup tooling 108/108, both TypeScript builds, ESLint, cleanup
  and TSDoc enforcement, docs/API, Buf lint, generated cleanliness, formatting,
  and `git diff --check`.
- Next: run the final `verify:release` attempt.

## 2026-07-31 — Final verification

- `pnpm --config.verify-deps-before-run=false verify:release` passes on the
  converged task tree.
- Verification evidence: 168 test files passed and 3 skipped; 3,265 tests
  passed and 25 skipped. Coverage is 94.17% statements (18,412/19,551), 90.00%
  branches (10,274/11,415), 94.63% functions (4,426/4,677), and 95.01% lines
  (17,383/18,295).
- Node/version, Proto generation and checksums, example Proto quality, frozen
  descriptor provenance, generated and tooling typechecks, repository ESLint,
  cleanup and TSDoc enforcement, formatting, TypeDoc/API, Buf lint, generated
  cleanliness, and release readiness all pass. Release readiness verifies 67
  package imports, 44 package assets, and 273 relative Markdown links.
- The task tree is reviewed, converged, and ready to commit, push, and integrate.

## 2026-07-31 — Integration and post-merge verification

- Committed review corrections as `5c67ebfd` and pushed the feature branch.
  Local and remote task refs matched before integration.
- Merged into `main` without conflicts as `18090c2f` and pushed the merge
  immediately. The protected untracked human-review files remained untouched.
- The merge tree and fully verified task tree are byte-identical at tree hash
  `a7858616`; `main` did not move during task implementation, so the protocol
  requires focused post-merge checks rather than a duplicate full coverage run.
- Refreshed the primary checkout's workspace links from the frozen lockfile
  without downloads or tracked changes. Post-merge Proto generation,
  TypeScript build, and real Playwright acceptance pass in Chromium, Firefox,
  and WebKit.
- The containing closure commit records completion and is pushed to
  `origin/main` immediately after creation.
