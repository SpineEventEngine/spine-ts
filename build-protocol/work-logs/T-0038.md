# T-0038 Work Log

Status: Framework children planned; T-0038a pending

## Start

- `2026-07-14T00:59:38Z`: created branch/worktree from verified `main` at
  `75340852`. Root contains only the untouched user-owned untracked file.
- T-0037f is integrated and post-merge verified. T-0038 is the next completion
  packet and owns audit evidence/classification only.
- No Sol High split is required: no subsystem, public/serialized contract,
  domain semantic, or concurrency rule changes in this parent.

## Skill Gate

- Session skill inventory, repo expected-skill manifest, full readable
  user-entrypoint enumeration, and targeted installed-skill lock evidence were
  checked before task work.
- Selected/read: `subagent-driven-development`, `using-git-worktrees`,
  `requesting-code-review`, `verification-before-completion`.
- Project durable files supersede `planning-with-files`; ADR, advanced types,
  Node patterns, TDD, and Spine JVM inspection are N/A unless a code child is
  created. Interactive doc coauthoring is not applicable to autonomous audit.
- `2026-07-14` implementer recheck: session inventory, expected manifest,
  complete user entrypoint enumeration (`find /Users/armiol/.agents/skills
-maxdepth 2 -type f -name SKILL.md -print`), and readable lock evidence were
  inspected; all eight expected entries were present. Fully read and selected:
  `verification-before-completion`, for evidence-backed closure claims.
  `subagent-driven-development` and `requesting-code-review` are inapplicable
  because the assignment forbids subagents; `using-git-worktrees` is
  inapplicable because this supplied worktree must not be recreated. Planning,
  ADR, runtime, testing, and JVM skills are N/A for this audit-only parent.

## Author Dispatch

- Existing implementer role; explicit expected `gpt-5.6-terra` / medium, no
  subagents. Ownership is the new release matrix plus T-0038 records only.
- Production, tests, public docs, examples, generated output, other task history,
  commits, pushes, merge, and the user-owned root file are excluded.
- Handback must classify every active requirement, provide concrete evidence,
  route every gap, and report focused validation and runtime profile.

## Historical Pre-review Path

This section records the pre-review handback and is superseded by the accepted
independent review findings and current parent review-fix handback below.

- Matrix completed with 29 classified requirement groups: 23 IMPLEMENTED,
  four DOCUMENTED_EXCLUSION, one EXAMPLE_GAP routed to T-0040, and one
  SECURITY_GATE routed to T-0041. No STALE_DOC_STATUS or mandatory framework
  defect was established, so no T-0038 child was made. T-0039 remains a planned
  completion pass rather than a routed finding.
- The main uncertainty is intentionally unexecuted release-wide verification
  and coverage; this task uses focused audit checks only. The required to-do
  child-process/local-IPC evidence is a confirmed example gap, not an inferred
  framework defect.
- Run focused audit checks and pre-review docs/status lint, then obtain the
  required independent review dispositions. This implementer assignment cannot
  dispatch reviewers because the human instruction explicitly forbids
  subagents.

## Focused Verification

- `pnpm --config.verify-deps-before-run=false proto:verify` passed: 25 copied
  Spine proto source checksums verified.
- `pnpm --config.verify-deps-before-run=false exec vitest run
scripts/verify-proto-sources.test.mjs scripts/check-generated-clean.test.mjs
scripts/check-cleanup-rules.test.mjs --passWithNoTests` completed with exit
  status 0. An earlier `node --test` invocation was invalid for these Vitest
  suites and failed with missing Vitest runner configuration; it is recorded as
  a harness error, not test evidence.
- Historical setup note: the author's first `node scripts/check-api-docs.mjs`
  attempt ran before ignored Proto generation and workspace declaration build,
  so it reported 222 missing-module/type errors. The coordinator then completed
  that setup; `docs:check:generated` passed with 100/28/205/19/17/3 expected
  package exports. The earlier result is superseded environment history.
- Coordinator-focused capability verification passed 5 files / 64 tests across
  proto, core, testing, the to-do example, and local IPC. Cleanup enforcement
  also passed.

## Historical Pre-Review Lint And Handback State

- At that earlier pre-review handback, status headers were synchronized as
  `Pre-review corrections complete; independent review pending`. This is
  historical evidence, not the current status.
- Focused docs/status lint found no unsupported current mismatch claim; the
  remaining `pending` wording accurately describes independent review only.
- Focused scans found no forbidden end-user API token in
  `examples/todo/src/index.ts` and no internal delivery-loop/direct-drain
  export token in `packages/server/src/index.ts`.
- `pnpm --config.verify-deps-before-run=false exec prettier --write` followed
  by `--check` passed for all four owned files, and `git diff --check` passed.
- Changed paths are limited to the owned release matrix, task record, work log,
  and review record. No production, test, public-doc, example, generated, Git,
  or user-owned-root file was modified.
- Runtime profile: coordinator immutable-role metadata confirms actual
  `gpt-5.6-terra` / medium matching explicit dispatch; no subagent was used.

## Coordinator Verification And Correction

- `2026-07-14T01:10:13Z`: spawn runtime metadata immutably pins the existing
  implementer role to actual Terra Medium, matching explicit dispatch; author
  closed, no subagents.
- Coordinator generated ignored Proto output and built workspace declarations.
  `docs:check:generated` then passed with 100/28/205/19/17/3 expected package
  exports; the focused proto/core/testing/example/local-IPC suite passed 5
  files / 64 tests. Cleanup enforcement passed. Earlier unresolved-package
  failures are environment setup evidence, not current blockers.
- Pre-review lint rejects row 23's `STALE_DOC_STATUS`: the row itself says no
  specific false current-behavior claim was established. Reclassify it
  truthfully, retain T-0039 only as planned documentation closure, remove the
  resolved precondition wording, and synchronize counts and routed gaps.
- Resume the same Terra Medium implementer for this records-only correction;
  no production, tests, public docs, examples, or generated output may be
  tracked.

## Final Pre-review Classification Finding

- `2026-07-14T01:13:45Z`: strict table lint finds only 22 exact `IMPLEMENTED`
  cells. Row 29 appends explanatory text to the classification cell, violating
  the matrix's exact-token contract. Move that text to evidence and normalize
  the cell; do not change the 23/4/1/1 semantic count.
- Generated-clean passes through the repository pnpm script. Resume the same
  Terra Medium context for this one-row records correction.

## Independent Review Assignment

- `2026-07-14T01:15:54Z`: accepted/closed actual Terra Medium implementer from
  explicit dispatch plus immutable role metadata, no subagents. Exact owned
  four-path diff only.
- Coordinator verification is green: strict 29/23/4/1/1/0 classification lint;
  5 files / 64 capability tests; 100/28/205/19/17/3 API exports; cleanup,
  generated-clean, format, status, and diff checks.
- Assign documentation Luna Medium and style/API/reliability Terra High, all
  explicit, read-only, bounded to their existing concern, and no subagents.
  Security is deferred to T-0041; full verify remains reserved for clean final
  task acceptance.

## Independent Review Result

- `2026-07-14T01:23:48Z`: docs actual Luna Medium; style/API/reliability actual
  Terra High. All explicit dispatches match immutable role metadata; no
  subagents; all four reviewers closed.
- Parent fix batch: add a route column so classification cells are exact;
  separate release tooling from pending final proof; root governing paths;
  narrow the child-process evidence sentence; mark old status text historical;
  and add a complete package export-subpath inventory with public/internal and
  TypeDoc/import/declaration evidence or explicit exemption.
- T-0038a framework defect: invalid fallback type-URL prefixes are unchecked.
  Minimal child owns validation and regression coverage.
- T-0038b framework defect: low-level local IPC and callback binding exist, but
  framework-owned multi-process command/event/delivery execution is not
  integrated with server/environment lifecycle. Minimal architecture-planned
  child owns the missing seam and real cross-process proof.
- Parent matrix correction may proceed in this worktree. Runtime children use
  separate branches/worktrees and integrate into `main` before the parent audit
  is refreshed and closed.

## Parallel Assignments

- Same Terra Medium implementer resumes parent-only matrix/record fixes; exact
  four-file ownership, no subagents.
- Existing requirements splitter runs at explicit Sol High, no subagents, and
  owns only `framework-children-plan.md`. It must inspect Spine JVM server
  evidence for T-0038b and keep both children minimal and separately executable.
- No overlapping files or production writers. Coordinator retains child branch,
  commit, review, merge, push, and parent back-merge operations.

## Child-Plan Coordinator Finding

- `2026-07-14T01:41:13Z`: accept/close parent Terra Medium author; Sol High
  planner one-file handback requires correction before acceptance.
- Use Terra Medium for both implementation children. Integrate each child
  through `main` in order, then back-merge `main` into the parent audit; never
  branch a child from the unmerged parent and thereby merge audit records early.
- Limit the type-URL defect to public custom fallback selection/derivation.
  Packing and implicit registry use only default/file-option derivation and
  remain compatibility checks. Resume only the same Sol High planner against
  its sole file.

## Child-Plan Acceptance

- `2026-07-14T01:44:38Z`: accept/close corrected actual Sol High planner;
  explicit immutable-role dispatch matched, no subagents. Plan passes scoped
  Prettier and new-file whitespace check.
- The plan now uses Terra Medium implementers, serial child merges through
  `main`, and accurate direct custom-fallback semantics while preserving all
  accepted T-0038b JVM/lifecycle boundaries.
- Same Terra Medium parent author owns the final matrix wording correction and
  synchronized handback only.

## Historical Implementer Correction Result

This correction result predates and is superseded by the independent review
findings at `2026-07-14T01:23:48Z`.

- Row 23 now classifies truthful current documentation separation as
  IMPLEMENTED. T-0039 is preserved only as the planned completion pass and is
  removed from routed findings.
- Resolved generated/build setup language is replaced by the coordinator's
  green TypeDoc/API export, 5-file/64-test capability, and cleanup evidence.
- Counts and all four status headers are synchronized for independent review
  handback.
- Correction validation: Prettier check passed for all four owned files and
  `git diff --check` passed. The changed-path scan lists only those four files.
- Final classification correction: row 29's closure qualifier moved into its
  evidence cell and its classification is exactly `IMPLEMENTED`; no other row
  classification or evidence changed. Strict counts remain 23/4/1/1.

## Parent Review-Fix Handback

- Added a separate Route column. Strict taxonomy now covers 31 rows with 23
  IMPLEMENTED, four DOCUMENTED_EXCLUSION, one EXAMPLE_GAP, one SECURITY_GATE,
  two FRAMEWORK_DEFECT, and no unexpected classification token.
- Added T-0038a for invalid fallback type-URL handling while retaining valid
  type-URL behavior separately. Added T-0038b for missing framework-owned local
  multi-process command/event/delivery execution while retaining low-level
  transport/ZeroMQ behavior separately. T-0040 now depends on T-0038b.
- Narrowed row 29 to implemented release tooling. Full T-0038 verify/coverage
  and T-0042 release proof are recorded only as non-classified pending closure
  evidence.
- Corrected row 21 to acknowledge `examples/todo/src/index.ts`; the missing
  evidence is the child-process harness/test and framework-level cross-process
  proof.
- Inventoried all ten package-manifest export entries with public/internal
  designation and TypeDoc/import/declaration evidence or explicit exemption.
  The matrix records the actual limited scope of `scripts/check-api-docs.mjs`
  rather than claiming exact source allowlists it does not enforce.
- Current handback is parent-corrections-complete with selective child planning,
  implementation, integration, matrix rerun, and re-review still pending.

## Parent Review-Fix Validation

- Strict taxonomy/route lint passed: 31 rows; 23 IMPLEMENTED, four
  DOCUMENTED_EXCLUSION, zero STALE_DOC_STATUS, one EXAMPLE_GAP, one
  SECURITY_GATE, two FRAMEWORK_DEFECT, zero unexpected tokens, and zero route
  errors.
- Manifest-to-matrix export inventory lint passed: ten package `exports`
  entries and ten inventory rows, with no missing entry.
- Prettier check passed for all four owned files and `git diff --check` passed.
  `git ls-files` and generated-path `git diff --name-only` returned no tracked
  generated path or generated-path diff.
- All four current status headers agree. Tracked changes remain the four owned
  records. `framework-children-plan.md` appears as an untracked concurrent file
  from the separately authorized planning assignment; this implementer did not
  read, edit, stage, or otherwise alter it.
- Full verify was not run. Remaining uncertainty is the behavior owned by
  T-0038a/T-0038b and the later parent rerun/re-review after their integration.

## Child-Plan Precision Handback

- Row 30 and its routed finding now limit T-0038a to malformed
  caller-supplied fallback input accepted by public `getTypeUrlPrefix()` /
  `deriveTypeUrl()`.
- Packing and implicit registry APIs expose no custom fallback input. Their
  file-option/default behavior remains compatibility evidence, not defect
  propagation.
- The 31-row taxonomy, ten-entry export inventory, T-0038b route, and dependent
  T-0040 route are unchanged. Framework-child implementation begins with
  T-0038a.
