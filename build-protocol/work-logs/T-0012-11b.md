# Work Log: T-0012.11b Projection Event Updates

Task log: `build-protocol/tasks/T-0012-11b-projection-event-updates/TASK.md`
Branch: `task/T-0012-11b-projection-event-updates`
Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0012-11b-projection-event-updates`
Baseline commit: `f38fcac`

## Entries

- `2026-07-05 00:23 WEST`: Created task branch/worktree from parent
  T-0012.11 commit `f38fcac`.
- `2026-07-05 00:23 WEST`: Created the subtask task brief, implementation
  report, review log, and work log before behavior changes.
- `2026-07-05 00:33 WEST`: Added focused RED tests for projection subscriber
  execution, `Stand` updates, and SubscriptionService delivery from real event
  handling. The focused command failed as expected before implementation:
  subscriber calls stayed at `0`, Stand updates were absent, and the
  SubscriptionService handler timed out waiting for the projection update.
- `2026-07-05 00:33 WEST`: Implemented context-bound projection event
  execution through repository dispatch. Built projection repositories now load
  read-side state through context-owned `Stand`, invoke matching event
  subscribers, and write changed projection state plus event version metadata
  through `Stand`. Focused GREEN passed for the new tests, the full repository
  routing test file passed with 25 tests, and `pnpm typecheck` passed.
- `2026-07-05 00:45 WEST`: Added focused branch-coverage tests for unchanged
  projection subscribers, stored-state rehydration, route-valid events with no
  projection subscriber, imported tenant metadata from stored aggregate events,
  missing event version metadata, and missing multitenant command tenant
  context. Full verification passed: focused tests, `pnpm typecheck`,
  `pnpm lint`, `pnpm format:check`, `pnpm docs:check`, `git diff --check`, and
  escalated `pnpm test:coverage` (45 files, 573 tests; statements 94.97%,
  branches 90.14%, functions 97.48%, lines 94.99%). Sandboxed coverage failed
  only on known local IPC/HTTP2 endpoint permissions.
- `2026-07-05 00:56 WEST`: Started round-1 review-fix pass for tenant
  propagation, projection version typing, diagnostics, and stale docs/logs.
  Focused RED verification failed as expected: command-tenant aggregate events
  did not update tenant-scoped projections, the shared missing-method
  diagnostic still said aggregate execution, and the projection version
  `@ts-expect-error` was unused.
- `2026-07-05 00:57 WEST`: Implemented round-1 review fixes. Aggregate-produced
  events now retain command context as origin when no origin is already present,
  projection dispatch reads command-origin tenants for multitenant `Stand`
  access, handler-backed projection repositories require `number` versions, and
  the shared missing-method diagnostic uses neutral repository entity wording.
  Focused GREEN passed with 2 files and 3 selected tests; `pnpm typecheck`
  passed.
- `2026-07-05 01:02 WEST`: Completed round-1 review-fix verification. Focused
  selected tests, `pnpm typecheck`, `pnpm lint`, `pnpm format:check`,
  `pnpm docs:check`, and `git diff --check` passed. Sandboxed
  `pnpm test:coverage` failed only on known local IPC/HTTP2 endpoint
  permissions (`Operation not permitted` for ZeroMQ IPC and
  `listen EPERM 127.0.0.1` for service tests). Escalated `pnpm test:coverage`
  passed with 45 files and 575 tests; coverage summary: statements 95%,
  branches 90.09%, functions 97.48%, lines 95.02%.
- `2026-07-05 01:15 WEST`: Implemented round-2 review fixes. Focused RED
  reproduced the missing failure channel for post-commit already-stored event
  redispatch. Focused GREEN passed after `BoundedContext` recorded copy-safe
  `storedEventDispatchFailures()` diagnostics while preserving aggregate
  command completion after storage/snapshot handling. Added focused coverage
  for projection subscriber failures reached through stored aggregate event
  redispatch, public docs for the diagnostic surface, and parent
  task/report/review status synchronization.
- `2026-07-05 01:20 WEST`: Completed round-2 review-fix verification. Focused
  tests, `pnpm typecheck`, `pnpm lint`, `pnpm format:check`,
  `pnpm docs:check`, and `git diff --check` passed. Sandboxed
  `pnpm test:coverage` failed only on known local IPC/HTTP2 endpoint
  permissions (`Operation not permitted` for ZeroMQ IPC and
  `listen EPERM 127.0.0.1` for service tests), with 555 tests passing before
  the permission failures. Escalated `pnpm test:coverage` passed with 45 files
  and 576 tests; coverage summary: statements 95.01%, branches 90.09%,
  functions 97.49%, lines 95.03%.
- `2026-07-05 01:45 WEST`: Implemented round-3 review fixes. Aggregate-produced
  events now bind origin to the executing command even when handler output
  already carries `importContext` or `pastMessage` tenant metadata. Stored-event
  redispatch diagnostics now use a bounded buffer, cloned events, and frozen
  `DispatchErrorSnapshot` values. Internal callbacks were renamed to
  `recordDispatchFailure`, and public/API docs were corrected.
- `2026-07-05 01:45 WEST`: Completed round-3 verification. Focused repository
  routing tests passed with 1 file and 37 tests. `pnpm typecheck`,
  `pnpm lint`, `pnpm format:check`, `pnpm docs:check`, and
  `git diff --check` passed. Sandboxed `pnpm test:coverage` failed only on
  local IPC/listen permissions, with 43 files and 557 tests passing before or
  around those failures. Escalated `pnpm test:coverage` passed with 45 files
  and 579 tests; coverage summary: statements 94.97%, branches 90.04%,
  functions 97.5%, lines 94.99%.
- `2026-07-05 01:56 WEST`: Implemented round-4 review fixes. Aggregate-produced
  events now bind command context as origin even when a multitenant command has
  no id, so projection updates route under the command tenant instead of a
  handler-provided import or past-message tenant. Added a tenant-a/no-id versus
  tenant-b-origin regression, shortened dispatch wait helper names, reflowed
  old report command lines, and corrected the README route/execution wording.
- `2026-07-05 01:56 WEST`: Round-4 focused repository verification passed for
  command-tenant routing and stored-event dispatch diagnostics: 1 file and 9
  selected tests. Final verification then passed: full repository routing
  tests, `pnpm typecheck`, `pnpm lint`, `pnpm format:check`,
  `pnpm docs:check`, and `git diff --check`.
- `2026-07-05 02:03 WEST`: Sandboxed `pnpm test:coverage` still failed only on
  local endpoint permissions: ZeroMQ IPC reported `Operation not permitted`,
  and gRPC tests reported `listen EPERM 127.0.0.1`. Escalated coverage passed
  with 45 files and 580 tests; statements 94.97%, branches 90.04%, functions
  97.5%, lines 94.99%.
- `2026-07-05 02:15 WEST`: Implemented round-5 review fixes. Aggregate command
  execution now rejects missing `command.id` before produced events are bound,
  applied, or stored, avoiding the invalid `pastMessage` origin shape without
  required `Origin.message`. The no-id tenant regression now expects rejection
  and verifies no tenant-a or tenant-b projection write; command-with-ID tenant
  overwrite tests remain in place. Newly introduced repository-routing helper
  names were shortened.
- `2026-07-05 02:15 WEST`: Focused repository verification for round 5 passed:
  the repository routing file covered the no-id rejection and tenant-origin
  tests with 1 file and 4 selected tests.
- `2026-07-05 02:20 WEST`: Completed round-5 verification. Focused repository
  tests passed with 1 file and 4 selected tests, and the full repository
  routing file passed with 1 file and 38 tests. `pnpm typecheck`,
  `pnpm lint`, `pnpm format:check`, `pnpm docs:check`, and
  `git diff --check` passed. Sandboxed `pnpm test:coverage` still failed only
  on local endpoint permissions: ZeroMQ IPC reported `Operation not permitted`,
  and gRPC service tests reported `listen EPERM 127.0.0.1`. Escalated coverage
  passed with 45 files and 580 tests; statements 94.97%, branches 90.04%,
  functions 97.5%, lines 95%.
- `2026-07-05 02:32 WEST`: Started round-6 cleanup after final documentation
  and API review. The patch keeps the past-message test fixture contract-valid
  by adding the required `Origin.message`, and documents the public
  `command.id` requirement for aggregate command execution in API docs,
  user-facing docs, server README, and Repository TypeDoc.
- `2026-07-05 02:36 WEST`: Round-6 focused verification passed: selected
  repository routing tests passed with 1 file and 14 selected tests.
  `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm docs:check`, and
  `git diff --check` also passed.
- `2026-07-05 02:37 WEST`: Final style cleanup rewrapped the API README
  sentence so `default repositories` stays together.

## Current State

- Projection event updates plus round-1 through round-5 review fixes are
  implemented.
- Round 4's ID-less command-origin binding was superseded by the round-5
  contract-safe rejection of aggregate command execution without `command.id`.
- Round-5 focused verification passed for no-id rejection and tenant-origin
  coverage. Final verification passed. Sandboxed coverage remains blocked only
  by local endpoint permissions; escalated coverage passed.
- Round-6 cleanup is implemented and locally verified. Final re-review remains
  required before merging the subtask into the parent worktree.
