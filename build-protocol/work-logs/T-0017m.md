# Work Log: T-0017m Production Parity Docs And Example Positioning

Status: complete

Task:
`build-protocol/tasks/T-0017m-production-parity-docs/TASK.md`

Review log:
`build-protocol/reviews/T-0017m-production-parity-docs.md`

Branch: `task/T-0017m-production-parity-docs`

Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0017m-production-parity-docs`

## Activity

- `2026-07-09`: Created the task branch/worktree from `main` at `dc0c69f`
  after T-0017l post-merge verification was recorded.
- `2026-07-09`: Inspected the T-0017 roadmap and current public docs before
  creating this task brief. Initial stale-doc risk: the root README still names
  durable subscription recovery as future work even though T-0017i landed.
- `2026-07-09`: Created the durable task, review, and work-log files before
  implementation edits.
- `2026-07-09`: Implementation sub-agent updated public positioning docs only.
  The pass removed stale durable subscription recovery future-work wording,
  clarified that completed T-0017a-l runtime slices are now present, kept
  production gaps explicit, and positioned `examples/todo` as a real local
  Connect/Node gRPC-compatible app backed by process-local in-memory storage.
- `2026-07-09`: Updated package-level docs where status/deferred wording was
  stale after T-0017a-l. The server package now describes durable inbox/local
  shard drain and runtime transport binding as present while leaving
  transport-backed workers, broker/process supervision, production storage
  adapters, and deployment hardening as open production gaps. The transport
  package now says delivery/inbox live in `@spine-ts/server`, while remote
  transport, broker supervision, retained retry history, and production health
  policy remain outside the package.
- `2026-07-09`: No runtime behavior or public API implementation was changed.
  `human-review-1-jul.md` was not touched.
- `2026-07-09`: Implementation sub-agent
  `019f46c5-59ec-7b62-b804-e3639e5eb2cf` completed the docs-only pass and was
  closed by the coordinator.
- `2026-07-09`: First review round completed and all reviewers were closed.
  Documentation, security, and performance/reliability reported clean.
  Style/maintainability requested accurate durable agent/review evidence.
  TypeScript/API docs requested adding `@spine-ts/testing` to the documented
  `docs:check` export verification list.
- `2026-07-09`: Applied first-round fixes: review/work logs now name actual
  implementation and reviewer agents, and `docs/api/README.md` includes
  `@spine-ts/testing` in the `docs:check` export verification list.
- `2026-07-09`: Focused style and TypeScript/API docs re-reviews reported
  clean and were closed.

## Decisions

- Treat this as a docs and positioning slice only. Runtime implementation stays
  unchanged unless verification exposes a broken reference.
- Avoid broad "production-ready" language. The docs should say what is verified
  locally and what remains production hardening.
- Preserve the user's end-code constraints in public docs: application handlers
  return domain messages, use bare decorators, and do not own framework event
  envelopes, transactions, `@Apply`, or handler materialization.

## Files Changed

- `README.md`.
- `docs/USER_GUIDE.md`.
- `docs/api/README.md`.
- `docs/architecture/README.md`.
- `examples/todo/README.md`.
- `examples/todo/USER_GUIDE.md`.
- `packages/server/README.md`.
- `packages/transport/README.md`.
- `build-protocol/tasks/T-0017m-production-parity-docs/TASK.md`.
- `build-protocol/reviews/T-0017m-production-parity-docs.md`.
- `build-protocol/work-logs/T-0017.md`.
- `build-protocol/work-logs/T-0017m.md`.

## Verification

- `pnpm --config.verify-deps-before-run=false format:check` initially failed
  before dependency installation because `prettier` was missing from the fresh
  worktree (`spawnSync prettier ENOENT`).
- `pnpm install` first failed under restricted network with registry
  `ENOTFOUND` / `fetch failed`; rerun with network approval passed and
  installed workspace dependencies from the lockfile.
- `pnpm --config.verify-deps-before-run=false format:check` passed:
  `All matched files use Prettier code style!`.
- `pnpm --config.verify-deps-before-run=false docs:check` initially failed
  before build artifacts existed because TypeDoc could not resolve workspace
  package declarations.
- `pnpm --config.verify-deps-before-run=false typecheck:build` passed after
  `proto:generate` verified 25 copied Spine proto source checksums and `tsc -b`
  completed.
- `pnpm --config.verify-deps-before-run=false docs:check` passed after the
  build. TypeDoc emitted the known invalid `origin` warning only and
  `check-api-docs.mjs` confirmed expected exports for proto, core, server,
  storage, transport, and testing.
- `pnpm --config.verify-deps-before-run=false vitest run
  examples/todo/src/index.test.ts --passWithNoTests` first failed in the
  sandbox with `listen EPERM: operation not permitted 127.0.0.1`; rerun with
  loopback approval passed: 1 test file, 20 tests.
- Stale-language scan passed for the requested stale phrases. Remaining
  `complete` matches are ordinary task/business verbs such as completing a
  to-do task or activation streams completing without updates, not broad
  framework completion claims.
- `git diff --check` passed.
- After updating the durable task/review/work logs, reran
  `pnpm --config.verify-deps-before-run=false format:check` and
  `git diff --check`; both passed. The stale-language scan remained clean for
  the requested stale phrases, with only ordinary `complete` business verbs
  remaining.
- After first-round fixes, `pnpm --config.verify-deps-before-run=false
typecheck:build` passed.
- After first-round fixes, `pnpm --config.verify-deps-before-run=false
docs:check` passed with only the known TypeDoc invalid-origin warning; API
  export checks passed and confirmed `@spine-ts/testing` exports.
- Final verification before commit passed:
  `pnpm --config.verify-deps-before-run=false format:check`,
  `git diff --check`, stale-language scan for stale production/readiness
  phrases, native
  `pnpm --config.verify-deps-before-run=false vitest run
examples/todo/src/index.test.ts --passWithNoTests` (1 file / 20 tests),
  `pnpm --config.verify-deps-before-run=false typecheck:build`, and
  `pnpm --config.verify-deps-before-run=false docs:check` with only the known
  TypeDoc invalid-origin warning.

## Participants

- Implementation sub-agent `019f46c5-59ec-7b62-b804-e3639e5eb2cf` completed
  and was closed.
- First review round agents closed: style
  `019f46d3-2ef4-7c41-9b53-41bc67f46f64`, documentation
  `019f46d3-2fac-7413-8a71-78f3eeb84bd4`, TypeScript/API docs
  `019f46d3-3018-7ae0-b0db-23b66c259354`, security
  `019f46d3-30ca-7ae0-a616-58d67a14a141`, and performance/reliability
  `019f46d3-3169-77b3-8744-ff3270ff9d16`.
- Focused re-review agents closed: style
  `019f46d6-8279-78d1-a6e4-c7b267a83040` and TypeScript/API docs
  `019f46d6-8357-7230-81a6-ff2afc3a4493`.
