# T-0139 Work Log

## 2026-08-08 Start

- Created `task/T-0139-inbox-shards` from pushed T-0138 commit `713658d6` in
  `.worktrees/T-0139-inbox-shards`.
- Offline frozen-lockfile installation completed with all 386 packages reused.
- Classified high-risk because serialized delivery records, shard concurrency,
  deduplication, provider layout, and remote removal behavior change together.
- Architecture assignment uses the existing `requirements_splitter`, expected
  and explicitly dispatched as `gpt-5.6-sol` / `high`. Runtime metadata will be
  recorded when exposed; otherwise the immutable configured profile and the
  limitation are accepted absent a visible mismatch or fallback.

## 2026-08-08 Architecture Complete

- The assignment completed with the immutable configured
  `requirements_splitter` profile `gpt-5.6-sol` / `high`. Runtime
  self-introspection was unavailable, with no visible mismatch or fallback.
- Frozen direct generated `InboxMessage`, `ShardSessionRecord`, and `TenantId`
  record specifications and provider identities.
- Frozen derived shard expiry, complete `WorkerId` lifetime identity, exact-row
  compare-and-set fencing, restart and lost-acknowledgement behavior, and
  drain-until-empty ownership.
- Frozen delivered-row deduplication and the documented at-least-once boundary;
  no transaction was invented across handler effects and completion storage.
- Frozen deletion of attempts, claims, separate dedup guards, JSON-in-`Any`,
  every removal-quarantine/fingerprint surface, receipts, and markers.
- Frozen a deliberate compile boundary: T-0139 leaves no compatibility attempt
  facade, while T-0140 owns the remaining monitor/failure-policy integration.
- Human blockers: none. Implementation proceeds through nine ordered test-first
  slices.

## 2026-08-08 Implementation Ownership Correction

- The first implementation context repeatedly returned after individual
  in-progress edits instead of carrying the bounded batch to convergence.
- To reduce turn overhead without discarding completed work, remaining
  implementation is reassigned to a fresh existing `implementer` role,
  explicitly dispatched as `gpt-5.6-terra` / `medium`.
- The new owner starts from the durable worktree and tests; it must not repeat
  architecture or already-green direct Inbox discovery.

## 2026-08-08 Remaining-Scope Ownership Correction

- The replacement writer converged and pushed direct shard storage and drain
  behavior, but twice returned provider inventory without implementing it.
- Remaining provider conformance, delivery-client removal-quarantine deletion,
  attempt deletion, and the T-0140 handoff are assigned to a fresh existing
  `implementer`, explicitly dispatched as `gpt-5.6-terra` / `medium`.
- The new owner starts from pushed commit `8952014a` and must not repeat the
  direct Inbox, TenantIndex, shard, or drain slices.

## 2026-08-08 Implementation: Slice 1 In Progress

- Existing role: `implementer`; explicitly dispatched as `gpt-5.6-terra` /
  `medium`. Runtime model and reasoning self-introspection are unavailable on
  this surface; the immutable configured profile is recorded, with no visible
  mismatch or inherited-profile fallback.
- TDD RED: added `direct-inbox-records.test.ts`; after `pnpm proto:generate`,
  its direct-schema assertion failed because the legacy `Any` record spec had
  no `sourceType`.
- TDD GREEN: `inboxRecordSpec` now declares `InboxMessageSchema` as both
  source and stored record, `InboxMessageIdSchema` as its record identifier,
  and the frozen direct-record columns. Focused test passed:
  `pnpm exec vitest run packages/server/test/delivery/direct-inbox-records.test.ts`
  (1 test).
- The generated-record migration is intentionally incomplete at this point:
  the legacy `Any` mapper/storage still consumes the old rows and the focused
  test only proves the new record-spec seam. Continue Slice 1 by replacing that
  mapper and storage typing before accepting the slice.
- Architecture acceptance correction: `when_received` uses the `timestamp`
  column type, and direct record extraction rejects a missing ID/shard rather
  than materializing sentinel zero or empty values.
- TDD RED/GREEN (mapper): the direct-record test initially received
  `google.protobuf.Any` from `InboxRecords.write`; it now receives generated
  `spine.server.delivery.InboxMessage`, including the direct identifier,
  signal ID, and `int32` version. Focused test: 2 passing assertions.
- Focused legacy inbox integration was run after the mapper change and fails at
  the expected unconverted storage seam: the legacy string-key `InboxStorage`
  attempts to clone an ID through the new message-ID `RecordSpec`. This is the
  next implementation target, not a test/environment blocker.
- TDD storage GREEN: direct inbox receive/read now opens the direct
  `InboxMessage` record family and uses `InboxMessageId` CAS IDs; the existing
  shared-storage behavior test passes (1 selected test, 125 skipped). Legacy
  guard/claim code remains below the direct path and is scheduled for removal
  in Slice 2 rather than accepted as final behavior.
- Slice 2 transition: direct inbox admission rereads the exact pending row and
  creates work without persisting a message claim; completion uses the direct
  pending-to-delivered CAS path. Focused direct-record suite passed (3 tests).
  Prettier was applied to the active inbox source/test files and its check is
  clean. Remaining compiled legacy claim/guard implementation is still pending
  deletion before this slice can be accepted.
- TDD RED/GREEN (delivered-row dedup): a duplicate pending row was initially
  admitted after its matching delivered predecessor. Direct admission now scans
  delivered rows inside the already-owned shard, ignores expired `keep_until`,
  CAS-marks a matching duplicate delivered, and returns no callback work. The
  focused direct suite is green (3 tests).
- Deletion pass started: removed the dedicated `inbox-claim.ts` production
  module. The direct Inbox suite remains green (3 tests); embedded legacy guard
  implementation in the old mapper/storage remains the next deletion target.

## 2026-08-08 Implementation: Direct Inbox Deletion

- TDD RED: extended the direct Inbox test with a source-level exclusion check;
  it failed because the storage module still imported and compiled per-message
  exclusion and separate duplicate paths despite the direct path returning
  first.
- TDD GREEN: replaced `inbox-storage.ts` with the direct generated
  `InboxMessage` family only. It now has collision-safe absence CAS, exact
  pending-to-delivered CAS, and shard-held delivered-row suppression; it opens
  no separate durable family and contains no per-message exclusion machinery.
- Evidence: `pnpm exec vitest run packages/server/test/delivery/direct-inbox-records.test.ts`
  passed 4/4. The legacy delivery consumer still imports the deleted internal
  seam; its exact compile fallout is reserved for the required T-0140 handoff
  inventory after the remaining direct-record deletion work.
- Direct TenantIndex migration is now in place: its record, source, and ID are
  the generated `TenantId`, and `all()` rejects non-`value` oneof storage rows.
  `pnpm exec tsc -p packages/server/tsconfig.json --noEmit` confirms that the
  prior TenantIndex record-spec error is gone. Remaining failures are: old
  `delivery-attempts` and Any shard specs, three old Inbox internal-access
  imports in delivery orchestration, and four stale legacy mapper references.

## 2026-08-08 Implementation: Direct Inbox Mapper Deletion

- TDD RED: expanded the direct Inbox source exclusion test to cover both the
  storage and mapper modules; it failed on the remaining legacy mapper path.
- TDD GREEN: replaced the mapper with a generated `InboxMessage` conversion
  only. Inbox rows have no local encoded record envelope or auxiliary record
  type; signal payloads continue to use their generated Command/Event fields.
- Evidence: `pnpm exec vitest run packages/server/test/delivery/direct-inbox-records.test.ts`
  passes 4/4. The narrowed typecheck no longer reports mapper errors; remaining
  failures are old attempts, old inbox consumer imports, and the old shard
  record implementation.

## 2026-08-08 Implementation: Direct Shard Record Start

- TDD RED: added a generated-record specification test; it failed because the
  shard record specification was not exported and remained Any-based.
- TDD GREEN (specification only): the exported ungrouped specification now has
  `ShardSessionRecord` as source/record and `ShardIndex` as ID, with no
  materialized columns. Focused Inbox plus shard-spec test evidence is 5/5.
- Runtime acquisition/renewal/release remains the next slice. The current
  compile inventory adds the expected direct-record generic mismatch in the
  old registry implementation; it must be removed by the direct CAS rewrite,
  not papered over.

## 2026-08-08 Implementation: Direct Shard Runtime

- Direct runtime conversion uses generated `ShardSessionRecord` rows and a
  generated `ShardIndex` key. `when_last_picked + leaseMs` is the sole expiry
  calculation, so equality at the boundary is expired.
- Focused fencing evidence: one worker excludes another before expiry; the
  second worker takes over at the equality boundary with a distinct WorkerId;
  the stale worker cannot renew or release. Release stores an unowned direct
  row retaining the pickup time.
- The old shard suite was deleted because it only injected/validated obsolete
  JSON-in-Any record fixtures and deletion semantics. Replacement direct tests
  pass with Inbox coverage: `pnpm exec vitest run
  packages/server/test/delivery/direct-inbox-records.test.ts
  packages/server/test/delivery/direct-shard-session-records.test.ts` (6/6).
- `prettier --check` and `git diff --check` are clean. TypeScript has no shard
  error; remaining inventory is delivery attempts and three old Inbox
  internal-access imports in T-0140 orchestration files.

## 2026-08-08 Shard Review Correction

- Confirmed finding: generating an identifier inside each pickup violated the
  worker-lifetime identity requirement. TDD RED proved two pickups by one
  registry produced distinct values.
- GREEN: the registry creates one UUID at construction and uses it with the
  caller node as the complete durable WorkerId on every pickup. A distinct
  registry represents a restart/new worker and receives a distinct UUID.
- Evidence: direct shard tests pass 3/3; Prettier and `git diff --check` are
  clean. The public `ShardSession` still exposes the legacy scalar accessors
  for current delivery callers while its durable `worker` identity is complete;
  removing those callers is coupled to the pending T-0140 handoff.

## 2026-08-08 Shard Contract Correction

- The first correction still retained scalar `id`/`node` accessors and allowed
  a varying node argument, so it did not satisfy the frozen WorkerId contract.
- `pickUp` now accepts a complete generated `WorkerId`; it validates and writes
  that identity unchanged. `ShardSession` carries only the complete generated
  WorkerId and the exact derived-lease snapshot—no scalar session ID/node
  aliases remain.
- Focused behavior tests pass 3/3 and formatting/diff checks are clean. The
  intentional compile handoff now additionally names old T-0140 delivery
  callers whose work-registry port still accepts a node string.

## 2026-08-08 Direct Drain Primitive

- TDD RED: a shard drain invocation was absent after adding a test where a
  second pending message arrives while processing the first.
- TDD GREEN: `drainUntilEmpty` acquires the complete WorkerId, renews ownership
  before reads and callbacks, repeatedly rescans until no pending work remains,
  and releases its exact session in `finally`.
- Evidence: direct shard suite passes 4/4; changed files are Prettier-clean and
  `git diff --check` is clean.

## 2026-08-08 Remaining Scope: Provider And Deletion Batch

- Existing role: `implementer`; explicitly dispatched as `gpt-5.6-terra` /
  `medium`. Runtime model and reasoning self-introspection remain unavailable
  on this surface; the immutable configured role/profile is the available
  metadata, with no visible mismatch or inherited-profile fallback.
- TDD RED/GREEN (remote removal): a direct constructor test failed with
  `DeliveryQuarantineError`; `RemoteInbox` now rereads the authoritative pending
  row and removes it directly on completion, with no removal record,
  fingerprint, configuration, or lifecycle state. The client package deletes
  `RemovalQuarantine`, `RemovalQuarantineRecord`, and `DeliveryQuarantineError`,
  their exports, dedicated tests/fixtures, and all active package docs.
- TDD RED/GREEN (attempt deletion): the direct-record deletion check first saw
  `delivery-attempts.ts`; the attempt and retry-decision sources plus their
  dedicated test are deleted, without a compatibility facade.
- TDD RED/GREEN (drain fencing): a replacement owner during a drain initially
  caused `release(undefined)`; `drainUntilEmpty` now releases only an exact
  retained session after a lost renewal.
- Provider conformance uses actual runtime construction for each new direct
  family. MySQL `setTableName(...)` reaches `InboxMessage`,
  `ShardSessionRecord`, and `TenantId`; Datastore `useRecordStorage(...)` reaches
  the same record-only schemas. The surviving-family ledger remains:
  `EntityRecord` current/grouped state history, `Event` grouped event history/
  Event Store, `InboxMessage`, `ShardSessionRecord`, `SubscriptionRecord`,
  `GatewayAuthenticatedSubscription`, `ApplicationNodeLease`, and TenantId.
  Existing provider-conformance tests remain the evidence for the unchanged
  families; the new direct-record test covers the three T-0139 families.
- Focused evidence: direct shard/provider tests pass 8/8; client codec and work
  registry tests pass 19/19; delivery-client typecheck exits zero.
- Focused `vitest --coverage` executed the same four test files and passed all
  27 tests, but the repository-wide Vitest configuration collected every source
  package and failed its global 90% threshold (3.70% statements, 3.83% branches,
  4.31% functions, 3.84% lines). This is not a changed-source coverage measure;
  no threshold/configuration workaround was added while the T-0140 compile
  boundary remains intentionally unresolved.
- Intentional TypeScript handoff after deleting attempts: `delivery-builder.ts`
  still imports legacy `inboxStorageAccess` and supplies a scalar-node work
  registry; `delivery-loop.ts` still imports that accessor; and `delivery.ts`
  still imports `delivery-attempts`, `delivery-retry-decision`, and the accessor,
  supplies the scalar registry, and reads `ShardSession.node`. `tsc` additionally
  exposed the direct drain nullable-release defect, which this batch fixed.
  These remaining errors are the exact T-0140 integration inventory; no monitor
  or compatibility implementation was added here.

## 2026-08-08 Autonomous Continuation Assignment

- The Desktop execution surface supports explicit child role, model, and
  reasoning dispatch. The continuing bounded writer is the existing
  `implementer` role, expected and explicitly dispatched as
  `gpt-5.6-terra` / `medium`; subagent spawning is prohibited.
- Scope: preserve and finish the unstaged T-0139 batch from `8952014a`, make
  the direct Inbox test independent of the T-0140 orchestration graph, complete
  the frozen corruption/CAS/dedup/shard/worker/provider behavior coverage,
  reconcile affected Server and delivery-client documentation/API claims, and
  run focused RED/GREEN checks. No compatibility attempt facade, monitor,
  replacement persistent state, T-0140 implementation, merge, or non-`origin`
  push is authorized.
- Canonical skill applicability was refreshed from the session inventory,
  `build-protocol/skills/EXPECTED_SKILLS.md`, the complete bounded
  `find ~/.agents/skills -maxdepth 2 -type f -name SKILL.md -print` inventory,
  and `~/.agents/.skill-lock.json`. Selected governing skills are `implement`,
  `subagent-driven-development`, `using-git-worktrees`,
  `test-driven-development`, and `verification-before-completion`; their
  entrypoints were read completely. `requesting-code-review` is deferred until
  the specialist-review gate. `planning-with-files` is N/A because the human
  prohibited scratch/ledger directories and the task/work logs are durable
  memory. Architecture/domain/API design skills are N/A because T-0139's
  architecture and public boundary are already frozen; debugging skills are
  not selected because the known load failure has a concrete import-graph
  cause. Project protocol and the frozen task ledger govern any skill conflict.

## 2026-08-08 Bounded Implementation Converged

- Fixture isolation: `inbox-message-fixture.ts` now imports only the direct
  Inbox and shard modules, so `direct-inbox-records.test.ts` does not load the
  T-0140 server-root orchestration graph.
- TDD RED/GREEN: same-worker repeat pickup, stale renewal after an applied
  renewal, and stale release after an applied release initially failed to
  converge. The direct registry now rereads the exact live record and converges
  only for the same complete `WorkerId`; a different worker remains fenced.
  Lease-expiry arithmetic also fails closed on overflow.
- Added direct Inbox coverage for malformed generated rows, exact
  pending-to-delivered CAS/idempotence, live delivered suppression, and expired
  delivered-row admission. Added drain takeover/lost-renewal coverage and
  tightened Datastore provider construction to verify the exact three direct
  record schemas, independent of incidental construction order. MySQL continues
  to exercise actual configured table construction for the same three families.
- Updated narrow Server and delivery-client README, REFERENCE, and TSDoc claims:
  shard ownership excludes concurrent delivery; delivered rows are dedup facts;
  handler effect plus delivered CAS is non-transactional; lost acknowledgement
  can redeliver; downstream handling must be idempotent. Remote completion
  remains direct `removeOne` with no removal receipt, fingerprint, or quarantine
  state. The existing remote shard-mutation uncertainty guard was deliberately
  not changed because it is outside the frozen remote-removal deletion scope.
- Focused evidence:
  `pnpm exec vitest run packages/server/test/delivery/direct-inbox-records.test.ts packages/server/test/delivery/direct-shard-session-records.test.ts packages/server/test/delivery/direct-record-provider-conformance.test.ts packages/delivery-client/test/codec-immutability.test.ts packages/delivery-client/test/remote-work-registry.test.ts`
  passed 35/35 tests. `pnpm exec tsc -p packages/delivery-client/tsconfig.json
  --noEmit` exited 0. `git diff --check` exited 0, and the targeted client scan
  found no `RemovalQuarantine`, removal record/error, or removal-fingerprint
  surface. Vitest reported only stale dist sourcemaps for the deliberately
  deleted server attempt files.
- Exact intentional T-0140 compile inventory from
  `pnpm exec tsc -p packages/server/tsconfig.json --noEmit` (exit 2):
  `delivery-builder.ts` imports `inboxStorageAccess` and supplies the scalar
  registry; `delivery-loop.ts` imports `inboxStorageAccess`; `delivery.ts`
  imports deleted `delivery-attempts`, deleted `delivery-retry-decision`, and
  `inboxStorageAccess`, supplies the scalar registry, and reads removed
  `ShardSession.node`. No compatibility facade, monitor, or T-0140 source was
  added. `verify:task` was intentionally not run under the assignment boundary.

## 2026-08-08 Coverage-Correction Ownership

- The continuing implementer added direct TenantIndex, Inbox, shard, remote
  Inbox, and remote-delivery tests but repeatedly returned before the measured
  correction reached the required threshold. Its last committed endpoint
  remains `4937cd52`; all useful uncommitted tests are preserved.
- The remaining bounded test-only correction is reassigned to a fresh existing
  `implementer` role, expected and explicitly dispatched as
  `gpt-5.6-terra` / `medium`, with subagent spawning prohibited. It owns only
  the preserved T-0139 coverage-test edits and the task/work evidence needed to
  reach at least 90% aggregate statements, branches, functions, and lines over
  the exact six behavior-changed sources. Production behavior and the T-0140
  boundary must remain unchanged.

## 2026-08-08 Coverage Correction Complete

- The reassigned bounded implementer retained the configured `implementer`
  `gpt-5.6-terra` / `medium` profile; runtime self-introspection remains
  unavailable, with no visible profile mismatch or fallback. No production or
  T-0140 file changed.
- Added direct behavior coverage for TenantId modes, generated Inbox payload
  validation and storage CAS/dedup/read bounds, shard absence/expiry/unowned
  rows and drain ownership, RemoteInbox cursor/admission/terminal completion,
  Remote adapter observation validation, and RemoteDelivery close retry and
  close-during-open behavior.
- Exact changed-source V8 coverage command (seven focused suites, six explicit
  `--coverage.include` sources) passed 7 files / 52 tests: statements 93.49%
  (532/569), branches 90.11% (392/435), functions 97.05% (132/136), and lines
  95.86% (487/508). No coverage threshold or configuration exclusion was
  changed. The six runtime sources are TenantIndex, Inbox records/storage,
  shard registry, Remote adapters, and RemoteDelivery; export-only and
  TSDoc-only changed files are deterministic API/docs targets rather than
  runtime coverage sources.
- Fresh focused non-coverage evidence passed the same 7 files / 52 tests;
  Prettier check and `git diff --check` passed. Vitest continues to report only
  stale sourcemaps for deliberately deleted legacy server attempt files.
