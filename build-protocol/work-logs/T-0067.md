# T-0067 Work Log

Status: complete; integrated and post-merge verified.

## 2026-07-22 — Start

- Created isolated branch `task/T-0067-wave1-closure` from pushed `main` at
  `4e3b43d6`.
- Classified the packet high-risk as the final documentation, upstream-delta,
  security, and release acceptance boundary for Wave 1.
- Recorded behavior-focused acceptance criteria and explicit Wave 2/3/4 and
  delivery-server storage exclusions before content changes.
- Selected execution surface is Codex Desktop. It supports the required
  existing roles and explicit child model/reasoning dispatch.
- Implementer assignment is the existing `implementer` role, expected and to
  be explicitly dispatched as `gpt-5.6-terra` / `medium`. Runtime metadata or
  the immutable-profile/self-introspection limitation will be recorded before
  accepting the result.
- The latest predecessor full gate passed at the exact runtime tree before
  record-only T-0066 closure: 127 test files passed, 3 skipped; 2,317 tests
  passed, 21 skipped; 90.09% branch coverage. This packet will run its own full
  final gate after documentation and review convergence.

## 2026-07-22 — Baseline And Upstream Audit

- Locked dependency installation passed its supply-chain policy and reused all
  packages from cache.
- A clean generated build exposed a missing `@spine-ts/client` to
  `@spine-ts/core` TypeScript project reference. Building core first made the
  unchanged build and baseline suite pass: 127 test files passed, 3 skipped;
  2,317 tests passed, 21 skipped. The correction is separately owned by
  T-0067a and must integrate before this branch resumes final verification.
- Fetched official `SpineEventEngine/core-java` and
  `SpineEventEngine/delivery-server` repositories into temporary audit copies.
  Current default branches and full commit metadata were read directly from
  their Git histories.
- `core-java` advanced 41 commits from frozen
  `a408b0d70dafd603efc55b89c8b4b6f3e8c19d3b` to current `master` head
  `026266f15d866464a99386fa891d5c8ec39f854e`. Every code delta is confined to
  the new shared signal-dispatching entity hierarchy, Process Manager event
  journal/recent-event-history work, Aggregate recent history, and related
  tests/docs/dependencies. Classification: defer to the already approved Wave
  2 recent state/event history and Aggregate/Process Manager work; adopt no
  Wave 1 runtime or contract delta.
- `delivery-server` current `master` remains exactly the frozen commit
  `21f2901f393e552208b97166f4eaeb942f9f5172`; there is no delta to classify,
  including under the only in-scope `simple-server` module.

## 2026-07-23 — Build Correction Integration

- T-0067a corrected the missing client-to-core TypeScript project reference,
  passed clean composite-build review/verification, merged into `main`, and was
  pushed at closure commit `b45a4655`.
- Fast-forwarded this worktree to `b45a4655` without losing its audit records or
  touching the dependency-setup executable-bit change. The final documentation
  review baseline is now the corrected pushed `main`.

## 2026-07-23 — Documentation Correction And Audit Record

- Implementer assignment remains the existing `implementer` role, explicitly
  dispatched as `gpt-5.6-terra` / `medium`. Runtime self-introspection is not
  exposed by this surface; the immutable configured assignment is the available
  metadata, with no visible role/profile mismatch or fallback.
- Corrected stale root/package claims that said the client lacked query
  execution/DSL and that the delivery-server package was listener-free. The
  low-level core remains listener-free, while the package also exports the
  standalone in-memory `DeliveryServer` listener.
- Updated the framework user guide with the completed standalone server's
  configuration, lifecycle, Admin/health behavior, trusted-network boundary,
  two-machine topology, and exclusions. Replaced obsolete task-number-specific
  T-0063 language with current Wave 1 inclusion and Wave 2/3 handoffs.
- Recorded the upstream audit separately at
  `build-protocol/research/T-0067-upstream-delta-audit.md`: core-java current
  head `026266f15d866464a99386fa891d5c8ec39f854e` is 41 commits beyond frozen,
  all relevant changes defer to Wave 2; delivery-server remains exactly frozen
  at `21f2901f393e552208b97166f4eaeb942f9f5172`; no Wave 1 delta is adopted.
- Added `docs/check-typescript-snippets.mjs`, a small deterministic syntax
  check for TypeScript fences in the root guide and affected package READMEs.
  It also resolves every named `@spine-ts/*` import against the package's
  current public declaration entrypoint, including type-only bindings. Consumer
  substitutions, declared application-owned values, and callback implementations
  remain intentionally unresolved illustrative seams.
- Focused checks passed: `node docs/check-typescript-snippets.mjs`;
  `pnpm docs:check:generated` (TypeDoc/API checker); `git diff --check`; and
  `pnpm format:check`; focused package-root verification
  `pnpm vitest run packages/delivery-server/test/public-api.test.ts` (1 file,
  2 tests); and composite `pnpm typecheck:build:generated` (`tsc -b`).

## 2026-07-23 — Coordinator API-Actuality Correction

- Replaced the root README's stale framework-wide claims that remote/multi-host
  transport, worker supervision, and health were excluded. Those are now
  explicitly distinguished from the intentionally local in-memory to-do
  example; current remaining Wave 1 exclusions are retained.
- Corrected both guide remote-supervisor examples. Each builder now receives
  `RemoteInbox(client, durableRemovalQuarantine)` and
  `RemoteWorkRegistry(client)`, and the shown shutdown order closes the
  supervisor before the client and the application-owned quarantine lifecycle.
  The prose explicitly rejects an in-memory `Map` for production quarantine.
- Extended `docs/check-typescript-snippets.mjs` to validate syntax plus every
  named `@spine-ts/*` public declaration import. It does not pretend to resolve
  consumer `@example/*` modules, app-owned declarations, or callback bodies.
- The implementation report now marks this endpoint ready for orchestrator
  review. No security artifact or generator-mode change was touched.
- Correction gate passed: `node docs/check-typescript-snippets.mjs`;
  `pnpm docs:check:generated`; `pnpm vitest run
packages/delivery-server/test/public-api.test.ts` (1 file, 2 tests);
  `pnpm typecheck:build:generated`; `pnpm format:check`; and
  `git diff --check`.

## 2026-07-23 — Dependency And Security Inputs

- T-0067b refreshed only `brace-expansion` 1.1.15 to 1.1.16,
  `brace-expansion` 5.0.6 to 5.0.7, and `linkify-it` 5.0.1 to 5.0.2. Its
  dependency review is clean; it is merged, post-merge verified, and pushed at
  `main` closure `893d8756`. Frozen install and supply-chain policy passed;
  production and full low-threshold audits both report zero known
  vulnerabilities.
- Fast-forwarded this worktree to `893d8756` without losing documentation or
  security artifacts. This is the final review baseline.
- Applied the repository-grounded threat-model skill using context already
  explicitly validated by the human: Node only, loopback defaults,
  unauthenticated cleartext delivery only on a trusted private network,
  application-defined data sensitivity, consumer-owned identity-to-tenant
  authorization, in-memory delivery-server state, and explicit Wave 2/3/4
  exclusions. No further service-context question blocks the autonomous final
  review.
- Extended the canonical threat model with TB-11 through TB-16 and TM-013
  through TM-018 for client, environment, BlackBox, delivery server/Admin,
  remote wire, supervisor, and unknown-mutation/quarantine boundaries. Added
  `build-protocol/security/T-0067-wave1-security-findings.md` as the pending
  reviewer ledger; it makes no clean claim before the dedicated review.

## 2026-07-23 — Final Review Wave

- Pre-review mechanical verification passed: locked installation and
  supply-chain policy; TypeScript snippet syntax and named public exports;
  generated documentation/API checks; delivery-server public API tests (1
  file, 2 tests); composite TypeScript build; formatting; and diff check.
- Documentation review is clean with no P0-P3 findings. The immutable assigned
  profile is `gpt-5.6-luna` / `medium`; runtime self-introspection is
  unavailable, with no visible mismatch or fallback.
- TypeScript/API review is clean with no P0-P3 findings. The immutable assigned
  profile is `gpt-5.6-terra` / `high`, explicitly dispatched; runtime
  self-introspection is unavailable, with no visible mismatch or fallback.
- The final security reviewer returned two blocking P1 findings: unbounded
  retained/enumerated delivery-server state and incomplete server-side inbox
  validation that permits poison records. Both are accepted as one correction
  batch. Style/maintainability and performance/reliability are reopened because
  the correction changes production admission, capacity, and response-work
  behavior; TypeScript/API will re-review any public configuration surface.
- TM-013 remains a documented trusted-network residual matching the frozen
  simple-server. TM-015 through TM-018 are clean within their declared
  boundaries; TM-014 remains blocked until capacity correction passes.
- The security reviewer used the existing immutable `security_reviewer` role,
  explicitly dispatched as `gpt-5.6-terra` / `high`. Runtime self-introspection
  is unavailable, with no visible mismatch or fallback.
- The security correction is assigned to one bounded owner using the existing
  `implementer` role, expected and explicitly dispatched as
  `gpt-5.6-terra` / `medium`. The owner has exclusive write responsibility for
  the delivery-server runtime/tests and affected public/security documentation.
- The initial correction implemented finite safe defaults and validated options
  for retained Inbox records (10,000), serialized retained bytes (32 MiB), and
  tracked shards (initially 10,000, reduced to the client-compatible 1,000 in
  the final correction); server admission rejects capacity breaches
  atomically. It also validates complete client-decodable Command/Event records
  and their one-megabyte payload ceiling before one/batch writes. Admin snapshot
  and expiration response work was initially capped at 10,000 shard
  observations and is now capped at 1,000. Focused
  direct-service regressions passed; affected specialist re-review is next.
- Before correction re-review dispatch, recorded the existing affected roles
  and explicit profiles: style/maintainability, performance/reliability,
  TypeScript/API, and security use `gpt-5.6-terra` / `high`; the immutable
  documentation role uses `gpt-5.6-luna` / `medium`. All five concerns form one
  aggregated re-review wave despite execution-capacity sequencing.
- The complete correction re-review wave is not clean. Security and reliability
  keep TM-014 blocked on >4 MiB Inbox page responses; reliability also found
  permanent released-shard retention and a 10,000-server/1,000-client Admin
  mismatch. Style and API found the public-core shard-limit bypass and
  duplicated defaults. API additionally requires client-side 128-byte worker
  identity parity and directly documented public core options. Documentation
  requires honest symmetric batch and option range/failure claims.
- All re-review assignments used their recorded explicit profiles. Runtime
  self-introspection was unavailable without visible mismatch or fallback.
  Unrestricted delivery-server verification passed 15 files / 59 tests; the
  reviewer sandbox listener `EPERM` was environmental only.
- Accepted one final correction batch covering response-safe explicit paging,
  released-shard pruning, a shared 1,000-shard bound, early symmetric batch
  limits, centralized defaults/public option declarations, client worker-ID
  validation parity, and exact documentation/tests. The existing implementer
  remains the single owner, explicitly `gpt-5.6-terra` / `medium`, with added
  ownership of the narrowly affected delivery-client codec/tests/docs.
- Targeted finding rechecks are clean for security, reliability, and
  maintainability. API and documentation each retain one accepted P2: preserve
  actionable `RESOURCE_EXHAUSTED` for an oversized safe-read page, and clarify
  that retained-state capacity rejection applies to writes while the 1..100
  bound applies to both write/remove batches. These exact deterministic fixes
  return to the same implementer; they do not reopen a broad review wave.
- The deterministic P2 corrections are implemented. Safe reads return the
  original `RESOURCE_EXHAUSTED` status without retry while other nonretryable
  statuses remain sanitized, and the guide now distinguishes symmetric batch
  lengths from write-only retained-state admission. Focused lifecycle
  verification passed 1 file / 26 tests. Delivery-client build/declarations,
  focused ESLint, generated API docs, guide snippets, repository formatting,
  and diff integrity also passed.

## 2026-07-23 — Final Correction Batch

- Implemented all seven accepted corrections. Full records leave wrapper room
  below 4 MiB; `findOne`, newest, and exact requested page responses are encoded
  and checked, with explicit `RESOURCE_EXHAUSTED` for an oversized page and no
  silent shortening or new paging abstraction.
- Released message-free shards are pruned immediately and after final-message
  removal. Core/listener/Admin/expiration/client use a 1,000-shard ceiling.
- Write/remove batches reject lengths outside 1..100 before record processing.
  Limit defaults are centralized; public core/listener ranges and synchronous
  failures are declared directly.
- Client pickup/release reject worker and node IDs above 128 combined UTF-8
  bytes before RPC. Focused server tests (36) and client tests (6), plus both
  package builds, pass. Full unrestricted verification then passed:
  delivery-server 15 files / 63 tests and delivery-client 10 files / 81 tests,
  including real gRPC and multi-process topology.
- Final targeted security, reliability, and maintainability rechecks are clean.
  The two remaining API/documentation P2s were corrected deterministically:
  safe reads preserve actionable `RESOURCE_EXHAUSTED`, and the guide separates
  symmetric batch bounds from write-only capacity admission. Client lifecycle
  tests passed 26/26 twice; typecheck, lint, generated docs, snippets,
  formatting, and diff integrity passed. All required concerns are clean and
  the full repository gate is next.

## 2026-07-23 — Full Verification

- The first full-gate attempt exposed test-only protobuf initializer values
  passed directly to `toBinary`; materializing them with their generated
  schemas fixed tooling typecheck, and the focused Inbox suite passed 10/10.
- The next attempt exposed the documentation checker's missing explicit Node
  globals and two stale line-pinned inherited cleanup-name exceptions after
  public option documentation shifted existing declarations. The checker now
  imports Node globals explicitly; only those exact exception lines were
  repinned. Cleanup enforcement and its 94-test suite passed.
- Final full `pnpm --config.verify-deps-before-run=false verify` passed: 127
  test files passed, 3 skipped; 2,325 tests passed, 21 skipped. Coverage is
  94.17% statements, 90.07% branches, 95.05% functions, and 94.69% lines.
  TypeScript builds, tooling typecheck, ESLint/cleanup, formatting, TypeDoc/API
  exports, copied Proto checksums, frozen descriptors, generated cleanliness,
  18 package imports, and 121 Markdown links all passed.

## 2026-07-23 — Integration And Wave 1 Closure

- Committed the reviewed task tree as `023234ee` and pushed
  `task/T-0067-wave1-closure`; the local and remote task refs matched exactly.
- Fast-forwarded `main` from `893d8756` to the identical reviewed task tree
  without touching unrelated dirty or untracked user files.
- The first focused post-merge run used stale ignored `dist` output in the main
  worktree and reproduced the old shard-retention behavior. Rebuilding package
  output resolved it without a source correction, proving the failure was a
  verification-order issue rather than a merged-tree defect.
- Post-merge tree equality and focused verification passed after the build: 25
  delivery-client/server files and 144 tests, including real gRPC and
  multi-process topology; snippets, cleanup, generated API docs, 18 package
  imports, 121 Markdown links, and diff integrity also passed.
- Wave 1 is closed. Wave 2 remains intentionally inactive until its required
  human Q&A and approved plan.
