# T-0150 Work Log

## Start

- Started: 2026-08-09.
- Branch: `task/T-0150-shared-runtime`.
- Baseline: `34900a85` (`docs(protocol): close Datastore tenancy task`).
- Classification: high-risk public tenant-boundary, shared storage identity,
  server behavior, migration, and release cutover.
- Implementation owner: `/root`, one production writer. Ordinary TypeScript
  implementation uses the current orchestrator surface; specialist reviewers
  remain read-only until deterministic convergence.
- The Desktop execution surface supports the required explicit configured
  reviewer profiles. Runtime self-introspection may remain unavailable and is
  recorded as such rather than treated as a fallback.

## Initial failing boundary

The final T-0149 task profile passed Node/Proto gates and exposed 54 intentional
T-0150 shared-server errors:

- scalar/string tenants versus complete generated `TenantId` and discriminated
  `StorageContext`;
- function-only RecordColumn declarations versus typed Proto column contracts;
- Stand/repository/delivery/environment snapshots still carrying the old
  tenant shape.

No compatibility facade will be added. The first implementation batch migrates
these owning server sources and their focused tests, then deletes the old
scope/TenantId persistence seams.

## Shared-runtime checkpoint

- Migrated server RecordSpecs to typed Proto ID and column declarations.
- Replaced scalar tenant values with complete generated `TenantId` snapshots
  across Bounded Context, repository, Stand, delivery, service, and environment
  contracts.
- EventStore now opens record storage per selected operation: event envelopes
  select append tenants and reads require an explicit tenant.
- In-memory storage identity is tenant boundary plus record family, with a
  deterministic factory-owned tenant catalog. Bounded Context names do not
  enter physical identity, and no generic TenantId record family is created.
- Focused EventStore and server context verification passed: 6 files and 139
  tests. Server TypeScript, changed-TypeScript ESLint, Prettier, and
  `git diff --check` passed.
- The initial broad storage/server diagnostic passed 1,633 tests and exposed
  127 stale string-tenant/eager-handle fixtures. Context and EventStore fixture
  clusters are now converged; delivery, Stand, services, repository, and
  environment clusters remain in progress.

## Delivery and Stand fixture convergence

- Replaced scalar tenant fixtures with complete generated tenant IDs.
- Corrected the physical-sharing assertion: equal tenant plus record family
  shares rows even when diagnostic Bounded Context names differ.
- Removed the obsolete generic TenantId provider-family assertions.
- Updated MySQL conformance fixtures to use the direct ID/bytes/declared-column
  layout and the same Stringifier registry for message-valued columns.
- Delivery and Stand verification passed: 20 files and 331 tests. Scoped
  ESLint, Prettier, and `git diff --check` passed.

## Repository and service fixture convergence

- Migrated repository, history, catch-up, QueryService, and subscription
  fixtures to complete value/domain/email tenant messages.
- Proved EventStore appends select the event-envelope tenant even when a base
  reader context names another tenant; catch-up reads only the explicitly
  selected tenant slice.
- Repository and service verification passed: 6 files and 340 tests. Server
  TypeScript, scoped ESLint, Prettier, and `git diff --check` passed.

## Server-environment fixture convergence

- Migrated attachment, dynamic admission, generation replacement, and delivery
  record fixtures to complete generated tenant IDs.
- Updated durable-subscription MySQL fixtures to direct `ID`/`bytes` parameter
  positions and server startup assertions to lazy operation-selected storage.
- Full server lifecycle verification passed: 13 files and 411 tests, with no
  unhandled errors. Server TypeScript, scoped ESLint, Prettier, and
  `git diff --check` passed.

## Shared runtime test convergence

- Migrated the last EventBus fixture from a mutable scalar tenant getter to an
  event-envelope tenant snapshot and explicit tenant readers.
- The complete storage and server suites passed: 85 files and 1,760 tests, with
  no failures or unhandled errors. This closes the initial 127-failure fixture
  handoff without a scalar tenant, eager-handle, or persisted TenantId
  compatibility facade.
- Server TypeScript, all checkpoint-changed TypeScript ESLint, Prettier, and
  `git diff --check` passed.

## Legacy-layout inventory checkpoint

- Added fail-closed package commands for inspecting every configured MySQL
  tenant database and every discoverable Datastore namespace before cutover.
- The MySQL inventory reports retired `_scope`/`_revision` columns and
  scope-based primary keys. The Datastore inventory reports retired `_scope`
  properties and scope-derived keys.
- Provider READMEs, references, the API guide, and the beginner user guide now
  require a stopped-writer, offline migration and conflict-resolution step;
  there is no dual layout or compatibility reader.
- Focused inventory verification passed: 2 files and 8 tests. Scoped ESLint,
  Prettier, and `git diff --check` passed.
- The repository-wide tooling typecheck remains intentionally red on unfinished
  T-0150 test declarations: scalar tenant helper signatures,
  non-discriminated `StorageContext` literals, old RecordColumn declarations,
  and two missing RecordSpecs. These fixtures are the next migration batch.

## Typed fixture convergence

- Removed the remaining scalar-tenant helper signatures, widened boolean
  contexts, untyped RecordColumn constructors, and missing direct Entity
  RecordSpecs from shared package and server fixtures.
- Multitenant lifecycle fixtures now provide a complete generated tenant for
  startup, buffered readiness, replay, and retry behavior; no empty or string
  tenant compatibility path was introduced.
- Focused verification passed: 17 files and 752 tests. Thirteen live MySQL and
  Datastore tests were skipped because no provider endpoint was configured.
- The repository tooling TypeScript check, scoped ESLint, Prettier, and
  `git diff --check` passed.

## Runtime migration preflight

- MySQL factory construction now inspects every configured tenant database for
  retired `_scope`/`_revision` columns and scope-based primary keys before the
  factory becomes usable.
- A legacy or uninspectable target releases its probe, closes its pool, closes
  every previously opened tenant pool, and returns a sanitized configuration
  or connection failure. It does not open a dual-layout reader.
- Renamed delivery coordinator prose that used “canonical scope” for an
  unrelated logical worker obligation, preventing it from being confused with
  the deleted physical storage scope.
- MySQL and coordinator verification passed: 8 files and 129 tests; nine live
  database tests were skipped without a configured endpoint. MySQL and server
  TypeScript, scoped ESLint, Prettier, and `git diff --check` passed.

## Clean package build outputs

- Reproduced a release-packaging defect: incremental TypeScript builds retained
  compiled files for deleted sources, so local package tarballs could contain
  retired storage and delivery inventions even though their sources and exports
  were gone.
- Replaced the delivery-client-only root cleanup with one fail-closed cleanup of
  the `dist` directory for every project referenced by the root TypeScript
  build. The target list is fixed, validated against the root project
  references, and rejects symbolic links or non-directory targets.
- A clean root rebuild passed. The rebuilt outputs contain no
  `canonical-scope`, delivery-attempt, retry-decision, inbox-claim,
  delivery-quarantine, or session-revocation artifact.
- Focused cleanup and package-metadata verification passed: 2 files and 12
  tests. Scoped ESLint, Prettier, and `git diff --check` passed.

## Message Board native subscription topology

- Reproduced a native gRPC-Web subscription failure in which backend activation
  received the public subscription UUID rather than the private backend
  subscription envelope retained at subscription creation.
- Corrected the interop harness to place `DynamicSubscriptionCreator` between
  the public Gateway and its native backend. The one-node dynamic owner now
  retains backend envelopes, while unary calls continue through the native
  transport and binding disposal cancels the logical subscription.
- Native lifecycle and topology verification passed: 2 files and 4 tests. The
  distributed Docker example remains separately red because an authoritative
  query does not yet observe a posted message; that runtime path is under
  diagnosis and was not hidden by the harness correction.

## Message Board provider stringifier integration

- Reproduced the distributed authoritative-query failure with service logs.
  The command was accepted, but Projection delivery failed while Datastore
  materialized an `Any`-valued producer column: compact Proto JSON could not
  resolve the packed application `MessageId` without generated type metadata.
- The production Message Board Datastore factory now supplies a
  `StringifierRegistry` backed by the application `TypeRegistry`. This keeps the
  provider mapping symmetric for packed message-valued IDs and the queries that
  compare their materialized values.
- The distributed lifecycle now prints all Compose service logs on failure,
  preserving the first contained delivery error for future diagnosis.
- Message Board deployment/model verification passed: 2 files and 26 tests.
  The application build, scoped ESLint, Prettier, and `git diff --check` passed.
- Clean local images rebuilt successfully. The complete distributed Message
  Board verification passed: 2 files and 4 tests across two application nodes,
  one Gateway, shared Datastore, remote delivery, authoritative queries,
  subscription updates, and controlled shutdown.

## Beginner documentation and stale-claim convergence

- Reviewed active package READMEs/references, the beginner User Guide, API and
  architecture guides, governing runtime/API records, release inventories, and
  the Message Board application guide against the corrected runtime sources.
- Added source-backed Proto-to-provider examples. MySQL shows `ID`, authoritative
  `bytes`, framework Entity columns, model `(column)` fields, and parameterized
  Query SQL. Datastore shows native tenant namespace, source/group kind, mapped
  key name, authoritative unindexed `bytes`, framework Entity properties, and
  property-filter Query mapping.
- Documented JVM-compatible `Identifiers`, default compact Proto JSON
  `Stringifiers`, custom `StringifierRegistry`, application `TypeRegistry` for
  `Any`, and the required symmetry between stored ID/column values and Query
  operands. Linked the runnable Message Board provider composition.
- Removed active contradictions that still claimed Bounded Context physical
  partitioning, persisted generic `TenantId` rows, absent delivery supervision,
  future-only storage layers, or non-interoperable provider values. Historical
  design records now carry visible superseded guidance.
- Documentation verification passed: audience check, release-readiness check
  over 397 relative links, TypeScript snippet/package-metadata tests, Wave 8
  invention audit, Prettier, and `git diff --check`.
