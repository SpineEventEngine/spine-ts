# T-0091 Work Log

Task: `build-protocol/tasks/T-0091-durable-subscription-registry/TASK.md`

## 2026-08-02 — Framing

- Created the isolated task from pushed `main@a33364f9` after T-0090 passed
  release verification, merged, passed post-merge build and 199 focused tests,
  and was pushed.
- Classified B1 as high-risk because it changes public async admission,
  durable private state, provider CAS requirements, and production fail-closed
  startup.
- Froze the approved B1 boundary around existing bindings, storage, codec, and
  combined browser-host seams. Cross-gateway fencing/cleanup remains T-0092;
  standalone hosting remains T-0093.
- Recorded the existing implementer and four specialist reviewer profiles with
  explicit models/reasoning before dispatch.
- Next: push this framing checkpoint, establish focused baseline evidence, and
  dispatch one implementation owner to write the first failing contract tests.

## 2026-08-02 — Baseline

- `pnpm --config.verify-deps-before-run=false proto:generate` passed all 40
  Spine source checksums, example Proto quality checks, and 49 frozen
  descriptor digests.
- `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  completed successfully in the fresh isolated worktree.
- Focused baseline command covered auth subscription bindings, server browser
  assembly, in-memory record storage, and storage-factory contracts. Result:
  4 files and 149 tests passed.
- Next: push this evidence and dispatch the recorded implementer to add the
  awaitable-reservation RED tests before production changes.

## 2026-08-02 — Durable registry checkpoint

- Implementer: existing `implementer` role, explicitly dispatched as
  `gpt-5.6-terra` / `medium`. The execution surface exposes no runtime
  self-introspection metadata, so the configured immutable role/profile is the
  available acceptance evidence.
- Inspected the task-relevant JVM subscription-service research at
  `spine-jvm-docs/spine-server-runtime-and-bounded-context.md`. It confirms
  that returned subscription identities stay opaque and activation/cancellation
  route through the subscription service; the registry keeps that boundary and
  does not alter service-owned records.
- RED: the new durable-binding contract tests failed because the binding class
  did not exist. GREEN: 58 focused auth/server tests pass after adding the
  awaitable reservation contract and one StorageFactory-backed, versioned,
  namespaced registry handle.
- The handle copies backend bytes at every boundary, requires owner/expiry
  facts, validates finite configuration, closes only its record-storage handle,
  and preserves the supplied storage factory for reopening.
- Next: add malformed-record and production-admission regressions, then run
  coverage and the complete cheap preflight before reviewer handoff.

## 2026-08-02 — Hardened implementation

- Added bounded malformed-record validation without exposing backend bytes,
  cancellation and expiry behavior, exact-once reservation release, namespace
  isolation, durable capability admission, and local registry reopen coverage.
- Production browser assembly now rejects absent or volatile subscription
  bindings before listener setup. Local configuration retains the deliberate
  in-memory default.
- Changed-source coverage command passed: 127 focused tests; 94.79% statements,
  90.75% branches, 96.92% functions, and 97.13% lines. A broad package-only
  coverage invocation was intentionally not used as a gate because Vitest's
  global include covers unrelated workspace source; the bounded changed-source
  command is the recorded evidence.
- Cheap preflight passed: formatting, affected ESLint, `tsc -b packages/auth
  packages/server`, focused tests, TSDoc enforcement, API/documentation checks,
  and `git diff --check`. One combined server-test run had a known timing-only
  HTTP/2 session-order assertion failure; its isolated rerun with the same
  source passed 58/58 tests.
- Next: push this coherent checkpoint and give the complete implementation
  package to the four recorded specialist review lanes.
