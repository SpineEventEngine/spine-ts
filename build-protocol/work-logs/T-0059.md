# T-0059 Work Log

## 2026-07-23 — Startup

- Created isolated branch/worktree `task/T-0059-client-subscriptions` from exact
  pushed `origin/main` baseline `11a54925` after durable T-0058 closure.
- Classification is high-risk public/network streaming and bounded-resource
  lifecycle work. T-0052 already performed the accepted architecture split; no
  duplicate deep planner is dispatched.
- Two read-only investigation functions are queued before the implementation
  owner: current TS client/server subscription seams and frozen JVM client
  subscription behavior. Both use existing `explorer` function assignments,
  explicitly `gpt-5.6-terra` / `medium`, may not edit or spawn children, and
  must report runtime metadata if exposed. If self-introspection is unavailable,
  the immutable configured profile and limitation are recorded.
- The implementation owner is the existing `implementer`, explicitly
  `gpt-5.6-terra` / `medium`, dispatched only after the orchestrator freezes the
  smallest idiomatic public contract from accepted evidence.

## 2026-07-23 — Investigation and baseline

- Current TypeScript seam explorer completed read-only with the explicitly
  dispatched `explorer`, `gpt-5.6-terra` / `medium` profile. Runtime
  self-introspection was unavailable and no fallback was visible. It confirmed
  separate state/event update branches, equality-only state filters, event
  include-all behavior, a server queue bound of 100, and the reusable private
  T-0058 bounded-stream lifecycle. It made no edits.
- Frozen JVM explorer completed read-only against exact core-java commit
  `a408b0d70dafd603efc55b89c8b4b6f3e8c19d3b`, using the explicitly dispatched
  `explorer`, `gpt-5.6-terra` / `medium` profile. Runtime self-introspection was
  unavailable and no fallback was visible. It confirmed separate state/event
  subscriptions, immutable actor scope, subscribe-before-activate ordering,
  no-longer-matching ID preservation, event context preservation, terminal
  stream errors, fresh resubscription after cancellation, and client-owned
  cleanup. It made no edits.
- Frozen JVM implementation artifacts intentionally excluded: Java classes and
  overload builders, `StreamObserver`/callback APIs, exposed subscription
  collections, static ID helpers, logging handlers, and Java cancellation
  bookkeeping. The accepted TypeScript adaptation is the smallest idiomatic
  schema-driven `AsyncIterable` API.
- Generated Proto verification passed with 39 source checksums and 48 frozen
  descriptors exact. Generated TypeScript build/typecheck passed after adding
  ignored dependency links required by the isolated worktree. The native
  client/server baseline then passed: 8 files and 216 tests. The earlier
  missing-module attempt was classified as worktree preparation, not a source
  regression.
- The public contract is frozen in the task record. One existing `implementer`
  is assigned exclusive ownership of the T-0059 production/test/docs slice,
  explicitly `gpt-5.6-terra` / `medium`; model and reasoning are present in the
  dispatch. The owner must use focused RED/GREEN tests and may not install,
  commit, push, merge, edit protocol disposition records, or spawn children.

## 2026-07-23 — Implementation wave

- The existing `implementer` completed the initial production/test/docs slice
  under the explicitly dispatched `gpt-5.6-terra` / `medium` profile. Runtime
  self-introspection was unavailable and no fallback was visible. The owner
  added public state/event handles, topic construction, immutable decoding,
  bounded lifecycle ownership, exports, focused tests, package documentation,
  and end-user guide examples without installing, committing, pushing, or
  spawning children.
- The first pnpm RED command was blocked before test execution by pnpm's stale
  dependency-policy check, so it is not accepted as behavioral RED evidence.
  The executable local Vitest command passed 30 tests across the new and T-0058
  lifecycle suites; focused typecheck, ESLint, format, and diff checks passed.
- The orchestrator reran the native loopback slice with listener permission:
  3 files and 31 tests passed. The implementation is therefore functionally
  GREEN at its first boundary, but not yet ready for specialist review.
- A pre-review acceptance audit returned one correction batch to the same
  implementation context: schema-bound public generics, fail-closed malformed
  wire decoding, cycle/depth-safe predicate validation, complete listener and
  queue terminal cleanup, race/overflow/declaration/native coverage, and
  self-contained documentation snippets. This is test/completeness hardening,
  not a public-contract expansion.

## 2026-07-23 — Pre-review acceptance

- The same existing implementation context completed the consolidated
  correction batch under the explicitly configured `gpt-5.6-terra` / `medium`
  profile. Runtime self-introspection remained unavailable and no fallback was
  visible. The accepted slice now includes schema-bound state/ID generics,
  typed masks, equality-only cycle/depth-bounded predicate compilation,
  fail-closed protocol decoding, listener/queue cleanup, bounded overflow,
  idempotent cancellation, aggregated close failures, declaration isolation,
  and complete package/end-user subscription guidance.
- Deterministic fake coverage includes state/event order and overflow at item
  33, one consumer/one pending read, pending-Subscribe abort and late cleanup,
  activation failure, explicit/abort/close cancellation, listener accounting,
  dual cleanup failure aggregation, malformed wire input, immutable decoding,
  no-longer-matching IDs, and T-0058 lifecycle regression.
- Native public integration initially exposed and corrected two fixture defects:
  a missing generated Projection-column definition and a primitive ID schema
  mismatch/activation handoff race. The final real-server suite passed 3 tests:
  matching state followed by no-longer-matching, event/context delivery, and
  command/query regression.
- Orchestrator pre-review gates passed: repository generated/tooling typecheck;
  full ESLint plus cleanup enforcement; full Prettier and diff checks; and the
  native client/server regression set with 9 files and 233 tests.
- The complete four-lane review wave is now dispatched. Expected profiles were
  recorded before dispatch: TypeScript/API, style/maintainability, and
  performance/reliability use their existing reviewer roles configured as
  `gpt-5.6-terra` / `high`; documentation uses the immutable existing
  `gpt-5.6-luna` / `medium` role. Every reviewer is read-only and may not spawn
  children.

## 2026-07-23 — Activation-readiness architecture block

- Review Wave 1 required a remote activation-ready creation boundary. A direct
  Connect transport handshake passed fake tests but native verification proved
  that Connect does not resolve the server-stream response until the TS server
  yields its first message; both state and event creation therefore deadlocked.
- Frozen JVM inspection shows `Stand.activate()` produces an internal success
  acknowledgement through an errors-only observer, while `SubscriptionService`
  does not emit that success as a `SubscriptionUpdate`; the JVM client returns
  after starting the async activation call. The current frozen wire therefore
  has no observable remote-ready acknowledgement before the first domain
  update.
- This is a demonstrated public-contract/serialized-stream ambiguity, so the
  existing `requirements_splitter` is dispatched before further production
  edits, explicitly `gpt-5.6-sol` / `high`, read-only, with no child spawning.
  It must choose the smallest parity-preserving TS boundary among local
  attachment semantics, a TS server response-only activation acknowledgement,
  or another evidence-backed option, and classify Wave 3 compatibility impact.
  Runtime metadata will be recorded when exposed; otherwise the immutable
  configured profile and limitation are recorded.
- The existing requirements splitter completed under the explicitly configured
  `gpt-5.6-sol` / `high` profile; runtime self-introspection was unavailable and
  no fallback was visible. It selected local activation-call/consumer
  attachment semantics as the only parity-preserving option. Frozen local/JVM
  subscription Proto sources are byte-identical, and JVM high-level clients
  return after starting async activation rather than awaiting a wire ack.
- The deadlocking raw-transport handshake is removed. The correction retains
  exact subscription/update identity validation, shared bounded lifecycle, and
  deterministic causal tests, while documenting that creation is not a remote
  readiness signal. This requires no Wave 3 shim and introduces no Proto or TS
  server behavior change.

## 2026-07-23 — Review closure

- Review Wave 1 and its focused affected-lane confirmations are closed. Final
  dispositions are CLEAN for TypeScript/API, documentation,
  style/maintainability, and performance/reliability; security is N/A for this
  client-only packet. All expected profiles and runtime-metadata limitations
  are durably recorded in the review record.
- The final orchestrator native/focused boundary passed 4 files and 54 tests,
  including real state/no-longer-matching/event integration. Generated and
  tooling typechecks passed. The task now advances to its required full
  repository verification/coverage/docs/Proto/release gate before acceptance.

## 2026-07-23 — Final acceptance

- The first full gate passed all 2,051 executed tests but exposed a branch
  coverage regression at 89.65% against the unchanged 90% threshold. The same
  implementer added 25 behavior-focused tests only; no production code,
  threshold, exclusion, or protocol contract changed. These tests cover state
  criteria shapes, predicate guards, hostile/malformed wire branches,
  lifecycle terminal paths, tenant scopes, and immutable byte views.
- The independent final `verify:generated` chain passed completely:
  generated/tooling typecheck, ESLint and cleanup enforcement, Prettier, full
  tests, full coverage, TypeDoc/API inventory, Proto lint/checksum/descriptor
  verification, generated-clean, and release readiness.
- Final test evidence: 97 files passed and 3 skipped; 2,077 tests passed and 21
  skipped. Coverage is 94.37% statements, 90.08% branches (6,243/6,930), 94.94%
  functions, and 94.71% lines. Client internals reach 91.78% branches.
- TypeDoc verified 40 intended `@spine-ts/client` exports. Proto evidence is 39
  copied source checksums and 48 normalized frozen descriptors. Release
  readiness verified 16 package imports and 121 relative Markdown links.
- Verification-only dependency symlinks were removed; no dependency contents
  or user files were deleted. T-0059 is accepted for commit, immediate task
  branch push, merge to `main`, post-merge verification, and main push.
