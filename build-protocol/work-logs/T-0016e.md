# T-0016e Work Log

Branch: `task/T-0016e-delivery-worker`

Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0016e-delivery-worker`

Started: `2026-07-08T11:54:09Z`

Baseline commit: `f226fda`

## Entries

- `2026-07-08T11:54:09Z`: Started T-0016e from `main` commit `f226fda`.
  Root `main` had only the user-owned untracked `human-review-1-jul.md`.
- `2026-07-08T11:54:09Z`: Created branch
  `task/T-0016e-delivery-worker` and worktree
  `/Users/armiol/development/experiments/spine-ts/.worktrees/T-0016e-delivery-worker`.
  The first sandboxed attempt failed because Git could not create the branch
  ref lock under `.git/refs`; the escalated retry succeeded under the user's
  explicit worktree permission.
- `2026-07-08T11:58:52Z`: Inspected current Spine JVM delivery sources:
  `Delivery`, `Inbox`, `InboxStorage`, `ShardedMessageDelivery`,
  `TargetDelivery`, `LiveDeliveryStation`, `GroupByTargetAndDeliver`,
  `Conveyor`, `Station`, `DeliveryMonitor`, and `ShardedWorkRegistry`.
  Recorded the implementation impact in the task brief before code changes.
- `2026-07-08T11:58:52Z`: T-0016e scope is a small local delivery worker over
  existing inbox/shard storage: claim one shard, read `TO_DELIVER` rows,
  dispatch through a supplied callback, mark successes delivered, leave
  failures retryable, and release the shard. Catch-up, schedulers, retained
  attempt history, batch listeners, observers, maintenance dispatchers, and
  transport-backed runtime remain out of scope.
- `2026-07-08T13:06:00Z`: Baseline unsandboxed `corepack pnpm verify` passed in
  the T-0016e worktree after dependency installation. Full tests passed with 50
  files and 842 tests. Coverage passed with 94.98% statements, 90.16% branches,
  97.77% functions, and 94.98% lines. TypeDoc completed with the existing
  invalid-`origin` source-link warning, proto lint passed, and generated proto
  outputs were confirmed ignored, untracked, and freshly regenerated.
- `2026-07-08T13:11:11Z`: Implementation sub-agent added the first
  framework-owned direct delivery drain. `Delivery.drain()` now claims one
  shard, reads `TO_DELIVER` rows in order, invokes one supplied callback per
  message, marks successful rows `DELIVERED`, leaves callback failures pending
  for retry, returns simple run statistics, and releases the shard in a
  `finally` path. `InboxStorage.markDelivered()` performs the narrow
  `TO_DELIVER` to `DELIVERED` status update and keeps final dedup guard
  metadata aligned so delivered rows with live `keepUntil` still block
  duplicates.
- `2026-07-08T13:38:21Z`: Review-fix sub-agent addressed first-round findings.
  `InboxStorage.markDelivered()` now validates the caller snapshot against the
  stored row, updates/repairs the dedup guard before advancing the inbox row,
  returns `undefined` for missing, non-pending, or mismatched marker input, and
  tolerates the benign delivered-row/stale-guard duplicate-receive race. The
  drain callback option was renamed from `deliver` to `onMessage`. Public docs
  now describe durable inbox/shard storage plus the local direct drain while
  keeping scheduler/catch-up/transport loops and retained attempt history
  deferred.
- `2026-07-08T13:01:16Z`: Second review-fix sub-agent addressed the remaining
  re-review findings. Duplicate receive now tolerates and repairs the normal
  guard-`DELIVERED`/row-`TO_DELIVER` window left by guard-first
  `markDelivered()` by validating the guard/row identity and CASing the row to
  `DELIVERED`; genuine identity and metadata mismatches still fail closed. The
  architecture docs now state that durable inbox rows, dedup guards, shard
  leases, and local `Delivery.drain()` exist, while scheduler/catch-up,
  transport-backed loops, and retained attempt history remain deferred. The
  review log now records first-round findings, fixes, re-review status so far,
  and this second-pass reliability/docs fix.
- `2026-07-08T13:58:00Z`: Final reliability re-review reported clean. Final
  documentation re-review confirmed public docs were no longer stale and
  requested a final review-log freshness update. The review log now records all
  required lanes as clean.

## Participants

- Implementation sub-agent: added direct shard drain and focused delivery tests.
- Review-fix sub-agent: fixed first-round reliability/security, naming, and
  documentation findings.
- Second review-fix sub-agent: fixed remaining reliability and documentation
  re-review findings without spawning sub-agents.
- Reviewers: first round completed; code style, documentation, TypeScript/API,
  security, and performance/reliability lanes are clean after targeted
  re-review.

## Verification

- Baseline unsandboxed `corepack pnpm verify`: passed. Full tests passed with
  50 test files and 842 tests. Coverage passed with 94.98% statements, 90.16%
  branches, 97.77% functions, and 94.98% lines. TypeDoc completed with the
  existing invalid-`origin` source-link warning, proto lint passed, and
  generated proto outputs were confirmed ignored, untracked, and freshly
  regenerated.
- Focused red check:
  `corepack pnpm test:generated packages/server/test/delivery/delivery-worker.test.ts`
  failed as expected before implementation because `Delivery.drain` did not
  exist.
- Focused green check:
  `corepack pnpm test:generated packages/server/test/delivery/delivery-worker.test.ts`
  passed with 1 test file and 5 tests after implementation.
- Focused delivery check:
  `corepack pnpm test:generated packages/server/test/delivery` passed with 5
  test files and 142 tests after adding bounded-drain and idempotent marker
  coverage.
- Sandboxed `corepack pnpm verify` failed as expected under listener
  restrictions: ZeroMQ local IPC tests failed with `Operation not permitted`,
  gRPC service tests and the example server failed with
  `listen EPERM: operation not permitted 127.0.0.1`, and the affected service
  tests timed out after the listener failures.
- Escalated `corepack pnpm verify` passed. Full tests passed with 51 files and
  849 tests. Coverage passed with 94.87% statements, 90.02% branches, 97.79%
  functions, and 94.86% lines. TypeDoc completed with the existing invalid
  `origin` source-link warning, API export checks passed with 190 server
  exports, proto lint passed, and generated proto outputs were confirmed
  ignored, untracked, and freshly regenerated.
- Review-fix focused red check:
  `corepack pnpm test:generated packages/server/test/delivery/delivery-worker.test.ts`
  failed as expected before implementation updates. Existing tests using the
  renamed `onMessage` option did not dispatch through the old `deliver`
  property, the forged-marker regression proved same-ID trust, and the
  delivered-row/stale-guard duplicate receive raised
  `DeliveryStorageCorruptionError`.
- Review-fix focused green check:
  `corepack pnpm test:generated packages/server/test/delivery/delivery-worker.test.ts`
  passed with 1 test file and 12 tests after the fixes.
- Review-fix focused delivery check:
  `corepack pnpm test:generated packages/server/test/delivery` passed with 5
  test files and 147 tests.
- Review-fix required checks:
  `corepack pnpm typecheck`, `corepack pnpm lint`,
  `corepack pnpm format:check`, and `corepack pnpm docs:check` passed.
  `docs:check` emitted the existing TypeDoc invalid-`origin` source-link
  warning.
- Review-fix sandboxed `corepack pnpm verify` failed under local listener/IPC
  restrictions. ZeroMQ local IPC tests failed with `Operation not permitted`,
  the standalone example failed with `listen EPERM: operation not permitted
127.0.0.1`, and real gRPC `SpineServices` tests timed out after the listener
  failures.
- Review-fix escalated `corepack pnpm verify` passed end to end. Full tests
  passed with 51 files and 854 tests. Coverage passed with 94.84% statements,
  90.01% branches, 97.80% functions, and 94.84% lines. TypeDoc completed with
  the existing invalid-`origin` warning; API export checks passed with 190
  server exports; proto lint passed; and generated proto outputs were confirmed
  ignored, untracked, and freshly regenerated.
- Second review-fix focused delivery worker check:
  `corepack pnpm test:generated packages/server/test/delivery/delivery-worker.test.ts`
  passed with 1 test file and 13 tests.
- Second review-fix focused delivery check:
  `corepack pnpm test:generated packages/server/test/delivery` passed with 5
  test files and 148 tests.
- Second review-fix required checks: `corepack pnpm typecheck` passed;
  `corepack pnpm docs:check` passed with the existing TypeDoc invalid-`origin`
  source-link warning; initial `corepack pnpm format:check` failed on
  `build-protocol/reviews/T-0016e-delivery-worker.md`; `corepack pnpm format`
  normalized the review log; rerun `corepack pnpm format:check` passed.
