# Work Log: T-0012.5 CommandBus, EventBus, And Handler Registration

Status: implementation complete

## Entries

| Time                    | Actor                    | Action                                 | Evidence                                                                                                                                                                                                                                                                                                                                                                        | Result                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------- | ------------------------ | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `2026-07-01 21:52 WEST` | Main orchestrator        | Created task worktree and baseline.    | Created `task/T-0012-5-buses-handler-registration` from parent branch `task/T-0012-cleanup-replan`; installed dependencies; inspected current server handler metadata/decorator code, storage `EventStore`, and task-relevant JVM routing/dispatch sources; ran escalated `env CI=true corepack pnpm verify`.                                                                   | Baseline passed with 32 test files, 294 tests, coverage above 90%, docs/API checks with the existing invalid-`origin` TypeDoc warning only, proto lint/generate, and generated-clean comparison.                                                                                                                                                              |
| `2026-07-01 22:16 WEST` | Implementation sub-agent | Added first bus seam and verification. | Read task docs plus JVM `CommandBus`, `CommandDispatcherRegistry`, `EventBus`, and `EventDispatcherRegistry`; added `packages/server/src/bus/*`, updated server package exports/dependencies/docs, added mirrored bus tests, ran `pnpm test packages/server/test`, `pnpm test packages/storage/test`, `pnpm typecheck:build`, and escalated `env CI=true corepack pnpm verify`. | `CommandBus` and `EventBus` now dispatch generated Spine envelopes asynchronously through small dispatcher contracts; command duplicates are rejected, events append to `EventStore` before deterministic fan-out, package/docs export gates are updated, and full verify passed after rerunning outside the sandbox for the existing ZeroMQ IPC smoke tests. |

## Current State

- Task branch/worktree exists from verified corrective parent commit `746e862`.
- Storage/event-store seam exists.
- Bus implementation and verification are complete.
- No blocking human question is known.
