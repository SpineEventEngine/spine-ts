# T-0093 Work Log

## 2026-08-02 — Task initialization

- Started the standalone gateway host from merged `main` at `ef7912e1` in an
  isolated worktree and branch.
- Classified it high-risk because production listener assembly, exact auth
  routing, backend forwarding, finite request handling, and shutdown change.
- Frozen constraints include durable production subscription bindings, exact
  reserved Spine routes, bounded explicit auth routes, locked JVM descriptor
  fixture evidence without JVM builds, and no general router or health API.
- Requirements splitter dispatch uses the existing role with explicit
  `gpt-5.6-sol` / high. Runtime self-introspection will be recorded when
  exposed; otherwise the immutable configured profile and limitation apply.
- Next: requirements split and checkpoint implementation. Work continues.

## 2026-08-02 — Requirements split accepted

- The existing `requirements_splitter` ran with explicit `gpt-5.6-sol` / high.
  Runtime self-introspection was unavailable; no visible profile mismatch or
  fallback occurred.
- No governing conflict exists. The accepted five-checkpoint split deepens the
  current `Server`/`BrowserServer` path, uses `browser.backend` to select
  standalone mode, keeps ResolveContext local, adds exact bounded auth routes,
  converges cancellation/close, and tightens Envoy plus static JVM evidence.
- The split adds no new runner/router, health API, duplicate namespace field,
  JVM build, storage selector, or Wave 6 behavior.
- Next: one implementation owner starts configuration and mode RED tests, then
  proceeds through the accepted checkpoints. Work continues.

## 2026-08-02 — Implementation checkpoint 1 in progress

- Implementer assignment: existing `implementer` role, explicitly configured
  as `gpt-5.6-terra` / medium by the orchestrator. This surface exposes no
  runtime self-introspection metadata; the immutable dispatched profile is the
  available evidence and no fallback is visible.
- Skill applicability used the session inventory plus
  `build-protocol/skills/EXPECTED_SKILLS.md`, `find ~/.agents/skills -maxdepth
2 -type f -name SKILL.md -print`, and `~/.agents/.skill-lock.json`.
  `test-driven-development` is selected and fully read because every runtime
  change requires a RED/GREEN cycle. The expected planning, worktree, review,
  verification, ADR, TypeScript, and Node skills are installed but are not
  selected for this bounded implementation step: the accepted split and
  isolated worktree already supply their corresponding workflow, and no new
  architecture or dependency choice is being made.
- JVM guardrail: inspected
  `spine-jvm-docs/spine-server-runtime-and-bounded-context.md`, which records
  the relevant `core-jvm/server` `Server`, `CommandService`, `QueryService`,
  and `SubscriptionService` sources. It supports retaining one familiar
  service pipeline and not adding a new router or service family. The local
  JVM source path is not present, so the checked-in research note is the
  available source evidence.
- RED/GREEN evidence so far: invalid standalone backend origin; missing
  production registry; unnamed durable production bindings; missing standalone
  sessions collaborator; and durable namespace exposure. Focused tests pass:
  5 assertions across `server.test.ts` and
  `durable-subscription-bindings.test.ts`; `tsc --noEmit -p
packages/server/tsconfig.json` passes.
- Setup limitation resolved without source changes: fresh worktree dependency
  install used the frozen lock/cache, and deterministic Proto generation
  verified 40 source checksums plus 49 frozen descriptors. Generated outputs
  are transient/ignored; `git status` contains only owned source and test
  edits. Checkpoint 1 remains in progress while mode and remaining
  collaborator/binding validation are added. Checkpoint 1 is now complete:
  standalone mode is selected only by `browser.backend.baseUrl`, rejects an
  invalid backend before assembly, requires its auth/context/clock/fingerprint
  collaborators and explicit local bindings, and requires a registry plus a
  named durable binding registry in production. Combined mode remains on the
  existing native path. Next: prove one external-backend pipeline for the five
  forwarded services while keeping `ResolveContext` local. Work continues.

## 2026-08-02 — Checkpoints 2–5 implementation in progress

- Fresh checkpoint-1 evidence: the retained standalone Post regression and
  server typecheck pass. Post alone does not prove all five external forwarding
  operations, so that matrix remains required.
- RED/GREEN: exact Fetch callback registration initially received the framework
  rejection. The browser host now owns bounded exact auth registrations and
  dispatches only after per-route origin and body limits. Callback and
  fail-closed origin/method/body tests pass with the server typecheck.
- Envoy now renders exact `path` matches for its six fixed descriptors and
  supplied finite auth endpoints. No catch-all backend route is introduced.
  Remaining work: five-RPC/ResolveContext matrix, cancellation convergence,
  documentation, and preflight. Work continues.

## 2026-08-02 — Documentation and compatibility evidence

- `docs:check:generated` passes after recording the public `BrowserAuthRoute`
  export and updating server README/REFERENCE guidance. The TypeDoc inventory
  contains 224 expected server exports.
- Locked static JVM evidence passes without a JVM build or launch: fixture
  checks are 11/11 and wire checks are 4/4. This remains partial static source/
  descriptor evidence; the frozen fixture deliberately reports six unresolved
  transitive wire imports and makes no JVM runtime claim.
- The task-profile build caught two deliberate minimal production-binding test
  doubles that require an `unknown` bridge before `BrowserServerOptions`; the
  behavior tests remain focused on validation and pass. Full five-RPC RED and
  cancellation coverage are still required before review. Work continues.

## 2026-08-02 — Mechanical preflight correction

- The scoped task preflight reported 21 affected-file ESLint violations in the
  browser host and its server regressions. The correction adds no behavior:
  explicit callback statements, safe body-byte typing, a typed optional-view
  for runtime collaborator validation, and non-unbound test calls.
- Fresh affected-file ESLint is clean; the server typecheck and complete server
  host suite pass 76/76. Formatting and diff checks are rerun before push.

## 2026-08-02 — Cleanup-rule correction

- The cleanup checker required the browser-host lifecycle callback parameter to
  use the established `on...` naming convention. Renamed `beginDrain` to
  `onDrain` consistently without changing close/drain behavior; focused
  cleanup, lint, typecheck, server tests, formatting, and diff checks follow.

## 2026-08-02 — Public route TSDoc correction

- The TSDoc gate required complete summaries and handler parameter/return tags
  for `BrowserAuthRoute`. Documentation now describes every public field and
  its cancellation-aware handler contract without changing runtime behavior.

## 2026-08-02 — Scoped browser-host coverage

- Full existing server tests plus T-0093 auth-route regressions cover the
  changed browser host above every metric threshold: 223/230 lines (96.96%),
  152/166 branches (91.57%), and 34/35 functions (97.14%). The final branch
  was the allowed exact auth OPTIONS preflight, which returns 204 with exact
  CORS fields and never calls application code.

## 2026-08-02 — Aggregate auth admission

- Browser auth dispatch now has a finite aggregate active-request admission
  bound (default 64, configurable by `maxActiveAuthRequests`). Excess work
  receives the existing fixed 503 before handler invocation; capacity returns
  after a request completes. The focused recovery regression passes.

## 2026-08-02 — Narrow re-review correction

- Auth responses now buffer within `writeMaxBytes` and the route deadline
  before committing application status or headers. Overflow returns fixed 413
  without application headers. One abort signal races handler, request intake,
  and response reads; it cancels the active response reader and removes its
  listener at settlement.
- RED/GREEN evidence: a never-ending response stream now observes cancellation
  and returns 504; an unfinished chunked upload now returns 504 without
  invoking its handler. Both focused regressions and server typecheck pass.
- Added `pnpm test:envoy`: Node renderer checks plus a pinned
  `envoyproxy/envoy:v1.38.3@sha256:5f7c43e1147412fdb3af578c651c67478a3df818eae89d2261e707e06c209cdb`
  `--mode validate` test using generated TLS. The first harness run correctly
  failed because it rendered host TLS paths inside the container; it is fixed
  to render `/run/tls/*` and the pinned validation now passes 4/4.
- Documentation now distinguishes combined-mode implicit bindings from every
  standalone mode's explicit binding requirement, records auth admission and
  response bounds, and qualifies JVM evidence with its six unresolved imports.
