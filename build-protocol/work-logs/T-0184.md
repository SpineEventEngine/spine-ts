# T-0184 Work Log

## 2026-08-14 - Task Start

- Created high-risk T-0184 task, review, and work records before production
  edits in isolated worktree `.worktrees/T-0184-todo-interface-proof` at
  baseline `aed2f194`.
- Frozen example scope: `TaskListId`; list-aware task state; assign/reassign/
  unassign events; generated `TaskEvent`; authored `TaskAssignmentEvent`; exact
  `TaskReassigned` two-target precedence; durable replay with no reroute.
- Commands have no interface markers; `TaskReassignmentEvent`, `routeSemantic`,
  `@Route`, multiple-Gateway behavior, and serialized-format changes are out of
  scope.
- Existing `implementer` assignment is explicit `gpt-5.6-terra` / medium.
  Desktop runtime telemetry is unavailable; the immutable configured profile is
  the available evidence.
- Next: inspect the current To-Do model and tests, then write focused behavior
  RED evidence before changing production or generated-model sources.

## 2026-08-14 - WIP Blocked Checkpoint

- Installed the frozen workspace dependencies in the fresh worktree; the first
  format check could not run before that bootstrap.
- Added `interface-routing-contract.test.ts` first. RED evidence:
  `pnpm exec vitest run examples/todo/test/interface-routing-contract.test.ts`
  failed `2/2`: missing file-level TaskEvent options and missing application
  token routing.
- Added the narrow To-Do model/application proof work: `TaskListId`, list-aware
  CreateTask/Task state/events, assignment lifecycle commands/events,
  TaskAssignee projection, generated `TaskEvent`, authored
  `TaskAssignmentEvent`, a single TaskList token route, shared assignee token
  routing, and exact TaskReassigned two-target routing. Commands remain
  unmarked and no `TaskReassignmentEvent` was introduced.
- GREEN evidence: the focused contract test passed `1` file / `2` tests.
- Direct `pnpm -C examples/todo proto:generate` succeeds and emits the two
  interface files with generated/provenance comments and no copyright header.
- Blocker 1, reproduced by root `pnpm proto:generate`: atomic `stageModel()`
  copies only package/Proto configuration into a temporary package. Authored
  interface discovery therefore reports that `tsconfig.json` is missing. The
  staged location also makes To-Do's `../../tsconfig.base.json` extend path
  invalid, so copying `tsconfig.json`/`src` without an architecture correction
  is not safe.
- Blocker 2, reproduced after direct generation plus the normal companion and
  handler post-steps: `pnpm -C examples/todo exec tsc --noEmit` fails with
  `TS9013` in generated `interfaces/task-event.ts` and
  `interfaces/task-assignment-event.ts`; their inferred `memberSchemas` tuples
  are not declaration-safe under `isolatedDeclarations`.
- These are shared proto-workflow/T-0181 generator prerequisites outside this
  task's examples-only ownership. No shared file was changed. Formatting,
  focused ESLint, and diff checks passed after the WIP changes. Full preflight,
  coverage, integration proof, review, and `verify:task` are pending.

## 2026-08-14 - Prerequisite Superseded

- Historical blockers 1 and 2 are resolved by T-0184A, integrated, tagged, and
  post-merge verified at `0bacb0b3`. Its live source-view transaction resolves
  staged authored/configuration discovery and its exact tuple emission resolves
  `isolatedDeclarations`.
- T-0184 is resumed. Existing WIP behavior remains to be rebuilt and verified
  through focused tests, coverage, preflight, native proofs, and `verify:task`.

## 2026-08-14 - Resumption Verification RED

- T-0184A prerequisites are confirmed green: root `pnpm proto:generate`,
  generated build, and To-Do `tsc --noEmit` pass. A local Proto quality comment
  was corrected for the new TaskAssignee state.
- Full focused To-Do test selection is RED while legacy tests are migrated from
  scalar task-list IDs to `TaskListId` and from four to seven commands. Typed
  query, fixture, and smoke updates are in progress.
- The substantive integration RED remains: a valid CreateTask is accepted but
  the TaskList projection stays empty under the generated `TaskEvent` route.
  The runtime token includes and is identity-equal to `TaskCreatedSchema`, so
  this is not an interface-generation mismatch. Continue tracing projection
  delivery before claiming behavior GREEN.

## 2026-08-14 - Aggregate Admission Root Cause And GREEN

- Systematic tracing retained an exact-schema route alongside generated
  `TaskEvent`; both failed while token membership and handler schema identity
  were correct, ruling out route selection.
- A direct `validateEntityStateTransition()` reproduction showed the first
  Task update violated `task_list_id`: set-once fields cannot change after the
  framework-created aggregate state. CreateTask legitimately establishes the
  required list ID after that initial state is created.
- RED: the focused transition regression was invalid while the field retained
  `(set_once)`. GREEN: removing only that option, regenerating Proto outputs,
  and rebuilding made the transition test pass. The normal generated TaskEvent
  route now populates TaskList; a temporary exact TaskCreated probe did too and
  was removed after it established routing was not causal.
- Evidence: root `pnpm proto:generate`; `pnpm typecheck:build:generated`;
  `pnpm -C examples/todo exec tsc --noEmit`; focused contract `3/3`; focused
  black-box TaskEvent routing `1/1`; the temporary exact probe passed before
  removal.
- Continue: run the wider To-Do suite, complete the assignment/replay target
  matrix, and then measure changed example-production coverage.

## 2026-08-14 - Typed-ID And Rejection Compatibility GREEN

- Migrated the legacy black-box assertions, topic filters, diagnostics, and
  catch-up fixtures from scalar list IDs to `TaskListId` values.
- TaskList keeps its one generated `TaskEvent` route. Exact TaskAlreadyDone and
  TaskNotDone routes translate their legacy TaskId payload to the corresponding
  sample TaskListId, preserving rejection subscription behavior for this
  example's one-task-per-list fixtures.
- Focused interface-contract plus black-box suite is GREEN: `2` files / `36`
  tests. Next: add the assignment/reassignment/unassignment target and durable
  replay behavior proof.

## 2026-08-14 - Assignment Target And Replay GREEN

- Added a black-box lifecycle journey: CreateTask produces zero assignee
  targets; AssignTask produces one; exact TaskReassigned moves the task from
  the previous assignee to the new assignee; UnassignTask clears the final
  target.
- `catchUpReadSide()` then rebuilds from the four stored events and restores
  both assignee views as empty. It is a deliberate clear-and-rebuild path, not
  durable Inbox retry; no-reroute Inbox behavior remains covered by the
  established repository routing regression.

## 2026-08-14 - Durable Projection Replay GREEN

- RED investigation: a throwing TaskList handler is acknowledged by delivery,
  leaving its row `DELIVERED`; handler failure is not a restartable retry
  semantic.
- GREEN: one live TaskCreated admission invokes the generated TaskEvent route
  once. `DeliveryBuilder` persists a separate pending `UPDATE_SUBSCRIBER` row
  in the shared backend. A fresh context starts through BlackBox, materializes
  that row, and leaves its throwing TaskEvent callback at zero calls.
- Focused evidence: `pnpm exec vitest run examples/todo/test/black-box.test.ts
-t 'replays a persisted projection Inbox target' --reporter=verbose` passes
  (`1/1`).
- The example cannot import `@spine-event-engine/storage`: Vitest reports that
  the package is not declared/resolvable from `examples/todo`. The test uses
  the built workspace-storage entrypoint by relative path so its factory and
  backend share the runtime identity used by the built server package.

## 2026-08-14 - Compile-Convergence Preflight

- Assignment remains the existing `implementer` role, explicitly configured as
  `gpt-5.6-terra` / medium. Desktop telemetry does not expose actual runtime
  model metadata; the immutable configured profile remains the available evidence.
- Initial RED: `pnpm typecheck:tooling` failed only in legacy To-Do test typing:
  omitted `TaskListId`, incompatible exact-optional callback, string-ID
  `TaskListView` rows, unchecked generated optional fields, and three scalar
  TaskList fixture IDs.
- Corrected only assigned test files: imported `TaskListId`, retained the route
  callback's optional `| undefined` contract, use generated `TaskList` messages,
  guard optional IDs, and create server fixture IDs with `TaskListIdSchema`.
  No runtime or production source changed. `pnpm typecheck:tooling` exits 0,
  and the six requested Todo suites exit 0.
- ESLint and `git diff --check` exit 0. `lint:cleanup` and `lint:tsdoc` remain
  RED only on pre-existing, unassigned `examples/todo/src/index.ts` findings.
  Formatting was corrected in the assigned multi-process test; final formatting
  and coverage evidence remain pending.
- Prior branch coverage was RED at 86.54%, below the task's 90% requirement.
  Current LCOV is pending the one converged coverage-enabled `verify:task` run;
  no coverage tests are being invented in this compile-only correction.

## 2026-08-14 - Source Preflight Correction

- Extended this bounded correction to `examples/todo/src/index.ts` after the
  full cheap preflight identified its lifecycle handlers as the cleanup/TSDoc
  blockers. Required explicit generated event return types now use `*Event`
  aliases, preserving the handler invariant and generated-event provenance.
- Added canonical multi-line TSDoc descriptions for new aggregate and projection
  handlers. The two first-member blocks use the local `prettier-ignore`
  blank-line convention; the copyright header remains unchanged.
- Formatting, TSDoc before and after formatting, cleanup, full ESLint, tooling
  typecheck, and `git diff --check` are green. The individual black-box suite
  remains pending before the checkpoint and one coverage-enabled task gate.

## 2026-08-14 - Superseded Coverage Invocation

- The first coverage-enabled `verify:task` invocation was cancelled before
  acceptance because its source list was non-authoritative: it named raw
  `examples/todo/src/index.ts` and omitted `examples/todo/src/process.ts`.
  It provides no coverage evidence. The corrected authoritative run uses
  compiled `examples/todo/dist/src/index.js`, smoke helpers, and process helpers.
