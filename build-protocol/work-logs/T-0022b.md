# T-0022b Work Log

Status: round-six fixes verified; re-review pending

Branch: `task/T-0022b-process-manager-event-inbox-handoff`

Worktree:
`.worktrees/T-0022b-process-manager-event-inbox-handoff`

Started: `2026-07-10T00:21:00Z`

Baseline commit: `eccbbb1`

## Entries

- `2026-07-10T00:21:00Z`: Started T-0022b planning after T-0022a integration
  and post-merge full verification. Root `main` was clean except for the
  user-owned untracked `human-review-1-jul.md`.
- `2026-07-10T00:21:00Z`: Requirements-splitting sub-agent
  `019f4930-c363-7b31-ae00-0b103599ba85` selected process-manager event reactor
  inbox handoff as the next smallest non-blocked event endpoint slice.
- `2026-07-10T00:22:00Z`: Inspected task-relevant JVM docs and source before
  implementation: `spine-routing-dispatch-and-delivery.md`,
  `spine-entities-repositories-and-state.md`,
  `/private/tmp/spine-ts-core-jvm-t0022/ProcessManagerRepository.java`,
  `/private/tmp/spine-ts-core-jvm-t0022/Inbox.java`, and
  `/private/tmp/spine-ts-core-jvm-t0022/InboxOfEvents.java`. Recorded parity:
  `ProcessManagerRepository.dispatchTo(ids, event)` sends each routed ID through
  `inbox().send(event).toReactor(id)`, which stores `REACT_UPON_EVENT` rows; only
  `CATCH_UP` changes event inbox status to `TO_CATCH_UP`.
- `2026-07-10T00:34:00Z`: Implementation sub-agent selected applicable skills
  from the exposed session inventory and fully read `implement`,
  `test-driven-development`, and `verification-before-completion` before
  task actions governed by them. Checked
  `build-protocol/skills/EXPECTED_SKILLS.md`, enumerated readable
  `~/.agents/skills/*/SKILL.md` entrypoints with
  `find /Users/armiol/.agents/skills -maxdepth 2 -type f -name SKILL.md -print`,
  and inspected `/Users/armiol/.agents/.skill-lock.json`. Skipped
  `using-git-worktrees` because the orchestrator already provided the worktree,
  `requesting-code-review`/`review` because this implementation session cannot
  run the required independent reviewer sub-agent loop itself, and planning
  skills because the task brief already supplied the implementation plan.
- `2026-07-10T00:35:00Z`: Added red tests for local process-manager
  `REACT_UPON_EVENT` handoff, durable PM event row before handler invocation
  amid older backlog, duplicate concurrent live PM event handoff, and invalid
  stored PM event rows for wrong target ID/type, malformed payload, and wrong
  label.
- `2026-07-10T00:37:00Z`: Implemented the narrow JVM-familiar PM event inbox
  handoff. `LocalProcessManagerInbox` now accepts `REACT_UPON_EVENT` rows for
  registered process-manager targets. Process-manager repositories register an
  inbox target for command or event endpoints, live PM event dispatch writes a
  `REACT_UPON_EVENT` row with the original `Event` envelope, original event ID,
  PM state type URL plus routed PM ID, `TO_DELIVER`, `ShardIndex.single()`, and
  the local dedup window, then drains only that exact row. Stored PM event
  replay validates tenant, payload, target type URL, and routed target ID before
  handler invocation and preserves state/event/command behavior through the
  existing PM execution path.
- `2026-07-10T00:42:00Z`: Updated runtime/API/package/user documentation to
  describe PM event `REACT_UPON_EVENT` durable local inbox handoff and remove PM
  event reactors from the deferred delivery list. Aggregate event
  reactors/importers, projection catch-up through inbox storage, generic
  repository delivery, retries, schedulers, transport workers, and retained
  attempt history remain deferred.
- `2026-07-10T00:43:00Z`: Replacement implementation worker audited the existing
  uncommitted runtime/test/doc diff against the T-0022b acceptance criteria
  instead of broadening it. Confirmed the worktree already contains the narrow
  JVM-familiar handoff shape: live PM event routing writes
  `REACT_UPON_EVENT` rows through the local process-manager inbox, replays only
  the exact row target, and keeps the existing PM execution path for state,
  events, commands, tenants, and failures.
- `2026-07-10T00:44:00Z`: Final verification passed in this worker session:
  focused PM handoff/routing tests, `@spine-ts/server` package tests, and
  `docs:check`. Added a decision-log supersession note so the T-0022a
  historical consequence about direct PM event execution does not read as
  current runtime behavior.
- `2026-07-10T00:48:00Z`: Implementation sub-agent
  `019f4938-a90d-7c81-9d23-960815e1d2cb` was closed by the orchestrator after
  remaining active without a report and without visible task commits in the
  worktree. The replacement implementation sub-agent work recorded above was
  preserved and committed separately in this task branch.
- `2026-07-10T01:12:00Z`: Replacement implementation sub-agent
  `019f4942-5088-7471-a964-3316c3dfc7f7` committed the process-manager event
  inbox handoff and documentation reconciliation in `9bea82d`.
- `2026-07-10T01:29:00Z`: Round-one reviewers completed. Security and
  performance/reliability were clean. Code style, documentation, and
  TypeScript/API docs requested fixes for duplicated event inbox reading,
  overlong identifiers, stale direct-event-bus prose, missing safety-contract
  documentation, and stale internal inbox JSDoc. The review log records the
  detailed findings.
- `2026-07-10T01:40:00Z`: Round-one fix worker consolidated the duplicated
  process-manager/projection inbox-event reader in
  `packages/server/src/repository/repository.ts`, replaced the
  `fromBinary(...toBinary(...))` copy path with generated `Any` rebuilding
  before unpacking, and shortened the new PM event helper names to the local
  `Pm` forms.
- `2026-07-10T01:44:00Z`: Round-one fix worker updated the touched repository
  routing test helpers to shorter names, corrected stale API/package/runtime/
  user doc prose, documented the replay safety contract before handler code,
  moved the task status to `in review/fixes`, and added the implementation
  report stub for this slice.
- `2026-07-10T01:49:00Z`: Round-one fix verification passed in this worker
  session: `pnpm --config.verify-deps-before-run=false exec vitest run
  packages/server/test/context/process-manager-handoff.test.ts
  packages/server/test/repository/repository-routing.test.ts` passed with 2
  files and 135 tests, `pnpm --config.verify-deps-before-run=false --filter
  @spine-ts/server test` exited 0, and
  `pnpm --config.verify-deps-before-run=false docs:check` exited 0 with the
  existing invalid-`origin` TypeDoc warning only.
- `2026-07-10T01:52:00Z`: Round-two reviewers completed after the review-fix
  commit. TypeScript/API docs and security were clean. Code style requested one
  remaining name fix, documentation found impossible durable-log chronology and
  an incomplete implementation-report file list, and performance/reliability
  found a high-severity multi-target handoff gap: a failing earlier target can
  prevent later routed process-manager event targets from being written to the
  durable inbox.
- `2026-07-10T02:00:00Z`: Round-two fix worker added a red regression in
  `packages/server/test/repository/repository-routing.test.ts` for routed
  multi-target process-manager event handoff. The failing baseline proved the
  earlier routed replay stopped the later target before its durable inbox row
  could be observed.
- `2026-07-10T02:08:00Z`: Round-two fix worker shortened
  `validateProcessManagerReplayTenant` to `validatePmReplayTenant` and narrowed
  the reliability fix in `packages/server/src/repository/repository.ts`.
  Multi-target PM event handoff now writes every routed inbox row first, then
  replays those exact rows in order. This stayed closer to the JVM intent than
  `Promise.all(...)`: the later rows become retryable durable inbox work
  without changing failure propagation or depending on concurrent shard-drain
  races.
- `2026-07-10T02:12:00Z`: Round-two fix worker corrected the impossible
  round-one fix timestamps in this work log, updated
  `build-protocol/tasks/T-0022b-process-manager-event-inbox-handoff/IMPLEMENTATION_REPORT.md`
  to `2026-07-10`, and expanded the implementation-report changed-file list to
  include the previously omitted task files.
- `2026-07-10T02:18:00Z`: Round-two fix verification passed in this worker
  session. The focused process-manager handoff/routing suite passed with 2
  files and 136 tests, `pnpm --config.verify-deps-before-run=false --filter
  @spine-ts/server test` exited 0, and
  `pnpm --config.verify-deps-before-run=false docs:check` exited 0 with the
  existing invalid-`origin` TypeDoc warning only.
- `2026-07-10T02:34:00Z`: Round-three reviewers completed. Security and
  performance/reliability were clean. Code style requested renaming the new
  `createSplitRoutePmRepo` test helper, documentation requested the review
  summary table be brought forward to the latest lane state, and TypeScript/API
  docs requested narrowing the internal process-manager inbox input/message
  contract instead of accepting any inbox label/status at compile time.
- `2026-07-10T02:39:00Z`: Applied round-three fixes: renamed the split-route
  regression helper to `createSplitPmRepo`, added narrow internal
  process-manager inbox input/message aliases for `HANDLE_COMMAND` and
  `REACT_UPON_EVENT` `TO_DELIVER` rows, and updated the review summary table.
- `2026-07-10T02:46:00Z`: Round-four reviewers completed. Code style, security,
  and performance/reliability were clean. Documentation requested the work log
  and implementation-report status be brought forward from round-two wording.
  TypeScript/API docs requested narrowing process-manager replay message status
  to `TO_DELIVER` instead of only narrowing the label.
- `2026-07-10T02:52:00Z`: Applied round-four fixes: moved the implementation
  worker entries back into chronological order, updated current work/report
  status wording, and narrowed process-manager replay messages to
  `TO_DELIVER` before dispatching them to the internal replay target.
- `2026-07-10T02:58:00Z`: Round-four fix verification passed. The focused
  process-manager handoff/routing suite passed with 2 files and 136 tests,
  `pnpm --config.verify-deps-before-run=false --filter @spine-ts/server test`
  exited 0, `pnpm --config.verify-deps-before-run=false docs:check` exited 0
  with the existing invalid-`origin` TypeDoc warning only, and
  `git diff --check` exited 0.
- `2026-07-10T03:06:00Z`: Round-five reviewers completed. TypeScript/API docs,
  security, and performance/reliability were clean. Code style requested three
  lint fixes: unused imports, unnecessary `async`, and an unsafe matcher shape.
  Documentation requested the review-log status/table and the stalled-worker
  handoff wording be made current.
- `2026-07-10T03:10:00Z`: Applied round-five fixes: removed the unused
  Protobuf binary imports, made `SplitRouteProcessManager.reactTask()`
  synchronous, replaced the nested matcher with direct row inspection, updated
  the review-log status/table through round five, and clarified the stalled
  worker handoff wording.
- `2026-07-10T03:17:00Z`: Round-five fix verification passed.
  `pnpm --config.verify-deps-before-run=false lint:generated`,
  `pnpm --config.verify-deps-before-run=false exec vitest run
  packages/server/test/context/process-manager-handoff.test.ts
  packages/server/test/repository/repository-routing.test.ts`,
  `pnpm --config.verify-deps-before-run=false --filter @spine-ts/server test`,
  `pnpm --config.verify-deps-before-run=false docs:check`, and
  `git diff --check` all exited 0. `docs:check` reported only the existing
  invalid-`origin` TypeDoc warning.
- `2026-07-10T03:26:00Z`: Round-six reviewers completed. Code style,
  TypeScript/API docs, security, and performance/reliability were clean except
  for a high reliability finding: multi-target process-manager event prewrite
  used a local version counter starting at `1n` per dispatch, weakening
  same-millisecond retry ordering compared with the local inbox version
  allocator. Documentation also noted the review-log status/table needed to be
  brought forward from round five.
- `2026-07-10T03:31:00Z`: Applied round-six fixes: moved multi-target prewrite
  into `LocalProcessManagerInbox.receiveAll()` so it uses the same monotonic
  `#takeVersion()` allocator as other local PM inbox writes, and updated the
  review log through round six.
- `2026-07-10T03:38:00Z`: Round-six fix verification passed.
  `pnpm --config.verify-deps-before-run=false lint:generated`,
  `pnpm --config.verify-deps-before-run=false exec vitest run
  packages/server/test/context/process-manager-handoff.test.ts
  packages/server/test/repository/repository-routing.test.ts`,
  `pnpm --config.verify-deps-before-run=false --filter @spine-ts/server test`,
  `pnpm --config.verify-deps-before-run=false docs:check`, and
  `git diff --check` all exited 0. `docs:check` reported only the existing
  invalid-`origin` TypeDoc warning.

## Files Changed

- `packages/server/src/context/process-manager-handoff.ts`
- `packages/server/src/repository/repository.ts`
- `packages/server/test/context/process-manager-handoff.test.ts`
- `packages/server/test/repository/repository-routing.test.ts`
- `build-protocol/RUNTIME_ARCHITECTURE.md`
- `build-protocol/DEVELOPER_API.md`
- `packages/server/README.md`
- `docs/USER_GUIDE.md`
- `docs/api/README.md`
- `build-protocol/DECISION_LOG.md`
- `build-protocol/reviews/T-0022b-process-manager-event-inbox-handoff.md`
- `build-protocol/work-logs/T-0022b.md`
- `build-protocol/tasks/T-0022b-process-manager-event-inbox-handoff/TASK.md`
- `build-protocol/tasks/T-0022b-process-manager-event-inbox-handoff/IMPLEMENTATION_REPORT.md`

## Verification

- `pnpm install`: passed in the fresh worktree; reused lockfile packages with
  no downloads.
- Initial focused baseline `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  failed because ignored generated/dist artifacts were absent in the new
  worktree. Ran `pnpm --config.verify-deps-before-run=false proto:generate` and
  `pnpm --config.verify-deps-before-run=false exec tsc -b --force` to
  materialize ignored generated/build outputs.
- `pnpm --config.verify-deps-before-run=false lint:generated`: passed after
  `proto:generate`; `tsc -b`, ESLint, and cleanup enforcement passed.
- Focused baseline rerun passed: process-manager handoff and repository routing
  tests passed with 2 files and 131 tests.
- Red verification after adding tests:
  `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  failed as expected with 4 failures: local PM inbox rejected
  `REACT_UPON_EVENT`, direct PM event dispatch did not expose a pending inbox
  row before handler invocation, duplicate live PM event dispatch invoked the
  handler twice, and event-only PM repositories did not expose a replay target.
- Green focused verification:
  `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  passed with 2 files and 135 tests.
- Final verification rerun:
  `pnpm --config.verify-deps-before-run=false exec vitest run packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  passed with 2 files and 135 tests.
- `pnpm --config.verify-deps-before-run=false --filter @spine-ts/server test`:
  passed in this worktree.
- `pnpm --config.verify-deps-before-run=false docs:check`: passed. TypeDoc
  reported one expected warning about missing valid git remote `origin`, wrote
  temporary generated outputs plus `docs/api/reference`, and the API docs check
  confirmed the expected exported symbol counts for all packages.
- Round-two red-green-verification trail:
  - `pnpm --config.verify-deps-before-run=false exec vitest run packages/server/test/repository/repository-routing.test.ts -t "writes later process-manager event inbox rows when an earlier routed target fails"`
    failed first with a timeout waiting for the later routed target to appear,
    proving the sequential handoff gap.
  - The same targeted command passed after the narrow PM multi-target handoff
    fix.
  - `pnpm --config.verify-deps-before-run=false exec vitest run packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
    passed with 2 files and 136 tests.
  - `pnpm --config.verify-deps-before-run=false --filter @spine-ts/server test`
    exited 0.
  - `pnpm --config.verify-deps-before-run=false docs:check` exited 0 with the
    existing invalid-`origin` TypeDoc warning only.
