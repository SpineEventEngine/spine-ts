# T-0022b Work Log

Status: in progress

Branch: `task/T-0022b-process-manager-event-inbox-handoff`

Worktree:
`.worktrees/T-0022b-process-manager-event-inbox-handoff`

Started: `2026-07-10T00:21:00Z`

Baseline commit: `eccbbb1`

## Entries

- `2026-07-10T00:21:00Z`: Started T-0022b planning after T-0022a integration
  and post-merge full verification. Root `main` was clean except for the
  user-owned untracked `human-review-1-jul.md`.
- `2026-07-10T00:21:00Z`: Requirements-splitting sub-agent
  `019f4930-c363-7b31-ae00-0b103599ba85` selected process-manager event reactor
  inbox handoff as the next smallest non-blocked event endpoint slice.
- `2026-07-10T00:22:00Z`: Inspected task-relevant JVM docs and source before
  implementation: `spine-routing-dispatch-and-delivery.md`,
  `spine-entities-repositories-and-state.md`,
  `/private/tmp/spine-ts-core-jvm-t0022/ProcessManagerRepository.java`,
  `/private/tmp/spine-ts-core-jvm-t0022/Inbox.java`, and
  `/private/tmp/spine-ts-core-jvm-t0022/InboxOfEvents.java`. Recorded parity:
  `ProcessManagerRepository.dispatchTo(ids, event)` sends each routed ID through
  `inbox().send(event).toReactor(id)`, which stores `REACT_UPON_EVENT` rows; only
  `CATCH_UP` changes event inbox status to `TO_CATCH_UP`.
- `2026-07-10T00:48:00Z`: Implementation sub-agent
  `019f4938-a90d-7c81-9d23-960815e1d2cb` was closed by the orchestrator after
  remaining active without a report and without visible task commits in the
  worktree. A narrower replacement implementation sub-agent will continue the
  same task from the unchanged branch state.
- `2026-07-10T00:34:00Z`: Implementation sub-agent selected applicable skills
  from the exposed session inventory and fully read `implement`,
  `test-driven-development`, and `verification-before-completion` before
  task actions governed by them. Checked
  `build-protocol/skills/EXPECTED_SKILLS.md`, enumerated readable
  `~/.agents/skills/*/SKILL.md` entrypoints with
  `find /Users/armiol/.agents/skills -maxdepth 2 -type f -name SKILL.md -print`,
  and inspected `/Users/armiol/.agents/.skill-lock.json`. Skipped
  `using-git-worktrees` because the orchestrator already provided the worktree,
  `requesting-code-review`/`review` because this implementation session cannot
  run the required independent reviewer sub-agent loop itself, and planning
  skills because the task brief already supplied the implementation plan.
- `2026-07-10T00:35:00Z`: Added red tests for local process-manager
  `REACT_UPON_EVENT` handoff, durable PM event row before handler invocation
  amid older backlog, duplicate concurrent live PM event handoff, and invalid
  stored PM event rows for wrong target ID/type, malformed payload, and wrong
  label.
- `2026-07-10T00:37:00Z`: Implemented the narrow JVM-familiar PM event inbox
  handoff. `LocalProcessManagerInbox` now accepts `REACT_UPON_EVENT` rows for
  registered process-manager targets. Process-manager repositories register an
  inbox target for command or event endpoints, live PM event dispatch writes a
  `REACT_UPON_EVENT` row with the original `Event` envelope, original event ID,
  PM state type URL plus routed PM ID, `TO_DELIVER`, `ShardIndex.single()`, and
  the local dedup window, then drains only that exact row. Stored PM event
  replay validates tenant, payload, target type URL, and routed target ID before
  handler invocation and preserves state/event/command behavior through the
  existing PM execution path.
- `2026-07-10T00:42:00Z`: Updated runtime/API/package/user documentation to
  describe PM event `REACT_UPON_EVENT` durable local inbox handoff and remove PM
  event reactors from the deferred delivery list. Aggregate event
  reactors/importers, projection catch-up through inbox storage, generic
  repository delivery, retries, schedulers, transport workers, and retained
  attempt history remain deferred.

## Files Changed

- `packages/server/src/context/process-manager-handoff.ts`
- `packages/server/src/repository/repository.ts`
- `packages/server/test/context/process-manager-handoff.test.ts`
- `packages/server/test/repository/repository-routing.test.ts`
- `build-protocol/RUNTIME_ARCHITECTURE.md`
- `build-protocol/DEVELOPER_API.md`
- `packages/server/README.md`
- `docs/USER_GUIDE.md`
- `docs/api/README.md`
- `build-protocol/work-logs/T-0022b.md`

## Verification

- `pnpm install`: passed in the fresh worktree; reused lockfile packages with
  no downloads.
- Initial focused baseline `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  failed because ignored generated/dist artifacts were absent in the new
  worktree. Ran `pnpm --config.verify-deps-before-run=false proto:generate` and
  `pnpm --config.verify-deps-before-run=false exec tsc -b --force` to
  materialize ignored generated/build outputs.
- `pnpm --config.verify-deps-before-run=false lint:generated`: passed after
  `proto:generate`; `tsc -b`, ESLint, and cleanup enforcement passed.
- Focused baseline rerun passed: process-manager handoff and repository routing
  tests passed with 2 files and 131 tests.
- Red verification after adding tests:
  `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  failed as expected with 4 failures: local PM inbox rejected
  `REACT_UPON_EVENT`, direct PM event dispatch did not expose a pending inbox
  row before handler invocation, duplicate live PM event dispatch invoked the
  handler twice, and event-only PM repositories did not expose a replay target.
- Green focused verification:
  `pnpm --config.verify-deps-before-run=false vitest run packages/server/test/context/process-manager-handoff.test.ts packages/server/test/repository/repository-routing.test.ts`
  passed with 2 files and 135 tests.
- Final required verification lanes pending.
