# T-0147 Work Log

## Start

- Started: 2026-08-09T13:44:23+01:00.
- Branch: `task/T-0147-storage-contract`.
- Worktree:
  `/Users/armiol/development/experiments/spine-ts/.worktrees/T-0139-inbox-shards`.
- Baseline: `341e2d52`.
- Authoring owner: `/root`, single implementation owner on the Desktop
  orchestration surface. The configured main profile is `gpt-5.6-sol` /
  `medium`; runtime self-introspection is not exposed. No child was dispatched
  because the active session instruction reserves subagents unless requested.
- Classification: high-risk public/serialized storage-contract preparation.
- The existing linked worktree was detected through distinct Git worktree and
  common directories. No new worktree was created.

## Plan Review

- The human resolved the only material design choice: physical JVM
  interoperability is mandatory.
- The accepted sequence remains T-0147 common contracts, T-0148 MySQL, T-0149
  Datastore, and T-0150 atomic shared-runtime/release convergence.
- T-0147 is deliberately non-releasable and may expose exact downstream
  handoff failures; compatibility aliases and dual physical layouts are
  forbidden.

## Skill Applicability

- Session inventory source: Desktop-provided available-skills catalog.
- Project inventory sources:
  `build-protocol/skills/EXPECTED_SKILLS.md`, bounded enumeration with
  `find ~/.agents/skills -maxdepth 2 -type f -name SKILL.md -print`, and
  `~/.agents/.skill-lock.json`.
- Selected and fully read:
  - `architecture-patterns`: JVM-shaped provider ports and DDD storage
    boundaries;
  - `architecture-decision-records`: durable cross-runtime layout decision;
  - `executing-plans`: execution of the reviewed T-0146 plan;
  - `using-git-worktrees`: verified the already-isolated required worktree;
  - `test-driven-development`: mandatory RED/GREEN production changes.
- `subagent-driven-development` is not selected because the active session
  instruction reserves subagents unless the human requests them.
- `typescript-advanced-types` is deferred until a concrete public type problem
  requires its specialized guidance; ordinary generated-schema typing remains
  governed by current code and focused compiler tests.
- `nodejs-backend-patterns`, CQRS, event-store, and projection skills are not
  selected because this checkpoint changes storage adapter contracts rather
  than HTTP/server, CQRS, event-store, or projection responsibilities.
- Skill instructions are advisory and remain subordinate to the repository
  protocol and human ledger.

## Evidence Guardrails

- JVM evidence already inspected and frozen by T-0146 includes current
  `Identifier`, `Stringifiers`, `RecordSpec`, `RecordColumn`, `ColumnMapping`,
  `JdbcColumnMapping`, `IdColumn`, `DsColumnMapping`, `RecordId`, and query
  predicate/filter adapters.
- The implementation will use the existing `@bufbuild/protobuf` dependency for
  generated schemas, Any, binary serialization, and Proto JSON. It will not
  invent a second Protobuf runtime or generic serialization framework.
- Before production code, focused tests must fail for missing typed column,
  identifier/stringifier, tenant-boundary/catalog, and mapping behavior.

## Current State

- Task framing and durable human-requirements ledger are being committed before
  runtime edits.
- Immediate next action: establish the focused storage-package baseline and
  add the first failing common-contract tests.
