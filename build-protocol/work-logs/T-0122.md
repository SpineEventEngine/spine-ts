# T-0122 Work Log

## 2026-08-06 - Task opened

- T-0121 is integrated, post-merge focused verification passed, and
  `origin/main@d3aee369` is the accepted baseline.
- Classification: high-risk due to durable encoding, concurrent stream
  reconciliation, cancellation, restart, and shared Gateway assembly.
- Existing implementer assignment is recorded before dispatch with explicit
  `gpt-5.6-terra` / `medium`; subagents may not spawn subagents.
- Next: inspect current fixed fan-in/binding ownership and write focused RED
  tests for dynamic add/remove/races, zero-node recovery, v4 identity, and
  40-node bounded fan-in before production changes.

## 2026-08-06 - RED/GREEN evidence in progress

- Implementer assignment accepted as the existing `implementer` role with the
  explicitly dispatched `gpt-5.6-terra` / `medium` profile. This surface does
  not expose runtime self-introspection; configured role/profile is the
  available metadata.
- RED command after generating/building ignored workspace output:
  `pnpm typecheck:build:generated && pnpm exec vitest run packages/auth/test/dynamic-subscription-creator.test.ts`.
  It failed as expected because `DynamicSubscriptionCreator` was absent.
- The first build preparation attempt failed before source evaluation because
  this worktree lacked ignored generated Protobuf output. `pnpm proto:generate`
  restored that prerequisite; this is baseline worktree state, not task code.
- GREEN implementation is intentionally not accepted yet: the same focused
  command now demonstrates that an activation can race node removal before the
  test fixture observes every initial native stream. The next change must make
  per-generation stream-start acknowledgement and removal fencing explicit,
  then broaden tests to durable v4 identity, zero-node recovery, cancellation,
  and 40-node bounds before modifying the durable registry.
- The exploratory production/test draft was removed with `apply_patch` after a
  second focused command timed out: its local lifecycle model waited on
  controller aborts and could not safely acknowledge a membership transition.
  No unverified runtime code remains in the task tree.

## 2026-08-06 - Audit correction checkpoint

- Coordinator audit identified that the earlier checkpoints did not yet satisfy
  the implementation packet's logical-definition and shared-owner lifecycle
  contract. Work resumed in the same implementer context.
- GREEN evidence: `pnpm typecheck:build:generated && pnpm exec vitest run
packages/auth/test/subscriptions/index.test.ts
packages/server/test/server/durable-subscription-bindings.test.ts` passed
  176 focused tests after exposing final reservation IDs.

## 2026-08-06 - Demonstrated architecture blocker

- The RED attempt established that the v3 durable topology/envelope record,
  native activation fan-in, and T-0121 membership owner form one coordinated
  seam. A minimal bridge cannot safely acknowledge starts and removals without
  first separating logical definition persistence from ephemeral node streams.
- Per protocol, the existing requirements splitter is dispatched once with
  explicit `gpt-5.6-sol` / `high` to produce the bounded implementation packet.
  It may not edit code, spawn agents, create new tasks, or expand scope.

## 2026-08-06 - Architecture packet accepted

- The splitter retained one T-0122 implementation assignment and made
  `DynamicUnaryForwarder` the sole generation owner for node clients and
  ephemeral subscription children.
- Durable v4 stores canonical logical definitions only. Backend envelopes,
  node indexes, and topology remain ephemeral and are removed from durable
  identity.
- Cancel tombstones desired state before cleanup awaits; node removal never
  deletes the definition; fixed and dynamic membership share the same owner.
- The exact mutation ordering, RED sequence, deletion list, and safe
  checkpoints are persisted in
  `build-protocol/planning/T-0122_IMPLEMENTATION_PACKET.md`.
- Runtime self-introspection was unavailable. The explicit configured
  requirements-splitter profile `gpt-5.6-sol` / `high` is accepted evidence;
  no mismatch was visible.

## 2026-08-06 - Logical binding boundary checkpoint

- The binding boundary now persists `PublicSubscriptionWire` definitions rather
  than private backend envelopes. Capacity reservation provides the final
  logical ID before native creation, and the Gateway builds that exact
  canonical Subscription wire before it requests native children.
- Durable v4 records now encode the canonical definition and the in-memory
  binding path follows the same logical callback contract. Backend envelopes
  remain at the node-native creation, activation, cancellation, and
  compensation seam only.
- Evidence: `pnpm typecheck:build:generated` passed; `pnpm vitest run
packages/auth/test/subscriptions/index.test.ts
packages/auth/test/dynamic-subscription-creator.test.ts
packages/server/test/server/durable-subscription-bindings.test.ts` passed
  177 tests.
- Runtime self-introspection remains unavailable. The existing implementer
  dispatch explicitly used `gpt-5.6-terra` / `medium`, with no visible profile
  mismatch.
- Next: finish the owner's generation-fenced child cleanup/retry behavior and
  replace the remaining legacy direct-test adapters with canonical-definition
  fixtures before review.

## 2026-08-06 - Canonical binding cutover

- Removed the temporary backend-envelope compatibility adapters. Binding
  creation, activation, cancellation, in-memory cleanup, and durable cleanup
  now accept only canonical `PublicSubscriptionWire` values.
- Migrated focused direct helpers and durable record fixtures to definitions;
  no v3 backend field is read by the durable registry.
- Added retained per-child controllers, activation promises, tokens, and node
  incarnations. Node removal and Gateway close first fence routing, abort and
  join child work, then cancel native children before releasing clients.
- Evidence: `pnpm typecheck:build:generated` passed and the focused
  subscription/durable/dynamic suite passed 177 tests.
- Next: add the packet's full stale-completion, cleanup-retry, zero-node, and
  forty-node lifecycle coverage, then run the full affected-package preflight.

## 2026-08-06 - Activation and child-fencing checkpoint

- Logical Subscribe now installs native children without starting update
  streams. Public Activate marks the definition active and starts each current
  child exactly through the same generation owner.
- Each child start retains a controller until installation. Cancel and close
  tombstone desired state before awaiting reconciliation, abort pending starts,
  fence installed children, and retain failed native cleanup for a later
  reconciliation retry.
- Evidence: `pnpm typecheck:build:generated` passed; the focused auth/server
  subscription suite passed 180 tests.

## 2026-08-06 - Dynamic lifecycle race evidence

- New membership generations abort retained child starts before the owner moves
  to the latest snapshot. This fences delayed starts during A/B/C churn.
- Focused dynamic tests now cover inactive Subscribe, explicit Activate,
  reordered membership replay, zero-node retention and recovery, cancellation
  and close during delayed starts, all-forty-node fan-in, and a measured child
  start concurrency bound.
- Evidence: dynamic lifecycle tests passed 11 cases; the combined focused
  dynamic unary, binding, and durable registry suites remain green.

## 2026-08-06 - API and documentation alignment

- Updated the agent reference contracts to describe dynamic subscription
  membership, zero-node recovery, and durable canonical-definition storage.
  The references no longer describe a fixed-only subscription topology or
  durable backend-envelope persistence.
- Deterministic scans find no legacy callback, topology-fencing, v3, or
  positional fan-in contract in the binding paths. Remaining backend envelope
  occurrences are limited to the explicitly permitted ephemeral native child
  seam and compensation path.

## 2026-08-06 - Final deterministic lifecycle coverage

- Added an explicit deferred add/Activate race: public activation may arrive
  while the native child is still being created, but it starts exactly once
  after installation.
- Retained the existing zero-node recovery proof, including re-entry of the
  same node ID, and the explicit rejection for a brand-new definition when no
  backend node exists.
- Added the unexpected-completion proof: a completed child is removed, a
  replay creates one replacement, and a later identical replay does not create
  a duplicate replacement.
- Focused lifecycle verification passed 13 tests. The task is ready for the
  affected-package preflight and review wave; no release-complete claim is
  made before those gates.

## 2026-08-06 - Review correction checkpoint: logical child admission

- Introduced the logical subscription coordinator boundary between the public
  Gateway and per-node native subscription creators. Logical creation no
  longer fabricates a backend envelope from public definition bytes.
- Per-node envelope bounds now apply immediately after each native Subscribe
  response. An oversized child is cancelled and the logical creation fails
  without retaining its definition.
- A deterministic dynamic-owner regression test proves oversized child
  compensation. Focused dynamic lifecycle tests passed 14 cases.

## 2026-08-06 - Review correction checkpoint: activation lifetime

- Logical activation now remains open until its admitted downstream signal is
  aborted. That signal fences pending child creation and aborts every installed
  native activation stream before the public activation completes.
- The dynamic regression suite proves an update is relayed while activation is
  still pending, then completes only after the downstream abort. Existing
  lifecycle fixtures now cleanly own and terminate every active stream.
- Evidence: focused dynamic lifecycle tests passed 15 cases.
