# T-0101 Work Log

Task: `build-protocol/tasks/T-0101-beginner-readmes/TASK.md`

## 2026-08-03 — Framing and skill applicability

- Baseline: `origin/main` at `df9234f1`.
- Primary checkout is coordination-only and intentionally dirty with one
  user-owned `README.md` edit and two protected human-review files. Work is
  isolated in `.worktrees/T-0101-beginner-readmes`.
- `.worktrees/` is ignored by the root `.gitignore`. No native worktree tool was
  exposed, so the repository-standard `git worktree` fallback was used. The
  `using-git-worktrees` skill was fully read after worktree creation; no content
  edit preceded its read, and the resulting isolation conforms to the skill.
- Classification: Standard documentation. The task spans every README and
  changes public technical explanations, so it exceeds a micro task but does
  not cross a runtime, contract, persistence, concurrency, or security boundary.
- Canonical skill inventory used the session-provided catalog, the full
  `find ~/.agents/skills -maxdepth 2 -type f -name SKILL.md` listing,
  `build-protocol/skills/EXPECTED_SKILLS.md`, and the expected entries in
  `~/.agents/.skill-lock.json` (version 3). All expected entries are present.
- Selected and fully read skills:
  - `doc-coauthoring`: governs context transfer, section refinement, and fresh
    reader testing. The human already supplied audience, impact, ordering, and
    format constraints, so no non-blocking questionnaire is needed.
  - `using-git-worktrees`: governs isolation and cleanup.
  - `subagent-driven-development`: governs bounded ownership, concise handoff,
    review, and continuous execution. Repository task/work/review logs replace
    the skill's `.superpowers` scratch ledger because the repository previously
    removed that tooling and the build protocol is authoritative.
  - `requesting-code-review`: reinforces review before merge; the repository's
    concern-specific reviewer roles and severity loop remain authoritative.
  - `verification-before-completion`: requires fresh evidence before any pass
    or completion claim.
- Relevant-looking skills skipped:
  - `planning-with-files`: project task/work/review records already provide the
    persistent plan and recovery ledger.
  - `architecture-decision-records`: no architecture decision changes; diagrams
    document implemented topology.
  - `cqrs-implementation`, `projection-patterns`, and `domain-modeling`: no
    domain/runtime implementation changes; current code and existing docs are
    the factual sources.
  - `accessibility` and `web-quality-audit`: no web UI changes.
- Baseline tests were not repeated before documentation edits. Recent verified
  `main` evidence exists, and `BUILD_PROTOCOL.md` explicitly avoids a baseline
  full gate for documentation-only work; final `verify:task --no-tests` will
  provide fresh task evidence.
- Initial inventory: 34 repository READMEs. The historical literal `MessageBoard` appears in active
  human guides, agent references, architecture/user guides, and historical
  protocol records. Product prose will become “Message Board”; exact symbols,
  paths, package names, and literal evidence remain exact.

## Planned ownership and profiles

- README inventory audits: read-only documentation functions using the existing
  immutable `documentation_reviewer` role, configured
  `gpt-5.6-luna` / `medium`. Runtime self-introspection may be unavailable and
  will be recorded honestly.
- Implementation owner: existing `implementer`, explicitly
  `gpt-5.6-terra` / `medium`; sole writer for Markdown and protocol records.
- Formal documentation reviewer: existing immutable role,
  `gpt-5.6-luna` / `medium`.
- TypeScript/API and performance/reliability reviewers, if activated by the
  final changed claims: existing roles, explicitly `gpt-5.6-terra` / `high`.
- Fresh-reader verification: separate read-only documentation-reviewer
  instance on immutable `gpt-5.6-luna` / `medium`.
- Next: collect three independent README audit inventories, aggregate one
  authoring brief, and dispatch the sole implementation owner.

## 2026-08-03 — README audit convergence

- Three disjoint read-only audits completed on the existing immutable
  `documentation_reviewer` role configured as `gpt-5.6-luna` / `medium`.
  Runtime self-introspection was unavailable in every audit; no role/profile
  mismatch or inherited fallback was visible.
- Message Board/root/user-guide audit: all 15 scoped guides need product-name
  spacing. The primary example guide also needs Deployment last, a larger
  production context, and diagrams/prose for browser gRPC-Web or Connect,
  gateway authentication, private/native gRPC, authoritative queries,
  best-effort subscription hints, and combined versus standalone deployment.
  All existing relative links passed.
- Package audit: 15 of 18 package READMEs are already complete and receive
  no-change dispositions. Only link labels in client-react, proto-tools, and
  server need product-name spacing. Current code examples, claims, limits, and
  REFERENCE links were checked against package source and metadata.
- Remaining README audit: build-protocol, JVM compatibility, API, architecture,
  and JVM-research READMEs are appropriately scoped and need no structural
  change. Orders, Projects, and To-Do need a concrete behavior/architecture
  walkthrough and explained code example before tests/limits. Their links pass.
- The explicit human naming rule is broader than beginner product guides.
  Therefore unqualified product prose in authored planning, completion, task,
  review, and work-log Markdown will also become “Message Board”. Exact
  identifiers (`MessageBoardApplication`, `MessageBoardApp`, and similar),
  package/path/domain slugs, literal former UI/output evidence, and quoted
  historical requirements remain unchanged when spacing would make the record
  false.
- Sources the writer must inspect before describing topology:
  `examples/message-board/REFERENCE.md`, app entrypoints and deployment config,
  web board configuration/synchronization/view code, Compose/Kubernetes YAML,
  and the client-web transport factory. No runtime implementation changes are
  authorized.
- Complete authoring batch: Message Board family and active guides; Orders,
  Projects, and To-Do journeys; three package labels; authored Markdown naming;
  the durable README-learning rule; and this inventory. One implementer owns
  all writes to keep vocabulary and diagrams coherent.

## 2026-08-03 — Authoring complete and README dispositions

- The primary Message Board README now teaches purpose, local start, the
  browser/native-gRPC architecture, Aggregate-to-Projection flow, tests and
  local limits before its final Deployment section. Its local and production
  Mermaid diagrams are grounded in the application, React synchronization, and
  Compose/Kubernetes sources. `Build local images` is its final subsection.
- Orders, Projects, and To-Do now each introduce an actual command-to-read-model
  flow diagram and a short snippet copied from their current Aggregate sources
  before tests and limits. No APIs were invented.
- Changed READMEs: root; Message Board root, app, model, web, deployment, and
  container guides; Orders, Projects, To-Do, Envoy; client-node, client-react,
  proto-tools, and server. The three package-label-only changes are
  client-react, proto-tools, and server; client-node changes only the natural
  sample text `Hello, Message Board.` as directed.
- No-change READMEs: `build-protocol`, JVM compatibility, API, architecture,
  `packages/auth`, client-node (except its directed sample), client-web, core,
  delivery-client, delivery-client e2e fixture, delivery-server, proto,
  proto/proto, server fixture Proto, storage-datastore, storage-rdbms, storage,
  testing, transport, and JVM research. They already state audience and link to
  their references where applicable, or are intentionally narrow fixture and
  research guides with no Message Board product prose.
- Remaining README inventory dispositions: Message Board references changed for
  naming only; the 34-README audit is complete with every README either changed
  above or deliberately retained as already gradual/narrow.
- Runtime metadata self-introspection is not exposed to this implementer. The
  dispatch recorded the immutable `implementer` profile as `gpt-5.6-terra` /
  `medium`; no visible fallback or role mismatch occurred.
- Next: complete the historical/protocol naming classification, then run the
  requested focused Markdown, link, formatting, and release-readiness checks.

## 2026-08-03 — Focused mechanical verification

- Passed: `git diff --check`; changed-Markdown code-fence and Mermaid-fence
  balance checks; `node scripts/check-doc-audience.mjs`; and the relative
  Markdown-link portion of `node scripts/check-release-readiness.mjs` (280
  links inspected before its package-export phase).
- `pnpm format:check` first stopped at the dependency freshness guard; its
  documented bypass then reached the formatter but failed because this isolated
  worktree has no `node_modules` and therefore no `prettier` executable.
- `pnpm --config.verify-deps-before-run=false verify:task --no-tests` could not
  run after the same missing-dependency condition. Release readiness likewise
  reports pre-existing missing `dist/` package exports because the worktree has
  not been built; no Markdown link failure was reported.
- Naming correction: all historical prose now uses “Message Board.” Remaining
  matches are only code identifiers, the exact historical T-0086 requirement
  literal, and exact backticked prior output evidence.
- Final correction evidence passed: `git diff --check`, changed-Markdown code
  and Mermaid fence-balance checks, and `node scripts/check-doc-audience.mjs`.
  The final naming scan contains only `MessageBoardApplication`,
  `MessageBoardApp`, and `MessageBoardDeployment` identifiers;
  `MessageBoard local server ready ...` and earlier received-title strings as
  literal output evidence; and the T-0086 and T-0101 historical requirement
  literals that state the former spelling. No historical narration remains.

## 2026-08-03 — Pre-review verification and dispatch

- Installed dependencies from the frozen lockfile; supply-chain policy passed
  and no tracked dependency file changed.
- `pnpm proto:generate && pnpm typecheck:generated` passes. Generation verified
  40 Spine Proto checksums, example Proto quality, and 49 frozen descriptors;
  both the project build and tooling typecheck pass.
- `pnpm docs:check:generated`, `pnpm proto:check-generated:current`, and
  `pnpm check:release-readiness` pass. Evidence covers every expected TypeDoc
  export, ignored/untracked generated output, 67 package imports, 44 package
  assets, and 280 relative Markdown links.
- Fresh independent checkpoint checks also pass: `pnpm format:check`,
  `git diff --check`, documentation audience separation, and balanced code/
  Mermaid fences in all 44 changed Markdown files.
- Lightweight status/docs lint found no stale active task status, generated or
  review scratch, future-behavior overclaim, or unclassified product-name use.
- Review dispatch:
  - documentation completeness and teaching quality: existing immutable
    `documentation_reviewer`, `gpt-5.6-luna` / `medium`;
  - TypeScript/API snippets and package claims: existing
    `typescript_api_docs_reviewer`, `gpt-5.6-terra` / `high`;
  - topology, subscription, lifecycle, storage, and deployment claims: existing
    `performance_reliability_reviewer`, `gpt-5.6-terra` / `high`.
    Runtime metadata limitations will be recorded from each result. Style/
    maintainability remains N/A because no production structure changed.
- Queued next task from the human: after T-0101 is fully closed, create T-0102
  so a successful Message Board post clears only the message text and preserves
  the username. It remains outside this documentation diff and requires focused
  UI tests.
- Next: collect the complete three-lane review wave, return one accepted finding
  batch to the existing implementation owner, then run fresh-reader testing.
- Final README factual polish restores every persisted and emitted field in the
  Aggregate snippet, distinguishes the application image from the standalone
  gateway image, makes the delivery-server Mermaid node topology-neutral, and
  includes the required clean-checkout `pnpm typecheck:build` image-build step.
- Final focused checks passed: `pnpm format:check` (after formatting the one
  affected historical planning file), `git diff --check`, code/Mermaid fence
  balance, and `node scripts/check-doc-audience.mjs`.

## 2026-08-03 — Complete review wave and accepted correction

- Documentation completeness: clean. The immutable `documentation_reviewer`
  profile is `gpt-5.6-luna` / `medium`.
- Performance/reliability: clean. The immutable
  `performance_reliability_reviewer` profile is `gpt-5.6-terra` / `high`.
- TypeScript/API: one accepted P2. The new `@Assign` snippets were real method
  bodies but did not say they were excerpts, which could make beginners treat
  them as paste-ready files. Each now names and links to its Aggregate source
  and explicitly omits imports and the class declaration; all other API review
  findings are clean. The immutable `typescript_api_docs_reviewer` profile is
  `gpt-5.6-terra` / `high`.
- Runtime self-introspection was unavailable in all review lanes; the immutable
  configured profiles above are the available evidence and no mismatch was
  visible. Style/maintainability is N/A because this task changes no production
  code structure.
- Review scratch cleanup: an untracked `.superpowers` review package was
  created for the wave and removed afterward. The repository does not use that
  helper and no scratch artifact remains.

## 2026-08-03 — Correction re-review and reader-test dispatch

- The corrected TypeScript/API concern returns to the existing immutable
  `typescript_api_docs_reviewer`, configured as `gpt-5.6-terra` / `high`.
- Independent beginner-reader testing uses a fresh immutable
  `documentation_reviewer`, configured as `gpt-5.6-luna` / `medium`. The reader
  receives only the public README set and realistic newcomer questions, not
  this work log or the implementation history.
- Both dispatches state their expected model and reasoning explicitly. Runtime
  self-introspection will be recorded if exposed; otherwise the immutable role
  profile and that limitation remain the acceptance evidence.

## 2026-08-03 — Re-review and independent reader result

- TypeScript/API re-review is clean. All four handler blocks are unambiguously
  excerpts, disclose omitted imports and class declarations, and link to their
  complete Aggregate sources.
- Independent beginner-reader testing is clean. From public READMEs alone, the
  reader could identify prerequisites and startup commands; explain commands,
  Aggregates, events, Projections, queries, subscriptions, the browser gateway,
  and native gRPC path; distinguish authoritative re-query from best-effort
  live hints; compare combined and standalone deployment; and follow every
  example. All links in the five requested entry-point READMEs resolve.
- The configured profiles were explicit (`typescript_api_docs_reviewer`,
  `gpt-5.6-terra` / `high`; fresh `documentation_reviewer`, `gpt-5.6-luna` /
  `medium`). Neither surface exposes independent runtime self-introspection;
  no visible mismatch or fallback occurred.
- Next: run the selected final `verify:task --no-tests` profile once on the
  converged tree, then record closure and integrate.

## 2026-08-03 — Final branch verification

- `pnpm verify:task --no-tests` passes on the converged task branch.
- The gate covered generated Proto integrity, build and tooling typechecking,
  ESLint, cleanup and TSDoc rules, formatting, API documentation, Buf lint,
  generated-output cleanliness, package imports/assets, and Markdown links.
- Tests are intentionally omitted because the task changes only authored
  Markdown and protocol records, with no runtime, contract, dependency,
  generated-source, or manifest change.
- All applicable review concerns are resolved. Next: merge the reviewed tree
  into the unchanged `origin/main`, verify the merged tree, and push `main`.
