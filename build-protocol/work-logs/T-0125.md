# T-0125 Work Log

## 2026-08-07 - Task start and implementation dispatch

- T-0124 is durably complete at `origin/main@6b2e29ea`; T-0125 starts from that
  exact endpoint in its isolated worktree.
- Classification: high-risk. The accepted Wave 7 plan already resolves DNS,
  TTL, identity, readiness, scale-to-zero, and package-boundary decisions, so
  the frozen wave architecture pass is not repeated.
- Existing role: implementer. Expected explicit profile:
  `gpt-5.6-terra` / `medium`. Ownership is limited to
  `packages/deployment-gke/**` and the minimum proven standalone Server assembly
  hook. No other production writer may overlap.
- Both model and reasoning are explicit in dispatch. Runtime self-introspection
  may be unavailable; the immutable configured role/profile is accepted only
  when no visible mismatch or fallback occurs.
- RED/GREEN coverage must prove every DNS/TTL/failure/cancellation case, full
  40-address routing and subscriptions, disappearance/reuse fencing, and
  scale-zero recovery before specialist review.

## 2026-08-07 - Coverage completion handoff

- The original implementer pushed runtime and integration checkpoints through
  `ba3720c9`: DNS/TTL behavior, 40-node bounded routing, scale-zero recovery,
  durable subscription child removal/re-addition, and direct Node A/AAAA plus
  ENOTFOUND/ENODATA resolver behavior. Focused tests pass 10/10.
- Source-scoped coverage correctly exposed remaining untested cancellation,
  non-name resolver failures, validation, and lifecycle branches. This is a
  bounded test/completion task, not an architectural blocker.
- A fresh existing `implementer` role owns only this remaining completion pass.
  Expected explicit profile: `gpt-5.6-terra` / `medium`; runtime
  self-introspection may be unavailable, and no visible mismatch or fallback is
  acceptable.

## 2026-08-07 - Implementation checkpoint

- The configured implementer role/profile remains `gpt-5.6-terra` / `medium`.
  This surface exposes no independent runtime metadata; no visible fallback or
  profile mismatch occurred.
- RED evidence: the first focused DNS-discovery test failed on the absent
  `packages/deployment-gke` entrypoint. GREEN evidence follows implementation:
  focused package tests pass after standard generated Proto preparation.
- Dependency decision: use Node's maintained `node:dns/promises` Resolver with
  TTL records and `Resolver.cancel()` instead of a new DNS package; the
  injectable resolver preserves deterministic tests and alternate integration.

## 2026-08-07 - Dynamic Gateway checkpoint

- Added focused integration coverage that delivers GKE DNS snapshots into the
  existing `DynamicUnaryForwarder`. Forty addresses are each selected by unary
  routing while starts remain bounded at four. A successful answer, empty
  answer, and later success prove backend unavailability and routing recovery.
- Fresh focused evidence at this checkpoint: package ESLint passes and the
  discovery suite has eight passing cases. The task preflight previously passed
  after its generated-proto, build, and tooling checks.
- Remaining acceptance coverage: durable native subscription children, address
  reuse fencing against stale completion, and the full converged release gate.

## 2026-08-07 - Coverage completion: resolver and discovery lifecycle

- Existing role: implementer. The task dispatch explicitly configured
  `gpt-5.6-terra` / `medium`; this surface does not expose independent runtime
  metadata, and no visible fallback or profile mismatch occurred.
- RED: direct pre-aborted Node DNS resolution did not call `Resolver.cancel()`.
  GREEN: the adapter now cancels and rejects immediately before beginning either
  address-family lookup. The focused discovery suite passes 19/19 and also
  covers in-flight cancellation, non-name DNS failure propagation, invalid
  configuration rejection, duplicate watch/close idempotence, fallback-validity
  expiry with exactly one empty snapshot, and stalled-resolution close fencing.
- The invalid host configuration test accepts the underlying `URL` validation
  error: it proves rejection without changing the established shared TLS
  validator's error contract.
- Focused task-gate typechecking then found mutable test fixtures declared as
  readonly, including snapshot collectors in the pre-existing discovery cases.
  Their declarations now express mutable collectors with readonly published
  entries. Fresh `pnpm typecheck:tooling` succeeds, and the discovery suite
  remains 19/19.
- Source-scoped coverage initially had 80.95% functions because the default
  scheduler cancellation closure was unexercised, despite statements, branches,
  and lines already exceeding 90%. A focused default-scheduler shutdown test
  now proves that lifecycle path. Fresh scoped coverage is 96.73% statements,
  94.02% branches, 90.47% functions, and 98.75% lines (20/20 tests), without
  exclusions or threshold changes.
- Package ESLint then identified an unnecessary-condition false positive around
  the post-resolution abort check. The Node adapter now uses
  `AbortSignal.throwIfAborted()` after cancelling a pre-aborted resolver and
  after both address-family promises settle; this preserves the tested abort
  behavior while satisfying static analysis. Focused tests (20/20), package
  ESLint, and `pnpm typecheck:tooling` are fresh and clean.

## 2026-08-07 - Completion validation boundary

- Fresh source-scoped coverage after the cancellation change remains above the
  required threshold: 96.70% statements, 93.84% branches, 90.47% functions,
  and 98.75% lines (20/20 discovery tests).
- TSDoc enforcement, package ESLint, tooling typecheck, API-documentation
  validation, audience validation, scoped Prettier, and `git diff --check` are
  clean. Global `pnpm format:check` is blocked only by the untouched
  `build-protocol/tasks/T-0125-gke-dns-discovery/TASK.md`.
- Package metadata and TypeScript snippet tests are not clean because their
  shared fixed package/context lists omit the newly created
  `packages/deployment-gke` package and README. Those shared script test and
  documentation-context files are outside this completion pass's assigned
  ownership, so this is returned as a concrete integration blocker rather than
  silently broadening the change.

## 2026-08-07 - Package/tooling integration completion

- The orchestrator confirmed the three deterministic integration items are
  within T-0125 package/tooling scope. Added `deployment-gke` to the fixed
  workspace-package expectation and strict TypeScript-snippet context registry,
  then formatted the task record. The GKE README already has a typed public
  declaration, an agent-reference link, and package `files` coverage, so no
  prose or manifest correction was necessary.
- The configured implementer role/profile remains `gpt-5.6-terra` / `medium`.
  Runtime self-introspection remains unavailable; no visible fallback or
  profile mismatch occurred.
- Fresh focused evidence: package metadata and snippet suites pass 14/14;
  strict snippet checking, changed-file Prettier, `git diff --check`, package
  ESLint, tooling typecheck, and TSDoc enforcement are clean. Prettier's
  established preservation marker keeps mandatory TSDoc blank lines intact.
- Stable affected `verify:task` was invoked with the discovery test and both
  discovery sources. This surface stops streaming after the build/typecheck
  prefix, so the verifier's remaining deterministic gates were re-run directly:
  full ESLint, cleanup, TSDoc, audience, API documentation, Buf lint,
  generated-output cleanliness, release-readiness, global Prettier, and diff
  checks are clean. Fresh exact scoped coverage reports 20/20 tests and 96.70%
  statements, 93.84% branches, 90.47% functions, and 98.75% lines.

## 2026-08-07 - Specialist review correction batch

- Existing implementer ownership resumed from accepted review batch
  `71b38e0b`. The dispatch remains explicitly configured as
  `gpt-5.6-terra` / `medium`; runtime self-introspection is unavailable, with
  no visible fallback or profile mismatch.
- RED evidence: five new discovery/resolver cases failed for fatal-family DNS
  cancellation, close promise coalescing, equivalent IPv6 spelling identity,
  expiry/retry during a stalled lookup, and empty-answer/failure suppression.
  GREEN evidence: the focused suite passes 25/25 after canonical endpoint
  construction precedes ID/deduplication; close has one shared promise; expiry
  is independently scheduled; retries continue after expiry; fatal DNS
  cancellation reaches the sibling; and known empty membership is not
  republished on failure.
- The fake scheduler now removes cancelled callbacks and proves close cancels
  a real scheduled tick. The package-internal 32-node operational threshold is
  retained without a cap or public diagnostic; focused evidence proves all 40
  returned nodes are retained. `DnsLookup` is exported and documented as the
  intentional Node DNS test-injection contract.
- README assembly now includes a copyable standalone `Server.atPort`,
  `browser.discovery`, and `run()` example without task-roadmap jargon. The
  task includes the mandatory human-imposed requirements ledger, and the review
  record received its mechanical formatting correction.
