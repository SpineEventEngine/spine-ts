# T-0125 Work Log

## 2026-08-07 - Task start and implementation dispatch

- T-0124 is durably complete at `origin/main@6b2e29ea`; T-0125 starts from that
  exact endpoint in its isolated worktree.
- Classification: high-risk. The accepted Wave 7 plan already resolves DNS,
  TTL, identity, readiness, scale-to-zero, and package-boundary decisions, so
  the frozen wave architecture pass is not repeated.
- Existing role: implementer. Expected explicit profile:
  `gpt-5.6-terra` / `medium`. Ownership is limited to
  `packages/deployment-gke/**` and the minimum proven standalone Server assembly
  hook. No other production writer may overlap.
- Both model and reasoning are explicit in dispatch. Runtime self-introspection
  may be unavailable; the immutable configured role/profile is accepted only
  when no visible mismatch or fallback occurs.
- RED/GREEN coverage must prove every DNS/TTL/failure/cancellation case, full
  40-address routing and subscriptions, disappearance/reuse fencing, and
  scale-zero recovery before specialist review.

