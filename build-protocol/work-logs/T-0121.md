# T-0121 Work Log

## 2026-08-06 - Task Opened

- Baseline `origin/main@fa1ed36f` contains the reviewed Wave 7 plan.
- Classification: high-risk due to public API, concurrency, cancellation, and
  shared Gateway routing changes.
- Selected skills read by the orchestrator: `using-git-worktrees`,
  `executing-plans`, `subagent-driven-development`, and
  `test-driven-development`.
- Implementation profile: existing implementer, explicitly dispatched
  `gpt-5.6-terra` / `medium`; subagents may not spawn subagents.
- Next: implementation owner writes focused RED tests before production code.

## 2026-08-06 - RED: discovery contract

- Added `packages/deployment/test/discovery.test.ts` before production code.
- `pnpm exec vitest run packages/deployment/test/discovery.test.ts` failed as
  expected: `Cannot find module '../src/index.js'`. The test specifies canonical
  endpoint/TLS behavior and complete static snapshots.

## 2026-08-06 - RED: dynamic unary routing

- Added `packages/auth/test/dynamic-unary-forwarder.test.ts` before runtime
  code. `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts`
  failed as expected because the new deployment package was not yet declared as
  an auth dependency. The test specifies zero-node rejection/recovery,
  round-robin use, 40-node non-capping, and no retry after dispatch.

## 2026-08-06 - GREEN: dynamic unary routing

- `pnpm exec tsc -b packages/deployment packages/auth` passed.
- `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts`
  passed (2 tests). The runtime has a serialized membership reconciler, retains
  equal node endpoints, disposes removed/replaced clients, round-robins current
  unary clients, rejects at zero nodes, and sends no retry after dispatch.
- Generated `dist/` and `tsconfig.tsbuildinfo` outputs remain ignored and are
  not staged.

## 2026-08-06 - RED: membership churn and bounded starts

- Extended the dynamic unary test with A/B/C coalescing and a 40-node,
  concurrency-two start bound. The focused command failed as expected: the
  original serialized queue created B, and starts were sequential (`peak 1`).

## 2026-08-06 - GREEN: membership churn and bounded starts

- `pnpm exec tsc -b packages/deployment packages/auth` passed.
- `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts`
  passed (4 tests). A single running owner retains only the latest pending
  complete snapshot; client creation happens in finite bounded batches.

## 2026-08-06 - Server assembly

- Browser assembly accepts an optional platform-neutral `NodeDiscovery` beside
  existing fixed backend configuration. It uses discovery solely for dynamic
  unary routing; the existing 1–32 positional subscription fan-in, topology
  fingerprint, and envelope remain unmodified for T-0122.
- `pnpm install && pnpm exec tsc -b packages/deployment packages/auth packages/server`
  passed. Runtime profile self-introspection is unavailable on this surface;
  acceptance relies on the explicit dispatched implementer profile
  `gpt-5.6-terra` / `medium`.

## 2026-08-06 - Implementer self-review and preflight

- Self-review checked node-ID/endpoint replacement, empty-to-nonempty recovery,
  one selected unary dispatch, A/B/C coalescing, bounded 40-node starts, and
  preservation of fixed subscription fan-in. A close-path pending-promise leak
  was corrected before handoff.
- `pnpm verify:task -- --no-coverage packages/deployment/test/discovery.test.ts
packages/auth/test/dynamic-unary-forwarder.test.ts
packages/server/test/server/server.test.ts` passed (generated validation,
  build typecheck, and tooling typecheck). `git diff --check` passed.
- Review dispositions: style/maintainability relevant; documentation relevant;
  TypeScript/API relevant; performance/reliability relevant; security N/A
  because trusted operator discovery adds no authentication boundary. Ready for
  the orchestrator's specialist review wave.

## 2026-08-06 - Mechanical documentation correction

- The prior `verify:task -- --no-coverage ...` preflight did not run the full
  TSDoc and formatting gates. The subsequent `pnpm lint:tsdoc` gate correctly
  found missing multiline/public callable documentation and an internal task
  chronology claim. Those findings are corrected before specialist review.

## 2026-08-06 - Review correction batch

- Added deterministic scheduled discovery coverage for initial refresh and the
  ten-second default, IDN/IP TLS authority normalization, and duplicate-aware
  dynamic reconciliation. The focused tests and affected typecheck pass.
- Remaining browser transport SNI support depends on the Connect Node transport
  option shape and is under active implementation review; no subscription
  topology behavior was changed.

## 2026-08-06 - Review correction continuation

- Confirmed Connect Node's `GrpcTransportOptions.nodeOptions` forwards Node
  HTTP/2 `servername`; Browser assembly now supplies the normalized TLS
  authority only for HTTPS discovery nodes. The transport creates sessions on
  demand, so the dynamic client owns no fictitious eagerly-opened socket.
- Factory failures are contained by the reconciliation owner so a later
  complete snapshot can recover without an unhandled rejection. In-flight
  factory creation retains an abort controller that close aborts before join.
- `pnpm exec tsc -b packages/auth packages/server` and the focused deployment
  and dynamic-unary tests passed (8 tests).

## 2026-08-06 - Bounded completion and lifecycle recovery

- Replaced per-caller pending completion arrays with one shared reconciliation
  cycle completion. Rapid churn replaces only the latest snapshot.
- Added tests for two concurrent stalled starts aborted by idempotent close,
  factory failure followed by recovery, and close failure during removal.
- `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts`
  passed (6 tests); `pnpm exec tsc -b packages/auth packages/server` passed.

## 2026-08-06 - Complete specialist review wave

- Style/maintainability, documentation, TypeScript/API, and
  performance/reliability reviews all completed before corrections began.
- Expected profiles were explicit in dispatch: documentation used its
  immutable `gpt-5.6-luna` / `medium` role profile; the other three reviewers
  used `gpt-5.6-terra` / `high`.
- Runtime self-introspection was unavailable. Explicit dispatch and immutable
  configured profiles are the accepted evidence; no mismatch was visible.
- The deduplicated correction batch covers refresh scheduling, duplicate IDs,
  failure recovery, bounded waiters, cancellation-safe shutdown, TLS/IDN/SNI,
  actual Browser resource ownership, focused lifecycle tests, and public
  documentation. Details and dispositions are in the review record.

## 2026-08-06 - Focused re-review

- Coordinator preflight at `c8552397` passed 131 focused tests, affected
  package typecheck, TSDoc, Prettier, and diff checks.
- All four substantively affected concerns were re-reviewed with their original
  explicit role profiles and the same runtime-metadata limitation.
- The re-review accepted the first batch's bounded completion, cancellation,
  normal lifecycle, TLS replacement/SNI, and documentation corrections, but
  found a stale-start generation race, real HTTP/2 resource ownership and
  retryable-disposal gaps, scheduled-reader rejection/join and multi-watch
  gaps, permissive TLS parsing, fail-fast Browser cleanup, an inaccurate
  conflicting-ID test contract, and three small documentation/module-structure
  corrections.
- One deduplicated second correction batch is recorded in the review record.

## 2026-08-06 - Browser discovery assembly proof

- A focused BrowserServer open/close test now uses a fake discovery source that
  emits a complete HTTPS node snapshot. It proves one watch on startup and one
  stop callback across repeated close while fixed subscription bindings remain
  configured.
- `pnpm exec vitest run packages/server/test/server/server.test.ts` passed
  (117 tests); server typecheck, Prettier, and TSDoc checks passed.

## 2026-08-06 - Second correction batch checkpoint

- Added reconciliation generation fencing: stale successful client starts close
  themselves rather than entering newer membership. Scheduled discovery now
  enforces one active watch, retains its last delivered snapshot on reader
  failure, retries on its schedule, and joins an abort-aware active read on
  close.
- Focused deployment/auth/server tests passed (131 tests); affected typechecks
  passed. Remaining second-batch transport-manager ownership and phase-safe
  aggregate cleanup work continues in this implementation context.

## 2026-08-06 - Second correction batch transport and TLS

- Browser dynamic clients now own Connect `Http2SessionManager` resources and
  abort them on disposal. Failed disposals remain retryable across later
  reconciliation and close attempts. TLS authorities reject userinfo, explicit
  ports, query, fragment, bracketed IPv6, and IPv4 while retaining IDN support.
- Focused deployment/auth/server tests passed (137 tests); affected typechecks
  passed. Browser phase-failure aggregation remains the final active correction.

## 2026-08-06 - Phase-safe browser cleanup

- Browser shutdown now runs subscriptions, discovery stop, dynamic clients,
  listener, and native server in deterministic order even after a phase fails.
  It retains successful phase flags, retries only unfinished phases, preserves
  the earliest failure message, and attaches later failure detail without
  changing error precedence. The established drain test now proves native
  cleanup occurs during the first failed close.

## 2026-08-06 - Coordinator audit follow-up

- Coordinator audit found prior claims ahead of visible test coverage. Added
  abort-aware scheduled-read close joining, IDN and empty-port TLS cases, and
  an accurate contained-invalid-snapshot test that preserves the last valid
  unary membership. The remaining Browser manager and multi-phase test seams
  are still active work and are not claimed complete here.

## 2026-08-06 - Browser resource and cleanup proofs

- Added an internal dynamic-manager factory seam exercised through BrowserServer
  open, discovery removal, and close; owned HTTP/2 session managers abort once
  per removed or shutdown node. Added discovery-stop failure retry coverage
  proving native cleanup still runs and only the failed stop phase retries.
- Focused server tests passed (119 tests), server typecheck and TSDoc passed.

## 2026-08-06 - Final Browser phase tracking

- Discovery stop and dynamic cleanup now have independent completion flags.
  A later listener or native failure no longer repeats already-successful
  discovery/dynamic phases. Multi-failure coverage proves discovery remains the
  primary error, later native failure is attached in `cleanupErrors`, and only
  failed discovery/native phases retry.
- Focused server tests passed (120 tests), with server typecheck, Prettier, and
  TSDoc enforcement clean.

## 2026-08-06 - Coordinator formatting correction

- Coordinator preflight found one mechanical issue: `packages/deployment/package.json`
  was not formatted by the repository formatter. No runtime behavior changed;
  the package metadata is being formatted and the complete affected-file
  Prettier check rerun before handoff.

## 2026-08-06 - Final focused re-review

- Coordinator preflight at `d127ffa6` passed 143 focused tests, affected
  typecheck, TSDoc, Prettier, and diff checks.
- The final review accepted prior resource, retry, refresh, TLS-component,
  conflict, Browser cleanup, and documentation corrections. One final narrow
  batch remains: mutation-side generation fencing, settling failed parallel
  start batches, DNS-label grammar, terminal scheduled-watch documentation and
  enforcement, non-Error cleanup detail, shutdown ownership wording, and the
  still-misplaced Node import.
