# T-0121 Work Log

## 2026-08-06 - Task Opened

- Baseline `origin/main@fa1ed36f` contains the reviewed Wave 7 plan.
- Classification: high-risk due to public API, concurrency, cancellation, and
  shared Gateway routing changes.
- Selected skills read by the orchestrator: `using-git-worktrees`,
  `executing-plans`, `subagent-driven-development`, and
  `test-driven-development`.
- Implementation profile: existing implementer, explicitly dispatched
  `gpt-5.6-terra` / `medium`; subagents may not spawn subagents.
- Next: implementation owner writes focused RED tests before production code.

## 2026-08-06 - RED: discovery contract

- Added `packages/deployment/test/discovery.test.ts` before production code.
- `pnpm exec vitest run packages/deployment/test/discovery.test.ts` failed as
  expected: `Cannot find module '../src/index.js'`. The test specifies canonical
  endpoint/TLS behavior and complete static snapshots.

## 2026-08-06 - RED: dynamic unary routing

- Added `packages/auth/test/dynamic-unary-forwarder.test.ts` before runtime
  code. `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts`
  failed as expected because the new deployment package was not yet declared as
  an auth dependency. The test specifies zero-node rejection/recovery,
  round-robin use, 40-node non-capping, and no retry after dispatch.

## 2026-08-06 - GREEN: dynamic unary routing

- `pnpm exec tsc -b packages/deployment packages/auth` passed.
- `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts`
  passed (2 tests). The runtime has a serialized membership reconciler, retains
  equal node endpoints, disposes removed/replaced clients, round-robins current
  unary clients, rejects at zero nodes, and sends no retry after dispatch.
- Generated `dist/` and `tsconfig.tsbuildinfo` outputs remain ignored and are
  not staged.

## 2026-08-06 - RED: membership churn and bounded starts

- Extended the dynamic unary test with A/B/C coalescing and a 40-node,
  concurrency-two start bound. The focused command failed as expected: the
  original serialized queue created B, and starts were sequential (`peak 1`).

## 2026-08-06 - GREEN: membership churn and bounded starts

- `pnpm exec tsc -b packages/deployment packages/auth` passed.
- `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts`
  passed (4 tests). A single running owner retains only the latest pending
  complete snapshot; client creation happens in finite bounded batches.
