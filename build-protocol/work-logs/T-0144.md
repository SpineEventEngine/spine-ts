# T-0144 Work Log

## Start

- Base: integrated Wave 8 commit `4e64bacb` on
  `task/T-0144-wave8-closure`.
- Classification: high-risk release closure because the task inventories the
  complete Wave 8 surface and owns the single repository-wide release gate.
- Expected implementation: deterministic inventory/checks and durable audit
  evidence only. Any runtime defect returns to its original implementation
  context before this task resumes.
- Historical plans, decisions, reports, handoffs, tasks, work logs, reviews,
  and research evidence remain unchanged and are excluded explicitly from
  current-state scans.

## Model And Role Record

- Mechanical inventory/scanning: orchestrator-dispatched function or
  `gpt-5.6-luna` / `medium` when classification needs judgment.
- Any bounded audit-script implementation: existing `implementer`, explicitly
  `gpt-5.6-terra` / `medium`, serialized as the sole writer.
- Reviewers: existing TypeScript/API, performance/reliability, and
  style/maintainability roles at explicit `gpt-5.6-terra` / `high`; existing
  documentation reviewer at immutable `gpt-5.6-luna` / `medium`.
- Runtime metadata will be recorded when exposed; immutable configured profiles
  plus explicit dispatch fields satisfy the gate when introspection is absent.

## Implementation Dispatch

- Existing `implementer` role, explicitly `gpt-5.6-terra` / `medium`, owns the
  deterministic Wave 8 inventory record, audit script/tests, package-script
  integration, and T-0144 protocol records as the sole writer. The owner must
  not spawn subagents.

## Audit Checkpoint 1

- RED/GREEN evidence: the new fixture test initially failed because the audit
  module did not exist; after implementation,
  `pnpm exec vitest run scripts/check-wave8-invention-audit.test.mjs
scripts/package-metadata.test.mjs` passes 2 files / 11 tests.
- Added `audit:wave8-inventions` and made `verify:generated-gates` invoke it,
  so the final release profile cannot bypass the current-state audit.
- The checker scans active source, manifests, public docs, examples, and active
  Proto inputs while excluding historical plans/decisions/reports/tasks/logs/
  reviews/research, generated dist, and tests. It allows a truthful negative
  public-doc statement rather than banning words wholesale.
- Added `release/WAVE_8_INVENTION_AUDIT.md`, classifying the current persisted
  records, serialization, APIs, limits, delivery/subscription/auth/deployment,
  providers, examples, docs, and forbidden removals. No human decision is open.
- Evidence: scoped ESLint, Prettier, audit command, and `git diff --check` pass.
  `verify:release` remains parent-owned and was not run.

## Audit Completeness Reconciliation

- Reconciled the inventory against current `RecordSpec` uses, active Proto
  records, root exports, and provider configuration. The matrix covers Entity/
  history/Event Store, Inbox/shards, Stand, Gateway bindings, discovery,
  TenantIndex, and current provider customization.
- Added fixture coverage for receipts, markers, replacement dedup records and
  claims, attempts/exhaustion/retry decisions, revoked sessions, fingerprints,
  versioned discovery, and the retired validation package. The audit passes and
  focused tests pass 2 files / 16 tests.
- Exactly-once and cross-layout atomicity are classified in the inventory and
  reviewed in active provider/delivery documentation. Their prose uses qualified
  multi-line negative limitations, so an unsafe phrase-only ban is deliberately
  not used as a substitute for the scoped artifact scan.

## Review Correction Checkpoint

- Replaced recursive traversal with tracked-file scanning, excludes historical,
  generated, and test paths, and includes `.tsx` current example source.
- Moved forbidden identifiers into `.wave8-forbidden-artifacts.json`; the audit
  consumes that one manifest and the test suite exercises every manifest rule,
  arbitrary `ApplicationNodeLease:vN`, executable-source negatives, and mixed
  public-Markdown positive/negative text.
- Focused evidence: audit command, 2 focused test files / 29 tests, Prettier,
  ESLint, and `git diff --check` pass. Final release verification remains
  parent-owned.
- Follow-up inventory checkpoints added exact linked API/schema/provider
  evidence and exact bounded-mechanism values/outcomes. The orchestrator then
  removed the three duplicate coarse tables, preserved their unique contracts
  as linked rows, corrected the Tenant Index source link, and verified the
  consolidated ledger.
- Post-consolidation evidence: audit command, 2 files / 30 tests, formatting,
  audience checks, release readiness (80 imports, 51 assets, 380 links), and
  full branch-range `git diff --check` all exit 0.

## Inventory Evidence Expansion

- Added exact navigable contract evidence for separately exported storage,
  provider, delivery, monitor, Inbox, subscription, auth, and discovery
  surfaces, including `RecordSpecOptions`, and expanded the bounded-mechanism
  ledger with client/server/browser/query/provider limits and outcomes.
- The removed-artifact document section now names every fixture identifier from
  the machine-readable manifest; parity is exercised by the audit test.

## Cheap Preflight

- Removed one trailing blank line from each newly created task/review record
  after the branch-range diff check exposed them.
- Changed-file Prettier, `git diff --check 4e64bacb`, tooling typecheck, scoped
  ESLint, 2 files / 16 focused tests, `audit:wave8-inventions`, documentation
  audience checks, and release readiness all exited 0.
- Release readiness verified 80 package imports, 51 package assets, and 313
  relative Markdown links.
- Changed-production coverage is N/A: T-0144 changes internal audit tooling,
  fixtures, package-script wiring, and protocol records only.

## Targeted Re-review Residual Completion

- Tightened the exact ledger with the delivery-server distinction: one inbox
  payload is 1 MiB while an encoded inbox record is capped at `4 MiB - 64
bytes`; the 4 MiB inbound-RPC ceiling remains separate.
- Added navigable named-contract rows for `DeliverySupervisor` and its option
  family, `DatastoreStorageFactory`/builder and creator/layout contracts, and
  `MysqlStorageFactory`/builder/options/operation contracts. Browser entries
  now name read/write defaults and ranges, unary/auth-route byte limits,
  route timeout range, and the default 64 active requests. Auth records the
  finite `UnaryGateway` input and opaque-session defaults of eight hours,
  10,000 retained sessions, and three ID attempts.
- Clarified that a delivery row remains pending only when both the selected
  monitor action and its fallback durable acknowledgement fail.
- Added a real temporary Git-index test: a tracked forbidden `.tsx` file is
  reported while untracked, `.gitignore`d generated, and `.worktrees` control
  files are not scanned. The test also proves production enumeration fails
  closed outside Git and explicitly exercises `CommitReceipt`, `DedupRecord`,
  and `ApplicationNodeLease:v1` plus another numeric version.
- Evidence: `pnpm exec vitest run scripts/check-wave8-invention-audit.test.mjs`
  passes 1 file / 25 tests; `pnpm audit:wave8-inventions`, tooling typecheck,
  scoped ESLint, Prettier, API/audience documentation checks, and `git diff
--check` all exit 0. `verify:release` was not run; it remains parent-owned.

## Matcher Follow-up

- The public-Markdown exemption now evaluates every regex occurrence and only
  accepts a direct, clause-local negation of the actual matched spelling. A
  positive occurrence on the same line is reported even when an earlier
  occurrence is negative.
- Regression coverage rejects semicolon and comma positive follow-ons and
  contrast clauses, while accepting direct alternate-spelling negatives and
  an unambiguous `no` list. The audit itself passes against current docs,
  including the Datastore physical-layout `no ... schema fingerprint ...`
  list.
