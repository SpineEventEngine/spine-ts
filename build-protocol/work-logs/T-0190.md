# Work Log: T-0190

Task: `build-protocol/tasks/T-0190-normalized-query-execution/TASK.md`
Branch: `codex/wave-12-query-plans`
Baseline: `e2ab42d2`

Live Datastore plan GREEN (2026-08-15): dedicated isolated emulator case used a
unique kind and direct production `queryPlan()`. With alpha/beta/bravo/charlie,
legal `greaterOrEqual`, compatible ascending ordering, mask, and limit returned
exactly beta/bravo (1 passed, 3 skipped). Unsupported-shape rejection before
`runQuery` remains separate instrumented unit evidence. Earlier broad emulator
context/Entity failures are unrelated legacy fixtures and not provider proof.

| Time       | Activity                       | Evidence / result                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-08-15 | Assignment accepted            | Existing implementer explicitly configured `gpt-5.6-terra` / medium; no runtime telemetry; no subagents.                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-15 | Durable task/review records    | Created before product edits; TDD RED is next.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-15 | Production-path RED            | Focused `MysqlRecordStorage.queryPlan()` test used the real inherited plan method (no patching) and failed at baseline capability admission: `Storage provider does not support ordering.` A first run was a clean-worktree missing-generated-package error and was discarded; after Proto/build setup the behavioral RED was observed.                                                                                                                                                    |
| 2026-08-15 | First GREEN slice              | MySQL now advertises only the planned normalized capabilities and emits a single bound `SELECT ... WHERE ... ORDER BY ... LIMIT ?`; equality RED passes with `value` and limit as bound parameters. Shared execution now has the finite 10,000 default and base providers reject nonempty unsupported plans.                                                                                                                                                                               |
| 2026-08-15 | Datastore diagnostic RED/GREEN | Actual production planner baseline resolved nested/disjunctive, oversized-ID, multi-inequality, and wrongly ordered inequality plans through an unfiltered read. RED tests recorded that result. Datastore now advertises only flat legal overlap and rejects those shapes before `runQuery`; focused provider suites pass.                                                                                                                                                                |
| 2026-08-15 | Focused coverage limitation    | The three focused provider suites passed 92/92 tests, but their V8 invocation fails the repository-wide global 90% threshold (5.71% lines, 6.67% branches) because it instruments unrelated workspace code. It is not accepted changed-source coverage evidence; full coordinated coverage remains required.                                                                                                                                                                               |
| 2026-08-15 | Live MySQL limitation          | Exclusive `mysql:8.4.10` suite ran with supplied app URL and the disposable container's required admin URL. It passed 4/9; 5 tests failed before plan execution at Entity schema preparation. The first record-layout test had created `google_protobuf_StringValue`; later Entity tests reuse that family with incompatible Entity columns. No table was deleted. This supplies neither live normalized-plan SQL nor row-count evidence. MySQL window was released before Datastore work. |
| 2026-08-15 | Live MySQL plan GREEN          | The record-layout fixture now uses a unique storage group. Against exclusive MySQL 8.4.10 it wrote `a`/`b`, executed real `queryPlan()` with `greaterOrEqual`, descending order, and limit one, and returned exactly `b` (1 passed, 8 skipped). The same production method's bound SQL structure is captured separately by the unit contract. The full legacy Entity suite retains unrelated commit-fixture failures. MySQL window released.                                               |

## Changed-range coverage transfer

- The canonical focused profile passes 98/98 tests and all deterministic gates
  through release readiness. Whole-file provider coverage remains diagnostic.
- Fresh LCOV intersected with `e2ab42d2..33fccfce` reports 33/41 changed
  executable lines (80.49%) and 20/43 changed branches (46.51%). Datastore and
  the shared default constant are fully covered; gaps are the base fail-closed
  seam and MySQL's admitted IDs/comparison/composite/order/bound branches.
- The initial owner became inactive without editing after identifying those
  tests. A replacement implementer owns only the exact production-path matrix
  coverage and subsequent mechanical convergence; provider ownership remains
  in this stream until specialist review and explicit Inbox handoff.

## Changed-range coverage convergence

- Replacement existing `implementer` retains the explicitly dispatched
  `gpt-5.6-terra` / medium profile; Desktop runtime telemetry remains
  unavailable. No subagents were used and no Inbox or browser files changed.
- Production-path tests now call public `queryPlan()` only: the base seam
  rejects nonempty plans without an override and bounds the empty plan at
  10,001 candidates; MySQL covers IDs, all five comparison operators, flat and
  nested ALL/EITHER, mask, ascending/descending/no order, exact/default
  candidate bounds, and undeclared-column rejection before SQL access.
- Fresh focused V8 coverage over storage, MySQL, and Datastore sources passes
  5 files / 115 tests. Its aggregate whole-file threshold remains diagnostic
  (81.44% branches) because pre-existing provider paths are deliberately
  included. The retained `coverage/lcov.info`, intersected with added
  executable lines and branch locations in `e2ab42d2...HEAD`, reports 39/41
  lines (95.12%) and 41/43 branches (95.35%); this clears D-0114's exact
  changed-range >=90% gate.
- Next: run canonical `verify:task --no-coverage` with the same focused paths,
  then record deterministic evidence and checkpoint-push before review.

## Canonical task verification

- `pnpm verify:task -- --no-coverage` passed after the focused LCOV measurement:
  Node/version, Proto generation/lint/cleanliness, build and tooling typechecks,
  ESLint, cleanup/TSDoc/copyright/log containment, formatting, audience and API
  documentation checks, release-readiness, and 5 focused files / 115 tests.
  The `--no-coverage` profile is intentional: the prior retained LCOV is the
  exact changed-range evidence, whereas the verifier's whole-file threshold is
  a known pre-existing-provider diagnostic.
- Changed-test formatting and `git diff --check` remain required before the
  checkpoint commit; specialist review is the next action after the push.

## Accepted specialist correction batch

- The replacement existing `implementer` resumed under its explicitly recorded
  `gpt-5.6-terra` / medium profile; runtime telemetry is unavailable and no
  subagents were used. The seven accepted findings are addressed test-first:
  base/memory candidate fetch bounds use `(candidateLimit ?? 10,000) + 1`,
  normalized plans reject `offset` and unknown keys, Datastore uses the smaller
  exact-plan/candidate/provider bound and validates order columns before query
  creation, and MySQL rejects 1,000-or-more predicate binds before acquisition.
- Datastore and MySQL reference material now distinguish Datastore's 1,000-row
  provider scan ceiling from the shared candidate default and document MySQL's
  1,000-parameter normalized-plan bound. Unit RED produced the six expected
  failures; after GREEN, focused deterministic suites pass 4 files / 114 tests.
- Fresh retained `/tmp/t0190-correction-coverage/lcov.info`, intersected with
  the complete working `e2ab42d2` source diff, reports 52/54 changed executable
  lines (96.30%) and 57/61 branches (93.44%). The whole-file aggregate (77.93%
  branches) remains a pre-existing provider diagnostic.
- Live MySQL tenant/storage-group and Datastore emulator tenant/group query-plan
  cases are added but deliberately not run: the orchestrator has not granted an
  exclusive shared-provider window. The next action is deterministic static and
  documentation validation, then checkpoint-push and readiness reporting.

## Deterministic correction validation

- Generated build and tooling typecheck, changed-file ESLint, TSDoc, TypeDoc API
  inventory, changed-file Prettier, and `git diff --check` all pass. This is a
  bounded runtime correction, so the next canonical profile after the checkpoint
  is `verify:task` with focused unit paths; it does not authorize shared-provider
  execution.

## Exclusive MySQL evidence

- The orchestrator granted and the implementer released the retained
  `spine-wave12-mysql` MySQL 8.4.10 window. Only the focused
  `mysql-integration.test.ts` normalized-plan/storage-group case ran: 1 passed,
  9 skipped. In one unique single-tenant context, a normalized ID plan returned
  only `a` from its selected group and only `other` from a sibling group.
- The new two-tenant live case remains environment-gated because the retained
  resource exposes one configured database URL rather than two physical tenant
  database URLs. No broad database deletion or unrelated live suite ran.
  Datastore remains untouched; an exclusive window is now required for its
  prepared tenant/group query-plan case.

## Exclusive Datastore evidence

- The orchestrator granted and the implementer released the retained Datastore
  emulator at `127.0.0.1:8081`, project `spine-wave12`. Only the focused
  tenant/group normalized-plan case ran: 1 passed, 3 skipped. It seeded
  `single`, `group`, `tenant-a`, and `tenant-b` (plus the existing shared row)
  and proved ID plans return only the selected group or native tenant namespace.
  Exact cleanup removed every seeded ID; no broad emulator clearing occurred.
- Both sequential live windows are released. The MySQL tenant case remains an
  explicit limitation because the retained MySQL resource supplied one physical
  database URL, while the Datastore tenant/group proof is complete. Next: final
  non-live canonical task verification, record update, checkpoint push, and
  targeted re-review endpoint.

## Canonical correction verification

- Final non-live `pnpm verify:task -- --no-coverage` passed every deterministic
  gate and 4 focused files / 114 tests. The coverage profile remains intentionally
  separate: retained exact LCOV is 52/54 changed lines (96.30%) and 57/61
  branches (93.44%), while whole-file provider coverage is diagnostic.

## Final reliability evidence

- The final accepted test-only batch makes MySQL connection acquisition
  observable: the oversized 1,000-ID normalized plan rejects with zero
  acquisitions and zero SQL calls. Base fallback and the real
  `InMemoryRecordStorage` now each directly observe the omitted-budget 10,001
  fetch sentinel. No production file changed, so the retained exact changed
  range coverage remains applicable.
- Focused evidence tests pass 3 assertions across 2 files; the complete
  non-live focused suite passes 4 files / 116 tests. No live provider ran.
  Next: push this evidence checkpoint for the sole performance/reliability
  re-review.
