# T-0064 Work Log

Status: Verified; ready to commit

## 2026-07-23 — Start

- T-0063 is integrated into `main`, post-merge verified, and remotely
  synchronized through closure commit `3693d93f`; its task branch is pushed at
  `1717d319`, local/remote refs match, and its clean worktree was removed.
- Created `task/T-0064-in-memory-delivery-server` in the isolated
  `.worktrees/T-0064-in-memory-delivery-server` worktree at exact baseline
  `3693d93f`.
- Classified T-0064 high-risk because it introduces a public package,
  RPC-shaped service semantics, serialized concurrent mutations, cancellation
  admission, idempotency/reconciliation, shard-session ownership, exact stale
  timing, and finite in-memory resource ownership.
- Recorded behavior-focused acceptance criteria, test-first requirements,
  frozen source/Proto constraints, exclusions, human requirements, reviewer
  dispositions, and child acceptance gates before dispatch.
- The selected Desktop execution surface supports the required explicit child
  role/model/reasoning dispatch. Standard speed is used; Fast/boost, Max, and
  Ultra are not used.

## Requirements Splitter Dispatch

- Existing role: `requirements_splitter`.
- Assignment: read-only bounded decomposition of the frozen upstream
  `simple-server` and current TS delivery boundaries into package seams,
  invariants, test-first slices, review scope, and exclusions.
- Expected profile: explicit `gpt-5.6-sol` / `high`.
- Runtime metadata is recorded when exposed; otherwise the immutable configured
  role/profile plus unavailable introspection is the accepted evidence unless
  a mismatch or fallback is visible.
- Edits, child spawning, commits, pushes, merges, dependency installation, and
  protected/unrelated access are prohibited.

## Requirements Splitter Result

- Runtime self-introspection was unavailable. The immutable configured
  assignment matched the explicit existing `requirements_splitter`,
  `gpt-5.6-sol` / `high`, with no visible mismatch or inherited fallback.
- The split found no human blocker and constrained implementation to one
  wire-native in-memory package, one finite FIFO mutation boundary, Inbox and
  Shard handlers, and one narrow delivery-client strict-page correction.
- Frozen source inspection confirmed strict `when_received > since_when`,
  automatic stale takeover only at `elapsed > timeout`, manual expiration at
  `elapsed >= inactivity`, worker-agnostic explicit release, upsert writes,
  missing-remove no-op, and no background expiration timer.
- T-0062's inclusive timestamp-page assumption is incompatible with the frozen
  strict server. The accepted fix requests one JavaScript millisecond before
  the continuation, retains exact-anchor/tie fail-closed checks, and rejects
  date underflow before RPC. No wire cursor or public contract expansion is
  introduced.
- One canonical message map and shard map own detached wire snapshots. Counts
  and future Admin observations derive from actual state transitions. A fixed
  internal 100-waiter FIFO validates before enqueueing, cancels before
  admission, and commits synchronously/atomically after admission.
- Page size is safely bounded to 1..1000. Total ordering adds UUID only to
  stabilize upstream-unspecified ties. Restart data loss and worker-agnostic
  release are documented limitations.
- No third-party dependency is added; existing pinned Protobuf/Connect and
  workspace packages are sufficient.

## Implementer Dispatch

- Existing role: `implementer`.
- Assignment: own the accepted package, focused tests, narrow delivery-client
  correction, workspace/API/docs wiring, implementation report, and this log.
- Expected profile: explicit `gpt-5.6-terra` / `medium`.
- The owner may not spawn children, commit, push, merge, install dependencies,
  touch `@spine-ts/server` production semantics, or modify protected/unrelated
  files. It must preserve concurrent/user work and report any scope conflict.

## Implementation Progress

- Runtime metadata is not self-introspectable on this surface. The immutable
  configured implementer assignment remains explicitly `gpt-5.6-terra` /
  `medium`, with no visible fallback or mismatch.
- Recorded RED before production code: the public-core test failed because the
  new `src/index.ts` seam did not exist.
- Added the bounded listener-free package core, detached state, strict paging,
  finite mutation admission, and shard session behavior; retained shard records
  clear the worker on release while preserving the last-pick timestamp for
  T-0065 Admin semantics.
- Corrected RemoteInbox's strict server continuation request to use one
  JavaScript millisecond before its exact anchor and to reject lower-bound
  underflow before a remote call.
- Focused GREEN validation is blocked: this isolated worktree has no package
  links/generated proto output, and `pnpm proto:generate` refuses without an
  install (`ERR_PNPM_VERIFY_DEPS_BEFORE_RUN`). No install, generated source,
  commit, push, or merge was performed.
- Workspace links and frozen Proto generation were subsequently supplied by the
  orchestrator. Fixed the two isolated-declaration errors in state ownership;
  `typecheck:build:generated`, tooling typecheck, touched-file ESLint, Prettier,
  and focused server/client tests (16 tests) now pass.
- The accepted exhaustive TDD matrix remains incomplete (admission abort/capacity,
  contention/timing, detached restart, and real response-loss reconciliation).
  This historical note was resolved by the subsequent completed TDD matrix.

## Pre-review completion

- Completed Inbox, admission, Shard, restart/detachment, strict RemoteInbox
  paging, and caller-owned Connect response-loss tests. Lost write/remove
  outcomes reconcile through `findOne`; lost pickup is quarantined without a
  duplicate RPC; direct state outcomes prove lost release and expiration.
- Paging evidence includes the one-millisecond continuation anchor,
  minimum-Date pre-RPC rejection, and existing missing/truncated/tied-page
  fail-closed behavior. Admin observation remains deferred to T-0065.
- Final focused gate passed 38 tests across 7 files. Generated build/tooling
  typechecks, touched ESLint, TypeDoc/API check, Prettier, and diff check pass.
- Status is `Awaiting specialist review`; the earlier missing-links blocker is
  resolved historical evidence, not a current limitation.

## Consolidated review correction

- Applied the complete style, documentation, API, and reliability correction
  batch in the existing implementer context (`gpt-5.6-terra` / `medium`;
  runtime self-introspection unavailable).
- Deepened the package layout, detached Protobuf workers, tightened RPC field
  and timestamp validation, corrected strict paging minimum, and aligned
  expiration response handling with the canonical 4 MiB byte bound.
- Focused RED/GREEN regressions now cover response mutation, required fields,
  101 released sessions, and oversized post-commit unknown-outcome recovery.
- Final correction gate passed: 41 focused core/router/adapter tests; 76
  non-network delivery-client tests; the real loopback HTTP/2 integration test;
  generated build and tooling typechecks; full ESLint and tracked-layout
  cleanup; cleanup-rule fixtures; TypeDoc/API docs; repository Prettier; and
  `git diff --check`.
- The exact public names are frozen by the accepted three-export API, so the
  cleanup checker records only those three exact occurrence exceptions. No
  wildcard or unrelated naming exception was added.
- Exact correction commands:
  - `pnpm vitest run packages/delivery-server/test packages/delivery-client/test/in-memory-core-response-loss.test.ts packages/delivery-client/test/remote-adapters-quarantine.test.ts`
  - `pnpm vitest run packages/delivery-client/test --exclude packages/delivery-client/test/delivery-client-grpc.integration.test.ts`
  - `pnpm vitest run packages/delivery-client/test/delivery-client-grpc.integration.test.ts`
  - `pnpm typecheck:build:generated`
  - `pnpm typecheck:tooling`
  - `pnpm lint:generated`
  - `pnpm docs:check:generated`
  - `pnpm format:check`
  - `git diff --check`
- Final narrow RED/GREEN correction requires `ShardIndex.index <
ShardIndex.ofTotal` in both service boundaries. Focused tests prove impossible
  Inbox write/find/page and Shard pickup/release requests fail with
  `InvalidArgument` before admission; a subsequent valid pickup proves no
  impossible session was stored.
- Added and passed deterministic admission (pre-abort, queued abort, FIFO, 100
  pending/101st rejected) and Shard (20-way contention, release/reacquisition,
  automatic equality rejection/manual equality release) test slices. The
  subsequent completion section records the remaining accepted matrix.

## Independent Pre-Review Gate And Review Dispatch

- The orchestrator independently reran seven focused files / 38 tests,
  generated build typecheck, tooling typecheck, exact TypeDoc/API inventory,
  touched-file ESLint, full Prettier check, and diff hygiene; every gate passed.
- The API inventory contains exactly three public
  `@spine-ts/delivery-server` exports.
- Style/maintainability, documentation, TypeScript/API, and
  performance/reliability are required. Final security is N/A for this packet
  and remains deferred to T-0067 unless a critical issue appears.
- Expected profiles are recorded before dispatch in
  `build-protocol/reviews/T-0064-in-memory-delivery-server.md`; reviewers are
  read-only and the complete wave will be collected before one correction
  batch is returned to implementation.

## Review Convergence And Final Gate

- Focused style, documentation, TypeScript/API, and performance/reliability
  re-reviews resolved every original finding. The final ShardIndex invariant
  P2 received clean targeted reliability and API closure.
- Full `pnpm --config.verify-deps-before-run=false verify` passed 116 test
  files / 2,268 tests with 3 files / 21 tests skipped and global branch
  coverage 7,147/7,933 (90.09%).
- Exact API inventories include three `@spine-ts/delivery-server` exports.
  Proto checksums/descriptors, generated drift, full type/lint/cleanup/format,
  documentation, and release-readiness checks passed.
- T-0064 is ready for its task commit and immediate task-branch push.
