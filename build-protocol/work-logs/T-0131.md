# T-0131 Work Log

## 2026-08-07 - Start

- Started from verified `origin/main@242a98c3` in an isolated task worktree.
- Classified the task as high-risk because it changes the public generic
  storage specification and in-memory physical identity.
- Assigned one bounded implementation owner with explicit Terra/medium role
  configuration and RED-first tests.
- The project work log is the durable progress ledger; the previously removed
  `.superpowers` directory is not recreated.
- Next: implement and self-review the focused slice, then run deterministic
  preflight and specialist review.

## 2026-08-07 - Core implementation and focused verification

- Implementer assignment: existing `implementer` role, explicitly configured
  as `gpt-5.6-terra` / `medium` by the immutable desktop dispatch. This runtime
  does not expose self-introspection metadata; no visible profile mismatch.
- Selected `test-driven-development` from
  `/Users/armiol/.agents/skills/test-driven-development/SKILL.md`; it governed
  the RED-green sequence. Checked `build-protocol/skills/EXPECTED_SKILLS.md`,
  enumerated `/Users/armiol/.agents/skills/*/SKILL.md`, and read the installed
  lock manifest. Other expected skills are orchestration/review concerns and
  are not needed for this bounded implementation role.
- JVM evidence: `spine-jvm-docs/spine-validation-storage-observability-and-support.md`
  records `idType`, `recordType`, and `sourceType`, with Entity records using
  their state type as source type.
- RED evidence:
  `pnpm --config.verify-deps-before-run=false exec vitest run packages/storage/test/record/record-spec.test.ts`
  failed four new contract cases because the former implementation required
  `storageKey`; the backend-source isolation test then failed with
  `TypeError: create is not a function` because the old bind signature still
  expected a compatibility fingerprint.
- Implemented the exact `RecordSpec` object contract and read-only accessors;
  removed storage-key/fingerprint machinery; scopes in-memory generic records
  by `sourceType.typeName`; retained clone, column materialization, query,
  tenant, compare-and-set, and batch behavior. Updated the storage README and
  REFERENCE contract prose.
- Files changed: `packages/storage/src/{record,storage,memory}/**`, mirrored
  storage tests, and `packages/storage/{README,REFERENCE}.md` only.
- Focused GREEN evidence:
  `pnpm --config.verify-deps-before-run=false exec vitest run packages/storage/test/record/record-spec.test.ts packages/storage/test/memory/in-memory-storage-backend.test.ts packages/storage/test/memory/in-memory-record-storage.test.ts`
  passed 46 tests in 3 files.
- Deterministic preflight: changed-file Prettier completed; `git diff --check`
  passed. `pnpm --config.verify-deps-before-run=false exec tsc --noEmit -p
packages/storage/tsconfig.json` is blocked only by excluded downstream
  consumer `packages/storage/src/event/event-store.ts:381`, which still passes
  removed `schema` to `RecordSpec`. Per task ownership, no alias or consumer
  migration was added.
- Next: capture the coverage task-profile gate, self-review the bounded diff,
  and return the downstream migration dependency to orchestration.

## 2026-08-07 - Verification limitation and self-review

- Public compile-contract evidence:
  `pnpm --config.verify-deps-before-run=false exec tsc --ignoreConfig --noEmit
--target esnext --module nodenext --moduleResolution nodenext --strict
--skipLibCheck packages/storage/test/record/record-spec.test.ts` passed.
  It checks that the constructor input and instance surface omit `schema`,
  `storageKey`, and `compatibilityFingerprint`.
- Coverage task-profile attempt:
  `pnpm --config.verify-deps-before-run=false verify:task -- --coverage
packages/storage/test/record/record-spec.test.ts
packages/storage/test/memory/in-memory-storage-backend.test.ts
packages/storage/test/memory/in-memory-record-storage.test.ts --source
packages/storage/src/record/record-spec.ts
packages/storage/src/memory/in-memory-storage-backend.ts
packages/storage/src/memory/in-memory-storage-factory.ts
packages/storage/src/memory/in-memory-entity-commit.ts
packages/storage/src/memory/in-memory-entity-history.ts
packages/storage/src/storage/canonical-scope.ts` did not reach TypeScript or
  coverage. It passed Node, authored Proto, and frozen descriptor checks, then
  stopped because this isolated worktree lacks ignored package dependency links
  for `packages/client-node/codegen`; no production or dependency files were
  changed to work around it.
- Self-review: `git diff --check` passed. The implementation contains no
  compatibility alias, replacement hash, or persisted descriptor. The backend
  retains only values by canonical source-type scope. The remaining typecheck
  error is the intentionally untouched event-store constructor migration;
  no core-slice correctness defect was found.
- Limitation for orchestration: full package/repository verification and
  coverage cannot pass until the planned external callers migrate the removed
  `RecordSpec` names, beginning with `packages/storage/src/event/event-store.ts`.

## 2026-08-07 - Authorized event-store correction

- Orchestration authorized the minimal direct migration of
  `packages/storage/src/event/event-store.ts`; its record specification now
  supplies `recordType` and no removed constructor field. No event behavior
  changed.
- Strengthened the public compile contract so TypeScript callers must supply
  exactly one ID descriptor. `RecordSpec` accepts either `idSchema` or
  non-blank `idKind`, never neither or both; runtime validation remains for
  untyped JavaScript input. The record-spec test adds `@ts-expect-error` cases
  for both invalid forms.
- Correction verification passed:
  `pnpm --config.verify-deps-before-run=false exec tsc --ignoreConfig --noEmit
--target esnext --module nodenext --moduleResolution nodenext --strict
--skipLibCheck packages/storage/test/record/record-spec.test.ts`; then
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/storage/test/event/event-store.test.ts
packages/storage/test/record/record-spec.test.ts
packages/storage/test/memory/in-memory-storage-backend.test.ts
packages/storage/test/memory/in-memory-record-storage.test.ts` passed 59
  tests in 4 files; and
  `pnpm --config.verify-deps-before-run=false exec tsc --noEmit -p
packages/storage/tsconfig.json` passed.
- Configured runtime profile remains immutable `implementer` /
  `gpt-5.6-terra` / `medium`; this agent surface exposes no actual runtime
  model/reasoning self-introspection, so no stronger runtime metadata is
  available.

## 2026-08-07 - Entity metadata correction

- Broader coverage RED exposed four Entity-history tests that still required
  legacy layout and ID-codec fingerprint metadata. The required correction is
  removal, not silent acceptance: removed `EntityStorageInput.layout`,
  `EntityStorageInput.storageKey`, `EntityIdCodec.fingerprint`, and the
  compatibility fingerprint helper.
- Entity in-memory scope and atomic commit scope now derive from context plus
  `sourceType.typeName`. Updated conformance fixtures and tests retain
  current/history, context, tenant, and shared-backend semantics, replacing
  fingerprint mismatch assertions with distinct-source-type isolation.
- Correction verification:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/storage/test/event/event-store.test.ts
packages/storage/test/record/record-spec.test.ts
packages/storage/test/memory/in-memory-storage-backend.test.ts
packages/storage/test/memory/in-memory-record-storage.test.ts
packages/storage/test/entity/in-memory-entity-history.test.ts
packages/storage/test/entity/in-memory-entity-commit.test.ts` passed 99
  tests in 6 files. `pnpm --config.verify-deps-before-run=false exec tsc
--noEmit -p packages/storage/tsconfig.json` and `git diff --check` passed.

## 2026-08-07 - Storage-factory test correction

- Focused coverage RED found eight storage-factory tests still constructing
  legacy `RecordSpec` values or expecting removed compatibility failures.
  Migrated helpers to `recordType` and `sourceType`, removed mismatch-only
  expectations, and retained independent backend, shared backend, context,
  Unicode-delimited source scope, single/multitenant, sibling-close, and tenant
  isolation behavior.
- Verification:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/storage/test/event/event-store.test.ts
packages/storage/test/record/record-spec.test.ts
packages/storage/test/memory/in-memory-storage-backend.test.ts
packages/storage/test/memory/in-memory-record-storage.test.ts
packages/storage/test/entity/in-memory-entity-history.test.ts
packages/storage/test/entity/in-memory-entity-commit.test.ts
packages/storage/test/storage/storage-factory.test.ts` passed 112 tests in
  7 files. `pnpm --config.verify-deps-before-run=false exec tsc --noEmit -p
packages/storage/tsconfig.json` and `git diff --check` passed.

## 2026-08-07 - Mechanical pre-review correction

- Preserved runtime JavaScript ID validation through a deliberately broadened
  local input view, satisfying lint without weakening the statically exclusive
  constructor contract. Made generated message test types explicit and replaced
  test non-null assertions with presence guards.
- `pnpm --config.verify-deps-before-run=false exec eslint` over all changed
  storage source and test paths passed. The seven focused suites passed 112
  tests; package TypeScript check and `git diff --check` passed.

## 2026-08-07 - Accepted review correction batch

- Tightened `RecordSpec` so a message-ID specialization requires `idSchema` at
  compile time; primitive `idKind` remains only for non-message IDs. Added a
  compile regression while retaining runtime validation through the widened
  untyped input view.
- Added explicit `entity` and `record` backend namespaces. They prevent a
  generic `TenantRecords` value and transitional Entity backend from sharing one
  runtime slot at the same context/source type, without hash or compatibility
  descriptor machinery. Factory regressions prove both creation orders remain
  usable.
- Updated REFERENCE wording from ID/layout inputs to source-type inputs and
  documented the added backend namespace parameter.
- Verification passed: standalone strict compile-contract check for
  `packages/storage/test/record/record-spec.test.ts`; ESLint over all changed
  storage source/test paths; seven focused suites with 114 passing tests;
  `pnpm --config.verify-deps-before-run=false exec tsc --noEmit -p
packages/storage/tsconfig.json`; and `git diff --check`.

## 2026-08-07 - Review convergence and checkpoint

- The complete review wave returned two distinct P1 findings and one P2:
  message-ID accessor unsoundness, shared-backend runtime type collision, and a
  stale REFERENCE phrase. One combined correction batch resolved all three.
- Focused style, TypeScript/API, and performance/reliability re-reviews are
  clean. Documentation's deterministic wording finding is resolved without
  affecting the remainder of its clean review.
- Post-correction coverage passes 114 tests with 96.73% statements, 93.83%
  branches, 97.88% functions, and 97.09% lines.
- The repository task profile reaches only the expected later-consumer compile
  inventory. T-0131 will be committed and pushed as the reviewed base of the
  Wave 8 integration train, not merged into broken `main`.
