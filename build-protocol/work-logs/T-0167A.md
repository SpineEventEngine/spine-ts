# T-0167A Work Log

## 2026-08-11 - Startup

- Recorded the human decision to remove TypeScript `(is)/(every_is)`
  interpretation and all interface-based routing until a fresh frozen Proto
  contract provides TypeScript-specific metadata.
- Confirmed the correction preserves exact-schema routing,
  replacement-default routing, typed Inbox targets, and durable replay without
  application rerouting.
- Created an isolated branch/worktree from clean `origin/main@42b85501`; the
  dirty primary checkout remains untouched.
- Selected the release verification profile because the correction crosses
  core, transport, server, generated API inventory, and shared runtime
  contracts.
- Implementation assignment is the existing `implementer` role with explicit
  `gpt-5.6-terra` / medium dispatch. Runtime metadata will be recorded if the
  surface exposes it; otherwise the immutable configured role/profile and the
  metadata limitation will be recorded.

Status: baseline verification and test-first correction startup in progress.

## 2026-08-11 - Routing declaration RED/GREEN checkpoint

- Inspected the task-relevant server sources and focused routing tests, plus
  `spine-jvm-docs/spine-domain-model-and-signals.md` and
  `spine-jvm-docs/spine-routing-dispatch-and-delivery.md`. The available local
  material confirms `(is)`/`(every_is)` name Java interfaces and does not
  justify TypeScript interface routing; no local `core-jvm` source checkout is
  present beyond these research notes.
- Established RED with the three focused routing declaration tests. Each failed
  because its internal construction snapshot exposed `semantic: Map {}`.
- Removed `routeSemantic()` and semantic snapshot state from Command, Event,
  and StateUpdate routing declarations, and removed descriptor-based semantic
  selection from repository route resolution. Exact routes and defaults remain
  the only selection inputs in this slice.
- GREEN evidence: `pnpm exec vitest run packages/server/test/repository/command-routing.test.ts packages/server/test/repository/event-routing.test.ts packages/server/test/repository/state-update-routing.test.ts`
  passed (3 files, 6 tests); `pnpm exec tsc -p packages/server/tsconfig.json --noEmit`
  passed.
- Remaining correction surface: core registry semantic metadata/indexes, entity
  and readiness metadata, runtime topic construction, transport topic metadata,
  and their focused tests. The one-time residual audit and release verification
  have not yet been run.

Status: cohesive semantic-metadata removal continues.

## 2026-08-11 - Affected API reference correction

- Updated `packages/transport/REFERENCE.md` because its public topic contract
  directly claimed the removed `semanticTags` input/output. This is the narrow
  T-0167A API-reference exception recorded in the task, not Wave 10 product
  Markdown work.

## 2026-08-11 - One-time residual audit

- Ran one recorded scan: `rg -n 'routeSemantic|isTypes|everyIsTypes|findBySemanticTag|findByIs|findByEveryIs|SemanticOptionErrors|semanticTags|java_type|every_is' --glob '!node_modules/**' --glob '!dist/**' --glob '!coverage/**' .`.
- No active TypeScript runtime implementation remains. Results are canonical
  Proto declarations/generated exports, historical task/review records, frozen
  fixture declarations, and explicit T-0167A absence assertions. Active plans
  now state exact-route then replacement-default behavior only.
- This is the required one-time audit record; no permanent checker, manifest,
  script, or test-run scan was added.

## 2026-08-11 - Changed-range coverage inspection

- Compared `origin/main...HEAD` against the nine changed production files and
  mapped the direct focused-run `coverage/lcov.info` data to added ranges. The
  diff is deletion-dominant (494 deleted baseline lines); it has 18 added-range
  candidates, with 11 reported hits.
- The seven unhit candidates are TSDoc/type/import declarations plus the two
  transport routing-key construction lines. The latter are behaviorally covered
  by the focused transport assertions for canonical routing and subscription
  keys; coverage associates those calls with built package output rather than
  the source line. No uncovered added conditional or new runtime branch remains.
- No unrelated legacy whole-file coverage expansion is required for this
  deletion-heavy correction. Direct coverage ran 360 focused tests successfully;
  its non-zero exit is separately attributable to Vitest's global repository
  threshold, not a changed-range failure.
