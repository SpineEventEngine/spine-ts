# T-0167A Work Log

## 2026-08-11 - Startup

- Recorded the human decision to remove TypeScript `(is)/(every_is)`
  interpretation and all interface-based routing until a fresh frozen Proto
  contract provides TypeScript-specific metadata.
- Confirmed the correction preserves exact-schema routing,
  replacement-default routing, typed Inbox targets, and durable replay without
  application rerouting.
- Created an isolated branch/worktree from clean `origin/main@42b85501`; the
  dirty primary checkout remains untouched.
- Selected the release verification profile because the correction crosses
  core, transport, server, generated API inventory, and shared runtime
  contracts.
- Implementation assignment is the existing `implementer` role with explicit
  `gpt-5.6-terra` / medium dispatch. Runtime metadata will be recorded if the
  surface exposes it; otherwise the immutable configured role/profile and the
  metadata limitation will be recorded.

Status: baseline verification and test-first correction startup in progress.
