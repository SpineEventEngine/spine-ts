# T-0067a Work Log

Status: closed; merged and post-merge verified on `main`.

## 2026-07-22 — Reproduction And Isolation

- A fresh T-0067 worktree generated Proto sources and then failed
  `typecheck:build:generated` because `@spine-ts/client` could not resolve
  `@spine-ts/core`; the downstream callback parameter consequently appeared
  implicit-`any`.
- `packages/client/package.json` declares `@spine-ts/core`, but
  `packages/client/tsconfig.json` references only `../proto`. Root project order
  lists client before core.
- Building core first and then rerunning the unchanged generated build passed,
  confirming the missing TypeScript project edge as the root cause.
- Created isolated branch `task/T-0067a-client-core-reference` from pushed
  `main` at `4e3b43d6`. No T-0067 documentation change is in this worktree.
- Implementer assignment is the existing role, expected and to be explicitly
  dispatched as `gpt-5.6-terra` / `medium`; runtime metadata or the configured
  profile limitation will be recorded before acceptance.

## 2026-07-23 — Implementation

- Installed the lockfile-pinned workspace dependencies. The offline install
  first reported a missing cached `tslib@2.8.1` tarball; the regular frozen
  install then completed without lockfile changes.
- Generated ignored Proto sources before reproducing the TypeScript graph. A
  first build without them correctly failed at generated imports but was not
  the task failure. After generation, `tsc -b --clean` followed by
  `typecheck:build:generated` reproduced `TS2307` for `@spine-ts/core` in the
  client and the derived implicit-`any`, exactly isolating the missing edge.
- Compared the existing composite-project pattern in
  `packages/server/tsconfig.json`: workspace dependencies have explicit local
  project references. Added the smallest matching client edge, `../core`,
  before the existing `../proto` edge.
- No suitable existing build-graph unit-test seam exists. The focused clean
  composite build is the durable regression check; no new validator or test
  was added for one JSON reference.
- Implementer assignment was explicitly dispatched as the existing
  `implementer` role with expected `gpt-5.6-terra` / `medium`. This execution
  surface exposes no runtime model self-introspection metadata; acceptance must
  rely on the immutable dispatched profile unless the orchestrator reports
  otherwise.

## 2026-07-23 — Focused Verification Complete

- `corepack pnpm exec tsc -b --clean && corepack pnpm
typecheck:build:generated` passed from clean TypeScript composite outputs.
- `corepack pnpm exec tsc -b packages/client/tsconfig.json --force` and
  `corepack pnpm format:check` passed.
- `git diff --check` passed after restoring the unassigned executable-bit
  change that Proto generation made to
  `packages/client/codegen/generate-projection-columns.mjs`.
- The implementation report now contains the RED/GREEN evidence, no-new-seam
  rationale, exact focused commands, changed-file scope, and model-metadata
  limitation. The implementation is ready for the required review wave.

## 2026-07-23 — Review Wave 1 And Correction

- TypeScript/API review is clean. Runtime self-introspection was unavailable;
  the explicit immutable dispatched profile matched
  `gpt-5.6-terra` / `high`.
- Style/maintainability found one P2 record omission: the task brief lacked the
  mandatory human-imposed requirements ledger. Runtime self-introspection was
  unavailable; the explicit immutable dispatched profile matched
  `gpt-5.6-terra` / `high`.
- Added the applicable ledger as a deterministic record-only correction. No
  production, test, build, or API file changed; only focused maintainability
  re-review is required.

## 2026-07-23 — Review Convergence

- Focused style/maintainability re-review is clean. Runtime self-introspection
  remained unavailable; the explicit immutable dispatched profile matched
  `gpt-5.6-terra` / `high`.
- Both required concerns are clean. Documentation and reliability are N/A for
  the configuration-only endpoint; final security remains owned by T-0067.
- Final verification cleaned every TypeScript composite output and then passed
  `typecheck:build:generated` from scratch. Exact-file Prettier and
  `git diff --check` also passed. The endpoint is ready for commit.

## 2026-07-23 — Integration Closure

- Committed the accepted correction as `4341b359` and immediately pushed
  `task/T-0067a-client-core-reference` to `origin`.
- Fast-forwarded `main` to the correction while preserving every unrelated
  root file.
- Post-merge verification cleaned every TypeScript composite output and passed
  `typecheck:build:generated` on merged `main`.
