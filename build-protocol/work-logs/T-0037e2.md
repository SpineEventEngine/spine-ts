# T-0037e2 Work Log

Status: Slice 1 review wave 3 assigned

Derived status mirror: the canonical current state is the `Status` header in
`build-protocol/tasks/T-0037e2-reusable-generation-stop/TASK.md`.

## Slice 1 Implementer Start

- After architecture commit `b0a09e3f`, the existing implementer began only Slice 1, Private
  Stop Foundation And Happy Path, at assigned `gpt-5.6-terra` / `medium`, with
  no subagents. The required behavior is the sole private initial stop path,
  retained-state-only retry, one candidate, stable survivor handles,
  publication-only generation view, no worker overlap/lifecycle gap, and shared
  duplicate in-flight attempt. Slices 2--4 and all stated exclusions remain
  out of scope.
- Before production/test edits, performed the canonical skill-applicability check
  before production/test edits. Session inventory was supplied in the task
  context; task-provided skills are `test-driven-development`, `implement`,
  `javascript-testing-patterns`, `typescript-advanced-types`,
  `nodejs-backend-patterns`, `codebase-design`, and
  `verification-before-completion`. Read
  `build-protocol/skills/EXPECTED_SKILLS.md`; enumerated readable installed
  entrypoints with `find /Users/armiol/.agents/skills -maxdepth 2 -type f -name
SKILL.md -print`; and inspected `/Users/armiol/.agents/.skill-lock.json`.
  Selected and read the seven named skill instructions. TDD/testing governs
  focused real-behavior RED/GREEN; codebase design retains the existing
  attachment module as the private deep seam; TypeScript/Node guidance keeps
  new state private and lifecycle handling small; completion verification
  governs evidence-backed handoff. `implement`'s commit/review steps conflict
  with explicit no-commit/no-subagent instructions and are not followed.
  Expected workflow/ADR skills were skipped because the worktree already
  exists, no new plan or ADR is authorized, and this implementer cannot
  dispatch review. No source was unreachable.
- `2026-07-13T04:55:45Z`: RED evidence: `pnpm exec vitest run
packages/server/test/server/environment-generation-stop.test.ts` ran one
  focused behavior test and failed exactly because
  `serverEnvironmentAccess.stopDelivery` is not a function. No production code
  existed for the requested initial explicit-stop path at this point.
- `2026-07-13T04:57:21Z`: GREEN evidence: the focused test passed after the
  private access and transition implementation; the focused T-0037d/e1
  attachment regression command ran 2 files / 62 tests with zero failures.
  `pnpm typecheck:build:generated` also exited successfully after the focused
  test. Final formatting and scope/status checks remain pending.
- `2026-07-13T05:00:27Z`: Added focused stable-route and both-survivor-detach
  assertions, then ran `pnpm typecheck:build:generated` and the focused stop
  plus T-0037d/e1 attachment regression command: 2 files / 62 tests passed.
  The stable route assertion was added after its repair, so it is GREEN-only
  evidence and must not be represented as a second RED/GREEN cycle. The first
  explicit-stop test retains the valid RED/GREEN record above. Remaining Slice
  1 proof work is deterministic phase-order/candidate-union observation and a
  retained-stop retry continuation test; no later-slice fault/race machinery
  was added.
- `2026-07-13T05:05:00Z`: Added the requested deterministic sequential
  candidate barrier test directly against `EnvironmentAttachments`. It was
  validation-first because the current production ordering already held: the
  test passed with two focused tests. It proves old retire precedes candidate
  start, both handles retain the old generation through both deferred candidate
  recoveries, a closed stable route starts no candidate work, publication
  updates both handles together, route reopening admits the buffered readiness,
  both startup target URLs occur once, factory count is two, and the original
  handles complete non-last then last detach. The earlier entry-path test
  remains the honest RED/GREEN evidence for this feature.
- `2026-07-13T05:07:17Z`: Settlement-tail RED: added a third deferred
  candidate-worker gate for buffered readiness and asserted that publication
  updates both handles while the stop remains pending. Focused Vitest ran 2
  tests / 1 failed at `expect(stopSettled).toBe(false)`, receiving `true`; this
  proves fire-and-forget route notification allowed `stopDelivery()` to resolve
  before transition-admitted candidate work settled.
- `2026-07-13T05:07:37Z`: Settlement-tail GREEN: all prepared routes now open
  synchronously, their buffered scope union is admitted once through the
  candidate coordinator's awaited start path, and stop completion follows that
  settlement. The focused stop file passed 2 tests / 2 passed.
- `2026-07-13T05:08:10Z`: Final focused verification passed: reusable-stop plus
  T-0037d/e1 attachment regressions ran 2 files / 63 tests with zero failures;
  `pnpm typecheck:build:generated` exited successfully; focused Prettier write
  made no changes and focused Prettier check passed; `git diff --check` passed;
  all three canonical status mirrors remain exactly `Slice 1 implementation in
progress`. Exact inventory is the two assigned production files, the new
  focused stop test, and the three authorized T-0037e2 task/work/review records.
  No public docs, examples, exports, generated output, completion-plan policy,
  commit, push, or full `pnpm verify` is included.

## Slice 1 Coordinator Verification And Review Dispatch

- At `2026-07-13T05:09:52Z`, the coordinator independently reran the focused
  reusable-stop and T-0037d/e1 attachment suite: 2 files / 63 tests passed.
  `typecheck:build:generated`, focused Prettier check, and `git diff --check`
  also passed.
- Lightweight pre-review lint found exact status mirrors, the six authorized
  changed paths only, no generated output, no root/package export or public-doc
  mention of the private stop operations, no duplicated policy constant, and no
  active claim for future monitor/scheduler/topology/adapter/catch-up behavior.
  Historical superseded text remains non-actionable.
- Implementer dispatch was explicit `gpt-5.6-terra` / `medium`. The execution
  surface's immutable runtime role registry fixes the existing implementer to
  that same profile, and the child reported no contradictory runtime value; no
  subagents were spawned. This fixed runtime role metadata, explicit dispatch,
  and matching result satisfy the profile acceptance gate even though the
  child surface did not separately echo an independent profile field.
- Slice 1 review wave 1 assigns the existing style/maintainability reviewer at
  explicit `gpt-5.6-terra` / `high`, documentation reviewer at explicit
  `gpt-5.6-luna` / `medium`, TypeScript/API docs reviewer at explicit
  `gpt-5.6-terra` / `high`, and performance/reliability reviewer at explicit
  `gpt-5.6-terra` / `high`. All are read-only and may not spawn subagents.

## Slice 1 Review Wave 1 Outcome

- Documentation was clean. The lane confirmed exact status mirrors, accurate
  current-slice chronology/scope, no public stop docs/export claim, no future-
  slice overclaim, and no unrelated file.
- Style/maintainability, TypeScript/API docs, and performance/reliability
  independently identified one deduplicated P1: `GenerationStop` retains only a
  rejected promise/status, so retry restarts route closure rather than resuming
  one operation; simultaneous retries queue different attempts. This violates
  the active retained-operation contract and leaves the Slice 2 checkpoint
  foundation unsound.
- TypeScript/API docs identified one additional P1: stop admission does not
  reject while T-0037e1 failed-start rollback or unsafe last-detach recovery
  owns the generation. Stop could replace state still owned by another explicit
  retry operation.
- No lane found a public/root export, package-export, TypeDoc, Protobuf, docs, or
  generated-artifact leak. No other concrete Slice 1 defect was reported.
- All four lanes performed the canonical skill check and spawned no subagents.
  Immutable runtime role metadata fixes style, TypeScript/API docs, and
  performance/reliability to `gpt-5.6-terra` / `high`, and documentation to
  `gpt-5.6-luna` / `medium`; each was explicitly dispatched at that profile and
  reported no contradiction. Child surfaces did not echo a separate profile
  field, so acceptance relies on the fixed runtime role registry plus explicit
  matching dispatch rather than inherited defaults.
- The same existing implementer context is resumed at explicit
  `gpt-5.6-terra` / `medium` to fix the complete deduplicated batch. Required
  tests cover recovery-owner admission refusal, retained operation progress,
  and concurrent retry promise coalescing. No Slices 2--4 expansion is
  authorized beyond the minimum retained-state foundation needed to make retry
  real.

- `2026-07-13T04:26:45Z`: Created branch
  `task/T-0037e2-reusable-generation-stop` and isolated worktree from integrated,
  post-merge-verified `main` at `8e3139ac`. Root retains only the untracked
  user-owned `human-review-1-jul.md`, which remains untouched.
- Installed the frozen pnpm workspace with no lockfile change. Fresh-worktree
  baseline initially lacked ignored package declarations and Protobuf outputs;
  `proto:generate` followed by `typecheck:build:generated` corrected setup.
  Focused coordinator/parked/records/attachment baseline passes 4 files /
  135 tests. Generated artifacts remain ignored.
- T-0037e2 changes reusable generation ownership, four ordered fresh-transition
  phases, canonical-scope transfer, publication/admission, concurrency, and
  same-operation retry. One existing requirements splitter is assigned at
  expected and explicit `gpt-5.6-sol` / `high`, documentation-only ownership,
  no production/test edits, and no subagents. It must inspect current TS/JVM
  evidence and produce a small-slice architecture resolution before
  implementation.

## Requirements-Splitter Skill Applicability Check

- Check completed and durably recorded before architecture research or any
  other task edit, as required by `BUILD_PROTOCOL.md`. The assignment provides
  no task-specific skill path, but exposes a bounded session inventory of
  built-in/system and user-installed skills. The role is the existing
  requirements splitter at explicit expected profile `gpt-5.6-sol` / `high`;
  it will not spawn subagents.
- Repo-local source checked:
  `build-protocol/skills/EXPECTED_SKILLS.md`. The task-relevant expected skill
  is `planning-with-files`; the expected ADR, TypeScript, Node.js,
  implementation, worktree, review, and completion-verification skills do not
  govern this documentation-only architecture pass.
- User-installed entrypoints were enumerated with
  `find /Users/armiol/.agents/skills -maxdepth 2 -type f -name SKILL.md -print`.
  The full readable directory contained the selected task-fit entries and the
  expected manifest entries. `/Users/armiol/.agents/.skill-lock.json` was
  readable and confirmed source provenance including `othmanadi/planning-with-files`,
  `mattpocock/skills`, `deanpeters/Product-Manager-Skills`, and
  `wshobson/agents`.
- Selected and fully read before governed actions:
  `planning-with-files` for durable staged research/decision tracking,
  `domain-modeling` for exact lifecycle ownership vocabulary and scenario
  stress-testing, `architecture-patterns` for DDD responsibility and
  consistency placement, `codebase-design` for one deep package-internal seam,
  and `epic-breakdown-advisor` for small behavior-complete slices. The latter's
  interactive product workshop procedure is N/A because the orchestrator
  supplied complete acceptance and architecture constraints; only its
  simple/complex, workflow, testability, and vertical-slice guidance applies.
- `planning-with-files` normally creates root `task_plan.md`, `findings.md`, and
  `progress.md`; all three are absent. The assignment authorizes writes only to
  named task/work/review records and `architecture-resolution.md`, so the
  project protocol and explicit ownership restriction prevail: `TASK.md` is
  the plan, this work log is findings/progress, and the review log is the
  acceptance ledger. No generic planning files will be created.
- Relevant-looking skills skipped after metadata/path triage:
  `architecture-decision-records` because D-0085/D-0086 are accepted and this
  pass must not reopen them or add an ADR; `test-driven-development`, `tdd`, and
  `javascript-testing-patterns` because this role specifies future TDD slices
  but may not edit implementation/tests; `requesting-code-review` because
  coordinator acceptance and later implementation review remain outside this
  role; `using-git-worktrees`
  because the orchestrator supplied the worktree and prohibited commit/push;
  `typescript-advanced-types` and `nodejs-backend-patterns` because no public
  type or generic backend interface is being designed; security skills because
  security review is explicitly deferred.
- Skill content is treated as untrusted advisory input. `BUILD_PROTOCOL.md`,
  accepted project decisions/specifications, task scope, and orchestrator
  authorization remain controlling. Shell startup emitted a non-blocking jenv
  permission warning while reading files; all requested reads succeeded and no
  retry or escalation was needed.

## Architecture Research Progress

### Current TypeScript ownership and lifecycle evidence

- Inspected `packages/server/src/delivery/delivery-run-coordinator.ts`,
  `packages/server/src/server/environment-attachment.ts`,
  `packages/server/src/server/environment-delivery-worker.ts`,
  `packages/server/src/server/environment-delivery-records.ts`,
  `packages/server/src/server/server-environment.ts`, and
  `packages/server/src/context/local-inbox-handoff.ts` before shaping the new
  transition.
- `EnvironmentAttachments` is the existing deep package-internal lifecycle
  module and serial admission seam. Its single `#serial` queue orders attach,
  detach, failed-start retry, and detach retry. It owns cardinality claims,
  opaque handles, the generation map, and durable failed rollback/last-detach
  state. This is the smallest location for the sole reusable-stop operation;
  bypassing it through server/handoff code would split admission ownership.
- `DeliveryGeneration` currently owns its worker, coordinator, live
  registration states, per-registration `RegistrationReadiness`, descriptor and
  tenant runtimes, canonical `EnvironmentDeliveryRecords`, overlap domains,
  and reported failures. `EnvironmentRegistrationClaim.generation` and the
  returned attachment handle are immutable old-generation identities today;
  reusable stop therefore needs an internal rebinding indirection rather than
  mutating or replacing public/returned handle values.
- `DeliveryReadiness.transition()` is a one-way direct-to-routed handoff with
  bounded buffering while direct claims settle. `RegistrationReadiness` is a
  separate registration-local waiting/open/failed bridge. Existing descriptor
  readiness routes close only by invoking each descriptor transition; writes
  during that transition buffer there, while writes after routing but before
  registration readiness opens buffer in `RegistrationReadiness`.
- `EnvironmentDeliveryRecords` is generation-local and owns configured scope
  order plus registration/generation/shared parked obligations. Existing
  `retire()` consumes all ownership, while non-last `detach()` and failed-start
  `rollback()` intentionally remove narrower ownership. Reusable stop cannot
  call `retire()` before survivor state has been captured by a bounded
  transition owner, and scope transfer must preserve the existing canonical
  order without route-rebinding the scope values.
- `DeliveryRunCoordinator.retire()` is externally retryable. It closes
  admission and calls stop once, retries quiescence until proved, then attempts
  report and permanent worker retirement, marks `#finalized = true`, and only
  then propagates accumulated post-quiescence failures. Consequently
  `replacementSafe` is false for stop/quiescence failure and true for report or
  inert-retirement failure. The coordinator is permanently inert whenever
  replacement-safe, even when `retire()` rejects.
- Existing failed-start and detach retry states are opaque module-owned objects
  with stable progress flags and shared in-flight promises. Reusable stop should
  follow this precedent: one module-owned operation record, external retry of
  that record, no timer/background recursion, and no caller-visible retry
  token. It must remain distinct from `#failedRollback` and
  `#failedLastDetach`, which T-0037e1 already owns and this task must not reopen.

### Focused TypeScript test evidence

- Inspected focused retirement, readiness handoff, attachment linearization,
  failed-start/detach retry, and canonical record tests in
  `delivery-run-coordinator.test.ts`, `local-inbox-handoff.test.ts`,
  `environment-attachment.test.ts`, and
  `environment-delivery-records.test.ts`.
- Coordinator tests prove one published retirement promise under reentry;
  stop-before-active-settlement-before-await/report/retire order; false
  `replacementSafe` through pending report; true `replacementSafe` after
  reporting or permanent-retirement failure; stable final rejection without
  repeated phases; and external unsafe retry that repeats only the failed
  stop/quiescence checkpoint.
- `DeliveryReadiness` tests prove direct admission closes synchronously,
  pre-close drains settle, transition-time writes buffer and route exactly once,
  post-transfer routes cannot be replaced through `onReady`, an invalid scope
  fails the finite transition closed, and exactly one simultaneous retry wins
  from the failed checkpoint. Reusable stop therefore cannot reuse
  `DeliveryReadiness.transition()` to rebind an already routed route; the
  environment registration layer must own a private rebindable route while the
  descriptor's one-way route remains installed.
- Attachment tests prove `#serial` delays registration claims, descriptor
  enumeration, and worker construction; ordinary last detach clears a safe slot
  before a queued attach constructs one fresh generation; unsafe quiescence
  leaves the old slot and blocks queued attach until explicit retry; safe report
  or retirement failure still permits the queued fresh generation; and
  failed-start retry and detach retry remain separate entries with their own
  stable promises/checkpoints.
- Record tests prove stable configured order, deduplicated dynamic scopes,
  exact registration ownership, retained shared/generation records, and atomic
  whole-generation consumption. The reusable transition needs a transfer
  operation on a captured canonical snapshot/owner, not a second independent
  parked-record hierarchy and not repeated reconstruction from descriptors.
- The split must preserve behavior-complete vertical lifecycle increments. A
  phase-only horizontal package that cannot complete or safely retry an
  end-to-end transition is excluded; each slice must either establish one deep
  internal operation seam or add a complete externally observable failure/race
  behavior through that seam.

### Accepted decision/specification evidence

- Inspected accepted D-0085 and D-0086 in `DECISION_LOG.md`, T-0037b/d/e1 task
  records, the accepted T-0037e1 architecture resolution, and the active
  `PROJECT_COMPLETION_PLAN.md`, `RUNTIME_ARCHITECTURE.md`, `TECHNICAL_SPEC.md`,
  `DEVELOPER_API.md`, and `CODE_QUALITY.md` constraints. T-0037e1 is closed and
  must not be redesigned: its failed-start, non-last detach, ordinary last
  detach, handle identity, serial admission, and retry ownership remain intact.
- D-0086 exclusively assigns T-0037e2 the sole package-internal reusable-stop
  entry, one transition-owned candidate, separate rebind/transfer checkpoints,
  publication, admission reopen, external retry, and racing-attach join. It
  forbids direct primitive use by server/handoff code and any public lifecycle
  surface or documentation. No accepted source authorizes a second lifecycle
  gate, a second record ledger, route rebinding of canonical scopes, or
  candidate recreation after construction.

### Spine JVM evidence

- The instructed `/private/tmp/spine-research/core-jvm/server` checkout was not
  present. Following the repository's recorded fallback, inspected local notes
  `spine-jvm-docs/spine-routing-dispatch-and-delivery.md` and
  `spine-jvm-docs/spine-server-runtime-and-bounded-context.md`, then current
  local source under `/Users/armiol/development/Spine/core-jvm/server`:
  `ServerEnvironment.java`, `delivery/Delivery.java`,
  `delivery/InboxDeliveries.java`, `delivery/ShardedWorkRegistry.java`, and
  `delivery/DeliveryMonitor.java`.
- JVM `ServerEnvironment` shares one delivery facility and storage/transport
  configuration across bounded contexts in a process. TypeScript keeps the
  accepted explicit per-environment object rather than copying the JVM
  singleton, but environment-level delivery ownership is familiar and supports
  keeping replacement coordination inside `EnvironmentAttachments`.
- JVM `Delivery` centrally owns inbox storage, registered target deliveries,
  shard observers, and exclusive shard work sessions. `InboxDeliveries`
  registers and unregisters target callbacks, and `deliverMessagesFrom()`
  releases the picked shard in `finally` before notifying a late write. This
  supports explicit route/registration ownership and settlement before owner
  replacement; it does not define a second public generation object.
- JVM retries are explicit monitor-selected actions, and a stopped stage needs a
  later explicit trigger. That supports external retry with no self-loop, while
  D-0085/D-0086 add the stronger TypeScript requirement that transition-owned
  buffered readiness is eventually admitted during the same successful retry,
  without an unrelated write/monitor action.
- The JVM source has no reusable generation-stop/candidate publication state
  machine, no TypeScript promise-race policy, and no exact rebind/transfer
  checkpoint contract. Those semantics remain governed by accepted D-0085/
  D-0086. JVM public `DeliveryMonitor`, catch-up, singleton, per-message thread,
  repeat callback, scheduler, and topology concepts are expressly not copied.

### Completion-verification skill selection

- Before running this documentation pass's required completion gates, selected
  and fully read `verification-before-completion` from the repo-expected
  `obra/superpowers` installation. Its evidence-before-claim rule governs the
  fresh docs/status lint, Prettier, diff check, and exact inventory below. It was
  selected at the verification boundary rather than for architecture research;
  no completion claim predates these fresh commands.
- The first stale-status scan used an unsupported `rg` lookahead and returned a
  regular-expression parse error. It changed no files and is not accepted as
  evidence; the verification below reruns the check with a literal expression.

## Architecture Resolution Outcome

- Created
  `build-protocol/tasks/T-0037e2-reusable-generation-stop/architecture-resolution.md`.
  The smallest design keeps the existing `EnvironmentAttachments` serial gate,
  `DeliveryGeneration` runtime owner, `EnvironmentDeliveryRecords` canonical
  record owner, and stable `RegistrationReadiness` route. It adds only one
  private `GenerationStop` operation and private `RegistrationBinding`
  indirection.
- Resolved the package-internal interface as initial
  `serverEnvironmentAccess.stopDelivery(environment)` plus continuation
  `retryDeliveryStop(environment)`. The continuation resumes the sole retained
  operation and is not another stop caller. No public retry token, type, option,
  root export, documentation, or generated artifact is introduced.
- Resolved exact old/transition/candidate/claim/handle/attach ownership for every
  phase; one-way descriptor routes stay installed, while registration-local
  readiness destinations rebind under a closed transition gate. Claims and
  handles retain identity and commit their fresh generation binding only at
  candidate publication.
- Resolved one bounded canonical scope owner over configured, startup,
  buffered, and retained sources; source overlap coalesces, route and transfer
  checkpoints are separate, newly dirty readiness cannot be lost, and every
  transition-admitted candidate unit settles before error propagation.
- Resolved construction failure versus retained-candidate partial failure,
  unsafe quiescence retry, replacement-safe report/retirement error ordering,
  racing attach waiting without serial deadlock, publication/reopen, and
  once-only error propagation.
- Split implementation into four sequential review packages: private happy-path
  foundation; bounded capture/checkpoint/transition retry; old retirement
  safety/error ordering; and attach race/internal boundary. Each has explicit
  acceptance, likely files, focused tests, risk, and exclusions.
- No blocking architecture uncertainty remains. Coordinator acceptance is next.
  This role changed no production source, tests, public docs, examples,
  generated files, completion-plan policy, accepted decision, or unrelated
  record; it did not run implementation tests, commit, push, or spawn subagents.
- Requirements-splitter runtime metadata: expected and explicitly dispatched
  model `gpt-5.6-sol`, reasoning `high`; actual runtime metadata exposed to this
  role matches `gpt-5.6-sol` / `high`.

## Record Verification

- Docs/status lint passed: the canonical task, architecture resolution, work
  log, and review log each have the exact current status `Architecture
resolution ready for coordinator acceptance`; the changed inventory contains
  no production source, test, public documentation, example, generated file, or
  unrelated record.
- Prettier write and check passed for all four changed records.
- `git diff --check` passed.
- Exact changed inventory: modified `TASK.md`, this work log, and the review
  log; new `architecture-resolution.md`. `PROJECT_COMPLETION_PLAN.md` remains
  unchanged because its accepted T-0037e2 milestone scope and gate already
  match this resolution.
- Every check emitted the same non-blocking shell-startup warning that jenv
  could not write `~/.jenv/jenv.version`; each requested command itself exited
  successfully. No implementation test was run for this documentation-only
  requirements-splitting assignment.

## Coordinator Architecture Acceptance

- At `2026-07-13T04:49:19Z`, the coordinator accepted the architecture after
  checking the full resolution against the current T-0037b/d/e1 implementation,
  task invariants, and private-access boundary.
- The accepted design has one lifecycle owner, one retained unpublished
  candidate, one publication commit, stable handle identity, separate route and
  canonical-scope checkpoints, externally triggered retry, and an attach waiter
  that exits serial admission before waiting. These properties avoid duplicate
  coordinator authority, partially published ownership, and retry deadlock.
- Slice 1 is authorized as one complete happy-path increment only. The existing
  implementer is assigned at expected and explicit `gpt-5.6-terra` / `medium`,
  with production/test ownership limited to the Slice 1 files and no subagents.
  Construction/phase failures, retirement failures, attach races, public API,
  permanent close, and server integration remain excluded.
- Requirements-splitter acceptance metadata was checked before acceptance:
  expected and explicit dispatch `gpt-5.6-sol` / `high`; actual runtime metadata
  `gpt-5.6-sol` / `high`; no child subagents.

## Slice 1 Review Wave 1 Fix Evidence

- Coordinator audit before fix acceptance found one additional P1 in the same
  admission-ownership batch: the first fix moved generation/recovery snapshot
  to synchronous `stopDelivery()` call time instead of serialized admission.
  A stop called after a queued attach could resolve as a no-op before that
  attach became a survivor, and a stop queued behind a detach could miss the
  detach's later unsafe failure. The same implementer must move recovery checks,
  generation selection, and survivor snapshot back inside the existing serial
  gate, while preserving duplicate-call promise sharing and clearing a
  pre-admission refusal instead of retaining it as a failed stop operation.
- Coordinator contract audit also found that ordinary `stopDelivery()` after
  an admitted incomplete rejection returned the original failed promise. The
  accepted architecture requires an explicit retry-required refusal while only
  `retryDeliveryStop()` advances the retained operation. The same fix owner must
  add that bounded error boundary without changing in-flight duplicate sharing.

- `2026-07-13T05:19:28Z`: Focused Wave 1 RED ran 5 tests / 3 failed for the
  deduplicated batch. Concurrent retries returned distinct promises;
  failed-start rollback ownership allowed stop to resolve instead of refusing;
  and unsafe last-detach ownership reached route closure and failed with
  `Registration readiness is not open` instead of the established detach-retry
  refusal. The preceding parse-only test error at `05:19:10Z` was a test-edit
  placement mistake, changed no production code, and is not accepted as RED.
- `2026-07-13T05:20:38Z`: Focused Wave 1 GREEN ran 5 tests / 5 passed.
  `GenerationStop` now retains selected old generation, survivor snapshot,
  closed routes, old-retirement completion, constructed candidate identity,
  per-survivor preparation progress, publication, reopen/drain state, and one
  mutable attempt. Candidate-construction retry resumes the same operation,
  concurrent retries share one promise, ordinary stop retains the rejected
  attempt, and initial in-flight retry refuses. Failed-start rollback and unsafe
  last-detach owners now refuse stop before creating a retained blocker or
  mutating worker/route state; established recovery permits a later normal
  stop.
- `2026-07-13T05:21:27Z`: Wave 1 fix verification passed. Focused reusable-stop
  plus T-0037d/e1 attachment regressions ran 2 files / 66 tests with zero
  failures; `pnpm typecheck:build:generated` passed; focused Prettier write and
  check passed; `git diff --check` passed; public export/doc leakage scan found
  no stop/retry names; and the exact changed inventory contains only the
  assigned attachment production file, focused stop test, and three authorized
  T-0037e2 records. All status mirrors remain exactly `Slice 1 review wave 1
fixes assigned`. No full verify, generated change, commit, or push occurred.
- `2026-07-13T05:24:14Z`: Serialized-admission RED ran 7 tests / 2 failed.
  Attach queued before stop allowed the stop to settle as a synchronous no-op,
  and unsafe last detach queued before stop let the stop mutate a failed route
  and reject with `Registration readiness is not open` instead of the
  established detach-retry refusal.
- `2026-07-13T05:24:45Z`: Serialized-admission GREEN ran 7 tests / 7 passed.
  Initial stop now creates only one empty shared operation identity. Its queued
  continuation checks T-0037e1 recovery ownership and then selects generation,
  old runtime, and survivors before admission or route closure. Pre-admission
  refusal clears the operation blocker; admitted stops retain progress exactly
  as before. The three earlier Wave 1 fix timestamps above were corrected from
  mistakenly labelled Lisbon-local `06:xxZ` to coordinator UTC `05:xxZ`; no
  accepted history was otherwise changed.
- `2026-07-13T05:25:24Z`: Fix-audit verification passed. Focused reusable-stop
  plus T-0037d/e1 attachment regressions ran 2 files / 68 tests with zero
  failures; `pnpm typecheck:build:generated` and focused Prettier passed;
  `git diff --check` passed; the mistaken `06:19`/`06:20`/`06:21` UTC labels
  are absent; public export/doc leakage scan found no stop names; exact status
  mirrors match; and inventory remains the assigned attachment source, focused
  stop test, and three T-0037e2 records. No full verify, generated change,
  commit, push, or subagent occurred.
- `2026-07-13T05:27:48Z`: Explicit-retry contract RED ran 7 tests / 1 failed:
  ordinary `stopDelivery()` after candidate-construction rejection replayed
  `candidate construction failed` instead of the stable explicit-retry
  refusal. Worker retirement and factory counts were captured before explicit
  retry to prove ordinary stop performs no continuation work.
- `2026-07-13T05:28:01Z`: Explicit-retry contract GREEN ran 7 tests / 7
  passed. Running duplicate stops still share the exact attempt; rejected
  ordinary stop now returns private stable `Environment delivery stop requires
an explicit retry.` refusal without advancing retained state; concurrent
  `retryDeliveryStop()` calls continue to share and resume one attempt. The
  corrected parse-edit timestamp remains `05:19:10Z`.
- `2026-07-13T05:28:32Z`: Final contract-fix verification passed. Focused stop
  plus T-0037d/e1 attachment regressions ran 2 files / 68 tests with zero
  failures; `pnpm typecheck:build:generated`, focused Prettier, and
  `git diff --check` passed; all status mirrors match; `05:19:10Z` remains the
  sole parse-edit timestamp; public export/doc leakage scan is empty; and exact
  inventory remains the assigned attachment source, focused stop test, and
  three T-0037e2 records. No full verify, generated change, commit, push, or
  subagent occurred.
- At `2026-07-13T05:29:42Z`, the coordinator independently reran reusable-stop
  plus T-0037d/e1 regressions (2 files / 68 tests), the public-index regression
  (1 file / 10 tests), `typecheck:build:generated`, focused Prettier, and
  `git diff --check`; all passed. Fresh pre-review lint found exact status
  mirrors, only the five authorized fix paths, no generated/public export/doc
  leak, no duplicated policy constant, and no active future-policy overclaim.
- Slice 1 review wave 2 assigns the same four existing read-only reviewers with
  no subagents: style/maintainability `gpt-5.6-terra` / `high`, documentation
  `gpt-5.6-luna` / `medium`, TypeScript/API docs `gpt-5.6-terra` / `high`, and
  performance/reliability `gpt-5.6-terra` / `high`.

## Slice 1 Review Wave 2 Outcome

- Style/maintainability and performance/reliability were clean. They confirmed
  one retained stop owner, coalesced retry, serialized selection, pre-admission
  cleanup, ordered publication/reopen/settlement, bounded state, and no second
  coordinator/ledger/gate.
- Documentation reported one P2: two active task-summary sentences still said
  verification remained after coordinator verification had completed. Those
  sentences are corrected in the same durable update; no other chronology,
  status, public-doc, future-policy, or evidence defect was found.
- TypeScript/API docs reported one P1 ownership asymmetry. Stop admission checks
  failed-start and unsafe-last-detach recovery but not a rejected non-last
  detach whose live registration/readiness remains owned by `retryDetach()`.
  Conversely, detach and retry-detach do not refuse an admitted rejected stop,
  so they can remove a survivor frozen in that stop and let later stop retry
  publish a phantom registration.
- The same implementer is resumed at immutable and explicit
  `gpt-5.6-terra` / `medium`. Focused RED/GREEN must prove both directions:
  rejected non-last detach blocks stop until detach retry, and rejected stop
  blocks detach/retry-detach until stop retry. No attach-waiter/racing-attach or
  later-slice behavior is authorized.
- All four reviewers performed the canonical skill check, spawned no subagent,
  and reported actual retained profiles matching explicit dispatch: docs
  `gpt-5.6-luna` / `medium`; all other lanes `gpt-5.6-terra` / `high`.

## Slice 1 Review Wave 2 Fix Evidence

- `2026-07-13T05:39:34Z`: Wave 2 ownership RED ran 9 focused tests / 3
  failed for the intended defects. Stop reached a failed non-last registration
  route and rejected with `Registration readiness is not open.` instead of the
  established detach-retry refusal. A detach called after candidate
  construction rejection resolved and removed a frozen survivor, as did a
  detach queued behind that rejecting stop, instead of returning the private
  delivery-stop retry refusal.
- `2026-07-13T05:40:34Z`: Wave 2 ownership GREEN ran 9 focused tests / 9
  passed. Serialized stop admission now recognizes a rejected non-last detach
  through the existing handle operation and live generation registration.
  Call-time detach/retry-detach and serialized detach admission recognize a
  retained admitted incomplete stop's frozen survivor. No second owner registry
  or lifecycle gate was added.
- `2026-07-13T05:41:09Z`: The strengthened focused test remained 9 / 9 green
  after proving the remaining survivor's stable route still admits readiness
  following pre-admission stop refusal. Detach retry removes only its owned
  registration, after which a normal stop replaces the surviving generation.
- `2026-07-13T05:41:37Z`: Wave 2 fix verification passed so far: reusable-stop
  plus T-0037d/e1 attachment regressions ran 2 files / 70 tests with zero
  failures; the public-index regression ran 1 file / 10 tests; and
  `pnpm typecheck:build:generated` passed. Formatting, status/public/inventory
  lint, and `git diff --check` remain to be recorded below. No full verify,
  generated change, commit, push, or subagent occurred.
- `2026-07-13T05:43:25Z`: Final Wave 2 fix evidence passed after formatting:
  2 files / 70 focused and T-0037d/e1 tests, 1 file / 10 public-index tests,
  `pnpm typecheck:build:generated`, focused Prettier write/check, and
  `git diff --check`. All three status mirrors are exactly `Slice 1 review wave
2 fixes assigned`; exact inventory is the assigned attachment source,
  focused stop test, and three T-0037e2 records. Public/docs/examples/generated
  path leakage is empty, and each private explicit-retry policy message has one
  source definition. No full verify, generated change, commit, push, or
  subagent occurred.
- At `2026-07-13T05:45:29Z`, the coordinator independently reran the reusable-
  stop and T-0037d/e1 suite (2 files / 70 tests), public-index regression (1
  file / 10 tests), `typecheck:build:generated`, focused Prettier, and
  `git diff --check`; all passed. Fresh pre-review lint found exact status
  mirrors, the five authorized paths only, no generated/public export/doc leak,
  and one shared source definition for each private retry policy error.
- Slice 1 review wave 3 assigns the same four existing read-only lanes without
  subagents at their immutable explicit profiles: style, TypeScript/API docs,
  and performance/reliability `gpt-5.6-terra` / `high`; documentation
  `gpt-5.6-luna` / `medium`.
