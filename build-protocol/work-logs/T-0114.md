# T-0114 Work Log

## Task Identity

- Branch: `task/T-0114-system-event-policy`.
- Worktree: `.worktrees/T-0114-system-event-policy`.
- Baseline: `origin/main@b233d06e`.
- Authoring agent: `/root/t0114_impl`.
- Started: 2026-08-05 WEST.
- Ended: implementation complete; review and integration pending.
- Coverage: focused changed-source coverage passes: 97.14% statements, 92.18%
  branches, 98.38% functions, and 97.42% lines for `event-bus.ts`.
- Integration: implementation branch commit `bffc294d` pushed to canonical
  `origin/task/T-0114-system-event-policy`; review and integration remain
  pending.

## Start Boundary

- The Desktop execution surface supports the required explicit child model and
  reasoning fields.
- The approved Sol/high architecture pass is T-0113 and will not be repeated.
- The primary checkout was inspected and remains dirty with protected
  user-owned files; all implementation is isolated here.
- `pnpm install --frozen-lockfile` completed without warnings.
- Relevant current TS paths are `packages/server/src/bus/event-bus.ts` and
  `packages/server/test/bus/event-bus.test.ts`.
- Relevant JVM behavior was already frozen by T-0113. The implementer must
  inspect the task-relevant local JVM research/source evidence without building
  or modifying the JVM project before changing server runtime code.

## Decisions

- Keep the public constructor as the storing default.
- Add the smallest package-internal selector needed by T-0115.
- Do not select a third-party dependency; this is intrinsic bus behavior.
- Use RED-GREEN-REFACTOR and retain exact focused test evidence.

## Pending Boundaries

- Mechanical pre-review lint.
- Relevant specialist review wave.
- One converged release verification.
- Commit, immediate task-branch push, merge, post-merge verification, and main
  push before T-0115 starts.

## Implementation Evidence

- TDD RED: `pnpm exec vitest run packages/server/test/bus/event-bus.test.ts`
  ran before production changes and failed as intended: `TypeError:
eventBusAccess.createForgettingBus is not a function` at the new
  forgetting-policy behavior test (`35 tests | 1 failed`).
- GREEN: `pnpm exec vitest run packages/server/test/bus/event-bus.test.ts` passed
  all 35 tests immediately after implementation.
- Final focused test and coverage: `pnpm exec vitest run --coverage
--coverage.include=packages/server/src/bus/event-bus.ts
packages/server/test/bus/event-bus.test.ts` passed all 36 tests. Changed
  `event-bus.ts` coverage was 97.14% statements, 92.18% branches, 98.38%
  functions, and 97.42% lines.
- Mechanical validation passed: `pnpm typecheck:build`, `pnpm typecheck:tooling`,
  focused ESLint, `pnpm format:check`, `git diff --check`, `pnpm lint:tsdoc`,
  `pnpm docs:api:check`, and `pnpm proto:check-generated:current`.
- The TypeDoc API check found 235 expected server exports. The forgetting
  factory remains only on the `@internal` module access object and is absent
  from the server package root.
- A direct unscoped `vitest --coverage` run was intentionally not used as
  acceptance evidence because it enforces repository-wide thresholds against
  unexecuted packages. Source-scoped coverage above is the protocol-defined
  changed-source measure.
- Self-review: the public constructor still has the same two-parameter shape
  and uses the storing path; stored replay/follow-up methods remain unchanged.
  The new internal factory marks an instance before callers can post. Its
  forgetting branch validates, accepts, dispatches, and notifies without any
  `EventStore` operation. No public exports, settings, roles, or System Context
  behavior were added.
- Commit and push: implementation committed as `bffc294d`
  (`feat(server): add internal event forgetting policy`) and pushed immediately
  to `origin/task/T-0114-system-event-policy`.
- Pending: orchestrator review wave, converged release verification, and
  integration. The untracked review record is intentionally left for the
  orchestrator/reviewers.

## Implementation Assignment Acceptance

- Agent: `/root/t0114_impl`.
- Explicit dispatch: implementer, `gpt-5.6-terra` / medium.
- Actual available metadata: immutable implementer role/profile matches the
  expected model and reasoning. Independent runtime self-introspection was not
  exposed; no visible fallback or mismatch occurred.
- Outcome: accepted for specialist review.

## Review Wave 1

- Style/maintainability `/root/t0114_style`, explicitly Terra/high: accepted
  P2 findings for missing admission/order coverage and stale internal-access
  documentation.
- TypeScript/API `/root/t0114_api`, explicitly Terra/high: accepted P2 for
  stored-only subscriber/factory contracts that are now inaccurate.
- Performance/reliability `/root/t0114_reliability`, explicitly Terra/high:
  accepted P1 because forgetting still constructs and closes event storage;
  accepted overlapping P2 failure/order coverage.
- Documentation remains N/A because no public prose or workflow changed.
- Actual child self-introspection was unavailable; immutable configured roles
  match every explicit dispatch and no fallback was visible.
- One consolidated correction batch returns to the existing implementer.

## Review-Correction Evidence

- TDD RED: before correction code, `pnpm exec vitest run
packages/server/test/bus/event-bus.test.ts` ran 38 tests with 3 expected
  failures. The forgetting factory treated dispatchers as an EventStore (no
  schema registered), and close tried to call `#eventStore.close()` on that
  non-store value. The failures prove the accepted P1 ownership/lifecycle gap.
- GREEN correction: complete; converged release verification remains pending
  after targeted re-review.

## Review-Correction Verification

- GREEN: the focused EventBus suite passes 38/38 tests after the correction.
  The forgetting factory takes only dispatchers and constructs a bus with an
  internal sentinel, so the bus owns no EventStore. Public construction keeps
  its storing EventStore parameter and existing stored-event paths are intact.
- The new behavior tests prove no backing storage creation, append, read, or
  close over forgetting-bus construction, posting, and close. They also prove
  `validate`, `accept`, `dispatch`, then subscriber notification ordering and
  prove validation/admission failure prevents all later stages without storage
  access.
- Final changed-source coverage: 97.15% statements, 92.64% branches, 98.38%
  functions, and 97.43% lines for `event-bus.ts`.
- Correction validation passed: `pnpm typecheck:build`,
  `pnpm typecheck:tooling`, focused ESLint, `pnpm format:check`,
  `pnpm lint:tsdoc`, `pnpm docs:api:check` (unchanged 235 server exports),
  `pnpm proto:check-generated:current`, and `git diff --check`.
- Pending: correction commit/push, re-review of style, TypeScript/API, and
  reliability concerns, converged release verification, and integration.

## Targeted Re-review

- Style is clean.
- Reliability accepted the original P1/P2 correction and found one new P1:
  untyped `new EventBus(undefined)` must not select internal forgetting mode.
- TypeScript/API accepted export/signature isolation and found one new P2:
  place factory contract TSDoc on the `EventBusAccess` declaration.
- One final combined correction returns to `/root/t0114_impl`; only reliability
  and API reopen afterward.

## Final Targeted Correction

- TDD RED: `pnpm exec vitest run packages/server/test/bus/event-bus.test.ts`
  failed 1 of 39 tests because untyped `new EventBus(undefined)` did not throw.
- GREEN: 39/39 focused tests pass after adding the factory-only WeakSet mode
  marker and explicit runtime EventStore guard. The public constructor retains
  its typed storing signature.
- The factory contract now lives on the internal `EventBusAccess` declaration;
  the object-literal implementation has no redundant TSDoc.
- Final source-scoped coverage: 96.81% statements, 91.66% branches, 98.41%
  functions, and 97.05% lines for `event-bus.ts`.
- Typecheck, focused lint, TSDoc, TypeDoc/export, generated-clean, format, and
  diff checks were run for the corrected files.
- Final correction commit `286e8d7d` was pushed immediately to
  `origin/task/T-0114-system-event-policy`.

## Review Convergence

- Final reliability confirmation is clean: the public constructor cannot
  select forgetting, and forgetting has no EventStore lifecycle.
- Final TypeScript/API confirmation is clean: interface TSDoc is emitted,
  public construction is unchanged, and 235 server exports remain expected.
- Style remains clean. Documentation remains N/A for the recorded reason.
- No P0-P2 finding remains.

## Release Verification

- The mandatory cheap preflight passed 50 focused tests with 96.81% statement,
  91.66% branch, 98.41% function, and 97.05% line coverage for
  `event-bus.ts`. Both TypeScript checks, focused ESLint, formatting, TSDoc,
  TypeDoc/API, document-audience, generated-clean, and `git diff --check`
  passed.
- `pnpm verify:release` passed after review convergence: 186 test files passed
  and 3 skipped; 3,701 tests passed and 26 skipped. Repository coverage is
  93.99% statements, 90.02% branches, 94.48% functions, and 94.93% lines.
- Release gates verified 43 Spine Proto checksums, 52 frozen descriptors, 235
  server exports, 71 package imports, 47 package assets, and 294 relative
  Markdown links.
- Integration, post-merge verification, and remote `main` synchronization
  remain pending.
