# T-0097 Work Log

## 2026-08-03 — Task frame

- Started from pushed `origin/main@0f47c634` after T-0096 merged with exact
  reviewed-tree equality and passed 8/8 topology/policy plus 84/84 runtime
  post-merge tests.
- Classified high-risk because this is the final security, production-claim,
  container acceptance, and release-verification boundary for Wave 5.
- Reused the accepted G1 split; no duplicate requirements-splitter pass.
  Recorded one existing implementation owner with explicit expected
  `gpt-5.6-terra` / medium and all four canonical reviewers plus final security
  before dispatch.
- Runtime self-introspection is unavailable on this surface. Immutable
  configured role profiles and explicit dispatch fields are the acceptance
  evidence unless a visible mismatch or fallback occurs.
- Next: push the durable frame, inventory affected public/agent documentation,
  assign one bounded documentation owner, then run deterministic pre-review
  checks and one complete specialist wave.

## 2026-08-03 — Dependency preflight correction

- The documentation checkpoint is pushed as `f5651b23`; focused formatting,
  generated-doc, audience/jargon, and diff checks pass.
- Build-backed `verify:task -- --no-tests` exposed one real dependency defect:
  root `yaml@2.8.2` and the existing toolchain's `yaml@2.9.0` produce duplicate
  Vite type identities, so tooling TypeScript fails before review.
- Return this bounded correction to the same existing `implementer`, explicitly
  configured `gpt-5.6-terra` / medium. Align the root test dependency to the
  existing 2.9.0 line, regenerate the frozen lock, update D-0107 if needed, run
  the focused manifest tests, then rerun the complete cheap preflight. Commit
  and push immediately; no runtime or documentation scope expands.

## 2026-08-03 — Envoy validation correction

- After the dependency correction passed frozen install, 8/8 policy tests, and
  the complete cheap preflight, final local image acceptance passed 8/8.
- Final Compose lifecycle then failed because both Envoy containers exited.
  Direct `envoy --mode validate` reproduces the exact root cause in both files:
  `envoy.filters.http.cors` has an empty type URL, so Envoy cannot register the
  filter. All Compose resources were removed; no leak remains.
- Return this bounded configuration defect to the same existing `implementer`,
  explicitly `gpt-5.6-terra` / medium. Add the required v3 Cors typed
  configuration consistently to Compose and rendered Kubernetes references,
  add deterministic real-image config validation, rerun 8 policy tests and the
  full 3-case Compose lifecycle, commit, and immediately push. No other runtime
  or documentation scope expands.

## 2026-08-03 — Aggregated specialist correction

- Style and documentation reviews are clean. TypeScript/API accepted one P2
  storage/session ownership wording correction. Reliability accepted two P1
  Envoy defects: path-only routes admit non-POST direct requests, and disabling
  only the Activate route timeout leaves Envoy's default stream-idle limit.
- Return the complete batch to the same existing `implementer`, explicitly
  `gpt-5.6-terra` / medium. Require `:method = POST` for all six RPC routes,
  configure `stream_idle_timeout: 0s` consistently so quiet activated streams
  have no proxy idle deadline, update structural and data-plane regressions,
  correct the narrow ownership prose, rerun pinned validation/policy/lifecycle,
  commit, and immediately push.
- Runtime self-introspection remains unavailable; the immutable explicit
  configured profile is accepted with no visible mismatch.

## 2026-08-03 — Documentation reconciliation

- Reconciled the root README, end-user guide, server/auth/delivery-client
  REFERENCES, and MessageBoard human/agent/deployment guides against the
  accepted lifecycle, durability, auth, delivery, and topology boundaries.
- Corrected the end-user guide to identify listener-only TCP readiness, make
  `ServerEnvironment` closure ownership explicit, remove the stale public
  implementation-milestone reference, and return TLS/network ownership to the
  deployment. Tightened MessageBoard deployment guidance for combined versus
  standalone eligibility, one in-memory/non-HA delivery server, gateway versus
  application storage ownership, best-effort reconnect limits, and TCP-only
  readiness with no liveness probe.
- The work adds no runtime capability, no JVM build/modification, publication,
  or notification-completeness guarantee. Protected human-review files were
  not read or changed.
- Next: run deterministic Markdown link, command, prohibited-claim, and status
  checks; commit and immediately push the bounded documentation checkpoint.

## 2026-08-03 — Documentation checkpoint preflight

- Installed the locked workspace dependencies with `pnpm install --frozen-lockfile`;
  it completed without changing tracked files. It emitted
  expected pre-build `spine-proto` bin warnings because the clean worktree has
  no `packages/proto-tools/dist` output yet.
- Passed `pnpm format:check`, `pnpm docs:check:generated`, and the targeted
  public-prose scan rejecting `T-####`, Wave, reviewer, and remediation wording
  in root/user/package/MessageBoard human guides. `git diff --check` passed.
- The direct release-readiness scan enumerated 277 relative Markdown links
  before its package-export phase correctly stopped on absent clean-worktree
  `dist/` outputs; it is not evidence of a documentation-link defect. Full
  release verification remains the final task gate after the required build.
- Next: commit and push this bounded checkpoint, then hand the exact changes,
  checks, and clean-worktree limitations to the T-0097 orchestrator.

## 2026-08-03 — YAML dependency preflight correction complete

- Aligned the root development dependency and regenerated lockfile to the
  existing maintained `yaml@2.9.0` Vite line, eliminating the duplicate Vite
  type identity from root `yaml@2.8.2`. D-0107 now records that exact version
  and its single-toolchain rationale.
- `pnpm install --frozen-lockfile` passed after regeneration. The focused
  Compose/Kubernetes manifest-topology command passed 8/8:
  `node --test examples/message-board/deploy/compose/topology.test.mjs examples/message-board/deploy/kubernetes/manifests.test.mjs`.
- `pnpm --config.verify-deps-before-run=false verify:task -- --no-tests`
  passed: Node/version, Proto generation and 40 source checksums, 49 frozen
  descriptor digest checks, generated build, tooling typecheck, lint/TSDoc,
  formatting, API/audience documentation, Proto/generated-current, and
  package/asset/Markdown-link readiness. The former Vite type conflict did not
  recur.
- No runtime, public documentation, JVM, protected human-review, or publication
  scope changed. Next: commit and push this bounded dependency checkpoint.

## 2026-08-03 — Envoy typed-filter correction complete

- RED evidence: the pinned `envoy:v1.38.3` image rejected both Compose files
  with `envoy.filters.http.cors` type URL empty. The initial real-image
  Kubernetes validation then exposed its existing untyped router filters;
  direct validation cannot accept those embedded configurations without their
  matching v3 Router types.
- Added explicit v3 CORS typed configuration to the Compose and Kubernetes
  Envoy copies, plus the required v3 Router typed configuration to the two
  Kubernetes copies. Added one deterministic pinned-image test that extracts
  both Kubernetes ConfigMaps, supplies an ephemeral TLS fixture, and validates
  all four actual Envoy configurations with `envoy --mode validate`.
- GREEN evidence: direct Compose validation passed; the new real-image
  acceptance passed 1/1; the existing Compose/Kubernetes policy-topology suite
  passed 8/8; and `node --test
examples/message-board/deploy/compose/lifecycle.test.mjs` passed all 3 cases.
  Its cleanup assertions found no containers, networks, or volumes remaining.
- `pnpm format:check` and `git diff --check` passed. No JVM, protected
  human-review, publication, or unrelated runtime scope changed. Next: commit
  and push this bounded correction.

## 2026-08-03 — Aggregated specialist correction complete

- RED evidence: the strengthened structural policy suite failed on absent
  `:method` route constraints and absent HCM `stream_idle_timeout` policy.
- Added exact path plus `:method: POST` matching to all six browser RPC routes
  in Compose and Kubernetes Envoy configurations. Each HCM now declares
  `stream_idle_timeout: 0s`; the existing per-route 30-second unary timeouts
  and Activate `timeout: 0s` remain unchanged.
- Structural policy checks now parse each configuration and assert the method
  constraint, exact route set/timeouts, and unbounded quiet-stream policy.
  Added a Compose data-plane regression sending GET to the reserved Post RPC
  through Envoy and asserting its 404 no-route response before upstream work.
- Corrected deployment guidance: shared session signing and registry namespace
  apply only to browser-capable combined/gateway processes. Registry and
  revocation records are distinct logical resources with gateway/application
  ownership even if the application selects one shared storage provider.
- GREEN evidence: policy/topology passed 8/8; pinned real-image Envoy
  validation passed 1/1; and the three-case Compose lifecycle run completed
  with its existing leak-free container, network, and volume cleanup assertions.
  Next: formatting/diff checks, then immediate checkpoint push.

## 2026-08-03 — Specialist corrections verified

- Commit `17eece42` contains the complete accepted correction batch: exact
  path-plus-POST routing, unlimited HTTP connection-manager idle time for quiet
  subscription streams, a data-plane GET rejection regression, and corrected
  gateway/session storage guidance.
- The policy/topology suite passes 8/8, pinned Envoy validation passes 1/1 for
  all four configurations, and the Compose lifecycle suite passes 3/3 with
  leak-free cleanup. Formatting and `git diff --check` also pass.
- Style and documentation remain closed. Only TypeScript/API and
  performance/reliability are reopened for targeted confirmation before the
  final security review.

## 2026-08-03 — Affected re-review

- Performance/reliability re-review is clean. POST-only exact routing,
  unlimited quiet-stream idle policy, GET rejection, and cleanup evidence all
  match the accepted contract.
- TypeScript/API re-review found one P2 documentation remainder in the
  container and application READMEs. Both now describe session revocations as
  application-owned logical records distinct from the gateway-owned
  subscription registry and state which values browser-capable replicas share.
- `pnpm format:check`, `pnpm docs:check:generated`, and `git diff --check` pass.
  Next: push the correction and request final API confirmation, then start the
  final security review.
