# T-0095 Work Log

## 2026-08-02 — Task frame

- Started from pushed `main` baseline `1c53cbdf` in an isolated task worktree.
- Classified high-risk because package/install portability, production process
  ownership, and local image behavior are release-facing contracts.
- Scope is E1 only. Compose/Kubernetes and two-by-two topology remain T-0096;
  final Wave documentation/security closure remains T-0097.
- Recorded the existing requirements-splitter assignment with explicit expected
  `gpt-5.6-sol` / high reasoning before dispatch. Next: split the accepted E1
  behavior into one bounded implementation slice and deterministic RED gates.

## 2026-08-02 — Splitter timebox

- The requirements splitter completed repository analysis and was interrupted
  at the timebox boundary while finalizing its artifact. Its commit
  `ba58743a` completed at that same boundary and was pushed before the
  orchestrator's timebox record.
- Inspection confirms the artifact is complete, bounded, and accepted; no
  redispatch is required. It reuses installed `spine-proto`, existing
  server/browser lifecycle, MessageBoard assembly, and the delivery-server bin.
- Dispatch explicitly used the existing `requirements_splitter` role with
  `gpt-5.6-sol` / high reasoning. Runtime self-introspection was unavailable;
  no visible mismatch or inherited fallback occurred.

## 2026-08-02 — Requirements split accepted

- The existing `requirements_splitter` ran with explicitly dispatched
  `gpt-5.6-sol` / high. Runtime self-introspection was unavailable, so the
  immutable configured role/profile is the metadata evidence; no mismatch or
  inherited-profile fallback was visible.
- Inspected the governing Wave 5 E1 boundary, current packed Proto consumer and
  package metadata tests, MessageBoard model/application startup, the existing
  combined/standalone `Server` browser host, environment-owned delivery seam,
  and the current simple delivery-server bin and shutdown behavior.
- Docker client/server `20.10.24`, Docker Compose `2.17.2`, Node `24.18.0`, and
  pnpm `11.9.0` are available. Image acceptance is executable; Compose is
  precheck evidence only and remains unused until T-0096.
- Accepted one four-checkpoint split: a real fresh tarball-installed build that
  runs `generate`, `compose`, and `handlers` before one compile; one compiled
  MessageBoard artifact with combined and application-only Node entrypoints;
  app-specific standalone gateway and existing delivery-server local images;
  then one review wave, cheap preflight, image/package acceptance, and one
  `verify:release`.
- Public/runtime scope stays minimal: no framework runner or deployment CLI,
  no provider selector in infrastructure, no package/image publication, no
  topology files, no health API, and no Spine JVM build or launch.
- Exact RED tests, image inspection/signal/cleanup gates, file ownership,
  reviewer dispositions, risks, and T-0096 exclusions are recorded in
  `build-protocol/planning/T-0095_BUILD_ONCE_IMAGES_SPLIT.md`.
- Next: dispatch one existing implementer with explicit Terra/medium metadata
  to start the fresh packed-build RED test. Work continues.

## 2026-08-02 — Implementation owner dispatch

- Existing role: `implementer`; expected and explicitly dispatched profile:
  `gpt-5.6-terra` / medium reasoning.
- One owner holds the complete E1 package/application/image context to avoid
  drift between packed artifacts and container commands. The first assignment
  is checkpoint 1 only: prove the fresh packed-build RED before production
  changes, commit and push it, then implement GREEN within the accepted seam.

## 2026-08-02 — Checkpoint 1 RED

- Capability recheck passed: Docker client/server `20.10.24`, Docker daemon
  `20.10.24` on Linux, Docker Compose `v2.17.2` (evidence only; unused), Node
  `v24.18.0`, pnpm `11.9.0`, and a bounded `127.0.0.1` bind/release.
- Existing role/profile assignment: `implementer`, explicitly dispatched
  `gpt-5.6-terra` / medium. Runtime self-introspection is unavailable; the
  configured role/profile is the available metadata evidence, with no visible
  fallback or mismatch.
- Added the minimal focused packed-runtime contract test. The required
  checkpoint command is expected to fail until the MessageBoard package ships
  compiled application and combined entrypoints rather than a monorepo build
  command.
- RED command ran after the locked local dependency installation. The new test
  fails for the intended absence: `private: true` (and no compiled entrypoint
  scripts). The same command has three pre-existing packed-consumer failures
  caused by ungenerated `packages/proto/generated` imports after creating this
  fresh worktree; that baseline limitation does not mask the new direct
  contract failure.
- Next: commit and push this RED checkpoint, then add the smallest package
  metadata and compiled-entrypoint implementation.

## 2026-08-02 — Checkpoint 1 GREEN

- Corrected the RED assertion to the accepted contract: package tarballs may
  remain `private` because T-0095 never publishes them. The required behavior
  is a Node-only runtime command, not registry publication.
- MessageBoard now declares `start`, `start:combined`, and
  `start:application` as direct compiled-JavaScript Node commands. Its direct
  local runtime dependencies use the exact root snapshot version rather than
  `workspace:*`.
- After regenerating ignored Proto output required by the baseline, the exact
  focused checkpoint command passed: 89 tests in 3 files. This proves the
  package command contract and preserves existing packed-consumer behavior.
- Next: commit and push the GREEN package change, then start checkpoint 2
  entrypoint RED tests.

## 2026-08-02 — Checkpoint 2 RED

- Added a focused MessageBoard deployment-entrypoint test before entrypoint
  production code. It requires distinct explicit application-only and combined
  startup sources and runs with the unchanged local-entrypoint signal test.
- Next: run the required checkpoint command, record the absent-entrypoint RED,
  commit/push it, then introduce application-owned configuration and the
  smallest compiled entrypoint implementation.
- RED command failed for the intended reason: neither deployment source exists.
  The pre-existing local-entry test also reports no compiled `dist` artifact in
  the fresh worktree; it is a build prerequisite, not a new runtime regression.
- Next: commit/push the RED test, then add entrypoints and compile before the
  GREEN acceptance run.

## 2026-08-02 — Checkpoint 2 GREEN

- Added application-owned `MessageBoardDeployment` configuration. It requires
  `HOST`, `PORT`, and `DATASTORE_PROJECT_ID` for the native application and
  additionally `BROWSER_ORIGIN` for the combined listener. Errors name only
  the missing/invalid field and never include its value.
- The configuration constructs the existing Datastore storage factory in
  MessageBoard source; container/deployment code has no provider-selection
  branch. Local `start`/`run` behavior remains explicitly in-memory.
- Added explicit compiled `application-entry` and `combined-entry` sources.
  Both call `Server.run()` through the existing MessageBoard assembly; the
  application-only path has no browser listener.
- `pnpm typecheck:build` passed after the generated build, then the required
  focused entrypoint command passed: 3 tests in 2 files, including SIGINT and
  SIGTERM local listener release.
- Next: commit/push checkpoint 2 GREEN, then start checkpoint 3 local-image
  RED acceptance.

## 2026-08-02 — Checkpoint 3 RED

- Added the fixed local-image command and a narrow Node acceptance harness
  before container implementation. It requires a container-local Dockerfile,
  fixed builder, and the already compiled MessageBoard entrypoints.
- The harness intentionally contains no Compose/Kubernetes or registry/publish
  behavior. Next: run the required RED command, record its absent-container
  contract failure, commit/push, then add the fixed local builder/Dockerfile.
- RED command failed for the intended reason: no Dockerfile is present beneath
  the narrowly owned container directory. The already compiled entrypoint
  assertions are not reached.
- Next: commit/push this RED harness, then create the fixed three-target local
  image build contract without Compose, publication, or provider selection.
