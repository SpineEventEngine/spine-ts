# T-0183 Work Log

## 2026-08-14 - Task Start

- Created the high-risk T-0183 task, review, and work records before production
  edits on isolated branch `task/T-0183-interface-routing` at baseline
  `d02379f7`.
- Frozen scope: `.route(...)` accepts `MessageInterface` for Command/Event/state
  updates; exact > first registered matching interface > replacement/default;
  no routing callback on durable replay and no persisted-format change.
- Existing implementer assignment is explicit `gpt-5.6-terra` / medium; runtime
  telemetry availability will be recorded by the implementation owner.
- Next: RED public overload/snapshot tests, then one shared internal routing
  declaration/selection seam and repository integration.

## 2026-08-14 - Focused RED/GREEN

- Confirmed the linked worktree and explicit existing `implementer`
  `gpt-5.6-terra` / medium assignment. Runtime telemetry is not exposed by the
  desktop surface; the immutable configured role/profile is the available
  evidence.
- Ran RED shared-declaration tests after the required Proto generation/build
  setup. The initial direct test command could not resolve the built core
  package; root cause was the fresh worktree's absent generated/build outputs.
  After `pnpm proto:generate`, RED failed for the intended missing internal
  module.
- Added `routing-declarations.ts` and checkpointed it as `c74c9310`: nominal
  token validation, ordered immutable declaration snapshots, exact/token/default
  selection, and registered-member validation.
- Added the existing `.route(...)` token overloads to Command, Event, and
  state-update declarations. The first combined regression run revealed that a
  generic duplicate diagnostic had changed three established public messages;
  restored their signal-specific wording before proceeding.
- Checkpoint `27fa36df` integrates token snapshots into repository construction:
  exact routes win regardless of declaration order, tokens select in their
  declared order, defaults remain last, and incomplete token membership fails
  during construction.
- Focused test evidence so far: 20 declaration/selection/lifecycle tests pass
  (with 237 unrelated tests skipped by name filter); `pnpm typecheck:tooling`
  passes. Command, Event, and state-update interface routes each run once for
  admission and zero times for stored-row replay. No Inbox/provider/wire format
  was changed.
- Next: commit the lifecycle/type-contract checkpoint, run changed-source
  coverage and cheap preflight, then return the complete implementation bundle
  for specialist review.

## 2026-08-14 - Pre-Review Verification

- Pushed `11e5d867` with the public shared `InterfaceRouteMessage` type, the
  interface-token lifecycle tests, and the first durable records update.
- Full-project coverage passed: 95.25% lines (19,529 / 20,503), 90.44% branches
  (12,245 / 13,539), and 94.07% functions (5,110 / 5,432). The changed routing
  behavior is covered by the 257-test focused suite; full coverage supplies the
  global threshold evidence for shared `repository.ts`.
- `git diff --check origin/main...HEAD` and
  `pnpm verify:task -- --no-coverage` with the five focused routing test files
  passed. The latter executed the full non-Markdown task preflight and left the
  worktree clean.
- Implementation is ready for the planned TypeScript/API, style,
  reliability, and documentation specialist review lanes. Do not run
  `verify:release` until their findings converge.

## 2026-08-14 - Accepted Review Corrections

- Received one complete accepted batch. Explicit configured reviewer profiles:
  TypeScript/API `gpt-5.6-terra` / high; style/maintainability
  `gpt-5.6-terra` / high; reliability `gpt-5.6-terra` / high (CLEAN); and
  documentation/TSDoc `gpt-5.6-luna` / medium. Desktop telemetry does not
  independently expose runtime model metadata. Security remains N/A for this
  task and is deferred to T-0186.
- Wrote RED tests proving that the immutable map facade leaked its backing map
  through `forEach` and that common token classification was absent; both are
  GREEN after moving the classifier and tuple boundary to
  `RoutingDeclarations` and passing the facade itself to callbacks.
- Added the API inventory assertion, complete three-route TSDoc contract, and
  public compile-time two-member-token regression coverage. The type tests
  prove common-interface access, reject unconditional member-only access, and
  preserve exact-schema inference.
- Pushed correction checkpoint `788a22d0`.
- Post-correction evidence: 18/18 declaration/type tests, 262/262 routing
  tests, tooling typecheck, and API docs check pass. Full scripted coverage is
  95.25% lines (19,532 / 20,505), 90.44% branches (12,237 / 13,531), and
  94.11% functions (5,113 / 5,433). `git diff --check` and the complete
  `verify:task -- --no-coverage` preflight pass.
- Next: targeted reviewer confirmation only. Do not start reviewer work or
  `verify:release` in this implementation context.

## 2026-08-14 - Targeted API/TSDoc Residual

- Received one consolidated accepted residual P2 from the confirming
  TypeScript/API `gpt-5.6-terra` / high and documentation/TSDoc
  `gpt-5.6-luna` / medium lanes. Runtime telemetry is unavailable; immutable
  configured profiles are the acceptance evidence.
- Baseline TypeDoc generation exposed an unbound `schemaOrToken` parameter
  because each route overload was named `schema` or `token`. Renamed the first
  parameter of every Command, Event, and state-update overload to
  `schemaOrToken`, matching the existing shared TSDoc and implementation.
- Corrected TypeDoc generation, API docs check, tooling typecheck, 262 focused
  routing tests, format check, and diff check pass. The change is signature-name
  and documentation-only; no runtime or serialization behavior changed.
- Next: push this correction checkpoint and return to the orchestrator. Do not
  run `verify:release` or merge here.

## 2026-08-14 - Release Lint Correction

- The first post-review `verify:release` attempt failed at its initial full
  ESLint phase, before tests. The 17 findings were mechanical lint violations
  in the current T-0183 source/tests: redundant assertions, mutable/void
  callback syntax in the immutable facade, and test-style syntax.
- Applied only the corresponding mechanical correction. Exact/token/default
  selection, validation, admission-once and replay-zero routing, Inbox rows,
  and public runtime semantics remain unchanged.
- GREEN evidence: full `pnpm exec eslint .`; five focused routing suites
  (262/262); `pnpm typecheck:tooling`; `pnpm docs:api` and
  `pnpm docs:api:check`; `pnpm format:check`; and `git diff --check`.
- The runtime profile remains the explicit existing `implementer`
  `gpt-5.6-terra` / medium. Desktop telemetry does not expose independent
  runtime-model metadata; the immutable configured profile is the available
  evidence.
- Next: commit and push this correction checkpoint, then return the branch to
  the orchestrator for its final release-profile attempt. Correction checkpoint
  `b6ca9ab7` is pushed. Do not rerun `verify:release` or merge here.

## 2026-08-14 - Second Release Cleanup Correction

- The second `verify:release` attempt cleared full ESLint and stopped at the
  cleanup phase before tests. The only finding was the missing exact
  standalone-function necessity disposition for `readOnlyMap()#1`.
- Added the canonical T-0080F ledger row. Its reason is specific to the
  TypeScript immutable-collection boundary: the function creates the frozen
  facade needed to preserve `forEach` callback identity while withholding the
  mutable declaration map. No production behavior or API changed.
- GREEN evidence: `pnpm lint:cleanup`, full `pnpm exec eslint .`, five focused
  routing suites (262/262), format check, and diff check pass.
- Next: commit and push the ledger and records correction, then return the
  branch for the orchestrator's next release-profile attempt. Do not rerun
  `verify:release` or merge here.

## 2026-08-14 - Third Release TSDoc Correction

- The third `verify:release` attempt cleared ESLint and cleanup and stopped at
  TSDoc before tests. It required complete individual documentation for both
  `.route(...)` overload declarations in Command, Event, and state-update
  routing, plus summaries for the shared declaration type/state/snapshot
  properties.
- Added only concise declaration-level documentation. The implementation
  documentation remains the authoritative shared explanation of exact/token/
  default precedence, token validation, admission routing, and durable replay.
  No runtime behavior, public API shape, persistence, or replay semantics
  changed.
- GREEN evidence: TSDoc enforcement; API documentation generation/check;
  full ESLint; cleanup; five focused routing suites (262/262); format; and
  diff integrity checks pass.
- Next: commit and push this documentation correction, then return the branch
  for the orchestrator's next release-profile attempt. Do not rerun
  `verify:release` or merge here.
