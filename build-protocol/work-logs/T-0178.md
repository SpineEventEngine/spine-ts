# T-0178 Work Log

## 2026-08-13 - Planning Start

- Started from `origin/main@ab1a6deb` in the isolated
  `.worktrees/T-0178-wave11-plan` worktree on
  `task/T-0178-wave11-plan`.
- Preserved the dirty, stale primary checkout and all user-owned files.
- Classified the milestone as high-risk because it changes a frozen Proto
  intake, generated TypeScript contracts, build ordering, public routing,
  durable dispatch behavior, and the To-Do domain demonstration.
- Fully read `using-git-worktrees`, `codebase-design`, and
  `epic-breakdown-advisor` before planning actions.
- Recorded the complete human requirements ledger and the read-only
  requirements-splitter assignment with explicit `gpt-5.6-sol` / high
  dispatch requirements before dispatch.
- Committed and pushed the planning bootstrap as `2a2185a7` before dispatch.
- Dispatched the existing `requirements_splitter` read-only with explicit
  `gpt-5.6-sol` / high fields and prohibited subagent creation.
- Pinned `base-libraries` master at `51cb4287`, the canonical
  `options.proto` SHA-256 at `894468a9ee427d4805accae79ef83cbdf5aacb09e41193b4d5cc965b3ede0ad9`,
  and the introducing commit at `680092af`.
- Confirmed the upstream commit deliberately does not prescribe a TypeScript
  base shape for generated interfaces.
- Confirmed the current model generator already stages Buf output before one
  atomic publish, and the three routing declarations expose only exact
  `.route(MessageSchema, callback)` plus `replaceDefault()`.
- Confirmed current Buf output copies authored Proto copyright comments into
  generated TypeScript. Wave 11 must remove those copied headers, add generated
  provenance notices, and keep the copyright checker focused on authored
  source.
- Corrected the initial ledger before requirements splitting completed:
  interface routing follows explicit registration order after exact routes; it
  does not automatically sort by interface specificity.
- `pnpm install --frozen-lockfile` passed.
- The first clean-baseline `pnpm verify:task -- --no-tests` failed only because
  ignored generated Proto output was absent. `pnpm proto:generate` then passed
  47 source checksums and the 52-descriptor digest, and the rerun passed build,
  tooling, cleanup, TSDoc, copyright, log containment, formatting,
  documentation audience, 82 package imports, 51 package assets, and 360
  relative Markdown links.
- The requirements splitter completed read-only with the explicitly dispatched
  `gpt-5.6-sol` / high profile. Runtime self-introspection was unavailable; no
  visible mismatch or fallback occurred.
- Accepted its eight-task serial train, numbered T-0179 through T-0186.
- Resolved its sole naming question as public `MessageInterface` plus the
  generated-code factory `MessageInterfaces.define()`.
- Expanded the Wave 11 plan with the approved Proto/interface/token/routing
  examples, post-Buf pipeline, To-Do domain proof, exact file-family ownership,
  TDD criteria, reviewer lanes, verification profiles, and Wave 12 deferral.
- Fully read `requesting-code-review` and `verification-before-completion`
  before preparing the specialist review wave.
- Pre-review `docs:check:generated` passed API inventory, audience, and strict
  TypeScript snippet checks.
- Pre-review formatting, `git diff --check`, and release-readiness checks passed;
  release readiness verified 82 package imports, 51 package assets, and 361
  relative Markdown links.
- The first direct formatter invocation omitted the repository-required
  package-manager binary path and failed with `prettier ENOENT`; no file was
  changed. `pnpm exec prettier` applied the one plan-file correction, and the
  canonical format check then passed.
- Dispatched the complete specialist planning review wave using the durable
  concern assignments: documentation Luna/medium and TypeScript/API,
  style/maintainability, and performance/reliability Terra/high. Runtime
  telemetry was unavailable in every lane; explicit dispatch/configured role
  evidence showed no mismatch.
- Collected all reviewer findings before correction. The accepted batch covered
  authored-interface companion typing, generated-tree discovery exclusion,
  public token-factory honesty, staged/live compiler consistency, single
  generator ownership, shared routing selection, complete atomic failure
  tests, per-signal replay proofs, pre-Wave-11 Inbox compatibility, deterministic
  multi-source provenance, executable beginner flow, single-Gateway loopback
  scope, and canonical decision links.
- Applied one consolidated correction batch to the plan, task, decision,
  completion-plan mirror, and review record. Targeted re-review remains.
- Documentation and style/maintainability targeted re-reviews were CLEAN.
- TypeScript/API targeted re-review found one residual P1: token/factory/route
  generic relationships were stated only in prose. Added the exact
  schema-tuple-derived public signature, conditional member/interface
  constraint, exact callback intersection, runtime non-empty rule, and required
  compile-fail fixtures. A second API re-review remains.
- The second API re-review caught that an unconstrained readonly schema array
  still admitted `[]` because its member union was `never`. Corrected every
  public helper/signature to use the non-empty `InterfaceSchemas` tuple and
  retained runtime validation for dynamic JavaScript inputs. Final re-review
  remains.
- The non-empty-tuple correction committed locally as `2d1f83bf`, but its first
  push failed with GitHub SSH `Permission denied (publickey)` after earlier
  pushes succeeded. The commit remains intact and will be retried with this
  correction batch.
- Reliability targeted re-review accepted the per-signal replay and legacy
  Inbox corrections, then found three residuals. Added private WeakSet token
  identity and copy tests; a generation-ID/manifest commit-point protocol with
  fail-closed reader behavior and rollback-failure recovery evidence; and a
  complete eligible-path/content/extended-tsconfig inventory with add/remove/
  rename/config race fixtures.
- Reliability final re-review was CLEAN. TypeScript/API final re-review found
  only a task-ledger spelling mismatch (`TMessage` versus tuple `Schemas`),
  corrected before the final planning verifier.
- Final `pnpm verify:task -- --no-tests` passed the clean build, tooling,
  cleanup, TSDoc, copyright, logging containment, formatting, documentation
  audience, and release-readiness gates. Release readiness verified 82 package
  imports, 51 assets, and 364 links.
- Final record-only TypeScript/API confirmation was CLEAN. All relevant
  specialist lanes now converge.

## Current Status

T-0178 planning is reviewed and verification-complete. The plan awaits human
approval before Wave 11 implementation begins. No production source, generated
output, example domain, or reader-facing documentation has changed.
