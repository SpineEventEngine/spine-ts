# T-0165 Work Log

## 2026-08-11 - Startup

- Starts from `origin/main@81ca2834` after T-0164 integration.
- Classification is high-risk because the conformance path spans generator,
  runtime, handler metadata, Entity rollback, and Event/client outcomes.
- Initial inspection found an observed defect: generator, core runtime,
  build-time analyzer, and generated-registry ingestion all use broad
  `endsWith("rejections.proto")` checks. The invalid basename
  `notrejections.proto` therefore impersonates a rejection source.
- TDD starts with accepted `rejections.proto` / `*_rejections.proto` and
  misleading-suffix RED cases, followed by source-checker fixtures and existing
  end-to-end rollback/outcome proof.

Status: establishing RED naming/conformance evidence before production edits.

## 2026-08-11 - Naming and Runtime Conformance

- RED proved `notrejections.proto` was accepted by companion generation,
  `RejectionThrowable`, build-time handler analysis, and generated-registry
  Event-filter provenance. The source-checker RED was a missing module.
- GREEN restricts all four seams to the exact `rejections.proto` and
  `*_rejections.proto` basenames. Server analyzer and ingestion share one
  package-private matcher; generator/core keep their package-local checks.
- A tracked-source checker rejects misleading rejection-like Proto filenames
  and is wired into both Proto lint and generation through `proto-workflow`.
- Existing Aggregate and Process Manager rejection tests prove one typed Event,
  original rejected-Command context, unchanged prior state/history, no normal
  Event commit, and no duplicate rejection dispatch.
- Todo client conformance remains green. Its direct readiness Event probe now
  carries a real typed Task producer ID, matching T-0159 Event routing.
- The focused conformance selection passes 7 files / 32 tests; the complete
  naming selection passes 5 files / 118 tests. Live source and Buf checks pass.

Status: behavior converged; checkpoint, broad verification, concern review,
final verification, and integration remain.
