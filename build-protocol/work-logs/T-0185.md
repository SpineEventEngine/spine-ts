# T-0185 Work Log

## 2026-08-14 - Start

- Created `task/T-0185-beginner-docs` in isolated worktree
  `.worktrees/T-0185-beginner-docs` from integrated `origin/main` at
  `696bbac3`.
- The user's dirty primary checkout was not modified.
- Existing implementer assignment is explicit `gpt-5.6-terra` / medium.
  Desktop runtime telemetry is unavailable; the immutable configured profile is
  the available evidence.
- Frozen dependency installation passed with the lockfile unchanged.
- Baseline canonical Proto generation, generated build, documentation API
  inventory, audience check, and strict TypeScript snippet check passed.
- Current reader docs remain behaviorally stale by T-0185 design even though
  the existing mechanical gates are green. The first RED must therefore assert
  the newly required interface-routing/lifecycle journey rather than weaken a
  gate.

## Document Disposition Ledger

| Document                            | Disposition | Notes                                    |
| ----------------------------------- | ----------- | ---------------------------------------- |
| `README.md`                         | changed     | Root handoff links the runnable journey. |
| `docs/USER_GUIDE.md`                | changed     | Corrected token precedence/replay claims. |
| `docs/api/README.md`                | changed     | API map records both route overloads.    |
| `docs/architecture/README.md`       | changed     | Generation/routing/replay boundaries.    |
| `packages/core/README.md`           | changed     | MessageInterface beginner handoff.       |
| `packages/core/REFERENCE.md`        | changed     | Dense token semantics.                   |
| `packages/proto/README.md`          | changed     | Fresh option contract handoff.           |
| `packages/proto/REFERENCE.md`       | changed     | `ts_type` and Java-only field boundary.  |
| `packages/proto-tools/README.md`    | changed     | Generation workflow.                     |
| `packages/proto-tools/REFERENCE.md` | changed     | Discovery/compiler/provenance details.   |
| `packages/server/README.md`         | changed     | Beginner routing declaration.            |
| `packages/server/REFERENCE.md`      | changed     | Precedence/admission/replay details.     |
| `examples/todo/README.md`           | changed     | Runnable quick start and current model.  |
| `examples/todo/USER_GUIDE.md`       | changed     | Complete lifecycle tutorial.             |
| `examples/todo/REFERENCE.md`        | changed     | Dense To-Do model/route/rejection table. |

## 2026-08-14 - Reader-contract RED/GREEN

- Added one narrow reader-contract assertion to the existing strict TypeScript
  documentation-snippet test, rather than a new repository-wide semantic scan.
  Its first run failed because `examples/todo/USER_GUIDE.md` had no approved
  `every_is`/`is` declaration and source-linked interface-routing journey.
- Rewrote the owned reader docs against the integrated To-Do source and
  generated interfaces. The guide now uses an executable strict-gate TypeScript
  fence with the real generated imports and an authored same-module interface.
- GREEN evidence: `pnpm vitest run scripts/check-typescript-snippets.test.mjs`
  passed 12 tests; `pnpm docs:snippets:check:generated` passed after the fence
  was made self-contained. The first compiler attempt correctly rejected an
  incomplete excerpt; no permissive stub was introduced.
- Pending: deterministic API/audience/copyright/format and bounded task
  verification, then specialist-review handoff. No runtime, generated-output,
  or historical build-protocol narrative files were edited.

## 2026-08-14 - Implementation preflight

- Passed local-link validation across all 15 owned reader documents; API
  inventory, reader-audience, copyright, formatting, strict snippets, and the
  absent-name scan (`TaskReassignmentEvent`, `routeSemantic`, `@Route`,
  multiple-Gateway, and Cloud Run) also passed. `git diff --check` was clean.
- Selected expensive profile passed once after cheap convergence:
  `pnpm verify:task -- --no-tests` (exit 0). It ran canonical Proto-source,
  style, rejection, descriptor, generated TypeScript build/declaration
  normalization and the task-selected deterministic gates. No generated output
  remained changed.
- Checkpoint `f618145c` is pushed to `origin/task/T-0185-beginner-docs`.
- Ready for the required documentation and TypeScript/API documentation review.
  Style/maintainability, performance/reliability, and security remain N/A for
  this prose/snippet-only task, for the reasons stated in the task record.

## Next

- Add a deterministic reader-contract RED for the approved journey.
- Rewrite only current reader-facing owned documents.
- Run the complete deterministic documentation profile before specialist review.

## 2026-08-14 - Accepted review corrections

- Received and verified the complete accepted documentation/TypeScript API
  review batch. Recorded profiles: existing `documentation_reviewer`, immutable
  `gpt-5.6-luna` / medium, and existing `typescript_api_docs_reviewer`,
  immutable `gpt-5.6-terra` / high. Desktop telemetry does not expose runtime
  child metadata; configured profiles are the available evidence.
- RED: expanded the existing narrow strict-snippet reader contract. It failed
  because the To-Do guide lacked an observable zero/one/two assignment journey,
  the persisted Inbox no-reroute proof, and the dense same-model export rule.
- GREEN: the guide now runs the real focused black-box tests by exact name and
  explains their observations: create has zero assignee targets, assign one,
  exact reassignment two, and unassign zero. The durable proof observes a stored
  typed Inbox target after restart while the replacement route is not called;
  `catchUpReadSide()` is separately documented as clear-and-rebuild from stored
  events. No new runtime API or test fixture was invented.
- Restored the explicit Cloud Run/multiple-Gateway unsupported boundary in the
  root README and architecture notes. Added realpath/top-level named export/
  recursive parent discovery wording to the To-Do guide and Proto/proto-tools
  references; external property types remain allowed.
- Focused evidence passed: strict reader contract (12 tests), strict snippet
  compiler, and each named public black-box test (assignment lifecycle,
  persisted stored-target no-reroute replay, and read-side catch-up rebuild).
- Pending: full cheap deterministic correction preflight, correction commit and
  push, then targeted documentation and TypeScript/API re-review. No merge or
  release work is authorized.

## 2026-08-14 - Correction preflight

- Full cheap correction preflight passed: local links, prohibited-name scan,
  `git diff --check`, API inventory, reader audience, copyright, formatting,
  strict snippets, and the focused 12-test documentation contract suite.
- The correction remains documentation, source-linked snippets/tests, and T-0185
  records only. Generated outputs remained clean. Ready for targeted
  documentation and TypeScript/API re-review after this checkpoint is pushed.

## 2026-08-14 - Final targeted API residual

- Corrected the accepted review wording after API re-check: only the requested
  authored interface must be a top-level named module export. Recursive
  `extends` parents must resolve to interfaces in the same model module, but
  source does not require those parents to be named exports or top-level.
  External property types remain allowed.
- RED: the tightened existing reader contract failed on the previous broader
  parent-export claim. GREEN and focused deterministic checks are pending this
  correction's documentation-only commit.

## 2026-08-14 - Final targeted API residual verification

- GREEN: `pnpm vitest run scripts/check-typescript-snippets.test.mjs` passed
  12/12 after the narrower reader contract replaced the broader parent-export
  claim.
- Passed exactly the targeted deterministic profile: strict snippets,
  `docs:api:check`, `format:check`, and `git diff --check`. This residual does
  not alter the verified runnable lifecycle/replay content, runtime, generated
  output, or release state.
- Ready for targeted re-review after the correction commit is pushed.
