# T-0173 Work Log

## 2026-08-12 - Startup

- Started from post-main-verified `origin/main@a8f210d4` in an isolated worktree.
- Recorded exact 13-document ownership and the binding human ledger.
- Implementation assignment: existing `implementer`, explicitly
  `gpt-5.6-terra` / medium; runtime metadata limitation will be recorded.

Status: assignment checkpoint pending; implementation not dispatched.

## 2026-08-12 - Reader journey rewrite

- Assignment acceptance: existing `implementer`, explicitly dispatched as
  `gpt-5.6-terra` / medium. This surface does not expose runtime model
  self-introspection; the immutable configured assignment is the available
  metadata, with no visible mismatch or fallback.
- Classification remains high-risk documentation only. No runtime, public API,
  generated output, package, or shared-checker change is in scope.
- Rewrote all 13 assigned reader documents (`changed`): delivery client and
  server READMEs/references; deployment, GCE, and GKE READMEs/references;
  distributed Message Board README/reference; and Envoy README.
- The journey now starts locally, distinguishes remote authoritative Inbox rows
  from the in-memory local server, explains lease fencing, lost acknowledgements
  and idempotent effects, and links finite drains and limits to references. It
  preserves the existing single-Gateway/fixed-topology boundary; Multiple-
  Gateway and Cloud Run remain outside the offering.
- Review dispositions prepared: documentation, TypeScript/API, and
  performance/reliability require the orchestrator review wave; style is N/A
  because no shared tooling changed; security is N/A because no security
  boundary changed. No attempt-history or quarantine capability was introduced.

Status: focused deterministic validation pending.

## 2026-08-12 - Deterministic validation

- Passed generated build: `pnpm typecheck:build`; Proto workflow verified 47
  source checksums, authored Proto style/example quality, and 52 frozen
  descriptor files against digest
  `2a0db288787ac436c190ed86d72d78922deeb0fba4a64b2094b36b2525859db0`.
- Passed strict TypeScript snippets with all 13 owned document paths supplied
  explicitly. The Envoy JavaScript example is deliberately a `js` fence; its
  renderer is an `.mjs` module and is not represented as TypeScript.
- Passed `docs:audience:check`, `lint:copyright`, changed-file Prettier,
  `git diff --check`, and `check:release-readiness` (82 package imports, 51
  package assets, 325 resolving relative Markdown links).
- Passed topology/limit/deferment scan: reader docs retain local in-memory
  limits, bounded remote drains/snapshots, one fixed Gateway, and explicit
  Multiple-Gateway/Cloud Run exclusions; there is no positive claim of
  attempt-history or quarantine persistence.
- Passed selected profile once: `pnpm verify:task -- --no-tests` (Node gate,
  generated build, and tooling typecheck included). No runtime tests apply to
  this documentation-only change.

Status: implementation checkpoint ready for orchestrator review wave and push.

## 2026-08-12 - Accepted review correction

- Accepted one complete review batch without a new dispatch. The existing
  assignment remains `implementer`, explicitly `gpt-5.6-terra` / medium; this
  surface still exposes no runtime self-introspection, so configured profile is
  the recorded metadata limitation.
- Documentation (low priority): changed both delivery README links from an
  agent-only label to the reader-facing **Detailed delivery reference**.
- TypeScript/API P2 (two): corrected delivery-client and delivery-server
  reference claims. Release requires the supplied worker to match the current
  owner; it has no separate per-pickup-time fence, so a stale worker cannot
  release a newer worker's session.
- Performance/reliability P1: corrected Distributed Message Board recovery:
  valid complete subscription payloads update the UI locally, while gaps,
  unusable payloads, and disconnected posts require authoritative recovery;
  reconnect resynchronization may carry that state directly.
- Performance/reliability P2: corrected Envoy documentation. `renderEnvoy()`
  accepts uppercase auth-route methods, while its default CORS allow-list is
  `GET`, `POST`, and `OPTIONS`; another method needs matching CORS
  customization.
- Source verification preceded edits: `ShardService.releaseSession()` compares
  the supplied worker to the current owner; the client sends worker but no
  pickup timestamp; Envoy validates `/^[A-Z]+$/` and renders the stated CORS
  list. No runtime discrepancy or test-classification correction was found.

Status: correction validation pending.

## 2026-08-12 - Correction validation

- Passed explicit strict TypeScript snippets for all 13 owned reader documents,
  `check-api-docs`, and `docs:audience:check`. The reader-facing reference
  labels remain intact; the introductory sentences retain the required concise
  coding-agent API-audience handoff.
- Passed `lint:copyright`, changed-file Prettier, `git diff --check`, and
  `check:release-readiness` (82 package imports, 51 package assets, and 325
  relative Markdown links).
- Passed wording/topology scans: no stale non-worker-conditional release,
  query-after-any-notice/reconnect, or GET/POST-only auth-route claim remains;
  supported one-Gateway and Cloud Run deferral wording remains present.
- Passed `pnpm verify:task -- --no-tests` once after convergence. It rebuilt
  generated TypeScript and tooling successfully; runtime tests remain N/A for
  the documentation-only correction.

Status: accepted review batch corrected, verified, and ready to push.

## 2026-08-12 - Final Envoy boundary correction

- Verified the residual finding against `BrowserAuthRoute.method` in
  `packages/server/src/server/server.ts` (`"GET" | "POST"`) and
  `BrowserServer.authRoutes()` plus its rejection tests. The authenticated
  Spine Gateway supports only those methods.
- `interop/envoy/render.mjs` still mechanically accepts `/^[A-Z]+$/` for an
  externally customized route and renders a GET/POST/OPTIONS CORS list. The
  README now distinguishes that renderer input from the Spine Gateway contract:
  another method is only for a separately customized non-Spine upstream and
  needs matching CORS customization.
- The earlier API renderer-only finding is superseded by this reliability
  boundary finding. Worker-matched release and Distributed Message Board
  recovery corrections remain clean. No runtime behavior or test
  classification changed; existing server and Envoy tests provide the relevant
  behavioral evidence.

Status: final residual validation pending.

## 2026-08-12 - Final residual validation

- Passed `node --test interop/envoy/render.test.mjs` (5/5) and the focused
  server auth-route registration test (1 passed; the remaining file tests were
  skipped by the explicit name filter). These prove the renderer still accepts
  its documented mechanical input while the Gateway rejects methods beyond GET
  and POST.
- Passed strict TypeScript snippets for all 13 reader documents, API and
  audience checks, copyright, release-readiness (82 package imports, 51 package
  assets, 325 relative Markdown links), Prettier, diff hygiene, and the focused
  Envoy/worker/recovery wording scans.
- Passed `pnpm verify:task -- --no-tests` once after convergence. The profile
  rebuilt generated TypeScript and tooling successfully. No runtime behavior
  changed and no test classification correction was warranted.

Status: final residual corrected, verified, and ready to push.
