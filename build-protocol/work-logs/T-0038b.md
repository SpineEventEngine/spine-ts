# T-0038b Work Log

Status: Slice 3 late-ack regression green; reliability rereview ready

## Start

- `2026-07-14T02:27:17Z`: branch/worktree created from updated verified `main`
  at `1a682b0c` after T-0038a integration.
- Parent audit plan `9addd3b0` is accepted and defines three small slices.
- Existing implementer expected profile is explicit `gpt-5.6-terra` / medium,
  no subagents. Slice 1 owns internal context transport composition/tests only.

## Slice Order

- Slice 1: context route/bus composition and intake handle.
- Slice 2: server startup/close/failure ownership.
- Slice 3: native child-process ZeroMQ proof and observable docs.

## Current Gates

- Strict TDD and focused runtime/routing/bus tests.
- Generated typecheck, scoped lint, cleanup/public-leak, format/generated/diff.
- Relevant style/API/reliability review; docs N/A absent public doc changes.
- No full verify in Slice 1.

## Slice 1 Implementation Evidence

- Implementer: existing implementer role, actual `gpt-5.6-terra` / `medium`;
  no subagents dispatched or used.
- Skill applicability/read evidence completed before production work:
  `tdd`, `test-driven-development`, `implement`, `error-handling-patterns`,
  `javascript-testing-patterns`, `nodejs-backend-patterns`, and
  `verification-before-completion`, including applicable TDD references.
- Rechecked the parent plan's accepted JVM source references
  (`IntegrationBroker`, `BoundedContext`, `ServerEnvironment`, `Server`) and
  local JVM runtime/routing notes. Their accepted ownership impact is retained:
  the internal context adapter feeds existing buses; context transport neither
  owns delivery nor closes transport/context/storage. The historical raw JVM
  checkout was unavailable at `/private/tmp/spine-research/core-jvm`; this is
  recorded uncertainty, not a reason to redesign the accepted architecture.
- RED: `context-transport.test.ts` initially failed because the internal module
  did not exist. GREEN: `ContextTransport.open()` uses `RuntimeTransportBinding`
  with a fresh runtime and framework bus callbacks. A second RED lacked event
  registration; GREEN registers exactly one event intake route per accepted
  event type, leaving dispatcher fan-out to `EventBus`.
- Changed paths: `packages/server/src/runtime/context-transport.ts`,
  `packages/server/src/runtime/runtime-routing.ts`, and
  `packages/server/test/runtime/context-transport.test.ts` only, besides these
  three durable T-0038b records.
- Behavior proved: accepted commands post once to the owning context command
  bus; accepted events post once to its event bus despite two matching
  dispatchers; malformed/type-URL-invalid commands reach neither bus; empty
  contexts register no routes; binding close drains already accepted work and
  does not close the supplied transport. No root export or application callback
  registry was added.
- Checks: focused context/routing/bus Vitest `63/63`; native ZeroMQ
  runtime-transport regression `13/13`; generated build typecheck; scoped
  ESLint; cleanup enforcement; exact Prettier; generated-clean; public-root
  leak scan; and `git diff --check` all passed. The first sandboxed ZeroMQ run
  was denied `Operation not permitted`; an approved native rerun passed.
- Slice 1 hands to Slice 2 with lifecycle untouched: no `Server.start()` or
  `RunningServer.close()` ownership/order/retry behavior has been implemented.

## Slice 1 Pre-Review Correction Batch

- Coordinator inspection found that success tests used direct bus posts as an
  indirect drain instead of closing the runtime binding, malformed-event
  refusal was not covered, and context-independent event subscriber IDs could
  collapse two contexts' fan-out descriptor identities for one event type.
- Accepted correction: drain via handle close; add malformed-event refusal;
  derive collision-free logical event subscriber IDs from context identity;
  retain shared command responder identity; and prove two same-event contexts
  receive exactly once with distinct subscriptions.
- The original implementer was closed after the first handback and is resumed
  for this complete batch with explicit expected and actual immutable role
  metadata `gpt-5.6-terra` / `medium`; no subagents. Slice 2 remains out of
  scope until corrected focused checks and Slice 1 review are clean.

## Slice 1 Correction Result

- Actual immutable role metadata matched the explicit resumed dispatch:
  existing implementer, `gpt-5.6-terra` / `medium`; no subagents dispatched or
  used. Re-read and applied receiving-review, strict TDD, TDD references, and
  verification-before-completion before correction actions.
- Replaced both direct bus sentinel drains with the returned context transport
  handle close barrier. Assertions now contain only the one transported command
  and the two expected dispatcher observations from one transported event.
- Added missing-message and mismatched-type-URL event refusal checks; neither
  reaches the context event dispatcher.
- TDD sequence: the same-event two-context test failed `expected 2, received 1`
  for distinct descriptor keys. Updating the recording transport to retain and
  publish all same-key registrations left that RED unchanged, isolating the
  production collision. Context-scoped event worker IDs using
  `event-context-<base64url UTF-8 name>-worker-<ordinal>` then made the test
  green, with each context observing the event once. The shared
  `command-worker-1` identity was not changed.
- Recording handles remove exactly their own registration, are idempotent, and
  leave sibling same-key registrations active until those handles close.
- Changed production/test paths remain
  `packages/server/src/runtime/context-transport.ts`,
  `packages/server/src/runtime/runtime-routing.ts`, and
  `packages/server/test/runtime/context-transport.test.ts`, plus the three
  durable T-0038b records. No unrelated current change was reverted.
- Corrected gates passed: focused Slice 1/routing/bus `65/65`, native
  runtime-transport `13/13`, generated build typecheck, scoped ESLint, cleanup,
  exact Prettier, generated-clean, public-root leak, and diff/status checks.
- Remaining uncertainty: server lifecycle ordering, partial registration
  failure cleanup/retry, caller-owned sibling transport behavior, and real
  cross-process proof remain unimplemented Slice 2/3 work.

## Slice 1 Coordinator Verification

- Fresh coordinator evidence: focused context/routing/command-event bus tests
  `65/65`; native ZeroMQ runtime-transport tests `13/13`; generated build
  typecheck; scoped ESLint; exact Prettier; cleanup enforcement; canonical
  package-script generated-clean; public-root leak scan; status inspection; and
  `git diff --check` passed.
- A non-canonical direct `node scripts/check-generated-clean.mjs` invocation
  could not find `protoc-gen-es` outside pnpm's executable `PATH`; rerunning the
  canonical `pnpm proto:check-generated` command passed and is the accepted
  evidence.
- Lightweight docs/status lint passed: status mirrors agree, historical text is
  not claimed as active, existing routing/binding owners are reused, no root API
  or public-doc claim changed, and only the six expected Slice 1 paths are
  modified/untracked before the immutable commit.

## Slice 1 Reviewer Wave

- Implementation endpoint `b2a6e7e1`; review package
  `.superpowers/sdd/review-ae8f0f09..b2a6e7e1.diff` from `ae8f0f09`, one commit,
  46,562 bytes.
- Before dispatch, recorded expected explicit profiles: existing style,
  TypeScript/API docs, and performance/reliability reviewers each use
  `gpt-5.6-terra` / `high`, with no subagents. Actual runtime metadata must
  match before any result is accepted.
- Documentation is a concrete N/A for unchanged internal-only behavior and no
  public/observable doc edit. Security remains deferred to T-0041.

## Slice 1 Review Findings

- Style/maintainability reviewer `019f5e8a-a99c-7401-971c-bca6b26c4c38`
  returned clean; TypeScript/API docs reviewer
  `019f5e8a-ad5f-70c1-80fc-74e45251dd98` returned clean. Both actual immutable
  profiles matched explicit `gpt-5.6-terra` / high dispatch, used no subagents,
  and were closed.
- Performance/reliability reviewer
  `019f5e8a-affc-7c93-a658-e961038fed1a` returned P1/P2 at matching actual
  `gpt-5.6-terra` / high, no subagents, and was closed.
- Accepted P1: a command registration can survive an event-registration open
  failure when cleanup also rejects; the binding masks the primary error and
  discards its retry handle. Fix with primary-first combined diagnostics and a
  package-internal retained cleanup capability, not a public root lifecycle API.
- Accepted P2: the recording responder's no-op close does not prove command
  deregistration. Remove its exact handler idempotently and assert post-close
  command/event intake cannot invoke retired routes.
- Existing implementer is resumed for both findings at explicit
  `gpt-5.6-terra` / medium, no subagents. The fix may touch internal runtime
  binding/context composition and focused tests plus these records only.

## Slice 1 Reliability Fix Result

- Existing implementer actual immutable metadata matches the explicit
  `gpt-5.6-terra` / `medium` assignment; no subagents were dispatched or used.
  Receiving-review rigor, strict TDD and required references, and
  verification-before-completion governed this bounded correction.
- P1 RED observed the cleanup-close error alone after command registration
  succeeded and event registration failed. GREEN preserves the registration
  error first in an `AggregateError`, retains the same retryable binding handle
  in weak package-internal runtime/context access, and leaves the public binding
  API and its caller-owned retry policy unchanged.
- Coordinator-strengthened RED proved the initial fake did not model surviving
  ownership because a second open resolved. The corrected fake retains the
  command owner after its first close failure: pre-retry open fails with the
  exact duplicate error, cleanup retry releases the owner, and only the later
  open succeeds and closes. Successful-call counts are supplementary, not the
  ownership proof.
- P2 RED returned a closed-runtime intake failure through the retired command
  handler. The recording responder handle now removes only its exact handler,
  once, and repeated close is harmless. Focused post-close assertions prove
  command and event intake cannot reach retired registrations; the two-context
  event test proves closing one same-key registration preserves its sibling
  until sibling close.
- Current correction paths: internal `runtime-transport.ts` and
  `context-transport.ts`, their two focused tests, and these three records.
  No server lifecycle, public export/doc, example, Protobuf, manifest, or
  generated tracked file changed; Slice 2/3 were not started.
- Fresh checks passed: focused context/routing/bus `65/65`; native ZeroMQ
  runtime transport `14/14`; generated build typecheck; scoped ESLint; cleanup;
  exact Prettier; canonical `pnpm proto:check-generated`; root leak; status; and
  `git diff --check`.
- Remaining uncertainty: the internal retained cleanup seam is proven at the
  binding/context boundary but is not yet integrated into real server lifecycle
  checkpoint/retry ordering. Shared transport lifecycle and cross-process proof
  remain Slice 2/3.

## Slice 1 Fix Coordinator Verification

- Fresh coordinator evidence passed: focused context/routing/bus `65/65`;
  native ZeroMQ runtime transport `14/14`; build typecheck; scoped ESLint; exact
  Prettier; cleanup; canonical generated-clean; root leak; expected status; and
  diff checks.
- Inspection accepted the strengthened ownership test and internal weak cleanup
  capability. No public export, docs, server lifecycle, example, Protobuf, or
  generated tracked output changed.

## Slice 1 Reliability Rereview

- Corrected endpoint `a69ae867`; package
  `.superpowers/sdd/review-b2a6e7e1..a69ae867.diff` from `b2a6e7e1`, three
  commits, 40,183 bytes.
- Assigned existing performance/reliability concern only, with explicit
  expected `gpt-5.6-terra` / high and no subagents. Actual immutable metadata
  must match before acceptance.

## Slice 1 Clean Closure

- Reliability rereviewer `019f5e9a-7dca-7e80-ad6c-4f83479d43d2` returned
  CLEAN at matching actual immutable `gpt-5.6-terra` / high, no subagents, and
  was closed. Its focused runtime/context transport run passed `21/21`,
  including native ZeroMQ IPC.
- Corrected endpoint `a69ae867` is accepted for Slice 1. Prior clean style/API
  results remain valid; docs N/A and final security deferral remain recorded.

## Slice 2 Assignment

- Existing implementer is resumed at explicit expected and actual immutable
  `gpt-5.6-terra` / medium, no subagents.
- Scope: deterministic post-recovery/pre-listener registration open; retained
  partial-open cleanup; network-before-intake and intake-before-detach close;
  retry checkpoints; shared transport/sibling preservation; duplicate command
  responder startup failure; focused server lifecycle tests and these records.
- Excluded: public exports/options, new lifecycle abstractions outside the
  smallest internal grouping needed, cross-process fixture/docs, examples,
  Protobuf, topology, supervision, and retry timing policy.

## Slice 2 Implementation Result

- Existing implementer actual immutable metadata matches the explicit
  `gpt-5.6-terra` / `medium` assignment; no subagents. Read and applied the
  recorded implementation, worktree, strict TDD plus required references,
  JavaScript testing, error handling, Node lifecycle, and verification skills.
  User no-commit/no-subagent constraints and orchestrator-owned review take
  precedence over generic skill completion defaults.
- Baseline focused server/context/runtime lifecycle evidence passed `83/83`
  before edits. Vertical RED/GREEN cycles then established: missing
  post-recovery/pre-listener registration; missing running intake-close gate;
  missing retained server retry after partial-open cleanup failure; missing
  failed-listener registration close; and missing duplicate command ownership
  conflict. Exact RED observations were no subscription events before listener,
  resolved running close with zero registration attempts, two diagnostics
  instead of the required three, zero listener-failure registration closes, and
  a second `RunningServer` instead of duplicate rejection.
- New internal `ContextTransportGroup` opens built contexts in deterministic
  order, tracks successful handles and Slice 1 retained failed-open cleanup,
  and reuses `RetryableCloseGroup` for idempotent per-index retry. `Server`
  remains the single lifecycle owner and uses the group between attachment and
  listener creation and between network close and detach.
- Partial-open proof: context one event registration closes once; context two's
  surviving command registration fails its binding cleanup and first server
  cleanup, remains a hard gate with contexts/resources/attachment intact, then
  succeeds on cleanup-only `start()` retry. The retry neither rebuilds nor
  listens, advances detach/dependencies once, returns the established deferred
  cleanup completion, and leaves the server terminal. Diagnostics remain
  `[registration, binding-close, server-close]`.
- Running/listener proof: network close is not repeated after an intake close
  failure; successful registration indexes are not retried; detach and owned
  dependencies wait behind intake success; accepted callback work drains before
  delivery stop; listener bind failure closes intake before detach; caller-owned
  transport remains open; and owned transport closes only in the existing
  environment facility phase after resources.
- Shared ownership proof uses two real servers on one environment. Same-event
  routes each receive once, closing one preserves the sibling route/listener,
  and the supplied transport remains open. The real local environment transport
  rejects a second same-command responder before listener creation, failed
  startup cleans only the duplicate server, the existing server stays
  connectable, and a fresh server acquires ownership after the original closes.
- Current paths: modified `server.ts` and `server-lifecycle-fixture.ts`; new
  internal `context-transport-group.ts` and focused
  `server-context-transport-lifecycle.test.ts`; plus the three T-0038b records.
  No unrelated current work was reverted and `human-review-1-jul.md` was not
  read or touched.
- Fresh gates passed: server lifecycle `70/70`; context/routing/bus `65/65`;
  native runtime transport `14/14`; generated build typecheck; scoped ESLint;
  cleanup; exact Prettier; TypeDoc; canonical generated-clean; root leak;
  `git diff --check`; and expected-path status. No full verify or commit.
- Remaining uncertainty: Slice 3 still owns real cross-process ZeroMQ traffic,
  bounded child-process teardown/timeouts, observable external docs, and final
  all-slice verification. Security remains deferred to T-0041.

## Slice 2 Coordinator Verification

- Sandboxed lifecycle verification failed only because loopback listen was
  denied; the approved native rerun passed three files and `70/70` tests.
- Fresh accepted evidence: context/routing/bus `65/65`; native runtime transport
  `14/14`; build typecheck; scoped ESLint; exact Prettier; cleanup;
  `docs:check` with 205 expected server exports; canonical generated-clean;
  root leak; status; and diff checks.
- Lightweight docs/status lint passed. Existing lifecycle/close helpers are
  reused, the internal group is not root-exported, public TSDoc states only
  current observable ordering, and no generated or unrelated path is present.

## Slice 2 Reviewer Wave

- Endpoint `d4debde8`; package
  `.superpowers/sdd/review-ebdf959f..d4debde8.diff` from `ebdf959f`, one commit,
  67,201 bytes.
- Before dispatch, expected explicit profiles are recorded: style, API, and
  reliability existing roles use `gpt-5.6-terra` / high; documentation uses
  `gpt-5.6-luna` / medium; all no subagents. Actual immutable metadata must
  match before acceptance.

## Slice 2 Review Findings

- API reviewer `019f5eb2-b7fc-7481-abf4-32505c10ea17` and reliability
  reviewer `019f5eb2-bb5d-7473-9eae-4894164c5585` returned CLEAN at matching
  actual immutable `gpt-5.6-terra` / high, no subagents, and were closed.
- Style reviewer `019f5eb2-b450-7053-976f-780c76870110` returned accepted P2
  at actual `gpt-5.6-terra` / high: three failed-start paths repeat the same
  post-network intake/detach/owned-close state transition. Centralize it while
  preserving path-specific network handling and failure wording.
- Documentation reviewer `019f5eb2-b187-7cc1-8ce6-cf37e3582878` returned
  accepted P2 at actual `gpt-5.6-luna` / medium: public `Server.start()` TSDoc
  omits deterministic built-context registration order and the pre-listener
  registration-failure boundary.
- Both used no subagents and were closed. Existing implementer is resumed at
  explicit `gpt-5.6-terra` / medium for both fixes, no subagents.

## Slice 2 P2 Correction Result

- Existing implementer ran with actual immutable `gpt-5.6-terra` / `medium`;
  no subagents were dispatched or used. Canonical applicability was recorded
  before edits: `receiving-code-review`, TDD and all required references, and
  `verification-before-completion` were fully read and applied. The work used
  refactor safety because the accepted finding changes no behavior: the native
  lifecycle suite was GREEN at `70/70` before the edit.
- Centralized the repeated post-network failed-start checkpoint in the private
  `Server.#advanceFailedStartCleanup()` method. It advances context-intake
  close, attachment detach or failed-start rollback where present, the existing
  owned close group, and terminal cleanup state in the established order. It
  returns at unresolved intake/detach gates. Listener-specific network close and
  all caller-specific primary-error wording/aggregation remain at their callers,
  preserving retry-only-unfinished and AggregateError flattening semantics.
- Corrected public `Server.start()` TSDoc with current observable behavior:
  after recovery, built contexts register sequentially in deterministic input
  order; all registrations succeed before HTTP server creation/listener open;
  registration failure opens no listener. No internal group or handle policy is
  exposed, and `RunningServer` wording is unchanged.
- Current changed paths are `packages/server/src/server/server.ts` and these
  three records. Existing tests already protect all centralized paths and exact
  failure boundaries, so no structure-only test was added. No Slice 3, external
  docs, API/root, example, Protobuf, or generated tracked path changed.
- Remaining uncertainty is Slice 3 only: real child-process ZeroMQ proof,
  bounded teardown/timeouts, observable external docs, and final all-slice
  verification. Security remains deferred to T-0041.
- Fresh verification passed: pre/post native server lifecycle `70/70`, focused
  context/routing/buses `65/65`, native runtime transport `14/14`, generated
  build typecheck, scoped ESLint, cleanup enforcement, exact Prettier,
  `docs:check` with 205 server exports, canonical generated-clean, public-root
  and declaration leak scan, `git diff --check`, and expected status/diff. No
  full verify or commit was performed.

## Slice 2 P2 Coordinator Verification

- Fresh coordinator evidence passed native lifecycle `70/70`, build typecheck,
  scoped ESLint, exact Prettier, cleanup, docs/API check with 205 server exports,
  canonical generated-clean, root/status inspection, and diff checks.
- Only style/maintainability and documentation require rereview. Prior clean API
  and reliability results remain applicable because signatures and lifecycle
  behavior are unchanged.

## Slice 2 P2 Rereview

- Endpoint `adefe8c8`; package
  `.superpowers/sdd/review-d4debde8..adefe8c8.diff` from `d4debde8`, three
  commits, 25,092 bytes.
- Expected explicit profiles: style existing role `gpt-5.6-terra` / high;
  documentation existing role `gpt-5.6-luna` / medium; both no subagents.
  Actual immutable metadata must match before acceptance.

## Slice 2 Clean Closure

- Style rereviewer `019f5ebe-eb13-7692-9944-1d5911f90341` and documentation
  rereviewer `019f5ebe-e7db-7bb2-ae16-89e84446164e` returned CLEAN at matching
  actual immutable profiles, no subagents, and were closed.
- Corrected endpoint `adefe8c8` is accepted; all four canonical concerns are
  clean or applicable as recorded, security remains deferred.

## Slice 3 Assignment

- Existing implementer resumes at explicit expected and actual immutable
  `gpt-5.6-terra` / medium, no subagents.
- Scope: one public-composition Node child/ZeroMQ proof with bounded handshake,
  command/event/projection observations, cleanup/timeouts, observable docs, and
  final all-slice verification. No example edits, public internals, topology,
  supervision, distributed transport, or production retry policy.

## Slice 3 Implementation Result

- Existing implementer actual immutable profile remained
  `gpt-5.6-terra` / `medium`; no subagents were dispatched or used. Before
  implementation, applicability and complete reads were recorded for
  `implement`, `executing-plans`, `using-git-worktrees`, `tdd`,
  `test-driven-development` and required references,
  `javascript-testing-patterns`, `nodejs-backend-patterns`,
  `error-handling-patterns`, and `verification-before-completion`.
  `systematic-debugging` was read and applied only after a concrete final-cycle
  blocker appeared.
- TDD command-path RED: after correcting Vitest collection so the parent was
  `.test.ts`, the focused test launched and failed at child readiness with the
  exact missing `server-context-transport-child.mjs` module. GREEN created the
  plain Node child, used public package entry points, sent ready only after
  `Server.start()`, accepted one parent ZeroMQ command request, and passed
  bounded cleanup (`1/1`).
- Emitted-event RED timed out after 5000ms waiting for command handling and
  projection observation. GREEN added a real aggregate repository whose command
  handler emits a generated event, plus real primary projection state update;
  command handling and projection passed (`1/1`).
- Inbound-event RED timed out after 5000ms waiting for two downstream
  observations. Diagnostics showed the command-produced event repeatedly
  replaying to the audit projection. The public transition validator isolated
  the reason: `RichSetOnceState.tags` is a repeated set-once field and transition
  validation intentionally rejects it. A direct validator check proved
  `SingularSetOnceState` accepts the required default-to-mutable transition;
  swapping only that test state yielded final GREEN (`1/1`). The fixed inbound
  event is republished every 20ms only until both observations arrive, followed
  by a quiet window; the same event identity reaches each projection exactly
  once and duplicates fail.
- Parent and child use distinct `createZeroMqTransport()` instances with one
  deterministic adapter identity and unique private absolute mode-`0700` IPC
  directory. Transport request/publish phases are bounded at 2s and
  handshake/observation/exit phases at 5s. Node IPC is restricted to bounded
  readiness, sanitized errors, behavior/entity observations, shutdown, and
  stopped messages; generated command/event envelopes use ZeroMQ only.
- Final cleanup evidence closes parent transport first; child shutdown closes
  `RunningServer`, caller-owned environment/storage owner, then caller-owned
  transport; parent awaits exit, uses TERM/KILL only after grace, checks listener
  refusal, recursively removes the IPC directory, and confirms `ENOENT`.
  Non-zero exit, forced termination, duplicate observations, background
  transport failure, listener acceptance, or retained IPC files fail the test.
- Changed behavior/test paths:
  `packages/server/test/server/server-context-transport-cross-process.test.ts`,
  `packages/server/test/server/server-context-transport-child.mjs`, and one
  type-only third-generic correction in
  `packages/server/test/runtime/context-transport.test.ts`. Observable docs:
  `packages/server/README.md`, `docs/USER_GUIDE.md`, and
  `docs/architecture/README.md`. Durable records are synchronized. No production
  source, public root/API, example, Protobuf, generated tracked output, or
  `human-review-1-jul.md` changed; no commit was created.
- Focused native gate passed 12 files and `167/167` tests (new child, all Slice
  1/2 lifecycle/context/runtime/routing/bus regressions, and all ZeroMQ adapter
  tests). Generated build/tooling typechecks, scoped ESLint, cleanup, exact
  Prettier, `docs:check` with 205 server exports, canonical generated-clean,
  public-root/declaration/private-import/protected-path scans,
  `git diff --check`, and status/diff passed. The first final verify stopped at
  tooling typecheck on the owned missing transport generic and new-test
  narrowing; both were corrected and independently rerun green.
- Final native `pnpm --config.verify-deps-before-run=false verify` passed 71
  files and `1627/1627` tests in ordinary and coverage runs. Coverage was 95.37%
  statements, 90.15% branches, 98.13% functions, and 95.4% lines. Whole-repo
  typecheck/lint/format/cleanup, TypeDoc/API (205 server exports), proto lint,
  and generated-clean also passed.
- Remaining uncertainty: this proof is trusted same-host Node/V8 IPC only and
  uses local timing/temporary filesystem behavior. Remote topology, broker and
  process supervision, production retry policy, deployment hardening, and
  security review remain out of scope; security is deferred to T-0041. Slice 3
  is handed back for review only; parent T-0038 closure/merge has not started.

## Slice 3 Coordinator Pre-Review Findings

- Fresh native proof passed `1/1` before correction.
- Setup ownership is not failure-safe before `CrossProcessFixture` returns;
  close any created parent transport/child and remove the temporary directory
  when config/transport/fork/constructor setup rejects.
- Teardown must inspect the private IPC directory after child/transports close
  and fail on retained socket/file entries before recursive removal. Recursive
  removal remains mandatory even when the leak assertion fails.
- Existing implementer resumes at explicit `gpt-5.6-terra` / medium, no
  subagents. No docs/production/Slice 1/2 changes.

## Slice 3 Teardown Correction Work

- Role metadata is unchanged and immutable: existing implementer,
  `gpt-5.6-terra`, medium reasoning; no subagents. Skill applicability was
  recorded before work. Receiving-code-review and TDD were applicable to the
  bounded accepted batch; JavaScript testing and error-handling patterns shaped
  deterministic fault injection and primary-first cleanup aggregation;
  systematic debugging was applied when the native proof exposed retained IPC
  entries; verification-before-completion remains applicable and forbids a
  passing claim.
- RED/GREEN setup evidence: after a real parent transport and child were
  created, an injected pre-construction rejection initially escaped as the
  setup error alone. `CrossProcessFixture.create()` now catches all failures
  after `mkdtemp()`, closes any created parent transport, requests and bounds
  child shutdown/termination, recursively removes the directory, confirms
  absence, and throws the original setup error first followed by sanitized
  cleanup diagnostics. The focused setup test passed after implementation and
  independently observed one parent close, child exit, and directory absence.
- RED/GREEN leak-helper evidence: a retained-file test first failed with a
  missing inspection helper. The implemented helper records the exact retained
  entry count after close, still removes recursively, and verifies `ENOENT`; the
  focused test then passed. Child exit/termination and directory removal are
  shared between failed setup and normal close without duplicating the normal
  fixture close state machine.
- The complete focused native file is intentionally still RED: `3` tests ran,
  `2` passed, and the end-to-end proof failed only because the new inspection
  found `2` entries after the parent transport closed and the child completed
  its own transport close and exited. A separate native ZeroMQ probe retained a
  bound IPC pathname at 0/10/100/500/1000 ms after `close()` and after explicit
  `unbind()`. Source tracing confirms the adapter closes sockets but does not
  unlink bound deterministic endpoint files.
- This exposes a scope conflict rather than a harness cleanup race. The
  committed assignment permits only the cross-process harness and records and
  explicitly forbids production changes. Pre-inspection fixture deletion would
  falsify the accepted leak proof. Work is therefore handed back as
  `NEEDS_CONTEXT`: authorize the smallest ZeroMQ adapter endpoint-ownership
  cleanup or revise the retained-pathname criterion.
- Fresh green checks: `pnpm typecheck:generated`; scoped ESLint for the fixture;
  cleanup enforcement; exact fixture Prettier; `pnpm docs:check` (205 server
  exports); `pnpm proto:check-generated`; diff whitespace; private-import,
  public-root, protected-path, and expected status/diff scans. Correction-owned
  paths are
  `packages/server/test/server/server-context-transport-cross-process.test.ts`
  and the three T-0038b records. No full verify, production/docs, Slice 1/2,
  commit, or parent closure work was performed in this correction.

## Slice 3 Adapter Cleanup Authorization

- Coordinator authorizes the actual owner fix and rejects weakening the harness
  with pre-inspection deletion.
- Bound publisher and replier endpoint paths must be unlinked after socket
  close; connect-only subscribers/requesters own no path. `ENOENT` is success,
  all failures are attempted/reported, and a failed unlink remains retryable on
  later close.
- Authorized write scope adds only
  `packages/transport/src/zeromq/signal-transport.ts`, its existing focused
  test, the current harness, and these records. Same implementer continues at
  explicit `gpt-5.6-terra` / medium, no subagents; no public API/policy change.

## Slice 3 Adapter Cleanup Work

- Role/profile evidence is immutable: existing implementer, explicit
  `gpt-5.6-terra`, medium reasoning; no subagents. Before implementation,
  receiving-code-review, both TDD skills and required references, JavaScript
  testing patterns, error-handling patterns and details, and
  verification-before-completion were read and applied. Node backend patterns
  were N/A because no server/API lifecycle changed.
- Publisher vertical cycle: the new native test failed because one
  `se-p-...sock` pathname remained after `transport.close()` (`1` failed, `11`
  skipped). GREEN introduced the internal endpoint file boundary and removed
  the successfully bound publisher path only after native socket close (`1`
  passed, `11` skipped).
- Replier vertical cycle: the new native test failed because one
  `sc-r-...sock` pathname remained after registration close (`1` failed, `12`
  skipped). GREEN assigned only the successfully bound Reply path to its
  registration cleanup (`1` passed, `12` skipped). A separate native
  characterization proves closing Subscriber and Request connect-only sockets
  leaves sibling-owned Publisher and Reply paths intact.
- Retry vertical cycle: fail-once internal unlink injection first left both
  owned paths because handle failure aborted publisher cleanup (`1` failed,
  `14` skipped). GREEN separated socket-close completion from retained pathname
  ownership, continued through every handle/publisher, and retried only the
  failed path without a second native close (`1` passed, `14` skipped).
  Additional native cases prove concurrent close identity, stable
  replier-before-publisher aggregate diagnostics for two failures, successful
  retry of both, `ENOENT` success, and cleanup when close wins a publisher bind
  race. Final adapter file: `18/18`.
- Production paths are
  `packages/transport/src/zeromq/signal-transport.ts` and new package-internal
  `packages/transport/src/zeromq/endpoint-files.ts`; focused tests are in
  `packages/transport/test/zeromq/signal-transport.test.ts`. The helper is not
  exported from either transport root and introduces no public/test-only
  option. Publisher/Reply filesystem paths are tracked only after successful
  bind. Close first stops the socket, ignores missing files, retains non-missing
  unlink failures for retry, and attempts resources in deterministic order.
- Native child proof rerun: `3/3`, including pre-removal empty-directory
  inspection. Focused transport/runtime/context/routing/bus/server rerun: 13
  files and `182/182` tests. Fresh generated build/tooling typechecks, scoped
  ESLint, cleanup, exact Prettier, docs/API (17 transport and 205 server
  exports), canonical generated-clean, public/private/package/protected leak
  scans, `git diff --check`, and expected status/diff pass. One unnecessary
  assertion was the only scoped lint RED and was removed before the green
  rerun.
- Existing harness/docs and unrelated changes were preserved. No server
  lifecycle, public API/docs wording, example, Protobuf, topology, retry timing,
  commit, or parent closure change was made. Full verify is deferred until the
  reviewer accepts this correction. Remaining uncertainty is limited to review
  disposition and the already recorded trusted same-host/local deployment
  boundary.
- Coordinator handback inspection confirmed that successful cleanup removes
  map/set ownership, failed unlink cleanup remains retryable, native socket
  close is not repeated, in-flight binder promises are awaited, and connector
  sockets own no path. The independent native rerun passed the adapter and
  cross-process files together: `2` files, `21/21` tests. `git diff --check`
  passed. The implementation endpoint is accepted for pre-review lint and the
  four-lane review wave.

## Slice 3 Pre-Review Lint

- Status headers are aligned. The changed-path and export scan found no root or
  subpath export change and no internal endpoint-cleanup helper leak. The
  changed documentation explicitly preserves same-host-only scope and defers
  remote topology, supervision, scheduling/backoff, and retry policy.
- The duplicated-policy scan found one actionable issue before reviewer
  dispatch: `adapterIdentity` and `transportTimeoutMs` are defined separately
  by the parent and child fixture although they must agree. The same Terra
  Medium implementer is resumed to make the parent the single source and
  validate both environment values in the child. Focused native proof and
  durable-record updates are required before review packaging.

## Slice 3 Pre-Review Lint Correction Work

- Role/profile remained the existing implementer, explicit
  `gpt-5.6-terra` / medium; no subagents. Receiving-code-review, strict TDD and
  required references, JavaScript testing patterns, error-handling patterns,
  and verification-before-completion were applied as recorded.
- Parent/child handoff RED changed the parent identity and supplied both shared
  values through the child environment. The unchanged child bound the old
  identity, so the real command request timed out after 2000 ms (`1` failed,
  `2` skipped). GREEN removed both child constants and required the parent
  values (`1` passed, `2` skipped).
- Adapter-boundary RED initially found a test-only IPC pathname-length defect;
  shortening that diagnostic directory prefix produced the valid behavioral
  RED: the child reached ready with a whitespace-surrounded identity (`1`
  failed, `3` skipped). GREEN rejects non-normalized identity text and invalid
  characters before transport creation. Timeout RED proved permissive
  `Number()` accepted `2e3` and reached ready (`1` failed, `4` skipped). GREEN
  requires non-zero canonical decimal digits and a safe integer.
- Full unrestricted native child-process file passes `5/5` after the restricted
  sandbox refused the fixture's IPC bind with `Operation not permitted`. Fresh
  tooling typecheck, scoped
  ESLint, and exact Prettier pass. Changed implementation/test paths are only
  `packages/server/test/server/server-context-transport-cross-process.test.ts`
  and `packages/server/test/server/server-context-transport-child.mjs`, plus
  the three records. Existing endpoint cleanup, docs, production and unrelated
  work remain untouched. No public API, server lifecycle, timing value/policy,
  topology, example, Protobuf, commit, or parent closure change occurred.
- Exact record formatting, duplicate/private-path and protected-path scans,
  `git diff --check`, and expected five-path status/diff pass. Reviewer dispatch
  and full verification remain with the coordinator after this pre-review lint
  correction.
- Coordinator inspection and an independent unrestricted native rerun accepted
  the correction: the child-process file passed `5/5`, with no duplicate child
  defaults or fallback policy. The review package may now be generated from
  the clean Slice 2 endpoint.

## Slice 3 Review Dispatch

- Fresh package: `.superpowers/sdd/review-1b19e7a2..b031fe84.diff`, baseline
  `1b19e7a2`, endpoint `b031fe84`, five commits, 139514 bytes.
- Expected existing roles and immutable profiles are: style/maintainability
  `gpt-5.6-terra` / high; documentation `gpt-5.6-luna` / medium;
  TypeScript/API docs `gpt-5.6-terra` / high; performance/reliability
  `gpt-5.6-terra` / high. All are read-only, Slice 3 bounded, and have no
  subagents. Results must include actual runtime metadata before acceptance.
- Reviewer prompts prioritize concrete defects in cross-process behavior,
  cleanup ownership, public-doc accuracy, API containment, and bounded
  lifecycle behavior. Superseded historical text is ignored unless current
  records or changed active docs claim it. Security is deferred to T-0041.
- The first documentation result was substantively CLEAN but rejected because
  it reported actual runtime metadata as unavailable. Reviewer
  `019f5f0b-3c41-70b0-9ba6-c13e7e6ad284` is closed. A replacement existing
  documentation reviewer is assigned explicit `gpt-5.6-luna` / medium,
  read-only with no subagents, against the same package.

## Slice 3 Review Results And Fix Assignment

- Style reviewer `019f5f0b-3900-7c83-84fe-988c192aac28`: accepted P1, failed
  responder bind closes neither an owned handle nor the new native `Reply`.
  Runtime role metadata exposed to the orchestrator is immutable
  `gpt-5.6-terra` / high and matches explicit dispatch; reviewer closed.
- Replacement documentation reviewer
  `019f5f0d-6bbd-7442-9750-4c8b35f9f405`: accepted P2, exactly-once wording
  in three active docs must be limited to the bounded test observation and
  disclaimed as a general durable/retry/restart/remote guarantee. Actual
  immutable `gpt-5.6-luna` / medium confirmed; reviewer closed.
- TypeScript/API reviewer `019f5f0b-3f79-74e3-81ec-b9608a979edb`: CLEAN.
  Runtime role metadata exposed to the orchestrator is immutable
  `gpt-5.6-terra` / high and matches explicit dispatch; reviewer closed.
- Performance/reliability reviewer
  `019f5f0b-42df-7941-ad5e-12b3bd9bd5bd`: accepted P1 for close-vs-subscribe
  leaking a live connector and P2 for command observations returning before a
  delayed duplicate can arrive. Runtime role metadata exposed to the
  orchestrator is immutable `gpt-5.6-terra` / high and matches explicit
  dispatch; reviewer closed.
- All reviewers completed the bounded skill applicability check and used no
  subagents. The same existing implementer is resumed at immutable explicit
  `gpt-5.6-terra` / medium with the deduplicated complete finding batch. It must
  use focused TDD, update all three records, rerun affected native tests and
  static checks, and make no unrelated/public/policy changes.

## Slice 3 Specialist Fix Evidence

- `2026-07-14`: Existing implementer, actual immutable explicit
  `gpt-5.6-terra` / medium; no subagents. Applied receiving-review, both strict
  TDD skills and required testing references, JavaScript async/timer testing,
  TypeScript async cleanup/error aggregation, systematic debugging when native
  injection and static checks behaved unexpectedly, and fresh verification.
- Responder RED used deterministic package-internal bind/close injection and
  failed because failed bind invoked close zero times (`1` failed, `18`
  skipped). GREEN closes the newly created `Reply`; injected native close still
  runs real close and yields stable `[bindFailure, closeFailure]` diagnostics
  (`1/1`, `18` skipped). The initial real duplicate-bind and subclass-prototype
  spy was discarded: this native IPC bind was not a deterministic failure, and
  `close` belongs to the non-configurable base `Socket` prototype. The final
  internal access object is not re-exported from either public transport entry.
- Subscribe/close RED blocked IPC preparation and observed transport close
  settle early (`true` instead of `false`; `1` failed, `19` skipped). GREEN
  tracks the in-flight open, makes close wait, closes the resumed Subscriber,
  rejects the open as closed, starts no receive loop, and receives none of five
  later sibling publications (`1/1`, `19` skipped).
- Delayed-command RED used a child-only `100 ms` duplicate control observation;
  `observeCommand()` returned the first three instead of rejecting (`1` failed,
  `5` skipped). GREEN holds a `200 ms` quiet window shared with the fixed event
  proof and rejects four observations (`1/1`, `5` skipped). Real command and
  event envelope traffic remains ZeroMQ-only.
- The three reviewed docs now scope one-per-projection language to the fixed
  event's bounded observations and state that the proof is not a durable
  redelivery, retry, restart, remote, or general exactly-once guarantee.
- Native full-file results are adapter `20/20` and child process `6/6`. Generated
  build/tooling typechecks, scoped four-file ESLint, cleanup enforcement, exact
  seven-file Prettier, and docs/API checks pass with unchanged `17` transport
  and `205` server exports. Intermediate diagnostics were one optional-handle
  TypeScript narrowing error, one missing explicit `node:timers` lint import,
  and one overlong test diagnostic; all were locally corrected and affected
  gates passed on rerun.
- Changed paths are the ZeroMQ adapter/test, two child-process fixture files,
  three active docs, and three T-0038b records only. Endpoint-path cleanup,
  public surfaces, server production lifecycle, topology/retry policy, examples,
  Protobuf, unrelated work, and Git history are untouched. Canonical
  generated-clean, root/private/protected scans, exact ten-file Prettier,
  `git diff --check`, and expected ten-path diff/status pass. Re-review/full
  verify remain coordinator-owned; same-host and T-0041 security uncertainty is
  unchanged.
- Coordinator inspection accepted the responder cleanup ordering, tracked
  subscriber admission, delayed-duplicate proof, and narrowed docs. The
  independent unrestricted native rerun passed both affected files with
  `26/26` tests. The fix endpoint is ready to commit and package for targeted
  rereview.

## Slice 3 Targeted Rereview Dispatch

- Baseline `b031fe84`, corrected endpoint `85d9fd87`, package
  `.superpowers/sdd/review-b031fe84..85d9fd87.diff` (four commits, 56803
  bytes).
- Existing style and reliability roles are explicit `gpt-5.6-terra` / high;
  existing documentation role is explicit `gpt-5.6-luna` / medium. Each is
  read-only, fix-scoped, and has no subagents. Actual immutable runtime role
  metadata must match before acceptance. API's clean disposition is retained.

## Slice 3 Targeted Rereview Results

- Documentation reviewer `019f5f1f-39f6-7a52-8dda-d9b39cba66b1`: CLEAN,
  confirmed immutable `gpt-5.6-luna` / medium, no subagents, closed.
- Style reviewer `019f5f1f-36ad-7a11-b5ba-e4de82cb7633`: accepted P2 to
  restore primary-declaration-first order by moving the internal socket access
  below `createZeroMqTransport`; confirmed immutable `gpt-5.6-terra` / high,
  no subagents, closed.
- Reliability reviewer `019f5f1f-3d32-7e90-8938-9f6bb0fbb465`: accepted P2.
  Exact-three polling can miss the intermediate count and wait five seconds;
  injection also needs a deterministic parent quiet-window trigger. Confirmed
  immutable `gpt-5.6-terra` / high, no subagents, closed.
- Each completed skill applicability. TypeScript/API remains CLEAN. The same
  implementer is resumed at explicit `gpt-5.6-terra` / medium, no subagents,
  with both P2s in one bounded fix pass.

## Slice 3 Targeted Rereview Fix Evidence

- `2026-07-14`: Existing implementer at actual immutable explicit
  `gpt-5.6-terra` / medium; no subagents. Receiving-review, strict vertical TDD
  and required references, JavaScript promise/async testing, error handling,
  and fresh verification guidance were applied.
- RED changed only the duplicate regression contract from a child timer delay
  to parent quiet-window control. Because the new option had no implementation,
  the native test resolved `command-handled`, `primary-projected`, and
  `secondary-projected` instead of rejecting (`1` failed, `5` skipped).
- GREEN stores the child's actual command observation, then accepts one fixed
  parent control message with no entity or envelope data. The parent sends it
  only after `>= 3` admission and exact-three validation while entering the
  quiet window. Admission over-count rejects immediately; the `200 ms` quiet
  window checks every `10 ms` and rejects a later fourth. Focused GREEN is
  `1/1`, `5` skipped. The delay environment, parser, timer import, and timer
  callback are gone; command/event payloads remain ZeroMQ-only.
- The internal native access declaration moved below the unchanged public
  factory. Static order is factory line `50`, access line `58`; root and
  `./zeromq` declarations remain leak-free.
- Fresh native adapter and child files pass `20/20` and `6/6`. Generated build
  and tooling typechecks, scoped three-file ESLint, cleanup enforcement, exact
  Prettier, obsolete-delay and public-root scans, and `git diff --check` pass.
- Only the transport declaration-order source, two fixture files, and three
  records changed. Accepted docs and all production/public/topology/retry/
  example/Protobuf behavior remain untouched; no commit was created. Remaining
  work is coordinator acceptance/full verification, with same-host and T-0041
  security uncertainty unchanged.
- Coordinator inspection and an independent unrestricted rerun accepted the
  deterministic control path and declaration order. Adapter plus child-process
  files passed `26/26`. The endpoint is ready to commit; only style and
  reliability rereview remain.
- Final package is `.superpowers/sdd/review-85d9fd87..e354cf10.diff`, baseline
  `85d9fd87`, endpoint `e354cf10`, three commits, 34180 bytes. Existing style
  and reliability roles are explicit `gpt-5.6-terra` / high, read-only, no
  subagents. Docs/API clean results remain applicable.

## Slice 3 Final Rereview Result

- Style reviewer `019f5f29-a640-7433-8d5f-3c19434def8d` and reliability
  reviewer `019f5f29-aa89-7c30-bdfb-a0413c30fc32` both confirmed declaration
  order clean and independently reported the same P2: IPC send completion is
  not child-application evidence, so the fourth observation can arrive after
  the 200 ms deadline. Both roles have immutable runtime configuration
  `gpt-5.6-terra` / high, used no subagents, completed skill applicability, and
  are closed.
- The same existing implementer is resumed explicit `gpt-5.6-terra` / medium,
  no subagents. One bounded child-applied acknowledgment/observation barrier,
  exact-four rejection proof, focused native tests, and record updates are
  required. Docs/API and declaration-order clean dispositions remain.

## Slice 3 Final Rereview Acknowledgment Fix Work

- `2026-07-14`: Existing implementer; actual immutable profile explicit
  `gpt-5.6-terra` / medium; no subagents. Applied receiving-review, strict TDD
  and required references, JavaScript promise/async testing, TypeScript/Node
  error handling, and fresh verification-before-completion guidance.
- RED installed the parent-side child-application barrier without changing the
  child. The focused unrestricted native run failed as required (`1` failed,
  `5` skipped): the parent received the duplicate observation but, without a
  processed-control acknowledgment, reported `Cross-process command duplicate
control was not applied within the bounded quiet window.` instead of the
  expected four-observation rejection.
- GREEN changes the child duplicate-control branch to ordered async work: await
  submission of the remembered `observed` control, then submit the one-key
  `duplicate-command-observation-applied` acknowledgment. The parent accepts
  only that exact shape, waits for it within the existing quiet deadline, and
  only then requires exactly three observations, deterministically rejecting
  the received fourth. Focused native GREEN is `1/1` with `5` skipped; the full
  native child-process file is `6/6`.
- The real command/event proof and existing timing remain unchanged. Node IPC
  carries control only, with no command/event payload or reintroduced timer/
  environment delay machinery. Tooling typecheck, scoped ESLint, fixture
  Prettier, cleanup enforcement, and child syntax checks pass. Exact record
  formatting, protected/root/private scans, `git diff --check`, and five-path
  status/diff checks also pass.
- Paths are only
  `packages/server/test/server/server-context-transport-cross-process.test.ts`,
  `packages/server/test/server/server-context-transport-child.mjs`, and the
  three T-0038b records. Production transport, docs, public API, topology/retry
  policy, examples, Protobuf, unrelated work, and Git history are untouched.
  Handback is DONE for the deduplicated P2; coordinator acceptance/final verify
  and existing same-host/T-0041 uncertainty remain.
- Coordinator inspection confirmed ordered observation then acknowledgment and
  an exact child-message validator. The independent unrestricted native file
  passed `6/6`. The correction is accepted for reliability-only rereview.
- Reliability package: `.superpowers/sdd/review-e354cf10..d9c21256.diff`,
  baseline `e354cf10`, endpoint `d9c21256`, three commits, 24181 bytes. Existing
  performance/reliability role is explicit `gpt-5.6-terra` / high, read-only,
  no subagents. Other clean lanes remain applicable.
- Reliability reviewer `019f5f34-b644-7c93-9dae-08c4009ea35a` confirmed
  immutable `gpt-5.6-terra` / high, completed skill applicability, used no
  subagents, and is closed. It accepts barrier behavior but reports P2 missing
  deterministic coverage for an acknowledgment timestamp at/after the quiet
  deadline.
- Same existing implementer resumes explicit `gpt-5.6-terra` / medium, no
  subagents. Add one bounded parent-fixture late-classification case and update
  records; do not add child timers or change accepted behavior.

## Slice 3 Late-Acknowledgment Regression Work

- `2026-07-14`: Existing implementer, actual immutable explicit
  `gpt-5.6-terra` / medium; no subagents. Applied receiving-review, strict TDD
  and required references, JavaScript async-testing guidance, and fresh
  verification-before-completion.
- RED added the one behavior case first. Because its requested parent
  timestamp classifier was not implemented, the real child observation and
  acknowledgment followed normal classification; the focused native test
  failed with the four-observation diagnostic rather than the expected barrier
  diagnostic (`1` failed, `6` skipped).
- GREEN introduces one private `CrossProcessFixture` classifier from actual
  acknowledgment receipt time to classified receipt time. The default is the
  identity mapping. The regression maps the real receipt to
  `Number.MAX_SAFE_INTEGER`, which is deterministically after the quiet
  deadline and reaches the existing missing/late application-barrier error.
  Focused GREEN is `1/1` with `6` skipped; full native result is `7/7`.
- Child ordered observation/ack logic, Node IPC control shape, all real ZeroMQ
  traffic, and normal timing remain unchanged. No timers, environment delay,
  sleeps, production code, or child file were added or modified.
- Tooling typecheck, scoped ESLint, cleanup rules, exact four-file Prettier,
  `git diff --check`, protected-path scans, and exact four-path status/diff pass.
  Only the parent cross-process test fixture and three T-0038b records changed;
  no commit was created. Handback is DONE for this P2, with coordinator
  acceptance/final verify and existing same-host/T-0041 uncertainty remaining.
- Coordinator inspection confirmed the real acknowledgment is received while
  only its parent-side classified timestamp is made late. The independent
  unrestricted native file passed `7/7`; the correction is accepted for final
  reliability rereview.
