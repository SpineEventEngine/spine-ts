# T-0108 Work Log

- 2026-08-04: created high-risk task T-0108 from clean
  `origin/main@7c5457d1` in isolated branch/worktree
  `task/T-0108-durable-stand-registry` / `.worktrees/T-0108-durable-stand-registry`.
  The branch is pushed; the dirty primary checkout remains untouched.
- 2026-08-04: recorded acceptance criteria and the Human-Imposed Requirements
  Ledger from the approved Wave 6 plan. T-0108 owns the registry contract,
  persistence, cleanup, builder ownership, and production warning; T-0109 owns
  polling and listener reconciliation.
- 2026-08-04: assigned one milestone-boundary architecture pass to the existing
  `requirements_splitter`, explicit `gpt-5.6-sol` / `high`. Runtime
  self-introspection is unavailable; immutable role/profile and explicit
  dispatch are the metadata evidence unless a mismatch is visible.
- 2026-08-04: the Sol/high architecture pass completed with no human blocker
  and no JVM build. It freezes the public lifecycle/result contract, one
  definition row plus a provider-neutral CAS control row, staged crash recovery,
  100/lower limits, 1 MiB record bound, physical deletion, finite cleanup,
  context/custom ownership, close order, and environment warning. T-0109
  polling/listener responsibilities remain excluded.
- 2026-08-04: implementation is assigned to one existing `implementer`,
  explicit `gpt-5.6-terra` / `medium`, owning all overlapping production/tests/
  docs. Runtime self-introspection remains unavailable; immutable role/profile
  and explicit dispatch are the metadata evidence.
- Next: implement RED-first contract/in-memory, durable storage/races,
  context/warning, and provider conformance slices; push every checkpoint.
- 2026-08-04: RED/GREEN in-memory lifecycle checkpoint `ea149d74` was pushed.
  Focused Vitest proves create/pending, activate, physical delete, and the
  100-definition limit. Runtime self-introspection remains unavailable; the
  immutable implementer role/profile and explicit `gpt-5.6-terra` / `medium`
  dispatch are the available metadata evidence.
- 2026-08-04: codec and context ownership checkpoint `c2cf1eef` was pushed.
  The focused suite (four tests) and `pnpm typecheck:build:generated` passed.
  The default-storage checkpoint `e0d933e1` was then pushed with the same
  focused suite and typecheck green. It creates a storage-backed default
  registry and rejects non-atomic handles. Remaining required work is the
  shared CAS control record/recovery/race conformance plus warning and docs;
  no provider-live pass is claimed.
- 2026-08-04: the same existing implementation context is redispatched under
  explicit `gpt-5.6-terra` / `medium` to close every remaining acceptance gap:
  recoverable CAS control-slot admission/deletion, full durable lifecycle and
  races, memory/MySQL/Datastore conformance under existing service policy,
  production-only warning, complete lifecycle ownership tests, and server
  README/reference/TSDoc/export checks. Partial completion is not accepted and
  no architecture decision remains open.
- 2026-08-04: the original implementer context returned clean at `15db10e9`
  without addressing the resumed gaps and is treated as closed/unavailable, not
  as a project blocker. To keep ownership bounded, a fresh existing
  `implementer`, explicit `gpt-5.6-terra` / `medium`, is assigned only the
  durable CAS registry core, lifecycle, recovery/races, and provider conformance
  tests. Context/environment/docs follow serially after that pushed core; no
  production writers overlap.
- 2026-08-04: durable CAS checkpoint `0bff4fd7` is pushed and its six focused
  tests/typecheck/lint/format checks pass, but orchestrator acceptance is
  withheld. The public contract still diverges from the frozen disposition
  (result variants, bigint revision, revision-aware delete, cleanup result/page),
  and assigned durable evidence is missing for 50-row shape, staged
  create/delete restart recovery, malformed control/data, lifecycle races,
  close-drain semantics, and provider conformance. The same durable-core
  implementer, explicit `gpt-5.6-terra` / `medium`, is redispatched only to
  close these enumerated gaps before the next production slice.
- 2026-08-04: durable-core RED/GREEN is complete in the same explicit existing
  `implementer` context (`gpt-5.6-terra` / `medium`; runtime self-introspection
  unavailable). A failing cross-handle limit-one test proved the former
  snapshot-count race. The storage registry now has one definition storage and
  a separate atomic-CAS `Any` control slot with positive revision, bounded
  count, and at most one staged create/delete operation. Every public durable
  operation settles a staged operation before proceeding; startup/snapshot
  initialization validates at most `limit` definition rows and fails closed on
  malformed controls, records, count mismatches, or digest mismatches. Create
  reserves capacity before the definition CAS; delete stages before physical
  deletion and releases capacity only as recovery settles. Focused registry
  tests (six cases), server TypeScript, changed-file ESLint, Prettier, and
  `git diff --check` pass. Provider-live conformance and context/environment
  work remain owned by later serial slices and are not claimed here.
- 2026-08-04: resumed durable-core audit checkpoint `02d56cc1` was pushed by
  the same explicit existing `implementer` (`gpt-5.6-terra` / `medium`; runtime
  self-introspection unavailable). The frozen stable malformed-record error is
  now `Stand subscription record is invalid.` and its focused test, changed-file
  ESLint, and formatting pass. The public registry still requires the separately
  enumerated breaking contract conversion (generated `SubscriptionId`, lowercase
  phases, bigint revisions, discriminated results, revision-aware delete, and
  paged cleanup) before acceptance; no provider-conformance outcome is claimed.
- 2026-08-04: the second durable-core context closed after only the malformed
  error correction and is no longer accepted as implementation owner. A fresh
  existing `implementer`, explicit `gpt-5.6-terra` / `medium`, is assigned one
  smaller non-overlapping slice: exact public contract plus in-memory lifecycle,
  bounds, cleanup, close, clone/freeze, exports, and focused tests. Durable
  storage follows serially after this contract endpoint; no writers overlap.
- 2026-08-04: public/in-memory checkpoint `d1850202` is pushed with 11 focused
  tests and typecheck/ESLint/cleanup/format green. Acceptance remains pending:
  deterministic TSDoc reports incomplete public/member documentation, and the
  cleanup page currently selects the first 25 definitions rather than the
  first 25 expired pending definitions, which can starve expired rows behind
  active IDs. The same public/in-memory implementer, explicit
  `gpt-5.6-terra` / `medium`, is redispatched only for complete TSDoc and the
  expired-row page regression before this slice closes.
- 2026-08-04: the cleanup-starvation correction and its twelfth focused test
  are preserved uncommitted after the prior context stopped at the TSDoc gate.
  A fresh existing `implementer`, explicit `gpt-5.6-terra` / `medium`, is
  assigned to preserve that behavior, rewrite every new TSDoc block into the
  mandated multiline/blank-line form, complete the deterministic TSDoc list,
  run all focused gates, and commit/push the combined correction. No durable
  behavior or later context/environment scope is assigned.
- 2026-08-04: the same bounded existing `implementer` context, explicitly
  `gpt-5.6-terra` / `medium` (runtime self-introspection unavailable), replaced
  the incompatible public surface with lowercase lifecycle phases, bigint
  revisions, generated `SubscriptionId` operations, discriminated create and
  activate results, revision-aware string delete results, and bounded cleanup
  results. RED-first in-memory tests cover every result branch, exact expiry,
  canonical byte equivalence/conflict, limits, inclusive 1 MiB codec admission,
  paged cleanup, clone/freeze, close sharing, invalid inputs, and exports.
  Storage and codec were adapted only to compile against the new surface;
  durable behavioral completion remains explicitly out of this slice. Next:
  run changed-file quality checks, checkpoint/push this contract endpoint, then
  return durable behavior to its serial owner.
- 2026-08-05: this preserved public/in-memory correction remains owned by the
  existing `implementer`, explicitly `gpt-5.6-terra` / `medium`; runtime
  self-introspection is unavailable, so the immutable role/profile and explicit
  dispatch are the available metadata evidence. The active-prefix regression
  proves cleanup selects sorted expired pending definitions before taking its
  25-row page, reports `more` while expired rows remain, and preserves active
  definitions. The registry, codec, and bounded-context TSDoc now use mandatory
  multiline blocks, blank preceding lines, callable summaries, complete
  parameter tags, and return descriptions. No durable storage behavior or
  context/environment prose changed in this correction. The assigned
  standalone-function necessity ledger now records the required TypeScript
  serialization, persistence, public-value, storage, and immutability boundary
  dispositions. Focused TSDoc and the 12-test registry suite pass; server
  typecheck and remaining changed-file quality gates are next before the
  combined correction is committed and pushed.
- 2026-08-05: combined correction `1d6ddae1`
  (`fix(T-0108): close registry cleanup docs`) is committed and pushed to
  `origin/task/T-0108-durable-stand-registry`. Fresh evidence: focused registry
  Vitest passed 12/12; `tsc -b packages/server/tsconfig.json --pretty false`,
  changed-file ESLint, `pnpm lint:cleanup`, repository-wide `pnpm lint:tsdoc`,
  changed-file Prettier, and `git diff --check` all passed. This correction
  closes only the preserved public/in-memory cleanup and TSDoc scope; durable
  storage behavior remains explicitly outside its remit.
- 2026-08-05: after accepting the clean public/in-memory endpoint and pushed
  verification log `f263cc1d`, a fresh existing `implementer`, explicitly
  `gpt-5.6-terra` / `medium`, is assigned only the remaining durable registry
  behavior and provider conformance. It owns CAS admission/deletion recovery,
  the exact public lifecycle contract, restart and lifecycle races, close
  draining, 50-row shape, malformed durable data/control behavior, and reusable
  memory/MySQL/Datastore conformance under the repository's existing provider
  availability policy. It must not change context/environment/docs behavior,
  must not build Spine JVM, and must commit and push each durable checkpoint.
  Runtime self-introspection is unavailable; the immutable implementer profile
  and explicit dispatch are the accepted metadata evidence absent a visible
  mismatch.
- 2026-08-05: durable-core RED/GREEN adds focused storage-backed regressions
  for expiry activation physical deletion, expired-pending cleanup paging behind
  active lexical prefixes, shared close-promise identity, cross-handle capacity,
  restart visibility, and activate/delete interleaving. Delete recovery now
  clears a staged delete without changing capacity when activation wins its
  definition CAS, rather than treating the intentional revision change as
  malformed control data.
  The focused registry suite passes 18/18. Provider-live MySQL and Datastore
  conformance is still unclaimed pending their repository availability policy.
  This remains the existing `implementer` assignment, explicitly
  `gpt-5.6-terra` / `medium`; runtime self-introspection is unavailable, so the
  immutable role/profile and explicit dispatch remain the recorded evidence.
- 2026-08-05: focused durable shape evidence now proves 50 activated
  definitions occupy exactly 50 definition rows and one separate control row.
  The in-memory durable-provider suite passes 19/19. The repository's live
  MySQL policy requires `SPINE_TS_MYSQL_URL`; the Datastore emulator policy
  requires `DATASTORE_EMULATOR_HOST`. Neither environment variable is present,
  so no unavailable-provider pass is claimed and no live provider was invoked.
- 2026-08-05: orchestrator audit accepts pushed durable checkpoints through
  `0e46789a` and their 19-test evidence, but does not close the assigned slice.
  The focused test file still lacks direct fault injection before and after the
  definition-row write for both staged create and staged delete, malformed
  control/definition cases, snapshot/delete concurrency, and concurrent cleanup
  idempotency. The same existing `implementer`, explicitly
  `gpt-5.6-terra` / `medium`, retains this bounded production context and must
  add those tests and any corrections they expose. Provider variables remain
  unavailable and that honest limitation is accepted; no unavailable provider
  may be reported as passed. Runtime self-introspection remains unavailable,
  so the immutable role/profile and explicit dispatch are the metadata evidence.
- 2026-08-05: audit-closure RED/GREEN adds a focused scripted CAS seam to the
  registry test only. It proves replacement-registry recovery when the staged
  create CAS or definition-row CAS applies then throws, and likewise before or
  after durable delete's physical row removal. It also proves stable malformed
  control and definition rejections, snapshot/delete concurrency, and concurrent
  cleanup's finite (at most 25) idempotent pages. The focused suite passes
  25/25. The MySQL/Datastore environment-variable limitation remains unchanged;
  neither unavailable provider was invoked. This remains the existing
  `implementer` assignment, explicitly `gpt-5.6-terra` / `medium`; runtime
  self-introspection is unavailable, so immutable role/profile and explicit
  dispatch are the available metadata evidence.
- 2026-08-05: durable recovery checkpoint `0922ae1a` is accepted and pushed.
  A fresh existing `implementer`, explicitly `gpt-5.6-terra` / `medium`, now
  owns only the final context/environment/documentation slice: builder one-shot
  and mutual-exclusion semantics, default factory and configured limit,
  failed-build ownership, Stand-before-registry close ordering with complete
  cleanup attempts, one production warning per context for a non-persistent
  registry with no Local warning or startup failure, package-internal access,
  and beginner/agent server documentation of limits and the T-0109 boundary.
  It must use the repository's existing logging conventions rather than invent
  a broad logging subsystem. Runtime self-introspection is unavailable; the
  immutable implementer profile and explicit dispatch are accepted unless a
  visible mismatch appears.
- 2026-08-05: context/environment/docs RED/GREEN closes the custom-registry
  first-build transfer: the builder consumes and closes a custom registry even
  when preflight fails before `BoundedContext` construction, then a later build
  uses the default. Focused tests also cover custom/limit exclusion in both
  orders, lower-limit storage-backed default selection, production one-warning
  behavior through `Server.start`, and Local silence. The warning seam is the
  narrow `ServerEnvironmentSettings.warn` callback (defaulting to
  `console.warn`), and contains only the context name. README/reference now
  record lifecycle, limits, warning, recovery limitation, and T-0109 boundary.
  Runtime self-introspection is unavailable, so immutable implementer
  role/profile and explicit `gpt-5.6-terra` / `medium` dispatch remain the
  metadata evidence.
