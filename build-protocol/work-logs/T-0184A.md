# T-0184A Work Log

## 2026-08-14 - Start

- Created records before production edits at `aed2f194` in the isolated
  T-0184A worktree.
- Frozen scope corrects only the root source-view transaction and generated
  declaration-safe interface tuples. It does not modify unpublished To-Do work.
- The requirements-splitter is configured explicitly as `gpt-5.6-sol` / high;
  the existing implementation owner is configured explicitly as
  `gpt-5.6-terra` / medium. Desktop telemetry is unavailable, so immutable
  configured profiles are recorded as evidence.
- Next: commit and push records, then add focused failing tests before changing
  shared workflow or generator implementation.

## 2026-08-14 - RED/GREEN Checkpoints

- Pushed the preproduction records as `65cccdd6`.
- Tuple RED: two interface-generator assertions failed because companions used
  inferred `as const` arrays. GREEN: all 15 focused interface-generator tests
  pass after explicit readonly nonempty tuple emission; pushed as `ec06ddae`.
- Source-view-record RED: the new fixed-record helper was absent. GREEN: all
  eight focused source-view tests pass after canonical live-root record writing,
  strict reading, and digest revalidation.
- First root `pnpm proto:generate` correctly failed closed when its outer
  `.generated-*` package stage was accidentally included in the live authored
  inventory. Added an explicit exclusion regression and confined the exclusion
  to the transaction sibling pattern. Root `pnpm proto:generate` and
  `pnpm proto:check-generated:current` then passed.
- Focused workflow/source-view/interface-generator tests, tooling typecheck,
  format check, and diff check are being consolidated before the next pushed
  transaction checkpoint.
