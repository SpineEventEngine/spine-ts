# T-0184A Work Log

## 2026-08-14 - Start

- Created records before production edits at `aed2f194` in the isolated
  T-0184A worktree.
- Frozen scope corrects only the root source-view transaction and generated
  declaration-safe interface tuples. It does not modify unpublished To-Do work.
- The requirements-splitter is configured explicitly as `gpt-5.6-sol` / high;
  the existing implementation owner is configured explicitly as
  `gpt-5.6-terra` / medium. Desktop telemetry is unavailable, so immutable
  configured profiles are recorded as evidence.
- Next: commit and push records, then add focused failing tests before changing
  shared workflow or generator implementation.

## 2026-08-14 - RED/GREEN Checkpoints

- Pushed the preproduction records as `65cccdd6`.
- Tuple RED: two interface-generator assertions failed because companions used
  inferred `as const` arrays. GREEN: all 15 focused interface-generator tests
  pass after explicit readonly nonempty tuple emission; pushed as `ec06ddae`.
- Source-view-record RED: the new fixed-record helper was absent. GREEN: all
  eight focused source-view tests pass after canonical live-root record writing,
  strict reading, and digest revalidation.
- First root `pnpm proto:generate` correctly failed closed when its outer
  `.generated-*` package stage was accidentally included in the live authored
  inventory. Added an explicit exclusion regression and confined the exclusion
  to the transaction sibling pattern. Root `pnpm proto:generate` and
  `pnpm proto:check-generated:current` then passed.
- Focused workflow/source-view/interface-generator tests, tooling typecheck,
  format check, and diff check are being consolidated before the next pushed
  transaction checkpoint.

## 2026-08-14 - Review-Ready Checkpoint

- The strict fixed source-view record now uses short bounded names without
  changing its fixed-path contract. Its necessity dispositions are explicit in
  the canonical cleanup ledger.
- Focused record tests prove malformed JSON, version, digest, canonical-root,
  missing-record, symlink, and FIFO rejection. Mutation tests cover authored
  content/add/remove/rename and recursive configuration changes. Workflow
  coverage proves a revalidation failure leaves existing publication intact.
- Added an `isolatedDeclarations` compiler fixture covering exact readonly
  tuples for generated and authored one- and two-member companions.
- Focused source-view/interface-generator coverage: 29 tests, 95.03%
  statements, 91.06% branches, 97.77% functions, and 97.21% lines.
- Passed `pnpm typecheck:tooling`, full ESLint, cleanup, TSDoc, formatting,
  focused workflow tests, root `pnpm proto:generate`,
  `pnpm proto:check-generated:current`, and `git diff --check`.
- Ready for specialist review. `verify:release`, review, merge, tag, and
  integration remain intentionally unrun.

## 2026-08-14 - Specialist Correction Batch

- Accepted findings: the per-model validation was too early; record-currentness
  recomputed configuration after taking compiler inputs; and record bytes were
  unbounded before JSON parsing. API was clean. Style and reliability were
  configured `gpt-5.6-terra` / high; documentation was configured
  `gpt-5.6-luna` / medium. Runtime telemetry is unavailable; security remains
  deferred to T-0186.
- Validation is now a pre-journal callback after all staged model, companion,
  handler, and registry post-steps and publication safety checks. It walks each
  retained staged model handoff, so one failed validation aborts the complete
  publication transaction before any rename.
- Currentness now compares the digest captured by one `SourceInventory` pass.
  Fixed records are opened with no-follow/nonblocking flags, checked by fstat,
  and rejected above 16 KiB before read/parse. The focused oversized-record RED
  is green.
- Focused source-view/workflow tests, tooling typecheck, cleanup, full ESLint,
  TSDoc, format, and diff checks pass. Targeted re-review is next; release
  verification remains intentionally unrun.

## 2026-08-14 - Targeted Residual Ordering Proof

- Style and reliability, configured `gpt-5.6-terra` / high, accepted the P2
  residual for one ordered global-barrier regression. Documentation, configured
  `gpt-5.6-luna` / medium, accepted the overlapping claim disposition.
- The MessageBoard fixture now completes registry composition and fails Todo
  only at final source-view verification. It observes `composition` before
  `verification`, with no journal write, and proves all live generated trees,
  manifests, registry, stages, and journal stay unchanged. The ordering would
  fail if verification were restored inside `stageModel`.

## 2026-08-14 - Final Reliability Test Correction

- Reliability, configured `gpt-5.6-terra` / high, accepted P2 that the ordered
  regression did not inspect the actual registry and root Proto stage owners.
  It now asserts `.generated-*` cleanup in `examples/message-board/app` and
  `packages/proto`, alongside the existing Todo, live-artifact, and journal
  checks.

## 2026-08-14 - Release TSDoc Correction

- `verify:release` passed build, ESLint, and cleanup, then stopped at
  `lint:tsdoc`: the internal record-currentness callable and fixed-record
  fields lacked required summaries, and the bootstrap-only generation option
  lacked its preserved separator.
- Added concise internal summaries and the required Prettier-preserved blank
  line. Ran formatter, format check, TSDoc, formatter/check again, full ESLint,
  cleanup, focused source-view/workflow tests, and diff check. The release
  profile remains intentionally unrun after this correction.
