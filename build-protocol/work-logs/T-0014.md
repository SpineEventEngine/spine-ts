# Work Log: T-0014 End-User API Invariants

Task log:
`build-protocol/tasks/T-0014-end-user-api-invariants/TASK.md`
Branch: `task/T-0014-end-user-api-invariants`
Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0014-end-user-api-invariants`
Baseline commit: `cfc950c`
Authoring sub-agent: Guardrail implementation and review-fix sub-agents completed
Implementation commit: Pending branch commit
Final branch HEAD: Pending branch commit

## Purpose

Record chronological branch activity so the work can resume after interruption.

Redaction rule: record enough context for auditability, but never commit tokens,
credentials, auth headers, secret environment variables, sensitive local paths,
or sensitive payloads.

## Entries

- `2026-07-07 14:05 WEST` — Main orchestrator — Created task branch/worktree.
  Files/commands: `git worktree add` from `main` after sandbox-approved ref
  lock creation.
  Result: worktree created at baseline commit `cfc950c`.

- `2026-07-07 14:05 WEST` — Main orchestrator — Carried spec/protocol patch
  into task branch.
  Files/commands: applied `/private/tmp/spine-ts-human-invariants.patch`.
  Result: task branch contains prior human-invariant doc edits for this work.

- `2026-07-07 14:05 WEST` — Main orchestrator — Created durable task logs.
  Files/commands: task log, work log, and review log.
  Result: next step is requirements splitting before implementation.

- `2026-07-07 14:05 WEST` — Human — Added handler materialization invariant.
  Files/commands: task log and review log.
  Result: recorded that end-user application code must not discover or
  materialize decorated handler metadata; this belongs to framework/generated
  registry tooling.

- `2026-07-07 14:16 WEST` — Requirements splitter sub-agent
  `019f3cb1-01aa-7340-8dec-cd14f94b87f6` — Produced staged roadmap and found
  no blocking questions.
  Files/commands: `multi_agent_v1.wait_agent`, then `close_agent`.
  Result: first implementable task is guardrail/red-test baseline; sub-agent
  closed after completion.

- `2026-07-07 14:15 WEST` — Guardrail implementation sub-agent — Extended the
  cleanup checker for end-user example invariants.
  Files/commands: updated `scripts/check-cleanup-rules.mjs` and
  `scripts/check-cleanup-rules.test.mjs`; ran
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`.
  Result: focused checker tests passed, including new rejection coverage for
  `@Apply`, schema-bearing decorators, transaction control, envelope packing,
  internal event IDs, metadata materialization, framework envelope return
  types, and `requireTaskId(command.id)`.

- `2026-07-07 14:15 WEST` — Guardrail implementation sub-agent — Captured
  intentional RED evidence from the repo-level cleanup guard.
  Files/commands: `node scripts/check-cleanup-rules.mjs`.
  Result: failed intentionally on `examples/todo/src/index.ts`; output lists
  the known remaining end-user API violations that later T-0014 steps must
  remove.

- `2026-07-07 14:24 WEST` — Main orchestrator — Closed guardrail
  implementation sub-agent `019f3cb5-848b-7f21-b724-5510afb6812c`.
  Files/commands: `multi_agent_v1.close_agent`.
  Result: previous status was `DONE_WITH_CONCERNS`; no guardrail sub-agent
  remains open.

- `2026-07-07 14:24 WEST` — Main orchestrator — Re-ran guardrail
  verification.
  Files/commands: `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`
  and `node scripts/check-cleanup-rules.mjs`.
  Result: guard tests passed 10/10; cleanup guard failed intentionally on
  `examples/todo/src/index.ts` and listed the known forbidden patterns.

- `2026-07-07 14:24 WEST` — Main orchestrator — Completed current Spine JVM
  source inspection for this implementation.
  Files/commands: fetched/read `Assign.java`, `Apply.java`, `React.java`,
  `CommandRouting.kt`, `DefaultCommandRoute.kt`, `ByFirstField.kt`,
  `EventEmitter.java`, `EventFactory.java`, `AggregateCommandEndpoint.java`,
  and `AggregateEndpoint.java` under `/private/tmp`.
  Result: confirmed bare handler annotations, default first-field command
  routing, framework-owned event envelope creation, and ADR 0001's
  post-cutover no-applier aggregate model.

- `2026-07-07 14:28 WEST` — Review-fix sub-agent
  `019f3cbf-1d9b-7410-9d98-08897cb5ba7e` — Addressed round-1 guardrail review
  findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`, guardrail report;
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`;
  `node scripts/check-cleanup-rules.mjs`.
  Result: focused tests passed 15/15; cleanup guard still fails intentionally
  only on the current to-do example's forbidden public API patterns. Sub-agent
  closed after completion. Main orchestrator reflowed one synthetic import
  fixture line and re-ran the same verification with the same result.

- `2026-07-07 14:36 WEST` — Main orchestrator — Addressed round-2 guardrail
  findings locally.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: focused tests passed 19/19; cleanup guard still fails intentionally
  only on the current to-do example's forbidden public API patterns.

- `2026-07-07 14:42 WEST` — Main orchestrator — Addressed round-3 guardrail
  findings locally.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: focused tests passed 19/19; cleanup guard still fails intentionally
  only on the current to-do example's forbidden public API patterns.

- `2026-07-07 14:53 WEST` — Main orchestrator — Addressed round-4 documentation
  review finding.
  Files/commands: `build-protocol/tasks/T-0014-end-user-api-invariants/TASK.md`.
  Result: task header now records that guardrail rounds 1-3 are complete and
  round 4 re-review is in progress.

- `2026-07-07 15:00 WEST` — Main orchestrator — Addressed round-4 security
  review finding.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`.
  Result: forbidden handler return-type diagnostics now use a bounded resolved
  type label instead of raw source text, with regression coverage preventing
  comments or generic payload text from being echoed to CI logs.

- `2026-07-07 15:08 WEST` — Main orchestrator — Addressed round-4
  TypeScript/API, reliability, and JVM-alignment guardrail findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: focused tests passed 25/25. The guard now recurses through forbidden
  framework envelope return containers, catches aliased framework-owned helper
  imports, scans nested `examples/**/src`, rejects destructured command target
  parameters, and ignores nested callbacks that rebind the command parameter.
  The task and review headers now advance the guardrail to round-5 re-review.
  Repo cleanup remains intentionally RED only because the current to-do example
  still contains the public API violations this task is about to migrate.

- `2026-07-07 15:15 WEST` — Main orchestrator — Addressed round-5 documentation
  review finding.
  Files/commands: `build-protocol/tasks/T-0014-end-user-api-invariants/TASK.md`,
  `build-protocol/tasks/T-0014-end-user-api-invariants/guardrail-task-report.md`.
  Result: task-local work log and guardrail task report now record the round-4
  fixes, 25/25 focused verification, intentionally red example cleanup signal,
  and round-5 re-review state.

- `2026-07-07 15:25 WEST` — Main orchestrator — Addressed round-5 security,
  TypeScript/API, reliability, and JVM-alignment findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`, `build-protocol/DEVELOPER_API.md`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: focused tests passed 32/32. Recursive type scanning now has an
  explicit work budget and computes the forbidden return label once; type-only
  server imports no longer create decorator provenance; `.cts` import-equals
  framework namespaces are recognized; `Proto.*` return checks are narrowed to
  `Event` and `Command`; computed command target extraction is covered; and the
  cleanup heuristic no longer treats `@Command` event-to-command handlers as
  incoming command assignees. `DEVELOPER_API.md` now reflects ADR 0001 aggregate
  transaction semantics instead of event-sourced appliers.

- `2026-07-07 15:40 WEST` — Main orchestrator — Addressed round-6 guardrail
  review findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: focused tests passed 36/36. The example guardrail collector is split
  into smaller helpers; command target checks now reject validation-helper calls
  rather than ordinary ID reads; type-only proto import-equals aliases,
  import-equals member aliases, and wrapped validation arguments are covered.
  The to-do example remains intentionally red and is routed to the
  framework/example migration slice.

- `2026-07-07 15:48 WEST` — Main orchestrator — Addressed round-7 documentation
  review finding.
  Files/commands:
  `build-protocol/tasks/T-0014-end-user-api-invariants/guardrail-task-report.md`.
  Result: added a final current-state block after historical report sections so
  the latest 36/36 verification, intentional to-do example red state, and
  round-7 re-review state are visually final.

- `2026-07-07 15:56 WEST` — Main orchestrator — Addressed round-7
  TypeScript/API review finding.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: type-only import-equals member aliases such as
  `import type LegacyEvent = Proto.Event` are now tracked as forbidden framework
  envelope type aliases. Focused tests passed 37/37; repo cleanup remains
  intentionally red on the current to-do example migration input.

- `2026-07-07 16:02 WEST` — Main orchestrator — Addressed round-7 reliability
  review findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: import-equals member aliases are now resolved after namespace
  collection, so declaration order no longer matters; type-only proto member
  aliases still contribute type provenance; validation-helper calls through
  simple local aliases of command `id`/`target` fields are rejected. Focused
  tests passed 39/39; repo cleanup remains intentionally red on the current
  to-do example migration input.

- `2026-07-07 16:15 WEST` — Main orchestrator — Addressed round-8 style,
  TypeScript/API, security, reliability, and JVM-alignment findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: focused tests passed 43/43. Import provenance is split into smaller
  helpers; mixed type-only `@spine-ts/core` imports no longer look like value API
  use; import-equals namespace aliases are resolved with a fixed-point pass;
  explicit return type rules for `@Assign`, `@Command`, `@React`, and
  `@Subscribe` are enforced; command target validation aliases honor simple
  block shadowing. Repo cleanup remains intentionally red on the current to-do
  example migration input.

- `2026-07-07 16:36 WEST` — Main orchestrator — Addressed round-9 TypeScript/API,
  security, reliability, and human-clarification findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`, `build-protocol/BUILD_PROTOCOL.md`,
  `build-protocol/TECHNICAL_SPEC.md`, `build-protocol/DEVELOPER_API.md`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`.
  Result: focused tests passed 49/49. The guard now rejects import-type framework
  envelope returns, chained import-equals envelope aliases, chained command
  target validation aliases, and local example declarations of
  `materializeDecoratedEntityHandlers`; it also respects inner-scope decorator
  shadowing and nested callback target-alias shadowing. Repo cleanup remains
  intentionally red on the current to-do example migration input.

- `2026-07-07 16:50 WEST` — Main orchestrator — Addressed round-10 style,
  TypeScript/API, security, reliability, and JVM-alignment findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`, `build-protocol/TECHNICAL_SPEC.md`,
  `build-protocol/DEVELOPER_API.md`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`, `git diff --check`.
  Result: focused tests passed 54/54. The guard rejects local decorator value
  aliases, block-local envelope aliases, invalid explicit return annotations,
  non-domain return types, and recursive return aliases; it respects parameter
  and binding-pattern shadowing. Repo cleanup remains intentionally red on the
  current to-do example migration input.

- `2026-07-07 17:00 WEST` — Main orchestrator — Addressed round-11 TypeScript/API,
  security, reliability, and JVM-alignment findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`, `git diff --check`.
  Result: focused tests passed 60/60. Return-type acceptance now requires
  generated import provenance or aliases to generated imports; rest-only tuples
  are rejected; local namespace and object aliases of framework APIs are tracked;
  destructured core/proto/server helpers are covered; and nested blocks can
  shadow the command parameter name without false positives.

- `2026-07-07 17:12 WEST` — Main orchestrator — Addressed round-12 documentation,
  TypeScript/API, security, reliability, and JVM-alignment findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`, `build-protocol/BUILD_PROTOCOL.md`,
  `build-protocol/DECISION_LOG.md`, `build-protocol/TODO_EXAMPLE_SPEC.md`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`, `git diff --check`.
  Result: focused tests passed 68/68. The guard accepts generated namespace/value
  imports, respects local type alias shadowing, tracks object-destructured
  decorator aliases, rejects object-wrapped command target validation aliases,
  checks command-transforming `@Command` handlers, accepts labeled rest tuple
  returns, and restores target alias shadowing by lexical block scope.

- `2026-07-07 16:24 WEST` — Main orchestrator — Addressed round-13 documentation,
  TypeScript/API, security, reliability, and JVM-alignment findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`, `build-protocol/BUILD_PROTOCOL.md`,
  `build-protocol/DECISION_LOG.md`, `build-protocol/TECHNICAL_SPEC.md`,
  `build-protocol/TODO_EXAMPLE_SPEC.md`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`, `git diff --check`.
  Result: focused tests passed 76/76. The guard classifies generated handler
  returns by command/event family, detects generated command inputs for
  `@Command` route guardrails through namespaced and aliased types, propagates
  framework helper aliases through property and object aliases, tracks command
  target validation through command object aliases and object destructuring, and
  confines authored-code symlinks before reading tracked files. Repo cleanup
  remains intentionally red on the current to-do example migration input,
  including the example-owned `materializeDecoratedEntityHandlers` helper.

- `2026-07-07 16:40 WEST` — Main orchestrator — Addressed round-14 style,
  documentation, TypeScript/API, security, reliability, and JVM-alignment
  findings.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`, `build-protocol/BUILD_PROTOCOL.md`,
  `build-protocol/DECISION_LOG.md`, `build-protocol/RUNTIME_ARCHITECTURE.md`,
  `build-protocol/TECHNICAL_SPEC.md`, `build-protocol/TODO_EXAMPLE_SPEC.md`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`, `git diff --check`.
  Result: focused tests passed 82/82. The guard avoids recursive package `src`
  traversal, propagates dotted and destructured namespace aliases, handles
  command object paths, destructured command parameters, computed target
  destructuring, and conflicting import-equals aliases, and removes stale
  event-sourced aggregate replay wording. Repo cleanup still fails on the
  checked-in to-do example; this is now routed to the example/framework
  migration work rather than treated as a completed guardrail exception.

- `2026-07-07 17:06 WEST` — Main orchestrator — Migrated the framework and to-do
  example to the end-user API invariants.
  Files/commands: `packages/server/src/handler/handler-decorators.ts`,
  `packages/server/src/entity/entity.ts`, `packages/server/src/repository/repository.ts`,
  `examples/todo/src/index.ts`, `examples/todo/src/index.test.ts`,
  `scripts/check-cleanup-rules.mjs`, `scripts/check-cleanup-rules.test.mjs`,
  `packages/server/test/handler/handler-decorators.test.ts`,
  `packages/server/test/repository/repository-routing.test.ts`,
  `build-protocol/BUILD_PROTOCOL.md`, `build-protocol/DECISION_LOG.md`,
  `build-protocol/DEVELOPER_API.md`, `build-protocol/RUNTIME_ARCHITECTURE.md`,
  `build-protocol/TECHNICAL_SPEC.md`, `build-protocol/TODO_EXAMPLE_SPEC.md`,
  `node scripts/check-cleanup-rules.mjs`, `corepack pnpm lint`,
  `corepack pnpm format:check`, `corepack pnpm typecheck:build`,
  `corepack pnpm docs:check`, `corepack pnpm test`, `corepack pnpm test:coverage`.
  Result: the cleanup guard passes on the repository; full tests pass; coverage
  passes at 95.11% statements and 90.31% branches. The to-do example no longer
  uses framework `Event` returns, schema-bearing decorators, aggregate `@Apply`,
  transaction calls, internal event ID construction, default-route ID validation
  helpers, or local handler materialization.

- `2026-07-07 17:06 WEST` — Documentation reviewer — Found stale example README
  wording claiming the to-do example demonstrates event appliers.
  Files/commands: `examples/todo/README.md`.
  Result: changed the README to describe aggregate command handlers that update
  state and return domain events.

- `2026-07-07 17:25 WEST` — Main orchestrator — Addressed round-15 reviewer
  findings from documentation, TypeScript/API, security, reliability,
  style/maintainability, and JVM-alignment lanes.
  Files/commands: `packages/server/src/repository/repository.ts`,
  `packages/server/src/handler/handler-decorators.ts`, `examples/todo/src/index.ts`,
  `packages/server/test/repository/repository-routing.test.ts`,
  `packages/server/test/services/spine-services.test.ts`,
  `packages/testing/test/index.test.ts`, `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`, `packages/server/README.md`,
  `docs/USER_GUIDE.md`, `docs/api/README.md`, `examples/todo/README.md`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs
packages/server/test/repository/repository-routing.test.ts examples/todo/src/index.test.ts
packages/server/test/handler/handler-decorators.test.ts`,
  `corepack pnpm vitest run packages/testing/test/index.test.ts
packages/server/test/services/spine-services.test.ts`,
  `corepack pnpm test:coverage`, `corepack pnpm lint`,
  `corepack pnpm format:check`, `corepack pnpm docs:check`, `git diff --check`.
  Result: fixed framework `Event` envelope bypass in the managed aggregate path,
  separated actual applier schemas from event subscriber/reactor schemas, rejected
  empty managed aggregate command output, removed the projection
  `method.toString()` transaction heuristic, prevented managed-path dispatch when
  snapshot writes fail, changed the to-do aggregate to use framework-routed
  `this.id`, hardened cleanup checker path parsing/diagnostics with
  `git ls-files -z`, split the target-validation collector, and updated public
  docs away from schema-bearing decorators, `@Apply`, and app-owned materializers.
  Verification passed: focused tests 164/164; service/testing focused tests
  50/50; full coverage 733/733 with 95.12% statements and 90.46% branches;
  lint, format, cleanup guard, docs, and diff whitespace checks passed.

- `2026-07-07 17:33 WEST` — Main orchestrator — Addressed round-16 reviewer
  follow-ups before re-verification.
  Files/commands: `packages/server/src/repository/aggregate-storage.ts`,
  `packages/server/test/repository/repository-routing.test.ts`,
  `scripts/check-cleanup-rules.mjs`, `scripts/check-cleanup-rules.test.mjs`,
  `docs/USER_GUIDE.md`, `packages/server/README.md`.
  Result: added managed aggregate snapshot/event write prevalidation under the
  aggregate lock via `writeSnapshotWithEvents`, covered duplicate managed event
  IDs before snapshot mutation, escaped Unicode format and bidi controls in
  cleanup diagnostics, removed stale app-facing applier/materializer wording
  from public docs, and kept legacy application references scoped to
  compatibility APIs.

- `2026-07-07 17:39 WEST` — Main orchestrator — Re-verified the round-16
  follow-up fixes before reviewer handoff.
  Files/commands: `corepack pnpm format:check`, `corepack pnpm lint`,
  `corepack pnpm docs:check`, `node scripts/check-cleanup-rules.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs
packages/server/test/repository/repository-routing.test.ts examples/todo/src/index.test.ts
packages/server/test/handler/handler-decorators.test.ts
packages/server/test/services/spine-services.test.ts packages/testing/test/index.test.ts`,
  `corepack pnpm test:coverage`, `git diff --check`.
  Result: format, lint/typecheck, docs, cleanup guard, and diff whitespace
  checks passed. The focused test suite passed 215/215 when run outside the
  sandbox after the sandbox run failed on `listen EPERM 127.0.0.1`. Full
  coverage passed 734/734 with 95.14% statements and 90.47% branches.

- `2026-07-07 17:44 WEST` — JVM-alignment reviewer / Main orchestrator —
  Addressed round-17 findings.
  Files/commands: `packages/server/src/repository/repository.ts`,
  `packages/server/test/repository/repository-routing.test.ts`,
  `scripts/check-cleanup-rules.mjs`, `scripts/check-cleanup-rules.test.mjs`.
  Result: default routing rejects blank primitive route IDs before handler
  invocation, while generated message IDs follow schema validation. The cleanup
  guard also rejects end-user
  `rollbackTransaction()` calls alongside `startTransaction()` and
  `commitTransaction()`.

- `2026-07-07 17:45 WEST` — Main orchestrator — Re-verified the round-17
  JVM-alignment fixes.
  Files/commands: `corepack pnpm format:check`, `corepack pnpm lint`,
  `corepack pnpm docs:check`, `node scripts/check-cleanup-rules.mjs`,
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs
packages/server/test/repository/repository-routing.test.ts`,
  `corepack pnpm test:coverage`, `git diff --check`.
  Result: format, lint/typecheck, docs, cleanup guard, focused guard/routing
  tests, coverage, and diff whitespace checks passed. Focused guard/routing
  tests passed 141/141. Full coverage passed 735/735 with 95.18% statements and
  90.55% branches.

- `2026-07-07 17:47 WEST` — Documentation reviewer / Main orchestrator —
  Addressed round-18 documentation findings.
  Files/commands: `packages/server/README.md`, `docs/api/README.md`.
  Result: removed stale public-doc wording that described state-transition
  failures as happening while applying produced events. The docs now describe
  these refusals as failures from the framework-owned aggregate command
  transaction.

- `2026-07-07 17:48 WEST` — Main orchestrator — Re-verified the round-18
  documentation fixes.
  Files/commands: `corepack pnpm docs:check`, `corepack pnpm format:check`,
  `git diff --check`.
  Result: docs, formatting, and diff whitespace checks passed. A stale-pattern
  scan found only the intentional `materializeDecoratedEntityHandlers()`
  compatibility paragraph in `docs/api/README.md`.

- `2026-07-07 17:53 WEST` — Reliability reviewer / Main orchestrator —
  Addressed round-19 managed persistence consistency finding.
  Files/commands: `packages/storage/src/event/event-store.ts`,
  `packages/server/src/repository/aggregate-storage.ts`,
  `packages/server/test/repository/repository-routing.test.ts`,
  `packages/storage/test/event/event-store.test.ts`.
  Result: `EventStore` can delete stored events by ID for rollback, and managed
  aggregate persistence now appends internal events before writing the managed
  snapshot. If the snapshot write fails after append, the framework deletes the
  just-appended event IDs before surfacing the snapshot failure. A regression
  test covers event append storage failure after managed-batch prevalidation and
  verifies no managed snapshot is written. Direct event-store tests cover
  rollback tokens with cloned event IDs, empty input, and closed storage.

- `2026-07-07 17:56 WEST` — Main orchestrator — Addressed coverage-only
  verification timeout in the handler decorator semantic compiler fixture.
  Files/commands: `packages/server/test/handler/handler-decorators.test.ts`.
  Result: the TypeScript compiler fixture test now has a focused 15-second
  timeout so coverage instrumentation does not fail a valid semantic compile
  check at Vitest's default 5-second limit.

- `2026-07-07 17:58 WEST` — Main orchestrator — Re-verified the round-19
  reliability and verification-stability fixes.
  Files/commands: `corepack pnpm format:check`, `corepack pnpm lint`,
  `corepack pnpm docs:check`,
  `corepack pnpm vitest run packages/server/test/repository/repository-routing.test.ts
packages/storage/test/event/event-store.test.ts`,
  `corepack pnpm vitest run packages/server/test/handler/handler-decorators.test.ts`,
  `corepack pnpm test:coverage`, `git diff --check`.
  Result: format, lint/typecheck, docs, focused repository/storage tests,
  focused decorator tests, full coverage, and diff whitespace checks passed.
  Repository/storage focused tests passed 70/70; decorator focused tests passed
  10/10; full coverage passed 738/738 with 95.32% statements and 90.57%
  branches.

- `2026-07-07 18:02 WEST` — Reliability reviewer / Main orchestrator —
  Addressed round-20 rollback deletion finding.
  Files/commands: `packages/storage/src/event/event-store.ts`,
  `packages/server/src/repository/aggregate-storage.ts`,
  `packages/server/test/repository/repository-routing.test.ts`.
  Result: event rollback now deletes sequentially under the existing event-store
  lock. Managed aggregate rollback preserves both the original snapshot failure
  and rollback deletion failure in an `AggregateError`. A multi-event managed
  aggregate regression test covers snapshot failure followed by delayed failure
  deleting the second appended event, leaving the remaining same-dispatch event
  visible for recovery diagnostics.

- `2026-07-07 18:05 WEST` — Main orchestrator — Re-verified the round-20
  rollback deletion fixes.
  Files/commands: `corepack pnpm format:check`, `corepack pnpm lint`,
  `corepack pnpm docs:check`, `node scripts/check-cleanup-rules.mjs`,
  `corepack pnpm vitest run packages/server/test/repository/repository-routing.test.ts
packages/storage/test/event/event-store.test.ts`,
  `corepack pnpm test:coverage`, `git diff --check`.
  Result: format, lint/typecheck, docs, cleanup guard, focused
  repository/storage tests, full coverage, and diff whitespace checks passed.
  Focused repository/storage tests passed 71/71; full coverage passed 739/739
  with 95.32% statements and 90.57% branches.

- `2026-07-07 18:10 WEST` — JVM-alignment reviewer / Main orchestrator —
  Addressed round-21 ADR 0001 versioning and wording findings.
  Files/commands: `packages/server/src/repository/repository.ts`,
  `packages/server/src/repository/aggregate-storage.ts`,
  `packages/server/test/repository/repository-routing.test.ts`,
  `docs/USER_GUIDE.md`, `docs/api/README.md`.
  Result: managed no-applier aggregates now advance by one dispatch version per
  command, and all same-dispatch events carry that shared dispatch version while
  retaining unique internal event IDs. Legacy applier-backed aggregate execution
  keeps sequential per-event versions. Managed aggregate storage validates the
  same-dispatch event version against the managed snapshot version. Public docs
  no longer say aggregate commands apply produced events in the new path.
  Aggregate history reads now validate only the unsnapshotted event tail so
  same-version managed events represented by a snapshot do not make the history
  unreadable; no-snapshot histories still start validation at version 1. Legacy
  `appendEvents()` still validates already-stored event history as strictly
  consecutive before accepting new events.

- `2026-07-07 18:15 WEST` — Main orchestrator — Re-verified the round-21
  ADR 0001 versioning fixes.
  Files/commands: `corepack pnpm vitest run packages/server/test/repository/aggregate-storage.test.ts
packages/server/test/repository/repository-routing.test.ts`,
  `corepack pnpm format:check`, `corepack pnpm lint`,
  `corepack pnpm docs:check`, `node scripts/check-cleanup-rules.mjs`,
  `corepack pnpm test:coverage`, `git diff --check`.
  Result: focused aggregate-storage/repository tests, format, lint/typecheck,
  docs, cleanup guard, full coverage, and diff whitespace checks passed.
  Focused aggregate-storage/repository tests passed 98/98. Full coverage passed
  740/740 with 95.28% statements and 90.51% branches.

- `2026-07-07 18:18 WEST` — Documentation reviewer / Main orchestrator —
  Addressed round-22 architecture documentation findings.
  Files/commands: `docs/architecture/README.md`.
  Result: architecture docs now scope schema-bearing decorators, `@Apply`, and
  `materializeDecoratedEntityHandlers()` as legacy/framework compatibility,
  describe aggregate repository execution as framework-owned command
  transactions that pack/store returned domain events, and describe
  state-transition failures as command transaction rejections rather than
  applying produced events.

- `2026-07-07 18:19 WEST` — Main orchestrator — Re-verified the round-22
  architecture documentation fixes.
  Files/commands: `corepack pnpm format:check`, `corepack pnpm docs:check`,
  stale-pattern `rg` scan over public docs and the to-do example,
  `git diff --check`.
  Result: format, docs, stale-pattern scan, and diff whitespace checks passed.
  The remaining `materializeDecoratedEntityHandlers()` hits are compatibility
  paragraphs in architecture/API docs.

- `2026-07-07 18:26 WEST` — Security reviewer / Main orchestrator —
  Addressed round-23 security findings.
  Files/commands: `packages/storage/src/event/event-store.ts`,
  `packages/storage/test/event/event-store.test.ts`,
  `packages/server/src/repository/aggregate-storage.ts`,
  `scripts/check-cleanup-rules.mjs`, `scripts/check-cleanup-rules.test.mjs`.
  Result: arbitrary public event deletion was replaced by
  `appendAllWithRollback()`, which returns a rollback token scoped to the
  append-created event IDs. Managed aggregate snapshot failure uses that token
  for compensation. Cleanup diagnostics now use Unicode code points, escape
  unsafe code points with brace escapes, and cover astral tag and variation
  selector format controls.

- `2026-07-07 18:28 WEST` — Main orchestrator — Re-verified the round-23
  security fixes.
  Files/commands: `corepack pnpm typecheck:build`,
  `corepack pnpm vitest run packages/storage/test/event/event-store.test.ts
scripts/check-cleanup-rules.test.mjs packages/server/test/repository/repository-routing.test.ts`,
  `corepack pnpm format:check`, `corepack pnpm lint`,
  `corepack pnpm docs:check`, `node scripts/check-cleanup-rules.mjs`,
  `git diff --check`, `corepack pnpm test:coverage`.
  Result: no public `deleteAll()` references remain, focused
  storage/guard/repository tests passed 155/155, format, lint/typecheck, docs,
  cleanup guard, and diff whitespace checks passed, and full coverage passed
  740/740 with 95.31% statements and 90.56% branches.

- `2026-07-07 18:30 WEST` — Documentation reviewer / Main orchestrator —
  Addressed round-24 review metadata findings.
  Files/commands:
  `build-protocol/tasks/T-0014-end-user-api-invariants/TASK.md`,
  `build-protocol/reviews/T-0014-end-user-api-invariants.md`.
  Result: task and review headers now report round 24 re-review in progress
  after security verification instead of stale guardrail round-15 status.

- `2026-07-07 18:33 WEST` — Security/JVM-alignment reviewer / Main
  orchestrator — Addressed round-25 rollback token reuse finding.
  Files/commands: `packages/storage/src/event/event-store.ts`,
  `packages/storage/test/event/event-store.test.ts`.
  Result: rollback tokens returned by `appendAllWithRollback()` are now
  one-shot. Reusing a token rejects before deletion, so a stale token cannot
  delete a later event with the same ID. Event-store tests cover the stale-token
  reuse case.

- `2026-07-07 18:34 WEST` — Main orchestrator — Re-verified the round-25
  one-shot rollback token fix.
  Files/commands: `corepack pnpm typecheck:build`,
  `corepack pnpm vitest run packages/storage/test/event/event-store.test.ts`,
  `corepack pnpm vitest run packages/server/test/repository/repository-routing.test.ts`,
  `corepack pnpm format:check`, `corepack pnpm lint`,
  `corepack pnpm docs:check`, `node scripts/check-cleanup-rules.mjs`,
  `git diff --check`, `corepack pnpm test:coverage`.
  Result: typecheck/build, focused event-store tests, focused repository tests,
  format, lint/typecheck, docs, cleanup guard, diff whitespace, and full
  coverage passed. Event-store focused tests passed 11/11; repository focused
  tests passed 61/61; full coverage passed 740/740 with 95.31% statements and
  90.57% branches.

- `2026-07-07 18:38 WEST` — Documentation reviewer / Main orchestrator —
  Addressed round-26 review metadata findings.
  Files/commands:
  `build-protocol/tasks/T-0014-end-user-api-invariants/TASK.md`,
  `build-protocol/reviews/T-0014-end-user-api-invariants.md`.
  Result: task and review headers now use current re-review wording after round
  25 verification instead of hard-coding a stale active round number.

- `2026-07-07 18:42 WEST` — Reliability reviewer / Main orchestrator —
  Addressed round-27 managed orphan-tail finding.
  Files/commands: `packages/server/src/repository/repository.ts`,
  `packages/server/test/repository/repository-routing.test.ts`.
  Result: no-applier managed repositories now reject loaded history with
  unsnapshotted event tails instead of advancing from events they cannot replay.
  The rollback-failure regression now proves a later command against the
  orphaned tail is rejected as an explicit repair state.

- `2026-07-07 18:43 WEST` — Main orchestrator — Re-verified the round-27
  managed orphan-tail fix.
  Files/commands: `corepack pnpm typecheck:build`,
  `corepack pnpm vitest run packages/server/test/repository/repository-routing.test.ts`,
  `corepack pnpm format:check`, `corepack pnpm lint`,
  `corepack pnpm docs:check`, `node scripts/check-cleanup-rules.mjs`,
  `corepack pnpm test:coverage`, `git diff --check`.
  Result: typecheck/build, focused repository tests, format,
  lint/typecheck, docs, cleanup guard, full coverage, and diff whitespace
  checks passed. Repository focused tests passed 61/61; full coverage passed
  740/740 with 95.31% statements, 90.59% branches, 97.92% functions, and
  95.33% lines.

- `2026-07-07 18:44 WEST` — Reliability reviewer / Main orchestrator —
  Addressed round-28 cleanup guard scalability finding.
  Files/commands: `scripts/check-cleanup-rules.mjs`,
  `scripts/check-cleanup-rules.test.mjs`.
  Result: `git ls-files -z` and other guard Git subprocesses now use an
  explicit 64 MiB output buffer, and the guard test suite covers a synthetic
  tracked-file list larger than Node's default synchronous child-process
  buffer.

- `2026-07-07 18:45 WEST` — Main orchestrator — Adjusted round-28 guard
  scalability regression fixture after first verification attempt.
  Files/commands: `scripts/check-cleanup-rules.test.mjs`.
  Result: the first focused guard run failed before invoking the cleanup guard
  because the fixture `git commit` printed 25,000 create lines and overflowed
  the test helper's own child-process buffer. The fixture commit is now quiet so
  the test exercises the intended `git ls-files -z` guard path.

- `2026-07-07 18:50 WEST` — Main orchestrator — Re-verified the round-28
  cleanup guard scalability fix.
  Files/commands:
  `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs`,
  `node scripts/check-cleanup-rules.mjs`, `corepack pnpm format:check`,
  `git diff --check`, `corepack pnpm lint`, `corepack pnpm docs:check`,
  `corepack pnpm test:coverage`.
  Result: focused cleanup guard tests passed 84/84, cleanup guard, formatting,
  diff whitespace, lint/typecheck, docs, and full coverage passed. Full coverage
  passed 741/741 with 95.31% statements, 90.59% branches, 97.92% functions, and
  95.33% lines.

- `2026-07-07 18:51 WEST` — TypeScript/API reviewer / Main orchestrator —
  Addressed round-28 public API findings.
  Files/commands: `packages/storage/src/index.ts`,
  `packages/server/src/handler/handler-decorators.ts`, `docs/api/README.md`.
  Result: the public `EventRollback` type is exported from the storage package
  root, and public API docs/JSDoc now mark `@Apply` and
  `materializeDecoratedEntityHandlers()` as legacy/framework compatibility
  surfaces rather than ordinary application APIs.

- `2026-07-07 18:52 WEST` — Main orchestrator — Adjusted TypeScript/API docs
  manifest after first verification attempt.
  Files/commands: `scripts/check-api-docs.mjs`.
  Result: `corepack pnpm docs:check` correctly failed because the new
  `EventRollback` root export was not listed in the expected API export
  manifest. The manifest now includes `EventRollback`.

- `2026-07-07 18:53 WEST` — Main orchestrator — Adjusted `@Apply`
  compatibility docs after first lint attempt.
  Files/commands: `packages/server/src/handler/handler-decorators.ts`.
  Result: formal `@deprecated` JSDoc caused `no-deprecated` diagnostics in
  framework compatibility exports/tests. The public warning text remains, but
  the formal deprecation tag was removed so compatibility code builds cleanly.

- `2026-07-07 18:55 WEST` — Main orchestrator — Re-verified the round-28
  TypeScript/API public-surface fix.
  Files/commands: `corepack pnpm format:check`, `git diff --check`,
  `corepack pnpm lint`, `corepack pnpm docs:check`,
  `corepack pnpm test:coverage`.
  Result: formatting, diff whitespace, lint/typecheck, docs/API export
  manifest, and full coverage passed. Full coverage passed 741/741 with 95.31%
  statements, 90.59% branches, 97.92% functions, and 95.33% lines.

- `2026-07-07 18:56 WEST` — Reviewer sub-agents / Main orchestrator —
  Completed round-28 re-review.
  Files/commands: `multi_agent_v1.wait_agent`.
  Result: style/maintainability, documentation, TypeScript/API, security,
  performance/reliability, and JVM/ADR alignment reviewers reported `CLEAN`.

- `2026-07-07 18:57 WEST` — Main orchestrator — Closed participating round-28
  reviewer sub-agents.
  Files/commands: `multi_agent_v1.close_agent`.
  Result: closed style/maintainability
  `019f3d56-50ca-72e1-b111-acd828a9a0b1`, documentation
  `019f3d56-5146-7d90-af40-8f3ced3e33e0`, TypeScript/API
  `019f3d56-51b2-7d20-b477-a65ec48f69aa`, security
  `019f3d56-5237-7af2-90ff-b244c5f8911a`, performance/reliability
  `019f3d56-52b4-7121-ad34-4687a1939017`, and JVM/ADR alignment
  `019f3d56-5344-7e41-bc72-7aba3ce27d30`; each previous status was `CLEAN`.

- `2026-07-07 18:57 WEST` — Main orchestrator — Marked T-0014 complete.
  Files/commands: `build-protocol/tasks/T-0014-end-user-api-invariants/TASK.md`,
  `build-protocol/work-logs/T-0014.md`,
  `build-protocol/reviews/T-0014-end-user-api-invariants.md`.
  Result: completion gates are satisfied: implementation goal achieved,
  verification passed, docs/API docs updated, reviewer rounds clean, and
  participating sub-agents closed.

- `2026-07-07 18:58 WEST` — Main orchestrator — Committed the reviewed
  implementation.
  Files/commands: `git add`, `git commit -m "Implement end-user API invariants"`.
  Result: feature commit `0066de5` records the reviewed T-0014 implementation.
  This log-maintenance update records that now-known commit and cannot name its
  own future hash.

- `2026-07-07 19:02 WEST` — Main orchestrator — Integrated T-0014 into `main`.
  Files/commands: `git restore -- build-protocol/BUILD_PROTOCOL.md
build-protocol/DECISION_LOG.md build-protocol/DEVELOPER_API.md
build-protocol/RUNTIME_ARCHITECTURE.md build-protocol/TECHNICAL_SPEC.md
build-protocol/TODO_EXAMPLE_SPEC.md`,
  `git merge --ff-only task/T-0014-end-user-api-invariants`.
  Result: restored stale autonomous root spec-file edits that were superseded by
  the reviewed task branch, left untracked `human-review-1-jul.md` untouched,
  and fast-forwarded `main` from `cfc950c` to `7c7b27f`.

## Current State

- Last completed step: T-0014 integrated into `main`.
- Next step: select the next non-blocked build-protocol task.
- Known risks: generated registry/tooling inference for bare decorators is still
  a later task; this slice preserves explicit `defineEntityHandlers()` metadata
  for the migrated example while forbidding end-user-owned materialization.
- Open questions: none blocking.

## Open Risks And Follow-Up Routing

- Risk/follow-up: existing examples and docs may still encode old event-sourced
  aggregate patterns.
- Owner: main orchestrator.
- Linked task/decision: `D-0057`, ADR 0001.
- Disposition: accepted.
- Next review point: requirements split and review prompts.
