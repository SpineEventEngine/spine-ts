# T-0049 Work Log

Status: Implementation verified; canonical review pending

## 2026-07-21 — Milestone framing

- Start: `2026-07-21T15:23:50+01:00`.
- Baseline/main: `f421b7e3c4f0cca9b72b0a5db7352ccc019e1d06`.
- Branch/worktree: `task/T-0049-user-guide-datastore` at
  `.worktrees/T-0049-user-guide-datastore`.
- The human requires a complete source-accuracy review of prose and inline code
  plus a separate comprehensive Datastore development/configuration guide with
  code snippets.
- Root contained only preserved unrelated untracked files. The isolated
  worktree starts from tracked `main`; the protected human review file is not a
  task input.
- The milestone does not trigger requirements splitting: it changes no public
  or serialized contract, domain semantics, transaction policy, or subsystem
  architecture. The current Datastore contract is already accepted by T-0046.
- The selected Desktop surface supports explicit role/model/reasoning dispatch.
  The authoring assignment will use the existing `implementer`, expected and
  explicitly dispatched as `gpt-5.6-terra` / `medium`.
- Skill applicability and the full human ledger are recorded in the task brief
  before guide edits. `doc-coauthoring` governs the source audit, drafting, and
  fresh-reader test; its interactive questions are unnecessary because the
  human supplied the target document, audience, desired accuracy, and required
  Datastore content. Project protocol supplies the format and acceptance gates.
- No third-party library search is applicable: this documentation task adds no
  infrastructure or dependency.

## Acceptance evidence

- Clean-worktree baseline `pnpm --config.verify-deps-before-run=false verify`
  passed before authoring: ordinary and coverage executions each passed 80
  files / 1,855 tests with 2 files / 8 Datastore cloud/emulator cases skipped
  without their opt-in environment. Coverage was 94.41% statements, 90.09%
  branches, 94.68% functions, and 94.85% lines. Typecheck, lint/cleanup,
  formatting, TypeDoc/API, Proto, generated-clean, and release readiness passed
  with 59 package imports and 114 relative Markdown links.

## 2026-07-21 — Guide authoring and source trace

- Implementer dispatch profile recorded by the task: existing `implementer`,
  expected `gpt-5.6-terra` / `medium`, explicitly dispatched. Actual runtime
  model/reasoning metadata is not exposed in this author context and is not
  claimed here; orchestrator acceptance must apply its dispatch gate.
- Before edits, the author read `doc-coauthoring` and
  `verification-before-completion`; the former applies to this reader-facing
  source audit/refinement and the latter governs final assertions. The supplied
  worktree was confirmed as linked, so `using-git-worktrees` required no
  creation action. Runtime TDD is N/A: scope forbids runtime edits.
- Audited every guide section and inline snippet against current public source,
  public tests, generated-service imports, package READMEs, package metadata,
  accepted T-0046 documentation, and `examples/datastore-orders`. The guide
  now clearly separates repository source/workspace setup from any future
  registry distribution, and all new TypeScript imports use public package
  roots.
- Added a standalone Datastore chapter. Its claims are traced to
  `packages/storage-datastore/src/datastore/storage-factory.ts`,
  `record-storage.ts`, the adapter README and focused tests; generic lifecycle
  wording is traced to `packages/storage/src/storage/storage-factory.ts` and
  `record/record-storage.ts`. The example-composition shape is traced to
  `examples/datastore-orders/src/index.ts`.
- Corrected the emulator command to make its advertised `127.0.0.1:8081`
  endpoint explicit via `--host-port`. Limitation: this task does not execute a
  live emulator or cloud smoke test because both are opt-in/external
  environment checks; focused documentation/API/link and end-user API scans
  remain pending below.
- Precision follow-up: single-tenant contexts add no adapter per-tenant
  namespace, while a client-configured default namespace remains provider
  behavior. The guide now says the adapter never invokes client teardown rather
  than presuming a particular client close API, and says a custom scan budget
  requires the constructor plus an injected client because `create()` accepts
  only Google client options.
- Added links to the Datastore orders example and the separate to-do application
  guide so readers can choose the provider-neutral composition specimen or the
  general runnable application workflow.
- Fresh focused verification after the final guide edits passed: `git diff
--check`; `pnpm --config.verify-deps-before-run=false format:check`;
  `node scripts/check-release-readiness.mjs` (59 package imports and 119
  relative Markdown links); `pnpm --config.verify-deps-before-run=false
docs:check:generated` (TypeDoc plus API-doc check); and
  `node scripts/check-cleanup-rules.mjs`. Targeted guide scans found no
  forbidden end-user handler/API patterns and no direct package-source,
  test-only, or Datastore internal imports. No snippet fixture was added:
  the guide's consumer substitutions intentionally require application-specific
  generated packages, while all concrete framework and adapter imports were
  source-audited at public roots.

## Reviews

- Pending all four canonical dispositions.

## 2026-07-21 — Orchestrator author acceptance

- Author `/root/t0049_guide_author` completed with existing role `implementer`.
  Expected and explicitly dispatched profile was `gpt-5.6-terra` / `medium`.
  The collaboration runtime fixes that role to the same actual profile; both
  dispatch fields and runtime role metadata match. The child reported only
  that metadata was not visible inside its own context.
- Fresh orchestrator verification independently passed TypeDoc/API, release
  readiness at 59 package imports / 119 relative Markdown links, cleanup
  enforcement, focused Prettier, `git diff --check`, and the guide-only
  forbidden end-user/internal-import scan. The guide retains 19 fenced
  TypeScript, Protobuf, Bash, or shell blocks.

## Integration

- Pending.
