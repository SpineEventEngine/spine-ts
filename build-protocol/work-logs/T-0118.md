# T-0118 Work Log

## Start — 2026-08-05

- T-0118 starts from pushed `origin/main@f7065f58` in an isolated worktree.
- The approved T-0113 plan fixes the payload-first and recovery contracts; no
  new requirements split is needed unless a demonstrated client-state blocker
  appears.
- Selected skills remain `test-driven-development`,
  `subagent-driven-development`, `requesting-code-review`, and
  `verification-before-completion`. `webapp-testing` was fully read for the
  required managed Chromium smoke; its helper must be invoked with `--help`
  before use, and browser inspection must wait for `networkidle`.
- The reducer stays in the Message Board example. Framework/client packages are
  out of scope unless a RED proof exposes a contract defect that cannot be
  handled example-locally.

## Implementation Dispatch — 2026-08-05

- Existing role: implementer `/root/t0118_impl`.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Ownership is bounded to Message Board web synchronization/logging/tests and
  these task records. No subagents, broad docs, or T-0119 work.
- Runtime self-introspection is not exposed by the current surface; immutable
  configured role/profile metadata applies unless the child reports more.

## First Slice — Payload Reducer — 2026-08-06

- Implemented the example-local `BoardPayloads` pure reducer/parser in
  `examples/message-board/web/src/board-payloads.ts`. It has no query,
  React, or client-runtime dependency.
- Its deterministic result either applies an atomically validated
  `SubscriptionUpdate.entityUpdates` batch or returns an explicit recovery
  reason. Valid `state` values decode as `BoardMessageView`, require matching
  Message IDs and the selected board, upsert rows, and order oldest-first.
  Valid `noLongerMatching` values remove by decoded Message ID idempotently.
- Added 11 focused unit tests in
  `examples/message-board/web/test/board-payloads.test.ts`, covering valid
  decoding/application, stable ordering, removal, malformed kinds/types/IDs
  and state, identity/board checks, empty batches, and all-or-nothing batch
  validation.
- TDD evidence: the test was first run after generation/build preparation and
  failed because `../src/board-payloads.js` did not exist. The final focused
  Vitest run passes 11/11 tests.
- Mechanical evidence: `pnpm exec tsc --noEmit -p
examples/message-board/web/tsconfig.json`, changed-file ESLint, Prettier,
  and `git diff --check` pass. The initial isolated-worktree model build
  required `pnpm proto:generate` and `pnpm typecheck:build:generated`; both
  pass and establish the ignored generated model output used by tests.
- Dispatch acceptance metadata: existing implementer `/root/t0118_impl` was
  explicitly configured as `gpt-5.6-terra` / `medium`. This surface does not
  expose independent runtime self-introspection, so immutable configured
  role/profile metadata is the available evidence; no visible fallback
  occurred.
- Limitation and next slice: this commit deliberately does not wire the
  reducer into `useBoardSync`, recovery-query coalescing/generation, reconnect,
  or posting behavior. Those remain the next T-0118 slice.

## Second Slice — Baseline Hook Application — 2026-08-06

- Integrated `BoardPayloads` into the example's existing `useBoardSync`
  adapter. The initial `useEntityQuery` remains the single authoritative
  starting read. A valid live entity batch now replaces only local rows and
  performs no additional board query.
- An unusable batch logs its simple recovery reason and uses the existing
  coalesced `refresh()` operation. A reconnect recovery clears local rows and
  replaces them with its full authoritative response; `gapPossible` continues
  to use the same coalesced query path.
- Added hook-boundary tests for valid state/removal application with exactly
  one initial query, malformed recovery query coalescing, and full row
  replacement after reconnect recovery followed by a coalesced gap query.
  The subscription fixture now sends a real empty `SubscriptionUpdate` rather
  than an untyped object for malformed wire-shape coverage.
- TDD evidence: before hook integration, the newly added valid-payload and
  reconnect tests failed because the old hook refreshed and never rendered the
  local payload. The final focused run passes 45/45 tests across
  `message-board.test.tsx` and `board-payloads.test.ts`.
- Mechanical evidence: web TypeScript typecheck, changed production-file
  ESLint, Prettier, and `git diff --check` pass. ESLint reports the existing
  test file as ignored by the repository configuration, with no lint error.
- Limitation and next slice: local update generation, stale completion,
  board-switch/unmount handling, and connected/disconnected post behavior are
  intentionally unchanged.

## Third Slice — Recovery Generation Guard — 2026-08-06

- Added example-local generation semantics to `useBoardSync`. Every applied
  valid live batch advances the local update generation. Each `refresh()`
  request captures that generation plus its board identity and may replace rows
  only while both are still current.
- A valid live batch arriving during an in-flight recovery marks the older
  response stale and requests one coalesced follow-up query. Multiple batches
  collapse into that one follow-up through the existing request counter.
  Board-switch and unmount cleanup invalidate both refresh and update
  generations and abort active recovery work.
- Added deferred-query React tests for a live batch racing an older recovery,
  multiple live batches coalescing to one follow-up query, board-switch stale
  completion, and unmount stale completion. The first race tests failed before
  implementation because the old recovery replaced live rows and issued no
  follow-up query.
- Final focused evidence: 49/49 tests pass across `message-board.test.tsx`
  and `board-payloads.test.ts`; web TypeScript typecheck, changed-production
  ESLint, Prettier, and `git diff --check` pass.
- Limitation and next slice: connected/disconnected successful-post behavior
  and browser smoke remain intentionally out of scope.

## Fourth Slice — Post Connectivity And Implementation Closure — 2026-08-06

- `Board` now refreshes after an accepted post only when the live subscription
  is not connected. Connected posts rely on the later valid subscription
  payload; failed posts retain existing feedback and never request a refresh.
- Command and payload logs now report board-level progress without logging a
  command body, subscription payload body, outcome details, or transport
  internals. They retain clear messages for sending a command, accepting it,
  applying a server payload, and the reducer's explicit recovery reason.
- Added focused tests for connected post/no Query/live payload rendering,
  disconnected post/one Query, and failed post/no refresh. The connected-post
  test failed first because the prior callback unconditionally queried.
- Acceptance audit: all runtime items in the T-0118 ledger are covered by the
  reducer and React tests. The only remaining acceptance work is managed local
  Chromium smoke, then specialist review and the task-level convergence gate.
- Final implementation-slice evidence: all 7 Message Board web test files
  pass (65 tests); app and web typechecks, changed-production ESLint,
  `lint:cleanup`, `lint:tsdoc`, Prettier, and `git diff --check` pass. Test
  files are ignored by the repository ESLint configuration; this produces no
  lint error.

## Managed Chromium Smoke — 2026-08-06

- The repository-owned Playwright topology started the real Message Board
  application gateway and Vite web client, then ran the Chromium acceptance
  suite. Posting/querying and the 61-second live-update continuity scenario
  both passed (2/2 tests).
- The first run revealed a test-teardown race: the receiving page observed the
  live payload before the sender's command promise settled, so closing the
  sender produced a misleading console error despite successful delivery. The
  browser test now waits for the sender's post button to become enabled before
  teardown.
- The corrected timed scenario passes (1/1) without the misleading post error.
  Expected transport reconnect notices remain visible while the subscription
  automatically resumes and receives the cross-page update.
- The optional Python Playwright helper documented by the local web-testing
  skill was unavailable because its Python module is not installed. No global
  environment was mutated; the repository's existing Node Playwright suite
  provided the required managed real-browser evidence instead.

## Consolidated Preflight And Review Dispatch — 2026-08-06

- Selected `verify:task` because this task changes only the isolated Message
  Board example. The profile ran with focused reducer/React tests and explicit
  coverage sources.
- The first complete attempt found three strict tooling-TypeScript errors in
  test-only Protobuf fixtures. The fixtures now use generated message types and
  `MessageInitShape`; tooling TypeScript and 51 focused tests passed before the
  required complete rerun.
- The complete rerun passed Proto generation/quality, build and tooling
  TypeScript, cleanup, TSDoc, formatting, documentation audience/API checks,
  Buf lint, generated cleanliness, release-readiness checks, and 51/51 focused
  tests. Changed production coverage is 97.97% statements, 92.99% branches,
  100% functions, and 100% lines.
- Review wave assignments use existing project roles only. Style and
  performance/reliability are explicitly dispatched as `gpt-5.6-terra` /
  `high`; documentation is explicitly dispatched as `gpt-5.6-luna` /
  `medium`. Each reviewer must check the acceptance ledger and may not spawn
  subagents. TypeScript/API review is recorded N/A because the branch adds no
  package export, public framework type, Protobuf schema, declaration surface,
  or public API documentation; its example-local exported symbols are covered
  by style and deterministic TSDoc checks.

## Review Correction Batch — 2026-08-06

- RED evidence exposed one concrete framework-boundary blocker: the existing
  public subscription observation stores only the latest delivery, so no
  example-local reducer can recover a payload that React has coalesced away.
  `client-react` now accepts optional `onDelivery` and `onLifecycle` callbacks
  and invokes them for every live raw notice before updating coalesced React
  observation state. This is the minimal boundary exception;
  the reducer and all validation policy remain Message Board-local.
- `useBoardSync` reduces from its current row ref in each delivery callback,
  preserving burst order. A reconnect resynchronization advances the update
  generation before publishing authoritative rows, invalidating an older
  ordinary recovery completion. Its console log now reports only safe board,
  target, and row-count metadata.
- Post completion reads the immediate lifecycle ref, so it refreshes after a
  connected-to-failed transition and avoids a refresh after a
  disconnected-to-connected transition. Query and live ordering both use
  exported example-local `BoardRows.compare`; PostForm TSDoc calls its callback
  a post-success notification.
- TDD: the four correction tests were first RED (burst plus two lifecycle
  transition races; reconnect stale completion was added as a regression).
  After the implementation, client-react and web TypeScript checks passed,
  focused client-react plus Message Board Vitest passed 72/72, and changed
  production ESLint, Prettier, and `git diff --check` passed.
- Dispatch acceptance metadata remains existing implementer `/root/t0118_impl`
  explicitly configured as `gpt-5.6-terra` / `medium`; runtime
  self-introspection is unavailable and no visible fallback occurred.

## Final Targeted Correction Batch — 2026-08-06

- RED evidence showed a suspended replacement render could leave the active
  subscription with no committed callback. The client-react observer now uses
  React 19 `useEffectEvent` callbacks and checks its live generation and exact
  created handle before invoking either stream callback. This prevents retired
  handles and uncommitted renders from mutating current Message Board state
  while retaining callback replacement without subscription recreation.
- Added direct client-react behavior tests for every delivery/lifecycle notice,
  committed callback replacement, suspended replacement isolation, cleanup
  suppression, and throwing callbacks that publish the error and cancel once.
  The Message Board board-switch regression uses a deliberately retained old
  subscription and proves a late old resynchronization cannot replace board B.
- Added `OnSubscriptionDelivery` and `OnSubscriptionLifecycle` to the exact
  TypeDoc API inventory and updated the package reference with the trailing
  callback signature and its synchronous, pre-coalescing, identity, and error
  semantics.
- Final focused evidence: client-react and web TypeScript checks pass; focused
  client-react plus Message Board Vitest passes 78/78; exact API inventory
  passes with 13 client-react exports; changed-source ESLint, Prettier, and
  `git diff --check` pass.

## Confirmation Correction — 2026-08-06

- Confirmation found concrete fixture evidence superseding the prior clean
  reliability disposition: `retainCancelledSubscription` had been placed on a
  different post transition test, while the `createSubscription` parameter
  named `options` shadowed the outer fixture options. The Board A/B test now
  opts in to retention, fixture options and subscription options have distinct
  names, and a late old resynchronization is emitted after Board B is visible.
- The observer now establishes/invalidates its generation with
  `useLayoutEffect`; passive subscription cleanup can no longer leave an old
  reader callback-eligible after the replacement commit. Reader predicates
  still require the exact created handle. Callback replacement remains outside
  subscription identity.
- Added direct layout-effect delivery coverage for the commit-bound retirement
  path and documented both callback alias parameters. `pnpm lint:tsdoc` now
  passes in addition to the focused type, test, API inventory, lint, format,
  and diff checks.
- Final focused evidence: client-react and web TypeScript checks pass; focused
  client-react plus Message Board Vitest passes 79/79; TypeDoc/API inventory,
  TSDoc enforcement, changed-source ESLint, Prettier, and `git diff --check`
  pass. Existing implementer `/root/t0118_impl` remains explicitly configured
  as `gpt-5.6-terra` / `medium`; runtime self-introspection is unavailable.

## Confirmation Review — 2026-08-06

- Style and TypeScript/API confirmation are clean. The TSDoc and exact API
  gates pass, and the corrected retained-stream regression proves commit-bound
  retirement.
- Reliability reconciliation is clean after 90 focused tests; its earlier
  clean result was explicitly superseded and rerun against the corrected
  fixture.
- Documentation confirms the callback reference, safe logs, callback wording,
  and review dispositions are accurate. Its final P3 header mismatch is fixed
  deterministically in the review record and does not reopen review.
- Review is converged. The final `verify:release` gate is next.
