# T-0170 Work Log

## 2026-08-12 - Startup

- Started from `origin/main@0e7f9c12`, the post-main-verified T-0169 endpoint,
  in an isolated worktree. The user's dirty primary checkout remains untouched.
- Recorded exclusive ownership of 18 reader documents and the strict shared
  TypeScript snippet checker/wiring.
- Selected test-driven development for checker behavior and the release
  verification profile because this task changes shared documentation tooling.
- Implementation assignment is the existing `implementer` role with explicit
  `gpt-5.6-terra` / medium dispatch. Runtime metadata will be recorded if
  exposed; otherwise the immutable configured profile limitation is evidence.

Status: assignment-record checkpoint pending; implementation not dispatched.

## 2026-08-12 - Checker TDD and documentation pass

- The existing `docs/check-typescript-snippets.mjs` was a permissive mixed
  checker: non-strict documents received generated `any` import declarations
  and its top-level execution made focused tests depend on unrelated docs.
- RED: added the missing-document deterministic-diagnostic behavior test and
  ran `pnpm exec vitest run scripts/check-typescript-snippets.test.mjs`. It
  failed because the old checker threw while reading the first missing path,
  rather than returning the sorted `document:line` diagnostics.
- GREEN: replaced that behavior with one explicit Wave 10 path list, strict
  TypeScript programs rooted in each real document/source context, sorted
  diagnostics, and no module stubs. The focused test then passed (six tests).
  A follow-up list/diagnostic test was added with the implementation.
- `pnpm proto:generate && pnpm typecheck:build:generated` was run before
  testing public declarations. The initial build-only attempt failed because
  generated Proto output had intentionally not yet been produced; generation
  followed by the same build passed.
- `pnpm docs:snippets:check:generated` now passes all 18 admitted documents.
  The only discovered fence defect was a partial Todo aggregate method; it is
  now a type-checked view linked to the complete source handler.

### Reader-document disposition ledger

| Document                            | Disposition        | Notes                                                                                 |
| ----------------------------------- | ------------------ | ------------------------------------------------------------------------------------- |
| `README.md`                         | reviewed-no-change | Concise framework introduction and existing journey links remain accurate.            |
| `REFERENCE.md`                      | reviewed-no-change | Framework limits remain at repository level.                                          |
| `packages/core/README.md`           | reviewed-no-change | Concise API introduction; checked fences compile.                                     |
| `packages/core/REFERENCE.md`        | reviewed-no-change | Dense validation, rejection, and registry contract remains canonical.                 |
| `packages/proto/README.md`          | reviewed-no-change | Generation journey and exports remain accurate.                                       |
| `packages/proto/REFERENCE.md`       | reviewed-no-change | Keeps source/model constraints in the reference layer.                                |
| `packages/proto-tools/README.md`    | reviewed-no-change | Build-time generation path remains concise.                                           |
| `packages/proto-tools/REFERENCE.md` | reviewed-no-change | Generator and manifest contract remains canonical.                                    |
| `packages/server/README.md`         | changed            | Restructured durable-binding prose while preserving server limits.                    |
| `packages/server/REFERENCE.md`      | changed            | Added canonical exact/default routing, event-field filter, logging, and replay rules. |
| `packages/testing/README.md`        | reviewed-no-change | BlackBox introduction and checked examples remain accurate.                           |
| `packages/testing/REFERENCE.md`     | reviewed-no-change | Lifecycle and limit details remain canonical.                                         |
| `packages/transport/README.md`      | reviewed-no-change | Same-host scope and checked example remain accurate.                                  |
| `packages/transport/REFERENCE.md`   | reviewed-no-change | Transport lifecycle and topology limits remain canonical.                             |
| `packages/proto/proto/README.md`    | reviewed-no-change | Provenance and generation policy remain canonical.                                    |
| `examples/todo/README.md`           | changed            | Replaced an uncompilable partial handler fence with a checked source view.            |
| `examples/todo/REFERENCE.md`        | reviewed-no-change | Runtime boundary and command behavior remain accurate.                                |
| `examples/todo/USER_GUIDE.md`       | changed            | Added the beginner domain-to-server path and routing/replay explanation.              |

Runtime metadata is not exposed by this execution surface. Acceptance evidence
is the immutable assignment profile: existing `implementer`, explicitly
`gpt-5.6-terra` with medium reasoning.

## 2026-08-12 - Cheap preflight

- Focused checker tests pass: 8 tests.
- Generated build, strict snippet command, audience/API checks, formatting,
  copyright, tooling typecheck, cleanup, TSDoc, logging-containment,
  release-readiness, Buf lint, generated-clean, and diff checks passed.
- The first TSDoc run rejected four ordinary `/** ... */` comments in the new
  executable script. They were converted to ordinary line comments; the focused
  tests and TSDoc check then passed. No checker behavior changed.
- `verify:task --coverage ...` began its required generated build successfully,
  but this surface lost its final completion status after that phase. Its
  individual deterministic constituent checks above are recorded instead;
  coverage confirmation remains pending a reliable focused profile run.

## 2026-08-12 - Pre-review content correction

- RED/GREEN: the new Todo-fence test failed because the README had no real
  `createTask` behavior. It now checks a generated `TaskCreated` constructor,
  rejects assertion tricks, passes with the focused suite (9 tests), and passes
  the strict all-18-document command.
- Re-audited every ledger row. The reviewed-no-change entries retain their
  concrete recorded reasons: concise package introduction for README files;
  canonical detailed contract for reference files; provenance policy for Proto
  sources; and runtime boundary for the Todo reference. Changed documents now
  also include short durable-binding, rejection, and subscription explanations.
- Split the identified dense server browser/durable-binding material and Todo
  acknowledgement/subscription material into short paragraphs and lists.
- The server reference now states exact-before-default generated type routing,
  implicit IDs in the Todo journey, `@Where({ eventField, equals })`,
  ServerEnvironment logging installation, rejection rollback convention, and
  replay-safe handler effects. It contains neither removed routing API name.

## 2026-08-12 - Consolidated review correction

- Added the task's named Human-Imposed Requirements Ledger and made the review
  record reference it. The review record now captures configured reviewer
  profiles, findings, resolutions, security N/A, and the runtime-metadata
  limitation; status is correction-complete/re-review-ready.
- Replaced the mislabeled missing-context test with its accurate description.
  New source-context fixtures prove both a valid built `@spine-event-engine/core`
  declaration import and an invalid export diagnostic. RED first exposed a
  fixture context typo; after correcting it, the valid fixture compiles and the
  invalid fixture produces ordered diagnostics without an `any` stub.
- Corrected the Todo guide: `replaceDefault` belongs to a routing declaration,
  and a rejection rolls back the Aggregate transaction. The TaskList remains
  unchanged because no domain event reaches its Projection.
- Corrected the 18-document ledger: `packages/server/README.md` is `changed`
  because its durable-binding prose was restructured.
- Correction preflight passed: focused checker tests (11), strict all-18-doc
  snippets, generated build, tooling typecheck, TSDoc, copyright, formatting,
  API/audience checks, removed-routing-name absence scan, release-readiness,
  and `git diff --check`. The execution surface dropped combined-command tail
  output twice after successful phases, so remaining gates were rerun in
  smaller commands and recorded from their explicit successful output.

## 2026-08-12 - Targeted Re-review

- Documentation re-review is clean: all 18 dispositions, links, beginner prose,
  routing, rejection, logging, and replay claims are coherent.
- Performance/reliability re-review is clean: default routing and aggregate
  rollback/no-projection-event semantics match implementation.
- TypeScript/API remained clean and was not reopened by the correction.
- Style/maintainability found the implementation and real declaration fixtures
  clean. Its only residual P2 was an incomplete named ledger; the ledger now
  explicitly includes all 18 dispositions/fences, missing path/context
  diagnostics, and local-link/canonical non-duplication checks.

Status: specialist review complete and clean; release verification pending.

## 2026-08-12 - Release metadata correction

- The release suite reported one sole failure after 245 files and 3,905 tests
  passed: `scripts/package-metadata.test.mjs` still expected the old
  `docs:check:generated` chain without the strict snippet gate.
- Updated that test to assert the exact three-command generated docs chain and
  both standalone self-sufficient snippet commands. Production wiring was
  already correct, so the updated test passed immediately; this was an
  expectation correction, not a checker behavior change.
- Focused package-metadata and checker suites passed (21 tests), along with
  strict all-18-document snippets, TSDoc, copyright, format, API/audience,
  release-readiness, and diff checks. Per instruction, this task did not rerun
  the release suite.

## 2026-08-12 - Release Verification

- A full-suite run observed two extra repeated delivery admin observations in
  the unchanged multi-machine E2E test. The test passed three consecutive
  isolated runs, T-0170 changes no delivery runtime/test file, and the final
  complete release run passed. This existing timing sensitivity is recorded for
  final convergence rather than expanded into unrelated delivery work here.
- The converged `pnpm verify:release` passed 246 test files with 3 skipped files;
  3,906 tests passed and 14 were skipped.
- Coverage passed at 94.18% statements (20,433/21,695), 90.41% branches
  (11,910/13,173), 94.09% functions (5,000/5,314), and 95.19% lines
  (19,060/20,023).
- Release readiness checked 82 package imports, 51 assets, and 321 Markdown
  links. The strict all-18-document snippet gate passed in the release profile.

Status: release-verified; integration to `origin/main` pending.

## 2026-08-12 - Integration And Post-main Proof

- Fast-forwarded the release-verified endpoint `8b95d813` to `origin/main`; no
  upstream remote or package registry was touched.
- Confirmed the checkout exactly matched `origin/main`.
- Post-main proof passed the strict 18-document snippet gate, 2 focused files /
  21 tests, documentation audience, release readiness (82 imports, 51 assets,
  321 links), copyright, formatting, and diff checks.

Status: complete, integrated, and post-merge verified.
