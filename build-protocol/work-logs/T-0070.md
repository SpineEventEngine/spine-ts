# T-0070 Work Log

Status: Implementation in progress

## 2026-07-24 — Start

- Branch/worktree: `task/T-0070-shared-history-storage` /
  `.worktrees/T-0070-shared-history-storage`, based on reviewed and pushed
  `main` at `509b24b4`.
- T-0069 is complete: all live packages use `@spine-event-engine/*`.
- Classification: high-risk serialized/shared storage contract. The
  requirements-splitter and API/reliability planning reviews completed in
  T-0068; no further deep-planning dispatch is required.
- One existing `implementer` owns the shared contracts, in-memory behavior,
  all-caller `RecordSpec` migration, conformance fixtures, and affected
  documentation. Expected and explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`.
- The implementation surface does not expose actual runtime model metadata;
  the explicit immutable dispatch profile is the available evidence.
- Provider-specific Datastore and RDBMS packages are excluded until the shared
  contract is reviewed, committed, and pushed.
- The event-store-design skill sharpens immutable journal identity,
  correlation, ordering, and retry behavior. It does not authorize event
  reconstruction; current entity state remains the source for restoration.
- Root unrelated dirty files remain outside the isolated worktree and
  `human-review-1-jul.md` must not change.

## 2026-07-24 — Slice 1: storage identity

- Added required, validated `RecordSpec.storageKey`, a deterministic schema/id/
  column-name compatibility descriptor, and length-delimited in-memory scope
  binding. Equivalent compatible handles share state; incompatible bindings
  reject before storage access.
- Migrated production callers and began the mechanical migration of existing
  test/example callers as part of the breaking constructor change. The remaining
  inline provider-test callers still need explicit keys before full closure.
- Evidence: `pnpm proto:generate`, `pnpm typecheck:build:generated`, and
  `pnpm vitest run packages/storage/test/record/record-spec.test.ts
packages/storage/test/storage/storage-factory.test.ts` passed (11 tests).

## Bounded Concurrency-Slice Ownership Transfer

- The original implementation owner completed storage identity, scoped shared
  handles, current/state/event history, ordering/retry, maintenance hooks, and
  focused tests, but repeatedly exhausted its execution window before adding
  the backend-shared async entity lock and two deterministic concurrency tests.
  It made no incomplete lock/barrier change.
- Ownership of only
  `packages/storage/src/memory/in-memory-entity-history.ts` and
  `packages/storage/test/entity/in-memory-entity-history.test.ts` transfers to
  a fresh existing `implementer`. Expected and explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`.
- The bounded owner must preserve all existing behavior, implement the shared
  keyed queue plus async selection barrier, add the trim/append and
  truncate/append interleaving tests, run focused tests/typecheck, and may not
  touch other production files, commit, push, merge, or spawn children.
- No other production writer is active, so ownership does not overlap.

## Documentation and Verification Closure Transfer

- The original owner exhausted another execution window before beginning the
  remaining documentation audit and full gate; it made no file change in that
  turn.
- A fresh existing `implementer` now exclusively owns
  `packages/storage/README.md`, `docs/USER_GUIDE.md`, T-0070 task/work records,
  acceptance auditing, and verification execution. Expected and explicitly
  dispatched profile: `gpt-5.6-terra` / `medium`.
- Production storage files remain read-only for this owner unless its
  acceptance audit identifies and records a concrete defect; any such defect
  must be corrected test-first without reverting existing work.
- No other production or documentation writer is active.

## Pre-review Correction Owner

- The closure audit returned an exact pre-review batch: 29 lint diagnostics,
  one changed storage-index formatting issue, and missing direct behavior proofs
  enumerated in the task/work records.
- A fresh existing `implementer` exclusively owns the listed
  `packages/storage/src/**` and `packages/storage/test/**` corrections and may
  update T-0070 evidence. Expected and explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`.
- The owner must correct lint/style without suppressions, add the missing
  behavior tests before implementation changes, preserve existing 74-test
  behavior and documentation, and may not commit, push, merge, or spawn
  children.
- No other writer is active.

## 2026-07-24 — History foundation and verification

- Added shared `EntityRecord`, state/event-history maintenance and internal
  port declarations, plus in-memory immutable state and diagnostic-event
  journals. State history has version continuation, temporal reads, retry
  comparison, trim/truncate, and close checks; event history has event-ID
  identity, producer-version continuation, retry comparison, truncation, and
  close checks.
- Focused evidence: `pnpm vitest run packages/storage/test` passed 67 tests;
  `pnpm typecheck:build:generated`, `pnpm format`, `pnpm lint`,
  `pnpm docs:check`, generated-output, and release-readiness checks passed.
- The initial sandbox full-suite run had broad pre-existing local HTTP/2/IPC
  integration failures outside storage. An approved unsandboxed rerun started
  successfully but the execution surface lost its completion handle before it
  returned a verdict; this remains an infrastructure-verification limitation.

## 2026-07-24 — Exact history conformance slice

- Scoped entity storage now length-delimits context, tenant, and purpose key;
  compatible handles share current/state/event backing maps and mismatched
  fingerprints reject before use. State history tests cover newest-first
  temporal reads, exclusive version continuation, identical retry no-op, and
  divergent retry rejection.
- Evidence: `pnpm typecheck:build:generated` and focused entity-history tests
  passed (3 tests).

## 2026-07-24 — Maintenance proof slice

- Added deterministic internal maintenance-hook coverage for a durable partial
  truncate failure followed by idempotent resume, and for close after an active
  deletion chunk begins. Close rejects subsequent maintenance without corrupting
  retained history; no public cursor was added.
- Evidence: generated build passed and focused entity-history tests passed
  (5 tests).

## 2026-07-24 — Event-history conformance slice

- Added focused diagnostic-journal coverage for entity correlation, producer
  version continuation, deterministic descending event-ID tie order, identical
  event retry no-op, and divergent same-ID rejection.
- Evidence: generated build and focused entity-history suite passed (6 tests).

## 2026-07-24 — Shared state-history maintenance concurrency slice

- Added a backend-shared FIFO queue keyed by canonical entity ID and passed it
  into each independent in-memory state-history handle. `append` and `trim`
  for the same entity serialize; `trim` retains its lock from selection through
  deletion. The queue releases after failures, retaining retry/resume behavior.
- Extended the internal-only maintenance seam with awaited `afterSelection` and
  awaited `onChunk` results. `truncate` snapshots selected keys, awaits that
  barrier without taking an entity queue, then deletes only its snapshot;
  concurrently appended eligible rows therefore remain for a later invocation.
- Test-first evidence: before the adapter change,
  `pnpm vitest run packages/storage/test/entity/in-memory-entity-history.test.ts`
  failed the two new selection-barrier assertions (the hook was absent). After
  the change, the same focused suite passed 8 tests, including deterministic
  trim/append and truncate/append deferred-promise interleavings. Formatting of
  the two owned files and `pnpm typecheck:build:generated` also passed.

## 2026-07-24 — Documentation and mechanical closure audit

- Documentation now states the required `RecordSpec.storageKey`, compatibility
  binding, context/tenant/purpose scoping, shared current `EntityRecord`,
  immutable history ports, maintenance semantics, and the explicit T-0071
  repository/configuration deferral. It also states that diagnostic events are
  never a reconstruction source.
- Focused storage validation passed: `pnpm --config.verify-deps-before-run=false
exec vitest run packages/storage/test` — 9 files / 74 tests. `pnpm
--config.verify-deps-before-run=false proto:generate`, `typecheck:generated`,
  and `docs:check:generated` also passed. TypeDoc generated its ignored API
  reference output successfully.
- Generator-mode audit: `git diff --summary` reported no tracked executable-bit
  drift after generation; there was no install-induced generator mode to
  restore in this worktree.
- Pre-review correction batch (not an external blocker): `lint:generated`
  failed with 29 diagnostics. The implementation owner must correct these
  before review:
  - `packages/storage/src/entity/entity-history-storage.ts` —
    `@typescript-eslint/no-unused-vars` (2): generic `S` at 6:47 and `I` at
    14:37.
  - `packages/storage/src/memory/in-memory-entity-history.ts` —
    `@typescript-eslint/restrict-template-expressions` (2, 24:26 and 285:40),
    `@typescript-eslint/no-unsafe-assignment` (3, 98:5, 122:5, 206:5),
    `@typescript-eslint/require-await` (8, 101:3, 106:3, 125:3, 144:3, 169:3,
    213:66, 226:3, 236:3),
    `@typescript-eslint/no-deprecated` (1, 401:10).
  - `packages/storage/src/memory/in-memory-storage-factory.ts` —
    `@typescript-eslint/restrict-template-expressions` (1, 47:35) and
    `@typescript-eslint/no-deprecated` (1, 51:10).
  - `packages/storage/src/record/record-spec.ts` — `no-control-regex` (1,
    107:5).
  - `packages/storage/test/entity/in-memory-entity-history.test.ts` —
    `@typescript-eslint/no-unsafe-assignment` (2, 140:7, 158:7),
    `@typescript-eslint/restrict-template-expressions` (3, 150:54, 174:32,
    255:28), `prefer-const` (1, 166:5),
    `@typescript-eslint/no-confusing-void-expression` (1, 169:37), and
    `@typescript-eslint/no-invalid-void-type` (2, 183:32, 216:32).
  - `packages/storage/test/storage/storage-factory.test.ts` —
    `@typescript-eslint/no-unused-vars` (1, 3:22).
- `format:check` is also red: `packages/storage/src/index.ts` is an
  implementation-owned correction; `build-protocol/security/T-0067-wave1-security-findings.md`
  is an unrelated existing formatting finding and was not edited. No tracked
  executable-bit drift was reported by this check.
- Acceptance audit is recorded in the T-0070 task record. It identifies the
  direct behavior proofs still needed (fingerprint mismatch, tuple/tenant
  isolation, large histories, canonical tie ordering, complete cache groups,
  and all required failure stages) before a truthful review-ready status.

## 2026-07-24 — Pre-review correction closure

- Added direct independent-handle fingerprint rejection and length-delimited
  context, tenant, and purpose-isolation proofs. Current, state-history, and
  event-history scopes cannot share rows; compatible handles continue to share
  their own scope.
- Added large-history/short-read proofs, state temporal tie ordering by
  creation time then version, exclusive continuation, and event tie ordering
  by descending canonical UTF-8 event-ID bytes. The in-memory comparator now
  implements that byte ordering rather than locale-dependent collation.
- Added the reusable adapter-neutral entity-history conformance fixture and
  ran it against in-memory storage. It covers cloned current records, large
  state reads, exclusive continuation, temporal reads, and event tie ordering.
  Repository instance cache-group behavior is deliberately not an adapter
  storage concern and remains T-0071's repository configuration/read-cache
  work.
- Added resumable failure coverage after a completed state `trim`, state
  `truncate`, and event `truncate` deletion chunk. Single current/state/event
  append calls are atomic at this in-memory seam; repository dispatch ordering,
  latest-state/history/batch-flush partial failures remain T-0071 behavior.
- Corrected all 29 reported lint diagnostics without suppression, formatted
  all changed storage source/test files, and preserved the unrelated T-0067
  security record.
- Evidence: `pnpm --config.verify-deps-before-run=false exec vitest run
packages/storage/test` passed 9 files / 82 tests; generated typecheck and
  `lint:generated` passed; targeted Prettier check over all changed storage
  source/test files passed; `git diff --check` passed. Repository-wide
  `format:check` remains red solely for the pre-existing, out-of-scope
  `build-protocol/security/T-0067-wave1-security-findings.md`.

## 2026-07-24 — Focused branch-coverage correction

- Added focused behavior proofs for missing multitenant tenant IDs, missing and
  blank event IDs, every public event-history operation after close, temporal
  state misses, invalid trim counts and history depths, default storage maps
  and omitted maintenance hooks, one/two/three/four-byte UTF-8 event-ID order,
  RecordSpec empty/control-character validation, and deterministic
  compatibility fingerprints.
- The missing-tenant test found and corrected a real scope defect: an unnamed
  multitenant scope previously shared rows with a single-tenant scope of the
  same context name. In-memory canonical scope now length-delimits an explicit
  tenancy-mode segment, so missing/empty multitenant tenant IDs remain
  equivalent to each other but cannot collide with a single-tenant context.
- Test-first evidence: the missing-tenant isolation test initially failed; it
  passes after the scope correction. Focused entity-history and RecordSpec
  tests pass 31 assertions. Their coverage reports 92.63% branches for
  `in-memory-entity-history.ts` and 90.62% branches for `record-spec.ts`.
- A focused `--coverage packages/storage/test` run cannot meet the configured
  repository-global 90% threshold because the coverage include pattern spans
  all packages/examples while that command intentionally executes only storage
  tests. The full configured coverage gate is required for the global verdict.
- Final focused evidence: `pnpm --config.verify-deps-before-run=false exec
vitest run packages/storage/test` passed 9 files / 91 tests; generated
  typecheck, `lint:generated`, targeted Prettier, and `git diff --check`
  passed. The full `test:coverage:generated` gate started but failed in broad,
  unrelated IPC/gRPC integration suites (transport ZeroMQ, server lifecycle and
  services, delivery, BlackBox, and example local multi-process tests) before
  it could provide a trustworthy global coverage verdict. This repeats the
  recorded environment-wide full-suite limitation; the focused changed runtime
  modules meet the 90% branch target without coverage exclusions or production
  test hooks.

## 2026-07-24 — Consolidated review correction batch

- Extracted canonical UTF-8 length-delimited storage scope encoding. Entity
  scopes include validated tenant mode; record storage keeps tenant slicing in
  its existing `TenantRecords` layer while sharing compatible backend bindings
  across independent factory instances.
- Restored the full storage suite after migrating mixed lookup fixtures from a
  reused legacy key to distinct stable layout keys. The mismatch rule remains
  fail-closed for one key opened with an incompatible layout.
- Entity compatibility is now derived from state schema, explicit ID codec
  fingerprint, and explicit layout; mutable IDs are cloned at current/state/
  event boundaries. Event truncation checks close after every settled chunk.
- Root exports remain end-user maintenance-only. The narrow provider SPI is
  explicitly available at `@spine-event-engine/storage/internal/entity-history`
  for T-0070D/R; its compile fixture proves the provider imports without
  re-exporting row ports or purpose keys from the root.

## 2026-07-24 — Bounded maintenance correction

- State trim, state truncate, and event truncate now select and delete stable
  canonical keys in fixed adapter-internal batches (default 128), rather than
  materializing all eligible rows. The in-memory maintenance seam accepts a
  validated positive-safe-integer batch override only for deterministic tests.
- Truncate fixes its eligible high-water key before the selection barrier, so
  a concurrent eligible append remains for a later invocation. Every settled
  batch checks close before the next selection; close therefore settles its
  active bounded chunk and aborts subsequent work. Trim remains serialized with
  same-entity appends through the existing keyed queue.
- Focused entity-history evidence: 25 tests pass after the fixed-boundary
  correction; generated typecheck passes.

## 2026-07-24 — Coverage-gate correction assignment

- Existing role/function: bounded TypeScript implementation owner
  (`t0070_corrections`).
- Assignment: add focused behavior tests for meaningful uncovered branches in
  the new shared entity-history and storage-scope implementation, sufficient
  to restore the repository's 90% global branch threshold. Production changes
  are permitted only when a test exposes an actual defect; coverage-only
  implementation tricks are prohibited.
- Expected runtime profile: `gpt-5.6-terra`, medium reasoning. Both fields were
  explicit in the original child dispatch; this follow-up deliberately reuses
  that immutable configured implementation profile and its existing code
  context.
- Runtime metadata limitation: the execution surface does not expose child
  self-introspection. Acceptance will therefore use the immutable configured
  role/profile plus the orchestrator's mechanical verification.

## 2026-07-24 — Native global coverage verification

- The orchestrator reran the configured gate with native local IPC, gRPC,
  ZeroMQ, and child-process access after the sandboxed run's environmental
  failures.
- `pnpm --config.verify-deps-before-run=false test:coverage:generated` passed:
  128 test files passed and 3 skipped; 2,354 tests passed and 21 skipped.
- Coverage passed every repository threshold: 94.18% statements
  (13,791/14,642), 90.01% branches (7,439/8,264), 95.07% functions
  (3,472/3,652), and 94.72% lines (13,192/13,927).
- The earlier sandbox result is therefore an environment limitation, not a
  product or coverage blocker. T-0070 proceeds to specialist review.

## 2026-07-24 — Consolidated review-correction assignment

- Existing role/function: bounded TypeScript implementation owner
  (`t0070_corrections`), retaining the complete T-0070 implementation context.
- Expected runtime profile: `gpt-5.6-terra`, medium reasoning. Both fields were
  explicit in the original dispatch and the immutable configured profile is
  reused for this follow-up. Child self-introspection remains unavailable.
- One correction batch owns every confirmed lane finding: canonical shared
  scope encoding; complete derived compatibility fingerprints; backend-shared
  cross-factory record state; validated tenant identity; cloned current/state/
  event IDs; close-aware event truncation; bounded stable-order maintenance;
  the exact public-versus-internal adapter surface; API inventory/reference and
  storage-contract prose; continuation wording; and focused regression/
  conformance tests.
- The owner must assess the root-export finding against the frozen plan:
  `EntityRecordStorage` and row ports are internal while
  `EntityStateHistoryStorage`/`EntityEventStorage` are public. Cross-package
  provider adapter needs must use the narrowest planned SPI without inventing a
  second public abstraction. Any necessary contract interpretation must be
  recorded explicitly.
- The owner may not commit, push, merge, or spawn children. After focused
  mechanical checks, affected style, API, documentation, and reliability lanes
  will be re-reviewed once.

## 2026-07-24 — Consolidated correction continuation

- The first correction turn completed canonical scope sharing, tenant
  validation, derived entity identity/clone plumbing, event close checks,
  narrowed exports, and initial documentation updates, but its execution
  window ended before acceptance.
- The required backend-shared in-memory binding exposed one legacy test fixture
  which reused `EventSchema:legacy` for incompatible layouts. The mismatch
  rejection is correct and will not be weakened; the fixture requires an
  atomic stable-key/layout migration.
- The same existing implementation owner (`t0070_corrections`) is redispatched
  to preserve context. Expected immutable profile remains `gpt-5.6-terra`,
  medium reasoning; both fields were explicit in the original dispatch and
  child self-introspection remains unavailable.
- Remaining ownership: migrate the conflicting fixture, complete explicit
  column/ID compatibility descriptors and bounded stable-order maintenance,
  finish docs/API inventory, and return clean focused mechanical evidence.

## 2026-07-24 — Bounded-maintenance continuation

- The continuation restored the storage suite (10 files / 95 tests), and
  generated typecheck, lint, targeted formatting, and diff checks pass.
- The internal cross-package provider seam is now exported only at
  `@spine-event-engine/storage/internal/entity-history`; a compile fixture
  proves it is available to Datastore/RDBMS owners while the package root keeps
  only end-user maintenance types.
- The execution window ended with one substantive reliability finding still
  open: state/event maintenance still materializes its complete selection
  rather than using bounded stable key-only chunks. Documentation/release
  commands also lacked terminal exit evidence.
- The same existing implementation owner (`t0070_corrections`) is
  redispatched, retaining `gpt-5.6-terra`, medium reasoning, to own only
  bounded deterministic maintenance, its direct tests, and terminal docs/
  release evidence. Child self-introspection remains unavailable.

## 2026-07-24 — Descriptor-migration continuation

- Bounded maintenance is implemented with stable key scans, a production
  default batch size of 128, a validated adapter-internal override, fixed
  truncate high-water boundaries, and per-batch close checks. Focused history
  tests pass 25 tests; the full storage suite passes 10 files / 95 tests;
  generated typecheck, lint, targeted formatting, and diff checks pass.
- The owner correctly withheld acceptance because primitive
  `RecordSpec.idKind` and `RecordColumn.valueType` still default to
  `"unknown"`, allowing incompatible undeclared layouts to share a
  fingerprint.
- The same existing owner is redispatched with the immutable
  `gpt-5.6-terra`, medium profile to make both descriptors required and
  validated, migrate every caller atomically, add direct incompatibility
  proofs, and finish terminal docs/release evidence.

## 2026-07-24 — Descriptor-fixture continuation

- The descriptor API now requires and validates nonblank
  `RecordColumn.valueType`; primitive `RecordSpec` construction requires
  `idKind`, while schema-backed IDs reject a conflicting primitive kind.
- Production TypeScript column call sites compile after migration. The
  execution window ended while 32 storage test fixtures and remaining
  primitive-ID test callers were still being migrated, so the suite is
  intentionally red and no acceptance is claimed.
- The same existing implementation owner is redispatched under the immutable
  `gpt-5.6-terra`, medium profile to finish only the atomic fixture migration,
  direct mismatch/validation proofs, and terminal gates.

## 2026-07-24 — Descriptor-fixture ownership transfer

- The existing correction context exhausted its execution window after
  migrating core production and storage-test factories. Datastore/RDBMS
  integration fixtures still contain undeclared column/primitive-ID kinds, so
  the atomic migration remains red and no acceptance is claimed.
- Ownership transfers to one fresh bounded existing `implementer`,
  `t0070_descriptor_finish`, for only the remaining mechanical caller
  migration, direct descriptor tests, and terminal verification. No other
  writer owns these files.
- Expected configured profile: `gpt-5.6-terra`, medium reasoning. The dispatch
  selects the immutable implementer role/profile explicitly; child
  self-introspection is unavailable and will be recorded as such.

## 2026-07-24 — Stalled-agent recovery and strict-type closure

- `t0070_descriptor_finish` populated the Datastore/RDBMS descriptor migration
  but stopped responding across repeated command boundaries. The orchestrator
  interrupted the stalled child; all filesystem work was preserved.
- Fresh generated typecheck proves the descriptor migration is complete enough
  that only five strict test-value errors remain: three raw `Timestamp`
  literals, one exact-optional tenant fixture, and one `unknown`-to-string test
  clone.
- Ownership transfers to one fresh bounded existing `implementer`,
  `t0070_strict_type_finish`, for only these five test corrections, descriptor
  audits/direct proofs, and terminal gates. Expected immutable profile:
  `gpt-5.6-terra`, medium reasoning; self-introspection remains unavailable.

## 2026-07-24 — Full-suite backend-sharing blocker

- A fresh native global coverage run failed 228 tests with pervasive retained
  rows, duplicate event IDs, stale projections, and fingerprint conflicts.
  This is not the sandbox IPC limitation: the process-static
  `InMemoryStorageFactory` registry turns every separately constructed
  in-memory factory in the full test/application process into one hidden global
  database.
- The frozen contract requires compatibility binding in backend-shared state
  and cross-factory sharing for factories attached to the same backend; it does
  not justify accidental sharing between unrelated in-memory database
  instances. The current implementation therefore needs an explicit backend
  ownership boundary which preserves deliberate cross-factory sharing without
  factory-local fingerprints or global leakage.
- This is a demonstrated architecture/reliability blocker at the shared
  contract boundary. Existing `requirements_splitter`
  (`t0070_backend_boundary`) is dispatched read-only to select the smallest
  plan-conformant ownership model and acceptance tests.
- Expected immutable profile: `gpt-5.6-sol`, high reasoning. The fixed role
  profile is selected explicitly; child self-introspection is unavailable.

## 2026-07-24 — Backend-boundary decision and implementation assignment

- Actual planning metadata: child self-introspection was unavailable; the
  accepted immutable role/profile was `requirements_splitter`,
  `gpt-5.6-sol`, high reasoning, matching the recorded dispatch.
- Decision: introduce one opaque root-exported `InMemoryStorageBackend` token.
  Default record/entity factory construction owns a fresh isolated backend;
  passing the same token to independently constructed factories deliberately
  shares canonical scope bindings and payloads. Internal backend state uses a
  package-private token map and one bind/create authority.
- The token selects an ephemeral in-memory database only. Canonical context,
  tenant, storage key, row keys, and fingerprints remain the data identity.
  Closing one factory/handle never clears the backend or affects siblings.
- Rejected: factory-local binding maps, process-global maps/reset hooks, global
  string database names, constructor/spec identity, and ref-counted backend
  close lifecycle.
- One fresh bounded existing `implementer`, `t0070_backend_implement`, owns
  `packages/storage/**`, the API inventory/docs needed for the new token, and
  focused tests. Expected immutable profile: `gpt-5.6-terra`, medium reasoning;
  child self-introspection is unavailable. No parallel writer overlaps.

## 2026-07-24 — Backend implementation recovery

- `t0070_backend_implement` added the opaque backend token, shared bind
  authority, isolated/default and explicit-sharing tests, but stopped
  responding across required progress boundaries. The child was interrupted
  and all edits were preserved.
- Fresh generated typecheck reports four localized errors: `EntityIdCodec`
  omits its required fingerprint while `entityFingerprint()` reads it, and
  `EntityBackend` retains a redundant fingerprint field after binding moved to
  the backend authority.
- Ownership transfers to one fresh bounded existing `implementer`,
  `t0070_backend_finish`, for these four corrections, focused backend/storage
  tests, docs/API inventory, and terminal mechanical gates. Expected immutable
  profile: `gpt-5.6-terra`, medium reasoning; self-introspection remains
  unavailable.

## 2026-07-24 — Backend recovery verification

- Backend correction focused gates pass: generated typecheck; storage suite
  10 files / 101 tests; generated lint; targeted formatting; diff check;
  generated API docs/inventory; and release readiness.
- The first native full coverage attempt passed 128 files / 2,363 tests and
  failed only one multi-machine delivery readiness timeout. Its isolated rerun
  passed 3/3 tests.
- A fresh native full coverage rerun passed 129 files with 3 skipped and 2,364
  tests with 21 skipped. Coverage passed at 94.19% statements
  (13,851/14,704), 90.02% branches (7,494/8,324), 95.11% functions
  (3,488/3,667), and 94.72% lines (13,243/13,980).
- The previous 228-test global storage-leak pattern is eliminated. T-0070
  proceeds to one affected-lane re-review wave.

## 2026-07-24 — Final re-review correction assignment

- One final aggregated re-review batch owns: tenant-inclusive record binding
  scope; internal provider export of the reusable conformance contract/fixture
  needed by T-0070D/R; fixed event-truncate high-water selection; genuinely
  bounded state-trim retention selection; event retry entity-correlation
  comparison; compilable descriptor snippets; and consistent backend/export/
  descriptor documentation.
- One fresh bounded existing `implementer`, `t0070_final_corrections`, owns the
  shared storage implementation/tests and affected documentation. No parallel
  writer overlaps.
- Expected immutable profile: `gpt-5.6-terra`, medium reasoning. The fixed role
  is selected explicitly; child self-introspection is unavailable.
- The owner may not weaken compatibility or concurrency contracts, add a
  public cursor, commit, push, merge, or spawn children. All focused gates must
  pass before final affected-lane re-review.

## 2026-07-24 — Final correction recovery and disjoint closure

- `t0070_final_corrections` stopped responding and was interrupted. Inspection
  confirmed that it landed tenant-inclusive binding, framework-agnostic
  conformance export, event high-water/correlation checks, bounded trim
  selection, and their focused tests, but did not finish verification/docs.
- Fresh generated typecheck has one localized error: the conformance test still
  supplies `name`, while the new framework-agnostic adapter interface omits it.
- Two disjoint bounded existing implementers run in parallel:
  - `t0070_code_close` owns only `packages/storage/src/**`,
    `packages/storage/test/**`, and `packages/storage/package.json`; it fixes
    the single type error, completes focused code tests/gates, and may not edit
    docs/protocol.
  - `t0070_docs_close` owns only `docs/USER_GUIDE.md`,
    `docs/api/README.md`, `packages/storage/README.md`, and
    `scripts/check-api-docs.mjs`; it corrects snippets/inventory/prose and may
    not edit production/tests/protocol.
- Both use the immutable existing `implementer`, `gpt-5.6-terra`, medium
  profile. Child self-introspection is unavailable; there is no overlapping
  writer.

## 2026-07-24 — Dynamic-tenant binding correction

- Code closure passed typecheck and 30 focused entity/provider tests, but the
  full storage suite exposed three in-scope regressions after tenant-inclusive
  binding: two EventStore tests switch the current tenant on one open handle,
  while missing-tenant validation must occur on operation rather than factory
  construction.
- Root cause: the factory resolved canonical tenant scope once when opening the
  storage. The established `StorageContext` is dynamic, so backend binding must
  resolve the current tenant lazily for each storage operation.
- The same bounded `t0070_code_close` implementer is redispatched, retaining
  `gpt-5.6-terra`, medium reasoning, to replace the captured tenant-map input
  with a lazy backend-owned `TenantRecords` resolver. This preserves per-tenant
  scope/fingerprint binding, dynamic tenant switching, and operation-time
  missing-tenant rejection. Documentation ownership remains disjoint.

## 2026-07-24 — Final correction focused verification

- Code closure: lazy per-operation tenant binding is implemented; full storage
  suite passes 10 files / 105 tests, generated typecheck passes, scoped lint
  and targeted formatting pass, and `git diff --check` passes.
- Direct tests cover operation-time missing-tenant rejection, tenant-switching
  EventStore reads, cross-tenant incompatible layouts, same-tenant mismatch,
  event truncate high-water behavior, bounded large trim, divergent event
  correlation, and the internal provider conformance export.
- Documentation closure: targeted formatting, generated API docs/inventory
  with 33 expected storage exports, release readiness, and diff check pass.
  Snippets and identity/backend/API inventory prose reflect the final
  contracts.
- One fresh native global coverage gate and narrowly affected re-review wave
  remain before acceptance.

## 2026-07-24 — Tenant-validation message correction

- Native full coverage passed 127 files / 2,365 tests and failed three delivery
  assertions only because canonical scope validation returned a generic
  nonblank-tenant message instead of the established context-qualified
  `context.tenantId` error.
- Behavior is correct; this is a deterministic compatibility wording fix in
  the shared helper. The same `t0070_code_close` owner is redispatched under
  its immutable `gpt-5.6-terra`, medium profile for the message and focused
  delivery/storage verification. No lane is reopened by the record-only
  wording itself; final review still examines the complete corrected diff.

## 2026-07-24 — Final coverage-margin correction

- Native full execution passed all 129 files / 2,368 tests, but branch coverage
  is 89.93% (7,504/8,344), below the 90% gate by roughly six branches.
- Focused coverage maps the gap to meaningful new edges: canonical UTF-8 scope
  encoding, direct/local `InMemoryRecordStorage` tenant validation, and an
  unused `includeTenant=false` branch which the final contract no longer uses.
- The same `t0070_code_close` owner is redispatched under its immutable
  `gpt-5.6-terra`, medium profile to remove the obsolete optional scope branch
  and add direct behavior tests for multibyte scope identity plus local
  missing/blank tenant validation. Production coverage hacks/exclusions are
  prohibited.

## 2026-07-24 — Canonical maintenance-key comparator correction

- Final style confirmation found that maintenance high-water/paging compares
  keys with JavaScript UTF-16 ordering, which differs from the frozen canonical
  UTF-8 byte ordering for keys such as U+E000 and U+10000.
- The same bounded `t0070_code_close` implementer is redispatched under its
  immutable `gpt-5.6-terra`, medium profile to route `selectKeys` and
  `lastMatchingKey` through the existing canonical byte comparator and add a
  deferred multi-chunk non-ASCII event-truncate proof. No other behavior or
  public contract may change.

## 2026-07-24 — Final acceptance

- Canonical comparator RED/GREEN and final focused gates pass: history 30/30,
  storage 10 files / 108 tests, generated typecheck, scoped lint, targeted
  formatting, and diff check.
- Final native coverage passes 129 files with 3 skipped and 2,371 tests with 21
  skipped: 94.21% statements, 90.08% branches, 95.13% functions, and 94.74%
  lines.
- Final API, documentation, style, and reliability confirmations are clean.
  Final security is N/A because no external trust boundary was introduced.
- T-0070 acceptance is complete. Next action is commit and immediate task-branch
  push, then merge to `main`, immediate main push, and post-merge verification.

## 2026-07-24 — Strict-type fixture closure

- Existing role/function: bounded existing `implementer`,
  `t0070_strict_type_finish`, configured explicitly as `gpt-5.6-terra` /
  `medium`; this execution surface does not expose runtime model
  self-introspection.
- The pre-change generated typecheck was red with exactly the five recorded
  fixture errors: three raw `Timestamp` object literals in
  `entity-history-conformance.ts`, an exact-optional `tenantId` fixture, and
  one inferred `unknown` ID key helper. The fixture now materializes generated
  `Timestamp` messages, conditionally omits undefined `tenantId`, and types
  the key helper as `string -> string`. No runtime production behavior changed.
- Direct descriptor coverage already exists in
  `packages/storage/test/record/record-spec.test.ts`: blank `valueType`
  rejection, schema/primitive ID descriptor conflict rejection, and different
  column/primitive-ID compatibility fingerprints for the same storage key.
- Terminal evidence (all commands exited 0):
  - `pnpm --config.verify-deps-before-run=false typecheck:generated`;
  - `pnpm --config.verify-deps-before-run=false exec vitest run packages/storage/test`
    — 10 files / 98 tests passed;
  - `pnpm --config.verify-deps-before-run=false lint:generated`;
  - `pnpm --config.verify-deps-before-run=false exec prettier --check
packages/storage/test/entity/entity-history-conformance.ts
packages/storage/test/entity/in-memory-entity-history.test.ts`;
  - `git diff --check`;
  - `pnpm --config.verify-deps-before-run=false docs:check:generated`;
  - `pnpm --config.verify-deps-before-run=false check:release-readiness`.
- Read-only AST audit over 473 package TypeScript files passed: every
  `RecordColumn` constructor has an explicit third `valueType` argument, every
  object-literal `RecordSpec` without `idSchema` has `idKind`, and no literal
  `unknown` value-type or ID-kind descriptor remains.
- No commit, push, or merge was performed. The bounded strict-type closure is
  ready for the orchestrator's review/integration flow.

## 2026-07-24 — Backend shared-storage recovery

- Existing role/function: bounded existing `implementer`,
  `t0070_backend_finish`, configured explicitly as `gpt-5.6-terra` / `medium`;
  this execution surface does not expose runtime model self-introspection.
- Test-first red evidence: generated typecheck failed with exactly the four
  localized entity-backend errors recorded in the previous recovery entry.
  `EntityIdCodec` now requires the validated nonblank compatibility
  `fingerprint` consumed by derived entity binding, while `EntityBackend` no
  longer redundantly stores that fingerprint: backend scope binding remains its
  single authority.
- Confirmed behavior: default record and entity factories own isolated
  backends; an explicit shared `InMemoryStorageBackend` token shares compatible
  canonical scopes and rejects incompatible fingerprints before access; closing
  one record factory leaves the sibling's backend rows available. Context,
  tenant, and purpose isolation and the provider-only entity-history SPI
  compile fixture remain covered. The bind helper is absent from root exports;
  the root API exposes only the opaque backend token plus planned public
  storage types.
- Corrected the API-inventory allowlist and API/package/user documentation to
  state default isolation and explicit-token sharing. A lint-only cleanup makes
  the token an intentional opaque constructor and removes the unused local
  record interface; it changes no storage behavior.
- Terminal evidence (all commands exited 0):
  - `pnpm --config.verify-deps-before-run=false typecheck:generated`;
  - `pnpm --config.verify-deps-before-run=false exec vitest run
packages/storage/test` — 10 files / 101 tests passed;
  - `pnpm --config.verify-deps-before-run=false lint:generated`;
  - targeted Prettier check over changed backend source and documentation;
  - `git diff --check`;
  - `pnpm --config.verify-deps-before-run=false docs:check:generated`;
  - `pnpm --config.verify-deps-before-run=false check:release-readiness`.
- No commit, push, merge, or full native suite was run; the orchestrator owns
  the latter and review/integration flow.
