# T-0057 Work Log

Status: Complete — integration pending

## 2026-07-22 — Start And Assignment Record

- T-0056 is reviewed, committed, task-branch pushed, merged as `298d0549`,
  post-merge fully verified, durably closed as `eb810f48`, and pushed.
- Created isolated branch/worktree `task/T-0057-projection-query` /
  `.worktrees/T-0057-projection-query` from exact pushed `eb810f48`.
- Classified T-0057 high-risk for public generic/wire contracts, server
  validation, deterministic query semantics, SQL parameterization, Datastore
  finite materialization, and cross-provider conformance.
- The accepted T-0052 Wave 1 plan already performed the architecture and public
  contract split. It designates T-0056's normalized plan/capability policy as
  the sole provider boundary and gives this packet explicit ownership of
  in-memory, MySQL, and Datastore execution.
- Implementation assignment uses the existing `implementer` role with
  explicit `gpt-5.6-terra` / `medium`. The owner must work behavior-first,
  preserve unrelated files, avoid commits/pushes/merges, and not spawn children.
- Runtime metadata will be recorded if exposed; otherwise the immutable
  configured role/profile and surface limitation will be recorded honestly.

## Next Action

Run the complete canonical specialist review wave, aggregate one accepted
finding batch, and return it to the current implementation context if needed.

## 2026-07-22 — Implementation Owner Start

- Existing role: `implementer`; bounded ownership is the complete T-0057
  production/test/documentation packet in this worktree. The explicit dispatch
  profile is `gpt-5.6-terra` / `medium`, matching the expected assignment.
  Runtime model self-introspection is unavailable on this surface, so no
  runtime identifier is invented; the explicit dispatch and immutable role
  profile are the available evidence.
- Classification remains high-risk because the packet changes public generic
  and frozen-wire contracts, server trust-boundary validation, persistent
  provider execution, and deterministic cross-adapter semantics.
- Selected the available `implement` and `test-driven-development` skills from
  the session inventory and fully read both entrypoints before implementation.
  The repository protocol overrides the `implement` skill's ordinary commit
  step because this assignment explicitly prohibits commits, pushes, and
  merges. No other skill is needed for this bounded implementation role.
- Read the complete build protocol, completion plan, code-quality rules,
  T-0057 task/work/review records, accepted Wave 1 plan, and T-0052 accepted
  correction record. The accepted design has no open human question: T-0056's
  normalized plan and capability validator remain the sole policy boundary.
- High-risk assumptions to prove are: one conversion and validation before
  provider access; descriptor-kind comparison without generic lexical
  fallback; deterministic repeated ordering with one ID tie-breaker; identical
  null/missing behavior in all adapters; parameterized MySQL only; and a
  strictly finite Datastore candidate scan that errors rather than truncating.
- The worktree contains only the orchestrator-created untracked T-0057 records
  plus the expected untracked root `node_modules` link. They will be preserved;
  the dependency link is verification-only and must never be staged.

## Immediate Implementation Action

Inspect the task-relevant frozen JVM query sources and current T-0056/query
execution seams, then establish compile-time and runtime RED fixtures before
production changes.

## 2026-07-22 — JVM Query Guardrail

- Inspected the task-relevant sections of
  `spine-jvm-docs/spine-client-api-queries-subscriptions-and-tests.md`,
  `spine-jvm-docs/spine-server-runtime-and-bounded-context.md`, and
  `spine-jvm-docs/spine-validation-storage-observability-and-support.md`.
  They identify the frozen Query/Target/Filter wire, `QueryBuilder`,
  `EntityQueryToProto`, `QueryService`, `RecordQuery`, matcher, mask, ordering,
  and in-memory storage sources and establish that QueryService is a thin
  router while Stand/storage owns execution.
- The formerly documented `/private/tmp/spine-research/core-jvm` source tree is
  now empty except for tool metadata, and a bounded search found no local
  `QueryBuilder.java`, `EntityQueryToProto.java`, `QueryService.java`, or
  `RecordQueryMatcher.java` checkout. Direct source inspection is therefore
  unavailable in this worktree; the durable task-relevant notes and exact
  frozen Proto sources are the available guardrail.
- Implementation impact: preserve the generated frozen Protobuf messages,
  support repeated order clauses despite the JVM builder's known overwrite
  defect, keep missing values first ascending/last descending, validate before
  storage access, and keep service routing thin over Stand plus the canonical
  normalized storage plan.

## 2026-07-22 — Implementation And Focused Evidence

- Established RED fixtures before each production slice: the client builder
  and operator helpers were absent; the shared evaluator and normalized
  provider methods were absent; and MySQL/Datastore initially rejected the
  normalized feature set at T-0056's capability boundary.
- Added the Projection-only `ProjectionQuery` DSL with descriptor-owned typed
  columns, legal operator/value relationships, typed masks and orderability,
  repeated ordering, positive ordered limits, nested `ALL`/`EITHER`, and
  frozen Query protobuf compilation. Aggregate and process-manager targets
  remain deferred.
- Added one normalized wire-to-plan conversion in QueryService, descriptor-aware
  pre-provider validation, complete nested/range/system-column handling, and
  Stand execution that preserves version plus lifecycle semantics.
- Added the shared deterministic evaluator and full in-memory execution with
  missing-first ascending/missing-last descending, repeated order clauses, ID
  tie-breaking, masks, and limits.
- Added parameterized MySQL compilation for IDs, every comparison, nested
  boolean expressions, repeated ordering, tenant/scope bounds, and limits.
  Added only legal Datastore pushdown plus complete finite local post-filtering
  and explicit scan-overflow errors.
- Added compile-negative, frozen-wire round-trip, truth-table, scalar-family,
  provider SQL/pushdown, malformed-wire, tenant, version/system-column, and
  real gRPC integration coverage. Updated package/user documentation and the
  explicit TypeDoc export contract.
- Focused verification passed: ESLint on all changed TypeScript/tests; `tsc -b`;
  tooling typecheck; 123 focused client/storage/provider/Stand tests; 152 native
  QueryService integration tests; and the final expanded focused provider set
  of 75 tests.
- Frozen generation remains unchanged: 39 copied source checksums and 48
  descriptor files match digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`.
- Documentation/generated/release hygiene passed: TypeDoc expectations cover
  22 client and 29 storage exports; generated outputs are clean; release
  readiness reports 16 package imports and 121 relative Markdown links;
  formatting and `git diff --check` pass.
- Native full coverage passed on the final behavior: 93 files passed, 3
  skipped; 1,983 tests passed, 21 skipped. Coverage is 94.38% statements,
  90.01% branches, 94.85% functions, and 94.57% lines. Thresholds were not
  weakened.

## Next Action

Return the uncommitted implementation to the orchestrator for the canonical
specialist review wave.

## 2026-07-22 — Final Exact-Tree Verification

- `pnpm --config.verify-deps-before-run=false verify` passed under native
  loopback-enabled execution on the exact final tree.
- The complete gate passed Node compatibility, Proto generation, build and
  tooling typechecks, repository ESLint plus cleanup enforcement, formatting,
  non-coverage tests, coverage tests and thresholds, TypeDoc/API expectations,
  Proto lint and generated cleanliness, and release readiness.
- Final test result: 93 files passed, 3 skipped; 1,983 tests passed, 21 skipped.
  Final coverage: 94.38% statements, 90.01% branches, 94.85% functions, and
  94.57% lines.
- Final artifact result: 39 copied Proto checksums and 48 frozen descriptor
  files retain normalized digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`;
  TypeDoc expectations include 22 client and 29 storage exports; release
  readiness reports 16 package imports and 121 relative Markdown links.
- No commit, push, merge, or tag was performed by the implementation owner.

## 2026-07-22 — Accepted Review Correction Assignment

- The complete four-lane review wave returned nine deduplicated accepted
  corrections. The same existing `implementer` owns the consolidated batch at
  explicit `gpt-5.6-terra` / `medium`; runtime model self-introspection remains
  unavailable, so the immutable configured role/profile is the recorded
  evidence.
- Ownership remains exclusive to this worktree. No child dispatch, commit,
  push, merge, or tag is permitted. Orchestrator-owned task/review records,
  unrelated files, and the verification-only `node_modules` link remain
  preserved.
- The correction batch is behavior-first: public predicate selection typing;
  bounded cycle-safe client compilation; pre-materialization default candidate
  bounds; whole-plan Datastore legality; pre-enqueue server budgets; exact
  numeric `Any` wrapper validation; FIXED32 unsigned packing; shared
  three-provider conformance; and compile-covered consumer documentation.
- Security remains N/A only while these changes preserve or tighten the
  existing validated QueryService boundary and parameterized provider paths.

## 2026-07-22 — Accepted Review Corrections Complete

- Public predicates/groups now retain their column generic and `where()` ties
  every predicate column to both the selected Projection schema and registered
  column keys. Compile-negative coverage rejects a foreign Projection.
- Client predicate compilation is iterative, cycle-safe, and aligned with the
  T-0056 depth-64/node-10,000 bounds. Cyclic, deep, wide, sparse, empty, and
  malformed graph tests reject before frozen-wire allocation.
- Client/user documentation identifies both consumer-owned `@example/*`
  substitutions, compile-covers `byId`, `all`, and all five comparison helpers,
  and demonstrates exact `Any` state unpacking from `QueryResponse`.
- A framework `candidateLimit` now bounds provider materialization separately
  from semantic result limits. QueryService applies 1,000 candidates for
  absent/zero wire limits; in-memory and MySQL request one sentinel candidate,
  Datastore integrates the bound with its finite scan, and Stand system-column
  plans use the same overflow rule. Overflow errors replace response slicing.
- Datastore pushdown now evaluates whole-plan legality: no more than 30 IDs,
  one inequality property, and that property first in ordering. Illegal plans
  use the existing finite fallback.
- Server composite validation reserves the remaining composite budget before
  child access/enqueue. A getter-instrumented wide malformed tree proves zero
  child reads and zero provider access.
- FIXED32 uses `UInt32Value`; `4_294_967_295` round-trips. Server scalar/enum
  validation now requires the descriptor-compatible exact wrapper type URL;
  incompatible numeric families reject before provider access.
- One shared parameterized conformance fixture now runs against in-memory,
  MySQL, and Datastore, proving identical nested predicate/order/mask/limit
  results and policy rejection without provider access.
- Focused evidence passed: client/tooling/lint 11 DSL tests; 123 consolidated
  client/storage/provider tests; 156 native QueryService tests; TypeDoc/API,
  release, formatting, and diff hygiene gates.
- Final exact-tree
  `pnpm --config.verify-deps-before-run=false verify` passed. Final result: 93
  test files passed, 3 skipped; 1,993 tests passed, 21 skipped. Coverage is
  94.32% statements, 90.00% branches (5,987/6,652), 94.89% functions, and
  94.57% lines. TypeDoc expects 22 client and 30 storage exports; 39 copied
  Proto checksums and all 48 frozen descriptors retain digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`.
- No threshold, security boundary, dynamic-SQL rule, commit, push, merge, or
  tag was changed by the correction owner.

## Next Action

Return the accepted batch to the orchestrator for limited re-review of only
the substantively affected TypeScript/API, documentation,
performance/reliability, and style/maintainability concerns.

## 2026-07-22 — Limited Re-review Final Batch

- Documentation and style/maintainability are CLEAN.
- API retains one README export-ownership mismatch.
- Performance/reliability retains an explicit-limit candidate-bound gap and a
  top-level predicate-count gap.
- Existing implementation owner redispatch: immutable `implementer`, expected
  `gpt-5.6-terra` / `medium`, both explicit. It owns only the three recorded
  corrections and focused verification; no commit, push, merge, tag, or child
  dispatch is authorized.

## 2026-07-22 — Final Limited Re-review Corrections Complete

- The existing immutable `implementer` completed the bounded redispatch at the
  explicitly configured `gpt-5.6-terra` / `medium`. Runtime model
  self-introspection was not exposed on this surface, so no runtime identifier
  is invented; the explicit dispatch and immutable role profile remain the
  available metadata.
- The client README now demonstrates the actual ownership boundary: the
  generated companion exports `TaskListColumnDefinition`, while consumer code
  owns `TaskListColumns` and creates it with `ProjectionColumn.register()`.
- QueryService now supplies the finite 1,000-candidate bound for every valid
  query, including positive semantic limits. In-memory and system-column paths
  request the sentinel candidate before evaluation; MySQL binds the candidate
  sentinel independently of the semantic result limit. Focused ordinary and
  system-column tests prove that 1,001 candidates fail rather than return a
  misleading `limit(1)` result.
- `ProjectionQueryBuilder.where()` rejects the 10,001st distinct top-level leaf
  before adding it to the builder's pending predicate array. The regression
  exercises 10,000 accepted leaves followed by an immediate overflow error
  without calling `build()`.
- RED evidence observed each prior gap: the 10,001st top-level predicate was
  accepted, positive-limit plans carried no `candidateLimit`, ordinary and
  system positive-limit reads returned success over 1,001 candidates, and
  MySQL bound the semantic limit rather than the candidate sentinel.
- GREEN focused evidence passed: 209 affected client/MySQL/server tests, with
  all 156 server service tests running under native loopback access; build and
  tooling typechecks, targeted ESLint, TypeDoc/API checks, formatting, and diff
  hygiene passed.
- The first full verification attempt passed all 1,994 tests but exposed a
  89.95% global branch-coverage result. No threshold was changed. Focused
  descriptor-without-ID and explicit MySQL option tests added deterministic
  coverage for previously unexercised branches, and their 54-test/typecheck/
  lint gate passed.
- Final exact-tree `pnpm --config.verify-deps-before-run=false verify` passed:
  93 test files passed and 3 skipped; 1,995 tests passed and 21 skipped.
  Coverage is 94.33% statements (11,400/12,085), 90.01% branches
  (5,986/6,650), 94.89% functions, and 94.58% lines. TypeDoc retains 22 client
  and 30 storage exports; release readiness reports 16 package imports and 121
  relative Markdown links. All 39 copied Proto checksums and 48 frozen
  descriptors retain digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`.
- No child dispatch, commit, push, merge, tag, threshold change, or
  orchestrator-owned review disposition change was performed.

## Next Action

Return the verified final correction batch to the orchestrator for the limited
TypeScript/API and performance/reliability re-review.

## 2026-07-22 — Final Limited Re-review Accepted

- TypeScript/API: CLEAN; generated definition and consumer registration
  ownership are documented accurately.
- Performance/reliability: CLEAN; explicit-limit candidate bounding and the
  top-level predicate node budget are resolved without regressions.
- Documentation and style/maintainability remain CLEAN; security remains N/A
  for the recorded existing-boundary reason.
- All required review concerns now have accepted dispositions. The
  orchestrator owns the independent final verification, task commit/push,
  merge/push, and post-merge verification sequence.

## 2026-07-22 — Independent Final Verification

- Orchestrator-run exact-tree
  `pnpm --config.verify-deps-before-run=false verify` passed with exit 0 under
  native loopback access.
- 93 test files passed and 3 skipped; 1,995 tests passed and 21 skipped.
- Coverage: 94.33% statements, 90.01% branches (5,986/6,650), 94.89%
  functions, and 94.58% lines.
- TypeDoc retains 22 client and 30 storage exports. All 39 copied Proto
  checksums and 48 descriptor digests remain exact; generated-source and
  release-readiness checks pass with 16 package imports and 121 Markdown
  links.
- T-0057 is complete and ready for task commit, immediate task-branch push,
  merge to `main`, immediate main push, and post-merge verification.

## 2026-07-22 — Integration And Post-merge Correction

- Task commit `f8c54821` was pushed to
  `origin/task/T-0057-projection-query`; merge commit `a40bec0f` was pushed to
  `origin/main` immediately after merge.
- The first post-merge run stopped before tests because root dependency links
  lacked the frozen Buf executable. Offline frozen relinking restored 308
  cached packages with zero downloads and no lockfile change.
- The rerun then exposed a deterministic cleanup-rule failure: public
  `StorageQueryCandidateLimitError` had five semantic name components. The
  mechanical correction is `QueryCandidateLimitError` across implementation,
  exports, consumers, README, and API-doc expectations; behavior and export
  count remain unchanged.
- Correction typecheck, ESLint/cleanup, and TypeDoc/API checks pass. A limited
  public-API re-review precedes the final full post-merge rerun and correction
  commit/push.
- The post-merge limited TypeScript/API re-review is CLEAN: all live public and
  runtime references use `QueryCandidateLimitError`, no stale identifier
  remains, behavior is unchanged, and the storage export count remains 30.
- Final corrected post-merge
  `pnpm --config.verify-deps-before-run=false verify` passed with exit 0: 93
  files passed and 3 skipped; 1,995 tests passed and 21 skipped; coverage is
  94.33% statements, 90.01% branches (5,986/6,650), 94.89% functions, and
  94.58% lines. TypeDoc, all 39 Proto checksums and 48 descriptors,
  generated-clean, and release readiness are green.
- The correction is ready for its required commit and immediate `main` push;
  T-0057 is durably closed when origin contains that commit.
