# T-0137 Work Log

## 2026-08-08 Baseline

- Worktree created from pushed T-0136 commit `35b2b096` on
  `task/T-0137-stand-subscriptions`.
- Frozen offline dependency installation and Proto generation succeeded.
- The server composite build reaches ten stacked `RecordSpec.schema` failures.
  Three in `stand/subscription-registry.ts` are owned by T-0137; the other seven
  server consumers remain assigned to later Wave 8 slices.

## Architecture Dispatch

- Existing role: `requirements_splitter`.
- Expected and explicitly dispatched profile: `gpt-5.6-sol` / `high`.
- Runtime self-introspection is recorded if exposed; otherwise immutable
  configured role/profile and the limitation are accepted unless a mismatch or
  fallback is visible.

## 2026-08-08 Architecture Result

- The existing requirements splitter completed under explicit
  `gpt-5.6-sol` / `high`. Runtime self-introspection was unavailable; no
  mismatch or fallback was visible.
- It froze a one-row/one-handle CAS design over approved
  `spine.client.SubscriptionRecord`, exact create/activate/delete/cleanup
  transitions, approved-field polling identity, ten-second best-effort
  convergence, lifecycle semantics, provider selectors, and complete removal
  of invented recovery/capacity persistence.
- No human decision blocker remains. The necessary bounded-context/root-export
  ownership expansion is limited to removing the old global definition-capacity
  API; per-service cancellation concurrency remains unchanged.

## Implementation Dispatch

- Existing role: `implementer`.
- Expected and explicitly dispatched profile: `gpt-5.6-terra` / `medium`.
- Runtime self-introspection is recorded if exposed; otherwise the immutable
  configured role/profile and limitation are accepted unless a mismatch or
  fallback is visible.

## 2026-08-08 Implementation Checkpoint

- Existing `implementer` role is configured and explicitly dispatched as
  `gpt-5.6-terra` / `medium`; runtime self-introspection is unavailable on this
  surface and no fallback is visible.
- Replaced the first runtime seams with the approved `SubscriptionRecord`
  codec, direct subscription record storage, and attachment identity based on
  subscription plus creation time. Removed the builder capacity method and the
  private Proto source/manifest/checksum entries.
- The owned bounded-context capacity-removal fallout is resolved. Focused server
  TypeScript compilation reports no errors in the edited Stand files. The
  composite package still has exactly six unowned `RecordSpec.schema` migration
  errors in tenant-index, delivery-attempts, inbox-records, sharded-work-
  registry, and durable-subscription-bindings. No completion or test-pass claim
  is made.
- Focused approved-record registry tests pass (10/10), and focused runtime
  lifecycle/polling tests pass (4/4). A workspace-global coverage invocation
  instruments the monorepo and fails its global threshold despite 90.32% codec
  and 93.02% registry line coverage; remaining coverage must use a
  changed-source include.

## 2026-08-08 Continuation Checkpoint

- Replacement owner remains the existing `implementer`, explicitly dispatched
  as `gpt-5.6-terra` / `medium`. Runtime self-introspection is unavailable on
  this surface; the immutable configured role/profile is recorded and no
  fallback is visible.
- Replaced the obsolete provider conformance suite. The focused test uses the
  existing `mysql2` mock seam with a configured MySQL
  `SubscriptionRecordSchema` table and runs registry create/get/delete through
  that table. It also configures a Datastore `SubscriptionRecordSchema` creator,
  runs the same lifecycle, and proves that creator was selected.
- The new focused provider test passes (2/2). The first package-local Vitest
  invocation found no files because this repository uses workspace-root test
  includes; the corrected workspace-root command supplied the passing evidence.
- Updated `packages/server/README.md` and `REFERENCE.md` to describe one
  `SubscriptionRecord` per ID, provider configuration, and deletion without a
  revision. The records no longer claim capacity, control, staging, or recovery
  behavior.
- Known downstream limitation remains unchanged: six unowned server composite
  `RecordSpec.schema` migration failures in tenant-index, delivery-attempts,
  inbox-records, sharded-work-registry, and durable-subscription-bindings are
  assigned to later Wave 8 tasks. `verify:task` additionally identifies four
  unowned example migration failures: Orders still imports the retired
  Datastore options/create API, and Message Board still has two retired
  `RecordSpec.schema` calls plus retired Datastore creation.
- Scoped coverage ran before the full profile: 16 focused tests passed, but the
  three T-0137 runtime sources total 70.09% statements, 60.52% branches,
  70.12% functions, and 74.41% lines. This does not meet the required 90% in
  every dimension and needs further behavior tests before review.
- Focused ESLint is clean for the replacement provider test. Existing T-0137
  runtime sources are not review-ready: `subscription-registry.ts` has twelve
  `require-await`/non-null assertion findings and
  `subscription-runtime.ts` has two unnecessary assertions. The TSDoc check
  also reports missing summaries/parameter/return documentation in the new
  public registry API. Those owned runtime quality findings require a bounded
  correction; no later-task consumer was edited.
- Proto generation, Buf lint, and current generated-output cleanliness pass.
  `git diff --check` passes. `docs:check:generated` cannot reach the audience
  check because its API check stops at the six unowned server TypeScript errors.

## 2026-08-08 TSDoc And Stand-Test Lint Correction

- Completed the owned public registry TSDoc layout and structured parameter and
  return documentation. The source has no remaining TSDoc checker findings.
- Corrected the owned mirrored Stand test to use subscription contents plus
  creation time as attachment identity, replacing its retired revision fence.
- Exact mechanical evidence passes: `pnpm lint:tsdoc` has no
  `subscription-registry.ts` findings, ESLint on `stand.test.ts` passes,
  Prettier checks the corrected registry and test, and `git diff --check`
  passes. This micro-batch deliberately does not remeasure coverage.

## 2026-08-08 Final Changed-Source Coverage Correction

- Existing `implementer` role remained explicitly dispatched as
  `gpt-5.6-terra` / `medium`. Runtime self-introspection is unavailable on this
  surface; immutable configured role/profile is the available metadata and no
  fallback is visible.
- Added public behavior tests only at the `StorageFactory` / `RecordStorage`
  seam. They cover non-atomic storage rejection, absent/conflicting/matching
  create rereads after CAS failure, false-CAS retry for activation/deletion/
  cleanup, malformed stored records, durable codec bounds, and idempotent
  registry close. No runtime source or coverage configuration changed.
- Exact focused coverage suite (`subscription-registry`, provider conformance,
  runtime, and Stand tests) passes 80/80: 96.86% statements, 93.08% branches,
  97.43% functions, and 98.52% lines across the three changed runtime sources.
  This clears the required 90% changed-source threshold in every metric.

## 2026-08-08 Deterministic Verification Correction

- Existing `implementer` role remains explicitly `gpt-5.6-terra` / `medium`.
  Runtime self-introspection is unavailable; immutable configured role/profile
  is the available metadata and no fallback is visible.
- Prettier wrote the named owned T-0137 Proto, bounded-context, subscription
  record/registry, and context-test files. Removed stale client-schema imports
  from the bounded-context test and changed two failing runtime-test overrides
  to return rejected promises rather than use unnecessary `async` callbacks.
- Exact changed-file ESLint is clean except for 11 inherited error-typed
  cascades at bounded-context test lines 2110 and 2155–2169. They stem from the
  known six unowned downstream `RecordSpec.schema` migration failures; the same
  ESLint command excluding that affected test passes cleanly.
- `pnpm lint:tsdoc` still reports 11 inherited committed T-0134 MySQL findings
  in `packages/storage-rdbms/src/mysql/{testing,errors,scope}.ts`. They were
  not edited by this correction batch.
- Requested Prettier check and `git diff --check` pass. Exact scoped coverage
  remains green: 80/80 tests; 96.86% statements, 93.08% branches, 97.43%
  functions, and 97.84% lines.

## 2026-08-08 Exact-Optional Entry Narrowing Correction

- Existing `implementer` assignment remains explicitly `gpt-5.6-terra` /
  `medium`; runtime self-introspection is unavailable, so the immutable
  configured profile is the available metadata and no fallback is visible.
- RED: direct `pnpm exec tsc -b packages/server --pretty false` reported the
  six known deferred downstream `RecordSpec.schema` errors plus three owned
  TS2379 errors in subscription-registry `expiresAt()` calls (lines 254, 316,
  and 445). The retained public expiry-boundary behavior test was then run
  before changing production code.
- GREEN: `expiresAt()` now accepts the public entry contract and validates that
  it is pending with a defined expiry itself, preserving the malformed-record
  error without assertions or casts. Direct server inventory now reports only
  the six assigned downstream errors; the focused registry behavior test passes
  29/29.
- Exact scoped coverage passes 80/80: 96.86% statements, 93.16% branches,
  97.43% functions, and 97.49% lines. Owned ESLint subset, Prettier, and
  `git diff --check` pass. The complete changed-file lint has only the already
  recorded 11 inherited bounded-context typed cascades.

## 2026-08-08 Consolidated Review Correction Batch (in progress)

- Existing `implementer` assignment remains explicitly `gpt-5.6-terra` /
  `medium`; runtime self-introspection remains unavailable, with immutable
  configured role/profile as the available metadata and no visible fallback.
- RED/GREEN fixed the provider-visible RecordColumn spelling to
  `when_activation_expires`, changed the lifecycle entry to a pending/active
  discriminated union (`pendingUntil` required only for pending), replaced
  JSON equality with canonical Protobuf bytes, and changed attachment identity
  to canonical subscription bytes plus `createdAt`.
- Cleanup now requests exactly 26 pending records ordered by activation expiry
  then ID, validates every returned record through `StandSubscriptionRecords`,
  deletes at most 25 expired rows, and reports `more` only for an expired
  observed 26th row. In-memory cleanup applies the same ordered 25-plus-one
  selection. The retired private-Proto contract test now requires absence; the
  API inventory no longer exports `StandCapacityError`; the generation workflow
  removes the five stale private Stand generated artifacts after publication.
- Focused evidence: 103 tests across the Stand registry/runtime/provider,
  approved-record, and Proto-workflow suites pass. Scoped Stand coverage is
  97.03% statements, 92.94% branches, 97.64% functions, and 97.95% lines.
  `pnpm proto:generate` passes source, style, example-quality, and descriptor
  checks. Direct server TypeScript inventory still reports only the six known
  unowned downstream `RecordSpec.schema` migrations.
- Added end-to-end durable restart/poll coverage: a fresh durable registry and
  runtime rediscover an already-active definition and detach its observer after
  a remote delete on the next ten-second reconciliation. A same-millisecond
  changed-subscription regression proves attachment replacement. The converged
  focused coverage run passes 146 tests at 97.03% statements, 92.94% branches,
  97.64% functions, and 97.95% lines; changed-file ESLint, Prettier, and diff
  checks pass. The remaining API checker limitation is solely the six known
  unowned downstream server migrations.

## 2026-08-08 Wave Two Deterministic Correction

- Corrected TSDoc placement: base-entry orphan blocks are removed, pending and
  active union members document their actual `phase` and `pendingUntil`
  contracts, and the Proto-workflow helper uses the repository block layout.
- README and reference now state the exact physical columns, provider query of
  26 pending records ordered by `when_activation_expires` then ID, at-most-25
  deletion bound, and expired-26th lookahead rule.
- Focused regressions prove a duplicate create with an ActorContext Timestamp
  `seconds: bigint` returns `existing` (the prior JSON equality would throw),
  cleanup sends its pending-status filter and rejects a malformed queried row,
  and full `generateTargets()` publication removes stale retired artifacts.
- Final focused evidence: 149 tests pass; scoped coverage remains 97.03%
  statements, 92.94% branches, 97.64% functions, and 97.95% lines. Exact
  owned ESLint, Prettier, and `git diff --check` pass; `pnpm proto:generate`
  passes source verification, authored style, example quality, and frozen
  descriptor compatibility. No commit, push, or merge was performed.

## 2026-08-08 Final Acceptance

- Documentation re-review found one duplicate “bounded complete snapshot”
  phrase; the deterministic word removal was applied. API/TSDoc and
  maintainability re-reviews are clean, and reliability was already clean.
- Independent final evidence: focused Stand suite 87/87 with 97.03%
  statements, 92.94% branches, 97.64% functions, and 97.95% lines; Proto and
  workflow contracts 71/71; Proto generation, Buf lint, generated cleanliness,
  owned ESLint, Prettier, and diff integrity all pass.
- Direct server TypeScript inventory contains only the six known downstream
  `RecordSpec.schema` migrations. No T-0137 TypeScript error remains.
