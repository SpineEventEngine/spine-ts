# T-0142 Work Log

## 2026-08-08 Baseline

- The T-0141 final repository build identifies the owning stale example APIs:
  removed `DatastoreStorageOptions`/static factory creation, delivery quarantine
  records/configuration, browser fingerprints, durable-subscription disposal,
  retired RecordSpec `schema`, and session-revocation persistence.
- Exact inventory also finds dedicated Message Board quarantine and revocation
  source/tests. Frozen scope requires deletion without a replacement facility or
  record.

## Implementation Dispatch

- Existing role: project `implementer`.
- Expected and explicitly dispatched profile: `gpt-5.6-terra` / `medium`.
- Ownership is limited to `examples/**` source/tests/manifests and T-0142
  task/work records. Framework production APIs and the review record are
  read-only.
- The implementer must preserve all existing changes, remain the only
  production writer, and not spawn subagents.
- Runtime metadata will be recorded when exposed; otherwise the immutable
  configured role/profile and metadata limitation satisfy the acceptance gate.

## Cross-Example Runtime Regression Assignment

- Clean rebuilt Message Board and unmodified To-Do tests prove a shared
  framework boundary: accepted commands leave Aggregate state, EventStore rows,
  and Projection rows empty with in-memory storage. Generated handlers/labels,
  example provider wiring, and stale assertions are ruled out.
- The finding returns to the existing implementation context as required by the
  protocol. Explicit configured profile remains `gpt-5.6-terra` / `medium`.
  Read/diagnostic scope is command intake through repository commit and event
  dispatch. Framework edits are unauthorized until the exact causal boundary
  is demonstrated; any eventual fix must be the smallest bounded correction,
  add focused regression evidence, and must not restore retired delivery state
  or APIs. No subagents may be spawned.
- Independent read-only codebase tracing is assigned to an `explorer` with
  explicit profile `gpt-5.6-terra` / `medium`. Its sole question is where an
  accepted local command can settle without Aggregate/EventStore persistence
  after the stacked runtime changes. It may inspect diffs/tests but must not edit
  files or spawn subagents. Runtime metadata handling follows the same gate.

## RED: Example Removal Boundary

- Added a focused static migration test over the owned Orders and MessageBoard
  source files. It requires builder/client Datastore setup and the absence of
  removed static creation, quarantine, revocation, browser fingerprint, and
  durable-subscription disposal facilities, including both dedicated source
  files.
- The next focused test run is expected to fail against the current legacy
  example layout before any production source is edited.
- RED result: the test failed at the old Orders `DatastoreStorageOptions`
  boundary, proving the stale factory construction was still present.

## GREEN: Current Example Provider And Removal Boundary

- Replaced Orders and MessageBoard static Datastore factory construction with
  `newBuilder().setClient(client).build()` and made the client a caller-owned
  composition dependency.
- Removed MessageBoard quarantine and session-revocation source/tests/config,
  browser fingerprint options, and durable-binding dispose options without
  adding a replacement record, facility, marker, or alias.
- Compile follow-up: `DurableSubscriptionBindings` also rejects the retired
  lease/batch/record-size options. Replaced that entire legacy option group with
  its current required cleanup callback; the migration test now rejects every
  removed option name.
- Direct deployment fixtures retained the old caller signature and quarantine
  assertion. Updated them to supply the caller-owned Datastore client and to
  assert quarantine absence; updated the native board fixture to omit retired
  fingerprint/dispose options.
- Focused fixture follow-up distinguishes the retired durable binding option
  from the still-required in-memory binding cleanup callback. Restored the
  latter and removed the obsolete durable `.durable` assertion.
- The exact source scan found one remaining browser fingerprint in the owned
  web interop harness. Extended the removal test to that configuration before
  deleting the option.
- RED result: the extended test failed on that web-harness fingerprint; GREEN
  removes it without changing its in-memory lifecycle cleanup behavior.
- The removal test now scopes `dispose` absence to durable deployment bindings;
  the web harness retains its unrelated in-memory subscription and resource
  cleanup callbacks, which are not forbidden replacement persistence.

## Model Registry Failure Investigation

- Reproduced all three `model-registry.test.ts` failures after rebuilding the
  MessageBoard app output: accepted posts leave no in-memory entity state,
  Event Store rows, or projection rows. Rebuilding therefore rules out stale
  example output.
- Generated handler metadata names the current `BoardMessageAggregate`,
  `PostMessage`, `MessagePosted`, `BoardMessage`, and `BoardMessageView`
  schemas, ruling out an example-generated payload-label mismatch.
- The failing path uses `InMemoryStorageFactory`, not the migrated Datastore
  composition. The same no-projection/no-row symptom occurs in the unmodified
  To-Do black-box suite. The causal boundary is upstream command
  persistence/delivery lifecycle behavior in the stacked train, not an
  example-owned provider, configuration, or stale assertion. No framework API
  or speculative example workaround was changed.
- Runtime tracing continues under the corrective assignment. Public context
  inspection shows the expected PostMessage command route and MessagePosted
  event route are registered. The focused MessageBoard test now records whether
  the aggregate handler is invoked before its existing persistence assertion.

## Cross-Example Producer-ID Correction

- Focused delivery instrumentation proved the healthy local shard fence is not
  the failure: every observed `ShardedWorkRegistry.validateOwnership()` result
  remained defined. The aggregate handler executes, then Event history rejects
  the produced event with `Event history requires an EventContext producer ID.`
- The exact source was `RepositorySignals.runtimeProducerId()`: it only reads
  finite scalar IDs. The MessageBoard and To-Do aggregates use generated
  message-valued IDs, so their otherwise accepted produced events had no
  producer ID when the current entity-history contract validated them.
- The bounded repository correction now packs the actual repository ID field
  into every Aggregate and Process Manager produced-event context. It leaves
  `SignalMetadata`'s scalar input and rejection behavior unchanged.
- RED/GREEN evidence: the new MessageBoard message-ID EventStore regression
  failed before the correction and passes after it; the established scalar
  producer-ID repository regression also passes. The original projection
  subscription timeout remains a separate downstream delivery issue to trace.

## Cross-Example Projection Routing Correction

- The remaining projection timeout had a second, adjacent message-ID boundary:
  projection routing read a packed message producer as an unreadable scalar.
  Aggregate state and EventStore persistence were healthy, but local event
  delivery could not route the stored event to its Projection.
- Routing now unpacks a producer using the target message-ID schema only when
  the target ID field is message-valued, and verifies binary equality with the
  event's first-field target. Scalar producer routing, malformed producers, and
  rejection handling retain their existing paths.
- RED/GREEN evidence: the new repository routing regression failed with
  `Repository event routing requires a readable producer ID.` and then passed;
  all seven MessageBoard model-registry tests, including board projection and
  subscription delivery, pass after a clean server/example build.
