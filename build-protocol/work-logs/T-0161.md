# T-0161 Work Log

## 2026-08-11 - Startup

- Starts from `origin/main@68b2c428` after T-0160's state-subscription metadata cutover.
- Classification: high-risk public routing plus durable System-event delivery.
- Frozen planner assignment was requirements splitter `gpt-5.6-sol` / high; implementation is existing implementer `gpt-5.6-terra` / medium, both explicit, no subagents. Runtime metadata is unavailable on this surface.
- Scope is Projection-only: reuse `EntityStateChanged`, the paired System EventBus, `UPDATE_SUBSCRIBER`, `ProjectionInbox`, typed targets, and current tenant paths. Aggregate and Process Manager state subscription routes must fail at construction.
- TDD is active: production routing changes follow observed focused RED tests. Product Markdown remains Wave 10.

Status: inspecting seams and preparing RED coverage.

## 2026-08-11 - State Route-plan Checkpoint

- Public declarations, Projection-only option validation, and the separate
  repository System dispatcher are published in the first three checkpoints.
- The route-plan slice now indexes descriptor-backed state subscriptions,
  unpacks matching `EntityStateChanged.newState`, resolves custom/default
  routes, validates and stably deduplicates target IDs, and ignores unrelated
  state types.
- The generated build passes. Focused RED/GREEN evidence passes for
  Projection-only construction, the first compatible state field, unrelated
  state ignore, one-call multicast routing, stable deduplication, and frozen
  targets (3 focused repository behaviors plus the 2 declaration tests).
- Changed-source ESLint, Prettier, and `git diff --check` pass.

Status: route-plan checkpoint ready; durable handoff/replay and full adversarial
coverage remain.

## 2026-08-11 - Durable State Delivery

- A real System EventBus admission evaluates one state route once, stably
  deduplicates its multicast targets, writes through the existing
  `UPDATE_SUBSCRIBER` Projection inbox path, and replays the stored typed target
  without invoking routing again.
- The state subscriber runs once per unique target and commits independent
  Projection state through the existing transaction/Stand flow. An empty route
  invokes no subscriber and creates no Projection state.
- Focused state-update behavior passes 4/4; the generated build, changed-test
  ESLint, Prettier, and `git diff --check` pass.

Status: durable handoff/replay behavior is green; adversarial route declarations,
tenant coverage, fixture convergence, coverage, and mechanical gates remain.

## 2026-08-11 - Adversarial And Tenant Matrix

- Route precedence is covered end to end: exact schema, direct `(is)`, file
  `(every_is)`, replacement default, then the built-in first compatible field.
- Non-array results, more than 1,000 targets, and a late malformed ID fail before
  any subscriber or Projection state write. Unregistered exact routes fail at
  repository construction.
- Multitenant delivery stores and reads the Projection only in the originating
  tenant; missing tenant context fails before persistence.
- Generated build, exact changed-test ESLint, Prettier, diff check, and 11
  focused tests pass.
- The broad repository file currently retains 64 pre-existing fixture failures
  exposed by T-0160's v2/state-provenance cutover: generated registry v1 records
  and tests that use an Entity state schema as a domain Event. These fixtures
  are outside the new runtime behavior and must converge before release closure.

Status: core behavior matrix green; coverage/mechanical gates and review remain.

## 2026-08-11 - Built-in Route Compatibility

- Two focused RED tests proved that the built-in route skipped an empty first
  compatible field and that repository construction accepted a subscribed
  state with no field compatible with the Projection ID.
- The built-in route now selects the first descriptor-compatible field in
  declaration order, then validates that field's actual value. It never falls
  through to a later same-shaped field when the selected field is empty.
- Repository construction now fails when a subscribed state has neither a
  selected custom/replacement route nor a compatible built-in field.
- The focused matrix passes 13/13 and the generated TypeScript build passes.

Status: behavior implementation converged; coverage, mechanical gates, and
specialist review remain.

## 2026-08-11 - Fixture And Coverage Convergence

- T-0160's descriptor-provenance cutover had left 64 repository fixture
  failures because Entity state schemas were still used as domain Events and
  generated registry fixtures still declared v1. A non-Entity
  `ProjectionEvent` fixture now carries those Event scenarios; generated
  registries use v2, while `ProjectionState` remains reserved for Entity state
  and state subscriptions.
- The complete repository routing plus declaration suites now pass 232/232.
  This includes direct System-dispatcher lifecycle behavior, malformed System
  envelopes, corrupted durable replay rows, message-valued built-in IDs, and
  context normalization.
- Fresh V8 instrumentation over the four changed production sources reports
  changed-range coverage of 95.85% statements (185/193), 92.86% branches
  (117/126), 100% functions (43/43), and 96.13% lines (174/181). Whole-file
  thresholds are intentionally not used for the large shared repository and
  context modules.

Status: implementation and coverage converged; deterministic gates and review
remain.

## 2026-08-11 - Mechanical Pre-review Gate

- Generated build and tooling typecheck pass.
- Changed-file ESLint, cleanup enforcement, TSDoc enforcement, logging
  containment, API-documentation inventory/TypeDoc generation, Prettier, and
  `git diff --check` pass.
- The API documentation inventory now includes the public `StateUpdateRoute`
  and `StateUpdateRouting` exports.

Status: ready for the single specialist review wave.
