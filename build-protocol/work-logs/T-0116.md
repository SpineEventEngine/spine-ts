# T-0116 Work Log

## Task Identity

- Branch: `task/T-0116-lifecycle-system-events`.
- Worktree: `.worktrees/T-0116-lifecycle-system-events`.
- Baseline: `origin/main@6523a68c`.
- Started: 2026-08-05.
- Ended: pending.
- Coverage: pending.
- Integration: pending.

## Start Boundary

- T-0115 is reviewed, release-verified, merged, post-merge verified, and
  remotely synchronized.
- T-0113 freezes the lifecycle-event scope and ordering boundary.
- Desktop supports explicit model/reasoning dispatch.
- The requirements-splitter assignment is explicitly `gpt-5.6-sol` / high.

## Pending Work

- Bootstrap and focused baseline.
- Accept the schema/emission/RED-test map.
- Dispatch one bounded implementer with focused behavior tests.
- Run deterministic gates and one relevant specialist review wave.
- Run one converged release gate, integrate, post-merge verify, and push.

## Architecture Redispatch — 2026-08-05

- `/root/t0116_split` mapped all six frozen schemas, found the shared
  post-`EntityCommitStorage.commit()` seam, identified lifecycle-only commits
  currently mislabeled as `EntityStateChanged`, and found that Stand observes
  only state changes. It did not return its final bounded map after two explicit
  convergence requests and was interrupted to avoid further delay.
- Existing role redispatch: requirements splitter `/root/t0115_split`, reused
  for the remaining exact ID/order/time/version/tenant/kind sources and RED
  sequence only.
- Expected and explicitly dispatched model: `gpt-5.6-sol`.
- Expected and explicitly dispatched reasoning: `high`.
- Runtime self-introspection is unavailable; immutable configured role/profile
  metadata applies.

## Accepted Architecture Handoff — 2026-08-05

- The first requirements-splitter result is accepted for its schema map,
  durable post-commit seam, current lifecycle/state-change bug, and Stand gap.
  Runtime self-introspection was unavailable; the explicit immutable
  `requirements_splitter`, `gpt-5.6-sol` / high profile is the available
  metadata and no mismatch was visible.
- The second context did not return within the bounded redispatch window and
  was interrupted. Direct schema/code inspection completed the remaining
  bounded map in `build-protocol/reports/T-0116_LIFECYCLE_MAP.md` without
  broadening scope or inventing a new ID/clock policy.
- Existing role implementation owner: `/root/t0116_impl`.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Baseline evidence: generated build, seven focused server files and 402 tests,
  and server typechecking pass.

## Implementation Checkpoint — 2026-08-05

- Implementer role: existing `/root/t0116_impl`, explicitly dispatched as
  `gpt-5.6-terra` / medium. Runtime self-introspection is unavailable on this
  surface; the immutable configured role/profile is the available metadata and
  no visible mismatch occurred.
- RED: `publishes created before state-changed after the first committed
aggregate transition` timed out while waiting for the absent `EntityCreated`
  event (focused Vitest invocation, 2026-08-05).
- GREEN: repository post-commit publication now derives the frozen ordered
  lifecycle sequence from committed prior/current state and lifecycle flags.
  It routes only through the paired System EventBus and records post-commit
  dispatch failures without affecting the committed record.
- GREEN: System Stand subscriptions now render archive/delete as removal
  updates and unarchive/restore through existing state rendering, with a single
  composite System-bus observer cleanup handle.
- Focused evidence: `pnpm exec vitest run
packages/server/test/repository/repository-routing.test.ts
packages/server/test/stand/subscription-observer.test.ts` — 168 passing;
  `pnpm exec tsc --noEmit -p packages/server/tsconfig.json`; changed-file
  Prettier and ESLint; `git diff --check` all passed.
- Remaining: expand lifecycle-family/no-op/multitenant/failure coverage and run
  the selected converged verification profile before review.

## Subscription Wire Correction — 2026-08-05

- Checkpoint `c6d2b3bb` preserves the durable subscription definition on
  archive/delete removal updates, matching ordinary state-update rendering.
- Evidence: focused subscription-observer suite (11 passing), server typecheck,
  changed-file Prettier/ESLint, and `git diff --check` all passed before push.

## Aggregate Lifecycle Proof — 2026-08-05

- Added focused deterministic proof that a committed Aggregate archive,
  unarchive, delete, and restore emits only its matching lifecycle System event
  (not `EntityStateChanged`), with the state type, signal type, and committed
  version. A duplicate archive assignment is silent.

## Projection/Process Manager Rehydration Correction — 2026-08-05

- RED: a seeded Projection lifecycle-only archive emitted no System event.
- Cause: Projection and Process Manager entity constructors omitted persisted
  archive/delete flags during repository rehydration.
- GREEN: both paths now pass the stored lifecycle (or false defaults); the
  focused Projection archive-after-rehydration test emits `EntityArchived`.

## Process Manager Archive Proof — 2026-08-05

- The retained archive-only PM fixture proves handler routing after a seeded
  record and `EntityArchived` publication without a state-change event. The
  earlier multi-transition timeout was a fixture discriminator error, not a
  Process Manager publishing defect.

## Deleted Record Restore Correction — 2026-08-05

- RED: PM restore persisted `deleted: false` but emitted no `EntityRestored`.
- Cause: internal Projection/PM loaders replaced deleted records with default
  state, while publication used public Stand reads that omit deleted records.
- GREEN: internal loaders retain a deleted record's state, and publication uses
  `loaded.current.state`; PM archive/unarchive/delete/restore now emit their
  matching lifecycle events without creation/state-change noise.

## Projection Lifecycle Matrix — 2026-08-05

- Projection archive/unarchive/delete/restore emits the exact ordered lifecycle
  schemas, suppresses duplicate archive, and retains the committed state across
  delete/restore. The same immutable implementer profile applies; runtime
  self-introspection remains unavailable on this surface.

## Lifecycle Silence Proof — 2026-08-05

- Rejection and aggregate/projection storage-failure tests now capture every
  lifecycle schema and remain silent, proving publication occurs after the
  durable commit outcome. Aggregate duplicate archive no-op is covered by the
  lifecycle matrix checkpoint.

## Conflict Silence Proof — 2026-08-05

- The existing optimistic aggregate conflict seam now captures all six
  lifecycle schemas and proves the losing transition posts none.

## Post-Commit System Failure Proof — 2026-08-05

- Existing best-effort System follow-up policy is retained: rejected lifecycle
  posts are recorded as dispatch diagnostics, not retried and never rolled
  back. The focused test proves the second aggregate state/version is durable
  after two rejected publication attempts.

## Tenant Context Proof — 2026-08-05

- Multitenant lifecycle System events retain each causing command's actor tenant
  in EventContext, with same-ID tenant A and B execution remaining distinct.

## System Stand Lifecycle Rendering — 2026-08-05

- Composite System subscriptions render archive/delete as identity-preserving
  removals and unarchive/restore as ordinary state updates.

## Subscription Tenant Isolation — 2026-08-05

- A tenant-A lifecycle observer ignores an identical tenant-B archive envelope
  and renders only tenant A's removal.

## Lifecycle Filter Correction — 2026-08-05

- RED: archive removal leaked to a nonmatching ID-filtered subscription.
- GREEN: removal rendering evaluates `last_state` with the existing matcher;
  only subscriptions that previously matched receive removal updates.
- Unarchive/restore current-state updates likewise pass through the existing
  matcher: nonmatching rows are silent and matching rows render exactly once.

## Continuation Test Matrix Assignment — 2026-08-05

- Existing implementation owner `/root/t0116_impl` continues without
  rediscovery.
- Ownership: focused proofs for lifecycle-only transitions across Aggregate,
  Process Manager, and Projection; multitenant metadata/isolation; no-op,
  rejected, conflict/rollback silence; deterministic order/fields; and
  post-commit System publication failure isolation.
- Production changes are permitted only when a focused RED proof exposes a
  contract defect.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Runtime self-introspection is unavailable; immutable role/profile metadata
  applies.

## Catch-Up Reconstruction Architecture Assignment — 2026-08-05

- Existing requirements splitter `/root/t0116_split` is redispatched for a
  read-only, bounded map of the execution-mode seam separating explicit
  lifecycle restore from catch-up/cleared-row reconstruction.
- Expected and explicitly dispatched model: `gpt-5.6-sol`.
- Expected and explicitly dispatched reasoning: `high`.
- Runtime self-introspection is unavailable; the immutable configured
  requirements-splitter role/profile is the available runtime metadata.
- The splitter may not edit files, spawn children, or broaden T-0116.
