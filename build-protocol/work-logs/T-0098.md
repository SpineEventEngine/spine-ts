# T-0098 Work Log

## 2026-08-03 — Task frame

- Started from pushed `origin/main@af72de4b` in an ignored isolated worktree;
  the coordination checkout's unrelated README and protected human-review
  files remain untouched.
- Classified the bounded shared-build correction as standard and recorded one
  existing implementation owner with explicit expected
  `gpt-5.6-terra` / medium.
- Selected systematic debugging, test-driven development, and isolated
  worktree skills before governed action. No instruction conflict exists.
- Runtime self-introspection is unavailable on this surface. The immutable
  configured role and explicit dispatch fields will be the acceptance evidence
  unless a visible mismatch or inherited fallback occurs.
- Next: push this durable frame, reproduce the clean install exactly, trace the
  two output classes to their independent causes, and establish RED tests.

## 2026-08-03 — Root cause and RED regression

- Implementation owner: existing `implementer` role with explicit configured
  `gpt-5.6-terra` / medium dispatch. Runtime self-introspection remains
  unavailable, so no actual runtime metadata can be recorded beyond that
  immutable configured profile and limitation.
- Root cause: `packages/proto-tools/package.json` declared `spine-proto` at
  `./dist/src/cli/spine-proto.js`, which does not exist until TypeScript builds.
  During a clean workspace installation pnpm therefore cannot create that
  package link. The ten warnings are five initial link attempts plus five
  relink attempts. Separately, the repeated Scope/policy/package blocks in the
  interactive transcript are pnpm progress redraws, not repeated
  dependency-policy execution.
- Pattern comparison: `packages/client-node` exposes its package executable via
  a tracked JavaScript source file. The smallest matching boundary for
  proto-tools is a tracked launcher that imports its compiled CLI after the
  normal build; the packed external-consumer regression is the compatibility
  proof surface.
- RED: `corepack pnpm exec vitest run packages/proto-tools/test/package-bin.test.ts`
  failed as expected because the declared unbuilt source bin target was absent
  (`expected false to be true`).

## 2026-08-03 — Implementation and focused verification

- Added the tracked `packages/proto-tools/bin/spine-proto.mjs` launcher, changed
  the package `bin` declaration to it, and included it in the packed files.
  The launcher has no policy logic; it imports the existing compiled CLI, so
  post-build command behavior remains owned by `src/cli/spine-proto.ts`.
- Updated the packed external-consumer test to invoke the declared launcher
  rather than bypassing the public bin through its internal `dist` path.
- GREEN: `corepack pnpm exec vitest run packages/proto-tools/test/package-bin.test.ts`
  passed (1 file, 1 test), proving the target is present before build and in a
  `--ignore-scripts` package tarball.
- Build prerequisite evidence: after `corepack pnpm proto:generate` completed
  its source, example, and descriptor checks, `corepack pnpm --dir
packages/proto-tools exec tsc -b` completed. The launcher reached the built
  CLI and rejected `unsupported-command` with the established
  `spine-proto: unsupported command unsupported-command` error. The nonzero
  exit was intentional for that unsupported-command probe.
- Packed consumer GREEN: `corepack pnpm exec vitest run
packages/proto-tools/test/package-bin.test.ts
packages/proto-tools/test/external-consumer.test.ts` passed (2 files, 2
  tests). The external package now invokes `bin/spine-proto.mjs`, proving the
  packed public CLI delegates successfully after its normal build.
- Fresh-install GREEN: copied the uncommitted worktree into a new `/tmp`
  workspace without `.git`, `node_modules`, generated output, or `dist`, then
  ran `CI=true corepack pnpm install --frozen-lockfile --reporter append-only`.
  It exited 0, printed `Scope: all 22 workspace projects` once, and contained
  zero `Failed to create bin` warnings. This confirms the previous repeated
  policy-looking blocks were pnpm redraw output, while the ten missing-bin
  warnings were five link attempts plus five relink attempts and their source
  is resolved.
- Handoff correction: the warning classification above distinguishes the ten
  real pnpm bin-link warnings from the separate interactive progress redraws.
  The package-bin regression discovers the one generated tarball instead of
  assuming the current snapshot version in its filename.
- Focused preflight after the correction: the two proto-tools package tests,
  `git diff --check`, Prettier, targeted ESLint, and
  `corepack pnpm --dir packages/proto-tools exec tsc -b` all passed.

## 2026-08-03 — Mechanical preflight and review dispatch

- The complete cheap task preflight passed after the normal Proto generation:
  generated workspace build, tooling type-check, lint/TSDoc, formatting,
  documentation checks, generated-current checks, and package readiness.
- The focused package-bin and packed external-consumer tests passed again, 2/2.
- Implementation commit `ca4f109f` was immediately pushed to the task branch.
- Recorded one complete relevant review wave: style/maintainability,
  TypeScript/API package contract, and performance/reliability, each explicitly
  `gpt-5.6-terra` / high. Documentation and final security are N/A for the
  concrete reasons in the review record unless findings reopen them.
- Next: collect the complete review wave, aggregate any accepted findings once,
  and return one correction batch to the existing owner if needed.

## 2026-08-03 — Review wave aggregated

- Style/maintainability and TypeScript/API reviews are clean with no P0-P3
  findings. Existing human and agent documentation remains accurate because it
  documents the stable `spine-proto` command, not its internal package path.
- Performance/reliability accepted one P2: manually extracting the packed
  package and invoking its launcher does not exercise the package manager's
  generated `node_modules/.bin/spine-proto` shim, which is the corrected
  install boundary.
- All reviewers were explicitly configured existing roles with
  `gpt-5.6-terra` / high. Runtime self-introspection was unavailable, with no
  visible mismatch.
- Return the single complete correction batch to the same existing
  `implementer`, explicitly `gpt-5.6-terra` / medium: install the packed
  artifact through a package manager, run its public shim after the normal
  build, and keep the regression deterministic and bounded. Reopen only the
  reliability lane.

## 2026-08-03 — Reliability P2 correction

- The accepted P2 is technically valid: the existing external-consumer fixture
  manually extracts tarballs and therefore cannot create or execute a package
  manager `node_modules/.bin` shim.
- Runtime metadata remains unavailable. This correction is performed by the
  existing `implementer` role with explicitly configured `gpt-5.6-terra` /
  medium; no visible profile mismatch or inherited fallback exists.
- RED: adding an assertion for `users/node_modules/.bin/spine-proto` after the
  manual extraction failed as expected (`expected false to be true`), proving
  that it cannot satisfy the package-manager boundary.
- Correction: reuse the existing single normal-build-and-pack operation to
  create a small, separate local pnpm consumer. It installs the packed internal
  tarballs with `--offline --ignore-scripts`, executes the generated POSIX shim
  on POSIX or `spine-proto.cmd` via `cmd.exe` on Windows, and expects the
  established unsupported-command error. This is network-free and does not
  duplicate model/application generation setup.
- The first fixture attempt showed pnpm 11 ignores package-manifest
  `pnpm.overrides`; local tarball overrides therefore belong in the fixture's
  `pnpm-workspace.yaml`. This is a test-fixture correction, not a product or
  dependency-policy change.
- The workspace's offline store did not contain the newest `node-addon-api`
  tarball selected by the `zeromq` range. The fixture supplies the already
  installed `node-addon-api` directory through the same local override file,
  avoiding a network request while keeping real package-manager resolution.
- GREEN: `corepack pnpm exec vitest run
packages/proto-tools/test/external-consumer.test.ts` passed (1 file, 1 test).
  The test confirms pnpm creates the public shim from the normally built packed
  artifact and that the shim reaches the compiled CLI's established
  unsupported-command behavior.

## 2026-08-03 — Windows shim portability correction

- Reliability re-review confirmed the offline install, POSIX execution,
  bounded fixture, and cleanup, but accepted one Windows-only P2: `/s` quote
  stripping can split an absolute `.cmd` path containing spaces when the path
  is passed as a separate `cmd.exe /c` argument.
- Return this single deterministic correction to the same existing
  `implementer`, explicitly `gpt-5.6-terra` / medium. Supply one correctly
  quoted command string or another invocation that preserves a spaced shim
  path; add a host-neutral assertion for the exact command construction and
  rerun the focused tests. Only reliability remains open.
- Runtime self-introspection remains unavailable; the immutable configured
  implementer profile is the accepted `gpt-5.6-terra` / medium evidence.
- RED: the host-neutral assertion proved the existing `cmd.exe` arguments
  passed the spaced shim path and `unsupported-command` separately instead of
  as the single `/c` command string.
- GREEN: `windowsShimCommand()` quotes the `.cmd` path, escapes embedded quote
  characters, and appends only the fixed unsupported command. The Windows
  branch now passes that single string to `cmd.exe /d /s /c`; `corepack pnpm
exec vitest run packages/proto-tools/test/external-consumer.test.ts` passed
  1 file / 2 tests.

## 2026-08-03 — Review convergence

- Final performance/reliability re-review is clean with no P0-P3 findings.
  The reviewer confirmed the Windows command-string correction and the
  installed-shim fixture's offline, bounded, cleanup-safe behavior.
- Style/maintainability and TypeScript/API remain clean and were not
  substantively affected by test-only reliability corrections.
- All applicable review concerns are closed. Next: push this convergence
  record and run the single definitive `verify:release` profile.

## 2026-08-03 — Definitive release verification

- The release profile passed after review convergence: 178 test files passed
  with 3 skipped; 3,498 tests passed with 25 skipped.
- Coverage passed at 94.03% statements, exactly 90.00% branches, 94.52%
  functions, and 94.90% lines. No threshold or exclusion changed.
- Generated builds, tooling type-check, lint/TSDoc, formatting, documentation,
  Proto-current, package-readiness, and release-readiness all passed in the
  same run.
- The initial desktop output session detached while the original release
  process remained active. An accidental duplicate was identified by process
  group and terminated; the original single process completed successfully and
  supplied the evidence above.
- Task status is ready for integration. Next: push this evidence, merge the
  exact reviewed tree into current `origin/main`, run change-sensitive
  post-merge verification, and push `main`.

## 2026-08-03 — Integration and closure

- Merged T-0098 into unchanged `origin/main@af72de4b` without conflicts. Merge
  tree `138380cddb21f6fa63e64f769b44c7e8cabfd2f3` exactly matched the reviewed
  task tree, so the definitive release evidence applies byte-for-byte.
- In a new unbuilt integration worktree, `pnpm install --frozen-lockfile
--reporter append-only` exited 0 with one scope line, one successful policy
  result, and zero `Failed to create bin` warnings.
- The first focused packed-consumer attempt correctly exposed its documented
  precondition: generated framework Proto modules were absent in the new
  worktree. After the normal `pnpm proto:generate` step, the same focused suite
  passed 2 files / 3 tests. This was not a product or install regression.
- Pushed the verified merge to `origin/main@3fb1dd8a`. The task is complete;
  this final closure record is pushed to both `main` and the task branch.

## 2026-08-03 — Post-install mode drift

- The final remote audit found that pnpm changed the tracked launcher from mode
  `100644` to `100755` while creating the workspace bin link. Content was
  unchanged, but a successful first install must not dirty a fresh checkout.
- Reopened only this deterministic package-file correction. Add a POSIX
  executable-mode assertion to the existing package-bin regression, observe it
  fail against the committed mode, then commit the launcher at mode `100755`
  and rerun fresh install plus focused checks.
- This corrects package metadata already expected by pnpm and does not change
  CLI behavior, API, dependencies, or trust boundaries; the converged review
  lanes remain closed under the deterministic-correction rule.
- RED: the new package-bin assertion failed with executable bits equal to zero.
- GREEN: after setting the tracked launcher mode to `100755`, the regression
  passed 1/1. A frozen append-only install exited 0, emitted zero bin warnings,
  preserved mode `755`, and introduced no additional working-tree difference.
- Task status is complete. Push this deterministic closure commit to both
  `origin/main` and the published task branch, then audit the remote refs.
