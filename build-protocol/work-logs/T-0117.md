# T-0117 Work Log

## Start — 2026-08-05

- T-0117 starts from pushed `origin/main@eca8f7fe` in an isolated worktree.
- The approved T-0113 plan already fixes the architecture and scope. No new
  requirements split is justified unless a demonstrated admission-boundary
  blocker appears.
- Selected skills previously read in this stable session remain applicable:
  `test-driven-development`, `cqrs-implementation`,
  `subagent-driven-development`, `requesting-code-review`, and
  `verification-before-completion`. Project protocol supersedes generic
  workflow details where they differ.
- Session and repo-local skill inventory sources remain unchanged from T-0116;
  the expected-skill manifest and installed entrypoints were already checked.
  No newly available task-specific skill changes this scope.
- The implementation owner must inspect the frozen dispatch-event Protos and
  existing command/subscriber/reactor execution seams before writing a RED
  test. It must not infer payload fields or admission timing from memory.

## Implementation Dispatch — 2026-08-05

- Existing role: implementer `/root/t0117_impl`.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Ownership is bounded to T-0117 production/tests/records; no subagents,
  examples, broad docs, T-0118, or T-0119.
- Runtime self-introspection is not exposed by the current surface; the
  immutable configured role/profile is the available metadata unless the
  child surface reports more.

## Command Dispatch Slice — 2026-08-05

- Inspected frozen `entity_log_events.proto` fields for
  `CommandDispatchedToHandler`: `receiver` (`MessageId`), `payload`
  (`core.Command`), `when_dispatched` (`Timestamp`), and `entity_type`
  (`EntityTypeName.java_class_name`). The System event envelope carries the
  dispatch timestamp, causal origin, actor, and tenant context.
- Inspected `spine-jvm-docs/spine-routing-dispatch-and-delivery.md` and
  `spine-jvm-docs/spine-entities-repositories-and-state.md`, following their
  command-bus/handler and diagnostic guidance. This supports the existing
  repository admission seam; no JVM-inspired API or lifecycle abstraction was
  added.
- Mapped the TS command admission seam in `AggregateCommandExecution.run()` and
  `ProcessManagerCommandExecution.run()`: command schema validation, routing,
  assignee lookup, and entity load complete before the diagnostic; invocation
  and handler transaction follow it. Missing assignees return before emission.
- Added RED coverage first. With generated build output available, the new
  aggregate assertion failed by timing out waiting for the absent System event;
  after minimal implementation it passed. Focused coverage now proves payload,
  receiver, entity type, payload/envelope timestamp equality, causal origin,
  single- and multitenant propagation, pre-admission refusal/unroutable
  silence, Process Manager admission, and one-shot diagnostic-failure
  isolation.
- Runtime implementation registers and posts only through the repository
  runtime's paired System EventBus hooks. Diagnostic envelopes receive fresh
  event IDs, avoiding collisions with command-derived domain/lifecycle event
  IDs. Any registration or posting failure is caught and recorded as a
  diagnostic follow-up failure; it neither awaits, retries, nor rolls back
  admitted handler work.
- Scope remains command-handler diagnostics only. Projection subscriber and
  aggregate/process-manager reactor diagnostics remain unmodified for the next
  bounded slice.

## Projection Subscriber Dispatch Slice — 2026-08-05

- Inspected frozen `EventDispatchedToSubscriber` fields in
  `entity_log_events.proto`: `receiver` (`MessageId`), `payload`
  (`core.Event`), `when_dispatched` (`Timestamp`), and `entity_type`
  (`EntityTypeName.java_class_name`). The diagnostic envelope remains the
  owner of fresh event identity, timestamp, causal origin, and tenant context.
- Mapped projection admission in `ProjectionEventExecution`: matching
  subscriber lookup must succeed; each routed target loads its Projection;
  only then does the shared publisher emit immediately before subscriber
  invocation. No-subscriber and routing-refusal paths return or reject before
  that emission point.
- Deepened the existing command publisher into one internal
  `HandlerDispatchPublishing` helper. Subscriber diagnostics clone the original
  event and derive origin/tenant through `SignalMetadata.originFromEvent`; no
  public API, domain bus, or domain EventStore path changed.
- Read-side catch-up's existing `dispatchStoredProjectionEvent()` selects
  matching projection event types and invokes `dispatchProjectionDirect()`,
  which reaches the same `ProjectionEventExecution.runDirect()`/target seam.
  A stored matching event is therefore an accepted subscriber dispatch and
  emits one diagnostic; unmatched stored events are not dispatched and emit
  none. This records existing behavior rather than adding replay policy.
- RED evidence: the live subscriber assertion initially timed out waiting for
  an absent System event. Focused green coverage proves field fidelity,
  System-only routing, tenant propagation, catch-up replay, unmatched and
  routing-refusal silence, and diagnostic failure isolation from projection
  state work. Aggregate and Process Manager reactor diagnostics remain out of
  scope.

## Reactor Dispatch Slice — 2026-08-05

- Inspected frozen `EventDispatchedToReactor` fields in
  `entity_log_events.proto`: `receiver` (`MessageId`), `payload`
  (`core.Event`), `when_dispatched` (`Timestamp`), and `entity_type`
  (`EntityTypeName.java_class_name`). Its envelope owns a fresh System event
  ID plus the source event's causal origin and tenant context.
- Mapped Aggregate admission to the matched reactor loop after target load and
  any event-commanding handler work; mapped Process Manager admission to the
  guarded target execution after target load. The shared publisher emits once
  per admitted target immediately before reactor invocation. No matched
  reactor, absent event schema, or prior routing refusal reaches that point.
- Deepened `HandlerDispatchPublishing` so subscriber and reactor diagnostics
  share one Event payload/receiver/entity-type field assembly path. It posts
  only through the paired System EventBus hooks, records a one-shot failure,
  and never awaits, retries, or rolls back handler work.
- RED evidence: the Aggregate reactor diagnostic test initially timed out
  without the implementation. Green coverage now proves Aggregate and Process
  Manager receiver/payload/type/timestamp/origin fidelity, multitenant target
  routing, coexistence with Process Manager `@Assign` command diagnostics,
  unmatched/no-reactor silence, and publication-failure isolation. The PM
  command's produced domain event correctly yields its own separate accepted
  reactor diagnostic; the test selects the original source-event payload.

## Converged Cheap Preflight — 2026-08-05

- Independent preflight first found a test-helper oneof narrowing error in the
  root tooling typecheck. The test-only correction is pushed in `b010ec79` and
  changes no production behavior.
- Formatting, branch and local diff checks, generated build typecheck, root
  tooling typecheck, affected ESLint, cleanup/TSDoc, TypeDoc/API, and
  documentation audience checks pass.
- Repository-routing and EventBus suites pass all 217 tests.
- The deliberately narrow whole-file coverage command reports below global
  thresholds because it selects one large production file and two focused
  suites. Its LCOV data covers all 30 changed executable production lines
  (100%). The unchanged repository-global 90% thresholds remain authoritative
  in the single final `verify:release` run after review convergence.

## Converged Release Verification — 2026-08-05

- `pnpm verify:release` passes at committed endpoint `c7b7e76a` after review
  convergence.
- All build, tooling, lint, formatting, documentation, Proto, frozen
  descriptor, generated-cleanliness, and release-readiness gates pass.
- Test result: 187 files passed, 3 skipped; 3,746 tests passed, 26 skipped.
- Coverage: 94.06% statements, 90.06% branches, 94.60% functions, and 95.03%
  lines. No threshold, exclusion, or coverage configuration changed.
- T-0117 is ready for integration into current `origin/main`, followed by
  tree-equivalence and focused post-merge verification.

## Integration Closure — 2026-08-05

- The verified task endpoint `a5f5be66` merged into unchanged
  `origin/main@eca8f7fe` as `fa9f4d71` and was pushed to `origin/main`.
- The merge tree exactly equals the verified task endpoint tree. The final
  runtime release evidence remains the passing `c7b7e76a` gate; `a5f5be66`
  adds only its verification record.
- A fresh detached integration worktree was bootstrapped from the frozen
  lockfile and generated the verified Proto outputs. Post-merge generated
  build and server typecheck pass; repository-routing and EventBus suites pass
  all 222 tests; diff hygiene is clean.
- The dirty primary checkout and protected human-review files remained
  untouched. T-0117 is durably complete, and T-0118 is next.

## Preflight Type-Narrowing Correction — 2026-08-05

- The root tooling typecheck identified that the test-only `diagnosticTenants()`
  helper read a generated `TenantId.kind` oneof value without first narrowing
  its case. The helper now returns the string only for `kind.case === "value"`;
  other tenant-id cases continue to map to the existing empty-string fallback.
- This is a test typing correction only: no production source or dispatch
  behavior changed.
- Evidence: `pnpm typecheck:tooling`; focused repository and EventBus tests
  (217 passing); `pnpm lint:generated`; `pnpm format:check`; and
  `git diff --check` completed without reported failures.

## Accepted Review Corrections — 2026-08-05

- Consolidated command, subscriber, and reactor diagnostic delivery through
  one private best-effort System posting operation. Sync registration/posting
  throws and asynchronous post rejections each record one failure without
  awaiting, retrying, or changing handler failure semantics.
- Replaced the five fixed 20 ms absence-check sleeps with `context.close()`.
  Close invokes the existing command/event accepted-work drain loop, so each
  assertion observes the queue-idle endpoint rather than elapsed wall-clock
  time.
- Added focused coverage for admitted command-handler, Projection subscriber,
  and Aggregate reactor failures. Each operation still rejects with its
  original failure and leaves exactly one correct dispatch diagnostic.
- Evidence: `pnpm exec vitest run packages/server/test/repository/repository-routing.test.ts packages/server/test/bus/event-bus.test.ts`
  (220 passing); `pnpm typecheck:tooling`; `pnpm lint:generated`; formatter;
  and diff checks. No public API or serialized contract changed.

## Residual Process Manager Reliability Coverage — 2026-08-05

- Added tests for admitted Process Manager `@Assign` command and event-reactor
  failures. Each preserves the original failure and, after the deterministic
  `context.close()` System-bus drain, has exactly one respective diagnostic.
- The assertions match both original payload and Process Manager receiver to
  exclude diagnostics created by unrelated work. Existing runtime behavior
  passed without production changes.
- Evidence: focused PM tests, repository-routing plus EventBus regression,
  root tooling/generated typechecks, lint/cleanup/TSDoc, formatter, and diff
  checks.
