# T-0133 Work Log

## 2026-08-07 - Start

- Created `task/T-0133-histories-event-store` from reviewed and pushed T-0132
  checkpoint `7aaa2699`.
- Classified the task as high-risk and froze behavior-focused acceptance for
  JVM-style storage groups, generated history records, disabled-history
  non-allocation, receipt removal, Event Store isolation, retention, and
  in-memory atomicity.
- Recorded explicit exploration and implementation model allocations before
  dispatch. No JVM build, provider migration, compatibility alias, or receipt
  replacement is allowed.
- The first explorer dispatch explicitly requested the preferred
  `gpt-5.6-luna` / `medium` profile and the surface rejected it as unavailable;
  only Sol and Terra are exposed here. Both read-only explorers are therefore
  explicitly redispatched as `gpt-5.6-terra` / `medium`. This is a documented
  surface limitation, not an inherited-profile fallback.

## 2026-08-07 - Current JVM Source Comparison

- Read-only explorer completed at the explicit configured
  `gpt-5.6-terra` / `medium` profile. Independent runtime-model introspection
  is unavailable; no mismatch was visible. No JVM build or edit occurred.
- Current frozen source confirms `StorageGroup` is passed separately to the
  factory. State history uses state source + `EntityStateKey`/`EntityRecord`;
  Entity event history uses `Event` source/record + the state-named group;
  Event Store is ungrouped. History columns are `entity_id`, `created`, and
  `version`; Event Store columns are `type` and `created`.
- The adjacent JVM event-history prose and implementation disagree on source
  type; the executable implementation and identity tests use `Event`. T-0133
  follows that behavior and relies on the state-named group for separation.

## 2026-08-07 - Frozen EntityStateKey Gap

- The TypeScript inventory correctly found no tracked `EntityStateKey` even
  though T-0133 requires the JVM state-history key. Direct source verification
  located it in the separate current JVM file
  `spine/server/entity/state_key.proto` at the already pinned commit.
- This is a bounded frozen-contract omission, not a new stored record. T-0133
  now owns a byte-for-byte copy plus manifest/checksum/descriptor evidence. No
  JVM build or modification is allowed.

## 2026-08-07 - TypeScript Storage Inventory

- Read-only explorer completed at explicit configured
  `gpt-5.6-terra` / `medium`; runtime self-introspection is unavailable and no
  mismatch was visible.
- The inventory confirms grouping belongs at `StorageFactory`, generic in-memory
  scope identity must include the group, the current bespoke Entity backend
  eagerly allocates both histories, and receipt data is isolated to the commit
  contract/in-memory implementation plus repository outcome handling.
- Ordinary Inbox replay and Event Store-based Projection catch-up are not
  receipt behavior and must remain. Provider receipt/layout consumers are
  downstream T-0134/T-0135 failures, not T-0133 ownership.

## 2026-08-07 - Lazy-History Architecture Block

- The implementation owner confirmed that history ports still use bespoke eager
  Entity-backend maps, while grouped `RecordStorage` is opened only by the outer
  in-memory factory. Atomic commit staging must share the exact same grouped
  records; simply adding enablement flags would create split storage.
- This is a demonstrated high-risk ownership/concurrency block. The existing
  requirements-splitter role is explicitly dispatched as `gpt-5.6-sol` /
  `high` for one minimal, read-only design decision. Implementation resumes in
  the existing owner context after that result.

## 2026-08-07 - Lazy-History Architecture Decision

- Requirements splitter completed at explicit configured `gpt-5.6-sol` /
  `high`; runtime self-introspection is unavailable and no mismatch was visible.
- Accepted one ownership seam: `InMemoryStorageFactory` conditionally opens
  grouped generated histories and exposes the exact private backing used for
  staging. The Entity factory retains only current records. Disabled ports bind
  nothing; enabled ports and commits share identical grouped `TenantRecords`.
- Atomic publish stages current, enabled histories, and the ungrouped Event
  Store and replaces live families only after success. Event Store operations
  share a context lock to prevent a concurrent direct append from being lost.
- Implementation sequence is frozen in the task and returned to the existing
  implementer. Providers and later consumers remain excluded.

## 2026-08-07 - Initial RED/GREEN Slices

- Runtime metadata: existing `implementer` role explicitly dispatched as
  `gpt-5.6-terra` / `medium`. The execution surface exposes the configured
  profile but has no independent model self-introspection; no visible mismatch
  was observed.
- RED: the grouped-record test failed because `StorageGroup` was not a
  constructor. The frozen-key contract test failed because
  `state_key.proto` was absent.
- GREEN: `StorageGroup` is passed separately to `StorageFactory` and included
  in the in-memory backing scope without altering `RecordSpec`. The copied JVM
  source hashes to
  `a93a91761171f87da5b3f9c269e4a23a41075d9cfc169922e52fadcacf6f3ffb`.
  The two focused assertions pass; `git diff --check` passes.
- Limitation: `state_key.proto` has not yet been entered in the frozen-source
  provenance manifest or generated descriptor set. History record specs,
  lazy allocation, receipt removal, repository migration, and full focused
  verification remain outstanding. The full storage-factory test file retains
  two pre-existing T-0132 legacy current-record helper failures.

## 2026-08-07 - Group Scope Review Correction

- Corrected the initial in-memory group scope to use a separately
  length-delimited canonical group component with an explicit ungrouped value;
  raw source/group string concatenation could collide when names contain
  delimiters.
- Focused group-isolation and delimiter-collision assertions pass. Frozen source
  provenance now verifies 49 checksums, while descriptor generation correctly
  stops on the expected changed frozen descriptor digest
  `2a0db288787ac436c190ed86d72d78922deeb0fba4a64b2094b36b2525859db0`.

## 2026-08-07 - Frozen Descriptor Integration

- `cmp -s` proved the copied `state_key.proto` is byte-identical to the pinned
  core-jvm source and its SHA-256 matches the recorded provenance.
- Under the task's explicit frozen-contract authority, updated the normalized
  frozen descriptor digest from
  `0f4a4191434623473e9a9b5e179bfd4f95f64044161bc7e5d38020f4cc0edd6d`
  to `2a0db288787ac436c190ed86d72d78922deeb0fba4a64b2094b36b2525859db0`.
  Deterministic generation now verifies all 49 source checksums and 52 frozen
  descriptor files; the focused contract/group suite passes 3/3.

## 2026-08-07 - State History Record Spec

- RED: the focused state-history layout test failed because the generated
  record-spec factory did not exist.
- GREEN: `entityStateHistoryRecordSpec(stateType)` returns an external
  state-type-named `StorageGroup` and a `RecordSpec<EntityStateKey,
EntityRecord>` with state source type and exact `entity_id`, `created`, and
  `version` columns. The key is derived only from the persisted entity ID and
  JVM int32 version.
- Evidence: after rebuilding generated Proto output, the focused test passes
  1/1 and `pnpm exec tsc -p packages/storage/tsconfig.json --noEmit` exits 0.

## 2026-08-07 - Event History And Event Store Specs

- RED: the event-history layout test failed because its grouped `EventId`/`Event`
  specification did not exist.
- GREEN: event history now uses `Event` for source and record under the served
  state-type group, with `entity_id`, `created`, and `version` columns derived
  from producer context. The independent Event Store remains ungrouped and its
  layout columns are `type` and `created`.
- Evidence: focused history-spec/Event Store tests pass 14/14; storage
  TypeScript compilation and `git diff --check` exit 0.

## 2026-08-07 - Repository Generated History Envelope Slice

- Runtime metadata: existing implementer role at the explicitly configured
  `gpt-5.6-terra` / `medium` profile. The execution surface exposes no
  independent model self-introspection; no visible mismatch was observed.
- Repository commit payloads now retain generated `EntityRecord` envelopes
  with full `Version` timestamps and clone generated diagnostic `Event` rows.
  Repository history caches unpack generated records with the repository state
  schema before deriving public state and version values.
- Evidence: Prettier check for changed repository source and focused history
  cache test path passed after formatting; `git diff --check` passed; scoped
  repository source/test search contains no deleted history DTO names or direct
  DTO `createdAt`/`producerVersion` field assumptions.
- Limitation: the narrow history-cache test cannot build until the in-progress
  storage-side adapter conversion resolves its generated-port TypeScript errors;
  no provider files were changed.

## 2026-08-07 - Repository Transition Verification

- Corrected the remaining aggregate diagnostic and Projection retained-state
  payloads to generated `Event` and `EntityRecord` forms.
- Build inventory now contains only later `RecordSpec.schema` callers in
  deployment, context, delivery, server bindings, and Stand registry paths.
- The focused aggregate transition/history tests start but both stop before
  their assertions in unrelated Inbox dedup setup:
  `InMemoryStorageFactory.tenantRecords` reads an undefined `sourceType` from a
  later in-memory record-storage integration path. This task did not modify
  that provider/inbox path.

## 2026-08-07 - State Adapter Ownership Redispatch

- The original implementation owner removed the receipt contract and converted
  the history SPI to generated records, but repeatedly stopped at the
  map-specific `InMemoryEntityHistory` rewrite after identifying the same
  ownership points. This is a demonstrated implementation barrier, not an
  architectural ambiguity; the accepted lazy-history design remains unchanged.
- A fresh existing `implementer` role is assigned only the storage-side state
  adapter/factory/conformance file family. Expected dispatch metadata is
  explicitly `gpt-5.6-terra` / `medium`. The prior owner is complete and no
  overlapping production-code writer remains active.

## 2026-08-07 - Generated State-History Storage Slice

- The replacement existing implementer completed the assigned state-history
  adapter slice at the explicitly configured `gpt-5.6-terra` / `medium`
  profile. The execution surface provides no independent runtime-model
  introspection; no visible mismatch occurred.
- `InMemoryEntityHistory` now delegates to an enabled grouped
  `RecordStorage<EntityStateKey, EntityRecord>`. It queries the materialized
  packed `entity_id` column, orders backward reads by `version` then `created`,
  reads `stateAt` by `created` then `version`, and deletes actual record keys in
  bounded maintenance chunks. It validates the generated record/key and packed
  state schema and returns cloned values.
- `MemoryEntityStorageFactory` no longer allocates a state map or keyed state
  queue. `InMemoryStorageFactory` opens the state group only when
  `stateHistory` is enabled; otherwise the Entity handle receives the disabled
  no-allocation port. Focused tests prove disabled append rejection, enabled
  raw grouped-record visibility, ordering, continuation, time selection, trim,
  and truncation.
- Evidence: `pnpm exec vitest run
packages/storage/test/entity/in-memory-entity-history.test.ts` initially
  passed 3/3. The required narrow self-review restored the adapted established
  state behavior coverage; the same command now passes 8/8. Changed-file
  Prettier and `git diff --check` pass.
- Current integration-train limitation: storage TypeScript compilation now
  fails only in `memory/in-memory-entity-commit.ts`, which still stages the
  removed `EntityBackend.states` and `stateQueue` maps. That atomic-commit
  migration is the next T-0133 slice and outside this state-adapter ownership.

## 2026-08-07 - State Slice Self-Review

- Reviewed only the assigned diff for user TSDoc layout, generated-record
  cloning/type validation, lazy state-history opening, and query semantics.
  Newly authored public TSDoc uses multiline blocks; the deterministic scan
  found no one-line TSDoc in the owned production paths.
- Deleted the residual legacy state DTO alias and every map-specific state
  ordering/selection helper. `KeyedSerialQueue` remains only as an operation
  ordering primitive around generated-record append and per-Entity trim; it is
  not storage.
- Strengthened the disabled-history test with a spy on
  `createRecordStorage`: constructing a disabled Entity handle makes zero
  grouped-record storage calls before the test itself opens a raw inspection
  handle. Enabled raw grouped visibility remains covered separately.
- Restored/adapted state coverage for generated `EntityRecord` values:
  defensive cloning, schema/ID rejection, idempotency/conflict, version/time
  ties, exclusive continuation, tenant isolation, close behavior, invalid
  bounds, bounded maintenance chunks, and trim/truncate concurrency. The
  adapter queries exact packed `entity_id`, sorts backward by `version` then
  `created`, and sorts temporal lookup by `created` then `version`.
- The same five atomic-commit map references remain the complete storage
  TypeScript error inventory. They are intentionally not changed by this
  state-only ownership slice.

## 2026-08-07 - Generated Event-History Storage Slice

- The same existing `implementer` role continued under the explicitly
  configured `gpt-5.6-terra` / `medium` profile. The surface does not expose
  independent runtime-model introspection; no visible mismatch or fallback was
  observed.
- `MemoryEntityEventHistory` now delegates to grouped
  `RecordStorage<EventId, Event>` and `MemoryEntityStorageFactory` no longer
  creates an event map. `InMemoryStorageFactory` opens the state-type-named
  event group only when `eventHistory` is enabled; disabled handles receive the
  no-allocation port.
- Appends validate the generated Event ID, producer ID/schema, version, and
  timestamp before persistence. Stored event snapshots clear enrichment,
  matching the intended retained diagnostic record behavior. Backward reads
  select the packed producer `entity_id`, order by version, time, and canonical
  Event ID bytes, and use an exclusive version continuation. Truncation stages
  selected generated row keys and deletes them in bounded chunks.
- The combined focused suite now passes 15/15 and covers disabled zero-open,
  grouped raw visibility, UTF-8 tie ordering, tenant isolation, invalid IDs
  and bounds, defensive copies, large histories, closure, failure/resume, and
  post-selection append survival (including a later canonical UTF-8 ID).
- Evidence: `pnpm exec vitest run
packages/storage/test/entity/in-memory-entity-history.test.ts` passed 15/15;
  changed-file Prettier and `git diff --check` passed. Storage TypeScript
  compilation has exactly eight expected errors, all in the next
  `in-memory-entity-commit.ts` migration slice referencing removed event/state
  maps or the old state queue.

## 2026-08-07 - History Validation Acceptance Corrections

- Rechecked the generated history append boundaries. State history now fails
  before persistence unless an EntityRecord has a schema-matching packed state,
  matching Entity ID, Version, and Version timestamp. Event history now fails
  before persistence unless its Event ID, producer ID, EventContext version,
  and EventContext timestamp are present and compatible; backward reads no
  longer admit missing versions.
- Verified the actual stored event clone clears EventContext enrichment, rather
  than only normalizing the append input. The focused 15/15 suite now explicitly
  covers both state and event required-field failures plus the stored-enrichment
  assertion, alongside the previously restored behavior coverage.
- Confirmed the current-record `read()` has one reachable return path. No
  legacy history DTO, map, queue, or replacement marker remains in the owned
  history source. Formatting and `git diff --check` remain clean.

## 2026-08-07 - Atomic Commit Ownership Redispatch

- The history-adapter owner completed generated state/event storage and the
  shared Entity mutation queue, but stopped twice before replacing the cohesive
  obsolete commit-map implementation. The accepted staging architecture and
  lazy typed backing opener are unchanged.
- A fresh existing `implementer` role owns only the in-memory atomic commit,
  narrow factory wiring, and mirrored tests. Expected dispatch metadata is
  explicitly `gpt-5.6-terra` / `medium`; no overlapping writer remains active
  in those files.

## 2026-08-07 - Atomic Current/History/Event Commit Rewrite

- The replacement existing `implementer` completed the bounded atomic-commit
  rewrite at the explicitly configured `gpt-5.6-terra` / `medium` profile.
  The execution surface exposes no independent runtime-model introspection;
  no visible mismatch or fallback was observed.
- `MemoryEntityCommitStorage` no longer has receipt/replay behavior,
  `InMemoryCommitLocks`, or state/event map staging. It takes the shared
  per-Entity mutation queue, snapshots the current map and only those exact
  grouped `TenantRecords` that the incoming commit uses, validates/writes fresh
  staged adapters, and publishes all participating families only after success.
- A nonempty delivery-event batch holds the same Event Store context lock used
  by direct Event Store appends before taking the Entity queue. This preserves
  both a direct append and an Entity commit; commits without delivery events do
  not bind or lock Event Store records. Disabled state/event histories reject a
  nonempty matching batch before any backing store opens.
- Focused evidence: `pnpm exec vitest run
packages/storage/test/entity/in-memory-entity-commit.test.ts
packages/storage/test/entity/in-memory-entity-history.test.ts
packages/storage/test/event/event-store.test.ts` passes 3 files / 40 tests;
  `pnpm exec tsc -p packages/storage/tsconfig.json --noEmit`, changed-file
  Prettier, and `git diff --check` pass.
- Server TypeScript still reports the integration-train inventory: ten legacy
  `RecordSpec.schema` consumers, two repository generated-history DTO callers,
  and two stale checked-in storage declaration references to deleted history
  DTOs. This bounded commit slice does not alter those files; the coordinator
  must route the repository migration and later consumer restoration.

## 2026-08-07 - Atomic Staging Handle Correction

- Acceptance review found that staged input initially spread caller-supplied
  history handles. A current-only commit could then close such a live handle
  when it closed its temporary staged Entity wrapper.
- The staged input now explicitly omits both supplied history-handle fields and
  installs only fresh staged record storages for a participating history
  family. A focused regression supplies a live grouped state-history handle and
  proves a current-only commit neither reads nor closes it.
- Evidence: the commit suite passes 14/14; storage TypeScript compilation,
  changed-file Prettier, and `git diff --check` pass.

## 2026-08-07 - Storage Factory Fixture Migration

- Migrated the two creation-order fixture writes from the removed ad-hoc
  current-record shape to generated `EntityRecord` values with packed IDs and
  state. The fixture now supplies both `EntityIdCodec.pack` and authoritative
  unpacking; production validation remains unchanged.
- Evidence: `pnpm exec vitest run packages/storage/test
packages/proto/test/entity-state-key-contract.test.ts` passes 14 files / 127
  tests; storage TypeScript compilation, Prettier, and `git diff --check` pass.

## 2026-08-07 - Storage Documentation Convergence

- Updated the beginner storage README with a concise external `StorageGroup`
  example and its separation purpose. Updated the agent reference with the
  third `createRecordStorage` argument, complete in-memory physical identity,
  generated Entity history layouts, lazy disabled histories, independent
  ungrouped Event Store, and in-memory-only atomic commit behavior.
- `node scripts/check-tsdoc.mjs packages/storage` passes. The broad generated
  documentation check is currently blocked by the known integration-train
  compile inventory in deployment, server, datastore/RDBMS, and client packages;
  it reports 172 existing errors before audience checks can run. No unrelated
  source was changed.

## 2026-08-07 - Consolidated Review Corrections

- Runtime metadata: existing `implementer` role explicitly configured as
  `gpt-5.6-terra` / `medium`; the surface does not expose independent runtime
  model introspection and no mismatch was visible.
- RED/GREEN: added race regressions for different-ID atomic commits, direct
  event-history append versus commit, and divergent same-Event-ID appends.
  All three failed before the shared Entity-scope FIFO mutation sequence and
  pass after it. Current records, state history, event history, and commits
  now share the scope sequence; staged adapters retain independent queues so
  an outer commit does not re-enter the live queue.
- Added bounded `limit` windows at history RecordStorage reads and maintenance
  selection seams, restored generated-record current/state/event conformance
  coverage, removed obsolete `EntityStorageKey` and `EntityRecordPurpose`, and
  re-exported only canonical history RecordSpec builders from the internal
  provider seam. The storage README group example is self-contained.
- Evidence: focused storage tests pass 3 files / 36 tests and storage `tsc`
  exits 0. The coverage-enabled task profile reaches the known unowned shared
  build inventory before running tests: legacy `RecordSpec.schema` consumers,
  Datastore/RDBMS historical provider migrations, declarations, and examples.

## 2026-08-07 - Exact History Paging Correction

- Replacement implementer assignment: existing implementer, explicitly
  configured `gpt-5.6-terra` / `medium`. Runtime model self-introspection is
  unavailable; configured profile metadata is the available evidence and no
  mismatch was visible.
- History queries now use finite stable keyset pages (default 10,000 rows) and
  carry every page forward for event/state backward reads, `stateAt`, state
  trim, and both truncates. The internal maintenance seam permits a small page
  size for deterministic coverage. State truncate holds the shared Entity-scope
  FIFO for selection and deletion.
- Evidence: the focused history/commit/provider/Event Store command passes 4
  files / 52 tests; storage `tsc`, targeted paging-source/history-test ESLint,
  changed-file Prettier, and `git diff --check` pass.
- Final correction evidence: full changed-storage-file ESLint now passes with
  no suppressions. `node scripts/check-tsdoc.mjs packages/storage`,
  `pnpm docs:audience:check`, changed-file Prettier, `git diff --check`, and
  storage `tsc` all pass. Focused coverage runs all 52 history/commit/provider/
  Event Store tests successfully and reports the changed history adapter at
  95.91% statements and 96.37% lines.
- Limitation: the focused coverage command exits nonzero solely because its
  repository-global 90% threshold counts the untouched monorepo (3.87% overall
  coverage). This is not a focused test failure. The later integration-train
  `verify:task` build inventory remains the previously recorded unowned
  `RecordSpec.schema`/provider/declaration/example migration work.

## 2026-08-07 - Streaming Paging Final Batch

- Replaced full-history paging accumulation with stable keyset streaming. Reads
  stop at their exact depth/temporal answer; trim and truncates delete page and
  chunk windows while retaining only a page. Equal-created state-version and
  non-BMP Event-ID tie boundaries are covered with a two-row page seam.
- Conformance-created and reopened handles now close through `try/finally`.
  Focused history/commit tests pass 40 tests; storage TypeScript and targeted
  lint pass.
- `check-api-docs` reaches the recorded later integration inventory before
  export checking: 151 unchanged server/Datastore/RDBMS migration errors,
  principally removed `RecordSpec.schema`/storage-key and old history DTO
  assumptions. No downstream source was altered.

## 2026-08-07 - Final Changed-File Lint Closure

- The required exact command
  `git diff --name-only --diff-filter=ACMR -z -- '*.ts' | xargs -0 pnpm exec eslint`
  now exits 0. It cleared 13 server source/test findings without suppressions.
  Storage TypeScript compilation passes.
- Focused scanner and storage history/commit suites pass. The repository-routing
  suite reaches the pre-existing integration-train provider defect in the
  unmodified inbox record-storage path: `InMemoryStorageFactory.tenantRecords`
  receives a RecordSpec with undefined `sourceType`, causing 141 dependent
  routing failures. This is the same later record-storage consumer migration
  family; no routing assertion failure was attributed to this lint correction.

## 2026-08-07 - Bounded In-Memory Provider Selection Closure

- The existing `implementer` continued this final narrow correction at the
  explicitly configured `gpt-5.6-terra` / `medium` profile. This execution
  surface does not expose independent runtime-model introspection; the
  configured profile is the available metadata, and no visible mismatch or
  fallback occurred.
- RED evidence: a 128-row tied-record regression with a continuation, offset
  of two, and limit of two failed against the old provider because it sorted all
  111 continued matches. GREEN evidence: finite queries now scan candidates and
  retain only the sorted top `offset + limit` window; filters, continuation,
  ID tie-breaking, masks, candidate limits, and unbounded-query behavior are
  unchanged. The regression passes after the change and asserts the exact
  `event-019`, `event-020` page without permitting a sort above that four-row
  retained window.
- Corrected the `prettier-ignore` conformance-fixture indentation. The API guide
  now inventories `StorageGroup`, documents
  `createRecordStorage(context, spec, group?)`, and truthfully describes source
  type plus optional group identity. It no longer claims a RecordSpec storage
  key, layout fingerprint, or purpose key. `scripts/check-api-docs.mjs` already
  contains the required `StorageGroup` inventory entry and remains unchanged.
- Evidence: focused TenantRecords/history/commit/provider/Event Store tests pass
  5 files / 92 tests; package storage TypeScript compilation, exact full
  changed-TypeScript ESLint, changed-file Prettier, `git diff --check`, and
  storage TSDoc and full documentation TypeScript snippets all pass. Focused coverage executes the same 92 tests and
  reports `memory/tenant-records.ts` at 95.09% statements and 95.78% lines.
  Its nonzero process status is solely the repository-global 90% threshold
  against the untouched monorepo denominator (4.81% statements overall), not a
  test assertion or changed-source coverage failure.
- `docs:audience:check` and the direct API-doc checker remain blocked before
  their target checks by the existing 151-error integration inventory. Durable
  Datastore group-identity support is a stacked T-0134 release blocker, and
  durable RDBMS group-identity support is a stacked T-0135 release blocker;
  neither is represented as an implemented T-0133 provider capability or user
  documentation claim.

## 2026-08-07 - Canonical Event Tie Paging Closure

- The existing `implementer` completed this final bounded correction at the
  explicitly configured `gpt-5.6-terra` / `medium` profile. Independent runtime
  model introspection remains unavailable; the configured profile is the
  available metadata and no visible mismatch or fallback occurred.
- RED evidence: with page size two and equal Event version/timestamp values,
  shallow `backward(..., 1)` selected U+10000 instead of canonical UTF-8-greater
  U+10001 because provider slot IDs used UTF-16 code-unit order. A tied
  RecordStorage continuation after U+E000 also returned no non-BMP rows.
  GREEN evidence: storage slot identity and explicit `id` sort/continuation
  values now compare canonical UTF-8 bytes, while ordinary record-column and
  path string comparisons retain their existing code-unit behavior. Event
  history explicitly pages by version descending, creation descending, and ID
  descending, matching its final canonical ordering. The page-size-two shallow
  regression and tied continuation/top-window regression both pass.
- Removed the dangling `Storage adapters must make repeated` fragment from the
  API guide.
- Evidence: focused TenantRecords/history/commit/provider/Event Store tests pass
  5 files / 94 tests. Storage TypeScript, exact changed-TypeScript ESLint,
  changed-file Prettier, and `git diff --check` pass. Focused coverage runs the
  same tests and reports `memory/tenant-records.ts` at 92.88% statements and
  94.63% lines. Its nonzero process status is solely the existing global 90%
  threshold against the untouched monorepo denominator (4.98% statements
  overall), not a test or changed-source coverage failure.

## 2026-08-07 - Canonical UTF-8 Utility Deduplication

- The existing `implementer` completed this P2-only refactor at the explicitly
  configured `gpt-5.6-terra` / `medium` profile. Independent runtime-model
  introspection remains unavailable; configured profile metadata is the
  available evidence, with no visible mismatch or fallback.
- Extracted one documented internal `CanonicalUtf8` comparator/encoder used by
  both in-memory tenant record selection and Entity event-history ordering.
  Removed the duplicated hand-rolled UTF-8 comparators and encoders without
  changing the canonical ordering rule or public API.
- Evidence: focused TenantRecords/history/commit/provider/Event Store tests pass
  5 files / 94 tests; storage TypeScript, exact changed-TypeScript ESLint,
  Prettier, and `git diff --check` pass. Focused coverage executes the same
  tests and reports `canonical-utf8.ts` at 96.15% statements / 100% lines.
  Its nonzero process status is solely the existing 90% global monorepo
  threshold (4.91% statements overall), not a test failure.

## 2026-08-07 - Closure

- All required specialist concerns are clean. Final fixed reviewer profiles
  were `gpt-5.6-terra` / `high` for style, TypeScript/API, and reliability, and
  `gpt-5.6-luna` / `medium` for documentation. Runtime self-introspection was
  unavailable; no visible mismatch or fallback occurred.
- Closure preflight passed 95 focused tests, storage TypeScript, exact
  changed-file ESLint, Proto verification, documentation snippets, TSDoc,
  Prettier, and diff checks.
- The single coverage-enabled `verify:task` stopped only on the accepted
  integration-train compile inventory owned by T-0134 and later tasks. Proto
  generation and frozen-source verification passed before the stop; no local
  T-0133 regression was reported.
