# T-0051 Work Log

Status: Complete; merged to main, post-merge verified, and pushed

## 2026-07-21 — Framing And Research Intake

- Baseline/main: `1a33114712513aeac56c707d7da035c581ab8e3e`.
- Branch/worktree: `task/T-0051-storage-rdbms` at
  `.worktrees/T-0051-storage-rdbms`.
- Root retained four unrelated untracked files; the isolated worktree begins
  from tracked `main`, and the protected human review file is not a task input.
- Classified high-risk because persistence, transactions/CAS, tenant isolation,
  SQL generation, client lifecycle, dependencies, and real-database acceptance
  are in scope.
- The authoritative JVM repository was shallow-cloned read-only at
  `1b778327a6d24c9834102af86e18f9c532b8ac98`. Initial inventory confirms one
  `rdbms` module with JDBC factory, record/table mapping, engine detection,
  MySQL-specific write behavior, PostgreSQL type mappings, query operations,
  connection pooling, and real MySQL/PostgreSQL testcontainers coverage.
- Initial TypeScript inventory confirms the external seam already exists in
  `@spine-ts/storage`; Datastore implements it in a provider package with a
  small public root, provider-side query pushdown, bounded value/ID encoding,
  explicit client ownership, fake unit tests, and opt-in emulator/cloud tests.
- Full task-level skill applicability and expected child profiles are recorded
  in the task brief. No production or package file has been edited.

## 2026-07-21 — Requirements Split Complete

- Existing assigned role: `requirements_splitter`; explicit expected and
  dispatched profile: `gpt-5.6-sol` / `high`. The immutable configured role
  profile matches. Runtime self-introspection is unavailable, so no additional
  model identifier is asserted.
- Selected and fully read skill:
  `/Users/armiol/.agents/skills/codebase-design/SKILL.md`, plus its directly
  relevant `DESIGN-IT-TWICE.md` and `DEEPENING.md` references. No subagent was
  spawned; the project/assignment prohibition supersedes the skill's parallel
  design technique.
- Inspected governing sources: `AGENTS.md`, `build-protocol/BUILD_PROTOCOL.md`,
  `build-protocol/CODE_QUALITY.md`, current `packages/storage` and
  `packages/storage-datastore` sources/tests/docs, accepted T-0046 task/work
  records, and workspace package/build configuration.
- Verified the authoritative shallow JVM clone at
  `1b778327a6d24c9834102af86e18f9c532b8ac98` and inspected its JDBC factory,
  data-source/transaction handling, table/record/ID/column mapping, query and
  operation paths, MySQL upsert/type mapping, and real MySQL case/long-ID tests.
- Compared three materially different seams: public engine-neutral factory,
  caller-composed driver/dialect ports, and a MySQL-named adapter inside the
  shared RDBMS package. Chose the third for the deepest module, strongest
  locality, and honest seam placement.
- The binding plan is now in the task brief: small root interface, private
  two-table normalized schema, typed/bounded values, actual-slot identity,
  parameterized SQL, tenant predicates, atomic writeAll, row-lock/unique-key
  CAS, owned-pool lifecycle, redacted errors, real MySQL acceptance, dependency
  questions, risks/exclusions, and five ordered milestone packets.
- No production, test, dependency, review, Git, commit, or push state was
  changed. Planning edits are limited to `TASK.md` and this work log as assigned.
- No human blocker was found. The implementation dependency/version research
  is a non-blocking Packet 1 gate. Real MySQL availability is a later acceptance
  blocker if the opt-in suite cannot be run.

## 2026-07-21 — Plan Accepted And Packet 1 Dispatched

- Orchestrator acceptance confirmed the chosen public `MysqlStorageFactory`,
  unchanged generic storage seam, two private normalized tables, provider-side
  queries, and private future-dialect boundary as source-grounded and complete.
- Fresh-worktree preparation required one registry-backed retry after offline
  install lacked `tslib@2.8.1`; `pnpm install --frozen-lockfile` then completed
  without changing the lockfile.
- `pnpm proto:generate && pnpm typecheck:build:generated` passed.
- Focused baseline passed: six generic storage files / 46 tests and one
  Datastore factory file / 29 tests. The first pre-generation Vitest attempt
  reached zero tests because generated package outputs were absent; generation
  corrected the environment rather than repository code.
- Current registry evidence identifies `mysql2@3.23.1` as the stable candidate.
  Packet 1 must complete the driver/query-builder/harness comparison and record
  the accepted versions in D-0098 before runtime acceptance.
- Existing assigned role: `implementer`. Ownership: Packet 1 package scaffold,
  dependency/build references, public factory/configuration contract, private
  schema initialization proof, D-0098, and T-0051 records only. Expected and
  explicitly dispatched profile: `gpt-5.6-terra` / `medium`. No child spawning,
  commit, push, or changes outside this packet are authorized.

## 2026-07-21 — Packet 1 Implementation And Focused Evidence

- Existing assigned role: `implementer`; explicit dispatched profile:
  `gpt-5.6-terra` / `medium`. This surface exposes the immutable role/profile
  but not independent runtime self-introspection; no mismatch is visible.
- Added `@spine-ts/storage-rdbms` with exact `mysql2@3.23.1`, a small root
  (`MysqlStorageFactory`, driver-neutral URL/pool options, and three stable
  sanitized configuration/connection/schema errors), private fixed DDL, and
  an owned async close promise. `@spine-ts/storage` remains unchanged: `void`
  permits the promise-returning close override under TypeScript's method
  substitutability rules.
- Genuine RED: the first focused factory test failed because
  `packages/storage-rdbms/src/index.ts` did not exist. A later schema-version
  test failed because an incompatible default was accepted. GREEN: the focused
  fake-backed factory suite passed 5 tests and `pnpm typecheck:build:generated`
  passed after the minimal implementation.
- Real acceptance: with `SPINE_TS_MYSQL_URL` pointed at the supplied local
  MySQL 8.4.10 disposable database,
  `pnpm --filter @spine-ts/storage-rdbms test:mysql` passed 1 test. It
  concurrently created four factories, verified
  the two fixed `utf8mb4_bin` InnoDB tables and schema version default, then
  removed only those tables. The first run found an actual metadata-case
  failure and was corrected by explicit information-schema aliases before the
  green run.
- D-0098 records the mysql2/direct-driver, MySQL 8.4.10, and explicit external
  URL harness decisions. No Testcontainers dependency, query builder, public
  driver/SQL seam, generic storage-port change, CRUD/query/CAS behavior, commit,
  or push was introduced.
- Final focused mechanics passed: factory tests (5 passed), the ordinary
  suite with external proof intentionally skipped (5 passed, 1 skipped),
  `pnpm typecheck:build:generated`, focused ESLint, focused Prettier, and
  `git diff --check`. TypeDoc/API validation passed before the final
  test-harness-only adjustment. The final opt-in real-MySQL command also
  passed (1 test) after that adjustment.
- Audit hygiene correction: aligned the review evidence with 5 focused tests
  and completed mechanics, repaired the wrapped MySQL command, and reflowed
  changed API prose. Prettier and `git diff --check` pass. The deterministic
  full changed-file scan reports only pre-existing long lines in the decision
  log/API document; no newly added non-lock text exceeds 120 characters.
- Orchestrator acceptance reran the focused factory tests (5/5), live MySQL
  8.4.10 initialization/cleanup test (1/1), composite TypeScript build,
  focused ESLint and Prettier, TypeDoc/API validation, and `git diff --check`;
  all passed. Packet 1 is accepted without a specialist review wave because
  the task plan intentionally aggregates the four relevant concerns at the
  complete Packet 5 endpoint.

## 2026-07-21 — Packet 2 Dispatched

- Packet 1 commit `87bd8fb8` was pushed immediately to
  `origin/task/T-0051-storage-rdbms`.
- Existing assigned role/context: `implementer`. Ownership: Packet 2 private
  scope/tenant/slot and payload/value codecs, final CRUD schema refinements,
  atomic write/read/delete behavior, independent-handle and tenant isolation
  tests, real MySQL CRUD acceptance, and T-0051 records only. Expected and
  explicitly dispatched profile: `gpt-5.6-terra` / `medium`. No child
  spawning, commit, push, query translation, batching, or CAS is authorized.

## 2026-07-21 — Packet 2 Schema/Codec Ownership Transfer

- The original implementer produced green initial CRUD/ID/tenant and generic
  duplicate-column slices, then returned three times with the audited
  schema-v3/sortable-value sub-slice incomplete. Those uncommitted edits remain
  preserved but are not accepted evidence.
- A fresh instance of the existing `implementer` role now owns only the
  coherent schema-v3 widths/index/FK metadata, private sortable value codec,
  focused tests, and mechanical checks needed to finish that sub-slice. Expected
  and explicitly dispatched profile: `gpt-5.6-terra` / `medium`. It must not
  discard the prior edits, expand into live CRUD/query/batch/CAS behavior,
  commit, push, or spawn children.

## 2026-07-21 — Packet 2 Partial CRUD Evidence

- Existing implementer profile remains explicitly dispatched as `gpt-5.6-terra`
  / `medium`; this surface does not expose runtime self-introspection.
- Genuine RED: the live CRUD test failed with the Packet 1 unavailable-record
  operation error. GREEN: canonical recursive ID codec tests pass (2 tests),
  focused factory/codec tests pass (7 tests), TypeScript build and focused
  ESLint pass, and MySQL 8.4.10 live schema plus CRUD proof passes (2 tests).
- Schema version is now 2: column bytes use `VARBINARY(768)`, preserving an
  indexable bounded representation for Packet 3 rather than a MEDIUMBLOB.
  Packet 2 currently provides transactional one-record write/read/delete with
  bound scope/tenant/slot/column values and sanitized provider/decode errors.
- Remaining Packet 2 work: adversarial tenant/case/accent/boundary/payload and
  rollback coverage, independent-handle proofs, duplicate-column validation,
  and complete record-log evidence. No query translation, batches, or CAS was
  implemented.

## 2026-07-21 — Packet 2 Schema/Sortable Codec Sub-slice Accepted Locally

- Existing assigned role/context: `implementer`; explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`. This surface does not expose runtime
  self-introspection, so the immutable configured profile is the available
  runtime metadata.
- Genuine RED/GREEN: the new exact-value-data-boundary test first failed because
  the sortable string codec consumed an extra kind byte. The private codec now
  stores the kind only in `value_kind`, leaving all 768 `VARBINARY` bytes for
  data; the focused codec/factory/generic `RecordSpec` suite is green (17
  passed; 2 external-MySQL tests skipped without `SPINE_TS_MYSQL_URL`).
- Schema version 3 fixes widths at scope `512`, tenant `255`, slot `768`, column
  name `255`, and sortable value `768` bytes. Exact declared index totals are:
  records PK `512 + 255 + 768 = 1535`; columns PK `512 + 255 + 768 + 255 =
1790`; lookup `(scope_key, tenant_key, column_name, value_kind, value_data,
slot_key)` `512 + 255 + 255 + 1 + 768 + 768 = 2559`. Each is below MySQL's
  3072-byte InnoDB limit and is asserted in the factory test.
- Schema verification now fails closed on exact records/columns primary-key
  order, the six-column lookup order, and the three-column cascade foreign key
  to records. The fake metadata rows were updated to the v3 shape; DDL and
  metadata tests cover `ON DELETE CASCADE`.
- Focused typecheck, ESLint, Prettier, and `git diff --check` pass. The optional
  live MySQL harness remains unrun because no external URL was supplied; no
  query translation, batching, CAS, public export, commit, or push was added.

## 2026-07-21 — Packet 2 Adversarial CRUD Continuation

- The successful replacement `implementer` context retains Packet 2 ownership
  for the remaining unit and live-MySQL CRUD acceptance cases. Its expected and
  explicitly dispatched immutable profile remains `gpt-5.6-terra` / `medium`;
  independent runtime self-introspection is unavailable.
- Scope is limited to corrupt-payload distinction, pre-pool bounds, dynamic
  tenant isolation, canonical ID/value edges, independent handles, stale
  column replacement, cascade cleanup, transactional rollback, sanitized
  operation errors, live schema v3 proof, and records/mechanics. Query
  translation, batches, CAS, commits, pushes, and child spawning remain
  excluded.

## 2026-07-21 — Packet 2 Adversarial CRUD Evidence Accepted Locally

- RED/GREEN: unit tests first proved that v3 `value_data` remained nullable and
  that a non-byte payload was incorrectly treated as absence. GREEN changes
  make `value_data` `NOT NULL`, verify `IS_NULLABLE`, preserve a present corrupt
  payload as `MysqlStorageDataError`, and use the exported, sanitized
  `MysqlStorageOperationError` for read/write/delete provider failures.
- Unit evidence: focused RDBMS codec/factory plus generic `RecordSpec` tests
  pass with 21 tests; the same command skips 7 live cases when no external URL
  is configured. It proves malformed canonical aliases, blank/oversize
  scope/tenant/slot/column/value rejection before pool acquisition, exact
  boundaries, and provider error sanitation.
- Live MySQL 8.4.10 evidence: the explicit `SPINE_TS_MYSQL_URL` package command
  passes all 7 tests. It proves exact v3 columns/nullability/PK/lookup/FK
  metadata, independent handles, operation-time tenant switching, case/accent
  ID distinction, all canonical ID variants, exact 768-byte slot and sortable
  string boundaries, signed-64 endpoints, stale-column removal, cascade
  deletion true/false behavior, malformed payload distinction, and transaction
  rollback. The rollback case uses a disposable named check constraint (the
  supplied server disallows trigger creation under binary logging), removes it
  in `finally`, and proves both prior payload and prior encoded column bytes
  survive the failed insert.
- Accepted ID-order decision: packet 3's deterministic ordering and
  continuation use only the binary canonical `slot_key` bytes; no separate
  semantic slot-order column exists. This is recorded in the task ledger.
- Typecheck, focused ESLint/Prettier, TypeDoc/API validation, and
  `git diff --check` pass. No query translation, write-all, CAS, commit, or
  push was added.

## 2026-07-21 — Packet 2 Orchestrator Acceptance

- The orchestrator independently reran the focused RDBMS codec/factory and
  generic `RecordSpec` suite: 3 files and 21 tests passed.
- The explicit MySQL 8.4.10 suite independently passed all 7 integration cases.
- Composite TypeScript build, focused ESLint, Prettier, canonical TypeDoc/API
  validation, and `git diff --check` passed. An initial nonexistent
  `docs:api:check` invocation was corrected to the repository's canonical
  `docs:check:generated` command; it was a command-selection error, not a
  product failure.
- Source audit accepted the private codecs, exact schema-v3 metadata checks,
  pre-connection validation, transactional CRUD/rollback behavior, sanitized
  errors, and generic duplicate-column guard. Packet 2 is accepted; Packet 3
  owns query translation and deterministic bounded result materialization.

## 2026-07-21 — Packet 3 Dispatched

- Packet 2 commit `07481614` was pushed immediately and the local/remote task
  refs were confirmed equal at
  `0748161426a8961b542025ad3059e61e7b94c7d6`.
- Existing assigned role: `implementer`. Ownership: Packet 3 private SQL query
  translation, query/value validation, deterministic ordering/continuation,
  offset/limit pushdown, representative real-MySQL index-plan proof, focused
  tests, and T-0051 records only. Expected and explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`. No child spawning, commit, push, generic query API
  change, batching, CAS, lifecycle, or Packet 5 documentation is authorized.

## 2026-07-21 — Packet 3 Query Translation Evidence

- Existing `implementer` assignment retained its explicit immutable profile
  `gpt-5.6-terra` / `medium`; this surface exposes no independent runtime
  self-introspection, so configured dispatch metadata is the available evidence.
- TDD RED/GREEN: focused query tests first failed at the Packet 2 unavailable
  operation, then passed after private MySQL query translation was added. SQL
  binds scope/tenant, IDs, typed column values, continuation values, offset,
  and limit; dynamic names remain encoded bound values.
- Packet 3 compiles ANDed filter joins with independent aliases, typed mixed-kind
  IN alternatives, materialized-column sorting, canonical binary `slot_key` as
  the final ascending tie-break, complete lexicographic keyset continuation,
  and SQL offset/limit. Empty filter arrays return before pool acquisition;
  `ids: []` remains unrestricted. Dotted paths are rejected rather than scanned.
- Focused fake-backed factory suite passed 12 tests. Opt-in MySQL 8.4.10 proof
  at the supplied `SPINE_TS_MYSQL_URL` passed 8 tests, including typed filters,
  mixed boolean/string continuation, repeated-column aliases, no-sort slot
  continuation, tenant-scoped rows, corrupt payload classification, and an
  `EXPLAIN` assertion for `spine_ts_columns_lookup`.
- Limitation: MySQL server-prepared statements reject JS-number `LIMIT` binds
  with `Incorrect arguments to mysqld_stmt_execute`; read queries therefore use
  mysql2's parameterized `query()` path, retaining bound values without SQL
  interpolation. No client-side result scan or materialization is used.

## 2026-07-21 — Packet 3 Acceptance Evidence Correction

- The existing `implementer` correction batch retained its explicitly dispatched
  immutable `gpt-5.6-terra` / `medium` profile; independent runtime
  self-introspection remains unavailable on this surface.
- Source audit requested direct real-MySQL proof for multi-field mixed-direction
  tuples, descending continuation, positive offset-before-limit, and dynamic
  query tenant isolation. The new test was added before rerunning the suite;
  it passed without a production-code change because the established compiler
  already emits the complete tuple/keyset predicates and scope/tenant binds.
- Live evidence now proves ascending `group`, descending `rank`, canonical
  ascending slot tie-break, descending `rank` keyset continuation, offset `1`
  before limit `2`, and a mutable operation-time tenant adversary: one storage,
  one constant slot, and one shared filter value return only that tenant's
  distinct payload after each tenant switch. The opt-in suite now passes 9 tests.

## 2026-07-21 — Packet 3 Orchestrator Acceptance

- Initial audit rejected the first green draft because bind order, mixed-kind
  predicates, cross-kind keysets, and required multi-sort/window/tenant evidence
  were incomplete. One correction batch in the same implementation context
  repaired the compiler and added the missing adversarial real-MySQL cases.
- Independent final verification passed 3 focused files / 23 tests and all 9
  MySQL 8.4.10 integration cases. Composite TypeScript build, focused ESLint,
  Prettier, and `git diff --check` also passed.
- Source audit accepts parameter ordering, typed mixed-kind alternatives,
  lexicographic ascending/descending continuation, independent aliases,
  deterministic slot tie-breaking, SQL window pushdown, actual-slot decoding,
  tenant/scope isolation, and sanitized corrupt/provider failures. Packet 3 is
  accepted; Packet 4 owns transactional batching and CAS.

## 2026-07-21 — Packet 4 Dispatched

- Packet 3 commit `a2c0d985` was pushed immediately and local/remote task refs
  were confirmed equal at `a2c0d9857a9eca626e20ce5d94c80db78c925bc5`.
- Existing assigned role/context: `implementer`. Ownership: Packet 4 atomic
  `writeAll`, complete payload-based compare-and-set, missing-row unique-key
  races, existing-row locks, conditional delete, rollback/commit/release
  behavior, independent-handle concurrency, focused/live tests, and T-0051
  records only. Expected and explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`. No child spawning, commit, push, generic API,
  query, schema/public surface, lifecycle, or Packet 5 documentation changes
  are authorized.

## 2026-07-21 — Packet 4 Initial Batch/CAS Evidence

- Existing `implementer` profile remains explicitly dispatched as
  `gpt-5.6-terra` / `medium`; runtime self-introspection is unavailable.
- Initial runtime implementation pre-encodes batch records before pool use,
  writes ordered replacements in one transaction, and locks/scopes existing CAS
  rows before comparing deterministic payload bytes. Missing-row creation uses
  the records primary key and maps only `ER_DUP_ENTRY` to `false`.
- The first real-MySQL Packet 4 behavior case passes: multi-record batch
  success with duplicate-slot last-write-wins, absent create, replacement,
  stale mismatch, and conditional delete. Failure injection, synchronized race,
  logical-ID divergence, and release-path proof remain in this packet.
- Corrected missing-row CAS for InnoDB: `undefined -> next` now claims the
  records primary key directly before any locking read. Only a duplicate from
  that records insert becomes `false`; column/provider failures remain
  sanitized errors, and rollback failure is not suppressed before that result.

## 2026-07-21 — Packet 4 Acceptance Ownership Transfer

- The first Packet 4 owner returned three partial boundaries after establishing
  the core batch/CAS path, live ordered-batch behavior, direct unique-key race,
  and addressed-slot semantics. The preserved slice remains unaccepted because
  the required fake failure/release matrix and real later-column batch rollback
  proof are absent.
- A fresh instance of the existing `implementer` role now owns only completion
  of that failure matrix, any minimal production corrections it exposes, final
  Packet 4 focused/live mechanics, and T-0051 records. Expected and explicitly
  dispatched profile: `gpt-5.6-terra` / `medium`. No child spawning, commit,
  push, review, lifecycle, documentation, query/schema/public API expansion, or
  loss of the preserved partial work is authorized.

## 2026-07-21 — Packet 4 Completion Evidence

- Existing assigned role/context: `implementer`. The expected and explicitly
  dispatched immutable profile was `gpt-5.6-terra` / `medium`; this execution
  surface does not expose independent runtime self-introspection, so that
  configured profile is the available metadata.
- Focused fake-backed RED/GREEN completion adds a configurable connection fake
  and proves batch pre-encoding rejects before pool acquisition, exactly one
  connection/transaction, ordered statements, later statement and commit
  failure rollback exactly once before release, and sanitized acquisition
  errors. The one source-proven correction centralizes record-operation pool
  acquisition so provider errors cannot escape unredacted.
- CAS matrix now proves exact duplicate claims return `false` only after a
  successful single rollback; duplicate rollback failure and nonduplicate claim
  failure are `MysqlStorageOperationError`; stale mismatch and corrupt locked
  payload each roll back and release once. The direct records-key claim remains
  the only duplicate-to-false path.
- Real MySQL 8.4.10 evidence adds a disposable check constraint removed in
  `finally`: a batch changes one prior row, reaches a later record's column
  insert failure, and proves all prior payloads/column bytes survive with no
  later partial row. The independent-handle absent-create test now gates both
  calls at one start barrier and proves exactly one `true`/`false`; it retains
  addressed-slot/logical-body-ID divergence coverage.
- Evidence: 21 fake factory tests and 11 codec/generic-RecordSpec tests pass
  (32 total); explicit `mysql://spine:spine_test@127.0.0.1:33306/spine_t0051`
  acceptance passes all 12 integration tests. `pnpm typecheck:build:generated`,
  focused ESLint, Prettier, and `git diff --check` pass. Packet 4 is
  implementation-complete pending orchestrator acceptance; no commit, push,
  review, or lifecycle work was performed.

## 2026-07-21 — Packet 5 Lifecycle And Documentation Progress

- Existing `implementer` dispatch remains explicitly `gpt-5.6-terra` /
  `medium`; runtime self-introspection is unavailable on this surface.
- Lifecycle TDD adds private handle unregistration, factory-close behavior, and
  a gated admitted-operation proof. Twenty-four focused factory tests pass,
  including shared close promise, observable pool-end failure, manual-handle
  closure without pool closure, synchronous prevention of new handles/work, and
  admitted write completion/release before close settles.
- The RDBMS README and user guide now describe the implemented MySQL behavior,
  exact UTF-16/string boundary, lifecycle, least-privilege operations, and
  opt-in local proof. Packet 5 remains in progress pending release-truth and
  broader verification work.

## 2026-07-21 — Packet 4 Orchestrator Acceptance

- Independent final verification passed 3 focused files / 32 tests and all 12
  MySQL 8.4.10 integration cases. Composite TypeScript, focused ESLint,
  Prettier, canonical TypeDoc/API validation, and `git diff --check` passed.
- Source audit accepts whole-batch pre-encoding, ordered one-transaction writes,
  exact rollback/release behavior, sanitized acquisition/provider failures,
  existing-row payload locks, direct unique-key absent creation, precise
  duplicate classification, addressed-slot semantics, conditional deletion,
  and the independent-handle race. Every SQL mutation remains scope/tenant
  bound. Packet 4 is accepted; Packet 5 owns lifecycle, documentation, coverage,
  final specialist review, and task integration closure.

## 2026-07-21 — Packet 5 Dispatched

- Packet 4 commit `33cc5dc3` was pushed immediately and local/remote task refs
  were confirmed equal at `33cc5dc33d5ab30141c407cc4d5e410b7c2915ff`.
- Existing assigned role/context: `implementer`. Ownership: MySQL factory/handle
  lifecycle and retained-handle cleanup, package README, framework user guide,
  root/release-readiness truth, package graph/API documentation alignment,
  focused/full coverage remediation, and T-0051 records before the final review
  wave. Expected and explicitly dispatched profile: `gpt-5.6-terra` / `medium`.
  No child spawning, commit, push, reviewer work, PostgreSQL implementation,
  public driver/SQL seam, or unrelated cleanup is authorized.

## 2026-07-21 — Packet 5 Coverage Ownership Transfer

- The lifecycle/documentation owner completed the handle-unregistration and
  admitted-operation close proof plus the initial guide/root/API corrections,
  but returned multiple partial boundaries without closing coverage or release
  inventory. All edits remain preserved and unaccepted.
- A fresh instance of the existing `implementer` role now owns behavior-focused
  RDBMS coverage remediation, the outstanding release inventory/docs cleanup,
  full pre-review mechanics, and T-0051 records. Expected and explicitly
  dispatched profile: `gpt-5.6-terra` / `medium`. No child spawning, threshold
  or exclusion changes, unrelated production fixes, reviews, commits, pushes,
  PostgreSQL runtime, or loss of preserved work is authorized.

## 2026-07-21 — Packet 5 Coverage And Lifecycle Continuation

- Existing assigned role: `implementer`; explicit dispatch profile:
  `gpt-5.6-terra` / `medium`. This execution surface exposes no independent
  runtime model/reasoning self-introspection; the immutable orchestrator
  dispatch profile is the available metadata limitation.
- The private handle callback now closes over its exact handle and accepts no
  argument, removing the previous `unknown` type assertion without changing a
  public contract. Retained lifecycle tests prove manual-handle unregistration,
  idempotent close, factory-before-pool close ordering, shared pool-close
  failure, and an admitted operation releasing before factory close settles.
- Focused coverage command `pnpm exec vitest --root ../.. run --coverage
test/mysql-codec.test.ts test/mysql-storage-factory.test.ts
test/mysql-integration.test.ts` recorded 41 unit tests passed and 12 opt-in
  MySQL cases skipped without `SPINE_TS_MYSQL_URL`. Package sources exceed 90%
  in every dimension: factory 318/332 lines (95.78%), 154/171 branches
  (90.05%), 67/71 functions (94.37%); codec 94/96 lines (97.92%), 112/121
  branches (92.56%), 10/10 functions (100%). The global coverage invocation is
  expected to fail when filtered because its repository-wide threshold includes
  untouched source files; no threshold, include, exclude, or configuration was
  changed.
- Documentation audit corrected the guide from the false claim that only
  declared columns are queryable: any materialized column name or `id` can be
  queried, while an absent materialized name matches no rows. The README/user
  guide remain MySQL-only, disclose PostgreSQL as unsupported future work, and
  describe exact configuration/TLS, lifecycle, errors, bounds, transactions,
  schema/tenancy, opt-in live test, query limits, and credential handling.
- Mechanical checks passed: generated build/typecheck, targeted ESLint,
  Prettier, TypeDoc/API (`pnpm docs:check`), release-readiness inventory, and
  `git diff --check`. The first canonical `pnpm test:coverage` attempt was
  invalid because another shared-worktree Vitest process removed
  `coverage/.tmp/coverage-15.json`; it is recorded for an uncontended rerun,
  not treated as a product/test failure.

## 2026-07-21 — Packet 5 Orchestrator Pre-Review Acceptance

- Independent focused coverage passed 41/41 tests at 95.04% statements,
  91.09% branches, 95.06% functions, and 96.26% lines for the RDBMS package.
  Independent opt-in acceptance against disposable MySQL 8.4.10 passed all 12
  cases.
- The uncontended canonical `pnpm --config.verify-deps-before-run=false
test:coverage:generated` run passed 82 files with three skipped and 1,897
  tests with 20 skipped. Repository coverage is 94.44% statements, 90.14%
  branches, 94.69% functions, and 94.91% lines. This supersedes the invalid
  concurrent coverage attempt and confirms the prior ZeroMQ zero-coverage
  result was not reproducible without shared-output contention.
- Lightweight status/docs/public-surface lint corrected the stale Packet 5
  headers and found no product issue, public driver/SQL leak, future PostgreSQL
  overclaim, duplicated policy, generated output, or tracked review scratch.
  Packet 5 is accepted for one four-concern specialist wave; no implementation
  commit or push occurs before that required review gate.

## 2026-07-21 — Packet 5 Specialist Wave 1

- Documentation returned two P2 accuracy corrections; style/maintainability
  returned one P2 SQL-composition correction and one P3 test-name clarification;
  TypeScript/API returned one P2 public `RecordSpec` invariant correction; and
  performance/reliability returned three P2 corrections for close-error
  sanitation, bounded query structure, and real-MySQL in-flight shutdown proof.
  No lane found a P0 or P1 issue.
- The complete wave is accepted as one batch. The existing implementer context
  owns the corrections and affected verification with explicit
  `gpt-5.6-terra` / `medium`; no commit, push, unrelated refactor, PostgreSQL
  runtime, public SQL seam, threshold/config weakening, or child spawning is
  authorized.

## 2026-07-21 — Packet 5 Affected Re-Review

- Style/maintainability, documentation, and TypeScript/API returned clean.
  Reliability accepted the corrections and found one final P2 test gap for an
  admitted, pending pool acquisition that rejects while factory close waits.
- The P2 is accepted as a targeted test-only correction under the two-wave
  limit. The existing implementer context is explicitly `gpt-5.6-terra` /
  `medium`; it may change only deterministic fake lifecycle proof and durable
  records, then rerun affected mechanical/live gates. No third full specialist
  wave, production expansion, commit, or push is authorized in that pass.

## 2026-07-21 — Packet 5 Independent Final-Gate Failure

- Full `pnpm --config.verify-deps-before-run=false verify` failed at
  `typecheck:tooling` on T-0051 test-only declarations and narrowing. Errors
  cover mysql2 `RowDataPacket` query constraints, exact-optional fake call
  records, schema-row fixture shape, and unsafe test result/property access.
- The failure is task-owned and actionable; it is not classified as baseline or
  environment debt. The existing implementer is explicitly
  `gpt-5.6-terra` / `medium` for one mechanical test-only correction and must
  rerun tooling typecheck, affected tests, and the full gate. No commit, push,
  runtime/public/docs change, or third review wave is authorized.

## 2026-07-21 — T-0051 Orchestrator Acceptance

- The mechanical tooling correction is accepted: independent
  `typecheck:tooling` passes, and the capable-surface full `pnpm verify` passes
  ordinary plus coverage runs at 82 files passed/three skipped and 1,901 tests
  passed/21 skipped. Repository coverage is 94.45% statements, 90.14%
  branches, 94.71% functions, and 94.93% lines; every remaining repository gate
  passes.
- Independent final MySQL 8.4.10 acceptance passes all 13 cases. Review
  convergence has no unresolved P0/P1 or accepted P2. T-0051 is complete on its
  task branch and proceeds to commit, immediate remote push, main integration,
  post-merge verification, and main push.

## 2026-07-21 — T-0051 Integration Closure

- Packet 5 was committed as `c4a0ba52` and immediately pushed; task local and
  remote refs match. The complete task was merged into `main` as `81a6b335` and
  immediately pushed; main local and remote refs matched before this closure
  record.
- The first post-merge gate exposed only stale main-worktree dependency links.
  Offline restoration lacked one lockfile tarball; the approved frozen-lockfile
  install restored all 12 workspace projects without changing the lockfile.
- Post-merge full verification then passed 82 files/1,901 tests in both ordinary
  and coverage runs, with three files/21 tests skipped and 94.45% statements,
  90.14% branches, 94.71% functions, and 94.93% lines. All static, docs/API,
  Protobuf, generated-clean, and release gates passed. Final MySQL 8.4.10
  acceptance passed 13/13. T-0051 is durably complete.

## 2026-07-21 — Packet 5 Wave 1 Corrections

- Existing assigned role/context: `implementer`. The expected and explicitly
  dispatched profile was `gpt-5.6-terra` / `medium`; this surface exposes no
  independent runtime model/reasoning self-introspection, so the immutable
  configured dispatch is the available metadata.
- All accepted findings are resolved. Query fragments now keep SQL text with
  their bind values through compilation and concatenate values in exact SQL
  order. A focused assertion covers multi-kind filtering, sorting,
  continuation, and offset. The manual-close test now names only its observable
  behavior.
- Private, fixed query limits reject before pool acquisition: 256 IDs, 32
  filters, 64 values per filter, eight sorts, and 2,048 total binds. Boundary
  tests accept every exact maximum, including an exact 2,048-bind query;
  overflow tests reject every component and the aggregate bind count.
- Pool-close rejection is translated to `MysqlStorageConnectionError` without
  exposing the provider message, while repeated `close()` calls retain the same
  promise and rejection. A private admitted-operation counter prevents
  `pool.end()` until every admitted connection is released and prevents new
  work once closing starts.
- Documentation now states that every factory startup executes
  `CREATE TABLE IF NOT EXISTS`, requires that privilege, limits materialized
  column names to 255 UTF-8 bytes, and describes the exact nonconfigurable query
  limits and close behavior. `RecordSpec` TSDoc and public API prose now state
  duplicate declared-column names are rejected.
- RED evidence reproduced three unit defects: provider close text escaped, 257
  IDs were accepted, and a query above 2,048 binds was accepted. The first live
  lifecycle proof also exposed that immediate `pool.end()` interrupted an
  admitted write. The fake lifecycle proof then remained RED until pool ending
  was deferred behind connection release. All are GREEN after the bounded
  production corrections.
- The opt-in MySQL proof uses only normal transaction/DML access: a control
  connection holds `SELECT ... FOR UPDATE` on the exact target row, the adapter
  write is observed blocked through bounded `information_schema.processlist`
  polling, factory close rejects new work and remains unsettled, then control
  commit lets the write release and close resolve. No sleep is a correctness
  oracle and no `LOCK TABLES` privilege is required.
- Focused unit/API verification passes three files and 49 tests. Focused RDBMS
  coverage is 95.18% statements, 91.13% branches, 95.55% functions, and 96.52%
  lines. Opt-in MySQL 8.4.10 verification passes all 13 cases. Generated build
  typecheck, focused ESLint, Prettier, generated TypeDoc/API checks,
  release-readiness inventory, and `git diff --check` pass.
- Parallel typecheck and documentation generation briefly collided while both
  regenerated shared Protobuf outputs; sequential regeneration restored the
  workspace and all canonical checks passed. A separate tooling typecheck still
  exposes the existing mysql2 test-fake row-typing backlog; the new process-row
  helper conforms to `RowDataPacket` and adds no new error. No commit, push, or
  reviewer work was performed; all four affected concerns remain pending
  re-review.

## 2026-07-21 — Packet 5 Final Targeted Correction

- Existing assigned role/context: `implementer`; the expected and explicitly
  configured profile is `gpt-5.6-terra` / `medium`. Runtime model/reasoning
  self-introspection remains unavailable on this surface, so the immutable
  dispatch is the available metadata.
- The final accepted P2 is closed by one deterministic fake-pool lifecycle
  case. A deferred `getConnection()` remains admitted while factory close is
  started; the test proves close is unsettled and pool end has not run, rejects
  acquisition with provider text, observes sanitized
  `MysqlStorageOperationError`, and then proves exactly one pool end and close
  completion. Only the fake hook and this regression test changed; production,
  public API, and documentation behavior did not.
- RED failed at the intended missing fake seam: `acquisitionPending` remained
  false while the other 36 lifecycle tests passed. The minimal fake then awaits
  the opt-in deferred acquisition hook; GREEN passes all 37 lifecycle tests.
- Focused unit/API coverage passes three files and 50 tests at 95.18%
  statements, 91.13% branches, 95.55% functions, and 96.52% lines. Generated
  build/typecheck passes after verification of 25 copied Protobuf checksums;
  targeted ESLint, Prettier, and `git diff --check` pass.
- The first live attempt was denied by sandbox loopback policy with
  `connect EPERM 127.0.0.1:33306`; the approved rerun against the existing
  MySQL 8.4.10 test instance passes all 13 integration cases. This is an
  execution-surface restriction, not a product failure.
- The two-wave correction cycle is converged with no third specialist wave.
  No commit, push, review, production expansion, public change, or unrelated
  cleanup was performed; orchestrator acceptance remains next.

## 2026-07-21 — Packet 5 Final-Gate Tooling Correction

- Existing assigned role/context: `implementer`; expected and explicitly
  configured profile: `gpt-5.6-terra` / `medium`. Independent runtime
  model/reasoning self-introspection is unavailable, so the immutable dispatch
  remains the available metadata.
- RED `pnpm typecheck:tooling` reproduced every task-owned error: mysql2 query
  result declarations did not satisfy `RowDataPacket`, one uninferrable column
  callback exposed `object`, one optional row access could construct bytes from
  `undefined`, fake call records explicitly materialized optional `undefined`,
  and one schema fixture replacement discarded required fields.
- The mechanical test correction adds small named row interfaces extending
  `RowDataPacket`, an explicit `StringValue` callback parameter, a missing-row
  guard, conditional optional-property construction, and a fixture spread.
  No blanket cast, `as any`, runtime behavior, public API, documentation, query,
  or lifecycle change was introduced.
- GREEN tooling typecheck and composite build/typecheck pass. Focused coverage
  passes three files and 50 tests at 95.18% statements, 91.13% branches, 95.55%
  functions, and 96.52% lines. The MySQL 8.4.10 suite passes 13/13. Targeted
  ESLint, Prettier, and `git diff --check` pass.
- The first full gate exposed seven task-owned source lines over the cleanup
  limit. Those lines were wrapped mechanically; the four factory edits preserve
  the exact concatenated SQL/comment text and change no behavior. Cleanup,
  targeted lint, formatting, and tooling typecheck then passed.
- A sandboxed full test run was invalid because loopback TCP and ZeroMQ IPC
  binds failed with `EPERM`. The approved capable-surface command
  `pnpm --config.verify-deps-before-run=false verify` passed end-to-end:
  ordinary and coverage runs each passed 82 files with three skipped and 1,901
  tests with 21 skipped. Repository coverage is 94.45% statements, 90.14%
  branches, 94.71% functions, and 94.93% lines. TypeDoc/API, Protobuf
  lint/generated cleanliness, and release readiness at 60 package imports and
  120 relative Markdown links all pass.
- Packet 5 is final-gate verified for orchestrator acceptance. No commit, push,
  specialist review, runtime/public/docs expansion, or unrelated cleanup was
  performed.
