# T-0051 Work Log

Status: Packet 3 accepted; Packet 4 next

## 2026-07-21 — Framing And Research Intake

- Baseline/main: `1a33114712513aeac56c707d7da035c581ab8e3e`.
- Branch/worktree: `task/T-0051-storage-rdbms` at
  `.worktrees/T-0051-storage-rdbms`.
- Root retained four unrelated untracked files; the isolated worktree begins
  from tracked `main`, and the protected human review file is not a task input.
- Classified high-risk because persistence, transactions/CAS, tenant isolation,
  SQL generation, client lifecycle, dependencies, and real-database acceptance
  are in scope.
- The authoritative JVM repository was shallow-cloned read-only at
  `1b778327a6d24c9834102af86e18f9c532b8ac98`. Initial inventory confirms one
  `rdbms` module with JDBC factory, record/table mapping, engine detection,
  MySQL-specific write behavior, PostgreSQL type mappings, query operations,
  connection pooling, and real MySQL/PostgreSQL testcontainers coverage.
- Initial TypeScript inventory confirms the external seam already exists in
  `@spine-ts/storage`; Datastore implements it in a provider package with a
  small public root, provider-side query pushdown, bounded value/ID encoding,
  explicit client ownership, fake unit tests, and opt-in emulator/cloud tests.
- Full task-level skill applicability and expected child profiles are recorded
  in the task brief. No production or package file has been edited.

## 2026-07-21 — Requirements Split Complete

- Existing assigned role: `requirements_splitter`; explicit expected and
  dispatched profile: `gpt-5.6-sol` / `high`. The immutable configured role
  profile matches. Runtime self-introspection is unavailable, so no additional
  model identifier is asserted.
- Selected and fully read skill:
  `/Users/armiol/.agents/skills/codebase-design/SKILL.md`, plus its directly
  relevant `DESIGN-IT-TWICE.md` and `DEEPENING.md` references. No subagent was
  spawned; the project/assignment prohibition supersedes the skill's parallel
  design technique.
- Inspected governing sources: `AGENTS.md`, `build-protocol/BUILD_PROTOCOL.md`,
  `build-protocol/CODE_QUALITY.md`, current `packages/storage` and
  `packages/storage-datastore` sources/tests/docs, accepted T-0046 task/work
  records, and workspace package/build configuration.
- Verified the authoritative shallow JVM clone at
  `1b778327a6d24c9834102af86e18f9c532b8ac98` and inspected its JDBC factory,
  data-source/transaction handling, table/record/ID/column mapping, query and
  operation paths, MySQL upsert/type mapping, and real MySQL case/long-ID tests.
- Compared three materially different seams: public engine-neutral factory,
  caller-composed driver/dialect ports, and a MySQL-named adapter inside the
  shared RDBMS package. Chose the third for the deepest module, strongest
  locality, and honest seam placement.
- The binding plan is now in the task brief: small root interface, private
  two-table normalized schema, typed/bounded values, actual-slot identity,
  parameterized SQL, tenant predicates, atomic writeAll, row-lock/unique-key
  CAS, owned-pool lifecycle, redacted errors, real MySQL acceptance, dependency
  questions, risks/exclusions, and five ordered milestone packets.
- No production, test, dependency, review, Git, commit, or push state was
  changed. Planning edits are limited to `TASK.md` and this work log as assigned.
- No human blocker was found. The implementation dependency/version research
  is a non-blocking Packet 1 gate. Real MySQL availability is a later acceptance
  blocker if the opt-in suite cannot be run.

## 2026-07-21 — Plan Accepted And Packet 1 Dispatched

- Orchestrator acceptance confirmed the chosen public `MysqlStorageFactory`,
  unchanged generic storage seam, two private normalized tables, provider-side
  queries, and private future-dialect boundary as source-grounded and complete.
- Fresh-worktree preparation required one registry-backed retry after offline
  install lacked `tslib@2.8.1`; `pnpm install --frozen-lockfile` then completed
  without changing the lockfile.
- `pnpm proto:generate && pnpm typecheck:build:generated` passed.
- Focused baseline passed: six generic storage files / 46 tests and one
  Datastore factory file / 29 tests. The first pre-generation Vitest attempt
  reached zero tests because generated package outputs were absent; generation
  corrected the environment rather than repository code.
- Current registry evidence identifies `mysql2@3.23.1` as the stable candidate.
  Packet 1 must complete the driver/query-builder/harness comparison and record
  the accepted versions in D-0098 before runtime acceptance.
- Existing assigned role: `implementer`. Ownership: Packet 1 package scaffold,
  dependency/build references, public factory/configuration contract, private
  schema initialization proof, D-0098, and T-0051 records only. Expected and
  explicitly dispatched profile: `gpt-5.6-terra` / `medium`. No child spawning,
  commit, push, or changes outside this packet are authorized.

## 2026-07-21 — Packet 1 Implementation And Focused Evidence

- Existing assigned role: `implementer`; explicit dispatched profile:
  `gpt-5.6-terra` / `medium`. This surface exposes the immutable role/profile
  but not independent runtime self-introspection; no mismatch is visible.
- Added `@spine-ts/storage-rdbms` with exact `mysql2@3.23.1`, a small root
  (`MysqlStorageFactory`, driver-neutral URL/pool options, and three stable
  sanitized configuration/connection/schema errors), private fixed DDL, and
  an owned async close promise. `@spine-ts/storage` remains unchanged: `void`
  permits the promise-returning close override under TypeScript's method
  substitutability rules.
- Genuine RED: the first focused factory test failed because
  `packages/storage-rdbms/src/index.ts` did not exist. A later schema-version
  test failed because an incompatible default was accepted. GREEN: the focused
  fake-backed factory suite passed 5 tests and `pnpm typecheck:build:generated`
  passed after the minimal implementation.
- Real acceptance: with `SPINE_TS_MYSQL_URL` pointed at the supplied local
  MySQL 8.4.10 disposable database,
  `pnpm --filter @spine-ts/storage-rdbms test:mysql` passed 1 test. It
  concurrently created four factories, verified
  the two fixed `utf8mb4_bin` InnoDB tables and schema version default, then
  removed only those tables. The first run found an actual metadata-case
  failure and was corrected by explicit information-schema aliases before the
  green run.
- D-0098 records the mysql2/direct-driver, MySQL 8.4.10, and explicit external
  URL harness decisions. No Testcontainers dependency, query builder, public
  driver/SQL seam, generic storage-port change, CRUD/query/CAS behavior, commit,
  or push was introduced.
- Final focused mechanics passed: factory tests (5 passed), the ordinary
  suite with external proof intentionally skipped (5 passed, 1 skipped),
  `pnpm typecheck:build:generated`, focused ESLint, focused Prettier, and
  `git diff --check`. TypeDoc/API validation passed before the final
  test-harness-only adjustment. The final opt-in real-MySQL command also
  passed (1 test) after that adjustment.
- Audit hygiene correction: aligned the review evidence with 5 focused tests
  and completed mechanics, repaired the wrapped MySQL command, and reflowed
  changed API prose. Prettier and `git diff --check` pass. The deterministic
  full changed-file scan reports only pre-existing long lines in the decision
  log/API document; no newly added non-lock text exceeds 120 characters.
- Orchestrator acceptance reran the focused factory tests (5/5), live MySQL
  8.4.10 initialization/cleanup test (1/1), composite TypeScript build,
  focused ESLint and Prettier, TypeDoc/API validation, and `git diff --check`;
  all passed. Packet 1 is accepted without a specialist review wave because
  the task plan intentionally aggregates the four relevant concerns at the
  complete Packet 5 endpoint.

## 2026-07-21 — Packet 2 Dispatched

- Packet 1 commit `87bd8fb8` was pushed immediately to
  `origin/task/T-0051-storage-rdbms`.
- Existing assigned role/context: `implementer`. Ownership: Packet 2 private
  scope/tenant/slot and payload/value codecs, final CRUD schema refinements,
  atomic write/read/delete behavior, independent-handle and tenant isolation
  tests, real MySQL CRUD acceptance, and T-0051 records only. Expected and
  explicitly dispatched profile: `gpt-5.6-terra` / `medium`. No child
  spawning, commit, push, query translation, batching, or CAS is authorized.

## 2026-07-21 — Packet 2 Schema/Codec Ownership Transfer

- The original implementer produced green initial CRUD/ID/tenant and generic
  duplicate-column slices, then returned three times with the audited
  schema-v3/sortable-value sub-slice incomplete. Those uncommitted edits remain
  preserved but are not accepted evidence.
- A fresh instance of the existing `implementer` role now owns only the
  coherent schema-v3 widths/index/FK metadata, private sortable value codec,
  focused tests, and mechanical checks needed to finish that sub-slice. Expected
  and explicitly dispatched profile: `gpt-5.6-terra` / `medium`. It must not
  discard the prior edits, expand into live CRUD/query/batch/CAS behavior,
  commit, push, or spawn children.

## 2026-07-21 — Packet 2 Partial CRUD Evidence

- Existing implementer profile remains explicitly dispatched as `gpt-5.6-terra`
  / `medium`; this surface does not expose runtime self-introspection.
- Genuine RED: the live CRUD test failed with the Packet 1 unavailable-record
  operation error. GREEN: canonical recursive ID codec tests pass (2 tests),
  focused factory/codec tests pass (7 tests), TypeScript build and focused
  ESLint pass, and MySQL 8.4.10 live schema plus CRUD proof passes (2 tests).
- Schema version is now 2: column bytes use `VARBINARY(768)`, preserving an
  indexable bounded representation for Packet 3 rather than a MEDIUMBLOB.
  Packet 2 currently provides transactional one-record write/read/delete with
  bound scope/tenant/slot/column values and sanitized provider/decode errors.
- Remaining Packet 2 work: adversarial tenant/case/accent/boundary/payload and
  rollback coverage, independent-handle proofs, duplicate-column validation,
  and complete record-log evidence. No query translation, batches, or CAS was
  implemented.

## 2026-07-21 — Packet 2 Schema/Sortable Codec Sub-slice Accepted Locally

- Existing assigned role/context: `implementer`; explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`. This surface does not expose runtime
  self-introspection, so the immutable configured profile is the available
  runtime metadata.
- Genuine RED/GREEN: the new exact-value-data-boundary test first failed because
  the sortable string codec consumed an extra kind byte. The private codec now
  stores the kind only in `value_kind`, leaving all 768 `VARBINARY` bytes for
  data; the focused codec/factory/generic `RecordSpec` suite is green (17
  passed; 2 external-MySQL tests skipped without `SPINE_TS_MYSQL_URL`).
- Schema version 3 fixes widths at scope `512`, tenant `255`, slot `768`, column
  name `255`, and sortable value `768` bytes. Exact declared index totals are:
  records PK `512 + 255 + 768 = 1535`; columns PK `512 + 255 + 768 + 255 =
1790`; lookup `(scope_key, tenant_key, column_name, value_kind, value_data,
slot_key)` `512 + 255 + 255 + 1 + 768 + 768 = 2559`. Each is below MySQL's
  3072-byte InnoDB limit and is asserted in the factory test.
- Schema verification now fails closed on exact records/columns primary-key
  order, the six-column lookup order, and the three-column cascade foreign key
  to records. The fake metadata rows were updated to the v3 shape; DDL and
  metadata tests cover `ON DELETE CASCADE`.
- Focused typecheck, ESLint, Prettier, and `git diff --check` pass. The optional
  live MySQL harness remains unrun because no external URL was supplied; no
  query translation, batching, CAS, public export, commit, or push was added.

## 2026-07-21 — Packet 2 Adversarial CRUD Continuation

- The successful replacement `implementer` context retains Packet 2 ownership
  for the remaining unit and live-MySQL CRUD acceptance cases. Its expected and
  explicitly dispatched immutable profile remains `gpt-5.6-terra` / `medium`;
  independent runtime self-introspection is unavailable.
- Scope is limited to corrupt-payload distinction, pre-pool bounds, dynamic
  tenant isolation, canonical ID/value edges, independent handles, stale
  column replacement, cascade cleanup, transactional rollback, sanitized
  operation errors, live schema v3 proof, and records/mechanics. Query
  translation, batches, CAS, commits, pushes, and child spawning remain
  excluded.

## 2026-07-21 — Packet 2 Adversarial CRUD Evidence Accepted Locally

- RED/GREEN: unit tests first proved that v3 `value_data` remained nullable and
  that a non-byte payload was incorrectly treated as absence. GREEN changes
  make `value_data` `NOT NULL`, verify `IS_NULLABLE`, preserve a present corrupt
  payload as `MysqlStorageDataError`, and use the exported, sanitized
  `MysqlStorageOperationError` for read/write/delete provider failures.
- Unit evidence: focused RDBMS codec/factory plus generic `RecordSpec` tests
  pass with 21 tests; the same command skips 7 live cases when no external URL
  is configured. It proves malformed canonical aliases, blank/oversize
  scope/tenant/slot/column/value rejection before pool acquisition, exact
  boundaries, and provider error sanitation.
- Live MySQL 8.4.10 evidence: the explicit `SPINE_TS_MYSQL_URL` package command
  passes all 7 tests. It proves exact v3 columns/nullability/PK/lookup/FK
  metadata, independent handles, operation-time tenant switching, case/accent
  ID distinction, all canonical ID variants, exact 768-byte slot and sortable
  string boundaries, signed-64 endpoints, stale-column removal, cascade
  deletion true/false behavior, malformed payload distinction, and transaction
  rollback. The rollback case uses a disposable named check constraint (the
  supplied server disallows trigger creation under binary logging), removes it
  in `finally`, and proves both prior payload and prior encoded column bytes
  survive the failed insert.
- Accepted ID-order decision: packet 3's deterministic ordering and
  continuation use only the binary canonical `slot_key` bytes; no separate
  semantic slot-order column exists. This is recorded in the task ledger.
- Typecheck, focused ESLint/Prettier, TypeDoc/API validation, and
  `git diff --check` pass. No query translation, write-all, CAS, commit, or
  push was added.

## 2026-07-21 — Packet 2 Orchestrator Acceptance

- The orchestrator independently reran the focused RDBMS codec/factory and
  generic `RecordSpec` suite: 3 files and 21 tests passed.
- The explicit MySQL 8.4.10 suite independently passed all 7 integration cases.
- Composite TypeScript build, focused ESLint, Prettier, canonical TypeDoc/API
  validation, and `git diff --check` passed. An initial nonexistent
  `docs:api:check` invocation was corrected to the repository's canonical
  `docs:check:generated` command; it was a command-selection error, not a
  product failure.
- Source audit accepted the private codecs, exact schema-v3 metadata checks,
  pre-connection validation, transactional CRUD/rollback behavior, sanitized
  errors, and generic duplicate-column guard. Packet 2 is accepted; Packet 3
  owns query translation and deterministic bounded result materialization.

## 2026-07-21 — Packet 3 Dispatched

- Packet 2 commit `07481614` was pushed immediately and the local/remote task
  refs were confirmed equal at
  `0748161426a8961b542025ad3059e61e7b94c7d6`.
- Existing assigned role: `implementer`. Ownership: Packet 3 private SQL query
  translation, query/value validation, deterministic ordering/continuation,
  offset/limit pushdown, representative real-MySQL index-plan proof, focused
  tests, and T-0051 records only. Expected and explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`. No child spawning, commit, push, generic query API
  change, batching, CAS, lifecycle, or Packet 5 documentation is authorized.

## 2026-07-21 — Packet 3 Query Translation Evidence

- Existing `implementer` assignment retained its explicit immutable profile
  `gpt-5.6-terra` / `medium`; this surface exposes no independent runtime
  self-introspection, so configured dispatch metadata is the available evidence.
- TDD RED/GREEN: focused query tests first failed at the Packet 2 unavailable
  operation, then passed after private MySQL query translation was added. SQL
  binds scope/tenant, IDs, typed column values, continuation values, offset,
  and limit; dynamic names remain encoded bound values.
- Packet 3 compiles ANDed filter joins with independent aliases, typed mixed-kind
  IN alternatives, materialized-column sorting, canonical binary `slot_key` as
  the final ascending tie-break, complete lexicographic keyset continuation,
  and SQL offset/limit. Empty filter arrays return before pool acquisition;
  `ids: []` remains unrestricted. Dotted paths are rejected rather than scanned.
- Focused fake-backed factory suite passed 12 tests. Opt-in MySQL 8.4.10 proof
  at the supplied `SPINE_TS_MYSQL_URL` passed 8 tests, including typed filters,
  mixed boolean/string continuation, repeated-column aliases, no-sort slot
  continuation, tenant-scoped rows, corrupt payload classification, and an
  `EXPLAIN` assertion for `spine_ts_columns_lookup`.
- Limitation: MySQL server-prepared statements reject JS-number `LIMIT` binds
  with `Incorrect arguments to mysqld_stmt_execute`; read queries therefore use
  mysql2's parameterized `query()` path, retaining bound values without SQL
  interpolation. No client-side result scan or materialization is used.

## 2026-07-21 — Packet 3 Acceptance Evidence Correction

- The existing `implementer` correction batch retained its explicitly dispatched
  immutable `gpt-5.6-terra` / `medium` profile; independent runtime
  self-introspection remains unavailable on this surface.
- Source audit requested direct real-MySQL proof for multi-field mixed-direction
  tuples, descending continuation, positive offset-before-limit, and dynamic
  query tenant isolation. The new test was added before rerunning the suite;
  it passed without a production-code change because the established compiler
  already emits the complete tuple/keyset predicates and scope/tenant binds.
- Live evidence now proves ascending `group`, descending `rank`, canonical
  ascending slot tie-break, descending `rank` keyset continuation, offset `1`
  before limit `2`, and a mutable operation-time tenant adversary: one storage,
  one constant slot, and one shared filter value return only that tenant's
  distinct payload after each tenant switch. The opt-in suite now passes 9 tests.

## 2026-07-21 — Packet 3 Orchestrator Acceptance

- Initial audit rejected the first green draft because bind order, mixed-kind
  predicates, cross-kind keysets, and required multi-sort/window/tenant evidence
  were incomplete. One correction batch in the same implementation context
  repaired the compiler and added the missing adversarial real-MySQL cases.
- Independent final verification passed 3 focused files / 23 tests and all 9
  MySQL 8.4.10 integration cases. Composite TypeScript build, focused ESLint,
  Prettier, and `git diff --check` also passed.
- Source audit accepts parameter ordering, typed mixed-kind alternatives,
  lexicographic ascending/descending continuation, independent aliases,
  deterministic slot tie-breaking, SQL window pushdown, actual-slot decoding,
  tenant/scope isolation, and sanitized corrupt/provider failures. Packet 3 is
  accepted; Packet 4 owns transactional batching and CAS.
