# T-0177 Work Log

## 2026-08-12 - Startup

- Started from `origin/main@12e3318a` in isolated worktree
  `.worktrees/T-0177-copyright-spacing`; the user-owned primary checkout is
  untouched.
- The actual shared template/checker was inspected. It currently accepts
  `*/\nimport` and adds an empty line only before immediate TSDoc. Therefore the
  earlier claim of universal spacing was false.
- Official Angular, Visual Studio Code, and React sources were checked directly;
  each project uses one empty line after its per-file header. TypeScript and
  NestJS generally do not use per-file headers and are not counterexamples.
- Clean focused baseline after worktree-local dependency installation:
  `scripts/check-copyright.test.mjs`, `scripts/copyright-header.test.mjs`, and
  `scripts/proto-workflow.test.mjs` pass 77/77 before correction.
- Read-only inventory and generator audits are assigned independently with
  explicit `gpt-5.6-terra` / medium profiles. Runtime self-introspection will be
  recorded if unavailable.

Status: durable requirements recorded; independent audits and test-first RED
pending.

## 2026-08-12 - Independent file-inventory audit

- Assignment function/profile: read-only repository inventory auditor,
  `gpt-5.6-terra` / medium. Both fields were explicit. The execution surface did
  not expose separate runtime self-introspection.
- Exact tracked inventory: 502 eligible TypeScript/TSX files. All 502 have the
  exact approved header at the correct byte/shebang position.
- Spacing classification: 463 have zero empty lines after the header, 39 have
  exactly one, none have multiple lines, and none have malformed placement.
- All three shebang files are zero-spacing violations.
- All 43 manifest-owned upstream/frozen Proto sources retain their upstream
  notice and no CodeMatters header. Proto spacing remains outside this
  correction.
- The live checker exits successfully against this incorrect inventory, which
  confirms the missing enforcement rather than a source-header defect.

Status: generator audit pending; no implementation has started.

## 2026-08-12 - Independent generator audit

- Assignment function/profile: read-only generator/template auditor,
  `gpt-5.6-terra` / medium. Both fields were explicit. The execution surface did
  not expose separate runtime self-introspection.
- The shared normalizer only separates an immediate TSDoc block, and the checker
  validates no post-header spacing.
- Exactly two checked-in derived TypeScript outputs were identified:
  `examples/message-board/app/src/model-registry.ts` currently regenerates with
  zero empty lines, while
  `packages/server/test-fixtures/entity-metadata-fixtures.ts` already regenerates
  with one because it begins with TSDoc.
- Ignored application-generated registries and Proto-derived generated modules
  are not eligible checked-in outputs and must not acquire this project header
  policy. Proto producers remain unchanged.
- Required RED coverage is pinned for zero/exact/multiple spacing, shebangs,
  imports, declarations, ordinary comments, TSDoc, and both checked-in derived
  outputs.

Status: audits complete; test-first implementation ready to start.
