# T-0177 Work Log

## 2026-08-12 - Startup

- Started from `origin/main@12e3318a` in isolated worktree
  `.worktrees/T-0177-copyright-spacing`; the user-owned primary checkout is
  untouched.
- The actual shared template/checker was inspected. It currently accepts
  `*/\nimport` and adds an empty line only before immediate TSDoc. Therefore the
  earlier claim of universal spacing was false.
- Official Angular, Visual Studio Code, and React sources were checked directly;
  each project uses one empty line after its per-file header. TypeScript and
  NestJS generally do not use per-file headers and are not counterexamples.
- Clean focused baseline after worktree-local dependency installation:
  `scripts/check-copyright.test.mjs`, `scripts/copyright-header.test.mjs`, and
  `scripts/proto-workflow.test.mjs` pass 77/77 before correction.
- Read-only inventory and generator audits are assigned independently with
  explicit `gpt-5.6-terra` / medium profiles. Runtime self-introspection will be
  recorded if unavailable.

Status: durable requirements recorded; independent audits and test-first RED
pending.
