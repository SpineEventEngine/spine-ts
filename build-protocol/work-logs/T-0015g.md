# T-0015g Work Log

## 2026-07-08 00:05 WEST

- Created the isolated worktree
  `/Users/armiol/development/experiments/spine-ts/.worktrees/T-0015g-todo-generated-registry`
  on branch `task/T-0015g-todo-generated-registry` from `main` at `81c325b`.
- Confirmed root `main` only has the intentionally untouched untracked
  `human-review-1-jul.md`.
- Confirmed `examples/todo/src/index.ts` already uses bare `@Assign` and
  returns generated domain event messages, but still manually calls
  `defineEntityHandlers()` inside repository construction.
- Confirmed generated registry primitives exist in `@spine-ts/server`:
  `analyzeBuildHandlers()`, `GeneratedRegistryWriter`,
  `GeneratedRegistryDiscovery`, and `HandlerRegistryIngestor`.
- Open decision: exact build entry point for producing the to-do generated
  handler registry. Prefer the smallest package script or shared script that
  uses existing analyzer/writer APIs.

## 2026-07-08 00:12 WEST

- Ran `corepack pnpm install` in the worktree. The sandboxed run failed with
  npm registry DNS `ENOTFOUND`; stopped it and reran with approved network
  escalation. The escalated install completed successfully using pnpm `11.9.0`.

## 2026-07-08 00:18 WEST

- Fetched and reviewed the current Spine JVM de-event-sourcing references from
  `core-jvm`:
  `docs/adr/0001-aggregates-without-event-sourcing.md`,
  `.agents/tasks/de-event-sourcing-brief.md`, and
  `.agents/tasks/de-event-sourcing-plan.md`.
- Alignment notes for this task: aggregate state is loaded from latest state
  rather than replay; handlers mutate framework-opened transactional state and
  return domain event messages; `@Assign` must emit at least one event or
  reject; framework validation/rollback owns failed transactions; aggregate
  `@Apply` is rejected rather than silently ignored.

## 2026-07-08 00:27 WEST

- Implemented `scripts/generate-handler-registry.mjs`, a small build-time
  script that reuses the existing server analyzer/writer sources, analyzes an
  application tsconfig, and writes a guarded registry file only under an
  ignored `generated/` root.
- Wired `scripts/proto-workflow.mjs generate` to regenerate the to-do handler
  registry after Protobuf-ES output is published and before `tsc -b`.
- Migrated `examples/todo/src/index.ts` away from `defineEntityHandlers()`:
  `createTodoContext()` now loads the compiled registry through
  `GeneratedRegistryDiscovery` and repository construction pulls matching
  handler metadata from the discovered registry.
- Chose an async `createTodoContext()` instead of top-level registry loading
  because the generated registry imports entity classes from `src/index.js`;
  lazy loading avoids an ESM top-level-await cycle.
- Updated the focused example test to await context creation and assert that
  generated handler metadata registers the four accepted command types.
- Updated example docs, API docs, and build-protocol docs to explain
  generated-registry generation/loading for application packages.
- Ran `corepack pnpm proto:generate`; first runs exposed temporary transpile
  issues in the script (`NodeNext` option mismatch, CommonJS output in `.mjs`,
  and workspace dependency resolution from system temp). Fixed those by
  emitting plain ESM and placing temporary tool modules under ignored
  `node_modules/.cache`.
- Verification so far:
  - `corepack pnpm proto:generate` passed.
  - `corepack pnpm typecheck:build` passed.
  - `corepack pnpm vitest run examples/todo/src/index.test.ts` initially found
    one immutable-array assertion bug and sandbox `listen EPERM`; after fixing
    the assertion, the escalated rerun passed: 16/16 tests.
  - `corepack pnpm vitest run packages/server/test/handler/build-time-handler-analyzer.test.ts packages/server/test/handler/generated-registry-writer.test.ts packages/server/test/handler/generated-registry-discovery.test.ts packages/server/test/handler/generated-handler-registry.test.ts`
    passed: 57/57 tests.
  - `corepack pnpm format:check` initially flagged three code files; after
    Prettier on those files, `corepack pnpm format:check` passed.

## 2026-07-08 00:35 WEST

- Adjusted the focused example test preflight to require the compiled example
  entry point and compiled generated registry to exist, rather than comparing
  mtimes. TypeScript incremental builds may correctly skip identical output and
  leave an older `dist` mtime.
- Confirmed generated artifacts remain ignored:
  `docs/api/reference/`, `examples/todo/dist/`, and
  `examples/todo/generated/`.
- Final verification:
  - `corepack pnpm typecheck:build` passed.
  - `corepack pnpm vitest run examples/todo/src/index.test.ts` passed with
    local listener escalation: 16/16 tests.
  - `corepack pnpm vitest run packages/server/test/handler/build-time-handler-analyzer.test.ts packages/server/test/handler/generated-registry-writer.test.ts packages/server/test/handler/generated-registry-discovery.test.ts packages/server/test/handler/generated-handler-registry.test.ts`
    passed: 57/57 tests.
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm lint` passed, including cleanup enforcement.
  - `corepack pnpm format:check` passed.
  - `git diff --check` passed.

## 2026-07-08 review-fix

- Addressed review findings on top of `ff93c98`.
- Refreshed the root README current-status paragraph so the to-do example and
  generated-registry workflow are no longer described as deferred.
- Made `scripts/generate-handler-registry.mjs` keep `main()` local, added
  staged-generation support, and redirected TypeScript reads from the published
  generated root to the staged protobuf root during registry analysis.
- Changed `scripts/proto-workflow.mjs generate` so protobuf output and the
  generated handler registry are both staged first and published together only
  after registry generation succeeds.
- Added content-based freshness checks to the to-do example test preflight:
  the test now re-renders the expected registry from current decorators and
  re-emits the example into a temp directory before importing `dist`.
- Fixed `createTodoContext()` registry metadata caching so a failed registry
  load clears the cached promise; retries use a discovery cache-bust token to
  recover from Node ESM failed-import caching.
- Verification:
  - `corepack pnpm typecheck:build` passed.
  - `corepack pnpm vitest run examples/todo/src/index.test.ts` passed with
    local listener escalation: 17/17 tests.
  - `corepack pnpm vitest run packages/server/test/handler/build-time-handler-analyzer.test.ts packages/server/test/handler/generated-registry-writer.test.ts packages/server/test/handler/generated-registry-discovery.test.ts scripts/proto-workflow.test.mjs`
    passed: 52/52 tests.
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm lint` passed.
  - `corepack pnpm format:check` passed.
  - `git diff --check` passed.

## 2026-07-08 00:46 WEST docs round-2 fix

- Addressed remaining round-2 documentation findings in `docs/USER_GUIDE.md`.
- Updated the guide's current-status and "What Exists Now"/deferred wording so
  the to-do example is described as runnable, generated-registry-backed, and no
  longer placeholder/later work.
- Reworked the envelope packing section so ordinary application handlers return
  generated domain event messages, while `packEvent()` is documented only as a
  low-level framework/test-fixture helper for code that already owns event
  IDs/contexts.
- Reworked the handler section so bare decorators plus generated registry
  loading are the ordinary application workflow. `defineEntityHandlers()` is
  now described only as a framework-test, generated-ingestion, or legacy
  non-decorator escape hatch.
- Updated the low-level runtime assembly example to load a generated registry
  instead of constructing manual handler metadata.
- Verification:
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm format:check` passed.
  - `git diff --check` passed.

## 2026-07-08 01:00 WEST final verification

- Closed all T-0015g implementation, fix, and reviewer sub-agents after their
  final reports.
- Ran root-agent final verification before integration:
  - Focused tests:
    `corepack pnpm vitest run examples/todo/src/index.test.ts packages/server/test/handler/build-time-handler-analyzer.test.ts packages/server/test/handler/generated-registry-writer.test.ts packages/server/test/handler/generated-registry-discovery.test.ts scripts/proto-workflow.test.mjs --passWithNoTests`
    passed with local listener escalation: 5 files, 69 tests.
  - `corepack pnpm typecheck:build` passed.
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm lint` passed, including cleanup enforcement.
  - `corepack pnpm format:check` passed.
  - `git diff --check 81c325b..HEAD` passed.
  - `corepack pnpm test` passed with local listener/IPC escalation: 49 files,
    810 tests.

## 2026-07-08 01:04 WEST main integration

- Merged `task/T-0015g-todo-generated-registry` into `main` with
  `git merge --no-ff task/T-0015g-todo-generated-registry -m "Merge T-0015g todo generated registry"`.
  Merge commit: `6a7845e`.
- Confirmed root `main` status still has only the user-owned untracked
  `human-review-1-jul.md`.
- Ran post-merge verification on `main`:
  - `git diff --check HEAD^..HEAD` passed.
  - `corepack pnpm test` passed with local listener/IPC escalation: 49 files,
    810 tests.
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm format:check` failed only because it includes the untouched
    untracked `human-review-1-jul.md`.
  - Equivalent Prettier check over the tracked project formatting surface
    passed.
  - `corepack pnpm lint` passed, including cleanup enforcement.
