# T-0126 Work Log

## 2026-08-07 - Task start and implementation dispatch

- T-0125 is durably complete at `origin/main@c6ff000c`; T-0126 starts from that
  exact endpoint in an isolated worktree.
- Classification: high-risk because the task defines production deployment,
  secret-reference, readiness, scaling, and replacement behavior. The approved
  Wave 7 plan is the architecture pass; no unresolved product decision remains.
- Existing role: implementer. Bounded ownership is
  `packages/deployment-gke/terraform/**`, the package beginner guide/examples,
  `packages/deployment-gke/test/terraform-policy.test.ts`, and only the root
  deployment-guide link needed for discoverability.
- Expected dispatch profile is explicitly `gpt-5.6-terra` / `medium`. The
  Desktop surface supports explicit child model/reasoning dispatch. Runtime
  metadata will be recorded when exposed; otherwise the explicit configured
  role/profile and absence of mismatch are the acceptance evidence.
- The implementation starts RED with deterministic Terraform policy and
  beginner-documentation checks before adding Terraform or guide content.
- Current local Terraform is 1.15.5. Provider/resource and Google Cloud claims
  require current official-source verification before acceptance.

## 2026-08-07 - RED checkpoint

- Added deterministic policy and guide-contract coverage before any Terraform
  module or deployment-guide implementation.
- The intended initial command is `pnpm exec vitest run
packages/deployment-gke/test/terraform-policy.test.ts`; it must fail because
  the module and its beginner deployment sections do not yet exist.
- Observed RED result: the command failed four assertions. Three reported the
  expected missing `terraform/main.tf` or `terraform/variables.tf`; the guide
  assertion reported the absent `## Before you begin` section. The failure
  demonstrates that the new policy checks are not passing against baseline.

## 2026-08-07 - GREEN implementation and source verification

- Added the editable `packages/deployment-gke/terraform` module: a private
  headless application Service, one private Gateway Deployment, one private
  simple-delivery Deployment, private ClusterIP Services, readiness probes,
  configuration, names of externally-created Secrets, and an off-by-default
  external-metric HPA. It contains no Kubernetes Secret resource, secret value,
  storage-engine setting, Cloud Run resource, or public load balancer.
- Added the package's gradual beginner guide, agent reference, root
  discoverability link, and deterministic policy checks. The guide makes clear
  that an operator-owned TLS/authentication edge exposes the private Gateway
  Service, while application code supplies storage and `GkeNodeDiscovery`
  assembly.
- Observed initial GREEN evidence: `pnpm exec vitest run
packages/deployment-gke/test/terraform-policy.test.ts` passed 1 file and 4
  tests. `terraform -chdir=packages/deployment-gke/terraform init
-backend=false` initially selected `hashicorp/kubernetes v2.38.0`; `terraform
-chdir=packages/deployment-gke/terraform validate` reported a valid
  configuration. The generated provider lock file is tracked for reproducible
  initialization.
- Official-source claims used by the template and guide:
  - Kubernetes documents headless Services and Service endpoint discovery:
    https://kubernetes.io/docs/concepts/services-networking/service/
  - Kubernetes documents that ready endpoints are the default unless
    `publishNotReadyAddresses` is set:
    https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/
  - Kubernetes documents readiness probes and their effect on Service traffic:
    https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-probes/
  - Google documents HPA external/custom metrics:
    https://cloud.google.com/kubernetes-engine/docs/concepts/custom-and-external-metrics
  - Google documents KEDA as the Standard-GKE scale-from-zero path:
    https://cloud.google.com/kubernetes-engine/docs/tutorials/scale-to-zero-using-keda
  - HashiCorp documents versioned Kubernetes provider resources:
    https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs/guides/versioned-resources
  - HashiCorp documents format and validation as the Terraform style checks:
    https://developer.hashicorp.com/terraform/language/style
  - HashiCorp documents that Terraform Secret arguments are stored in state,
    which is why this module references existing Secrets instead:
    https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs/resources/secret

## 2026-08-07 - Pre-review corrections

- Corrected the Gateway-replacement statement: the durable subscription
  registry is Gateway-configured and separate from service-owned subscriptions;
  it is not automatically the application storage. This matches
  `packages/server/REFERENCE.md`.
- Updated the Kubernetes provider constraint and tracked lock selection to the
  current Registry result, `hashicorp/kubernetes v3.2.1`. The module uses the
  documented versioned resources and validated against that provider.
- Made application capacity ownership explicit. With autoscaling disabled,
  Terraform sets `application_replicas`. With it enabled, Terraform omits the
  Deployment `replicas` value while the external-metric HPA owns it, avoiding
  routine Terraform applies resetting HPA decisions. The guide records how to
  return to manual capacity. Policy coverage proves this conditional behavior.
- Added nonblank image/Secret/context checks, integer TCP-port checks, and a
  resource precondition requiring the autoscaling minimum not exceed its
  maximum.
- Corrected two Terraform syntax findings during this work: HCL numeric
  literals do not use underscore separators, and a resource lifecycle
  precondition belongs outside a Kubernetes resource `spec`. Final focused
  checks after correction passed: `terraform fmt -check -recursive
packages/deployment-gke/terraform`, `terraform -chdir=packages/deployment-gke/terraform
validate`, and `pnpm exec vitest run
packages/deployment-gke/test/terraform-policy.test.ts` (1 file, 4 tests).
- The first focused task-gate run reached all type, lint, documentation, API,
  and Proto checks, then rejected the beginner-guide word `candidate` under the
  repository's internal-execution-history vocabulary policy. The guide now says
  `available Gateway backend`; no deployment behavior changed.

## 2026-08-07 - Implementation acceptance

- The existing implementer completed and pushed checkpoints `40ee2d57`,
  `f47c2e22`, and `7f5f5812`. The branch is clean and synchronized with
  `origin/task/T-0126-gke-terraform-guide`.
- The dispatch explicitly selected the existing `implementer` role with
  `gpt-5.6-terra` / `medium`. Runtime self-introspection was unavailable; the
  immutable configured role/profile and absence of a visible mismatch or
  fallback are the acceptance evidence.
- Final `pnpm verify:task -- --no-coverage
packages/deployment-gke/test/terraform-policy.test.ts` passed every shared
  gate and the four focused tests. Terraform 3.2.1 initialization, recursive
  formatting, and validation also passed.

## 2026-08-07 - Specialist correction dispatch

- The complete four-lane review wave at `585ab4ec` is recorded in review commit
  `f1e6d8bd`. One deduplicated P0/P1/P2 batch returns to the original
  implementation context; no fresh fixer will rediscover the design.
- Existing role: implementer. Ownership remains limited to the T-0126 Terraform,
  policy test, package guide/reference, and task records. Expected configured
  profile remains `gpt-5.6-terra` / `medium`; the original dispatch explicitly
  selected both fields, and the follow-up resumes that immutable context.
- Runtime self-introspection remains unavailable. No visible role/profile
  mismatch or fallback is acceptable.

## 2026-08-07 - Specialist correction implementation

- Added RED-first policy coverage for delivery listener environment, bounded
  Gateway resource matching, both declared-and-consumed Secret inputs, rollout
  commands, public listener convention, durable browser bindings, and KEDA
  ownership. The initial focused test run failed two expected assertions: the
  delivery Deployment had no explicit listener environment and the guide had
  no external listener/binding assembly.
- Added explicit `HOST=0.0.0.0` and configured `PORT` environment values to
  every workload, plus application and standalone-Gateway entrypoint examples.
  The Gateway example now shows the production sessions, authorization,
  trusted context, origins, clock, fingerprint, registry, discovery, and named
  `DurableSubscriptionBindings` inputs. The guide links the detailed server
  and authentication guides and states that production rejects missing or
  volatile bindings.
- The module HPA has a minimum of one and is mutually exclusive with
  operator-managed KEDA. Incompatible replacement and rollback now disable the
  module HPA before scaling to zero and restore exactly one policy: manual
  replicas, the module HPA, or KEDA. Verification waits for application,
  Gateway, and delivery rollouts before inspecting endpoints.
- Cheap GREEN evidence: `pnpm exec vitest run
packages/deployment-gke/test/terraform-policy.test.ts` passed 1 file and 4
  tests; `node docs/check-typescript-snippets.mjs`, `terraform fmt -check
-recursive packages/deployment-gke/terraform`, and `terraform
-chdir=packages/deployment-gke/terraform validate` passed. A narrowed
  snippet-check environment variable is unsupported by the repository script;
  its normal full invocation passed instead.
- Follow-up execution remains the existing implementer with explicitly
  configured `gpt-5.6-terra` / `medium`. Runtime self-introspection is not
  exposed by this surface; the immutable configured profile and absence of a
  visible mismatch are the available evidence.
- Required focused gate passed after the cheap preflight: `pnpm verify:task --
--no-coverage packages/deployment-gke/test/terraform-policy.test.ts` passed
  generated-source checks, build and tooling typechecks, cleanup/TSDoc/format
  checks, audience and API-documentation checks, Buf and generated-output
  checks, release-readiness, and 1 focused file with 4 tests.
