# T-0141 Work Log

## 2026-08-08 Registry Recheck

- The live npm registry publishes
  `@spine-event-engine/validation@2.0.0-snapshot.7` as both `latest` and
  `snapshot`.
- Published metadata retains peer dependency `@bufbuild/protobuf ^2.10.2`,
  dependency `temporal-polyfill 1.0.1`, and repository
  `SpineEventEngine/validation-ts` under `packages/validation`.
- `@spine-event-engine/validation-ts` remains published only through
  `2.0.0-snapshot.4`. The external gate is resolved; implementation proceeds
  with the frozen exact package rename/version and no Git dependency.

## Implementation Dispatch

- Existing role: project `implementer`.
- Expected and explicitly dispatched profile: `gpt-5.6-terra` / `medium`.
- Scope is limited to package manifests/lockfile, validation imports, and
  focused compatibility tests. No example migration, facade redesign, or
  subagent spawning is authorized.
- Runtime metadata will be recorded when exposed; otherwise the immutable
  configured role/profile and metadata limitation satisfy the acceptance gate.

## RED: Renamed Validation Boundary

- Updated the focused core facade boundary test to mock only
  `@spine-event-engine/validation`; the production import intentionally remains
  on the old package until the RED run proves that the new seam is required.
- Planned evidence: the focused test must fail because the old runtime import
  bypasses the renamed-package mock, after which the minimal manifest, lockfile,
  and import migration will be applied.
- RED result: `pnpm exec vitest run packages/core/test/validation-facade-boundary.test.ts`
  failed both tests as expected: the old import bypassed the renamed mock and
  returned `valid: true` where mocked upstream failures require `valid: false`.

## GREEN: Minimal Package Rename

- Replaced only the core facade runtime import and direct core dependency with
  `@spine-event-engine/validation@2.0.0-snapshot.7`; the public facade remains
  unchanged.
- The package-manager lockfile refresh is the remaining mechanical dependency
  update before rerunning the focused boundary test.
- Lockfile refresh: `pnpm install --lockfile-only` resolved the exact published
  package and its declared `temporal-polyfill` dependency; a frozen install then
  installed the renamed package locally.
- Focused GREEN result: the core facade boundary test passed 2/2 and
  `pnpm exec tsc --noEmit -p packages/core/tsconfig.json` exited successfully.

## RED/GREEN: Packed Consumer Dependency Staging

- RED result: focused proto-tools consumer tests found the packed external
  fixture still staging `validation-ts`, so executing the package-built core
  failed with `ERR_MODULE_NOT_FOUND` for `@spine-event-engine/validation`.
- GREEN change: replaced only those two test fixture dependency names and paths
  with the renamed package, preserving the package-consumer behavior test.
- Follow-up RED result: the fixture then reached the published validator and
  failed because its declared runtime dependency `temporal-polyfill` (and its
  two direct dependencies) was absent from the isolated consumer tree.
- GREEN change: the fixture now stages the validator's declared temporal
  dependency graph only when it stages the validator, so the packed-consumer
  test continues to exercise a fully isolated install.

## Compatibility Evidence And Known Limitation

- Core facade compatibility RED evidence: the renamed published package keeps
  invalid constraint and packed-error behavior but changes its upstream required
  field message template and redacted placeholder keys. Updated the focused
  compatibility assertions to the exact `snapshot.7` output while retaining the
  facade contract: invalid result, structured `ValidationError`, no field value,
  and every placeholder value redacted.
- Server transition-validation coverage passed in the same focused run.
- The unmodified To-Do black-box suite could not establish example compatibility:
  19 of 33 tests timed out in projection reads/subscriptions, including unrelated
  create, query, rename, completion, subscription, and catch-up flows. This task
  owns no example code; the failure is recorded for the stacked train rather
  than masked or repaired here.

## Implementation Checkpoint Evidence

- Role/profile: existing project `implementer`, explicitly dispatched as
  `gpt-5.6-terra` / `medium`. This execution surface does not expose immutable
  per-turn runtime model metadata, so the dispatch configuration is the durable
  evidence; no visible fallback or profile mismatch was observed.
- Exact migration: core now imports and depends on
  `@spine-event-engine/validation@2.0.0-snapshot.7`; `pnpm-lock.yaml` records
  its npm integrity, Node `>=24.0.0` constraint, and `temporal-polyfill`
  dependency. No vendoring or Git dependency was added.
- Focused GREEN evidence: `pnpm exec vitest run` over core facade/index,
  server transition-validation/transaction, and proto-tools packed-consumer
  suites passed 199/199 after formatting. Core and proto-tools TypeScript
  checks and scoped ESLint also exited successfully.
- Cheap-preflight evidence: `git diff --check` passed; the exact old-package
  scan over `packages/**` package manifests, runtime files, and tests returned
  no `@spine-event-engine/validation-ts` reference; scoped Prettier check
  passed after formatting the two changed fixture tests.
- Selected final profile remains focused `verify:task` with coverage for the
  changed core source and focused consumers. It is deferred to the orchestrator
  after the required specialist review wave, per the standard-task protocol.
