# T-0026 Work Log

Status: Round 44 fixes committed; re-review pending

Branch: `task/T-0026-transport-backed-delivery-workers`

Worktree: `.worktrees/T-0026-transport-backed-delivery-workers`

Started: `2026-07-10T03:44:01Z`

Baseline commit: `ca8fb2b3`

## Entries

- `2026-07-10T08:34:24Z`: Round 18 cleanup worker removed the exported
  standalone `requireShardLeaseMs` helper from the shard registry module.
  Lease validation is now owned privately by `ShardedWorkRegistry` and
  `Delivery`, preserving the same bound before timer scheduling without
  widening the public delivery API. Also split `Delivery.#drainAvailableMessages`
  so the method stays focused on bounded page scanning while
  `#tryDrainMessage` and `drainProgress()` own the per-row accounting.
- `2026-07-10T07:40:04Z`: Root orchestrator resumed T-0026 after session
  compaction and completed the active review round intake. Style and
  reliability reviewers reported clean. Documentation, TypeScript/API docs, and
  security reviewers returned follow-up findings covering overbroad retry
  wording, narrow delivery failure TypeDoc, and inherited internal inbox-claim
  metadata in public snapshots. The documentation reviewer handle was no longer
  discoverable when closed; API, security, and reliability reviewer agents were
  closed after their reports.
- `2026-07-10T03:44:01Z`: Started T-0026 after T-0025 durable status records
  were aligned on `main`. Root `main` was clean except for the user-owned
  untracked `human-review-1-jul.md`.
- `2026-07-10T03:44:01Z`: Requirements splitter
  `019f4a1c-11cb-7091-bb52-59bfeb3def83` selected transport-backed delivery
  workers as the next non-blocked implementation slice and reported no real
  blockers.
- `2026-07-10T03:44:01Z`: Created branch
  `task/T-0026-transport-backed-delivery-workers` and worktree
  `.worktrees/T-0026-transport-backed-delivery-workers`. The sandboxed Git
  attempt failed on ref locking; the escalated retry succeeded under the user's
  standing permission for per-task worktrees.
- `2026-07-10T03:44:01Z`: Created the task brief, work log, review log, and
  D-0077 before runtime implementation edits. The task scope intentionally keeps
  production broker supervision, retry monitors, retained attempt history, and
  ZeroMQ topology out of this slice.
- `2026-07-10T03:44:01Z`: Completed the canonical skill applicability check.
  Session and installed skill inventories were available; expected skills and
  the installed-skill lock manifest were readable. The worktree shell prints a
  `jenv-refresh-plugins` permission warning while commands still complete.
- `2026-07-10T03:44:01Z`: Completed the required Spine JVM server inspection.
  Read local delivery/server/storage notes and the actual `core-jvm/server`
  delivery source in the local checkout for `Delivery`, `Inbox`,
  `InboxOfCommands`, `InboxOfEvents`, `ShardedWorkRegistry`,
  `LiveDeliveryStation`, `TargetDelivery`, and `ServerEnvironment`. The TS
  worker must reuse existing delivery-loop and inbox primitives rather than
  cloning JVM conveyor/station hierarchy.
- `2026-07-10T03:50:51Z`: Implementation sub-agent resumed in the T-0026
  worktree after the initial task setup. Re-read the task brief, work log,
  review log, required build protocol sections, code quality rules, delivery
  primitives, local inbox handoffs, bounded context/server lifecycle code, and
  focused delivery/context/server tests before edits. Planned the smallest
  follow-up slice as package-internal context replay access plus a server-owned
  closeable `DeliveryLoop` runtime, with tests written before production code.
- `2026-07-10T04:05:00Z`: Root orchestrator corrected the T-0026 task, work,
  and review status headers after implementation commit `94b4c632` so the
  branch accurately enters the required review loop.
- `2026-07-10T05:04:38Z`: Root orchestrator ran fresh review-readiness
  verification. Focused delivery/context/index Vitest passed with 72 tests;
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` passed. `docs:check` still reports only the existing
  invalid-origin TypeDoc warning.
- `2026-07-10T04:27:00Z`: lease reliability fix sub-agent addressed the P1
  finding that a delivery drain could outlive its shard lease while awaiting an
  endpoint callback, letting another worker pick up the same expired shard and
  invoke the same `TO_DELIVER` row concurrently.
- `2026-07-10T04:31:10Z`: lease reliability fix sub-agent completed final
  requested verification for the P1/P3 fixes. Focused delivery Vitest,
  `typecheck:build:generated`, `format:check`, and `git diff --check` passed.
- `2026-07-10T06:06:35Z`: Round 5 fix worker reproduced the remaining review
  findings with focused red tests before production edits. The delivery-worker
  row-claim regression failed because `delivery.inbox.claim` did not exist; the
  shard-registry release regression failed because `release()` returned `true`
  after the stored session expiry.
- `2026-07-10T06:10:10Z`: Round 5 fix worker added a durable optional inbox row
  claim fenced by shard-session id/node/expiry, acquired by `Inbox`/
  `InboxStorage` before endpoint invocation. Delivery drains now invoke
  endpoints with unclaimed message snapshots, mark delivered with the claimed
  storage snapshot, skip live competing claims, and best-effort clear unchanged
  claims after failed attempts. This keeps the fence framework-owned and avoids
  adding scheduler, retry, supervisor, transport-topology, or attempt-history
  subsystems.
- `2026-07-10T06:10:10Z`: Round 5 fix worker changed
  `ShardedWorkRegistry.release()` to mirror `renew()` by returning `false`
  when the current stored session is already expired at the release clock.
  Documentation comments now describe storage-backed pickup/renew/release,
  renewal as framework-owned lease fencing, and `DeliveryWorker` as a
  local/direct wrapper over configured shard loops rather than production
  supervision or topology.
- `2026-07-10T06:10:10Z`: Round 5 style/API fix added `replay(...)` to the
  internal process-manager and projection inbox contracts so local handoff
  tests and worker code can type against the internal endpoint interface rather
  than concrete classes.

## JVM Inspection Evidence

- JVM `Delivery.local()` uses single-shard synchronous local delivery with a
  deduplication window; `localAsync()` only changes dispatching to asynchronous
  local observer execution.
- JVM `Delivery.deliverMessagesFrom(shard)` picks a shard for the current node,
  runs delivery pages, releases the shard in `finally`, reports stats, and lets
  monitor actions decide repeats or pickup-failure behavior.
- JVM `LiveDeliveryStation` filters `TO_DELIVER`, deduplicates, sorts
  chronologically, dispatches to target delivery, and marks dispatched messages
  `DELIVERED`.
- JVM `TargetDelivery` routes stored rows through command/event inbox endpoints
  and reports failed reception to `DeliveryMonitor`; it does not expose
  framework envelopes to application code.
- JVM `ServerEnvironment.delivery()` defaults to local delivery and stores the
  configured delivery in process-wide environment state.

## TDD Evidence

- Red `2026-07-10T03:53:08Z`: initial direct focused Vitest attempts for
  new handoff/worker tests failed before test import because workspace package
  `dist` entrypoints had not been built yet. This was environment setup noise,
  not accepted red behavior.
- Red `2026-07-10T03:53:52Z`: after `pnpm --config.verify-deps-before-run=false
proto:generate`, focused tests failed for the intended missing behavior:
  `process-manager-handoff.test.ts -t "replays existing durable"` read rows
  but delivered none because `LocalProcessManagerInbox.replay()` was absent;
  `projection-handoff.test.ts -t "replays existing durable"` likewise delivered
  none because `LocalProjectionInbox.replay()` was absent; and
  `delivery-worker-runtime.test.ts` failed with `DeliveryWorker is not a
constructor`.
- Green `2026-07-10T03:54:54Z`: the same focused tests passed after adding the
  framework-owned replay delegates and `DeliveryWorker` wrapper:
  process-manager handoff `1 passed | 14 skipped`, projection handoff
  `1 passed | 5 skipped`, and delivery worker runtime `2 passed`.
- Red `2026-07-10T04:13:44Z`: reliability regression test added for
  `DeliveryWorker` shard-loop rejection handling. Focused
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker-runtime.test.ts` failed before
  the fix: `close waits for active shard drains after another shard rejects`
  observed `closed === true` while the second shard drain was still blocked, and
  `reports all shard drain rejections when multiple loops fail` received only
  `Error: first shard failed` instead of an `AggregateError` containing both
  loop failures.
- Green `2026-07-10T04:14:06Z`: the same focused
  `delivery-worker-runtime.test.ts` command passed after changing
  `DeliveryWorker.start()` to wait for every shard loop promise to settle before
  clearing `#running`; 1 file, 4 tests.
- Red `2026-07-10T04:25:15Z`: lease reliability regression added in
  `delivery-worker.test.ts`. Focused
  `pnpm --config.verify-deps-before-run=false exec vitest run -t "keeps active
shard ownership" packages/server/test/delivery/delivery-worker.test.ts` failed
  before the fix: worker B returned `DRAINED`, processed and delivered the row,
  while the expected result was `SKIPPED` with zero processed rows.
- Green `2026-07-10T04:26:23Z`: the same focused regression passed after adding
  storage-backed shard session renewal and drain-local lease keepalive; 1 file,
  1 passed, 25 skipped.
- Green `2026-07-10T04:26:38Z`: wider focused
  `delivery-worker.test.ts` and `sharded-work-registry.test.ts` pass succeeded:
  2 files, 63 tests.
- Red `2026-07-10T06:06:35Z`: focused
  `delivery-worker.test.ts -t "skips a row already claimed"` failed before the
  fix with `TypeError: delivery.inbox.claim is not a function`.
- Red `2026-07-10T06:06:35Z`: focused
  `sharded-work-registry.test.ts -t "does not release a delayed shard session
after expiry"` failed before the fix because `release(session)` resolved
  `true` instead of `false`.
- Green `2026-07-10T06:08:40Z`: both focused Round 5 regressions passed after
  adding the inbox claim CAS and release expiry guard.
- Green `2026-07-10T06:10:10Z`: requested Round 5 focused Vitest batch passed:
  `inbox.test.ts`, `sharded-work-registry.test.ts`,
  `delivery-worker.test.ts`, `delivery-worker-runtime.test.ts`,
  `process-manager-handoff.test.ts`, and `projection-handoff.test.ts`; 6 files,
  182 tests.

## Files Changed

- `build-protocol/DEVELOPER_API.md`
- `build-protocol/RUNTIME_ARCHITECTURE.md`
- `build-protocol/tasks/T-0026-transport-backed-delivery-workers/TASK.md`
- `build-protocol/work-logs/T-0026.md`
- `build-protocol/reviews/T-0026-transport-backed-delivery-workers.md`
- `docs/USER_GUIDE.md`
- `docs/api/README.md`
- `docs/architecture/README.md`
- `packages/server/README.md`
- `packages/server/src/context/process-manager-handoff.ts`
- `packages/server/src/context/projection-handoff.ts`
- `packages/server/src/delivery/delivery-worker.ts`
- `packages/server/src/delivery/delivery.ts`
- `packages/server/src/delivery/inbox-records.ts`
- `packages/server/src/delivery/inbox-storage.ts`
- `packages/server/src/delivery/inbox.ts`
- `packages/server/src/delivery/sharded-work-registry.ts`
- `packages/server/src/index.ts`
- `packages/server/src/repository/repository.ts`
- `packages/server/test/context/process-manager-handoff.test.ts`
- `packages/server/test/context/projection-handoff.test.ts`
- `packages/server/test/delivery/delivery-worker-runtime.test.ts`
- `packages/server/test/delivery/delivery-worker.test.ts`
- `packages/server/test/index.test.ts`
- `scripts/check-api-docs.mjs`

## Verification Evidence

- `2026-07-10T12:40:50Z`: Round 28 focused delivery/API verification passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/index.test.ts`; 6 files, 229 tests.
- `2026-07-10T12:40:50Z`: Round 28 generated build/docs/format verification
  passed: `pnpm --config.verify-deps-before-run=false
typecheck:build:generated`, `pnpm --config.verify-deps-before-run=false
docs:check` (with only the existing invalid-origin TypeDoc source-link
  warning), `pnpm --config.verify-deps-before-run=false format:check`, and
  `git diff --check`.
- `2026-07-10T08:34:24Z`: Round 18 cleanup verification passed:
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  (`tsc -b`), `pnpm --config.verify-deps-before-run=false format:check`, and
  `git diff --check`.
- `2026-07-10T08:35:29Z`: Round 18 cleanup focused delivery Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts`; 2 files,
  81 tests.
- `2026-07-10T03:55:22Z`: `pnpm --config.verify-deps-before-run=false exec
vitest run packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts` passed:
  3 files, 41 tests.
- `2026-07-10T03:55:22Z`: `pnpm --config.verify-deps-before-run=false exec
vitest run packages/server/test/context/process-manager-handoff.test.ts
packages/server/test/context/projection-handoff.test.ts` passed: 2 files,
  21 tests.
- `2026-07-10T03:55:22Z`: `pnpm --config.verify-deps-before-run=false
typecheck:build:generated` passed.
- `2026-07-10T03:58:57Z`: `pnpm --config.verify-deps-before-run=false exec
vitest run packages/server/test/index.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts` passed:
  2 files, 12 tests.
- `2026-07-10T03:58:57Z`: `pnpm --config.verify-deps-before-run=false
docs:check` passed with the existing TypeDoc invalid-origin warning only.
- `2026-07-10T03:58:57Z`: `pnpm --config.verify-deps-before-run=false
format:check` passed.
- `2026-07-10T04:00:15Z`: final sandboxed focused Vitest command passed the
  delivery, context, and index tests but server listener tests failed with
  `listen EPERM: operation not permitted 127.0.0.1`; rerun with approved local
  listener permissions.
- `2026-07-10T04:01:02Z`: escalated `pnpm
--config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/context/process-manager-handoff.test.ts
packages/server/test/context/projection-handoff.test.ts
packages/server/test/index.test.ts packages/server/test/server/server.test.ts`
  passed: 7 files, 93 tests.
- `2026-07-10T04:00:15Z`: final `pnpm
--config.verify-deps-before-run=false typecheck:build:generated` passed.
- `2026-07-10T04:00:15Z`: final `pnpm
--config.verify-deps-before-run=false docs:check` passed with the existing
  TypeDoc invalid-origin warning only.
- `2026-07-10T04:00:15Z`: final `git diff --check` passed.
- `2026-07-10T05:04:38Z`: coordinator review-readiness focused Vitest passed:
  `packages/server/test/delivery/delivery-worker.test.ts`,
  `packages/server/test/delivery/delivery-loop.test.ts`,
  `packages/server/test/delivery/delivery-worker-runtime.test.ts`,
  `packages/server/test/context/process-manager-handoff.test.ts`,
  `packages/server/test/context/projection-handoff.test.ts`, and
  `packages/server/test/index.test.ts`; 6 files, 72 tests.
- `2026-07-10T05:04:38Z`: coordinator
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed.
- `2026-07-10T05:04:38Z`: coordinator
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T05:04:38Z`: coordinator
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T05:04:38Z`: coordinator `git diff --check` passed.
- `2026-07-10T05:09:03Z`: documentation fix sub-agent addressed the P2
  doc-review finding that `packages/server/README.md` still described
  "Supported delivery workers" as an open production gap after T-0026 exported
  and documented `DeliveryWorker`. The README now narrows the gap to
  process-supervised delivery workers, transport-topology workers,
  scheduler/retry workers, retained attempts, production policy, and catch-up
  work while preserving that full production supervision/retry policy remains
  open.
- `2026-07-10T05:10:00Z`: documentation fix sub-agent
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T05:10:00Z`: documentation fix sub-agent `git diff --check`
  passed.
- `2026-07-10T05:18:42Z`: coordinator verification after reliability fix
  passed. Focused delivery/context/index Vitest passed with 74 tests;
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check HEAD` passed. `docs:check` still reports only the existing
  invalid-origin TypeDoc warning.
- `2026-07-10T04:14:06Z`: reliability fix sub-agent focused Vitest passed:
  `packages/server/test/delivery/delivery-worker-runtime.test.ts`; 1 file,
  4 tests.
- `2026-07-10T04:15:38Z`: reliability fix sub-agent required focused Vitest
  passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/delivery/delivery-loop.test.ts`; 2 files, 18 tests.
- `2026-07-10T04:16:06Z`: reliability fix sub-agent required
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T04:16:06Z`: reliability fix sub-agent required
  `pnpm --config.verify-deps-before-run=false format:check` passed after
  formatting the work log.
- `2026-07-10T04:16:06Z`: reliability fix sub-agent required
  `git diff --check` passed.
- `2026-07-10T04:26:38Z`: lease reliability fix sub-agent focused Vitest
  passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts`; 2 files,
  63 tests.
- `2026-07-10T04:27:00Z`: lease reliability fix sub-agent
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T04:31:10Z`: lease reliability fix sub-agent required focused
  Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-loop.test.ts`; 4 files, 81 tests.
- `2026-07-10T04:31:10Z`: lease reliability fix sub-agent required
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T04:31:10Z`: lease reliability fix sub-agent required
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T04:31:10Z`: lease reliability fix sub-agent required
  `git diff --check` passed.
- `2026-07-10T05:32:48Z`: coordinator verification after lease reliability
  fix passed. Focused delivery/context/index Vitest passed with 112 tests;
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check HEAD` passed. `docs:check` still reports only the existing
  invalid-origin TypeDoc warning.
- `2026-07-10T04:42:03Z`: Round 3 fix worker started on the remaining
  review findings. Verified that active drains already renew shard sessions,
  but the foreground `requireActive()` guard still relied only on the renewal
  timer setting `failed`.
- `2026-07-10T04:42:03Z`: Round 3 red regression added for delayed renewal:
  a drain advances past the current session expiry without running the renewal
  timer. Before the fix, focused Vitest delivered the row (`delivered: 1`,
  `failed: 0`) instead of leaving it pending.
- `2026-07-10T04:42:03Z`: Round 3 fixes implemented: delivery lease checks now
  compare the active session expiry to the delivery clock on every
  `requireActive()` call; the registry's renew/release snapshot helper was
  renamed to neutral session-claim terminology; curated docs now describe the
  pickup/renew/release seam and framework-owned renewal fencing.
- `2026-07-10T04:44:33Z`: Round 3 required focused Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts`; 3 files,
  68 tests.
- `2026-07-10T04:44:33Z`: Round 3 required
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T04:44:33Z`: Round 3 required
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T04:44:33Z`: Round 3 required
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T04:44:33Z`: Round 3 required `git diff --check` passed.
- `2026-07-10T04:46:21Z`: Round 3 post-fix local review used
  `HEAD~1...HEAD` (`1218eb11` to `eebe7b6f`) because no separate Agent tool was
  exposed in this session. Standards/spec review of the committed diff found no
  additional issues.
- `2026-07-10T04:53:23Z`: Round 4 fix worker started on the remaining review
  findings. Added a registry-level delayed-renewal regression before production
  edits. The focused test failed because `ShardedWorkRegistry.renew()` returned
  a renewed `node-a` session after the stored lease had expired.
- `2026-07-10T04:53:23Z`: Round 4 fix implemented the minimal registry fence:
  after loading the current session and confirming session ID/node ownership,
  `renew()` now returns `undefined` when `current.expiresAt` is at or before the
  renewal clock. The delayed-renewal regression then passed.
- `2026-07-10T04:53:23Z`: Round 4 documentation updated
  `RUNTIME_ARCHITECTURE.md` to describe storage-backed shard pickup, renewal,
  and release, and to state that renewal is framework-owned lease fencing for
  active drains rather than production retry or supervision policy.
- `2026-07-10T04:56:04Z`: Round 4 required focused Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts`; 2 files, 65 tests.
- `2026-07-10T04:56:04Z`: Round 4 required
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T04:56:04Z`: Round 4 required
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T04:56:04Z`: Round 4 required
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T04:56:04Z`: Round 4 required `git diff --check` passed.
- `2026-07-10T06:13:48Z`: Round 5 required focused Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/context/process-manager-handoff.test.ts
packages/server/test/context/projection-handoff.test.ts`; 6 files, 182 tests.
- `2026-07-10T06:13:48Z`: Round 5 required
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T06:13:48Z`: Round 5 required
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T06:13:48Z`: Round 5 required
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T06:13:48Z`: Round 5 required `git diff --check` passed.
- `2026-07-10T05:32:00Z`: Round 6 fix worker added a red regression for an
  endpoint callback that outlives the original per-message claim expiry while
  shard renewal succeeds. Before the fix, a competing package-internal claim
  could take the row after 25 ms.
- `2026-07-10T05:32:00Z`: Round 6 fixes implemented: active delivery now
  renews the current row claim to each renewed `ShardSession` expiry with
  compare-and-set fencing; claim-renewal failure is treated as lease loss;
  shared exact-message delivery logic was extracted from `drain()` and
  `drainMessage()`; public `InboxMessage`, `DeliveryEndpoint`, and `Inbox`
  no longer expose claim internals; claim storage types and helpers moved
  behind package-internal modules not re-exported from `packages/server/src/index.ts`;
  claim snapshotting now uses a direct internal codec instead of fabricating an
  inbox message.
- `2026-07-10T05:32:00Z`: Round 6 documentation updated the runtime
  architecture, developer API, generated API README source, package README, and
  delivery source comments to describe durable per-message claim fencing,
  skipped live competing claims, claimed-snapshot marking, unchanged-claim
  clearing after failures, and unclaimed endpoint snapshots.
- `2026-07-10T05:32:00Z`: Round 6 required focused Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/context/process-manager-handoff.test.ts
packages/server/test/context/projection-handoff.test.ts
packages/server/test/index.test.ts`; 7 files, 193 tests.
- `2026-07-10T05:32:00Z`: Round 6 required
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T05:32:00Z`: Round 6 required
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T05:32:00Z`: Round 6 required
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T05:32:00Z`: Round 6 required `git diff --check` passed.
- `2026-07-10T05:48:00Z`: Round 7 fix worker addressed the full review batch.
  Row claims are now durable fences regardless of expiry: `InboxStorage.claim`
  skips any already-claimed row instead of auto-reclaiming stale claims, while
  active drains still renew their own claim for diagnostics and CAS evidence.
- `2026-07-10T05:48:00Z`: Round 7 added `DeliveryRun.claimed` and aggregated
  `DeliveryLoopRun.claimed` so loops can distinguish rows read from rows
  actually claimed for endpoint work. `DeliveryLoop` now stops as `IDLE` when a
  drain reads only already-claimed rows, avoiding tight re-drain loops.
- `2026-07-10T05:48:00Z`: Round 7 added storage-bound read-limit validation
  for `InboxReadOptions.limit` with a fixed upper bound, and public drain/loop
  regressions for above-bound limits.
- `2026-07-10T05:48:00Z`: Round 7 made direct delivery fail closed for stored
  `CATCH_UP` labels before endpoint invocation. Supported endpoint labels
  remain `HANDLE_COMMAND`, `UPDATE_SUBSCRIBER`, and `REACT_UPON_EVENT`; the
  failing catch-up row remains pending and clears only the unchanged claim.
- `2026-07-10T05:48:00Z`: Round 7 documentation updated
  `docs/architecture/README.md`, `docs/USER_GUIDE.md`, and the
  `DeliveryLoop` class comment to describe shard pickup/renew/release CAS,
  durable row-claim fencing, skipped competing or abandoned claims, claim-free
  endpoint snapshots, and future production abandoned-claim recovery policy.
- `2026-07-10T05:47:23Z`: Round 7 focused delivery Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts`; 4 files,
  149 tests.
- `2026-07-10T05:48:00Z`: Round 7 interim
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T05:50:15Z`: Round 7 final requested Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/context/process-manager-handoff.test.ts
packages/server/test/context/projection-handoff.test.ts
packages/server/test/index.test.ts`; 8 files, 219 tests.
- `2026-07-10T05:50:15Z`: Round 7 final
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T05:50:15Z`: Round 7 final
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T05:50:15Z`: Round 7 final
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T05:50:15Z`: Round 7 final `git diff --check` passed.
- `2026-07-10T06:05:12Z`: Round 8 fix worker addressed the full review batch.
  Added a regression for the row-renewal/final-mark race; it failed before the
  fix with the endpoint callback invoked but the run reporting `delivered: 0`
  and `failed: 1`.
- `2026-07-10T06:05:12Z`: Round 8 serialized active row-claim renewal with
  final marking. After a callback returns, delivery now waits for any in-flight
  shard renewal, marks with the latest active internal snapshot, and does not
  clear the durable row claim merely because final marking failed after a
  successful callback.
- `2026-07-10T06:05:12Z`: Round 8 kept public delivery APIs claim-free in docs:
  `Delivery.drain()` and `drainMessage()` comments, `docs/api/README.md`, and
  `build-protocol/DEVELOPER_API.md` now describe framework-owned lease-fenced
  draining, supported endpoint labels, skipped unavailable rows, and
  `DeliveryRun` stats without row-claim mechanics.
- `2026-07-10T06:05:12Z`: Round 8 narrowed bounded-context inbox fields to
  internal interfaces that combine `ProcessManagerInbox`/`ProjectionInbox` with
  `register(...)`, leaving concrete local inbox classes only at construction.
- `2026-07-10T06:05:12Z`: Round 8 documentation now states local/direct workers
  do not auto-reclaim expired durable row claims, any existing internal durable
  claim is skipped including expired or abandoned claims, and stale recovery
  remains future production policy.
- `2026-07-10T06:05:12Z`: Round 8 focused regression passed after the fix:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts -t "marks delivered with
the renewed row claim when renewal races final marking"`.
- `2026-07-10T06:05:12Z`: Round 8 requested Vitest batch passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/context/process-manager-handoff.test.ts
packages/server/test/context/projection-handoff.test.ts
packages/server/test/index.test.ts`; 8 files, 220 tests.
- `2026-07-10T06:05:12Z`: Round 8 final
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T06:05:12Z`: Round 8 final
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T06:05:12Z`: Round 8 final
  `pnpm --config.verify-deps-before-run=false format:check` passed after
  formatting `delivery-worker.test.ts`.
- `2026-07-10T06:05:12Z`: Round 8 final `git diff --check` passed.
- `2026-07-10T06:21:29Z`: Round 9 fix worker addressed the full review batch.
  Replaced standalone exported inbox claim helpers with one grouped
  `@internal` `inboxStorageAccess` object that is imported only from the
  implementation module and is not re-exported by `packages/server/src/index.ts`.
- `2026-07-10T06:21:29Z`: Round 9 public storage hardening rejects caller
  snapshots that include internal `claim` metadata on `InboxStorage.write()` and
  public `markDelivered()` paths. Internal delivery workers still serialize
  claim-bearing snapshots only through `inboxStorageAccess` for claim, renewal,
  final mark, and clear CAS flows.
- `2026-07-10T06:21:29Z`: Round 9 documentation updated public user-guide
  delivery sections to describe lease-fenced local drains, skipped unavailable
  rows, public `InboxMessage` endpoint snapshots, and future abandoned-row
  recovery without exposing claim mechanics. Package README deferred wording was
  narrowed to transport topology, broker/process supervision, retained attempt
  history, and production retry policy. `DeliveryLoopRun.claimed` TypeDoc and
  the Developer API `drainMessage()` bullet were updated.
- `2026-07-10T06:21:29Z`: Round 9 focused preflight Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts`; 3 files, 148 tests.
- `2026-07-10T06:23:41Z`: Round 9 final requested Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/context/process-manager-handoff.test.ts
packages/server/test/context/projection-handoff.test.ts
packages/server/test/index.test.ts`; 8 files, 222 tests.
- `2026-07-10T06:23:41Z`: Round 9 final
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T06:23:41Z`: Round 9 final
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T06:23:41Z`: Round 9 final
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T06:23:41Z`: Round 9 final `git diff --check` passed.
- `2026-07-10T06:41:50Z`: Round 10 fixed Proxy-hidden public claim injection
  by snapshotting public inbox write and mark-delivered inputs into explicit
  claim-free `InboxMessage` objects before serialization. Visible top-level
  `claim` fields still fail closed; hidden Proxy claim metadata is ignored.
- `2026-07-10T06:41:50Z`: Round 10 renamed public delivery run stats from
  `claimed` to `accepted` on `DeliveryRun` and `DeliveryLoopRun`, updated loop
  aggregation and local handoff checks, and updated public API docs. Internal
  claim terminology remains only in worker/storage fencing code and internal
  architecture notes.
- `2026-07-10T06:41:50Z`: Round 10 reworded `ShardSession.shard` TypeDoc from
  "Shard claimed by this session" to "Shard held by this session."
- `2026-07-10T06:41:50Z`: Round 10 fixed stale-clock registry renew/release
  decisions by refreshing the clock after awaited storage reads and before
  expiry checks/CAS decisions. Added delayed-read/clock-advance regressions for
  both renew and release.
- `2026-07-10T06:38:06Z`: Round 10 focused preflight passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts`; 2 files, 141
  tests.
- `2026-07-10T06:41:50Z`: Round 10 requested Vitest batch passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/context/process-manager-handoff.test.ts
packages/server/test/context/projection-handoff.test.ts
packages/server/test/index.test.ts`; 8 files, 226 tests.
- `2026-07-10T06:42:00Z`: Round 10 final
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T06:42:17Z`: Round 10 final
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T06:42:26Z`: Round 10 final
  `pnpm --config.verify-deps-before-run=false format:check` passed after
  formatting `delivery.ts`.
- `2026-07-10T06:42:26Z`: Round 10 final `git diff --check` passed.
- `2026-07-10T06:55:21Z`: Round 11 fix worker reproduced the direct-drain limit
  finding with a focused regression. Before the fix, an oversized
  `Delivery.drain()` limit failed only after shard pickup/release storage access;
  the test observed two storage opens before rejection.
- `2026-07-10T06:55:21Z`: Round 11 reused the inbox read-limit validator at the
  direct `Delivery.drain()` boundary, preserving the positive safe integer and
  `1000` upper-bound contract while rejecting invalid limits before shard pickup.
- `2026-07-10T06:55:21Z`: Round 11 updated claim-free API docs in
  `docs/api/README.md` and `build-protocol/DEVELOPER_API.md` to state that
  `DeliveryLoopRun` aggregates `DeliveryRun` counts across loop drains and to
  name `status`, `runs`, `processed`, `accepted`, `delivered`, `failed`, and
  `failures`.
- `2026-07-10T06:55:21Z`: Round 11 focused red/green regression passed after the
  fix:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts -t "rejects invalid drain
limits before shard storage access"`.
- `2026-07-10T06:58:16Z`: Round 11 requested Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts`; 4 files, 192
  tests.
- `2026-07-10T06:58:16Z`: Round 11 final
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated` passed
  (`tsc -b`).
- `2026-07-10T06:58:16Z`: Round 11 final
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T06:58:16Z`: Round 11 final
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T06:58:16Z`: Round 11 final `git diff --check` passed.
- `2026-07-10T07:09:12Z`: Round 12 fix worker addressed the post-claim lease
  gap in `Delivery.#deliverMessage()`: after an inbox row is accepted, delivery
  now awaits any in-flight shard renewal and re-checks the active lease before
  endpoint label validation or callback invocation. If the lease is inactive,
  the existing pre-callback cleanup clears the row acceptance and leaves the row
  pending for a later run.
- `2026-07-10T07:09:12Z`: Round 12 added a delayed inbox-acceptance regression
  that pauses the acceptance CAS, advances the worker clock past shard expiry,
  and verifies no endpoint callback runs and the row remains `TO_DELIVER`.
- `2026-07-10T07:09:12Z`: Round 12 separated the developer API low-level
  shard pickup/read sample from the higher-level `Delivery.drain()` sample by
  releasing the manual shard session before calling `drain()`.
- `2026-07-10T07:09:12Z`: Round 12 reworded public delivery callback TypeDoc
  from per-pending-row wording to "for each available supported worker row" in
  `delivery.ts`, `delivery-loop.ts`, and `delivery-worker.ts`.
- `2026-07-10T07:09:12Z`: Round 12 focused regression passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts -t "clears row acceptance
without dispatch when claim completion crosses shard expiry"`; 1 test passed.
- `2026-07-10T07:11:00Z`: Round 12 requested Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/inbox.test.ts`; 3 files, 152 tests.
- `2026-07-10T07:11:00Z`: Round 12 final
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T07:11:00Z`: Round 12 final
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T07:11:00Z`: Round 12 final
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T07:11:00Z`: Round 12 final `git diff --check` passed.
- `2026-07-10T07:21:10Z`: Round 13 fix worker reproduced the remaining
  pickup stale-clock finding with a focused delayed-read regression. Before the
  fix, a competing pickup returned `undefined` even though the awaited shard
  read advanced the clock past the stored session expiry.
- `2026-07-10T07:21:10Z`: Round 13 refreshed `ShardedWorkRegistry.pickUp()`'s
  clock immediately after `readShardRecord()`/`readSession()` on each attempt,
  so both the live-session check and replacement `ShardSession` timestamps use
  the post-read clock, matching `renew()` and `release()`.
- `2026-07-10T07:21:10Z`: Round 13 focused regression passed after the fix:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/sharded-work-registry.test.ts -t "replaces a
shard session when storage read delay crosses expiry during pickup"`.
- `2026-07-10T07:21:10Z`: Round 13 requested Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/delivery/delivery-worker.test.ts`; 2 files, 78 tests.
- `2026-07-10T07:21:10Z`: Round 13
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T07:21:10Z`: Round 13
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T07:21:10Z`: Round 13
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T07:21:10Z`: Round 13 final `git diff --check` passed.
- `2026-07-10T07:30:12Z`: Round 14 docs fix worker addressed the developer
  API example finding: the manual shard pickup/read snippet no longer calls an
  undefined `inspectPendingRows()` helper. It now consumes the pending rows
  inline with a small `for...of` loop that logs public message fields.
- `2026-07-10T07:32:33Z`: Round 14 final
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T07:32:33Z`: Round 14 final
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T07:32:33Z`: Round 14 final `git diff --check` passed.
- `2026-07-10T07:44:11Z`: Round 15 fix worker reproduced the remaining
  inbox-record claim snapshot finding with a focused regression. Before the
  fix, `InboxRecords.write()` serialized proxy-provided optional `claim`
  metadata hidden from public own/has checks, and the read snapshot exposed the
  internal field.
- `2026-07-10T07:44:11Z`: Round 15 changed inbox record snapshotting so the
  optional internal `claim` field is read only when it is an own property.
  Added regression coverage for both proxy-provided and inherited claim
  metadata staying out of public record snapshots.
- `2026-07-10T07:44:11Z`: Round 15 narrowed delivery retry docs to endpoint
  callback failures and updated `DeliveryRun`, `DeliveryFailure`, and
  `DeliveryLoopRun` TypeDoc so failure counts/errors cover endpoint callback,
  fail-closed validation, lease/fencing, and delivery-status update failures
  without exposing internal claim details.
- `2026-07-10T07:46:23Z`: Round 15 requested focused Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/delivery-worker.test.ts`; 2 files, 137 tests.
- `2026-07-10T07:46:23Z`: Round 15
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T07:46:23Z`: Round 15
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing invalid-origin TypeDoc warning only.
- `2026-07-10T07:46:23Z`: Round 15
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T07:46:23Z`: Round 15 final `git diff --check` passed.
- `2026-07-10T07:48:19Z`: Root orchestrator independently verified the Round
  15 fix before commit. Focused inbox and delivery-worker Vitest passed with 2
  files and 137 tests; `typecheck:build:generated`, `docs:check`,
  `format:check`, and `git diff --check` passed. `docs:check` reported only
  the existing invalid-origin TypeDoc warning.
- `2026-07-10T07:52:54Z`: Round 16 review intake completed. Style and security
  reviewers reported clean. Documentation and TypeScript/API docs reviewers
  found remaining broad retry wording in `packages/server/README.md` and
  `build-protocol/DEVELOPER_API.md`, plus stale task status. Reliability found
  a high-severity bug: claim-clear failures after endpoint or validation
  failure were suppressed, allowing a retryable `TO_DELIVER` row to remain
  durably unavailable.
- `2026-07-10T07:56:10Z`: Round 16 fix worker added a focused claim-clear
  regression and confirmed the red failure before the implementation change:
  the new test reported only `Error: endpoint failed` instead of an
  `AggregateError` containing both the endpoint and claim-clear failures. The
  delivery catch path now surfaces claim-clear failures through
  `DeliveryRun.failures` / `DeliveryFailure.error` while preserving the
  original endpoint or validation failure in the aggregate error. Public
  README/API wording now limits later-run retry wording to endpoint callback
  failures after active-claim cleanup succeeds, and states validation,
  lease/fencing, status-update, and claim-clear failures have no immediate
  recovery guarantee in this slice. The task brief status is updated to the
  current review-fix state.
- `2026-07-10T07:58:44Z`: Round 16 verification completed. Focused delivery
  Vitest passed with 1 file and 37 tests. `typecheck:build:generated` passed.
  `docs:check` passed and reported only the existing invalid-origin TypeDoc
  warning. `format:check` initially failed on review-log Markdown wrapping and
  later on this verification-log wrapping; after formatting the flagged files,
  the final `format:check` passed. `git diff --check` passed.
- `2026-07-10T08:01:29Z`: Root orchestrator independently verified the Round
  16 fix before commit. Focused `delivery-worker.test.ts` passed with 37 tests;
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` passed. `docs:check` reported only the existing
  invalid-origin TypeDoc warning.
- `2026-07-10T08:06:46Z`: Round 17 review intake completed. Security reported
  clean. Style, documentation, and TypeScript/API docs found public/API wording
  that still mentioned row-claim cleanup or omitted framework-cleanup
  qualification for endpoint retry. TypeScript/API docs also found the public
  `AggregateError` message leaked internal claim wording. Reliability found
  that cleanup returning `undefined` after endpoint failure was not reported,
  and that `DeliveryLoop.maxFailures` lacked a practical upper bound.
- `2026-07-10T08:12:00Z`: Round 17 fix worker added red regressions for
  claim-free cleanup error wording, cleanup returning `undefined`, and
  `DeliveryLoop.maxFailures` above 1000. Before the fix, focused Vitest failed
  on the old `AggregateError` message, the ignored cleanup result, and missing
  max-failure bound. The implementation now reports framework cleanup failures
  through `DeliveryRun.failures`, treats cleanup returning `undefined` as a
  framework cleanup failure, caps `maxFailures` at 1000 before loop start, and
  updates public docs/TypeDoc to describe cleanup failures without public claim
  terminology.
- `2026-07-10T08:12:00Z`: Round 17 focused delivery Vitest passed after the
  fix: `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts`; 2 files, 55 tests.
  Remaining required verification was still pending at this checkpoint.
- `2026-07-10T08:13:51Z`: Round 17 verification completed. Focused delivery
  Vitest rerun passed with 2 files and 55 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with the existing invalid-origin TypeDoc warning only.
  `pnpm --config.verify-deps-before-run=false format:check` initially failed
  on `packages/server/src/delivery/delivery-loop.ts`; after formatting that
  file with Prettier, the rerun passed. `git diff --check` passed.
- `2026-07-10T08:16:50Z`: Root orchestrator independently verified the Round
  17 fix before commit. Focused `delivery-worker.test.ts` and
  `delivery-loop.test.ts` passed with 55 tests; `typecheck:build:generated`,
  `docs:check`, `format:check`, and `git diff --check` passed. `docs:check`
  reported only the existing invalid-origin TypeDoc warning.
- `2026-07-10T08:21:18Z`: Round 18 review intake completed. TypeScript/API
  docs, style, and documentation reviewers found remaining public architecture
  and task-brief wording that exposed row-claim internals or overpromised retry
  without framework-cleanup success. Style also found declaration-order and
  naming issues in `delivery-loop.ts`, plus an exported standalone inbox read
  limit helper. Security found unbounded `leaseMs` could overflow Node timer
  delay behavior and spin renewals. Reliability found a P1 starvation bug:
  an all-unavailable first read page could make `DeliveryLoop` report `IDLE`
  while later rows remained deliverable.
- `2026-07-10T08:27:34Z`: Round 18 fix worker added red regressions for
  unavailable head-row scan-through and oversized `leaseMs`. Before the fix,
  focused Vitest failed with no dispatch for `signal-available-tail` and with
  `leaseMs: 2147483648` accepted. The implementation now validates shard lease
  durations as positive safe integers at most `2147483647`, uses a bounded
  scan window in `Delivery.drain()` to skip unavailable rows and reach later
  available rows, moves the delivery-loop failure bound below the primary class,
  renames the bounded integer helper, and groups inbox read-limit validation
  under `inboxStorageAccess`.
- `2026-07-10T08:27:34Z`: Round 18 docs now use public claim-free delivery
  wording: rows unavailable to the active worker are skipped before endpoint
  invocation, endpoint callbacks receive public `InboxMessage`, endpoint
  failures remain pending for later runs only when framework-owned cleanup
  succeeds, and cleanup, validation, lease/fencing, and delivery-status
  failures have no immediate retry or recovery guarantee in this slice.
- `2026-07-10T08:27:34Z`: Round 18 verification completed. Focused delivery
  Vitest passed with 4 files and 199 tests. The generated build typecheck
  passed (`tsc -b`). `docs:check` passed and reported only the existing
  invalid-origin TypeDoc warning. `format:check` initially flagged
  `packages/server/src/delivery/delivery-loop.ts`; after formatting that file,
  the final `format:check` passed. `git diff --check` passed.
- `2026-07-10T08:37:50Z`: Root orchestrator independently verified the Round
  18 bundle before commit. Focused delivery Vitest passed with 4 files and 199
  tests; `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` passed. `docs:check` reported only the existing
  invalid-origin TypeDoc warning. A coordinator cleanup removed the newly
  introduced exported standalone lease helper before this verification.
- `2026-07-10T08:44:02Z`: Round 19 review intake completed. API docs and
  security reported clean. Documentation found delivery `limit` wording still
  described a page size even though the starvation fix made it an accepted-work
  bound plus scan window. Reliability found the bounded growing scan still
  cannot pass a full `maxReadLimit` page of unavailable rows because storage
  queries have no cursor/offset. Style found remaining long methods/stateful
  closure cleanup opportunities in `delivery.ts`.
- `2026-07-10T08:57:33Z`: Round 19 fix worker added `RecordQuery.offset`
  validation and in-memory offset application after sort/before limit, threaded
  inbox read offsets into delivery scanning, and changed `Delivery.drain()` to
  advance past rows that remain pending/unavailable while delivered rows fall
  out of the `TO_DELIVER` query. The regression with 1000 unavailable head rows
  and one deliverable tail row first failed before the final stop-condition fix
  (`seen` was `[]`), then passed after exhaustion was based on a short page
  rather than `maxReadLimit`. Delivery `limit` docs now describe an
  accepted-work cap plus initial scan window, while `InboxReadOptions.limit`
  remains a read page-size. `drainMessage()`/delivery attempt handling were
  split into smaller helpers and the active-claim closure was replaced with a
  private `ActiveClaim` class.
- `2026-07-10T08:57:33Z`: Round 19 focused verification passed after refreshing
  generated package output with
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`.
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/storage/test/memory/in-memory-record-storage.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts` passed with 3 files and
  72 tests. Remaining required verification was still pending at this
  checkpoint.
- `2026-07-10T08:59:58Z`: Round 19 final verification completed. Focused
  storage/delivery Vitest passed with 3 files and 72 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with the existing invalid-origin TypeDoc warning only.
  `pnpm --config.verify-deps-before-run=false format:check` passed.
  `git diff --check` passed.
- `2026-07-10T09:03:24Z`: Root orchestrator independently verified the Round
  19 bundle before commit. Focused storage, delivery, context handoff, and
  server index Vitest passed with 9 files and 250 tests;
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` passed. `docs:check` reported only the existing
  invalid-origin TypeDoc warning. Public storage docs were also aligned to
  mention non-negative `RecordQuery.offset`.
- `2026-07-10T09:09:57Z`: Round 20 review intake completed. API docs and
  reliability reported clean. Documentation found low-severity wording/status
  drift around delivery `limit` and the task integration result. Style found
  `#drainAvailableMessages` still exceeded the method-length target and
  questioned exported claim-bearing storage internals. Security found a
  medium-severity storage canonicalization bug: user-controlled object keys
  such as `__proto__`, `constructor`, `prototype`, `bigint`, or `bytes` could
  collide with plain-object normalization or internal tag detection.
- `2026-07-10T09:13:31Z`: Round 20 fix worker added an in-memory storage
  regression for collision-prone user object keys (`__proto__`, `constructor`,
  `prototype`, `bigint`, and `bytes`). The focused storage test failed before
  the fix with `SyntaxError: Cannot convert bigint:a to a BigInt`, then passed
  after normalized user objects moved to null-prototype records with
  `Object.defineProperty` and internal bigint/bytes tags moved to private
  symbols plus a custom canonical encoder.
- `2026-07-10T09:13:31Z`: Round 20 also extracted
  `Delivery.#readPendingDeliveryPage()` from `#drainAvailableMessages`, aligned
  delivery `limit` docs to say accepted delivery attempts include endpoint work
  and fail-closed validation, and updated the task integration result to the
  Round 19 final state with Round 20 verification still pending. The
  claim-bearing `inboxStorageAccess` export was left unchanged in this pass to
  avoid broad churn outside the requested small semantic helper.
- `2026-07-10T09:16:08Z`: Round 20 verification completed. The required
  focused storage/delivery Vitest passed with 2 files and 56 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with the existing invalid-origin TypeDoc warning only.
  `pnpm --config.verify-deps-before-run=false format:check` passed after
  formatting `packages/storage/src/memory/tenant-records.ts`.
  `git diff --check` passed.
- `2026-07-10T09:19:43Z`: Root orchestrator independently verified the Round
  20 fix before commit. Focused in-memory storage and delivery-worker Vitest
  passed with 2 files and 56 tests; `typecheck:build:generated`,
  `docs:check`, `format:check`, and `git diff --check` passed. `docs:check`
  reported only the existing invalid-origin TypeDoc warning.
- `2026-07-10T09:24:24Z`: Round 21 review intake completed. API docs and
  security reported clean. Documentation found missing storage-offset mentions
  in user/architecture summaries and one stale retry qualifier in the task
  brief. Reliability found `DeliveryLoop.maxFailures` was enforced only after a
  whole drain, so a single drain could exceed the failure budget. Style found
  several long `InboxStorage` private methods and kept the internal
  `inboxStorageAccess` export concern open.
- `2026-07-10T09:29:18Z`: Round 21 fix worker added a regression with two
  failing pending rows and default `maxFailures: 1`; it failed before the fix
  because both endpoint attempts ran in one drain. `DeliveryLoop` now validates
  the configured read limit before running and caps each `Delivery.drain()` by
  the smaller of the configured accepted-work limit and the remaining failure
  budget, so the loop returns `FAILED` after one accepted failure attempt.
- `2026-07-10T09:29:18Z`: Round 21 docs now mention non-negative
  `RecordQuery.offset` in user and architecture storage summaries, including
  that offsets apply after sorting and before limits. The task brief retry
  sentence now includes the framework-owned cleanup success qualifier.
  `InboxStorage.#handleStoredGuardMessage()` was split into guard-finalization
  and row-repair helpers. The exported `inboxStorageAccess` bridge remains
  unchanged because narrowing it would require broader worker/test call-site
  churn outside the requested scoped method-length cleanup.
- `2026-07-10T09:29:18Z`: Round 21 verification completed. The required
  focused delivery Vitest passed with 2 files and 119 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with the existing invalid-origin TypeDoc warning only.
  `pnpm --config.verify-deps-before-run=false format:check` passed.
  `git diff --check` passed.
- `2026-07-10T09:34:08Z`: Root orchestrator independently verified the Round
  21 fix before commit. Focused delivery-loop and inbox Vitest passed with 2
  files and 119 tests; `typecheck:build:generated`, `docs:check`,
  `format:check`, and `git diff --check` passed. `docs:check` reported only
  the existing invalid-origin TypeDoc warning.
- `2026-07-10T09:39:00Z`: Round 22 review intake completed. Documentation and
  reliability reported clean. Style found public architecture prose in
  `build-protocol/RUNTIME_ARCHITECTURE.md` still exposed row-claim internals
  and repeated inbox-storage method-length concerns. Security found unsupported
  but valid `CATCH_UP` rows could consume loop failure budget repeatedly and
  starve supported rows. TypeScript/API docs found `packages/server/README.md`
  still omitted `RecordQuery.offset` from `queryVersioned()` docs.
- `2026-07-10T09:43:30Z`: Round 22 fix worker added a red/green regression
  proving a `CATCH_UP` row ahead of a supported row stays pending, does not
  invoke the endpoint, does not consume failure budget, and does not starve the
  supported row. The red run failed with `accepted: 2` and `failed: 1`; after
  the fix, `Delivery.drain()` and `drainMessage()` skip worker-unsupported
  labels before row acceptance, storage-claiming, or callback invocation.
- `2026-07-10T09:43:30Z`: Round 22 docs now describe direct drains through
  public concepts: shard lease fencing, rows unavailable to the active worker,
  public `InboxMessage` snapshots, endpoint callback cleanup failures, and
  deferred stale-row recovery. `packages/server/README.md` now documents
  non-negative `RecordQuery.offset` for `queryVersioned()`, applied after
  sorting and before limits. Scoped inbox-storage cleanup split
  `#claimAndWrite()` rollback handling into `#rollbackPendingGuard()`;
  broader internal-access redesign remains out of scope.
- `2026-07-10T09:43:30Z`: Round 22 verification completed. Required delivery
  Vitest passed with 3 files and 159 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with the existing invalid-origin TypeDoc warning only.
  `pnpm --config.verify-deps-before-run=false format:check` passed.
  `git diff --check` passed.
- `2026-07-10T09:46:19Z`: Root orchestrator independently verified the Round
  22 fix before commit. Focused delivery-worker, delivery-loop, and inbox
  Vitest passed with 3 files and 159 tests; `typecheck:build:generated`,
  `docs:check`, `format:check`, and `git diff --check` passed. `docs:check`
  reported only the existing invalid-origin TypeDoc warning.
- `2026-07-10T10:08:00Z`: Round 23 review intake reconciled the next reviewer
  batch: keep callback-visible delivery snapshots private from the internal
  claimed CAS row, correct the finite skipped-row scan budget, clean up stale
  unsupported-label docs wording, and write a Round 23 fix report before
  finishing verification.
- `2026-07-10T10:08:00Z`: Round 23 fixes cloned callback-visible
  `InboxMessage` state away from the internal claimed row, corrected the finite
  scan budget to storage read cap plus accepted-work limit, refreshed docs so
  valid worker-unsupported labels stay pending while malformed/deprecated
  legacy data still fails closed, and wrote
  `build-protocol/tasks/T-0026-transport-backed-delivery-workers/round-23-fix-report.md`.
- `2026-07-10T10:08:00Z`: Round 23 verification completed. Focused delivery
  Vitest passed with 3 files and 162 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with the existing invalid-origin TypeDoc warning only.
  `pnpm --config.verify-deps-before-run=false format:check` passed.
  `git diff --check` passed.
- `2026-07-10T11:22:00Z`: Round 24 review intake reconciled the remaining
  review trail and code findings: add missing Round 23 and Round 24 durable
  log/review entries, tighten the public endpoint callback type so `CATCH_UP`
  is excluded from endpoint/failure rows, then-current expired per-message
  claim reclaim at claim CAS time, stop counting pre-callback failures against
  the accepted-work limit, and decouple inbox page size from accepted-work
  limit so limit `1` does not force one query per skipped row. Historical
  correction: Round 35 commit `5c3705e2` superseded the expired-claim reclaim
  behavior at the time; Round 43 later restored expired-claim reclaim during
  claim CAS while live row claims still block.
- `2026-07-10T11:22:00Z`: Round 24 fixes exported `DeliveryEndpointMessage`
  for supported worker labels only, narrowed `DeliveryEndpoint` (later renamed
  to `OnDeliveryMessage` in Round 25) and
  `DeliveryFailure.message` to that public shape, updated exact-row callback
  docs, then allowed `InboxStorage` claim CAS to replace expired claims using
  the storage clock. Historical correction: Round 35 / `5c3705e2` later
  removed expired-claim reclaim. Round 43 superseded that no-reclaim rule:
  live row claims block, while expired row claims may be replaced during claim
  CAS using the storage clock. Round 24 also clarified the claim comment, kept
  pre-callback claim/lease failures visible without incrementing accepted
  endpoint work, widened direct drain page reads to the storage read cap while
  preserving the finite scan budget, updated delivery-loop coverage for
  live-vs-expired claims, and wrote
  `build-protocol/tasks/T-0026-transport-backed-delivery-workers/round-24-fix-report.md`.
- `2026-07-10T11:22:00Z`: Round 24 verification completed. Focused delivery
  Vitest passed with 3 files and 165 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed after updating API-doc expectations for `DeliveryEndpointMessage`,
  still reporting only the existing invalid-origin TypeDoc warning.
  `pnpm --config.verify-deps-before-run=false format:check` passed.
  `git diff --check` passed.
- `2026-07-10T10:58:57Z`: Round 25 review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..71ba68e0.diff` after the handoff-only
  commit. Documentation and TypeScript/API docs found public prose still
  naming callback snapshots as `InboxMessage` instead of
  `DeliveryEndpointMessage`, and documentation found stale wording that treated
  expired per-message ownership as wholly future recovery. Style found callback
  naming violations in `DeliveryEndpoint`, `renewClaim`, and `action`.
  Reliability found scan-budget exhaustion can make `DeliveryLoop` stop idle
  before a supported tail row, expired ownership can be missed when it expires
  during the claim read, and pre-callback failures can exceed a loop failure
  budget because they do not increment `accepted`. Security found that
  `leaseMs: 1` can drive an unsafe renewal cadence. One fix worker will address
  the complete batch before re-review.

## Review Rounds

Lease reliability follow-up verified after implementation commit `94b4c632`.
Round 4 delayed-renewal registry fix verified before commit.
Round 5 message-claim/release/API/docs fix verified before commit.
Round 6 per-message claim renewal/API cleanup/docs fix verified before commit.
Round 7 no-auto-reclaim/loop-idle/limit/catch-up/docs fix verified before commit.
Round 8 renewal/mark serialization/API-doc cleanup fix verified before commit.
Round 9 internal access grouping/public claim-injection hardening/docs fix
verified before commit.
Round 10 Proxy-hardening/API-stat/stale-clock fix verified before commit.
Round 11 early drain-limit validation/API docs fix verified before commit.
Round 12 post-claim lease recheck/docs fix verified before commit.
Round 13 pickup stale-clock fix verified before commit.
Round 14 developer API snippet fix verified before commit.
Round 15 retry-doc/API/security snapshot fix verified before commit.
Round 16 claim-clear reporting and retry-doc fix verified before commit.
Round 17 framework-cleanup wording/reporting and max-failure-bound fix verified.
Round 18 scan-through, lease-bound, style, and claim-free-doc fix verified.
Round 19 storage-offset scan exhaustion, limit docs, and delivery style fix
verified.
Round 20 canonical storage keys, limit docs, and delivery helper fix verified.
Round 21 failure-budget cap, offset docs, and inbox-storage helper split
verified.
Round 22 unsupported-label skip, public docs, offset docs, and inbox-storage
helper split verified.
Round 23 snapshot privacy, finite scan budget, docs cleanup, and verification
report fix verified.
Round 24 durable trail, endpoint type narrowing, then-current expired-claim
reclaim, accepted-limit accounting, and bounded page-read fix verified; the
expired-claim reclaim behavior was superseded by Round 35 / `5c3705e2`.
Round 25 five-lane review found blocking docs/API, style, security, and
reliability findings; fix verified and re-review pending.
Round 26 re-review found blocking API and reliability findings; fix verified
and re-review pending.
Round 27 re-review found blocking documentation and security findings; fix
verified and re-review pending.
Round 28 re-review found blocking style, documentation, API, security, and
reliability findings; fix verified and re-review pending.
Round 29 re-review found blocking style, documentation, and reliability
findings; fix verified and re-review pending.
Round 30 re-review found minor style and TypeScript/API docs findings; fix
verified and re-review pending.
Round 31 re-review found documentation trace and reliability findings; fix
verified and re-review pending.
Round 32 re-review found lint-gate and documentation trace findings; fix
verified and re-review pending.
Round 33 re-review found Round 32 commit-trace and report-format findings; fix
verified and re-review pending.
Round 34 re-review found tooling typecheck and Round 33 commit-trace findings;
fix verified and re-review pending.
Round 35 re-review found report-format, Round 34 commit-trace, expired
row-claim reclaim, and moving-offset pagination findings; fix verified and
re-review pending.
Round 36 re-review found format and lint-gate findings; fix verified and
re-review pending.
Round 37 fix updated durable commit breadcrumbs and historical expired-claim
reclaim wording, added a boundary/read race regression, fixed drain
rescanning, and passed verification; re-review pending.
Round 38 re-review found commit-note cleanup only; fix verified and re-review
pending.
Round 39 records-only cleanup was verified and committed as `faa2d814`; Round
40 re-review found stale Round 39 status records plus a full stale offset-page
reliability finding; fix verified and re-review pending.
Round 41 re-review found a range whitespace verification failure, a public raw
worker callback security boundary finding, and partial stale-head rescan
query-bound regression risk; fix verified and re-review pending.
Round 42 re-review found remaining root raw-callback delivery exports,
inaccurate Round 41 range-check records, and a work-log formatting issue; fix
verified and re-review pending.

- `2026-07-10T15:05:00Z`: Round 35 fix worker started in the existing
  T-0026 worktree. Read the required protocol, task, work log, review log,
  `delivery.ts`, `inbox-storage.ts`, and focused delivery tests before edits.
  Completed the canonical skill applicability check in
  `round-35-fix-report.md`. The implementation will use TDD regressions for
  expired-claim blocking and moving pending-set pagination, preserve the
  existing uncommitted durable-log intake, avoid `human-review-1-jul.md` and
  `.codex-review-packages/`, and will not commit.

### Round 25 Fix Start - `2026-07-10`

- Began the required review-fix batch in the isolated T-0026 worktree without
  touching the human review file or creating a commit.
- Recorded the canonical skill applicability check and TDD intent in
  `tasks/T-0026-transport-backed-delivery-workers/round-25-fix-report.md`.
- Scope: callback API terminology/naming, claim-recovery docs, a shared safe
  lease lower bound, finite-scan loop continuation, then-current post-read
  claim expiry/reclaim behavior, and loop-only pre-callback failure budget
  enforcement. Historical correction: Round 35 / `5c3705e2` superseded reclaim;
  Round 43 later restored expired-claim reclaim during claim CAS while live
  claims still block.
- Focused red evidence reproduced all four runtime/security findings before
  implementation. The fix adds a `1000ms` shared lease floor, clock refresh
  after claim reads, loop continuation beyond an all-skipped finite scan,
  loop-only pre-callback failure stopping, and the `OnDeliveryMessage` public
  callback name. Public docs then stated copied snapshot and reclaim behavior;
  Round 35 / `5c3705e2` superseded the reclaim portion with no-reclaim
  blocking until explicit recovery policy exists.
- `2026-07-10T12:15:58+02:00`: Coordinator verification after the Round 25 fix
  worker passed. Focused delivery Vitest passed with 4 files and 210 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with only the existing invalid-origin TypeDoc source-link warning.
  `pnpm --config.verify-deps-before-run=false format:check` passed.
  `git diff --check` passed.
- Round 25 fix commit: `e089963f` (`Fix delivery loop reliability and docs`).
- `2026-07-10T11:29:35Z`: Round 26 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..2caee0d7.diff`. Documentation and
  security reported clean. Style reported only the existing minor test-helper
  size concern. TypeScript/API docs found that loop-only `scanOffset` and
  `maxFailures` leaked through exported `DeliveryDrainOptions`. Reliability
  found `DeliveryLoop` still reduced the accepted-work limit to the remaining
  failure budget, defeating batching for healthy backlogs. One fix worker will
  address both blocking findings before another re-review.
- Round 26 fix work recorded its canonical skill applicability check in
  `round-26-fix-report.md`, added public type coverage excluding `scanOffset`
  and `maxFailures`, and added a red/green loop regression. The pre-fix test
  completed two successful callbacks and one failure over three drains; the
  fixed loop completes them in one drain while still stopping at the first
  failure. `Delivery` now keeps those controls behind its internal,
  non-barrel delivery access capability. Required five-file Vitest (220 tests),
  build typecheck, docs check, format check, and `git diff --check` passed; the
  docs check retained only the existing invalid-origin TypeDoc warning.
- `2026-07-10T12:41:43+02:00`: Coordinator verification after the Round 26 fix
  worker passed. Focused delivery/API Vitest passed with 5 files and 220 tests.
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with only the existing invalid-origin TypeDoc source-link warning.
  `pnpm --config.verify-deps-before-run=false format:check` passed.
  `git diff --check` passed.
- Round 26 fix commit: `47672dc8` (`Fix delivery drain internal controls`).
- `2026-07-10T11:55:46Z`: Round 27 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..599d6bcf.diff`. Style and
  performance/reliability reported clean, with the previous test-helper size
  note still non-blocking. TypeScript/API docs reported a minor TypeDoc clarity
  issue for the non-exported `DeliveryEndpointLabel` referenced by
  `DeliveryEndpointMessage.label`. Documentation found the broader guides still
  under-specify direct-drain accounting and finite scanning, and noted
  historical Round 24 entries should reconcile `DeliveryEndpoint` with the
  later `OnDeliveryMessage` rename. Security found two important issues:
  mutable caller-owned tenant context can split a drain across tenants after an
  awaited callback, and `DeliveryLoop.run()` lacks an aggregate scan/run bound
  under continuous unsupported writes. One fix worker will address the complete
  Round 27 batch before another re-review.
- `2026-07-10T12:08:03Z`: Round 27 fix worker completed the required skill
  applicability check in
  `build-protocol/tasks/T-0026-transport-backed-delivery-workers/round-27-fix-report.md`,
  then added focused red regressions before production edits. The first failed
  because mutating a multitenant delivery context during an awaited callback
  redirected renew/mark/cleanup/release to another tenant, producing
  `delivered: 0` and `failed: 1` instead of a delivered row. The second failed
  because `DeliveryLoop.run()` kept starting skipped-only drains until the test
  seam threw `DeliveryLoop kept starting skipped-only drains without stopping.`
- `2026-07-10T12:08:03Z`: Round 27 implementation now snapshots and validates
  multitenant storage context once per drain, reuses that immutable scope for
  every inbox, dedup, and shard-registry operation in the drain, and returns a
  resumable `PAUSED` loop status after two saturated skipped-only drains so a
  later `run()` resumes from the saved scan offset instead of letting one
  invocation scan forever. Public docs now spell out the direct-drain contract:
  `limit` caps callbacks actually invoked, scanning is bounded by
  `maxReadLimit + limit`, and skipped/unsupported rows do not consume accepted
  work or loop failure budget. Round 29 correction: pre-callback
  claim/validation/lease/cleanup/status-update failures leave `accepted`
  unchanged, but do increment `failed` and count toward
  `DeliveryLoop.maxFailures`. The public
  `DeliveryEndpointMessage.label` property now inlines its supported-label
  union, and the historical Round 24 notes now mention the later
  `OnDeliveryMessage` rename.
- `2026-07-10T12:08:03Z`: Round 27 final verification passed. Focused delivery
  and API Vitest passed for `delivery-worker`, `delivery-loop`, `inbox`,
  `sharded-work-registry`, and `index` tests (5 files, 222 tests).
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`). `pnpm --config.verify-deps-before-run=false docs:check`
  passed with only the existing invalid-origin TypeDoc source-link warning.
  `pnpm --config.verify-deps-before-run=false format:check` passed after
  formatting the touched review log, architecture guide, `delivery.ts`, and the
  delivery-worker test. `git diff --check` passed. No commit was created.
- `2026-07-10T12:11:51Z`: Coordinator reran the Round 27 verification after
  inspecting the worker diff. Focused delivery/API Vitest passed with 5 files
  and 222 tests. `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` passed; `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning.
- Round 27 fix commit: `770981ea` (`Fix delivery drain tenant scope and loop
bounds`).
- `2026-07-10T12:22:12Z`: Round 28 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..0dd1bfdf.diff`. API docs and security
  found that `DeliveryWorkerRun.status` can hide `PAUSED` behind `SKIPPED`.
  Style found `deliveryAccess.drain()` should fail fast for non-owned
  instances, the paused-loop test should exercise loop-only controls through a
  real delivery/internal access path, the delivery-worker fault fixture should
  be split or moved by concern, and the loop read-cap constant duplicates
  `inboxStorageAccess.maxReadLimit`. Documentation found Round 27 wording
  incorrectly said pre-callback failures do not consume failure budget; they
  count toward `failed`/`maxFailures` while not incrementing `accepted`.
  Documentation also found legacy `IMPORT_EVENT` corruption should be described
  as read/drain rejection with `DeliveryStorageCorruptionError`, not as returned
  `DeliveryRun.failures`. Reliability found raw paused `scanOffset` can skip a
  supported row after earlier skipped rows disappear between runs. Security also
  found same-event-loop lease renewal can be starved by CPU-bound callbacks; the
  fix worker must either implement a bounded mitigation or explicitly
  adjudicate the trust-boundary limitation with evidence.
- `2026-07-10T12:38:05Z`: Round 28 fix worker added focused red regressions for
  three behavior seams before production edits: mixed worker loop status
  aggregation, stale paused-loop resume after earlier skipped rows disappear,
  and `deliveryAccess.drain()` falling through to public `Delivery.drain()`
  for non-owned instances. The red `vitest` run failed as expected on those
  three paths before implementation.
- `2026-07-10T12:38:05Z`: Round 28 implementation replaced raw paused
  `scanOffset` reuse with an internal pending-boundary resume cursor validated
  against the current pending set before reuse, preserving forward progress
  without skipping shifted supported rows. `DeliveryWorker` status aggregation
  now prefers `PAUSED` over `SKIPPED`; `deliveryAccess` now fails fast for
  non-owned instances and exposes internal owned-instance override/status
  helpers for package-local tests; the delivery-worker storage fault harness
  moved into `delivery-storage-fault-fixture.ts`; and the loop read-cap check
  now reads through `inboxStorageAccess.maxReadLimit`.
- `2026-07-10T12:38:05Z`: Round 28 documentation pass corrected the broader
  direct-drain contract: skipped worker-unsupported rows such as `CATCH_UP` do
  not consume loop failure budget, but pre-callback
  claim/validation/lease/cleanup/status-update failures do increment `failed`
  and count toward `DeliveryLoop.maxFailures` while leaving `accepted`
  unchanged. Legacy stored `IMPORT_EVENT` rows are now documented as
  fail-closed `DeliveryStorageCorruptionError` aborts before any
  `DeliveryRun` is returned. The durable docs also adjudicate the same-event-
  loop trust boundary: lease renewal uses timers on the same JavaScript event
  loop, so CPU-bound synchronous endpoint callbacks can still starve renewal
  because the framework cannot preempt them in-process.
- `2026-07-10T12:45:07Z`: Coordinator reran the Round 28 verification after
  inspecting the worker diff. Focused delivery/API Vitest passed with 6 files
  and 229 tests. `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` passed; `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning.
- Round 28 fix commit: `0c622787` (`Fix delivery loop resume and worker
status`).
- `2026-07-10T12:53:31Z`: Round 29 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..5e17283b.diff`. TypeScript/API docs and
  security reported clean. Documentation found stale Round 27 summaries still
  saying pre-callback failures do not consume loop failure budget. Style found
  the paused/resume path still too implicit because `DeliveryLoop` infers
  skipped-scan exhaustion from public counters and reads a resume cursor from
  `DeliveryRun` WeakMap metadata, and found the moved storage-fault fixture
  still too broad without scenario-focused helpers. Reliability found mixed
  success/failure drains can preserve a cursor past a failed pending row and
  later report `IDLE` while that row remains retryable. One fix worker will
  address the complete Round 29 batch before another re-review.
- `2026-07-10T13:06:31Z`: Round 29 fix worker added the required red
  regression in `delivery-loop.test.ts` for `maxFailures: 2` with a failed row
  followed by a successful row. The focused `vitest` run failed before
  production edits because the loop observed only `["signal-fails",
"signal-succeeds"]` and went idle instead of retrying the failed head row.
- `2026-07-10T13:06:31Z`: Round 29 implementation moved paused/resume
  semantics fully onto an explicit package-local `DeliveryDrainOutcome`
  contract: `Delivery` now decides whether a resume cursor is safe to keep,
  omits it after any failed run, and reports skipped-scan exhaustion
  explicitly. `DeliveryLoop` now branches on that explicit internal outcome
  instead of inferring resume state from public counters or hidden metadata.
  The delivery storage fault fixture was also finished as scenario-focused
  compare-and-set/read probes so tests arm intentful helpers instead of
  mutating one ambient fault plan.
- `2026-07-10T13:06:31Z`: Round 29 documentation/history correction updated
  `round-27-fix-report.md` and this work log to state the accurate accounting
  contract: only skipped unsupported rows avoid failure-budget consumption;
  pre-callback claim/validation/lease/cleanup/status-update failures still
  increment `failed` / `DeliveryLoop.maxFailures` while leaving `accepted`
  unchanged.
- `2026-07-10T13:10:15Z`: Round 29 verification passed. The focused required
  delivery/API Vitest batch passed with 6 files and 230 tests after the fix,
  and `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` all passed. `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning. No commit was created.
- `2026-07-10T13:14:12Z`: Coordinator inspected the Round 29 diff after the
  fix workers returned, closed both workers, and reran verification. The
  focused delivery/API Vitest batch passed with 6 files and 230 tests;
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` all passed. `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning.
- Round 29 fix commit: `fd563047` (`Fix delivery drain resume outcome`).
- `2026-07-10T13:20:32Z`: Round 30 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..fd563047.diff`. Documentation, security,
  and performance/reliability reported clean. TypeScript/API docs found
  `DeliveryWorkerOptions.maxFailures` should mirror the `DeliveryLoop`
  default/cap wording. Style found the storage fault fixture still starts with
  support/protocol types instead of the scenario probe API. One compact fix
  worker will address both minor findings before another re-review.
- `2026-07-10T13:23:46Z`: Round 30 fix worker updated
  `DeliveryWorkerOptions.maxFailures` TypeDoc to include the `DeliveryLoop`
  default/cap wording and reordered `delivery-storage-fault-fixture.ts` so
  `deliveryStorageFaults()` and named scenario probe helpers precede internal
  support/protocol types and storage wrapper classes. The fixture-local
  `Deferred<T>` helper type is now private, and all existing public test helper
  names were preserved.
- `2026-07-10T13:24:11Z`: Round 30 required focused Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts`; 3 files,
  77 tests.
- `2026-07-10T13:24:11Z`: Round 30 required
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T13:24:11Z`: Round 30 required
  `pnpm --config.verify-deps-before-run=false docs:check` passed with only the
  existing invalid-origin TypeDoc source-link warning.
- `2026-07-10T13:24:11Z`: Round 30 final
  `pnpm --config.verify-deps-before-run=false format:check` passed after
  applying Prettier to the existing Round 29 fix report table and the new Round
  30 fix report.
- `2026-07-10T13:24:11Z`: Round 30 final `git diff --check` passed.
- `2026-07-10T13:28:38Z`: Coordinator inspected the Round 30 diff after the
  fix worker returned, closed the worker, and reran verification. Focused
  delivery worker/loop/runtime Vitest passed with 3 files and 77 tests;
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` all passed. `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning.
- Round 30 fix commit: `8a65e2b6` (`Polish delivery worker docs and fault
fixture`).
- `2026-07-10T13:35:21Z`: Round 31 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..8a65e2b6.diff`. Style, TypeScript/API
  docs, and security reported clean. Documentation found this review log still
  has one unqualified stale Round 27 phrase about pre-callback failures and
  failure-budget consumption, and that Round 30 verification records should
  name fix commit `8a65e2b6`. Reliability found resume cursor validation can
  still miss reachable supported work before the cursor if prefix churn
  preserves the boundary row at `offset - 1`, and found cursor validation
  should happen after shard pickup so live-owned shards return `SKIPPED` before
  boundary reads. One fix worker will address the complete batch before another
  re-review.
- `2026-07-10T13:40:19Z`: Round 31 fix worker completed the required skill
  applicability check in `round-31-fix-report.md`, selected TDD,
  implementation, and verification-before-completion guidance, and recorded the
  explicit no-commit/no-human-review scope.
- `2026-07-10T13:40:19Z`: Round 31 red regression
  `delivery-loop.test.ts -t "rescans before going idle after a resumed zero-work drain"`
  failed before production edits because a resumed run returned `IDLE` with
  `delivered: 0` after a live-claimed supported head row became reachable while
  the saved boundary row remained valid.
- `2026-07-10T13:40:19Z`: Round 31 implementation moved shard pickup before
  internal resume-cursor validation and added one bounded head rescan when a
  resumed cursor reads zero rows after the cursor. The delivery-loop tests now
  cover the missed-row regression and the live-owned-shard `SKIPPED` ordering
  without inbox boundary queries. The review/task logs now also qualify the
  stale Round 27 accounting text and tie Round 30 verification to commit
  `8a65e2b6`.
- `2026-07-10T13:40:19Z`: Round 31 focused red/fix tests passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-loop.test.ts -t "rescans before going idle after a resumed zero-work drain|skips a live-owned shard before validating a resume cursor"`;
  1 file, 2 tests.
- `2026-07-10T13:40:19Z`: Round 31 required focused Vitest passed:
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/index.test.ts`;
  6 files, 232 tests.
- `2026-07-10T13:40:19Z`: Round 31 required
  `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed (`tsc -b`).
- `2026-07-10T13:40:19Z`: Round 31 required
  `pnpm --config.verify-deps-before-run=false docs:check` passed with only the
  existing invalid-origin TypeDoc source-link warning.
- `2026-07-10T13:40:19Z`: Round 31 required
  `pnpm --config.verify-deps-before-run=false format:check` passed.
- `2026-07-10T13:40:19Z`: Round 31 final `git diff --check` passed.
- `2026-07-10T13:44:53Z`: Coordinator inspection found the initial Round 31
  rescan fix only covered zero rows after the saved cursor. A resumed drain
  that processed skipped rows after the cursor could still return
  non-exhausted `IDLE` while reachable supported work remained before the
  cursor. The coordinator will extend the regression and tighten the rescan
  guard before commit.
- `2026-07-10T13:47:56Z`: Coordinator extended the Round 31 regression with a
  skipped tail row after the saved cursor and tightened delivery finalization
  to perform one bounded head rescan before any non-exhausted
  zero-accepted/zero-failed resumed finish. The affected resume tests now
  expect `PAUSED` when remaining unsupported backlog keeps the loop in bounded
  skipped-scan mode after the reachable supported row is delivered.
- `2026-07-10T13:47:56Z`: Coordinator verification passed after the follow-up:
  the focused Round 31 pair passed with 1 file and 2 tests; the required
  focused delivery/API Vitest batch passed with 6 files and 232 tests; and
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` all passed. `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning.
- Round 31 fix commit: `a06e3749` (`Fix delivery resume cursor rescan`).
- `2026-07-10T13:54:16Z`: Round 32 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..a06e3749.diff`. TypeScript/API docs,
  security, and performance/reliability reported clean. Style found the branch
  fails `pnpm --config.verify-deps-before-run=false lint:generated` with 35
  ESLint errors on changed T-0026 files, so lint/verify cannot pass.
  Documentation found Round 31 verification records should name fix commit
  `a06e3749`. The next fix batch will reproduce and fix the lint gate plus the
  durable commit trace before another re-review.
- `2026-07-10T13:57:24Z`: Round 32 lint/documentation fix worker started. The
  required skill applicability check is recorded in
  `build-protocol/tasks/T-0026-transport-backed-delivery-workers/round-32-fix-report.md`.
  The current uncommitted intake diff already ties the Round 31 task/work/
  review logs and Round 31 report to fix commit `a06e3749`
  (`Fix delivery resume cursor rescan`), so the remaining Round 32 work is
  lint-safe cleanup plus fresh verification.
- `2026-07-10T15:05:11Z`: Round 32 lint-safe cleanup resolved the reproduced
  35-error `lint:generated` batch. The code changes were limited to unused
  imports/type parameters, redundant assertion removal, explicit claim-free
  message snapshots, non-`Error` lease renewal normalization before throwing,
  and memory-storage normalized-value typing.
- `2026-07-10T15:05:11Z`: Round 32 verification passed:
  `pnpm --config.verify-deps-before-run=false lint:generated`;
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/sharded-work-registry.test.ts
packages/server/test/index.test.ts
packages/storage/test/memory/in-memory-record-storage.test.ts` with 7 files
  and 248 tests; `typecheck:build:generated`; `docs:check`; `format:check`;
  and `git diff --check`. `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning.
- `2026-07-10T14:08:41Z`: Coordinator inspected the Round 32 lint cleanup and
  reran verification. `lint:generated` passed, the focused delivery/storage
  Vitest batch passed with 7 files and 248 tests, and
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check` all passed. `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning.
- Round 32 fix commit: `a66ab6b5` (`Fix delivery lint gate`).
- `2026-07-10T14:16:16Z`: Round 33 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..a66ab6b5.diff`. TypeScript/API docs,
  security, and performance/reliability reported clean. Documentation found
  Round 32 verification records should name fix commit `a66ab6b5`. Style found
  `format:check` currently fails on the Round 32 fix report table. The next
  fix will add the commit breadcrumb and apply formatting before another
  re-review.
- `2026-07-10T14:17:39Z`: Round 33 fix added the Round 32 fix-commit
  breadcrumb `a66ab6b5` (`Fix delivery lint gate`) and applied Prettier
  formatting to the Round 32 fix report. `format:check`, `git diff --check`,
  and `lint:generated` passed.
- Round 33 fix commit: `8cd57172` (`Record Round 32 fix evidence`).
- `2026-07-10T14:24:06Z`: Round 34 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..8cd57172.diff`. TypeScript/API docs,
  security, and performance/reliability reported clean. Documentation found
  the Round 33 trace still needs commit `8cd57172` and current-status cleanup.
  Style found `typecheck:tooling` fails on delivery test/fixture typing:
  scenario probe helper return annotations erase internal probe capability,
  fixture generic `Message` to `Any` casts are unsafe, and a delivery-loop test
  helper infers `InMemoryStorageFactory` instead of general `StorageFactory`.
  The next fix will address these findings before another re-review.
- `2026-07-10T14:27:09Z`: Round 34 fix worker started. Reproduced
  `pnpm --config.verify-deps-before-run=false typecheck:tooling` before edits;
  it failed with the expected 15 TypeScript errors. Opened
  `round-34-fix-report.md` with the canonical skill applicability check and
  will keep `human-review-1-jul.md` untouched.
- `2026-07-10T14:30:19Z`: Round 34 type-only fix completed. The delivery-loop
  `createDelivery()` helper now accepts `StorageFactory`, delivery storage
  fault probe controls retain the internal probe capability required by
  `deliveryStorageFaults(...probes)`, and the fixture's known inbox `Any`
  record reads use one explicit `unknown` bridge. Round 33 trace records now
  name fix commit `8cd57172` (`Record Round 32 fix evidence`), and the current
  review status/table records the Round 34 findings as fixed pending
  re-review.
- `2026-07-10T14:30:19Z`: Round 34 verification passed:
  `typecheck:tooling`, `lint:generated`, the requested focused
  delivery/storage Vitest batch with 7 files and 248 tests, `docs:check`,
  `format:check`, and `git diff --check`. `docs:check` retained only the
  existing invalid-origin TypeDoc source-link warning.
- `2026-07-10T14:34:08Z`: Coordinator inspected the Round 34 fix and reran
  verification. `typecheck:tooling`, `lint:generated`, the focused
  delivery/storage Vitest batch with 7 files and 248 tests, `docs:check`,
  `format:check`, and `git diff --check` all passed. `docs:check` retained
  only the existing invalid-origin TypeDoc source-link warning.
- Round 34 fix commit: `7a5378eb` (`Fix delivery tooling typecheck`).
- `2026-07-10T14:47:00Z`: Round 35 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..7a5378eb.diff`. TypeScript/API docs
  reported clean. Documentation found Round 34 verification records should
  name fix commit `7a5378eb`. Style found `format:check` currently fails on
  `round-34-fix-report.md`. Security found expired row-claim reclaim can
  double-invoke endpoint callbacks while the earlier owner is still inside
  `onMessage`. Performance/reliability found absolute offset paging can skip a
  reachable supported row when skipped head rows disappear mid-drain and
  falsely stop idle. The next fix will update the durable Round 34 trace,
  format the report, block any already-claimed row until explicit recovery
  policy exists, fix moving-set pagination, verify, and repeat five-lane
  re-review.
- `2026-07-10T15:48:50Z`: Round 35 fix verified. Red regressions first failed
  for expired-claim reclaim and moving-set pagination, then passed after
  `InboxStorage` blocked every existing row claim and `Delivery` added one
  boundary-validated head rescan before stale offset-page reads. The fix also
  updated reclaim-era tests to require explicit claim recovery, recorded Round
  34 fix commit `7a5378eb` (`Fix delivery tooling typecheck`), formatted the
  Round 34 report, and created `round-35-fix-report.md`. Verification passed:
  focused delivery Vitest with 5 files and 223 tests,
  `typecheck:build:generated`, `docs:check`, `format:check`, and
  `git diff --check`. `docs:check` retained only the existing invalid-origin
  TypeDoc source-link warning. No commit was created by the fix worker; the
  coordinator later committed this as `5c3705e2` (`Fix delivery claim blocking
and offset rescan`).
- `2026-07-10T15:54:00Z`: Coordinator refined the Round 35 fix before commit:
  pending-boundary validation now happens before all offset-page reads so both
  empty and non-empty shifted pages reset once, and public docs plus
  `InboxClaim` comments then stated that expired and live per-message ownership
  both blocked competing delivery until future explicit recovery policy
  existed. Round 43 later restored expired-claim reclaim during claim CAS while
  live claims still block. Coordinator verification passed: the two Round 35
  regressions; focused delivery Vitest with 5 files and 223 tests;
  `typecheck:build:generated`; `docs:check`; `format:check`; and
  `git diff --check`. `docs:check` retained only the existing invalid-origin
  TypeDoc source-link warning.
- Round 35 fix commit: `5c3705e2` (`Fix delivery claim blocking and offset
rescan`).
- `2026-07-10T16:05:00Z`: Round 36 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..5c3705e2.diff`. Documentation,
  TypeScript/API docs, security, and performance/reliability reported clean.
  Style found `format:check` fails on `round-35-fix-report.md` and `lint`
  fails on unused `_claim` destructuring in delivery test helpers plus a
  `let delivery` local that can be `const`. The next fix will format the
  report, remove the unused bindings, tighten the test local, verify, and
  repeat five-lane re-review.
- `2026-07-10T16:08:00Z`: Round 36 style/lint fix completed. The Round 35
  report and T-0026 logs are formatted, ignored claim destructuring in delivery
  test helpers was replaced with explicit claim-free message snapshots, and
  the moving-page regression uses `const` for its delivery instance.
  Verification passed: `lint`, `format:check`, focused delivery Vitest with 5
  files and 223 tests, and `git diff --check`. Coordinator commit `e4388fb5`
  (`Fix delivery review gate cleanup`) recorded this fix.
- `2026-07-10T16:12:00Z`: Round 37 fix worker started and read the required
  protocol, task, work log, review log, `delivery.ts`, and relevant delivery
  loop/worker tests. Added `round-37-fix-report.md`, updated Round 35/36 commit
  breadcrumbs, and marked Round 24/25 expired-claim reclaim wording as
  historical. Added a focused regression for skipped head rows disappearing
  after boundary validation but before offset-page read; it failed before the
  fix with `status: IDLE` and `delivered: 0`.
- `2026-07-10T16:12:45Z`: Round 37 reliability fix updated
  `Delivery.#drainAvailableMessages()` to revalidate the pending boundary after
  a short offset-page read with no accepted or failed work and rescan the head
  once when the boundary moved. The focused regression passed after the fix.
- `2026-07-10T16:17:00Z`: Round 37 final verification passed. Focused delivery
  worker/loop/runtime/inbox/shard-registry Vitest passed with 5 files and 224
  tests; `typecheck:build:generated`, `docs:check`, `lint`, `format:check`,
  and `git diff --check` passed. `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning. No commit was created, per
  instruction.
- `2026-07-10T16:22:00Z`: Coordinator inspected the Round 37 fix and reran
  verification. The focused boundary/read race regression passed, the focused
  delivery batch passed with 5 files and 224 tests, and
  `typecheck:build:generated`, `docs:check`, `lint`, `format:check`, and
  `git diff --check` all passed. `docs:check` retained only the existing
  invalid-origin TypeDoc source-link warning.
- Round 37 fix commit: `1403505e` (`Fix delivery offset boundary race`).
- `2026-07-10T16:31:00Z`: Round 38 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..1403505e.diff`. TypeScript/API docs,
  security, and performance/reliability reported clean. Style and documentation
  found the Round 37 report still said no commit was created even though
  `1403505e` now records the fix. Documentation also found older Round 29 and
  Round 30 reports still said coordinator commits were pending. This records
  cleanup only; runtime behavior is unchanged.
- `2026-07-10T16:39:00Z`: Round 38 commit-note cleanup verified and committed
  as `7927c4d3` (`Record delivery review breadcrumbs`). `format:check` and
  `git diff --check` passed before commit.
- `2026-07-10T16:40:00Z`: Round 39 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..7927c4d3.diff`. TypeScript/API docs,
  security, and performance/reliability reported clean. Style and documentation
  found only durable status cleanup: review-log status/table still showed
  Round 38 findings pending, the work-log summary still said Round 38 fix
  pending, and the Round 37 report's coordinator breadcrumb should explicitly
  name `1403505e`.
- `2026-07-10T16:45:00Z`: Round 39 records-only cleanup verified and committed
  as `faa2d814` (`Record delivery round 39 review status`). `format:check` and
  `git diff --check` passed before commit.
- `2026-07-10T16:48:00Z`: Round 40 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..faa2d814.diff`. TypeScript/API docs and
  security reported clean. Style and documentation found Round 39 status
  records still described the `faa2d814` cleanup as future/pending.
  Performance/reliability found offset pagination can still skip a deliverable
  row if the skipped head set disappears after boundary validation and the
  stale offset page remains full. The fix updates status records, adds a
  regression for the full stale offset-page race, repairs the rescan path,
  verifies, and repeats five-lane re-review.
- `2026-07-10T16:49:00Z`: Round 40 fix worker recorded the canonical skill
  applicability check in `round-40-fix-report.md`, read the required TDD
  guidance and local JVM delivery research note, then added the focused stale
  full-offset-page regression before production edits. The red Vitest command
  failed with `PAUSED` and zero deliveries; after the production change, the
  same command passed and delivered the shifted supported row during the same
  loop run. Delivery now revalidates the offset boundary after reading each
  offset page, discards a page read from a moved pending set, and resets once
  to the head without exceeding the existing scan budget. The bounded paging
  test now expects four inbox queries for the intentional post-read check.
- `2026-07-10T16:49:00Z`: Round 40 verification passed: focused delivery
  worker/loop/inbox Vitest with 3 files and 178 tests;
  `typecheck:build:generated`, `docs:check`, `lint`, and `format:check`.
  `docs:check` retained only the existing invalid-origin TypeDoc source-link
  warning. The worker created no commit. Coordinator commit `9c51b77a` (`Fix
delivery stale offset page rescan`) recorded the verified fix; five-lane
  re-review remains pending.
- `2026-07-10T17:05:00Z`: Round 41 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..9e831767.diff`. Style, documentation,
  TypeScript/API docs, and performance/reliability found that
  `round-40-fix-report.md` contains Markdown hard-break trailing spaces, so
  `git diff --check ca8fb2b3..HEAD` fails even though the report says the diff
  check passed. Security found the root-exported `DeliveryWorker` exposes an
  arbitrary raw inbox callback boundary that bypasses framework replay
  validation. Performance/reliability also found partial stale-head rescans can
  lose bounded page behavior by re-reading already-seen skipped rows one at a
  time. The next fix will remove the range whitespace, repair the public worker
  callback boundary, preserve bounded page reads for partial stale-head rescans,
  add focused regression coverage, verify, and repeat five-lane re-review.
- `2026-07-10T17:12:00Z`: Round 41 fix worker removed the root-barrel
  `DeliveryWorker` callback boundary, updated API export checks and public docs,
  removed the Round 40 report's two spaces, and added a partial stale-head
  query-count regression. The regression was red at `1004` inbox queries and
  green at `5` while preserving `processed: 1001`. The prescribed five-file
  delivery Vitest command passed with 194 tests; generated typechecking,
  docs/API checks, lint, formatting, and working-tree `git diff --check`
  passed. `docs:check` retained only the existing invalid-origin TypeDoc
  source-link warning. `git diff --check ca8fb2b3..HEAD` was rerun after
  coordinator commits `2a673e42` and `d7c9b35e` and now passes; the Round 41
  range-check follow-up is resolved. No worker commit was created. Coordinator
  commit `2a673e42` (`Fix delivery worker API and rescan paging`) recorded the
  fix.
- `2026-07-10T17:25:00Z`: Round 42 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..d7c9b35e.diff`. Performance/reliability
  reported clean. Documentation found that Round 41 records still say
  `git diff --check ca8fb2b3..HEAD` remains red even though it now passes after
  coordinator commit `2a673e42`. Style found a work-log continuation line for
  that same commit breadcrumb was flush-left. TypeScript/API docs and security
  found that removing only `DeliveryWorker` did not close the raw callback
  boundary because root-exported `Delivery`, `DeliveryLoop`, and
  `OnDeliveryMessage` still expose public `onMessage` callbacks. The next fix
  will remove or close the remaining root raw-callback delivery surface, update
  docs/API export checks, correct Round 41 range-check records, verify, and
  repeat five-lane re-review.
- `2026-07-10T17:35:00Z`: Round 42 fix worker added
  `round-42-fix-report.md`, completed the canonical skill applicability check,
  and read the local JVM delivery-model notes. The root barrel no longer exports
  direct raw-callback delivery classes/types; behavior tests now import them
  from package-internal source paths. Public delivery docs and API export checks
  describe only the remaining inbox/storage surface and framework-owned replay.
  The Round 41 range records now mark `git diff --check ca8fb2b3..HEAD` passed,
  and the coordinator-commit continuation is formatted. Red/green root export
  evidence, the five-file delivery suite with 194 tests, generated typecheck,
  docs/API check, lint, format, range diff, and working-tree diff all passed.
  `docs:check` retained only the existing invalid-origin TypeDoc source-link
  warning. No worker commit was created. Coordinator commit `be299a5d` (`Close
delivery raw callback exports`) recorded the fix; five-lane re-review remains
  pending.
- `2026-07-10T17:45:00Z`: Round 43 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..59c44c44.diff`. All five lanes returned
  findings. Style, documentation, TypeScript/API docs, security, and
  performance/reliability found the active handoff/review contract expects
  expired per-message claims to be reclaimable during claim CAS while live
  claims block, but current code, tests, and docs still treat every existing
  row claim as unavailable. Style, documentation, and TypeScript/API docs also
  found `build-protocol/DEVELOPER_API.md` still presents raw callback delivery
  classes, `onMessage`, and direct-drain types as current public API after
  Round 42 removed them from the root barrel. TypeScript/API docs additionally
  found the root export-surface test still imports and type-checks internal raw
  callback delivery types. The next fix will restore expired-claim CAS reclaim,
  keep live claims blocking, update the opposite tests/docs, move internal raw
  callback type assertions out of the root export test, correct
  `DEVELOPER_API.md`, verify, and repeat five-lane re-review.
- `2026-07-10T18:02:00Z`: Round 43 fix worker started in the existing
  T-0026 worktree. Required inputs read before code edits:
  `BUILD_PROTOCOL.md`, `CODE_QUALITY.md`, `DEVELOPER_API.md`,
  `RUNTIME_ARCHITECTURE.md`, the T-0026 task, work log, review log, Round 42
  fix report, and `/Users/armiol/.agents/skills/test-driven-development/SKILL.md`.
  Canonical skill applicability check was refreshed: the session inventory
  exposed task-relevant workflow, testing, TypeScript, backend, docs, security,
  review, and verification skills; the task explicitly provided the TDD skill
  path; `build-protocol/skills/EXPECTED_SKILLS.md` was read; the installed
  skill entrypoint search over `/Users/armiol/.agents/skills` succeeded; and
  `/Users/armiol/.agents/.skill-lock.json` was readable. Selected skill:
  `test-driven-development`, fully read before behavior edits. Relevant-looking
  skills skipped for this narrow fix: `javascript-testing-patterns`,
  `typescript-advanced-types`, and `nodejs-backend-patterns`; no new test
  framework, advanced type design, or Node lifecycle boundary is being added.
  Existing uncommitted Round 43 intake edits in task/work/review logs are
  preserved; no commit will be created by this worker.
- `2026-07-10T18:10:00Z`: Round 43 red/green claim fix completed. Focused
  reclaim tests first failed because endpoint callbacks were not invoked for
  an expired row claim or for a row claim that expired while the claim-row read
  was pending. `InboxStorage` now uses its storage clock during claim CAS:
  live row claims remain unavailable, while expired row claims may be replaced
  by the active worker claim. The same focused reclaim command then passed.
- `2026-07-10T18:18:00Z`: Round 43 public-boundary/docs fix completed. The
  root export-surface test no longer imports internal raw callback delivery
  types; the direct-drain option/callback type assertions remain in
  `delivery-worker.test.ts`. `DEVELOPER_API.md` now describes root-public
  delivery as durable inbox/storage primitives with package-internal validated
  replay, and the known docs now say live per-message ownership blocks while
  expired ownership may be replaced during claim CAS using the storage clock.
  Focused delivery/index Vitest passed with 4 files and 189 tests.
- `2026-07-10T18:28:00Z`: Round 43 verification before final format/diff
  checks passed: `typecheck:build:generated`, `docs:check` with only the
  existing invalid-`origin` TypeDoc warning, and `lint`. The first
  `format:check` reported wrapping needed in T-0026 durable logs; the formatter
  was run. Round 43 report was added before the final format and diff checks
  recorded below. No worker commit was created; coordinator commit remains
  pending.
- `2026-07-10T18:34:00Z`: Round 43 final verification passed after report/log
  formatting: `pnpm --config.verify-deps-before-run=false format:check`,
  `git diff --check`, and `git diff --check ca8fb2b3..HEAD`. No worker commit
  was created. Coordinator commit `9477830c` (`Fix delivery expired claim
reclaim`) recorded the fix; five-lane re-review remains pending.
- `2026-07-10T18:36:00Z`: Coordinator review of the Round 43 patch found one
  durable-log command wrap and repaired it without changing runtime behavior.
  Coordinator verification then passed: focused delivery/index Vitest with 4
  files and 189 tests, `typecheck:build:generated`, `docs:check` with only the
  existing invalid-`origin` warning, `lint`, `format:check`, `git diff --check`,
  and `git diff --check ca8fb2b3..HEAD`. Coordinator commit `9477830c` recorded
  the fix; five-lane re-review remains pending.
- `2026-07-10T18:45:00Z`: Round 44 re-review intake completed against
  `.superpowers/sdd/review-ca8fb2b3..f7f56f54.diff`. Performance/reliability
  reported clean. Documentation and TypeScript/API docs found
  `build-protocol/DEVELOPER_API.md` still names internal
  `DeliveryOptions.leaseMs` in the public delivery error-contract sentence.
  Security found projection inbox replay validates the label but not
  `message.status` before crossing into repository/user projection code. Style
  found older durable records still contain authoritative-looking current
  contract wording from Round 35 that omits the Round 43 / `9477830c`
  supersession. The next fix will correct the public API doc sentence, add a
  projection replay status guard with focused coverage, update stale durable
  records, verify, and repeat five-lane re-review.
- `2026-07-10T18:58:00Z`: Round 44 fix worker implemented the complete batch.
  Added focused red/green coverage for projection replay snapshots with
  `DELIVERED`, `SCHEDULED`, and `TO_CATCH_UP` status, then added the
  fail-closed `TO_DELIVER` guard before projection target invocation. Removed
  the internal `DeliveryOptions.leaseMs` public-doc mention, updated stale
  Round 24/25/37 durable reclaim records to name Round 43 / `9477830c` as the
  later expired-claim reclaim supersession, and added
  `round-44-fix-report.md`. Verification passed with focused context handoff
  Vitest (2 files, 22 tests), generated build typecheck, docs check with only
  the existing invalid-`origin` TypeDoc warning, lint, final format check, and
  both working-tree and baseline-range diff checks. No worker commit was
  created. Coordinator commit `9bb68f33` (`Fix projection replay status guard`)
  recorded the fix; five-lane re-review remains pending.
- `2026-07-10T19:05:00Z`: Coordinator verification of the Round 44 patch
  passed: focused context handoff Vitest with 2 files and 22 tests,
  `typecheck:build:generated`, `docs:check` with only the existing
  invalid-`origin` warning, `lint`, `format:check`, `git diff --check`, and
  `git diff --check ca8fb2b3..HEAD`. Coordinator commit `9bb68f33` recorded the
  fix; five-lane re-review remains pending.

## Handoff

- `2026-07-10`: Prepared durable chat handoff at
  `build-protocol/handoffs/2026-07-10-t0026-round24-handoff.md` after the Round
  24 code commit `4f397c9b`. No reviewers were required for the handoff-only
  commit. The next coordinator should generate a fresh review package from
  `ca8fb2b3` to `HEAD`, run the five required T-0026 reviewer lanes, close
  every reviewer, fix any findings through the protocol loop, and only then run
  final verification and merge T-0026 into `main`.
