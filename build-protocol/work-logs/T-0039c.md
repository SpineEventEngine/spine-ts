# T-0039c Work Log

Status: Accepted — integration pending

## Start

- Started `2026-07-14T13:03:12Z` from pushed `main` baseline `aaa31116` after
  T-0039b remote closure and clean worktree removal.
- Branch/worktree: `task/T-0039c-framework-user-guide` at
  `.worktrees/T-0039c-framework-user-guide`.
- Offline frozen install reused the lockfile/store with no dependency or
  lockfile change.
- Baseline evidence is T-0039b post-merge native full verify: ordinary and
  coverage `71` files / `1,642` tests, coverage
  `95.38/90.12/98.22/95.4`, and all static/docs/API/Proto/generated gates green.

## Milestone

- Replace the historical implementation inventory with the completion plan's
  supported end-user journey.
- Keep example-specific multi-process behavior/docs in T-0040 and make no
  runtime, export, Proto, generated, package, dependency, or policy change.

## Inventory

- `docs/USER_GUIDE.md` is 1,340 lines and currently mixes “What Exists Now,”
  “Remaining Gaps,” and implementation-seam chapters with user guidance.
- Stale active candidates include manual `createEntityTransaction()` guidance,
  relative generated imports, `packCommand()` client guidance, “later runtime”
  wording, future retry-policy claims, open-production-gap claims, duplicated
  delivery text, and pre-final server close ordering.
- Required replacement is an ordered install/model/handler/generation/assembly/
  lifecycle/client/testing/guarantee journey using public APIs only.

## Assignment

- Existing implementer, explicit immutable `gpt-5.6-terra` / medium, no
  subagents.
- Author owns only the guide and these records, plus a focused guide assertion
  only if a concrete regression need is first recorded.
- Coordinator owns pre-review lint, commits, review waves, final gates,
  integration, push, and cleanup.

## Author Handback

- Replaced the 1,340-line historical inventory with the ordered install,
  model, handlers, registry, assembly, lifecycle, command, query/subscription,
  refusal, testing, and delivery/limits journey.
- The guide uses package imports only. Every `@example/*` import is a named
  consumer substitution; practical fragments list their compiled-package or
  fixture prerequisites. No guide assertion changed: no concrete regression
  need was found.
- Factual sources: root/package READMEs, `docs/api/README.md`,
  `DEVELOPER_API.md`, `RUNTIME_ARCHITECTURE.md`, public exports, and current
  server lifecycle, environment, bounded-context, handler, testing, and
  transport implementations.
- Focused validation completed: exact-path `pnpm exec prettier --check`;
  `pnpm typecheck:build` (required to create workspace declarations in this
  fresh worktree); `pnpm docs:check`; guide link, command/import/end-user/
  future-policy/fixture scans; status equality; prohibited-scope/generated-clean
  checks; and `git diff --check`. All passed. Full `pnpm verify` is
  intentionally final-only and was not run.
- Remaining uncertainty: consumer projects must wire their own Protobuf-ES and
  registry-generation command around their build layout; the guide identifies
  required inputs but does not invent a package CLI. T-0040 still owns any
  multi-process example behavior.
- Skills used: doc-coauthoring (existing-doc structure and reader-facing
  reconciliation) and verification-before-completion (fresh evidence gate).
  Immutable author profile actually used: implementer, `gpt-5.6-terra` /
  medium; no subagents.

## Coordinator Pre-Review Findings

- `2026-07-14T13:15:07Z`: exact changed-scope, status-equality, prohibited API,
  schema-decorator, future-policy, generated-clean, and diff-integrity scans
  passed. The future-policy scan's only match was the method name
  `readEventually`, not policy prose.
- Accepted guide finding: the declared `Task` aggregate contract does not match
  the `TaskStateSchema`/`string` aggregate fragment, and the additional
  process-manager/projection schemas are not modeled. The same author must make
  the example vocabulary and generics coherent and keep every substitution
  explicit.
- Accepted status finding: the completion plan's top status, active frontier,
  and current-execution statement still name T-0039a. The same author may edit
  only those three statements to identify T-0039c's current pre-review-fix
  state.
- Fix assignment: existing implementer, explicit immutable
  `gpt-5.6-terra` / medium, no subagents. Expanded write scope is limited to
  `docs/USER_GUIDE.md`, the three T-0039c records, and the three stale plan
  status statements. Reviewer dispatch remains blocked until focused checks
  and a renewed coordinator lint are clean.

## Pre-Review Fix Implementation

- Made the guide's Protobuf model the source of every generated schema/type
  imported by its handler example. Entity ID generics now use generated
  `TaskId`; state schema names now correspond directly to modeled `Task`,
  `TaskWorkflow`, and `TaskList` messages.
- Corrected the process-manager producer flow so `@React` emits a generated
  event and `@Command` consumes that event and emits a generated command.
- Changed only the completion plan's top status, active frontier, and current
  execution statement, preserving dated plan-creation context and task packets.
- Changed paths are exactly `docs/USER_GUIDE.md`, the three T-0039c records,
  and `build-protocol/PROJECT_COMPLETION_PLAN.md`; the plan diff contains only
  its three assigned current-status statements.

## Pre-Review Fix Handback

- Exact-path formatting: the first
  `pnpm --config.verify-deps-before-run=false exec prettier --check` identified
  `docs/USER_GUIDE.md`; exact-path `prettier --write` formatted that file, and
  the final exact-path five-file `prettier --check` passed.
- Focused status/model/link/end-user/future-policy scan passed: all three record
  statuses matched, all three current-frontier statements identified T-0039c,
  every handler message/schema/ID had a matching section 2 declaration, all
  five relative links resolved, and no prohibited or speculative policy token
  was found.
- `pnpm --config.verify-deps-before-run=false docs:check` passed: copied-source
  checksums verified and TypeDoc/API checks found the expected `100` proto,
  `28` core, `205` server, `19` storage, `17` transport, and `3` testing
  exports.
- `pnpm --config.verify-deps-before-run=false proto:check-generated` passed;
  generated outputs are ignored, untracked, and freshly regenerated.
- `git diff --check`, changed-path inspection, and `git status --short` passed
  the assigned integrity/scope gate with only the five authorized paths
  modified. Full `pnpm verify` was not run because it remains final-only.
- Remaining uncertainty: the illustrative consumer package cannot resolve in
  this workspace, so coherence is established by exact modeled/generated
  identifier and public-package scans rather than compiling that hypothetical
  package. Consumers still own their Protobuf-ES and handler-registry build
  layout. T-0040 remains the owner of multi-process example behavior.
- Skills used: doc-coauthoring and verification-before-completion. Actual
  immutable profile: existing implementer, `gpt-5.6-terra` / medium; no
  subagents.

## Second Coordinator Pre-Review Finding

- `2026-07-14T13:22:15Z`: schema/type coherence is corrected, but the practical
  handler fragment still never calls the public protected draft-state update
  seam. This leaves the modeled aggregate, process manager, and projection at
  default state and omits the state-mutation step needed before later query and
  subscription guidance is useful.
- Complete bounded correction assigned back to the same existing implementer,
  explicit immutable `gpt-5.6-terra` / medium, no subagents. Write scope remains
  the guide and three T-0039c records; the completion-plan status changes from
  the first fix require no further edit.

## Second Pre-Review Fix Handback

- The guide now calls `updateDraftState()` for all three entity families,
  clones the framework-provided routed `this.id`, and explains that accepted
  framework work commits the draft while application code never opens,
  commits, or rolls back transactions itself.
- Exact-path format, model/state-seam, identity, manual-transaction,
  target-extraction, internal import, envelope/decorator, future-policy,
  status, docs/API, generated-clean, scope, and diff checks passed. Actual
  immutable author profile remained implementer, `gpt-5.6-terra` / medium; no
  subagents.

## Coordinator Pre-Review Closure

- Fresh coordinator exact-path Prettier, `docs:check`,
  `proto:check-generated`, status-equality, prohibited API, schema-decorator,
  future-policy, generated-output, changed-scope, and `git diff --check` gates
  passed. TypeDoc/API counts are `100` proto, `28` core, `205` server, `19`
  storage, `17` transport, and `3` testing exports.
- No constant/helper duplication or public API/export change applies to this
  docs-only diff. No future-policy overclaim was found; `readEventually` is a
  current public fixture method. The five authorized paths are ready for one
  immutable baseline-to-endpoint review package.

## Review Wave 1 Assignment

- `2026-07-14T13:29:10Z`: committed implementation endpoint
  `66a491e12972f942190cb1fd1ab83d4a89babf88` (`Complete T-0039c framework user
guide`). Worktree was clean immediately after commit.
- Generated literal package
  `.superpowers/sdd/review-aaa31116..66a491e1.diff`: baseline `aaa31116`,
  endpoint `66a491e12972f942190cb1fd1ab83d4a89babf88`, four commits, `125348` bytes.
- Four concurrent read-only/no-subagent assignments: style/API/reliability
  existing reviewers each explicit immutable `gpt-5.6-terra` / high;
  documentation existing reviewer explicit immutable `gpt-5.6-luna` / medium.
  Each receives only its recorded concern, exact ledger/current exclusions,
  current public evidence, and the historical-superseded-text rule.

## Review Wave 1 Result

- `2026-07-14T13:35:41Z`: complete wave collected before any fix. Reviewers:
  style `019f60d2-56b4-7851-8605-72056b591ce3`, documentation
  `019f60d2-5a9e-7a51-800c-310972b9955d`, TypeScript/API
  `019f60d2-613d-7470-a11c-6cd17f804896`, reliability
  `019f60d2-5df5-7932-aa3e-6de9375c4c64`. All are closed and used no
  subagents.
- Style was CLEAN at actual immutable `gpt-5.6-terra` / high. Documentation
  was CLEAN at actual immutable `gpt-5.6-luna` / medium. API and reliability
  ran at actual immutable `gpt-5.6-terra` / high. Exact actual profiles are
  established by the execution surface's immutable role metadata and explicit
  dispatch fields; generic or unavailable child self-identification is not
  treated as runtime metadata.
- Accepted P2 batch: label/separate state, command, and event Proto files per
  mandatory suffix conventions; document ordered query limit maximum `1000`;
  document 30-second inactive-subscription TTL plus activation consumption and
  100-update process-local active-queue close/discard behavior; state that
  pending failed delivery has no automatic scheduler/monitor and is not an
  autonomous eventual-delivery guarantee.
- Evidence check: `TECHNICAL_SPEC.md:49` and `PROTOBUF_CONTRACT.md` require the
  file suffixes; `spine-services.ts` owns `MAX_QUERY_LIMIT = 1000`,
  `DEFAULT_INACTIVE_TTL_MS = 30_000`, and `DEFAULT_QUEUE_LIMIT = 100` plus
  queue closure; current delivery docs explicitly exclude immediate retry,
  retry monitors/workers, and scheduler ownership.
- Same existing implementer assigned the complete batch, explicit immutable
  `gpt-5.6-terra` / medium, no subagents. Fix scope remains guide plus three
  T-0039c records; no runtime/Proto source/API/example/plan change.

## Review Wave 2 Assignment

- `2026-07-14T13:42:18Z`: Wave 1 fix endpoint
  `03d47b93e663c5cc15a2aedfbd336497de0160d5` committed as `Fix T-0039c review
findings`; worktree was clean immediately after commit.
- Fresh coordinator exact-path Prettier, `docs:check`, generated-clean,
  Proto-file/model/limit/subscription/delivery wording, prohibited API,
  future-policy, status, scope, and `git diff --check` gates passed.
- New literal package
  `.superpowers/sdd/review-aaa31116..03d47b93.diff`: baseline `aaa31116`,
  endpoint `03d47b93e663c5cc15a2aedfbd336497de0160d5`, seven commits, `139142` bytes.
- Four concurrent read-only/no-subagent assignments rerun: style/API/reliability
  existing explicit immutable `gpt-5.6-terra` / high; documentation existing
  explicit immutable `gpt-5.6-luna` / medium. Each is bounded to its recorded
  concern, current ledger/exclusions, and the Wave 1 fixes.

## Second Pre-Review Fix Implementation

- Aggregate creation now writes `Task` draft state before returning
  `TaskCreated`; the process manager writes `TaskWorkflow` state in both its
  event-producing and command-producing handlers; and the projection writes a
  queryable/subscribable `TaskList` row.
- All three families clone the framework-provided `this.id` into generated
  state/return messages. They call only protected `updateDraftState()` and
  leave transaction admission, validation, completion, storage, and default
  routing to the framework.
- `PROJECT_COMPLETION_PLAN.md` received no second-fix edit.

## Second Pre-Review Fix Handback

- Changed paths for the second fix are exactly `docs/USER_GUIDE.md` and the
  three T-0039c records. Changed-path inspection also shows the preserved
  `PROJECT_COMPLETION_PLAN.md` diff from the first fix; direct diff inspection
  confirmed it still contains only those prior three status statements.
- Exact-path
  `pnpm --config.verify-deps-before-run=false exec prettier --check` passed for
  the guide and three records.
- The focused section-3 state/model/API scan found four
  `this.updateDraftState()` calls and four
  `clone(TaskIdSchema, this.id)` uses across `TaskSchema`,
  `TaskWorkflowSchema`, and `TaskListSchema`. Bare-decorator, public-import,
  transaction-ownership, target-routing, end-user prohibition, future-policy,
  and status checks passed.
- `pnpm --config.verify-deps-before-run=false docs:check` passed: copied-source
  checksums verified and TypeDoc/API checks found the expected `100` proto,
  `28` core, `205` server, `19` storage, `17` transport, and `3` testing
  exports.
- `pnpm --config.verify-deps-before-run=false proto:check-generated` passed;
  generated outputs are ignored, untracked, and freshly regenerated.
- `git diff --check`, `git diff --name-only`, and `git status --short` passed
  the integrity/scope inspection. Full `pnpm verify` was not run because it
  remains final-only.
- Remaining uncertainty: the consumer package is intentionally illustrative
  and does not resolve in this workspace, so its snippet is verified through
  exact modeled-schema, public-API, state-update, and prohibition scans rather
  than compilation. Consumers retain responsibility for their generated
  package/build layout.
- Skills used: doc-coauthoring and verification-before-completion. Actual
  immutable profile: existing implementer, `gpt-5.6-terra` / medium; no
  subagents.

## Review Wave 1 Fix Implementation

- Reframed section 2 as three explicit Proto files while preserving the exact
  generated vocabulary consumed by section 3. Command/event files import the
  state file and Spine options and independently declare syntax, package, and
  type URL prefix.
- Documented ordered query limits as positive values capped at `1000`.
- Documented the 30-second inactive TTL, activation consumption, absence of
  pre-activation replay, process-local active stream/queue, default 100-update
  cap, and close/discard behavior on overflow.
- Documented that a failed supported delivery callback may remain pending and
  that no automatic scheduler/monitor makes durable handoff autonomous
  eventual delivery.
- Focused checks passed before handback.

## Review Wave 1 Fix Handback

- Changed paths are exactly `docs/USER_GUIDE.md`,
  `build-protocol/tasks/T-0039c-framework-user-guide/TASK.md`,
  `build-protocol/work-logs/T-0039c.md`, and
  `build-protocol/reviews/T-0039c-framework-user-guide.md`.
- Exact-path
  `pnpm --config.verify-deps-before-run=false exec prettier --check` passed for
  all four changed files.
- The focused Proto scan passed for `task_state.proto`,
  `task_commands.proto`, and `task_events.proto`: each source unit declares
  `proto3`, `acme.tasks.v1`, `type.acme.tasks`, required imports, and its exact
  modeled messages. The first combined wording scan stopped on a Markdown line
  wrap between “Activation” and “consumes”; its whitespace-tolerant rerun
  passed without a content change.
- Focused wording checks confirmed ordered limits `1..1000`, inactive default
  TTL 30 seconds and activation consumption, no pre-activation replay,
  process-local active streams/queues, default cap 100, overflow close/discard,
  no active recovery/replay, and no automatic scheduler/monitor or autonomous
  eventual-delivery guarantee after a supported callback failure.
- End-user API/import prohibitions, future-policy wording, and three-record
  status equality scans passed.
- `pnpm --config.verify-deps-before-run=false docs:check` passed: copied-source
  checksums verified and TypeDoc/API checks found the expected `100` proto,
  `28` core, `205` server, `19` storage, `17` transport, and `3` testing
  exports.
- `pnpm --config.verify-deps-before-run=false proto:check-generated` passed;
  generated outputs are ignored, untracked, and freshly regenerated.
- `git diff --check`, `git diff --name-only`, and `git status --short` passed
  with exactly the four authorized paths modified. Full `pnpm verify` was not
  run because it remains final-only.
- Remaining uncertainty: the illustrative consumer Proto/package cannot be
  compiled in this workspace, so the three displayed source units are checked
  structurally for filenames, declarations, imports, and vocabulary. No runtime
  behavior changed, and no runtime tests were warranted.
- Skills used: doc-coauthoring and verification-before-completion. Actual
  immutable profile: existing implementer, `gpt-5.6-terra` / medium; no
  subagents.

## Review Wave 2 Result

- `2026-07-14T13:47:23Z`: complete wave collected before fixes. Reviewers:
  style `019f60de-6e1b-7443-84ee-471c15b0e620`, documentation
  `019f60de-724a-7551-a6d5-d83e73ed80f1`, TypeScript/API
  `019f60de-75d5-7293-8c9c-608e6b783e95`, reliability
  `019f60de-79b5-75e0-874d-a6e9faaec566`. All are closed and used no
  subagents.
- Style and documentation were CLEAN. API accepted one P2 for describing
  cancellation as an ID-only call instead of passing the returned
  `Subscription` message. Reliability accepted two P2 findings: ambiguous
  durable projection-subscription wording and incomplete ZeroMQ retry/restart
  limitations.
- Actual immutable profiles matched explicit dispatch and execution-surface
  role metadata: documentation `gpt-5.6-luna` / medium; all other reviewers
  `gpt-5.6-terra` / high.
- Evidence: current Connect tests call `subscriptions.cancel(subscription)`;
  package docs distinguish durable projection handler delivery from
  process-local client streams; `packages/transport/README.md` excludes
  transport-owned retry loops and exactly-once, retry, restart, durable
  redelivery, and remote-delivery guarantees.
- Same existing implementer assigned the complete three-item batch, explicit
  immutable `gpt-5.6-terra` / medium, no subagents. Scope remains the guide plus
  three T-0039c records only.

## Review Wave 2 Fix Implementation

- `2026-07-14T13:53:40Z`: evaluated the complete accepted batch against current
  server tests, server/API docs, and `packages/transport/README.md` before
  editing.
- Section 8 now states that `Cancel` accepts the returned `Subscription`
  message rather than an ID alone. Its illustrative client cleanup uses the
  exact supported call `await subscriptions.cancel(subscription)`.
- Section 11 now names projection `@Subscribe` handler delivery as the durable
  server-side handoff and distinguishes it from active client-facing
  `SubscriptionService` streams and queues, which remain process-local.
- The ZeroMQ paragraph now states both that the adapter has no transport-owned
  retry loops and that it provides no retry or restart guarantee.

## Review Wave 2 Fix Handback

- Changed paths are exactly `docs/USER_GUIDE.md`,
  `build-protocol/tasks/T-0039c-framework-user-guide/TASK.md`,
  `build-protocol/work-logs/T-0039c.md`, and
  `build-protocol/reviews/T-0039c-framework-user-guide.md`.
- Focused wording checks found the returned-message cancellation contract and
  exact `subscriptions.cancel(subscription)` call, the projection-handler /
  client-stream boundary, and both ZeroMQ retry-loop and retry/restart
  exclusions.
- Exact-path formatting, status equality, prohibited API/future-policy,
  docs/API, generated-clean, scope, and diff-integrity results are recorded
  below after the final verification run. Full `pnpm verify` was not run
  because it remains final-only.
- Remaining uncertainty: the client block remains explicitly illustrative and
  requires consumer-generated `Query` and `Topic` fixtures; its public Connect
  call shape is checked against current server tests rather than compiled as a
  standalone workspace application. No runtime behavior changed.
- Skills used: receiving-code-review, doc-coauthoring, and
  verification-before-completion. Actual immutable profile: existing
  implementer, `gpt-5.6-terra` / medium; no subagents.

## Review Wave 2 Fix Verification

- Exact-path
  `pnpm --config.verify-deps-before-run=false exec prettier --check` passed for
  the guide and all three T-0039c records.
- The focused cancellation/delivery/ZeroMQ Node scan passed seven assertions
  and found exactly one
  `await subscriptions.cancel(subscription)` call. It also rejected the stale
  ID-only cancellation wording, ambiguous “live projection subscriptions,” and
  an ID-property cancellation call.
- The first status scan incorrectly assumed `Status:` was the first line and
  stopped after reporting the three Markdown headings. Its corrected
  status-line scan passed with the exact shared value
  `Author review wave 2 fix complete — coordinator rereview pending`; no
  content edit was needed for the scan correction.
- The prohibited API/import/future-policy scan passed for 11 internal or
  application-owned API tokens, relative/internal imports, and seven
  future-policy patterns.
- `pnpm --config.verify-deps-before-run=false docs:check` passed: copied Proto
  checksums verified and TypeDoc/API checks found the expected `100` proto,
  `28` core, `205` server, `19` storage, `17` transport, and `3` testing
  exports.
- `pnpm --config.verify-deps-before-run=false proto:check-generated` passed;
  generated Proto outputs are ignored, untracked, and freshly regenerated.
- `git diff --check`, `git diff --name-only`, and `git status --short` passed
  the integrity and scope inspection with exactly the four authorized paths
  modified.

## Review Wave 3 Assignment

- `2026-07-14T13:58:15Z`: Wave 2 fix committed at
  `6caf1c8ebb624a6d996d82de5c2cb85db59a2799` (`Fix T-0039c review wave 2
findings`); worktree was clean after commit.
- Fresh coordinator exact-path Prettier, `docs:check`, generated-clean,
  cancellation/delivery/ZeroMQ, prohibited API, future-policy, status, scope,
  and `git diff --check` gates passed.
- Literal package `.superpowers/sdd/review-aaa31116..6caf1c8e.diff`: baseline
  `aaa31116`, endpoint `6caf1c8ebb624a6d996d82de5c2cb85db59a2799`, ten
  commits, `151555` bytes.
- API and reliability existing reviewers are assigned explicit immutable
  `gpt-5.6-terra` / high, read-only, no subagents. Style/documentation stay
  CLEAN because their Wave 2 concerns were not changed by the three narrow
  fixes.

## Review Closure

- `2026-07-14T14:03:38Z`: TypeScript/API reviewer
  `019f60ed-904f-7962-aeb5-0227783b0d3c` and reliability reviewer
  `019f60ed-8c20-75a1-a624-1ed3a59e7763` both returned CLEAN at actual
  immutable `gpt-5.6-terra` / high and were closed; neither used subagents.
- Retained Wave 2 CLEAN dispositions: style
  `019f60de-6e1b-7443-84ee-471c15b0e620` at actual immutable
  `gpt-5.6-terra` / high and documentation
  `019f60de-724a-7551-a6d5-d83e73ed80f1` at actual immutable
  `gpt-5.6-luna` / medium. Both were already closed.
- All four canonical concerns are CLEAN over the accepted guide endpoint and
  no participating child remains open. Security remains the project-wide
  T-0041 gate. Final task `pnpm verify`, closure records, integration,
  post-merge verification, remote synchronization, and cleanup remain.

## Final Task Verification

- `2026-07-14T14:08:31Z`: native full gate
  `pnpm --config.verify-deps-before-run=false verify` exited `0`.
- Ordinary Vitest: `71` files / `1,642` tests passed. Coverage Vitest: `71`
  files / `1,642` tests passed.
- Coverage summary: statements `95.38%` (`9127/9569`), branches `90.12%`
  (`4499/4992`), functions `98.22%` (`2382/2425`), lines `95.40%`
  (`8950/9381`).
- Passed Node check, Proto generation/checksum verification, build and tooling
  typechecks, ESLint, cleanup enforcement, formatting, TypeDoc/API checks with
  `100` proto / `28` core / `205` server / `19` storage / `17` transport / `3`
  testing exports, Proto lint, and generated-clean verification.
- T-0039c is accepted locally. Remaining durable closure steps: commit these
  records, merge into `main`, rerun post-merge verification, push task branch
  and `main`, record remote refs, and remove the clean merged worktree.
