# T-0140 Work Log

## 2026-08-08 Startup

- Started from pushed T-0139 closure `6f7a1593` in the existing isolated
  worktree and created/pushed stacked branch `task/T-0140-delivery-monitor`.
- `git status` is clean; no T-0139 branch rewrite or merge to `main` occurred.
- Classified high-risk because the task changes public API plus delivery
  concurrency, lifecycle, and error-containment semantics.
- Reused the fully read Wave 8 plan and protocol as durable memory; no new
  architecture split was invoked because the frozen T-0140 scope has no
  contradiction.
- Selected applicable execution disciplines: implementation from the written
  plan, one subagent-driven production owner, RED/GREEN TDD,
  verification-before-completion, and specialist review before closure.
- Inspected the task-relevant JVM monitor/failure/action types and builder/test
  evidence listed in the task record. The TS asynchronous adaptation and its
  non-throwing default pickup result remain governed by the frozen Wave 8 plan.

## Assignment Metadata

- Implementation owner: existing `implementer` role.
- Explicit dispatch: `gpt-5.6-terra` / `medium`.
- Scope: all T-0140 production/tests/docs/protocol records; no subagents,
  examples, validation upgrade, persistent failure concepts, or branch
  integration.
- Runtime self-introspection will be recorded if exposed. Otherwise the
  immutable configured role/profile is accepted absent a visible mismatch or
  fallback.

## 2026-08-08 Implementation Checkpoint 1

- Read the complete assigned task/protocol records, the TDD and verification
  skill instructions, and the task-relevant local JVM delivery research notes.
  The implementation impact is the JVM monitor/action vocabulary with the
  frozen asynchronous TypeScript adaptation; no new scheduler or retry policy
  was introduced.
- RED: `pnpm exec vitest run packages/server/test/delivery/delivery-builder.test.ts`
  initially failed because the pre-cutover runtime imported deleted
  `./delivery-attempts.js`.
- GREEN slices: added the instantiable exported `DeliveryMonitor`, failure
  evidence/actions, WorkerId-aware registry port, and direct-Inbox delivery
  path. The default reception action durably marks delivery. A failed action
  falls back once to mark-delivered; if both acknowledgements fail, the row's
  target is blocked for this run while unrelated targets continue.
- Evidence: focused monitor test and acknowledgement-isolation test both pass;
  `pnpm exec tsc -p packages/server/tsconfig.json --noEmit` passes; `git diff
--check` passes. The inherited observer/attempt tests are intentionally still
  red and must be replaced in later T-0140 slices.
- Remaining work: remove the stale observer/attempt-facing test and runtime
  surfaces, complete WorkerId builder cutover, hook-fallback and graceful-stop
  adversarial coverage, public documentation, and final deterministic checks.

## 2026-08-08 Implementation Checkpoint 2

- Replaced the remaining builder observer-era tests with focused monitor
  contract tests. They cover direct/promised default hooks, opaque per-lifetime
  WorkerId values, explicit complete WorkerId use, acknowledgement isolation,
  and renewal-loss callback fencing.
- Delivery now allocates `WorkerId.value` with `randomUUID()` when callers do
  not supply a complete WorkerId. It never derives the opaque value from node
  text. Every pending callback and acknowledgement is preceded by leased
  session renewal; renewal loss stops the shard before callback or durable
  completion. Direct draining rescans until the shard is empty, including rows
  arriving during a preceding page.
- Operation abort prevents a later pickup or callback and the release remains
  ordered before the contained completion hook. Hook/action errors are
  contained; reception callback/action errors fall back once to durable mark.
- Evidence: `pnpm exec vitest run packages/server/test/delivery/delivery-builder.test.ts`
  (5 passing) and `pnpm exec tsc -p packages/server/tsconfig.json --noEmit`
  (passing). Full delivery-suite migration, coverage, lint/format, prohibited
  scans, and graceful-stop takeover adversarial tests remain before review.

## 2026-08-08 Checkpoint 2 Corrections

- Folded the orchestrator's two concrete findings into the current context:
  a no-progress pass over a same-target acknowledgement failure now terminates
  rather than rescanning the durable pending row forever, and focused tests
  retain session-kind and builder-bound validation alongside new monitor tests.
- Added the no-progress regression and reran the focused suite (8 passing).
  Prettier was applied to changed delivery, test, package-documentation, and
  work-log files; the source typecheck and `git diff --check` remain clean.

## 2026-08-08 Delivery-Suite Migration Inventory

- `pnpm exec vitest run packages/server/test/delivery` is intentionally not a
  passing acceptance gate yet: it reports 124 failed legacy assertions and
  three transform failures importing deleted `delivery-attempts.js` from
  `delivery-fencing`, `delivery-loop`, and `delivery-worker` tests. The retained
  `delivery-run-coordinator` and `delivery-worker-runtime` suites also assert
  deleted loop/private-access behavior.
- The same run reports direct-Inbox/dedup assertion failures in `inbox*` tests
  whose fixtures still name deleted attempt and dedup-claim surfaces. Those are
  frozen T-0139-to-T-0140 migration consumers, not evidence that the monitor
  focused suite regressed. They must receive a selective replacement/removal
  batch before task verification; no compatibility exports will be restored.

## 2026-08-08 Direct Inbox Cutover

- Replaced the remaining per-message work/claim completion path in the monitor
  runtime with `DeliveryInbox.markDelivered(message)`. Delivery now uses the
  direct durable Inbox acknowledgement API after fenced callback/repeat action;
  no delivery claim, receipt, marker, or attempt state is introduced.
- Focused monitor suite remains 8 passing after the cutover; server source
  typecheck, Prettier, and `git diff --check` remain clean.

## 2026-08-08 Delivery Worker Test Migration

- Replaced the legacy `delivery-worker.test.ts` fixture architecture. Removed
  its attempt/exhaustion/retry/claim/marker/guard imports, helpers, and
  assertions without compatibility aliases.
- The concise direct-Inbox/complete-WorkerId suite retains and rewrites worker
  ownership, pickup skip, complete WorkerId propagation, renewal before callback
  and acknowledgement, renewal-loss fencing, successful statistics, default
  reception acknowledgement, same-target durable-mark isolation/no-spin,
  mid-drain arrivals, abort-after-in-flight settlement/release, and unsupported
  label skipping.
- Direct storage/CAS/dedup/corruption behavior intentionally remains owned by
  the pushed T-0139 test files `direct-inbox-records.test.ts`,
  `direct-shard-session-records.test.ts`, `direct-record-provider-conformance.test.ts`,
  and `inbox-records.test.ts`; it is no longer duplicated through retired
  delivery claim/guard fixtures.
- Evidence: `pnpm exec vitest run packages/server/test/delivery/delivery-worker.test.ts`
  passes 8/8; `pnpm exec tsc -p packages/server/tsconfig.json --noEmit`,
  Prettier, prohibited-symbol scan, and `git diff --check` pass.

## 2026-08-08 Inbox Test Migration

- Replaced `inbox.test.ts` and `inbox-records.test.ts` JSON/dedup-claim/guard/
  attempt-era fixtures with concise direct generated-record tests. Removed all
  retired DedupRecords, claim, guard, marker, retry-recovery, JSON/base64, and
  attempt assertions without aliases.
- Retained public direct Inbox receive/read/readMessage/mark-delivered flow,
  ordering, shared-context isolation, invalid limits, immutable caller input,
  generated record round-trip, shard validation, malformed payload rejection,
  and generated shard coordinates.
- `direct-inbox-records.test.ts` remains the T-0139 authority for durable
  direct-storage CAS, continuation, pending/delivered record behavior,
  mapping validation, live/expired delivered-row dedup, and corrupt stored
  record classification; this task does not duplicate retired guard machinery.
- Evidence: `pnpm exec vitest run packages/server/test/delivery/inbox.test.ts
packages/server/test/delivery/inbox-records.test.ts
packages/server/test/delivery/direct-inbox-records.test.ts` passes 26/26;
  server typecheck, Prettier, prohibited-symbol scan, and `git diff --check`
  pass.

## 2026-08-08 Loop And Fencing Test Migration

- Replaced loop/fencing attempt-capacity, epoch/admission, private-access,
  claim, retry/exhaustion, and observer callback fixtures with finite-loop
  integration tests. No compatibility internals were added.
- DeliveryLoop now enforces its single-run guard, reports a pre-stopped run
  without starting a drain, and makes `close()` await an already running drain.
  The fencing suite retains abort-before-pickup and release-after-contained
  monitor-start failure. Worker and direct-shard suites own renewal, callback,
  acknowledgement, and replacement-worker detail coverage.
- Evidence: `pnpm exec vitest run packages/server/test/delivery/delivery-loop.test.ts
packages/server/test/delivery/delivery-fencing.test.ts
packages/server/test/delivery/delivery-worker.test.ts` passes 14/14;
  server typecheck, Prettier, prohibited-symbol scan, and `git diff --check`
  pass.

## 2026-08-08 Worker Runtime And Coordinator Migration

- Replaced `delivery-worker-runtime.test.ts` private delivery replacement,
  max-failure, paused/retry, and admission-era fixtures with current worker
  start/result ordering, second-start exclusion, and shard-list validation.
- Rewrote the one real-worker coordinator adapter case to use public worker
  settlement rather than the deleted `deliveryAccess.replace`/drain-outcome
  seam. No compatibility hook was added. The remaining coordinator suite keeps
  its independent generation-retirement coverage.
- Evidence: `pnpm exec vitest run packages/server/test/delivery/delivery-worker-runtime.test.ts
packages/server/test/delivery/delivery-run-coordinator.test.ts
packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-loop.test.ts` passes 61/61; server
  typecheck, Prettier, prohibited-symbol scan for deleted worker seams, and
  `git diff --check` pass.

## 2026-08-08 Supervisor Diagnosis

- Updated the real supervisor Inbox fixture to supply a generated Event payload
  compatible with `UPDATE_SUBSCRIBER`, removing the direct-record label/payload
  mismatch that previously failed eight tests before supervisor behavior ran.
- Systematic-debugging attempt 1 temporarily changed `DeliveryRunControl` to
  join the underlying controlled run after abort. Its focused regression still
  stalled, so that unconfirmed production hypothesis was removed rather than
  stacking fixes.
- Attempt 2 established that the test never reached `AdmissionInbox.read`:
  `RealDeliveryFixture` supplied the retired partial `{ onCompleted }` monitor.
  The current runtime calls `shouldContinueAfter("DELIVERY")`; the absent hook
  was safely contained as `false`, ending the run after pickup/release and
  before the paused read. Replacing the fixture with a `DeliveryMonitor`
  subclass and correcting the registry fixture to take complete `WorkerId`
  confirms the frozen current contract without a production change.
- The fixture now also delegates direct `markDelivered`. The formerly
  zero-ownership close test asserts current behavior: empty direct work is
  acquired, close aborts/fences the in-flight read, release occurs before the
  completion hook, and zero-grace close reports its bounded timeout after the
  run settles. Supervisor generation assertions now count each direct drain's
  terminal empty scan.
- Evidence: `pnpm exec vitest run
packages/server/test/delivery/delivery-supervisor.test.ts --reporter=verbose`
  passes 38/38, and `pnpm exec vitest run packages/server/test/delivery
--reporter=verbose` passes 15 files / 182 tests with no unhandled rejection
  output.

## 2026-08-08 Pre-review Documentation And Coverage

- Completed T-0140-owned TSDoc for `delivery-monitor.ts`, `delivery.ts`,
  `delivery-loop.ts`, `delivery-builder.ts`, and `delivery-ports.ts`, including
  complete-worker pickup terminology, documented monitor hooks/actions, and
  direct delivery lifecycle guarantees. `pnpm lint:tsdoc` now reports only the
  inherited `packages/storage-rdbms/src/mysql/{errors,scope,testing}.ts`
  findings; those storage files were not changed.
- Updated the API documentation expectation manifest for the public monitor
  actions/facts and deliberately deleted quarantine exports. No compatibility
  aliases were introduced. The next API-doc gate reaches an unrelated existing
  storage `RecordSpecInput` forbidden-TypeDoc exposure; it is outside T-0140.
- Updated Server README and REFERENCE: `DeliveryMonitor` is customizable; shard
  ownership excludes only concurrent same-shard delivery; delivered rows are
  dedup facts; handler effects and delivered CAS are not transactional; a lost
  acknowledgement can redeliver and downstream handlers own idempotency; the
  default failed-reception action marks delivered and independent targets
  continue. Attempts, quarantine, receipts, markers, timers, backoff, and
  scheduler policy are absent.
- Added focused behavior coverage for monitor default actions, one immediate
  repeat dispatch, pickup outcome mapping, continuation gates, acknowledgement
  fallback, controlled abort, exclusive ownership, and loop operation/status
  forwarding.
- Evidence: `pnpm exec vitest run packages/server/test/delivery --coverage
--coverage.include='packages/server/src/delivery/{delivery-monitor,delivery,
delivery-loop,delivery-run-control}.ts' --coverage.reporter=text` passes 15
  files / 191 tests at 95.58% statements, 90.81% branches, 96.77% functions,
  and 96.79% lines.

## 2026-08-08 Final Continuation Coverage Correction

- Added the reviewer regression for a `pageSize=1` direct scan with a failed,
  durably pending target A followed by independent target B. The test uses a
  stable ordered catalog and separate pending set to model keyset reads. It
  proves that the full no-progress A page supplies `after`, B then dispatches
  and acknowledges, the scan resets, and a final continued read exhausts A
  without a repeated A callback or spin.
- Evidence: `pnpm exec vitest run packages/server/test/delivery --coverage
--coverage.include='packages/server/src/delivery/{delivery-monitor,delivery,
delivery-loop,delivery-run-control}.ts' --coverage.reporter=text
--coverage.reporter=json --reporter=dot` passed 15 files / 195 tests with
  95.91% statements, 90.26% branches, 96.96% functions, and 97.66% lines.

## 2026-08-08 Abort And Renewal Evidence

- Added direct-worker adversarial coverage for a deferred pickup followed by
  abort: cleanup release returning `false` or rejecting is contained as
  `FAILED`, completion is not called, and cleanup receives no aborted
  operation. The same focused slice proves the exact caller `AbortSignal`
  reaches `Inbox.read`, and a thrown renewal is contained before callback or
  acknowledgement while release still occurs.
- Evidence: `pnpm exec vitest run packages/server/test/delivery/delivery-worker.test.ts
packages/server/test/delivery/delivery-run-control.test.ts
packages/server/test/delivery/delivery-supervisor.test.ts --reporter=dot`
  passed 3 files / 58 tests; `pnpm exec tsc -p packages/server/tsconfig.json
--noEmit` passed.

## 2026-08-08 Immutable Snapshot Evidence

- Retained failure facts now clone packed signal payloads and deadline/receive
  dates as well as immutable IDs and shards. Focused mutation coverage proves
  later caller mutation of payload bytes, dates, IDs, inbox identity, and
  shards cannot alter the retained fact.
- `DeliveryLoop` snapshots delivery, shard, callback, and operation fields at
  construction; a caller mutating its original options object cannot redirect a
  later run.
- Evidence: focused worker/loop tests passed 2 files / 23 tests; server
  typechecking and ESLint for the changed runtime/test files passed.

## 2026-08-08 Downstream Fixture And Gate Convergence

- Migrated Entity Inbox and Projection handoff fixtures to generated
  label-compatible Command/Event payloads and the approved default monitor
  semantics. Failed receptions are contained and acknowledged as delivered;
  whole-shard drains may process previously persisted backlog. No attempt,
  quarantine, receipt, marker, or replacement persistence was introduced.
- Preserved the configured environment node when constructing the direct
  `Delivery` owner, so the complete generated `WorkerId` derives from the
  intended node rather than silently falling back to the context name.
- Evidence: the six affected handoff/environment files pass 194/194 tests;
  seven focused correction files pass 86/86 tests; server typechecking passes.
  The final delivery coverage selection passes 15 files / 202 tests at 95.45%
  statements, 91.26% branches, 96.96% functions, and 97.09% lines.
- Changed T-0140 TypeScript, excluding the inherited
  `environment-attachment.test.ts` fixture baseline, passes ESLint. TSDoc has
  no T-0140-owned findings; only the existing MySQL findings remain.
  Documentation audience checks pass. API documentation reaches only the
  unrelated storage `RecordSpecInput` exposure. Repository formatting reports
  unrelated existing auth/MySQL files after the T-0140 protocol files are
  formatted.

## 2026-08-08 Delivery-Client Cutover Assignment

- Existing role: project `implementer`; ownership is limited to
  `packages/delivery-client` production/tests/docs and any directly required
  server public-declaration fixture adjustment. The implementer must not touch
  the current server correction files or review record and must not spawn
  subagents.
- Explicit dispatch profile: `gpt-5.6-terra` / `medium`, as required for
  ordinary TypeScript integration. Runtime metadata is recorded if exposed;
  otherwise the configured role/profile and limitation are sufficient.
- Acceptance: replace the retired `DeliveryInboxWork.begin()` adapter with
  direct authoritative `markDelivered()` removal; migrate remote shard pickup
  to complete `WorkerId`; remove every quarantine/release marker Set and stale
  lifecycle claim/test without a compatibility alias or replacement state;
  preserve bounded remote protocol behavior and make focused tests/typecheck
  green.

## 2026-08-08 Remote Commit-Fence Architecture Blocker

- Demonstrated contradiction: deleting the retired per-message work object is
  required by the direct Inbox port, but a real gRPC forced-expiry regression
  proves the object also carried the only commit-time remote ownership guard.
  A blocked stale handler can therefore resume after takeover and overlap the
  replacement, contradicting T-0140's frozen no-concurrent-replacement
  acceptance. Reintroducing the work object or quarantine state is forbidden.
- Existing role: project `requirements_splitter`; read-only scope is the
  smallest direct-port-compatible ownership fence. Explicit dispatch profile:
  `gpt-5.6-sol` / `high`, permitted only because the integration regression
  demonstrates an architectural/public-contract blocker. No subagents and no
  file edits are authorized.

## 2026-08-08 Remote Commit-Fence Resolution

- The explicitly dispatched `requirements_splitter` retained its configured
  `gpt-5.6-sol` / `high` profile; runtime self-introspection was unavailable.
  It confirmed the smallest correct design: the public work registry validates
  the exact retained session before dispatch, through the repository
  commit-time fence, and before acknowledgement. The direct Inbox remains
  independent and stores no work object.
- `ShardedWorkRegistry.validateOwnership()` returns its exact CAS-renewed
  session. `RemoteWorkRegistry.validateOwnership()` probes with the retained
  complete worker and pickup timestamp. A different owner invalidates the
  local session; an accidental pickup is boundedly released and still fails
  the stale run closed; an unknown probe outcome throws without retaining a
  marker or quarantine.
- Evidence: the forced-expiry gRPC regression passes with the replacement
  commit preserved and the resumed stale owner fenced; the remote validation
  and gRPC selection passes 20/20; the full server delivery suite passes
  202/202; the full delivery-client suite passes 95/95.
- The independent overflow regression was test-local: the child-process helper
  ignored its environment override, so the requested capacity of one was never
  configured. Forwarding explicit child environment values makes the isolated
  overflow/recovery test pass without a production change.

## 2026-08-08 Final Preflight Correction

- Isolated coverage directories removed an invalid concurrent Vitest artifact
  collision. The valid changed-source measurements pass: Server delivery at
  96.07% statements, 91.26% branches, 97.10% functions, and 97.17% lines
  (15 files / 203 tests); delivery-client adapters at 93.18%, 92.98%, 96.87%,
  and 95.72% respectively (12 files / 96 tests).
- The T-0140 environment-attachment fixture migration had removed the
  `StorageContext` type import while retaining typed fixture parameters. ESLint
  therefore classified every dependent context as error-typed and reported 29
  `no-unsafe-assignment` diagnostics. Restoring the one type-only import clears
  the affected file and the complete changed TypeScript/JavaScript lint scope;
  no runtime behavior changed.
