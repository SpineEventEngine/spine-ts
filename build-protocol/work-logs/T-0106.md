# T-0106 Work Log

Status: In Progress

- 2026-08-04: created the clean task worktree from
  `origin/main@c1af7fd7` after T-0105 was reviewed, verified, merged, pushed,
  and cleaned up.
- 2026-08-04: confirmed that Aggregate commands still invoke
  `AggregateCommandExecution` directly while Process Manager commands/events
  use a local single-shard Inbox and Projections use a separate Inbox.
- 2026-08-04: confirmed that context endpoint descriptors drive startup
  recovery and `ServerEnvironment` supplies delivery ports. Therefore the
  bounded context must own target-to-shard routing before environment
  attachment; the environment must not introduce a second strategy.
- 2026-08-04: classified the task as high-risk and recorded the existing
  `implementer` assignment with expected explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection is unavailable; the immutable role profile and
  explicit dispatch fields are the metadata evidence.
- 2026-08-04: applicability check reused the session inventory and
  `build-protocol/skills/EXPECTED_SKILLS.md`; selected `tdd` and
  `test-driven-development` from `~/.agents/skills/` for the required
  RED-first runtime workflow. Expected but unselected skills are not required
  for this bounded implementation owner. The lock manifest was readable.
- 2026-08-04: inspected `spine-jvm-docs/spine-routing-dispatch-and-delivery.md`
  (repository-to-Inbox handoff, `DeliveryStrategy`, and local/routed delivery).
  It confirms the implemented boundary must persist before handler replay and
  that target-to-shard selection belongs to delivery, not environment ports.
- 2026-08-04: RED checkpoint added a focused context test for a three-shard
  `withDeliveryStrategy(...)` Entity Inbox endpoint enumeration. After local
  dependency install and generated TypeScript output preparation, it failed as
  intended: `withDeliveryStrategy is not a function` (51 existing tests passed).
- 2026-08-04: GREEN checkpoint made the builder snapshot and validate an
  immutable context delivery strategy; the Process Manager Inbox now enumerates
  every configured shard for each supported label. Focused bounded-context
  verification passed: 52 tests.
- 2026-08-04: Aggregate command dispatch now routes through the existing
  context-owned inbox target owner and replays through the established
  transactional Aggregate executor. Aggregate and Process Manager focused
  command checks pass together (2 selected tests); inbox writes derive their
  shard from the context strategy.
- 2026-08-04: RED regression checkpoint proves a context strategy declaring
  two shards can still return `ShardIndex.single()` during endpoint setup;
  the focused suite failed only that test (52 passed). The next GREEN change
  must reject the invalid strategy before a context is built.
- 2026-08-04: the first implementation owner pushed Aggregate command handoff
  and Inbox-owned shard derivation through `cd296cc2`. Its focused context and
  command checks passed, but the owner repeatedly ended follow-up turns before
  completing the assigned generic naming, routed replay, acceptance coverage,
  and documentation batch. The branch remained clean and no work was lost.
- 2026-08-04: closed the first ownership context and recorded a non-overlapping
  reassignment to the existing `implementer` role. Expected dispatch remains
  explicit `gpt-5.6-terra` / `medium`; the new owner starts from pushed commit
  `cd296cc2` and owns all remaining production/test/documentation files.
- 2026-08-04: the shared implementation now lives in `entity-inbox.ts` as
  `LocalEntityInbox`; Aggregate and Process Manager replay share its durable
  target/shard path while Projection remains separate.
- 2026-08-04: direct Aggregate follow-up dispatch runs after the outer Entity
  row drains. A live causal follow-up tail avoids shared-shard races without
  delaying the originating command acknowledgement.
- 2026-08-04: deterministic preflight corrections completed at `8483bc8e`.
  The generated/build/tooling typechecks, ESLint, cleanup, TSDoc, formatting,
  documentation audience/API, Proto, generated-cleanliness, and release
  readiness gates all pass. The focused repository/context/Entity Inbox suite
  passes 223/223 tests.
- 2026-08-04: focused V8 inspection over the three changed production files
  reports 167/181 changed executable lines (92.3%) and 72/80 changed branches
  (90.0%). The new `entity-inbox.ts` reports 96.94% statements and 92.85%
  branches. Applying the task runner's 90% threshold to the entirety of the
  legacy `repository.ts` produces an expected whole-file artifact (72.74%
  branches); the final repository-wide `verify:release` remains authoritative.
- Next: run one complete relevant specialist review wave and return one
  aggregated correction batch to the implementation owner if needed.
- 2026-08-04: the complete specialist wave returned eight consolidated
  findings, recorded in the T-0106 review record. The correction owner pushed
  per-tenant/shard chained tails, one-time input shard resolution, and replay
  shard validation through `fa59ccbe`; the focused Entity Inbox suite passes
  29/29.
- 2026-08-04: the correction owner repeatedly ended turns without adding the
  reviewer-required concurrency, stateful-strategy, and multi-shard replay
  regressions or completing naming/types/docs. Closed that context with a clean
  pushed branch and recorded a non-overlapping reassignment to the existing
  `implementer`, explicit `gpt-5.6-terra` / `medium`.
- 2026-08-04: correction RED checkpoint `8b7bf68a` added focused Entity Inbox
  regressions for same-scope chained tails, cross-tenant/cross-shard progress,
  and stateful strategy routing. The stateful strategy test failed as intended:
  three input routes invoked `shardFor` six times because local replay resolved
  the target a second time.
- 2026-08-04: GREEN checkpoint `c28f4647` threads each already-resolved shard
  through the local drain, while descriptor/external replay still resolves and
  validates its stored route. Focused Entity Inbox/context/repository tests pass
  226/226. The remaining correction pass renames shared Entity Inbox seams and
  updates public documentation.
- 2026-08-04: `verify:generated-gates` stopped at tooling typecheck for the
  proposed `EntityInboxReplay` promise-shape union: existing target callbacks
  infer `Promise<void | EntityInboxFollowUp>`, which cannot satisfy a union of
  promise containers, while a no-void result union rejects normal
  `Promise<void>` callbacks. The prior `Promise<unknown>` is restored pending
  an explicit compatible contract decision.
- 2026-08-04: after the explicit-`undefined` replay-contract correction,
  focused Entity Inbox/context/repository tests pass 226/226 and
  `verify:generated-gates` passes: build/tooling typechecks, ESLint, cleanup,
  TSDoc, format, docs/API, Proto, generated cleanliness, and release readiness.
  The requested descriptor-specific Aggregate/PM nonzero-shard forged-row and
  fresh multi-shard recovery regression remains to be added before review close.
- 2026-08-04: the second correction owner reached its turn limit at pushed,
  clean endpoint `4bc830e0`. Assigned the remaining descriptor-only regression
  gap to a fresh non-overlapping existing `implementer`, explicit
  `gpt-5.6-terra` / `medium`. Runtime self-introspection remains unavailable;
  the immutable role profile and explicit dispatch fields are the metadata
  evidence.
- Next: add descriptor-level multi-shard replay/recovery evidence, rerun
  deterministic preflight, and re-review affected concerns.
- 2026-08-04: descriptor-only correction coverage is ready at the fresh owner
  endpoint. `bounded-context.test.ts` persists real `InboxMessage` fixtures
  through low-level `Delivery.inbox`, then proves descriptor replay for valid
  nonzero-shard Aggregate commands plus Process Manager commands and events;
  forged Aggregate/PM shards reject before state mutation. A fresh same-storage
  Process Manager context enumerates all six configured three-shard/label
  endpoints and the environment replays each pending row once to `DELIVERED`.
  The focused suite passes 55/55. No production defect was exposed, so no
  production GREEN change is appropriate; this correction contributes evidence
  only. The requested RED-before-GREEN checkpoint could not be truthfully
  created because the pushed runtime at `ecc2df43` already satisfies the new
  behavior.
- Next: commit/push the descriptor evidence, run the bounded task preflight,
  then re-review the affected reliability concern; do not mark review lanes clean.
- 2026-08-04: pushed descriptor evidence checkpoint `572d4a57` and its typed
  fixture follow-up `3abad35d`. Focused bounded-context, Entity Inbox, and
  repository suites pass 106/106; changed-test ESLint, direct Prettier checks,
  and `git diff --check` pass. The package `format:check` wrapper accepts no
  path argument, so its path-qualified invocation reported usage rather than a
  formatting failure; direct Prettier is the targeted formatting evidence.
