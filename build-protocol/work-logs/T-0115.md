# T-0115 Work Log

## Task Identity

- Branch: `task/T-0115-system-context-cutover`.
- Worktree: `.worktrees/T-0115-system-context-cutover`.
- Baseline: `origin/main@8059a0a6`.
- Authoring agent: `/root/t0115_impl`.
- Started: 2026-08-05 19:26 WEST.
- Ended: pending.
- Coverage: pending.
- Integration: pending.

## Start Boundary

- T-0114 is reviewed, release-verified, integrated, post-merge verified, and
  pushed.
- The approved T-0113 Sol/high architecture pass is final and will not be
  repeated absent a demonstrated blocker.
- Desktop supports explicit model/reasoning dispatch.
- Relevant TS paths include Bounded Context assembly, EventBus, Stand,
  SubscriptionService, subscription observer/registry, and repository system
  publication.
- The implementer must inspect the task-relevant JVM research/source evidence
  for `BoundedContextBuilder`, `SystemContext`, `DomainContext`, Stand, and
  `SystemWriteSide` without building or modifying Spine JVM.

## Decisions

- Keep one atomic owner because construction, Stand activation, service
  attachment, repository publication, and close overlap.
- Reuse the T-0114 storage-free forgetting EventBus.
- Keep one pair-owned subscription runtime rather than two Stand reconcilers.
- Add no third-party dependency and no public raw System Context.
- RED/GREEN checkpoint: domain EventBus schema registration now rejects
  `spine.system.*`; the internal system bus accepts only those schemas. The
  first RED failed at `eventBusAccess.registerSchemas()` exactly as expected.
- RED/GREEN checkpoint: `persistSystemEvents()` initially failed because the
  builder lacked the method. It now selects a separate paired System Context
  storage name; forgetting remains the default. Repository
  `EntityStateChanged` publication uses only the paired system bus.
- JVM evidence inspected: `spine-server-runtime-and-bounded-context.md`
  records `BoundedContextBuilder.build()` creating an internal system context,
  copied tenancy, and `SystemAwareStorageFactory` default-forgetting behavior.

## Checkpoint 1 — 2026-08-05

- Focused green evidence: `pnpm exec vitest run packages/server/test/context/bounded-context.test.ts packages/server/test/stand/subscription-observer.test.ts packages/server/test/bus/event-bus.test.ts` — 111 tests passed.
- Type evidence: `pnpm exec tsc --noEmit --project packages/server/tsconfig.json` — passed.
- Remaining: move timer/attachments/consumers to an explicit pair-owned runtime,
  complete partial-build and terminal close failure coverage, then mechanical
  verification and review.

## Checkpoint 2 — 2026-08-05

- System-only `EventDispatcher`s are now routed to the paired system bus;
  mixed domain/system dispatchers reject at assembly. Existing test-only
  `EntityStateChanged` injection uses a package-internal context seam, leaving
  the public domain endpoint unable to traverse the system bus.
- Focused green evidence: `pnpm exec vitest run packages/server/test/stand/stand.test.ts packages/server/test/services/spine-services.test.ts packages/server/test/context/bounded-context.test.ts packages/server/test/repository/repository.test.ts packages/server/test/services/native-subscription-integration.test.ts` — 229 tests passed.
- Type evidence: `pnpm exec tsc --noEmit --project packages/server/tsconfig.json` — passed.

## Active Limitation

- The current green implementation still hosts the shared subscription timer,
  attachment map, and consumer map on the domain Stand (although it now routes
  entity observers to the system bus). T-0115 requires moving those structures
  to an explicit pair-owned runtime before review; do not accept this checkpoint
  as task completion.

## Demonstrated Architecture Escalation

- Trigger: the Terra/medium implementer completed the paired bus and routing
  cutover but could not establish the explicit pair-owned subscription runtime
  and terminal lifecycle from the current Stand-owned structures.
- Existing role: requirements splitter `/root/t0115_split`.
- Scope: read-only extraction/cutover map for one pair-owned runtime, service
  attachment, partial-build cleanup, and terminal close tests; no contract
  change, code edit, JVM build, or new abstraction beyond the approved plan.
- Expected and explicitly dispatched model: `gpt-5.6-sol`.
- Expected and explicitly dispatched reasoning: `high`.
- Result: accepted. The exact file/method-level map is persisted in
  `build-protocol/reports/T-0115_SUBSCRIPTION_RUNTIME_CUTOVER.md` and does not
  change the approved public contract.
- Runtime metadata: the immutable configured requirements-splitter profile
  confirms `gpt-5.6-sol` / high. Independent runtime self-introspection was not
  exposed; no visible mismatch occurred.

## Serial Implementation Split

- The original implementer returned `NEEDS_CONTEXT` twice: first before the
  architecture map, then again after adding only a skeletal runtime seam. No
  human decision is missing; the task is too broad for one authoring turn.
- Existing role for the first bounded continuation: implementer
  `/root/t0115_runtime_impl`.
- Ownership: only explicit `SubscriptionRuntime` extraction, Stand field/method
  migration, split observer attachment, focused Stand/runtime tests, and the
  preserved uncommitted report/log. BoundedContext/SubscriptionService and
  terminal close remain for later serial slices.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Runtime metadata: the immutable configured implementer profile confirms
  `gpt-5.6-terra` / medium. Runtime self-introspection is not exposed; no
  visible profile mismatch occurred.
- RED/GREEN evidence: `SubscriptionRuntime.consume()` initially resolved after
  `beginClose()`; it now rejects late consumers before allocating one. The
  persisted-System acquisition test initially observed domain storage before
  System storage; dispatcher classification is now preflighted and the System
  EventStore is acquired first.
- Failure proof: a Stand with two tenant-scoped entity handles attempts and
  clears both when the first close throws. Existing gated-snapshot and consumer
  rollback tests remain green.
- Focused evidence: `pnpm exec vitest run packages/server/test/stand/subscription-runtime.test.ts packages/server/test/stand/stand.test.ts packages/server/test/stand/subscription-observer.test.ts packages/server/test/context/bounded-context.test.ts packages/server/test/repository/repository.test.ts packages/server/test/services/spine-services.test.ts && pnpm exec tsc --noEmit --project packages/server/tsconfig.json` passed: 240 tests and server typecheck.
- Focused coverage ran with the same test set and changed sources. It reports
  Stand 98.21% lines and SubscriptionRuntime 97.56%, but the repository-global
  threshold rejects the deliberately focused selection at 82.74% lines and is
  therefore recorded as a mechanical limitation, not passing evidence.
- Focused lint and changed-file formatting passed. The focused TSDoc checker
  remains red on migration-wide existing lifecycle declarations (including
  `SubscriptionRuntime`, `Stand`, and subscription observer seams); this slice
  does not expand into the separate broad TSDoc remediation. Independent API
  and generated checks remain required before review.

## Continuation Slice A — 2026-08-05

- Existing role: implementer `/root/t0115_runtime_impl`; bounded ownership: package-internal subscription runtime extraction, Stand routing cutover, BoundedContext runtime ownership, and SubscriptionService consumption delegation.
- Expected dispatch was explicit: `gpt-5.6-terra` / `medium`. Runtime self-introspection is not exposed by this surface; the immutable dispatched profile is the available metadata and no mismatch was visible.
- RED evidence: `pnpm exec vitest run packages/server/test/stand/subscription-observer.test.ts` failed exactly because `SubscriptionObservers.observeEvent` was `undefined` (the paired route API did not yet exist).
- GREEN evidence: `pnpm exec tsc --noEmit --project packages/server/tsconfig.json && pnpm exec vitest run packages/server/test/stand/subscription-observer.test.ts packages/server/test/stand/stand.test.ts packages/server/test/context/bounded-context.test.ts packages/server/test/services/spine-services.test.ts` passed: server typecheck passed and 216 focused tests passed.
- Behavior: `SubscriptionRuntime` now owns the registry reconciliation tail, ten-second timer, attachments, and consumers for one paired context. It chooses exactly one observer route by domain Stand metadata: domain events attach to the domain bus; entity-state updates attach to the System bus. BoundedContext exposes registry and consumer access through the pair-owned runtime; services retain their structural fake-context fallback.
- Limitation: standalone legacy Stand test seam remains package-internal solely to preserve direct Stand test fixtures during this serial migration; production BoundedContext construction owns the runtime. Terminal close sequencing and partial-build failure matrix remain deliberately deferred to the next slice.

## Continuation Slice B Assignment

- Existing implementer: `/root/t0115_runtime_impl`, reusing its migration
  context.
- Ownership: migrate direct Stand subscription tests to explicit paired
  `SubscriptionRuntime`, delete the compatibility start/consume/reconcile
  accessors and per-Stand runtime weak map, and prove two Stands cannot start
  independent timers or consumer maps.
- Expected profile remains explicitly `gpt-5.6-terra` / medium from the
  original dispatch; immutable role metadata applies and no mismatch is
  visible.
- Terminal close ordering and partial-build failure injection remain the next
  serial slice.

## Continuation Slice B — 2026-08-05

- RED evidence: `pnpm exec vitest run packages/server/test/stand/stand.test.ts` failed exactly because `standAccess` still exposed `startSubscriptions`; the new boundary test expected the lifecycle operation to be absent.
- GREEN evidence: `pnpm exec tsc --noEmit --project packages/server/tsconfig.json && pnpm exec vitest run packages/server/test/stand/stand.test.ts packages/server/test/stand/subscription-runtime.test.ts packages/server/test/stand/subscription-observer.test.ts packages/server/test/context/bounded-context.test.ts packages/server/test/services/spine-services.test.ts` passed: server typecheck passed and 218 focused tests passed.
- Behavior: direct Stand reconciliation tests now construct explicit paired `SubscriptionRuntime` instances. The legacy `standAccess.startSubscriptions`, `consumeSubscription`, and `reconcileSubscriptions` methods, per-Stand runtime weak maps, helper, and optional System-bus fallback are removed. Production BoundedContext and service paths remain runtime-owned.
- Immutable role metadata: existing implementer `/root/t0115_runtime_impl`, explicitly dispatched `gpt-5.6-terra` / medium. Runtime self-introspection remains unavailable; no visible profile mismatch occurred.

## Continuation Slice C — 2026-08-05

- Lifecycle behavior: `SubscriptionRuntime` has explicit begin/drain/finish phases; beginning close stops the ten-second timer, drain fences queued reconciliation and detaches observers, and finish closes its registry once. `BoundedContext` now drains domain work while its System bus remains open, then begins/drains the System bus, drains the runtime, finishes buses, closes both Stands, and finishes the runtime.
- Failure behavior: Stand now attempts every entity-storage handle and reports aggregate failures only after clearing all handles. BoundedContext flattens nested `AggregateError` values before reporting terminal close failure.
- Focused green evidence: `pnpm exec tsc --noEmit --project packages/server/tsconfig.json && pnpm exec vitest run packages/server/test/stand/subscription-runtime.test.ts packages/server/test/stand/stand.test.ts packages/server/test/context/bounded-context.test.ts packages/server/test/repository/repository.test.ts packages/server/test/services/spine-services.test.ts` passed: typecheck passed and 227 tests passed.
- Mechanical evidence: `pnpm verify:task -- --coverage packages/server/test/stand/subscription-runtime.test.ts packages/server/test/stand/stand.test.ts packages/server/test/stand/subscription-observer.test.ts packages/server/test/context/bounded-context.test.ts packages/server/test/repository/repository.test.ts packages/server/test/services/spine-services.test.ts --source packages/server/src/stand/subscription-runtime.ts packages/server/src/stand/stand.ts packages/server/src/context/bounded-context.ts` passed its Node, Proto-generation/checksum, example Proto quality, clean-generated-dist, build, and lint/typecheck gates.
- Limitation: deterministic injected partial-build and terminal-order failure-table coverage remains incomplete; this checkpoint changes the lifecycle paths but does not yet prove every acquisition failure or exact-once terminal hook with dedicated tests.

## Continuation Slice C Assignment

- Existing implementer: `/root/t0115_runtime_impl`, reusing the accepted pair
  lifecycle context.
- Ownership: construction acquisition/partial-build cleanup, Stand all-hook
  close, runtime phased close, BoundedContext terminal coalesced dependency
  order, flattened error aggregation, and injected-failure/leak tests.
- Expected profile remains explicitly `gpt-5.6-terra` / medium; immutable role
  metadata applies and no mismatch is visible.
- This is the final implementation slice before whole-task focused coverage and
  mechanical pre-review checks.

## Continuation Slice D Assignment

- Existing role: fresh implementer `/root/t0115_failures_impl` because the
  prior implementation context closed after lifecycle code without completing
  its required proof matrix.
- Ownership: tests first for partial-build acquisition cleanup, terminal close
  order/coalescing/error flattening/exact-once hooks, gated reconciliation,
  observer and Stand-handle independent failures, and zero leaks; production
  edits only when a RED test exposes a defect.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Runtime metadata and result: pending.

## Continuation Slice E Assignment

- Existing implementer: `/root/t0115_failures_impl`, reusing the failure-test
  context.
- Ownership: the two missing deterministic failure proofs, all TSDoc violations
  introduced or exposed on T-0115 changed production declarations, and an
  expanded affected-server coverage run that reaches at least 90% branches for
  the complete changed-source set.
- Expected profile remains explicitly `gpt-5.6-terra` / medium; immutable role
  metadata applies and no mismatch is visible.
- No lifecycle acceptance item may be deferred as “broad remediation”; every
  changed public/internal declaration must pass the repository TSDoc gate.

## Continuation Slice E — 2026-08-05

- Immutable role metadata: existing implementer `/root/t0115_failures_impl`,
  explicitly dispatched `gpt-5.6-terra` / medium. Runtime self-introspection
  is unavailable; no visible profile mismatch occurred.
- Observer failure proof: a mocked first EventBus observer unsubscribe throws;
  runtime close still detaches the second observer, clears consumer admission,
  and aggregates the observer failure.
- TSDoc evidence: `pnpm lint:tsdoc` passed after completing changed
  BoundedContext, Stand, SubscriptionRuntime, and SubscriptionObservers
  declarations.
- Focused evidence: `pnpm exec vitest run packages/server/test/stand/stand.test.ts packages/server/test/stand/subscription-runtime.test.ts && pnpm exec tsc --noEmit --project packages/server/tsconfig.json` passed: 42 tests and server typecheck.
- Remaining: the required single terminal order/aggregation proof and expanded
  full affected-server >=90%-branch coverage have not yet run; this checkpoint
  must not be accepted as T-0115 completion.

## Continuation Slice F Assignment

- Existing role: fresh implementer `/root/t0115_terminal_impl`; the prior
  failure context closed after unsubscribe/TSDoc coverage without the composite
  terminal proof.
- Ownership: exactly one deterministic terminal-order, aggregation,
  coalescing, and zero-leak RED test plus the smallest production fix it
  exposes.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Expanded changed-source coverage and remaining mechanical commands run as
  orchestrator verification immediately after this behavior checkpoint.

## Expanded Verification Finding

- The first complete server coverage run passed 1,894 tests and failed one
  repository-routing fixture before coverage reporting. That fixture registered
  one dispatcher for both `AggregateState` and system
  `EntityStateChanged`, which the accepted strict bus-role preflight correctly
  rejects.
- This is a stale test fixture, not a contract ambiguity. Existing implementer
  `/root/t0115_terminal_impl` owns the mechanical split into separate domain and
  system dispatchers and reruns the affected test before the orchestrator
  repeats complete server coverage.
- Expected profile remains explicitly `gpt-5.6-terra` / medium; immutable role
  metadata applies.

## Expanded Verification Fixture Correction — 2026-08-05

- Immutable role metadata: existing implementer `/root/t0115_terminal_impl`, explicitly
  dispatched `gpt-5.6-terra` / medium. Runtime self-introspection is unavailable on this
  surface; the immutable dispatched profile is the available metadata and no mismatch was visible.
- RED evidence: `pnpm exec vitest run packages/server/test/repository/repository-routing.test.ts
--testNamePattern='does not expose process-manager state or follow-ups when its atomic commit
fails'` failed at context construction with `An EventDispatcher cannot mix domain and system
event schemas.`
- GREEN behavior: the stale fixture now declares separate domain `AggregateState` and System
  `EntityStateChanged` dispatchers that share its observation sink, preserving the assertion that
  neither follow-up dispatches after the forced atomic entity-commit failure. No production code
  changed.
- Focused evidence: the same named Vitest test passed; `pnpm exec tsc --noEmit --project
packages/server/tsconfig.json`, Prettier check, and `git diff --check` passed.

## Continuation Slice F — 2026-08-05

- Immutable role metadata: existing implementer `/root/t0115_terminal_impl`, explicitly
  dispatched `gpt-5.6-terra` / medium. Runtime self-introspection is unavailable on this
  surface; the immutable dispatched profile is the available metadata and no mismatch was visible.
- RED evidence: the new deterministic `BoundedContext` terminal test drained a gated accepted
  domain command that published `EntityStateChanged` through the paired System EventBus, then
  failed because `boundedContextAccess.systemPairing(context)` remained reachable after close.
- GREEN behavior: terminal close now removes all pair-owned internal context metadata after
  every owned resource is attempted. The composite proof records System delivery before domain
  command completion, domain EventStore before both Stands, both Stand hooks before registry,
  and registry before tenant storage. It injects independent domain-Stand, System-Stand, and
  registry failures; one `BoundedContext close failed` aggregate preserves those original error
  objects in encounter order. Repeated `close()` returns the same rejecting promise and object;
  registry close runs once; post-close command/event intake and internal pairing, tenant, storage,
  delivery, and subscription-registry access are unavailable.
- Focused evidence: `pnpm exec vitest run packages/server/test/context/bounded-context.test.ts
packages/server/test/stand/subscription-runtime.test.ts packages/server/test/stand/stand.test.ts
packages/server/test/stand/subscription-observer.test.ts packages/server/test/repository/repository.test.ts
packages/server/test/services/spine-services.test.ts && pnpm exec tsc --noEmit --project
packages/server/tsconfig.json` passed: 242 tests and server typecheck.
- Limitation: this slice deliberately did not run broad changed-source coverage or whole-task
  mechanical/review gates; those remain orchestrator-owned follow-up work.

## Pending Boundaries

- Fresh-worktree bootstrap and focused baseline.
- RED-GREEN implementation with focused coverage.
- Mechanical pre-review lint and one complete relevant specialist wave.
- One converged release gate, merge, post-merge verification, and remote
  synchronization before T-0116 starts.

## Changed-Behavior Coverage Correction — 2026-08-05

- Expanded server verification passed all 63 test files and 1,895 tests. The
  deliberately restricted whole-file report measured 85.71% branch coverage
  because it includes historical paths in seven large server files.
- Diff-scoped coverage measured changed statements at 222/236 (94.07%),
  changed functions at 58/63 (92.06%), and changed branches at 91/103
  (88.35%). The branch result is below the project threshold and requires a
  bounded test correction before review.
- Existing role: implementer `/root/t0115_failures_impl`.
- Ownership: tests only for uncovered changed branches in paired-context
  shutdown and `SubscriptionRuntime` terminal/error paths; production changes
  are permitted only if a focused RED test exposes a real defect.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Runtime self-introspection is unavailable on this surface; the immutable
  configured role/profile is the available runtime metadata. No visible
  mismatch has occurred.

## Mechanical Lint Correction — 2026-08-05

- Sequential tooling typecheck passes after explicitly typing the Spine Proto
  `Event` used by the new service test helper.
- ESLint reported 15 deterministic findings in newly changed files: one unused
  import and test-only expression, const, assertion, async, and unbound-method
  style findings. No runtime behavior failed.
- Existing role: implementer `/root/t0115_terminal_impl`.
- Ownership: resolve exactly this ESLint batch without altering behavior, run
  changed-file lint plus focused affected tests/typecheck, update this log,
  commit, and immediately push the feature branch.
- Expected and explicitly dispatched model: `gpt-5.6-terra`.
- Expected and explicitly dispatched reasoning: `medium`.
- Runtime self-introspection is unavailable; the immutable configured
  role/profile is the available metadata and no visible mismatch exists.
- GREEN evidence: changed-file ESLint passed after removing the stale Stand
  import and making test callbacks and assertions lint-safe without changing
  their behavior. Focused EventBus, BoundedContext, subscription-observer, and
  SpineServices tests passed: 222 tests. Server typecheck and `git diff --check`
  also passed.
- Formatting note: the two lint-touched test files pass Prettier. The preserved
  orchestrator-owned blank line in `subscription-observer.ts` remains outside
  this lint-only correction and is the sole changed-file Prettier warning.

## Review Correction Batch — 2026-08-05

- Immutable role metadata: existing implementer `/root/t0115_terminal_impl`,
  explicitly dispatched `gpt-5.6-terra` / medium. Runtime self-introspection is
  unavailable; no visible profile mismatch occurred.
- RED/GREEN: removed the wrong-bus observer compatibility entry point and moved
  state attachment behind the paired System Stand access boundary. Partial-build
  cleanup tests initially exposed asynchronously retained bus-owned storage; the
  internal EventBus abort-close and runtime-only registry ownership restore
  deterministic cleanup. A gated timer proof confirms three elapsed intervals
  produce one pending reconciliation; direct detach failure retains one registry
  close and one coalesced terminal error.
- Documentation: EventBus TSDoc now states public construction is domain-only
  and rejects System schemas, dispatchers, and events.
- Focused evidence: EventBus, BoundedContext, observer, runtime, Stand, and
  SpineServices tests passed 266 tests. Server typecheck, changed-file ESLint,
  TSDoc, Prettier, and `git diff --check` passed.

## Tooling Typecheck Fixture Correction — 2026-08-05

- Immutable role metadata: existing implementer `/root/t0115_terminal_impl`,
  explicitly dispatched `gpt-5.6-terra` / medium. Runtime self-introspection is
  unavailable; no visible profile mismatch occurred.
- The gated timer test registry now returns the underlying cleanup
  `StandCleanupResult` after its pending gate releases, preserving the intended
  coalescing proof without changing production behavior.
- Evidence: focused runtime test passed 4 tests; server typecheck,
  `pnpm typecheck:tooling`, changed-file ESLint, and `git diff --check` passed.

## Second Review Correction Batch — 2026-08-05

- Immutable role metadata: existing implementer `/root/t0115_terminal_impl`,
  explicitly dispatched `gpt-5.6-terra` / medium. Runtime self-introspection is
  unavailable; no visible profile mismatch occurred.
- Timer reconciliation now coalesces only periodic ticks; explicit work queues
  one complete snapshot after current accepted work. Partial-build cleanup
  catches synchronous abort failures, attempts every resource, and retains the
  original construction error first. Runtime close independently starts drain
  and registry close with synchronous throw capture.
- Focused evidence: affected EventBus, context, observer, runtime, Stand, and
  SpineServices tests passed 266 tests. Server/tooling typechecks, changed-file
  ESLint, TSDoc, Prettier, and `git diff --check` passed.

## Changed-Behavior Coverage Slice — 2026-08-05

- Immutable role metadata: existing implementer `/root/t0115_failures_impl`,
  explicitly dispatched `gpt-5.6-terra` / medium. Runtime self-introspection
  is unavailable; no visible mismatch occurred.
- RED/GREEN behavior: the runtime's shared-consumer branch and repeated
  consumer-handle unsubscribe branch are now exercised by two consumers for
  one subscription ID; no production change was required.
- Focused evidence: `pnpm exec vitest run packages/server/test/stand/subscription-runtime.test.ts --coverage --coverage.include='packages/server/src/stand/subscription-runtime.ts'` passed 3 tests. Its isolated report is below global thresholds by construction (28.20% branches) because it excludes the paired-context scenarios; the required aggregate diff-scoped rerun remains pending.
