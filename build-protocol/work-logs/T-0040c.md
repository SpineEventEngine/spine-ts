# T-0040c Work Log

Status: In progress - Wave 5 finding accepted; fix pending

Branch: `task/T-0040c-todo-readme-user-guide-closure`

Baseline: `526b7b4d`

## 2026-07-14 - Task Started

- Created the isolated worktree from remotely synchronized `main` at
  `526b7b4d`. `.worktrees/` is ignored.
- T-0040b is durably closed. Its native task and post-merge gates each passed 72
  files / 1,667 tests with 90.12% branch coverage; this is the inherited
  baseline before documentation-only changes.
- Recorded the complete human ledger, acceptance criteria, scope, exclusions,
  risks, deep-planning N/A, and verification boundary before doc inspection.
- Protected `human-review-1-jul.md` is outside this worktree and is not a task
  input.

## Skill Applicability

- Session inventory source: Desktop-provided skills list. Repository source:
  `build-protocol/skills/EXPECTED_SKILLS.md`. Readable installed skill
  entrypoints and `/Users/armiol/.agents/.skill-lock.json` are advisory sources.
- Selected: `using-git-worktrees`, `subagent-driven-development`,
  `doc-coauthoring`, `javascript-testing-patterns`, `nodejs-backend-patterns`,
  `requesting-code-review`, and `verification-before-completion`.
- Architecture/domain/API-design skills are N/A because this task documents the
  stable accepted surface. Reassess only for a proven public-contract blocker.
- No dependency search/addition is planned: use the accepted pnpm, Buf,
  Protobuf-ES, Connect, Vitest, server, and example scripts already present.

## Investigation Function

- Coordinator read-only scan; this is not a new role.
- Map every current README/user-guide command, path, import, link, feature claim,
  and limitation to repository scripts and T-0040b behavior. Identify the
  smallest executable documentation patch before implementation dispatch.

## Next

1. Dispatch the recorded Terra Medium implementation owner and capture actual
   immutable runtime metadata.
2. Independently execute every documented command, real server/smoke, focused
   native tests, docs/link/import/status lint, and generated-clean checks.

## 2026-07-14 - Documentation Coverage Map And Assignment

- Locked offline install passed for all eight workspace projects with 199
  reused packages and no dependency-record change.
- Current docs accurately describe the in-memory local server and core public
  clients, but all three README/guide test commands still target deleted
  `examples/todo/src/index.test.ts`. The example guide ends after the basic test
  section and does not close local multi-process, registry recovery, lifecycle,
  links, or limitations.
- Current inline smoke is 117 lines and leaves transport session ownership
  implicit. The example package has only `start`; no reusable smoke command
  exists.
- Assignment: existing immutable `implementer`, expected explicit
  `gpt-5.6-terra` / medium, no subagents/Git mutation. Add one minimal public
  `scripts/smoke.mjs` plus package `smoke`, rewrite the example README/guide,
  correct root README stale path/link context, and update only task/work logs.
- RED: `pnpm --filter @spine-ts/example-todo smoke` is absent; stale path scan
  finds three public-doc references. GREEN: exact build/start/smoke and focused
  test commands execute, links/imports resolve, generated output stays clean,
  and no future policy is claimed.
- Agent `019f6235-15d0-7022-a8dc-ac7579dffb0c`: explicit dispatch and
  immutable Desktop runtime profile `gpt-5.6-terra` / medium.

## 2026-07-14 - Implementer RED Evidence

- Before owned edits, `pnpm --config.verify-deps-before-run=false --filter
@spine-ts/example-todo smoke` exited 1 because the package had no `smoke`
  script (`ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT`).
- A public-doc scan found stale `examples/todo/src/index.test.ts` paths in root
  `README.md`, example `README.md`, and example `USER_GUIDE.md`. Historical
  task/work evidence remains intentionally unchanged.

## 2026-07-14 - Implementer Documentation And Smoke GREEN

- Changed only the assigned root/example docs, example package script, one
  checked-in `scripts/smoke.mjs`, and T-0040c task/work records. The existing
  untracked review record and all unrelated files remain untouched. No subagent
  or Git mutation was used.
- The README is now the short reader path: Node 24+/pnpm, build/generation,
  terminal-one server, terminal-two smoke, exact black-box and local
  multi-process test paths, demonstrated behavior, guide/package links, and
  local-only limitations. The user guide holds stable command/query/filter,
  bounded subscription cleanup, registry/lifecycle, local-multi-process,
  test, link, and limitation detail. It does not present a supervisor or CLI.
- `smoke` runs `node scripts/smoke.mjs` after the documented build. It uses
  public dependencies plus compiled example generated schemas, an explicit
  `Http2SessionManager`, bounded command/query operations, finite diagnostics,
  and `session.abort()` in `finally`. It posts one generated `CreateTask`,
  requires `Ack` status `ok`, observes the matching projected `TaskList`, and
  prints one concise success line; it has no subscription.
- RED was recorded before changes: the absent package script exited 1 with
  `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT`, and the public docs contained four stale
  `examples/todo/src/index.test.ts` references. The current public-doc scan has
  none; task/work records retain historical references only.
- Clean-state `pnpm --config.verify-deps-before-run=false typecheck:build`
  passed. Sandboxed loopback start and black-box commands failed only with the
  documented `listen EPERM`; sandboxed local multi-process failed only because
  its child local IPC setup was denied. Native reruns passed:
  `pnpm --config.verify-deps-before-run=false vitest run
examples/todo/test/black-box.test.ts` (1 file, 26 tests) and
  `pnpm --config.verify-deps-before-run=false vitest run
examples/todo/test/local-multi-process.test.ts` (1 file, 19 tests).
- Final documented native start/smoke succeeded and printed `to-do smoke ok`.
  After SIGINT stopped the server, `lsof -nP -iTCP@127.0.0.1:8080
-sTCP:LISTEN` returned no IPv4 listener. Separate existing IPv6 port-8080
  activity belonged to another workspace and was not touched; this is the only
  host-level uncertainty.
- Focused ESLint for the smoke, Prettier for every owned file, `docs:check`,
  `proto:check-generated`, Markdown link/import/path scans, and `git diff
--check` passed. Generated outputs remain ignored/untracked. Full `pnpm
verify` is intentionally deferred to the coordinator gate.

## 2026-07-14 - Coordinator Verification And Pre-Review Lint

- Independently ran the exact documented install, build, server, smoke, and
  focused-test commands. Install was already up to date; build passed; smoke
  observed its generated `TaskList`; black-box passed 26 tests; local
  multi-process passed 19 tests; and no `127.0.0.1:8080` listener remained.
- Mechanical checks passed: `typecheck:tooling`, full `lint`, `format:check`,
  `docs:check`, `proto:check-generated`, `git diff --check`, Markdown-link
  resolution, public stale-path/policy scans, and tracked-generated-output
  scans. Generated output remains ignored and untracked.
- Status lint now agrees across task, work, and review records. The changed
  package script contains no duplicated framework policy; the smoke adds no
  export and uses public package imports plus its own compiled generated
  messages; changed docs make no future-policy or production-topology promise.
- The implementation is ready for a baseline-to-endpoint commit and the four
  task-scoped specialist concerns. Full `pnpm verify` remains reserved for the
  final task and post-merge gates.

## 2026-07-14 - Specialist Wave 1

- Closed all four reviewers after collecting the complete wave. Reliability
  returned clean.
- Accepted one style correction for trailing Node imports, one API correctness
  correction for optional/mismatched query row state, and two documentation
  completeness corrections for executable query and subscription workflows.
- One fix batch returns to the original implementation context. It must preserve
  the concise README, keep full examples in the user guide, use only public
  imports, and rerun focused docs/smoke/type/format checks before re-review.

## 2026-07-14 - Specialist Wave 1 Fix Implementation

- Existing immutable implementer `019f6235-15d0-7022-a8dc-ac7579dffb0c`,
  explicit `gpt-5.6-terra` / medium, resumed with no subagents or Git mutation.
  Changed only `examples/todo/scripts/smoke.mjs`,
  `examples/todo/USER_GUIDE.md`, and these owned task/work records. Coordinator
  commits, the review log, and unrelated work remain untouched.
- The smoke's `node:console`, `node:process`, and `node:timers` imports now lead
  its import block. No executable behavior changed.
- The guide now provides a complete shared query client/request/decode setup
  for all rows, exact `StringValue` ID, and declared `open_task_count` equality
  filter shapes. `decodeTaskLists()` checks optional `row.state` before
  unpacking and excludes undefined/mismatched `unpackAny()` results.
- The guide now provides a complete bounded subscription client matching the
  accepted black-box lifecycle: generated `Topic`, public service clients,
  activation with `AbortController`, iterator read before command post,
  deadline-wrapped remote operations, update decoding, awaited cancel,
  iterator return, stream abort, and `Http2SessionManager.abort()` in outer
  cleanup. Both modules use public package imports plus compiled example
  schemas only.
- Fresh focused evidence:
  - `pnpm --config.verify-deps-before-run=false typecheck:build` passed after
    Proto generation and `tsc -b`;
  - `pnpm --config.verify-deps-before-run=false docs:check` passed with all 372
    expected curated package exports;
  - a VM module syntax check parsed both guide JS blocks;
  - the exact native package start/smoke passed and printed `to-do smoke ok`;
  - both extracted guide modules executed against that server: all/ID/column
    reads exited 0 and the subscription received its generated task update;
  - native `pnpm --config.verify-deps-before-run=false vitest run
examples/todo/test/black-box.test.ts` passed 1 file / 26 tests;
  - after server `SIGINT`, the exact IPv4 listener check returned no listener;
  - `proto:check-generated` passed with ignored, untracked, fresh output.
- No environment limitation remained for this batch. Native loopback approval
  was available; full `pnpm verify` remains the coordinator gate.

## 2026-07-14 - Coordinator Wave 1 Fix Verification

- Confirmed resumed implementer runtime metadata remains the existing immutable
  `gpt-5.6-terra` / medium profile and closed the agent after result capture.
- Independently extracted and executed both changed guide modules against the
  native server. Query all/ID/column reads exited 0; subscription delivery
  printed its generated task ID; package smoke exited 0.
- Server `SIGINT`, extracted-module cleanup, and listener inspection left no
  `127.0.0.1:8080` listener or temporary repository file.
- Fresh build, docs/API, focused ESLint, full format, generated-clean, and diff
  checks passed. No public export, dependency, lockfile, runtime, or generated
  source changed.

## 2026-07-14 - Specialist Wave 2

- Closed all four reviewers after the complete wave. Documentation returned
  clean.
- Deduplicated the style/API/reliability smoke reports into one accepted
  optional-`unpackAny` guard defect affecting target matching and diagnostics.
- Accepted one reliability defect in the guide subscription module: an
  `includeAll` topic can consume an unrelated update. The fix must use the
  command's precomputed task ID for the topic criterion and delivered-state
  assertion, and retain bounded failure if attachment races.
- Return both findings to the original implementation context as one batch with
  focused regression evidence. No architecture/public API change is needed.

## 2026-07-14 - Specialist Wave 2 Fix Implementation

- Existing immutable implementer `019f6235-15d0-7022-a8dc-ac7579dffb0c`,
  explicit `gpt-5.6-terra` / medium, resumed with no subagents or Git mutation.
  Changed only the smoke, one focused smoke test, the existing causal black-box
  test, the example guide, and owned task/work records. Coordinator/reviewer
  commits, review log, framework runtime, dependencies, config, and exports are
  untouched.
- Smoke TDD:
  - focused RED, `pnpm --config.verify-deps-before-run=false vitest run
examples/todo/test/smoke.test.ts`, failed 1/1 because row inspection returned
    `[undefined, valid TaskList]` after absent and mismatched rows;
  - GREEN passed 1/1 after one private `inspectTaskListRows()` loop skipped
    absent, mismatched/undefined, and malformed rows;
  - both target matching and timeout diagnostics consume that inspection. IDs
    remain sanitized and capped at four decoded rows, with finite unavailable
    and omitted-row counters. The five-second eventual deadline is unchanged.
- Subscription evidence TDD:
  - focused native RED changed the existing causal test to subscribe to a
    requested ID, start `next()`, project an unrelated row, then post the target;
    the old `includeAll` helper delivered `task-subscription-unrelated` instead
    of `task-standalone`;
  - GREEN passed 1 selected / 25 skipped after the helper used the public
    `StringValue`/`TargetFilters` exact-ID shape;
  - no readiness Ack was added. Starting `next()` before command post retains
    the real activation race, and the existing 500 ms deadline fails boundedly
    if attachment misses the target update.
- Guide correction precomputes `taskId`, uses that exact ID in its topic and
  generated `CreateTask`, and checks `list.id === taskId` before success. Public
  dependency imports, compiled example schemas, cancellation, iterator return,
  stream abort, and explicit HTTP/2 session cleanup remain intact.
- Fresh verification completed so far:
  - combined native focused suite passed 2 files / 27 tests;
  - `typecheck:tooling` and `typecheck:build` passed;
  - `docs:check` passed with all 372 expected curated package exports;
  - exact native package smoke printed `to-do smoke ok`;
  - the extracted exact-ID guide subscription printed its matching generated
    task ID and exited 0;
  - the server was stopped with `SIGINT`.
- Final focused ESLint, Prettier on every owned file,
  `proto:check-generated`, `git diff --check`, and the no-listener inspection
  passed. Generated output remains fresh, ignored, and untracked.
- No design or environment blocker exists. Full `pnpm verify` remains the
  coordinator gate.

## 2026-07-14 - Coordinator Wave 2 Fix Verification

- Confirmed the resumed implementer retained immutable `gpt-5.6-terra` /
  medium metadata and closed it after result capture.
- Native focused verification passed 2 files / 27 tests. The new smoke test
  proves unavailable rows are skipped with bounded diagnostics; the black-box
  test proves an unrelated projected row cannot satisfy the exact-ID stream.
- Extracted guide query/subscription modules and package smoke all exited 0
  against a fresh native server. Cleanup removed extracted modules, stopped the
  server, and left no IPv4 listener.
- Tooling/build typechecks, docs/API, focused ESLint, format, generated-clean,
  and diff checks passed. No framework runtime, public export, dependency,
  lockfile, config, or generated source changed.

## 2026-07-14 - Specialist Wave 3

- Closed all four reviewers after collecting the complete wave. TypeScript/API
  docs returned clean.
- Accepted one typed-test-boundary correction and missing malformed-wire
  regression for smoke row inspection.
- Accepted guide corrections for activation-setup cancellation, explicit
  save/run commands and prerequisites, and collision-resistant per-run IDs.
- Accepted one black-box timing correction: start the raw pending iterator read
  before unrelated/target posts, but apply the target-delivery timeout only
  after target post so setup work does not consume its budget.
- Return the complete batch to the original implementation context. No
  framework runtime, public API, dependency, or architecture change is needed.

## 2026-07-14 - Specialist Wave 3 Fix Implementation

- Existing immutable implementer `019f6235-15d0-7022-a8dc-ac7579dffb0c`,
  explicit `gpt-5.6-terra` / medium, resumed with no subagents or Git mutation.
  Coordinator/reviewer commits and the review log remain untouched.
- TDD RED: `pnpm vitest run examples/todo/test/smoke.test.ts` exited 1 because
  `../src/smoke-task-lists.js` did not exist. GREEN passed 1/1 after adding the
  typed package-private source seam and switching both checked-in smoke and
  test to it. The regression includes absent state, mismatched type, malformed
  matching-type bytes (`0xff`), and a valid later row; diagnostics count all
  three unavailable rows and retain the sanitized valid ID.
- Changed `examples/todo/src/smoke-task-lists.ts`, `scripts/smoke.mjs`,
  `test/smoke.test.ts`, `test/black-box.test.ts`, `USER_GUIDE.md`, and these two
  owned records only. No root export, framework/example domain runtime,
  dependency, lockfile, build config, or generated source changed.
- The guide modules have exact filenames and node commands plus build/server
  prerequisites. The subscription module uses one `randomUUID()` suffix for
  task/topic/command IDs and places activate/iterator creation inside the
  post-subscribe cleanup scope. Cleanup aborts/cancels before bounded pending
  read and iterator cleanup; outer `session.abort()` is unchanged.
- The causal native test now starts an unwrapped raw read before unrelated and
  target posts, applies delivery timeout only after the target post, and aborts
  plus cancels before awaiting a still-pending read during cleanup. It continues
  to prove the unrelated task was projected before exact-ID target delivery.
- Focused verification:
  - sandbox combined smoke/black-box run: smoke passed; five listener-backed
    cases failed only with `listen EPERM: operation not permitted 127.0.0.1`;
  - approved native `pnpm vitest run examples/todo/test/black-box.test.ts`:
    1 file / 26 tests passed;
  - `pnpm --config.verify-deps-before-run=false typecheck:build` and
    `typecheck:tooling`: passed;
  - focused ESLint and focused Prettier check: passed;
  - `pnpm --config.verify-deps-before-run=false docs:check`: passed, 372
    expected curated package exports;
  - `pnpm --config.verify-deps-before-run=false proto:check-generated`: passed,
    outputs ignored, untracked, and freshly regenerated.
- Native documented workflow passed: exact package start and smoke printed
  `to-do smoke ok` with a UUID-backed ID. Both JavaScript blocks extracted
  verbatim from the guide and piped to Node from `examples/todo/scripts`
  exited 0; query all/exact-ID/column shapes ran, and subscription delivery
  matched its UUID-backed exact ID. Server `SIGINT` completed and
  `lsof -nP -iTCP@127.0.0.1:8080 -sTCP:LISTEN` returned exit 1/no listener.
- Activation-setup cleanup was not separately fault-injected because the
  module is documentation-only and a redundant checked-in executable was
  expressly excluded. The code's cleanup scope covers every point after a
  successful subscribe, while native execution proves the success lifecycle.
  No readiness acknowledgement was invented. Full `pnpm verify` remains the
  coordinator gate.

## 2026-07-14 - Coordinator Wave 3 Fix Verification

- Confirmed the resumed implementer retained immutable `gpt-5.6-terra` /
  medium metadata and closed it after result capture.
- Native smoke/black-box verification passed 2 files / 27 tests. The typed
  package-private decoder covers absent, mismatched, malformed, and valid rows;
  the causal test keeps a raw read pending but begins its timeout after target
  post.
- Executed the guide's exact save/run filenames and commands plus package smoke
  against a fresh native server. All exited 0 with matching UUID-scoped IDs.
- Cleanup left no copied guide modules or IPv4 listener. Tooling/build types,
  docs/API, focused ESLint, format, generated-clean, and diff checks passed.

## 2026-07-14 - Specialist Wave 4

- Closed all four reviewers after the complete wave. Style returned clean.
- Accepted documentation correction: the query module must consume an existing
  projected task ID and tell the reader how to obtain/pass it.
- Accepted reliability corrections: cap inspected/retained response rows,
  report omitted rows, bound sanitizer input before iteration, test oversized
  input, and skip malformed matching-type rows in the guide decoder.
- Rejected the API request to export example generated/compiled internals. The
  executable files live inside the private example package and use relative
  paths to app-owned build output, while every framework import is a supported
  package export. A new package export would broaden the public contract and
  contradict task scope.
- Return the accepted batch to the original implementation context. No
  framework runtime, public API, dependency, or architecture change is needed.

## 2026-07-14 - Specialist Wave 4 Fix Implementation

- Existing immutable implementer `019f6235-15d0-7022-a8dc-ac7579dffb0c`,
  explicit `gpt-5.6-terra` / medium, resumed with no subagents or Git mutation.
  Coordinator/reviewer commits, unrelated work, and the review log remain
  untouched.
- Changed only `examples/todo/src/smoke-task-lists.ts`,
  `examples/todo/test/smoke.test.ts`, `examples/todo/USER_GUIDE.md`, and these
  two owned records. No package export, framework/example domain runtime,
  dependency, lockfile, build config, or generated source changed.
- Focused RED: `pnpm vitest run examples/todo/test/smoke.test.ts` ran three
  tests and failed the two new behaviors. It reported 100 retained rows instead
  of 16 and `hidden suffix` instead of `<blank>` after 1,000 leading controls.
- GREEN: the same command passed 3/3 after capping inspected/retained rows at
  16, independently capping diagnostic IDs at four, reporting 12 omitted
  diagnostic and 84 omitted response rows for the 100-row fixture, and slicing
  sanitizer input at 256 characters before iteration. Existing absent,
  mismatched, malformed matching-type, and valid fourth-row behavior remains
  covered.
- The guide's saved query workflow now runs package smoke first and passes its
  printed ID through required `SPINE_TODO_TASK_ID`. The module validates that
  input, uses UUID-scoped query IDs, exact-ID filters with the seeded ID, and
  asserts the seeded row appears in all-row, exact-ID, and
  `open_task_count = 1` results. Its decoder catches malformed matching-type
  bytes without changing app-owned relative imports.
- Native exact workflow evidence:
  - `pnpm --filter @spine-ts/example-todo start`: listened on
    `http://127.0.0.1:8080`;
  - `pnpm --filter @spine-ts/example-todo smoke`: exited 0 and printed
    `smoke-289ab6f7-2e60-4bde-bae2-b144b62ba764`;
  - exact saved query command with that `SPINE_TODO_TASK_ID`: exited 0 and all
    three result sets contained that row with `openTaskCount: 1`;
  - exact saved subscription command: exited 0 with matching UUID-backed task;
  - extracted query without the environment variable: expected exit 1 and the
    explicit `Set SPINE_TODO_TASK_ID` error;
  - the guide decoder extracted verbatim into a temporary synthetic check
    skipped absent, mismatched, and malformed matching-type rows and returned
    the valid row;
  - server `SIGINT`, temporary-file cleanup, and listener inspection left no
    copied module/check or `127.0.0.1:8080` listener.
- Focused verification passed: `typecheck:build`, `typecheck:tooling`, focused
  ESLint, focused Prettier, `docs:check` with all 372 expected curated exports,
  and `proto:check-generated` with ignored, untracked, freshly regenerated
  output. Final format/diff/status checks follow after record formatting. Full
  `pnpm verify` remains the coordinator gate.

## 2026-07-14 - Coordinator Wave 4 Fix Verification

- Confirmed immutable `gpt-5.6-terra` / medium metadata and closed the
  implementation context after result capture.
- Native focused suites passed 2 files / 29 tests. Row inspection is capped at
  16, diagnostics at four IDs, sanitizer input at 256 characters, and output at
  64 characters, with finite omission counters.
- Exact native seed/query/subscription workflow passed and proved the seeded
  task in all three query result shapes. Missing task-ID input failed before
  session creation.
- Cleanup left no saved modules or listener. Tooling/build types, docs/API,
  focused ESLint, format, generated-clean, and diff checks passed.

## 2026-07-14 - Specialist Wave 5

- Closed all four reviewers after the complete wave. Style, documentation, and
  TypeScript/API docs returned clean.
- Accepted one reliability finding: the guide's all-row and column-filter
  requests still have no response format limit and retain/log every decoded
  row. Add a finite ordered server response and an independent client cap with
  omission reporting plus oversized decoder evidence.
- Return this single bounded documentation correction to the existing
  implementation context. Framework runtime/public API/dependencies remain
  unchanged.
