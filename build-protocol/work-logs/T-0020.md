# T-0020 Work Log: Server Environment Context Assembly

Status: complete; integrated to main

Date: 2026-07-09

## Log

- `2026-07-09`: Started T-0020 after T-0019 completed and post-merge
  verification passed. Root worktree was clean except untracked
  `human-review-1-jul.md`, which remains untouched.
- `2026-07-09`: Spawned requirements splitter
  `019f4879-c12f-7602-8ff8-312ac8226c07`; it reported no blockers and selected
  `Server.add(BoundedContextBuilder)` lazy assembly with
  `ServerEnvironment.storageFactory` as the smallest first slice.
- `2026-07-09`: Closed splitter
  `019f4879-c12f-7602-8ff8-312ac8226c07` after consuming its report.
- `2026-07-09`: Recorded JVM inspection evidence from
  `spine-jvm-docs/spine-server-runtime-and-bounded-context.md` and created the
  T-0020 task, work, and review logs.
- `2026-07-09`: Planning checkpoint committed on `main` at
  `179245937493445082662bd5e04c6be3e4892519`; follow-up base-record commit is
  `66ac6deff1aadf312968eb5d7ea80880209b2d59`, which is the implementation and
  review base for the task branch.
- `2026-07-09`: Created worktree
  `.worktrees/T-0020-server-environment-context-assembly` on branch
  `task/T-0020-server-environment-context-assembly` from
  `66ac6deff1aadf312968eb5d7ea80880209b2d59`. The sandboxed attempt failed to
  create `.git/refs/...lock` with `Operation not permitted`; the escalated
  rerun succeeded.
- `2026-07-09`: Read the task brief, review log, `BUILD_PROTOCOL.md`,
  `CODE_QUALITY.md`, and JVM reference note sections for `Server`,
  `ServerEnvironment`, `BoundedContextBuilder`, and environment/storage
  wiring before editing code.
- `2026-07-09`: Read the applicable `implement`, `test-driven-development`,
  and `verification-before-completion` skills for this implementation role.
- `2026-07-09`: Implemented server-side builder assembly by letting `Server`
  accept `BoundedContextBuilder` values, building them during `start()` with
  `ServerEnvironment.storageFactory` as the default storage source, and
  cleaning up already-built contexts if a later builder fails before listener
  open.
- `2026-07-09`: Kept builder storage precedence local and explicit by adding a
  package-local bounded-context builder build hook instead of a new public
  assembly facade or process-wide environment singleton.
- `2026-07-09`: Updated server/user/API/architecture docs and
  `ServerEnvironment` TypeDoc wording to remove the stale deferred builder
  integration caveat and document the new environment-backed default storage
  rule.
- `2026-07-09`: Added focused server tests for constructor/add builder intake,
  environment-default storage selection, explicit builder storage override, and
  cleanup after later builder assembly failure.
- `2026-07-09`: Orchestrator reran worktree setup after implementer
  verification failures. Sandboxed `pnpm install --frozen-lockfile` failed with
  npm registry `ENOTFOUND`; escalated rerun succeeded. `pnpm proto:generate`
  and `pnpm --config.verify-deps-before-run=false typecheck:build` then
  populated ignored generated/build outputs needed by focused Vitest.
- `2026-07-09`: Fixed lint issues in the new server tests: direct boolean
  checks, synchronous no-op dispatcher, and no redundant record-storage cast.
- `2026-07-09`: Review round 1 reported two TypeScript/API docs P2 issues:
  `Server.add()` and `ServerOptions.contexts` leaked the private
  `ServerContext` alias in declarations, and their TypeDoc did not state that
  added builders assemble during `start()` with
  `ServerEnvironment.storageFactory` unless `withStorageFactory(...)` selected
  a local factory first.
- `2026-07-09`: Fixed the review round 1 API surface by inlining
  `BoundedContext | BoundedContextBuilder` in public `Server` declarations
  while keeping the internal alias private, and expanded the TypeDoc on
  `Server.add()` and `ServerOptions.contexts` to describe start-time builder
  assembly and storage-factory precedence.
- `2026-07-09`: TypeScript/API docs re-review found the code fixes clean, then
  requested review-log wording to avoid claiming the lane passed before
  re-review. Corrected the wording and ran a final TypeScript/API docs
  re-review, which reported clean.
- `2026-07-09`: Closed all T-0020 implementation and review agents after their
  roles completed.
- `2026-07-09`: Post-merge `pnpm --config.verify-deps-before-run=false verify`
  failed in `typecheck:tooling` because the failing test event dispatcher
  returned `undefined` where `Promise<void>` was required. Changed the no-op
  test dispatcher to return `Promise.resolve()`.
- `2026-07-09`: The next post-merge verify progressed through typecheck, lint,
  and format, then exposed expected sandbox `EPERM` listener/IPC failures and a
  real context/builder discrimination bug. Existing service tests pass
  structural fake contexts to `Server`; treating every non-`instanceof`
  `BoundedContext` value as a builder broke those tests. Replaced the check with
  a package-local `boundedContextAccess.isBuilder(...)` guard, so framework
  builders are built and all other entries are treated as already-built
  contexts.
- `2026-07-09`: Avoided using concurrent proto-generating checks after a
  parallel local verification attempt raced generated-output publishing. Reran
  `typecheck:build` by itself successfully.
- `2026-07-09`: Post-merge TypeScript/API reviewer found
  `boundedContextAccess.isBuilder(...)` was not a total guard because
  `WeakMap.has(...)` rejects `null` and primitives. Added an object/non-null
  check before consulting the WeakMap.
- `2026-07-09`: First total-guard edit failed `typecheck:build` because the
  guarded `object` value still needed a local `BoundedContextBuilder` cast for
  the WeakMap key; added the cast after the runtime object/null guard.
- `2026-07-09`: Post-merge security reviewer flagged structural non-builder
  contexts as a widened trusted boundary. This matches the pre-existing
  TypeScript/JavaScript `ServerOptions.contexts` behavior and existing service
  tests, not a new trust boundary from this fix. Sent for re-review after the
  total-guard fix.
- `2026-07-09`: Closed the first post-merge review agents, then ran targeted
  TypeScript/API and security re-reviews. Both reported clean: the type guard is
  total for unknown inputs, and the security concern is pre-existing context
  API behavior rather than a new regression.
- `2026-07-09`: T-0020 is complete on `main` after post-merge fixes, required
  review loops, and final full verification.

## Agents

| Agent                                  | Role                  | Status |
| -------------------------------------- | --------------------- | ------ |
| `019f4879-c12f-7602-8ff8-312ac8226c07` | requirements splitter | closed |

## Verification Evidence

- `git diff --check` passed.
- `npx vitest run packages/server/test/server/server.test.ts packages/server/test/context/bounded-context.test.ts`
  failed before test execution in this worktree because both suites could not
  resolve `@bufbuild/protobuf` from the local worktree module layout.
- `pnpm --config.verify-deps-before-run=false typecheck:build` failed while
  generating protos because `buf` was not on the command PATH for this
  worktree invocation:
  `Failed to start buf generate packages/proto/generated: spawnSync buf ENOENT`.
- `pnpm --config.verify-deps-before-run=false format:check` failed because
  `prettier` was not on the command PATH for this worktree invocation:
  `Failed to start Prettier: spawnSync prettier ENOENT`.
- `pnpm --config.verify-deps-before-run=false docs:check` failed for the same
  proto-generation reason as `typecheck:build`:
  `Failed to start buf generate packages/proto/generated: spawnSync buf ENOENT`.
- Direct `../../node_modules/.bin/tsc -b --pretty false` also failed in this
  worktree because generated/package dependencies are not resolved here,
  starting with missing `@bufbuild/protobuf/...` and generated proto modules.
- `pnpm install --frozen-lockfile` failed in the sandbox with npm registry DNS
  `ENOTFOUND`; escalated rerun passed and installed workspace dependencies.
- `pnpm proto:generate` passed and verified 25 copied Spine proto source file
  checksums.
- `pnpm --config.verify-deps-before-run=false typecheck:build` passed after
  generating protos and running `tsc -b`.
- `npx vitest run packages/server/test/server/server.test.ts packages/server/test/context/bounded-context.test.ts`
  passed: 2 files, 67 tests.
- `pnpm --config.verify-deps-before-run=false docs:check` passed with the known
  invalid `origin` TypeDoc warning and API export counts: 100 proto, 28 core,
  213 server, 17 storage, 17 transport, 3 testing.
- `pnpm --config.verify-deps-before-run=false format:check` initially failed
  only on this work log. `pnpm --config.verify-deps-before-run=false format`
  rewrote the log, and the rerun passed.
- `pnpm --config.verify-deps-before-run=false lint` passed after generating
  protos, running `tsc -b`, ESLint, and cleanup enforcement.
- Final branch verification after review fixes:
  - `pnpm --config.verify-deps-before-run=false typecheck:build` passed.
  - `pnpm --config.verify-deps-before-run=false format:check` passed.
  - `git diff --check` passed.
  - `npx vitest run packages/server/test/server/server.test.ts packages/server/test/context/bounded-context.test.ts`
    passed: 2 files, 67 tests.
  - `pnpm --config.verify-deps-before-run=false lint` passed, including cleanup
    enforcement.
  - `pnpm --config.verify-deps-before-run=false docs:check` passed with the
    known invalid `origin` TypeDoc warning and API export counts: 100 proto, 28
    core, 213 server, 17 storage, 17 transport, 3 testing.
- Post-merge verification:
  - `pnpm --config.verify-deps-before-run=false verify` failed before tests in
    `typecheck:tooling` with
    `packages/server/test/server/server.test.ts(238,23): error TS2322: Type 'undefined' is not assignable to type 'Promise<void>'.`
  - After fixing the test dispatcher return type, sandboxed
    `pnpm --config.verify-deps-before-run=false verify` passed typecheck, lint,
    and format, then failed in listener/IPC suites with `listen EPERM` /
    `Operation not permitted` and also exposed the context/builder
    misclassification bug:
    `TypeError: Builder access requires a BoundedContextBuilder instance.`
  - A parallel local `typecheck:build` / `lint` run raced two proto generators
    and failed to publish generated output with
    `Failed to publish generated output: ENOENT ... packages/proto/generated/spine/ui`;
    the sequential `typecheck:build` rerun passed.
  - `npx vitest run packages/server/test/services/spine-services.test.ts` run
    natively passed: 1 file, 97 tests.
  - Escalated `pnpm --config.verify-deps-before-run=false verify` passed:
    57 test files, 1093 tests, coverage 94.97% statements / 90.04% branches /
    98.14% functions / 94.99% lines, docs/API checks with the known invalid
    `origin` TypeDoc warning, proto lint, and generated-clean.
  - After the total-guard fix and targeted re-reviews, final escalated
    `pnpm --config.verify-deps-before-run=false verify` passed: 57 test files,
    1093 tests, coverage 94.97% statements / 90.05% branches / 98.14%
    functions / 94.99% lines, docs/API checks with the known invalid `origin`
    TypeDoc warning, proto lint, and generated-clean.
