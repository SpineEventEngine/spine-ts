# T-0157 Work Log

## 2026-08-11 - Startup

- Created the isolated task worktree from `origin/main@9ab83fbc` and installed
  the frozen workspace dependencies.
- Frozen scope comes from
  `build-protocol/planning/WAVE_9_LOGGING_ROUTING_PLAN.md`: descriptor-backed
  `(is)` / `(every_is)` provenance and registry lookup only.
- Implementation assignment: existing implementer, explicit
  `gpt-5.6-terra` / medium, no subagents. Runtime metadata is recorded when the
  surface exposes it; otherwise the configured immutable profile is evidence.
- Verification profile: bounded `verify:task` after one converged specialist
  review wave.

## 2026-08-11 - Core And Server Checkpoint

- Implementer `t0157_semantic_metadata` uses the configured existing profile
  `gpt-5.6-terra` / medium; runtime metadata is unavailable on this surface.
- `1c5c788b` added core descriptor `isTypes` / `everyIsTypes` lookup indexes
  while preserving compatibility-only caller `semanticTags`. `bea8bd0f` makes
  Entity descriptor metadata reuse the core registry extraction.
- Rebuilt focused tests pass: `pnpm typecheck:build:generated` followed by
  `pnpm exec vitest run packages/server/test/index.test.ts packages/core/test/index.test.ts`
  (2 files, 63 tests). Remaining provenance, malformed, dedup, immutability,
  order, and coverage evidence is still required before pre-review.

## 2026-08-11 - Mechanical Pre-Review Evidence

- Implementer `t0157_semantic_metadata`: configured `gpt-5.6-terra` / medium;
  runtime metadata is unavailable.
- `pnpm exec vitest run --coverage --coverage.reportsDirectory=/tmp/t0157-cov-3b01cc45 packages/core/test/index.test.ts packages/server/test/index.test.ts` passed
  2 files / 65 tests. Intersecting `/tmp/t0157-cov-3b01cc45/lcov.info` with
  `git diff --unified=0 origin/main...HEAD -- packages/core/src packages/server/src`
  gives statements 19/20 (95%), lines 19/20 (95%), branches 9/10 (90%), and
  functions 7/7 (100%).
- Build/tooling, changed ESLint, TSDoc, API docs, formatting, diff, and the
  compatibility impersonation scan pass. Status: mechanically pre-review-ready;
  review and `verify:task` remain pending.

## 2026-08-11 - Accepted Review Corrections

- Accepted all-lane correction batch: at the server descriptor boundary, core
  malformed semantic-option errors now become `DescriptorMetadataError` with
  `INVALID_SEMANTIC_TAG`; unrelated registry errors remain unchanged. The
  obsolete server `(is)` / `(every_is)` parser and its imports are removed.
- Entity semantic tags now combine descriptor provenance, deduplicate, sort,
  and freeze. A reverse-lexical fixture proves canonical ordering; a shared
  direct/file tag proves deduplication. Focused RED exposed both missing
  ordering and missing `DescriptorMetadataError` typing before the GREEN
  implementation.
- Core TSDoc now states that `RegisterTypeOptions.semanticTags` and
  `TypeMetadata.semanticTags` are caller-supplied compatibility-only metadata:
  they cannot populate or impersonate descriptor-backed provenance, lookups,
  or routing.
- Review lanes represented in the aggregate: style/maintainability and
  TypeScript/API documentation (`gpt-5.6-terra` / high), documentation/TSDoc
  (`gpt-5.6-luna` / medium), and performance/reliability (`gpt-5.6-terra` /
  high). Runtime metadata is unavailable on this surface; configured profiles
  are the durable evidence.
- Fresh focused evidence: `pnpm typecheck:build:generated`, then
  `pnpm exec vitest run packages/server/test/index.test.ts packages/core/test/index.test.ts --coverage --coverage.reportsDirectory=/tmp/t0157-cov-correction-20260811`
  passed 2 files / 67 tests. LCOV points intersecting production lines added
  relative to `origin/main` are statements/lines 23/24 (95.83%), branches
  13/14 (92.86%), and functions 6/6 (100%).
- `pnpm typecheck:tooling`, `pnpm lint:tsdoc`, `pnpm docs:api:check`, exact
  changed-TypeScript ESLint, Prettier, and `git diff --check` pass. Correction
  complete; targeted re-review is needed only for performance/reliability,
  style/maintainability, and TypeScript/API documentation. `verify:task`
  remains pending.

## 2026-08-11 - Typed Semantic-Option Discriminator

- Accepted style P2 replaces server message-prefix matching with the documented
  core `SemanticOptionErrors.isInvalid()` guard. The core keeps parsing shared;
  the server translates only the exact code-bearing core error into
  `DescriptorMetadataError` / `INVALID_SEMANTIC_TAG`. A same-prefix plain
  `Error` passes through unchanged.
- Focused RED failed for both the same-prefix propagation and missing typed
  guard; GREEN passed after adding the internal code-bearing error and its
  supported core guard. `pnpm typecheck:build:generated` then focused core and
  server tests passed 2 files / 69 tests. Fresh
  `/tmp/t0157-cov-typed-final-20260811/lcov.info` intersected with production
  lines added relative to `origin/main` gives statements/lines 29/29 (100%),
  branches 14/14 (100%), and functions 8/8 (100%).
- Documentation reviewer P3 is resolved by restoring the historical coverage
  command to one complete bullet line, eliminating its unindented continuation.
  This is formatting-only and does not reopen the documentation/TSDoc lane.
- Final mechanical gates pass: `pnpm typecheck:tooling`, `pnpm lint:tsdoc`,
  `pnpm docs:api:check`, exact changed-TypeScript ESLint, Prettier, and
  `git diff --check`.

## 2026-08-11 - Verifier Cleanup Correction

- Parent-provided verifier failures: one core test fixture line exceeded the
  length limit; the former public standalone guard violated semantic-name
  component and standalone-necessity cleanup rules.
- Replaced the function with frozen root export `SemanticOptionErrors` and its
  documented `isInvalid(value)` method. The code-bearing error remains private;
  server translation and tests retain exact typed discrimination. The encoded
  local core fixture is split without changing its bytes.
- Focused RED failed while the object export was absent; GREEN passed 2 files /
  69 tests. The verifier-only correction affects style/maintainability and the
  supported core API/TSDoc surface; behavior remains unchanged.
- Final gates pass: `pnpm lint:cleanup`, `pnpm typecheck:build:generated`,
  `pnpm typecheck:tooling`, `pnpm lint:tsdoc`, `pnpm docs:api:check`, exact
  changed-TypeScript ESLint, Prettier, and `git diff --check`.

## 2026-08-11 - Final Convergence And Verification

- Final targeted style/maintainability, TypeScript/API, and
  performance/reliability re-reviews are CLEAN. Documentation/TSDoc remained
  CLEAN after its formatting-only correction; security remains N/A.
- `pnpm verify:task -- --no-coverage packages/core/test/index.test.ts
packages/server/test/index.test.ts` passed every repository gate and 2 files /
  69 tests.
- T-0157 is complete and accepted for merge into `origin/main`.
