# Work Log: T-0011.6 Server Runtime Wiring Integration

## 2026-07-01 03:06 WEST

- Created subtask worktree
  `/Users/armiol/development/experiments/spine-ts/.worktrees/T-0011-6-server-runtime-wiring-integration`
  on branch `task/T-0011-6-server-runtime-wiring-integration` from parent
  branch `task/T-0011-transport-foundation` at `78346ab`.
- Confirmed `.worktrees` is ignored.
- Recorded the user-requested server-module guardrail already present in
  `build-protocol/BUILD_PROTOCOL.md`: any server-module work must inspect
  task-relevant Spine JVM `core-jvm/server` code and avoid over-engineering.
- Inspected task-relevant JVM notes and source:
  `spine-server-runtime-and-bounded-context.md`,
  `spine-routing-dispatch-and-delivery.md`, `BoundedContext.java`,
  `BoundedContextBuilder.java`, `CommandBus.java`, `EventBus.java`, and
  `Server.java`.
- Implementation impact: T-0011.6 is limited to a deterministic server-owned
  routing-plan seam from existing readiness metadata to transport topics and
  subscriptions. Runtime execution, buses, services, storage, delivery, and
  process supervision remain deferred.
- Sandboxed `corepack pnpm install --frozen-lockfile` was interrupted after
  registry DNS `ENOTFOUND` retries. Escalated frozen install passed with
  lockfile unchanged, reused 197 packages, and ran the approved `zeromq@6.5.0`
  install script.
- Baseline `CI=true corepack pnpm verify` passed with native IPC access: 23
  test files / 280 tests, coverage 96.16% statements / 90.48% branches /
  99.33% functions / 96.10% lines, TypeDoc/API counts 100 proto / 28 core /
  124 server / 26 storage / 46 transport, copied Spine proto checksums, proto
  lint/generate, and generated-clean all passed. Existing invalid-`origin`
  TypeDoc warning only.
- Spawned implementation sub-agent
  `019f1b72-d92b-77c3-bea5-b734213b1035` (`Curie the 7th`) for T-0011.6.
- Implementation sub-agent returned `STATUS: DONE` and commit `67a217b`; the
  orchestrator closed it after consuming the report.
- Spawned round 1 reviewers:
  `019f1b8a-31c5-7b21-b4dc-f34289c0ee8e` code style/maintainability,
  `019f1b8a-3245-7de0-a1b2-5fe37e75c387` documentation,
  `019f1b8a-32c0-70e0-a543-2e833cd85848` TypeScript/API,
  `019f1b8a-3342-7a72-b1c6-b32040073bc2` security, and
  `019f1b8a-33de-7bb0-829e-10d711f556c0` performance/reliability.
- Round 1 reviewers returned comments; all five reviewer agents were closed.
- Spawned fix sub-agent `019f1b8e-bc7f-73e3-a79e-53b93615b055` to address
  raw readiness metadata leakage, handler-derived identifiers, validation and
  copy-safety gaps, lossy ID collision risk, broad API surface, and docs
  wording/completeness findings.
- Fix sub-agent returned `STATUS: DONE` and commit `2375ee5`; the orchestrator
  closed it after consuming the report.
- Spawned round 2 reviewers:
  `019f1b9f-83c9-7ae0-ab53-d61e191ada51` code style/maintainability,
  `019f1b9f-8469-75e1-8745-4e94a46f4dea` documentation,
  `019f1b9f-84d0-76c3-ae45-be17e36aad26` TypeScript/API,
  `019f1b9f-8567-7520-a4cd-0ad64db5cd9c` security, and
  `019f1b9f-85da-7ef3-8a73-3d585d5be9ac` performance/reliability.
- Round 2 reviewers returned comments in maintainability, security,
  performance/reliability, and TypeScript/API; documentation was clean. All
  round 2 reviewer agents were closed.
- Spawned round 2 fix sub-agent `019f1ba4-1b69-79d0-ac8d-e90bdff91112` for
  proxy readiness rejection, tagged deterministic validation errors, slimmer
  public route shapes, and unexpected-export API guard enforcement.

## 2026-07-01 03:15 WEST

- Implementation sub-agent re-ran the canonical skill applicability check
  before code changes and fully re-read:
  `test-driven-development`, `typescript-advanced-types`,
  `javascript-testing-patterns`, `codebase-design`,
  `nodejs-backend-patterns`, and `verification-before-completion`.
- Selected direct implementation skills:
  `test-driven-development`, `typescript-advanced-types`,
  `javascript-testing-patterns`, `codebase-design`,
  and `verification-before-completion`.
- Reviewed but de-emphasized `nodejs-backend-patterns` because T-0011.6 is a
  metadata-only routing seam, not an HTTP/service-hosting runtime slice.
- Confirmed the implementation shape will stay narrow: one new server
  `runtime-routing` module that derives immutable transport plans from built
  bounded-context metadata plus command/event readiness, with query,
  subscription, and system routing recorded only as explicit deferred seams.

## 2026-07-01 03:33 WEST

- Added `packages/server/src/runtime-routing.ts` and
  `packages/server/src/runtime-routing.test.ts`.
- Exported `createServerRuntimeRoutingPlan()` from `@spine-ts/server`, added
  the `@spine-ts/transport` workspace dependency, updated export guards/tests,
  and refreshed package/API/architecture docs for the new seam.
- RED: `corepack pnpm exec vitest run packages/server/src/runtime-routing.test.ts`
  failed with 4/4 tests red because `createServerRuntimeRoutingPlan` was not
  implemented yet.
- GREEN: `corepack pnpm exec vitest run packages/server/src/runtime-routing.test.ts packages/server/src/index.test.ts`
  passed with 2 files / 16 tests.
- `corepack pnpm typecheck` passed.
- `corepack pnpm docs:check` passed; TypeDoc emitted the existing invalid-`origin`
  warning only and now reports 134 expected `@spine-ts/server` exports.
- `git diff --check` passed.
- Escalated `CI=true corepack pnpm verify` passed with native IPC access:
  24 test files / 286 tests, coverage 95.99% statements / 90.14% branches /
  99.38% functions / 95.93% lines, TypeDoc/API checks, proto lint/generate,
  and generated-clean all passed.

## 2026-07-01 03:56 WEST

- Reworked `packages/server/src/runtime-routing.ts` to emit only sanitized
  public route descriptors plus transport contracts: planner-local route IDs,
  planner-local worker IDs, receiver groups, and message full type names/type
  URLs. Removed raw readiness metadata from public routes.
- Narrowed runtime-routing inputs to concrete
  `CommandRegistrationReadiness` / `EventRegistrationReadiness` instances and
  added explicit malformed-metadata rejection for handler kind,
  message-full-type, and schema/type-name mismatches.
- Replaced handler/entity-derived event worker IDs and the server-local
  logical-name normalizer with deterministic indexed planner-local IDs to
  remove handler leakage and lossy-ID collisions.
- Trimmed the root `@spine-ts/server` API by removing intermediate route-flavor
  re-exports, updated `scripts/check-api-docs.mjs`, and refreshed
  `packages/server/README.md`, `docs/api/README.md`, and
  `docs/architecture/README.md`.
- Focused GREEN: `corepack pnpm exec vitest run packages/server/src/runtime-routing.test.ts packages/server/src/index.test.ts`
  passed with 2 files / 18 tests.
- `corepack pnpm typecheck`, `corepack pnpm docs:check`, and
  `git diff --check` passed after the fix.
- Escalated `CI=true corepack pnpm verify` passed with native IPC access:
  24 test files / 288 tests, coverage 95.85% statements / 90.01% branches /
  99.38% functions / 95.79% lines, TypeDoc/API counts 100 proto / 28 core /
  130 server / 26 storage / 46 transport, copied Spine proto checksum
  verification, proto lint/generate, and generated-clean all passed. Existing
  invalid-`origin` TypeDoc warning only.

## 2026-07-01 04:16 WEST

- Reworked `packages/server/src/runtime-routing.ts` again to reject
  Proxy-wrapped concrete readiness instances before `instanceof` checks or
  readiness method calls, closing the remaining proxy-trap/raw-exception gap
  with deterministic `TypeError` failures.
- Replaced the prefix-based preserve list inside
  `withDeterministicValidation()` with a tagged internal
  `DeterministicValidationError`, so targeted validation errors no longer rely
  on message prefixes to survive wrapping.
- Slimmed public command/event routes to planner-local IDs, receiver groups,
  sanitized message descriptors, and correlation keys back to the plan-level
  topics/subscriptions/workers. Full transport contracts now stay only on the
  plan-level arrays.
- Tightened `scripts/check-api-docs.mjs` so `docs:check` now rejects
  unexpected `@spine-ts/server` root exports in addition to missing expected
  TypeDoc exports, and refreshed package/API/architecture docs for the new
  route/reference wording.
- RED: `corepack pnpm exec vitest run packages/server/src/runtime-routing.test.ts`
  failed with 4 route/proxy regressions before the fix.
- GREEN: `corepack pnpm exec vitest run packages/server/src/runtime-routing.test.ts packages/server/src/index.test.ts`
  passed with 2 files / 20 tests after the fix.
- `corepack pnpm typecheck`, `corepack pnpm docs:check`, and
  `git diff --check` passed after the round-two patch.
- Escalated `CI=true corepack pnpm verify` passed with native IPC access:
  24 test files / 290 tests, coverage 95.88% statements / 90.05% branches /
  99.38% functions / 95.82% lines, TypeDoc/API counts 100 proto / 28 core /
  130 server / 26 storage / 46 transport, copied Spine proto checksum
  verification, proto lint/generate, and generated-clean all passed. Existing
  invalid-`origin` TypeDoc warning only.
- Spawned round 3 reviewers:
  `019f1bb1-4199-7622-b336-7af5528a4a69` code style/maintainability,
  `019f1bb1-4222-74e2-8370-b33d6f59614e` documentation,
  `019f1bb1-42a9-79a3-a87d-d540039f84d0` TypeScript/API,
  `019f1bb1-431e-77b0-8957-db45498ee065` security, and
  `019f1bb1-43a0-7240-a6e1-b866b82e959b` performance/reliability.
- Round 3 documentation reviewer found one wording mismatch in
  `packages/server/README.md`: readiness inputs were described as required
  even though they are optional. Updated the README to describe optional
  concrete readiness and empty command/event plans when readiness is omitted.
- `corepack pnpm docs:check` and `git diff --check` passed after the README
  wording fix.
- Round 3 security reviewer found a remaining high issue: prototype-forged
  readiness objects can pass the `instanceof` gate and override readiness
  methods before deterministic validation runs.
- Spawned focused security fix sub-agent
  `019f1bb5-32f2-7903-9cca-b7233af4889b` to add an unforgeable readiness
  authenticity gate and update runtime-routing tests.
- Focused security fix sub-agent returned `STATUS: DONE` and commit
  `450c16f`; the orchestrator closed it after consuming the report.
- The fix added module-local authenticity tokens and `WeakSet` tracking to
  command/event readiness classes, internal authenticity guards used by
  runtime routing before readiness method calls, and regressions proving
  prototype-forged overrides and proxy traps do not run.
- Focused verification from the fix sub-agent passed:
  `corepack pnpm exec vitest run packages/server/src/runtime-routing.test.ts packages/server/src/index.test.ts`
  passed with 21/21 tests, `corepack pnpm typecheck` passed,
  `corepack pnpm docs:check` passed, and `git diff --check` passed.
