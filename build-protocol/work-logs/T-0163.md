# T-0163 Work Log

## 2026-08-11 - Startup

- Starts from `origin/main@e59095f1` after T-0162 field-stringifier closure.
- Classification: high-risk public decorator plus generated metadata and
  repository selection.
- Frozen planning used the existing requirements splitter profile
  `gpt-5.6-sol` / high. The primary orchestrator performs the existing bounded
  implementer function at `gpt-5.6-terra` / medium, without implementation
  subagents. Runtime metadata is not exposed.
- TDD is active. The first RED will cover the missing public decorator and exact
  analyzer declaration before runtime selection changes.
- Product Markdown remains assigned to Wave 10.

Status: inspecting handler-generation and execution seams; preparing RED.

## 2026-08-11 - Public Declaration Checkpoint

- The focused RED reached both intended boundaries: `Where` was absent from the
  public server API, and analyzed handlers carried no filter declaration.
- `WhereOptions` and `Where()` now expose the frozen method-decorator shape.
  Runtime decorator use validates an exact option object and public instance
  method shape without invoking the handler.
- The build analyzer accepts one exact object-literal declaration on
  Event-consuming `Subscribe`, `React`, or `Command` methods and records the two
  literal values. Unsupported/static declaration cases are the next adversarial
  matrix.
- Focused decorator/analyzer tests pass 41/41. Generated build, changed-source
  ESLint, formatting, and diff checks pass.

Status: declaration checkpoint ready; generated rendering/ingestion, path
validation, runtime selection, and adversarial coverage remain.

## 2026-08-11 - Filter Runtime Checkpoint

- Generated registry rendering and ingestion now preserve detached, frozen
  `WhereOptions` metadata and reject malformed or unsupported records.
- The internal filter compiler resolves Proto source-name paths, converts the
  terminal field with `Stringifiers.forField()`, validates one path, unique
  canonical values, and at most one fallback, and selects a filtered receptor
  before its fallback.
- A real Projection dispatch test observed the intended RED (both handlers ran)
  and is GREEN after repository selection uses the compiled plan. The same
  repository seam now selects Event subscribers, Event reactors, and
  Event-to-command reactors.
- Generated build and 49 focused tests pass. Changed-source formatting, lint,
  and diff checks are the checkpoint gate.

Status: safe pushed checkpoint pending. Remaining work is the analyzer
adversarial matrix, full Aggregate/Process Manager runtime coverage,
construction-conflict coverage, mechanical/coverage gates, specialist review,
corrections, final verification, and merge.

## 2026-08-11 - Implementation Convergence

- The analyzer now proves exact static declarations, namespace imports, every
  invalid literal/object form, duplicate decorators, and every excluded handler
  role, including descriptor-marked Entity-state subscriptions.
- Generated registry ingestion rejects null, arrays, primitives, inherited
  shapes, malformed keys, and unsupported handler roles through the stable
  ingestion error contract.
- Real dispatch tests prove selection for Projection subscribers, Aggregate
  Event reactors, Process Manager Event reactors, and Process Manager
  Event-to-command reactors. Each receptor category selects independently.
- Construction-time compiler coverage includes nested and missing paths,
  message-valued terminals, repeated/map and scalar-intermediate rejection,
  conflicting paths, canonical duplicate values, and multiple fallbacks.
- Multiple ordinary unfiltered handlers remain valid and all run. The
  single-fallback constraint applies when filtered handlers exist.
- Generated build and the complete six-file focused suite pass: 332/332 tests.

Status: implementation-converged; mechanical, coverage, review, correction,
final verification, and integration gates remain.

## 2026-08-11 - Mechanical And Coverage Convergence

- `pnpm typecheck:build:generated` and the complete six-file focused suite pass:
  333/333 tests.
- The new filter module is covered at 96.25% statements, 93.47% branches, 100%
  functions, and 97.10% lines.
- Intersecting the fresh LCOV report with executable lines and branches changed
  from `origin/main` gives 95.00% statements/lines (114/120), 90.48% branches
  (95/105), and 96.55% functions (28/29). This is the established bounded
  changed-range measurement for changes inside the large shared repository
  module.
- Tooling typecheck, cleanup lint, TSDoc lint, API documentation inventory,
  logging containment, scoped ESLint/Prettier, and `git diff --check` pass.
- The required callable `Where()` decorator factory is recorded in the
  standalone-function necessity manifest.

Status: deterministic convergence complete; ready for the single specialist
review wave.

## 2026-08-11 - Review Correction And Final Verification

- Concern-specific review found two P1 contract gaps: filter conversion did
  not consume a configured `StringifierRegistry`, and hostile generated
  command-input metadata could carry `where`.
- RED tests reproduced both gaps. Repositories now snapshot an optional shared
  registry and pass it to all Event receptor filter plans; custom message
  mappings therefore govern filter literals and runtime values exactly as they
  govern storage/query conversion when the application supplies the same
  source registry.
- Generated ingestion now accepts filters only for Event/rejection schema
  provenance and rejects command-input reactors through the stable ingestion
  error contract.
- Focused snapshot tests prove later mutation of the caller registry cannot
  change a built filter plan. Repository construction proves the public option
  reaches message-valued filter compilation.
- Generated build and the complete focused suite pass: 6 files, 336/336 tests.
  Tooling typecheck, cleanup/TSDoc/API checks, logging containment, scoped
  ESLint/Prettier, and diff check pass.
- The single final `verify:task` invocation passed its Proto workflow, clean
  generated build, focused tests, and bounded filter-source coverage gate.

Status: accepted, final-verified, and ready for integration into `origin/main`.

## 2026-08-11 - Message Board Integration Correction

- T-0166's first natural `@Where` example exposed that Protobuf-ES runtime
  descriptors normalize `file.name` by removing `.proto`, while generated
  registry ingestion recognized only the authored filename form.
- The emitted handler metadata was otherwise exact. A generated-shape RED test
  reproduced the rejection for `task_events` and `task_rejections`.
- The correction accepts only `events` / `*_events` and `rejections` /
  `*_rejections`, with or without the authored `.proto` suffix. Misleading
  normalized names such as `notevents` remain rejected.

### Correction verification

- Focused generated-registry tests pass 24/24.
- Source coverage is 97.46% statements, 94.25% branches, 100% functions, and
  97.36% lines.
- Generated build, ESLint, cleanup, TSDoc, formatting, and diff checks pass.
- Local affected-concern review is clean; the current execution policy did not
  permit spawning independent reviewer subagents.

Status: correction accepted; integration pending.
