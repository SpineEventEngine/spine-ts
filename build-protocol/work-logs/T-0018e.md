# Work Log: T-0018e Runtime Metadata And Registry Docs Closure

Status: complete; integrated to `main`

Task:
`build-protocol/tasks/T-0018e-runtime-metadata-registry-docs-closure/TASK.md`

Review log:
`build-protocol/reviews/T-0018e-runtime-metadata-registry-docs-closure.md`

Branch: `task/T-0018e-runtime-metadata-registry-docs-closure`

Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0018e-runtime-metadata-registry-docs-closure`

## Activity

- `2026-07-09`: Requirements splitter
  `019f482d-f4d7-7960-85ce-5dff71711f84` selected this task as the next
  non-blocked slice after T-0018d and was closed by root.
- `2026-07-09`: Created durable task, review, and work-log records before
  branch/worktree creation and implementation.
- `2026-07-09`: Committed planning checkpoint on `main` as `ae4a097`; this is
  the base commit for the task worktree.
- `2026-07-09`: Created branch/worktree
  `task/T-0018e-runtime-metadata-registry-docs-closure` at
  `.worktrees/T-0018e-runtime-metadata-registry-docs-closure`. The first
  sandboxed `git worktree add` attempt failed because `.git/refs/...lock` could
  not be created under sandbox restrictions; the rerun with explicit escalation
  succeeded.
- `2026-07-09`: Scanned the target public docs and durable logs for stale
  wording around runtime metadata generation, generated-registry closure,
  descriptor-derived roles, explicit-`void` `@React`, semantic tags, and
  example/test adoption before editing.
- `2026-07-09`: Updated the public docs closure in `README.md`,
  `docs/USER_GUIDE.md`, `docs/architecture/README.md`, `docs/api/README.md`,
  `packages/server/README.md`, `examples/todo/README.md`, and
  `examples/todo/USER_GUIDE.md` so they match the implemented T-0018a through
  T-0018d contract and call out semantic-tag runtime consumption as the
  remaining gap.
- `2026-07-09`: `format:check` initially failed before verification because the
  worktree had no `node_modules` tree, so Prettier was unavailable. A temporary
  `node_modules` symlink exposed a deeper resolution mismatch; after removing
  it, `pnpm install` completed successfully in the worktree and unblocked fresh
  verification.
- `2026-07-09`: Fresh verification after dependency install recorded
  `format:check` pass, `git diff --check` pass, `typecheck:build` pass, and a
  `docs:check` failure in the TypeDoc/API-doc pass due workspace module
  resolution errors unrelated to the docs-only edits.
- `2026-07-09`: Root reran `docs:check` in the task worktree after dependency
  installation settled; it passed with the existing TypeDoc origin warning only.
  Root corrected the review log to record the required independent review lanes
  as pending because implementation self-review does not satisfy the protocol.
- `2026-07-09`: Addressed the TypeScript/API-doc reviewer finding that
  `docs/api/README.md` and `packages/server/README.md` made
  descriptor-derived command/event roles sound like generated-registry record
  fields. Rephrased both generated-registry contract passages so the registry
  records carry their actual fields and descriptor-role derivation/validation is
  described as build-time analyzer behavior before registry writing.
- `2026-07-09`: Ran focused verification for the reviewer fix:
  `pnpm --config.verify-deps-before-run=false docs:check` passed with the
  existing TypeDoc invalid-origin warning,
  `pnpm --config.verify-deps-before-run=false format:check` passed, and
  `git diff --check` passed.
- `2026-07-09`: Required independent review lanes completed. Code style,
  documentation, security, and performance/reliability were clean in round 1.
  The TypeScript/API-docs lane found one generated-registry wording issue; the
  authoring agent fixed it, and the TypeScript/API-docs re-reviewer confirmed
  the lane clean.
- `2026-07-09`: Root ran final task-branch verification after all review lanes:
  targeted stale-wording scan, `git diff --check`,
  `pnpm --config.verify-deps-before-run=false format:check`,
  `pnpm --config.verify-deps-before-run=false docs:check`, and
  `pnpm --config.verify-deps-before-run=false typecheck:build`.
- `2026-07-09`: Committed task branch as `d7cb7b3` and merged it to `main`
  with merge commit `Merge T-0018e runtime metadata docs`.
- `2026-07-09`: Post-merge `pnpm --config.verify-deps-before-run=false verify`
  first failed inside the sandbox because loopback and ZeroMQ local IPC were not
  permitted. Rerunning the same command with explicit escalation passed the full
  suite.

## Decisions

- Keep this slice documentation-only unless review finds a direct contract
  mismatch that cannot be resolved by documentation.
- Use the slice to close stale public documentation for T-0018a through
  T-0018d and clearly identify semantic-tag runtime consumption as the next
  functional gap.
- Keep generated files untouched and limit example doc edits to wording that
  matches the existing runnable in-memory gRPC/query/subscription app and its
  freshness checks.
- Preserve the generated-registry record shape in docs: entity type, state
  schema, `kind`, `methodName`, `signalSchema`, `emittedSchemas`, and
  `parameterCount` are registry data; descriptor-derived command/event role
  inference and validation happen during build-time analysis before records are
  written.
- Treat self-review from an implementation agent as useful context only; the
  protocol gate is satisfied by independent reviewer lanes and any required
  re-review after fixes.

## Verification

- Targeted `rg` scans used:
  - `rg -n "runtime metadata|metadata generation|semantic-tag|semantic tag|generated registry|generated-registry|descriptor-derived|descriptor derived|void.+React|@React|@Subscribe|@Apply|manual.*transaction|example/test adoption|adoption|missing|deferred" ...`
    across the target public docs plus the T-0018e review/work logs. This
    showed the remaining stale areas in `docs/USER_GUIDE.md`,
    `docs/architecture/README.md`, `docs/api/README.md`,
    `packages/server/README.md`, and the example docs.
  - `rg -n "semantic tags?|every_is|\\(is\\)|generated registry|GeneratedRegistry|HandlerRegistryIngestor|void.*React|@React|@Subscribe|materializeDecoratedEntityHandlers|@Apply|withGeneratedRegistryRoot|SignalMetadata" packages/server examples/todo build-protocol -g '!**/generated/**'`
    to confirm the implementation contract before editing. This surfaced the
    relevant source/tests/review notes for generated-registry roles, explicit
    `void` `@React`, example freshness checks, and `SignalMetadata`.
  - `rg -n "semantic.*registr|tag.*registr|descriptor-derived|emitted schemas|return explicit void|no emitted schemas|bare decorators|schema-bearing" packages/server/test examples/todo/src build-protocol -g '!**/generated/**'`
    to anchor the wording against tests and prior task logs. This confirmed the
    remaining semantic-tag registry gap and the current emitted-schema rules.
- `pnpm --config.verify-deps-before-run=false docs:check`
  - Initially failed during the TypeDoc/API-doc step with workspace module
    resolution errors after worktree dependency setup.
  - Root rerun after dependency installation passed with the existing TypeDoc
    origin warning only.
- `pnpm --config.verify-deps-before-run=false typecheck:build`
  - Passed (`tsc -b` exited 0 after proto generation).
- `pnpm --config.verify-deps-before-run=false format:check`
  - Passed (`All matched files use Prettier code style!`).
- `git diff --check`
  - Passed.
- Reviewer-fix verification:
  - `pnpm --config.verify-deps-before-run=false docs:check`
    - Passed with the existing TypeDoc warning that the `origin` remote is not
      valid for source links.
  - `pnpm --config.verify-deps-before-run=false format:check`
    - Passed (`All matched files use Prettier code style!`).
  - `git diff --check`
    - Passed.
- Final task-branch verification:
  - Targeted scan
    `rg -n "descriptor-derived command/event signal roles|contains descriptor-derived|with descriptor-derived|record members" README.md docs packages/server examples/todo build-protocol`
    returned only the expected review-log line describing the fixed issue.
  - `git diff --check`
    - Passed.
  - `pnpm --config.verify-deps-before-run=false format:check`
    - Passed (`All matched files use Prettier code style!`).
  - `pnpm --config.verify-deps-before-run=false docs:check`
    - Passed. TypeDoc reported the existing invalid `origin` warning and zero
      errors; API export check reported the expected exports for all packages.
  - `pnpm --config.verify-deps-before-run=false typecheck:build`
    - Passed (`tsc -b` exited 0 after proto generation).
- Independent review:
  - Code style/maintainability reviewer
    `019f483a-1078-73f0-b2fc-468804a2285d`: clean.
  - Documentation reviewer `019f483a-3758-7890-8bbf-0bc6de0083a4`: clean.
  - TypeScript/API docs reviewer
    `019f483a-72a7-7d03-a066-2af44867351b`: one finding fixed in round 1.
  - TypeScript/API docs re-reviewer
    `019f4840-3ae8-7972-94e8-d72dd699385f`: clean.
  - Security reviewer `019f483a-a70d-7f71-8078-fe8d9c9303ef`: clean.
  - Performance/reliability reviewer
    `019f483a-cea6-7ae1-bc23-9361f2b3ce21`: clean.
- Post-merge verification on `main`:
  - Initial sandboxed `pnpm --config.verify-deps-before-run=false verify`
    failed with expected environment permission errors for local listener and
    IPC tests: `listen EPERM: operation not permitted 127.0.0.1` and ZeroMQ IPC
    `Operation not permitted`.
  - Escalated `pnpm --config.verify-deps-before-run=false verify`
    - Passed.
    - Unit/integration tests: 57 test files passed; 1088 tests passed.
    - Coverage: 95.05% statements, 90.13% branches, 98.19% functions, 95.07%
      lines.
    - TypeDoc/API export checks passed with the existing invalid `origin`
      source-link warning only.
    - Proto lint passed.
    - Generated proto outputs were ignored, untracked, and freshly regenerated.

## Participants

- Requirements splitter:
  `019f482d-f4d7-7960-85ce-5dff71711f84`; completed and closed by root.
- Implementation agent: `019f4832-45c2-7f61-8fe3-f94f7b1b065b`; completed
  implementation, fixed the TypeScript/API-docs reviewer finding, and was
  closed by root.
- Code style/maintainability reviewer:
  `019f483a-1078-73f0-b2fc-468804a2285d`; closed by root.
- Documentation reviewer: `019f483a-3758-7890-8bbf-0bc6de0083a4`; closed by
  root.
- TypeScript/API docs reviewer:
  `019f483a-72a7-7d03-a066-2af44867351b`.
- TypeScript/API docs re-reviewer:
  `019f4840-3ae8-7972-94e8-d72dd699385f`; closed by root.
- Security reviewer: `019f483a-a70d-7f71-8078-fe8d9c9303ef`; closed by root.
- Performance/reliability reviewer:
  `019f483a-cea6-7ae1-bc23-9361f2b3ce21`; closed by root.
