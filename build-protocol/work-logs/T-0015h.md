# T-0015h Work Log

## 2026-07-08 01:05 WEST

- Started from branch `task/T-0015h-generated-registry-closure` at baseline
  `0b6abf9` in worktree
  `/Users/armiol/development/experiments/spine-ts/.worktrees/T-0015h-generated-registry-closure`.
- The prompt-listed T-0015h task/review/work-log files were absent in this
  worktree, so this pass created them while using the prompt, T-0015g durable
  logs, and current repository state as the task contract.
- Ran the requested stale-pattern scans across active docs, packages, examples,
  and build-protocol history. Historical build-protocol files contain expected
  old-task references, but active example source had no forbidden generated
  registry regressions.
- Inspected `scripts/check-cleanup-rules.mjs`, `scripts/check-cleanup-rules.test.mjs`,
  `docs/api/README.md`, `docs/architecture/README.md`, `docs/USER_GUIDE.md`,
  `packages/core/README.md`, `packages/server/README.md`, and
  `examples/todo/README.md`.
- Found one high-value guardrail gap: `scripts/check-cleanup-rules.mjs`
  rejected end-user example `materializeDecoratedEntityHandlers()` usage but
  did not reject direct `defineEntityHandlers()` calls.
- Added cleanup enforcement for `defineEntityHandlers()` in end-user example
  source, including direct named imports and namespace/property access. Updated
  focused checker coverage to assert the new failure.
- Updated current docs so low-level envelope packing, explicit handler
  metadata, and schema-bearing/materializer APIs are described as framework,
  test, or legacy seams, while ordinary application docs point to bare
  decorators plus generated registry discovery.
- Verification:
  - `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs` passed:
    1 file, 86 tests.
  - `corepack pnpm typecheck:build` passed.
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm lint` passed, including cleanup enforcement.
  - `corepack pnpm format:check` passed.
  - `git diff --check` passed.
- Confirmed generated output remains ignored and uncommitted under
  `docs/api/reference/`, package `dist/` folders, and package/example
  `generated/` folders.
- Committed the closure pass. The exact branch HEAD is reported by the
  implementation handoff because a commit cannot contain its own final hash.

## 2026-07-08 Review-Fix Pass

- Applied the complete review finding list against the T-0015h closure branch.
- Reworked the prominent generated-registry README sample to use an exported
  `Aggregate<string, typeof TaskStateSchema, number>` subclass and an
  `import.meta.url`-derived absolute compiled package root for conventional
  registry discovery.
- Removed stale sample imports and updated active docs/comments so
  `allowImport`, entity families, `EntityTransaction`, and core transition
  validation describe current compatibility/runtime seams rather than future
  import/replay or pre-runtime layers.
- Split cleanup-rule coverage for `defineEntityHandlers()` into focused direct,
  namespace, named-alias, destructured-alias, import-equals, and local bare-call
  cases. The checker now rejects any bare local end-user example call named
  `defineEntityHandlers()`.
- Verification:
  - `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs` passed:
    1 file, 92 tests.
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm lint` passed, including cleanup enforcement.
  - `corepack pnpm typecheck:build` passed.
  - `corepack pnpm format:check` passed.
  - `git diff --check` passed.

## 2026-07-08 Second Review-Fix Pass

- Applied the remaining stale public docs/API-comment review findings.
- Updated handler decorator JSDoc so schema-bearing decorators and subscriber
  metadata are present-tense legacy/framework bridges while generated
  registries own ordinary schema inference and discovery.
- Updated entity and transaction docs/comments so protected hooks and
  `EntityTransaction` are current framework-owned seams used by repository and
  runtime code, not future-only placeholders.
- Updated active aggregate persistence docs to match the ADR direction:
  ordinary generated-registry aggregate loading uses latest persisted state,
  returned events are appended as a traceability journal, `@Apply` remains out
  of ordinary generated-registry app code, and low-level aggregate-history
  APIs are legacy/internal compatibility support rather than the current
  application persistence model.
- Verification:
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm format:check` passed.
  - `git diff --check` passed.
  - `corepack pnpm typecheck:build` passed.

## 2026-07-08 Final Stale-Wording Fix Pass

- Applied the final T-0015h stale wording findings in public docs and JSDoc.
- Updated deferred-work wording so event import / `ImportBus` is explicitly
  removed under ADR 0001 D1 rather than listed as future runtime work.
- Updated entity family, entity metadata, dispatcher, protected replacement
  hook, and bounded-context storage wording to describe current repository,
  bus-adapter, and built-context behavior.
- Verification:
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm format:check` passed.
  - `git diff --check` passed.
  - `corepack pnpm typecheck:build` passed.

## 2026-07-08 Final Verification

- Closed all T-0015h implementation, fix, and reviewer sub-agents after their
  final reports.
- Final focused docs/API/JVM re-review reported clean after the final
  stale-wording fix pass.
- Ran root-agent final verification:
  - `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs` passed:
    1 file, 92 tests.
  - `corepack pnpm typecheck:build` passed.
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm lint` passed, including cleanup enforcement.
  - `corepack pnpm format:check` passed.
  - `git diff --check 0b6abf9..HEAD` passed.

## 2026-07-08 Main Integration

- Merged `task/T-0015h-generated-registry-closure` into `main` with
  `git merge --no-ff task/T-0015h-generated-registry-closure -m "Merge T-0015h generated registry closure"`.
  Merge commit: `1dff3e0`.
- Confirmed root `main` status still has only the user-owned untracked
  `human-review-1-jul.md`.
- Ran post-merge verification on `main`:
  - `git diff --check HEAD^..HEAD` passed.
  - `corepack pnpm vitest run scripts/check-cleanup-rules.test.mjs` passed:
    1 file, 92 tests.
  - `corepack pnpm docs:check` passed. TypeDoc emitted the existing local
    warning that git remote `origin` is invalid for source links.
  - `corepack pnpm lint` passed, including cleanup enforcement.
  - Equivalent Prettier check over the tracked project formatting surface
    passed, leaving the untracked human note untouched.
