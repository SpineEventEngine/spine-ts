# T-0112 Work Log

## Framing And Implementation Allocation

- Started 2026-08-04 from `origin/main@79900f53` in isolated branch/worktree
  `task/T-0112-wave6-docs-closure`.
- Classification: standard documentation-accuracy and Wave-closure task. The
  accepted Wave 6 runtime and public/serialized contracts do not change, so the
  approved plan is sufficient and no requirements-splitting round is needed.
- Implementation owner: existing implementer role. Expected and explicitly
  dispatched model: `gpt-5.6-terra`. Expected and explicitly dispatched
  reasoning: `medium`. Runtime self-introspection may be unavailable; acceptance
  records the immutable configured role/profile and rejects an omitted field,
  visible mismatch, or inherited fallback.
- First mechanical pass scans affected docs for stale direct-dispatch,
  process-local Stand, single-backend Gateway, incorrect subscription
  completeness, internal wave/task wording, commands, paths, and diagrams.
- No JVM build/source edit, npm publication, runtime/API/Proto change, or future
  migration-remote push is allowed.

## Runtime Contract Documentation Slice

- Updated the server reader guide with the implemented Inbox, shared-delivery,
  Process Manager, and Stand lifecycle model. Updated the auth agent reference
  with the fixed Gateway backend list, one-attempt round-robin unary behavior,
  fan-in notice limits, and no-discovery boundary.
- Scope remains documentation-only: no runtime, TypeScript, Proto, package, or
  deployment behavior changed. Message Board and Distributed Message Board stay
  distinct runnable examples; the root guide already links to both.

## Service And Browser Contract Reconciliation

- Replaced the obsolete direct local post-drain claim with persisted Entity
  Inbox delivery. The user guide now distinguishes durable Stand definitions
  (storage-backed, 30-second pending cleanup, physical cancellation, 10-second
  reconciliation) from process-local active streams and queues.
- The browser guide records fixed startup Gateway topology, one-attempt unary
  forwarding, subscription fan-in limits, authoritative queries, and no dynamic
  discovery. No human-facing document uses internal milestone terminology.

## Todo Subscription Lifecycle Correction

- Replaced the Todo user's obsolete service inactive-TTL explanation with the
  current Stand registry behavior: pending definitions clean after 30 seconds,
  active definitions have no framework TTL, cancellation physically deletes the
  definition, and live stream/queue ownership is process-local.
- Remaining mandatory reconciliation is deliberately recorded: the detailed
  API and architecture service-lifecycle sections still need their old
  inactive-row/claim-marker narrative replaced as one reviewed prose block,
  followed by the remaining package/example deployment audit.

## API, Architecture, And Registry Record Reconciliation

- Replaced obsolete service-owned inactive rows, owner claims, cancel markers,
  and stale-claim recovery language in the API, architecture, and user guides.
  The current contract records one Stand definition, pending 30-second cleanup,
  active no-TTL, physical cancellation, ten-second bounded reconciliation, and
  process-local active streams.
- Corrected the internal-concepts inventory to name the internal Protobuf
  `StandSubscriptionRecord` and current `packages/server/src/stand` ownership;
  it no longer claims JSON/Base64 registry records or removed paths.

## Convergence Check Resolution

- The initial API-documentation attempt ran before this fresh worktree had built
  generated package output and therefore reported missing generated modules.
  This was stale build state, not a repository blocker. After one clean
  `typecheck:build`, `docs:api:check` passes with every expected package export.

## Package And Example Topology Audit

- Rechecked delivery-client and delivery-server READMEs/references against the
  current contract. They already state that all nodes may attempt shared
  delivery while one owner drains, and that the simple delivery server is
  process-local, in-memory, and non-HA; no speculative edits were needed.
- Repositioned the auth reference's fixed Gateway section after its trust
  boundary, preserving the fixed 1–32 backend list, one-attempt round robin,
  all-node subscription fan-in, authoritative-query, and no-discovery rules.
  The auth README continues to state that `OpaqueSessions` is process-local.
- Linked the small Message Board guide and deployment guide explicitly to the
  separate runnable Distributed Message Board topology: exactly two identical
  application nodes, one Gateway, shared storage, and the in-memory simple
  delivery server. The replica-oriented standalone deployment remains a
  distinct operator reference rather than being relabeled as that development
  topology.
- Added an architecture diagram and progressive explanation for persisted
  Entity Inbox delivery, shared-shard lease ownership, Stand's event sources,
  bounded Gateway routing, and best-effort refresh hints.

## Final Documentation Evidence

- `pnpm --config.verify-deps-before-run=false docs:audience:check` passed.
- `pnpm --config.verify-deps-before-run=false format:check` passed; `git diff
--check` passed and the task worktree is clean after the final log commit.
- A focused Markdown stale-term scan found only intentional statements that a
  command or query selects one backend from the fixed list with bounded
  round-robin forwarding. It found no obsolete subscription lifecycle or
  single-backend-topology claim.

## Residual Subscription Documentation Correction

- Replaced the remaining obsolete API and architecture references to inactive
  service records, configurable inactive TTLs, foreign active ownership, and
  claim/cancel lifecycle. The docs now distinguish registry definition capacity
  (the builder's built-in 1–100 limit or a custom registry), per-instance
  `SpineServices` active-stream/unknown-cancel capacity, and per-stream queue
  capacity.
- Updated the TS-versus-JVM concept inventory to describe internal registry
  staging/control/reconciliation rather than removed durable claim/cancel
  semantics. The final broader scan explicitly excludes historical project
  decision/review/task records, which retain their contemporaneous evidence.

## Converged Preflight And Review Dispatch

- Full documentation preflight passes: repository formatting, audience policy,
  API export validation, diff whitespace, 71 package imports, 47 package assets,
  289 relative Markdown links, and the broad obsolete lifecycle/topology scan.
- Documentation reviewer: existing role; expected and explicitly dispatched
  `gpt-5.6-luna` / medium.
- Style/maintainability reviewer: existing role; expected and explicitly
  dispatched `gpt-5.6-terra` / high.
- TypeScript/API reviewer: existing role; expected and explicitly dispatched
  `gpt-5.6-terra` / high.
- Performance/reliability reviewer: existing role; expected and explicitly
  dispatched `gpt-5.6-terra` / high.
- Runtime self-introspection may be unavailable. Acceptance records each
  immutable configured profile and rejects an omitted dispatch field, visible
  mismatch, or inherited fallback. Security is N/A because the task changes no
  trust boundary, credential, runtime, API, or serialized contract.

## Complete Review Wave

- Documentation review is clean. Its deterministic `docs:check` also passes.
  The reviewer confirmed the Inbox, delivery, Stand, Gateway, example, and
  audience-separation descriptions against current code. The configured
  documentation reviewer profile was `gpt-5.6-luna` / medium; runtime metadata
  was unavailable and no mismatch was visible.
- TypeScript/API review found two documentation defects. The
  `SpineServices.subscriptionLimit` text incorrectly included active transport
  streams even though runtime uses it only to bound concurrent unknown-ID
  cancellation work. The in-memory Stand registry warning also needs the exact
  registry name and the production `ServerEnvironment` hosting condition. Its
  topology, package-metadata, and whitespace checks pass. The configured role
  profile was `gpt-5.6-terra` / high; runtime metadata was unavailable and no
  mismatch was visible.
- Style/maintainability review found a stale architecture-scope contradiction,
  two diagrams that visually fan one unary request to multiple nodes, and a
  server README that introduces lease and registry internals before the
  beginner build/start path. The configured role profile was
  `gpt-5.6-terra` / high; runtime metadata was unavailable and no mismatch was
  visible.
- Performance/reliability review found two overclaims. Local Inbox delivery may
  persist and drain within the posting request when no environment worker is
  attached; the remote path does not justify saying delivery is never a request
  callback. A shard lease guarantees one active owner at a time for one bounded
  drain, not stable ownership until the shard is empty; repeated drains may use
  different owners. Stand, Gateway, and best-effort refresh claims are otherwise
  accurate. The configured role profile was `gpt-5.6-terra` / high; runtime
  metadata was unavailable and no mismatch was visible.
- One correction batch returns all accepted findings to the existing
  implementation owner. Documentation must state the current behavior; this
  task does not change runtime semantics to match incorrect prose.

## Review Corrections And Final Acceptance

- Corrected the `subscriptionLimit` TSDoc and API guide to describe only the
  bounded concurrent unknown-ID cancellation work that runtime implements.
  Named `InMemorySubscriptionRegistry` precisely and limited its production
  warning to a context attached to a production `ServerEnvironment`.
- Restored progressive beginner structure in the server README, distinguished
  local direct Inbox drain from environment-worker replay, and described one
  active lease owner per bounded drain with repeated finite attempts.
- Corrected the architecture scope and both affected diagrams. Unary traffic
  now selects one Gateway and one application node; topology and subscription
  fan-in links no longer look like command/query broadcast routes.
- API and reliability re-reviews are clean. Style re-review accepted the server
  and architecture corrections, found one missed Message Board diagram, and is
  clean after that deterministic correction. Documentation review remained
  clean and did not reopen.
- Final preflight passes Proto documentation, API export documentation,
  audience policy, release-readiness checks for 71 imports, 47 assets, and 293
  relative Markdown links, formatting, and diff whitespace.
- The required `pnpm --config.verify-deps-before-run=false verify:release`
  passes: 186 test files and 3,696 tests passed; 3 files and 26 tests are
  intentionally skipped. Coverage is 93.98% statements, 90.01% branches,
  94.48% functions, and 94.92% lines.
- T-0112 is accepted ready for integration. Remaining work is the no-rewrite
  merge to `main`, post-merge verification, durable Wave 6 closure record,
  pushes, and task branch/worktree cleanup.

## Review-Correction Batch

- Corrected `subscriptionLimit` documentation to cover only concurrent
  unknown-ID cancellation work; stream and queue limits remain distinct.
- Named `InMemorySubscriptionRegistry` and limited its warning to attaching a
  context using it to a production `ServerEnvironment`.
- Repositioned beginner delivery prose after the basic server start flow and
  corrected local persisted direct drain versus environment-worker replay.
  Updated the reference with the same distinction.
- Clarified architecture scope and linked Gateway/remote-delivery guides.
  The architecture and Message Board deployment diagrams now show unary
  backend selection rather than a request fan-out, and describe each lease as
  one bounded drain with later drains eligible for another owner.

## Style Residual Correction

- Corrected the remaining Message Board deployment diagram so its standalone
  path shows one selected Gateway and one selected private application node per
  unary request. Registry, delivery, and subscription fan-in links are now
  explicitly topology links rather than broadcast routes.
- API and reliability re-review findings remain clean; this correction changes
  documentation only and does not alter runtime behavior.
