# T-0112 Work Log

## Framing And Implementation Allocation

- Started 2026-08-04 from `origin/main@79900f53` in isolated branch/worktree
  `task/T-0112-wave6-docs-closure`.
- Classification: standard documentation-accuracy and Wave-closure task. The
  accepted Wave 6 runtime and public/serialized contracts do not change, so the
  approved plan is sufficient and no requirements-splitting round is needed.
- Implementation owner: existing implementer role. Expected and explicitly
  dispatched model: `gpt-5.6-terra`. Expected and explicitly dispatched
  reasoning: `medium`. Runtime self-introspection may be unavailable; acceptance
  records the immutable configured role/profile and rejects an omitted field,
  visible mismatch, or inherited fallback.
- First mechanical pass scans affected docs for stale direct-dispatch,
  process-local Stand, single-backend Gateway, incorrect subscription
  completeness, internal wave/task wording, commands, paths, and diagrams.
- No JVM build/source edit, npm publication, runtime/API/Proto change, or future
  migration-remote push is allowed.

## Runtime Contract Documentation Slice

- Updated the server reader guide with the implemented Inbox, shared-delivery,
  Process Manager, and Stand lifecycle model. Updated the auth agent reference
  with the fixed Gateway backend list, one-attempt round-robin unary behavior,
  fan-in notice limits, and no-discovery boundary.
- Scope remains documentation-only: no runtime, TypeScript, Proto, package, or
  deployment behavior changed. Message Board and Distributed Message Board stay
  distinct runnable examples; the root guide already links to both.

## Service And Browser Contract Reconciliation

- Replaced the obsolete direct local post-drain claim with persisted Entity
  Inbox delivery. The user guide now distinguishes durable Stand definitions
  (storage-backed, 30-second pending cleanup, physical cancellation, 10-second
  reconciliation) from process-local active streams and queues.
- The browser guide records fixed startup Gateway topology, one-attempt unary
  forwarding, subscription fan-in limits, authoritative queries, and no dynamic
  discovery. No human-facing document uses internal milestone terminology.
