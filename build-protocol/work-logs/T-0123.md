# T-0123 Work Log

## 2026-08-06 - Task opened

- T-0122 is integrated and post-merge verified; `origin/main@05a5bd85` is the
  accepted baseline.
- Classification: high-risk due to versioned persistence, atomic ownership,
  expiry boundaries, cleanup concurrency, and deterministic lifecycle closure.
- Existing implementer assignment is recorded before dispatch with explicit
  `gpt-5.6-terra` / `medium`. The implementation owner may not spawn subagents.
- The isolated worktree is ignored by Git and frozen-lockfile installation
  completed cleanly.
- Next: run the existing deployment-package baseline, then write and execute
  focused RED tests before any production implementation.

## 2026-08-06 - RED checkpoint

- The assigned implementation role and explicitly configured profile are
  `gpt-5.6-terra` / `medium`; this surface exposes no independent runtime
  model/reasoning introspection, and no visible fallback or mismatch occurred.
- Added the initial behavior tests for replacement fencing, exact expiry,
  namespace isolation, and forty-live-node reads. Added deployment's required
  explicit workspace storage dependency before executing them.
- RED evidence: `pnpm exec vitest run
packages/deployment/test/leased-node-registry.test.ts` ran three tests, all
  failed with `TypeError: LeasedNodeRegistry is not a constructor`. This is the
  expected absent-runtime behavior, not a test setup failure.
- Next: commit and push this RED checkpoint, then add the smallest registry
  implementation and extend behavior coverage before each subsequent change.

## 2026-08-06 - GREEN checkpoint 1

- Added the minimal platform-neutral registry with an explicit factory and
  namespace, version-1 storage key, atomic capability rejection, conditional
  register/renew/remove, exact-expiry reads, finite cleanup batches, namespace
  isolation, and close rejection.
- GREEN evidence: `pnpm exec vitest run
packages/deployment/test/leased-node-registry.test.ts` passed 3/3 tests;
  `pnpm typecheck:build:generated` passed.
- Next: push this checkpoint, then add RED coverage for malformed complete
  snapshots, unsupported atomic storage, cleanup concurrency, and closure.

## 2026-08-06 - GREEN checkpoint 2

- Expanded focused behavior coverage to seven tests: atomic capability
  rejection, malformed complete-snapshot rejection without mutation, finite
  concurrent repeatable cleanup, and idempotent closure join the initial
  fencing/expiry/namespace/capacity coverage.
- Validation is green: focused Vitest 7/7, `pnpm typecheck:build:generated`,
  changed-file Prettier, and `git diff --check`.
- Limitation: provider-specific Datastore/RDBMS integration paths require their
  external emulator/MySQL environments and remain for the orchestrator's final
  verification selection. The registry relies on their existing explicit atomic
  record-storage conformance capability.
- Next: commit and push the expanded implementation/docs checkpoint, then run
  the assigned affected-scope preflight.

## 2026-08-06 - RED checkpoint 2

- Added explicit unknown-version whole-snapshot/no-mutation coverage and a
  controllable-promise close test that starts a read before close without
  wall-clock sleeps.
- RED evidence: `pnpm exec vitest run
packages/deployment/test/leased-node-registry.test.ts` ran 8 tests, with two
  expected failures. Unknown version yielded generic `Application node lease
record is invalid.` instead of the required distinct unsupported-version
  failure; `close()` returned `undefined`, so it could not join the pending
  read. The other six tests passed.
- Next: commit and push the RED checkpoint, then fence starts, track each
  started registry operation, and make close await their settlement before
  closing the storage handle.

## 2026-08-06 - GREEN checkpoint 3

- `close()` now fences starts, returns one idempotent promise, waits for all
  already-started registry operations to settle, then closes its independent
  storage handle exactly once. The controllable-promise test proves no
  wall-clock wait, late-operation rejection, and in-flight joining. Unknown
  versions now fail the complete snapshot with an explicit unsupported-version
  error without mutation.
- GREEN evidence: focused Vitest 8/8 and `pnpm typecheck:build:generated`.
- Contract audit found two unresolved defects before acceptance: the current
  generic `google.protobuf.Struct` encoding does not preserve
  `ApplicationNode.tlsServerName`, and it is not a typed internal generated
  Protobuf record consistent with persisted-record precedent. The endpoint is
  therefore not accepted; replace this temporary encoding with a typed v1
  internal record and prove TLS preservation before final preflight.

## 2026-08-06 - Typed record descriptor update

- Added one Spine TS-owned internal schema file,
  `spine/system/deployment/application_node_lease.proto`; no frozen JVM source
  was edited. Its manifest checksum is
  `201ff753a950331f87fdad7c17579afd440c69cecd262d9732d42aabcc706040`.
- The normalized descriptor digest intentionally changes from
  `ec830e6fa9b02728de61f4f0342e27f6dc89a28d4ffb20ba8a99be4ea449c85a` to
  `901b62b201e077bf72be682a50e4a3bb2b7ca99fa609a8d84dd3c9bdc3877425`,
  reflecting the one added owned schema file.

## 2026-08-06 - Final implementation evidence

| Acceptance behavior                                                         | Focused evidence                                                       |
| --------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| Typed v1 key/descriptor, safe uint64 milliseconds                           | `leased-node-registry.test.ts` plus 44-source/53-descriptor generation |
| TLS endpoint round-trip, stale renew/delete, exact expiry, 40 rows          | Focused registry suite (9 tests)                                       |
| Namespace, atomic rejection, malformed/unknown whole reads without mutation | Focused registry suite                                                 |
| Finite concurrent repeat cleanup and async close join                       | Focused registry suite with controllable promise                       |

- Final deterministic evidence: descriptor compatibility test passed 2/2;
  `pnpm verify:task -- --coverage packages/deployment/test/discovery.test.ts
packages/deployment/test/leased-node-registry.test.ts --source
packages/deployment/src/index.ts packages/deployment/src/leased-node-registry.ts`
  completed successfully, including source checksum, descriptor, generation,
  build, and tooling typecheck gates.
- `DATASTORE_EMULATOR_HOST` and `SPINE_TS_MYSQL_URL` are unset. Datastore's
  Firestore-in-Datastore emulator suite and MySQL integration suite were not
  run; their existing provider conformance paths require those explicitly
  supplied external environments.

## 2026-08-06 - Review correction architecture handoff

- The complete review wave identified that Datastore physical record scoping
  does not include `RecordSpec.storageKey`. This crosses the leased-registry
  ownership boundary and affects all persisted record kinds, so provider
  production changes are paused as a demonstrated architecture blocker rather
  than treated as storage-layout tuning in this task.
- Existing role requested: requirements splitter. Expected explicit profile:
  `gpt-5.6-sol` / `high`. Scope is limited to the minimal storage-key isolation
  correction and its no-migration cutover under the approved no-real-usage
  policy. Runtime metadata remains subject to the configured-surface
  limitation. The implementation owner continues non-provider review fixes.

## 2026-08-06 - Review correction RED

- RED evidence: `pnpm exec vitest run
packages/deployment/test/leased-node-registry.test.ts` ran 10 tests; the new
  preallocation test failed exactly as expected because an invalid cleanup
  bound created one storage handle (`expected 0, received 1`) before throwing.
- The accepted provider hard-cutover decision is recorded: physical record
  scope is backend, context name, tenancy mode, tenant slice, and storage key;
  schema is codec metadata. Datastore alone requires the minimal generic kind
  correction, with no migration, dual path, or legacy support.

## 2026-08-06 - Complete review-wave disposition

- Review corrections through `a4cafa50` cover read pagination, finite cleanup
  progress, preallocation validation, simultaneous CAS, exact v1 key/schema,
  typed TLS persistence, and lifecycle join behavior. The Datastore hard
  cutover scopes generic records by context, tenancy, tenant slice, and
  storage key; its deterministic matrix is 7/7.
- API root verification now expects nine deployment exports. Documentation
  corrections cover explicit factory/namespace ownership, TLS round-trip,
  close joining, and storage-key scope/1500-byte Datastore kind semantics.
- Datastore emulator and MySQL integration remain unavailable because
  `DATASTORE_EMULATOR_HOST` and `SPINE_TS_MYSQL_URL` are unset.
- Re-review is pending for style, documentation, TypeScript/API docs, and
  performance/reliability. This task is not accepted or release-verified.

## 2026-08-06 - Coverage correction dispatch

- The deterministic task preflight passes every non-coverage gate and all 33
  focused tests. Branch coverage is 85.18% (92/108), six covered outcomes short
  of the required 90% threshold.
- Existing role: implementer. Expected explicit profile:
  `gpt-5.6-terra` / `medium`. The assignment is limited to meaningful
  regression tests for currently unvisited endpoint validation, watcher
  lifecycle, ownership fencing, paging, and malformed-record behavior. It must
  not alter production behavior, coverage thresholds, or measured source scope.
- Both model and reasoning are explicit in the dispatch. Runtime
  self-introspection is unavailable on this surface; acceptance will use the
  immutable configured role/profile and reject any visible mismatch or
  fallback.

## 2026-08-06 - Coverage correction result and redispatch

- Commit `609549ae` added test-only endpoint-validation and renewal-race
  regressions. The exact task preflight passed all deterministic gates and 39
  tests, but branch coverage reached only 86.11% (93/108), still five outcomes
  short of the required threshold.
- The same existing implementer remains the bounded test owner with explicit
  `gpt-5.6-terra` / `medium`. The correction is redirected to LCOV's still-zero
  watcher scheduling/close, paging/cleanup, malformed-ID, and false-CAS branch
  outcomes. Production, thresholds, and measured source scope remain frozen.
- Runtime self-introspection remains unavailable; the immutable configured
  role/profile is the available actual metadata, with no visible mismatch.

## 2026-08-06 - Provider regression and identity finding

- Provider verification initially passed 170/171 tests. Root-cause tracing
  showed the single Datastore failure injected malformed raw rows under the
  removed schema-derived kind. Deriving the fixture kind from context,
  tenancy, and storage key preserves the same malformed-payload assertion;
  the Datastore factory suite then passed 33/33.
- Coverage exploration exposed a correctness defect: complete registry reads
  passed only each record payload to validation and discarded the physical
  slot ID. A row whose embedded node ID differs from its slot could therefore
  be accepted.
- Existing role: implementer. Expected explicit profile:
  `gpt-5.6-terra` / `medium`. The next bounded correction must first add a RED
  whole-snapshot/no-mutation mismatch test, then preserve each queried entry's
  slot ID through validation and pass it as the expected ID. The same owner may
  add remaining behavior coverage after GREEN. Runtime self-introspection is
  unavailable; no visible role/profile mismatch occurred.

## 2026-08-06 - Converged preflight evidence

- Strict identity RED `24665adf` and GREEN `f5e28437` prove that a persisted
  lease whose embedded node ID differs from its physical slot fails the whole
  snapshot without mutation.
- Focused provider verification passes 171/171 across generic storage,
  deterministic Datastore storage-key scope/factory, and non-external MySQL
  factory behavior.
- Exact task preflight passes 43/43 deployment tests with 94.79% statements,
  92.59% branches, 95.34% functions, and 99.32% lines. Source checksums,
  descriptors, generated build/typecheck, lint, cleanup, TSDoc, formatting,
  documentation audience/API, Buf, generated cleanliness, and release
  readiness all pass.
- Affected style, documentation, TypeScript/API, and performance/reliability
  concerns now proceed to focused re-review. Security remains N/A because the
  task introduces no authentication, trust-boundary, network-service, or
  secret-handling behavior; registration identities are internal CAS fences,
  not credentials.
