# T-0166 Work Log

## 2026-08-11 - Startup

- Starts from `origin/main@b05720a7` after T-0165 integration and branch
  cleanup.
- Scope is example-only: Message Board manifests, deployment configuration,
  authored model/handlers, generator configuration, tests, and protocol
  records.
- Official LogLayer documentation identifies
  `@loglayer/transport-google-cloud-logging` as the direct transport around a
  caller-supplied `@google-cloud/logging` `Log`.
- Initial inspection confirms Message Board already proves rejection rollback
  and producer identity, but still has redundant explicit required options on
  declaration-first IDs and no application Google transport, custom/empty Event
  route, or natural `@Where` proof.

Status: establishing focused RED behavior before example edits.

## 2026-08-11 - Logging, Routing, And Model Proof

- RED: `MessageBoardDeployment.logger` did not exist, and Stand rejected
  `AnnouncementBoardView` as an unknown state type.
- The app now constructs LogLayer directly with
  `GoogleCloudLoggingTransport` around a caller-created Google `Log`. A fake
  `Log` proves WARNING severity, structured metadata, message placement, and
  write handoff without network access or a Spine adapter.
- All production entrypoints create the official Google `Logging` client and
  pass one application logger to `ServerEnvironment`; GKE discovery receives
  the same application logger. Local startup still supplies no logger, leaving
  the framework's structured default intact.
- Added `AnnouncementBoardView`, keyed by `BoardId`. Its exact
  `MessagePosted` route returns the Event's board only for `announcements` and
  returns `[]` otherwise. This naturally proves an ID-incompatible producer
  custom route and a no-target route.
- `AnnouncementBoardProjection.onAnnouncement()` uses
  `@Where({ eventField: "board", equals: '{"value":"announcements"}' })`.
  Generated handler metadata remains the source of handler registration.
- The declaration-first `PostMessage`, `BoardMessage`, `BoardMessageView`, and
  `AnnouncementBoardView` ID fields omit redundant explicit required options.
  Source assertions plus the existing missing-ID handler-negative test prove
  the implicit policy.
- Existing duplicate-post tests remain green and retain one typed rejection
  Event, unchanged state/history, no second normal Event, and no duplicate
  Projection update.
- The natural `@Where` proof exposed and drove the separately integrated
  T-0163 generated-filename correction at `origin/main@a933d91c`; that commit is
  merged forward into this task branch.
- Generated build passes. Proto lint, changed-file ESLint, cleanup/TSDoc,
  generated-current, Prettier, and diff checks pass. The complete focused model
  and app selection passes 8 files / 56 tests.

Status: cohesive feature checkpoint ready; broader Message Board verification,
coverage, concern review, final verification, and integration remain.

## 2026-08-11 - Supported Composition Correction

- Broad Message Board verification passes 69 web tests and 20
  deployment/container tests in addition to the 56 application tests.
- The first cleanup preflight rejected `repositories.ts` because end-user
  examples must not construct `Repository`, `GeneratedRegistryDiscovery`, or
  `HandlerMetadataRegistry` directly. This exposed a Wave 9 integration gap:
  custom routing was public on `RepositoryOptions`, but generated Entity-class
  registration had no supported way to receive it.
- RED: passing `{ eventRouting }` to `BoundedContextBuilder.add(EntityClass, …)`
  was ignored, so a deliberately unmatched route did not fail repository
  construction.
- GREEN: `add(EntityClass, GeneratedRepositoryOptions)` now forwards typed
  command, Event, state-update, and Stringifier configuration to the generated
  repository. Explicit Repository instances reject a second options argument.
- Message Board again uses the normal beginner-facing pattern:
  `withGeneratedRegistryRoot()`, Entity classes, and one routing option on the
  announcement Projection. The low-level repository assembly file is deleted.
- Generated build, 122 focused tests, cleanup enforcement, changed-file ESLint,
  TSDoc, TypeDoc/API inventory, tooling typecheck, Prettier, and diff checks
  pass.
- Fresh changed-range LCOV is 100% statements/lines (24/24), 93.75% branches
  (15/16), and 100% functions (7/7). The only uncovered changed branch is the
  defensive missing-board fallback in a handler whose Event contract requires
  that field.
- Required style, TypeScript/API, documentation/TSDoc, and reliability concerns
  are clean. Since the active system policy prohibits subagent dispatch, the
  primary orchestrator recorded each concern-specific review directly in the
  review record.

Status: review-complete and ready for one final bounded `verify:task`.

## 2026-08-11 - Final Verification

- The single converged bounded `verify:task -- --no-coverage …` passes. It runs
  Proto generation and authored-quality checks, rejection naming, generated
  TypeScript build, tooling typecheck, cleanup/log-containment/TSDoc/format/docs
  checks, TypeDoc/API inventory, generated-current validation, release
  readiness, and 121 focused tests.
- Coverage was measured separately from fresh LCOV because `index.ts` uses
  supported decorators executed from its compiled artifact with source maps;
  the exact changed-range result remains 100% statements/lines/functions and
  93.75% branches.

Status: complete and merge-ready.
