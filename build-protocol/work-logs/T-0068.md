# T-0068 Work Log

Status: Complete

## 2026-07-24 — Q&A Closure And Isolated Start

- The human approved all first-round history/query decisions, exact JVM runtime
  switching semantics with a warning against routine runtime use, breaking
  storage changes, and the workspace-wide `@spine-event-engine/*` scope.
- The human approved four optimized milestones, parallel Datastore/RDBMS
  implementation after the common contract freezes, and API plus reliability
  planning review with style/documentation N/A.
- D-0100 and D-0101 record the accepted product and package decisions.
- Branch/worktree: `task/T-0068-wave2-planning` /
  `.worktrees/T-0068-wave2-planning`, based on `main` at `1bcc2784`.
- Codex Desktop supports the required existing-role dispatch with explicit
  model and reasoning fields.
- The worktree, subagent-driven development, test-first, review-request, and
  verification-before-completion skills govern the task. The human-approved,
  isolated adapter parallelism overrides the subagent skill's generic
  single-implementer rule without relaxing repository ownership constraints.
- Skill inventory evidence: the session catalog, the complete
  `build-protocol/skills/EXPECTED_SKILLS.md`, a bounded
  `find ~/.agents/skills -maxdepth 2 -type f -name SKILL.md`, and the readable
  `~/.agents/.skill-lock.json` were checked. Every expected entry is installed.
- `architecture-decision-records` is not separately selected because the
  accepted project decision-log format already governs the two decisions.
  `planning-with-files` is skipped because the protocol task/work/review
  records are the canonical recovery ledger and unrelated root scratch files
  must remain untouched. `typescript-advanced-types`,
  `event-store-design`, and `projection-patterns` are deferred for
  task-specific selection when their implementation scopes are frozen.
- Root `eslint.config.mjs`, the generator mode change, planning scratch files,
  and both human-review files remain unrelated and untouched.

## Requirements-Splitter Dispatch Record

- Existing role: `requirements_splitter`.
- Bounded function: inspect current Spine TS and authoritative current
  `core-java`; return the parity matrix, public/storage seams, four-milestone
  task split, acceptance tests, ownership, risks, and verification cadence.
- Expected model: `gpt-5.6-sol`.
- Expected reasoning: `high`.
- Both fields will be explicit in dispatch. The child may not edit files,
  implement, commit, push, or spawn children.
- Actual runtime metadata will be recorded if exposed. Otherwise the immutable
  configured role/profile and metadata limitation are the accepted evidence
  unless a mismatch or fallback is visible.

## Baseline Verification

- Frozen installation passed for all 15 workspace projects and linked 308
  locked packages without a lockfile change.
- The first sandboxed `pnpm --config.verify-deps-before-run=false test` passed
  Proto provenance and the complete TypeScript build, then failed 190 tests in
  23 files on prohibited loopback listener and local ZeroMQ IPC operations.
  Errors were explicit `listen EPERM` and `Operation not permitted`; 104 files
  and 2,135 tests passed in the same run.
- The identical native-permission command then passed: 127 files passed and 3
  skipped; 2,325 tests passed and 21 skipped. This classifies the first result
  as a sandbox restriction and establishes a clean Wave 2 runtime baseline.

## Requirements Split Result And Source Corrections

- The splitter completed read-only with no human blocker. The original
  dispatch explicitly supplied the existing `requirements_splitter`,
  `gpt-5.6-sol` / `high` fields. Runtime self-introspection was unavailable;
  the immutable configured profile and no visible fallback are the accepted
  metadata.
- The returned four-milestone split, public history APIs, storage identity,
  adapter ownership, generic Query cutover, TDD matrix, and selective review
  cadence are accepted as the planning foundation.
- Orchestrator source verification found two corrections required before the
  plan freezes:
  1. JVM appends state history after each logical `store()` call, but batched
     delivery may persist that history before the deferred latest-state write;
     the plan must not promise a universal durable state-first ordering.
  2. Current JVM removed aggregate event sourcing. Wave 2 must replace TS
     aggregate snapshots/event replay with the common latest-state record
     model used by all entity kinds and a separate diagnostic event journal.
     Retaining aggregate snapshots would contradict the approved common
     repository/storage direction.
- The same immutable requirements-splitter context receives only these two
  corrections. Its expected profile remains explicitly recorded as
  `gpt-5.6-sol` / `high`; it remains read-only and may not edit, commit, push,
  or spawn children.

## Corrected Split And Frozen Draft

- The requirements splitter completed the correction with no human blocker.
  Actual runtime self-introspection remained unavailable; the immutable
  configured `requirements_splitter` profile and explicitly dispatched
  `gpt-5.6-sol` / `high` fields are accepted because no mismatch or fallback
  was visible.
- The corrected architecture converges Aggregate, Projection, and Process
  Manager current persistence on one latest-state entity-record hierarchy.
  Aggregate snapshots, stored-event reconstruction, replay tails, and
  persistence-driven appliers are removed atomically. Aggregate events remain
  only in a separate diagnostic/recent journal; delivery and inbox replay are
  preserved.
- The state-history contract now records the exact non-transactional ordering:
  every successful logical store appends history, an immediate latest row may
  already be durable, batched history may precede deferred latest-state
  persistence, and partial failures do not roll back already-written data.
- `build-protocol/planning/WAVE_2_JVM_PARITY_PLAN.md` freezes the parity matrix,
  shared storage/public seams, four dependency-ordered milestones, the
  Datastore/RDBMS parallelization gate, TDD acceptance behavior, reviews,
  verification cadence, and explicit deferrals.
- The pre-review mechanical gate passed: every changed planning file is
  Prettier-clean and `git diff --check` reports no defects.
- The approved planning review wave is ready for concurrent dispatch:
  `typescript_api_docs_reviewer` at explicit `gpt-5.6-terra` / `high`, and
  `performance_reliability_reviewer` at explicit `gpt-5.6-terra` / `high`.
  Both are read-only, may not edit/commit/push or spawn children, and must
  return only actionable findings against the frozen planning packet.

## First Planning Review Wave

- The TypeScript/API reviewer completed with two blocking and one important
  finding: `RecordSpec.storageKey` lacked an exact migration/validation
  contract; history and maintenance declarations lacked complete types; and
  the Entity terminology cutover omitted the public codegen subpath and
  generator binary.
- The performance/reliability reviewer completed with four P1 findings:
  canonical storage scoping, deterministic/idempotent history identity,
  double-dispatch concurrency/recovery semantics, and bounded resumable
  retention were underspecified.
- Runtime self-introspection was unavailable for both reviewers. Their
  immutable configured roles and explicit `gpt-5.6-terra` / `high` dispatch
  fields are accepted; no fallback or mismatch was visible.
- One correction batch now freezes a required validated `storageKey` and
  all-caller migration, exact protected history/public maintenance
  declarations, total row ordering and retry semantics, bounded internal
  maintenance, full Entity codegen terminology, and the exact JVM
  best-effort—not cross-machine atomic—double-dispatch guarantee.
- The corrected packet passes Prettier and `git diff --check`. Both materially
  affected lanes are ready for focused re-review by their same immutable
  existing roles and explicit profiles: `typescript_api_docs_reviewer`
  `gpt-5.6-terra` / `high`, and `performance_reliability_reviewer`
  `gpt-5.6-terra` / `high`. Re-review remains read-only with no edits, commits,
  pushes, or child dispatch.

## Focused Re-review Correction

- API re-review cleared `storageKey` and Entity codegen, then required explicit
  element freezing for `Readonly<T>` and declarations scoped to each history
  storage type.
- Reliability re-review cleared guard and retention semantics, then required
  provider-durable fingerprint bindings and exact event correlation,
  ordering, continuation, and immutable retry behavior.
- The final combined correction makes every returned clone and its array
  top-level frozen; exposes `trim` only on state history; persists and
  cross-process validates storage fingerprints; keys event queries by packed
  producer ID; orders deterministically through event ID; preserves JVM
  exclusive-version continuation without a generic cursor; and treats an
  identical history retry as a no-op while rejecting divergent same-key
  content.

## Planning Closure

- Final API confirmation: clean, with no unresolved blocking or important
  contract findings.
- Final reliability confirmation: clean, with no unresolved P1/P2 findings.
- Each final reviewer used its existing explicitly dispatched
  `gpt-5.6-terra` / `high` profile. Runtime self-introspection was unavailable;
  immutable configured metadata is recorded and no mismatch was visible.
- T-0068 now provides the approved execution-ready Wave 2 plan. The next
  autonomous milestone is T-0069, the atomic package namespace cutover.
- Final task-scoped verification passed: all six changed planning/protocol
  records are Prettier-clean, `git diff --check` is clean, and
  `human-review-1-jul.md` is absent from the diff. The earlier native full
  baseline remains 127 files passed and 3 skipped, with 2,325 tests passed and
  21 skipped.
- The repository-wide `pnpm format:check` still reports the pre-existing,
  unchanged `build-protocol/security/T-0067-wave1-security-findings.md`. T-0068
  does not rewrite unrelated accepted evidence; the focused changed-file gate
  is the applicable final formatting result.
