# T-0136 Work Log

## 2026-08-08 Baseline

- Worktree created from pushed T-0135 commit `74a3c17f` on
  `task/T-0136-node-discovery`.
- Frozen offline dependency installation and Proto generation succeeded.
- The targeted composite build reaches the intended RED at
  `packages/deployment/src/registry/leased-node-registry.ts`: the old lease
  record still supplies removed `RecordSpec.schema`.
- The initial pre-generation test attempt could not resolve generated package
  outputs; after normal Proto generation and dependency build, that setup issue
  was replaced by the exact behavioral migration RED above.

## Implementation Dispatch

- Existing role/function: `implementer`.
- Expected and explicitly dispatched profile: `gpt-5.6-terra` / `medium`.
- Runtime self-introspection must be recorded when exposed; otherwise the
  immutable configured role/profile and limitation are accepted unless a
  mismatch or fallback is visible.

## 2026-08-08 RED

- Updated the focused registry contract test to require the approved
  `spine.deployment.ApplicationNodeLease` source/record family and
  `spine.server.NodeId` ID schema before changing production code.
- `pnpm exec vitest run packages/deployment/test/leased-node-registry.test.ts`
  failed as expected: the old private record specification has no current
  `sourceType`, `recordType`, or message ID schema. Its existing record cloning
  also fails against the generated contract, confirming the test exercises the
  persistence boundary rather than a test-only seam.

## 2026-08-08 GREEN And Preflight

- Migrated `LeasedNodeRegistry` to the approved deployment lease, `NodeId`
  storage slots, `NodeRegistrationId` fencing, and validated millisecond
  `Timestamp` conversion without changing the public `NodeLease` ergonomics.
- Focused registry and provider tests pass. Changed runtime coverage is 95.86%
  statements, 91.76% branches, 100% functions, and 100% lines.
- Removed the private deployment Proto from the source manifest and checksum
  inventory; Proto generation verified 48 source checksums and 52 frozen
  descriptor files. The first bare `pnpm verify:task` invocation was rejected
  by the task verifier because it requires explicit focused-test arguments; the
  required single configured invocation follows this preflight.
- Runtime metadata is not exposed by this execution surface. Acceptance uses
  the immutable configured `implementer` profile `gpt-5.6-terra` / `medium`;
  no visible fallback or mismatch occurred.

## 2026-08-08 Task Verification

- The single configured `pnpm verify:task --coverage` invocation regenerated
  Proto output and passed all generation/source checks before reaching the
  integration-train TypeScript build boundary.
- It stopped only on unowned legacy consumers: ten server `RecordSpec.schema`
  inputs in bounded-context, tenant index, delivery, durable bindings, and
  Stand registry; two legacy Datastore factory API references in
  `examples/orders`; and two corresponding message-board calls. These are the
  assigned removals for later stacked-train tasks, not a T-0136 regression.
- No downstream code was edited and no storage-key, encoding-version, or
  compatibility alias was restored. The next action is the orchestrator's
  required review wave and integration-train handoff.

## 2026-08-08 Consolidated Correction RED

- Focused tests now prove the inclusive Protobuf Timestamp maximum
  `253_402_300_799_999`, reject the next millisecond both before persistence
  and from persisted data without rewriting, and reject cleanup batches over
  256 before storage allocation.
- The original MySQL test was replaced with an observed record-only builder
  proof: a built factory opens `leaseRecordSpec` at
  `application_node_leases`. The Datastore custom-storage selection proof
  remains behavioral.

## 2026-08-08 Consolidated Correction GREEN

- The focused deployment/Proto suite passes 83 tests. Registry coverage is
  95.91% statements, 91.56% branches, and 100% functions and lines.
- Package TypeScript passes. Exact Prettier, focused ESLint, documentation,
  Proto lint/generation/generated-cleanliness, and diff hygiene pass.
- Repository-wide TSDoc remains blocked only by the already-recorded T-0134
  MySQL source findings; it has no T-0136 finding. The one configured
  correction `verify:task` run reaches only the existing unowned stacked-train
  TypeScript failure inventory recorded above.
