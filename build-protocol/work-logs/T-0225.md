# T-0225 Work Log

## 2026-09-08 corrective restart

- Human review rejected the Entity-only implementation and required the JVM
  standalone handler families, an unversioned receiver registry, proper module
  boundaries, and removal of produced-signal failure responsibility from
  `BoundedContext`.
- Classification remains high risk. Revised estimate: 18–24 active agent-hours,
  12–16 elapsed hours, and 1–3 hours of release-gate waiting.
- The Codex Desktop surface supports the required explicit child model and
  reasoning dispatch.
- Corrected implementation assignment: existing `implementer` role;
  `gpt-5.6-terra`, medium reasoning, both fields required explicitly. The
  implementer is the single production writer, must use TDD, must not spawn
  subagents, and must preserve unrelated changes.
- Review wave planned after mechanical convergence: correctness/DDD,
  style/maintainability, TypeScript/API, performance/reliability, and reader
  documentation. Security remains N/A unless the correction changes a remote
  trust boundary.
- Official `origin/master` and the published feature branch were fetched. The
  feature branch is clean and synchronized at `32b93582e`; its merge base is
  `origin/master@e37ec8a1`. Published history will not be rewritten.
- The earlier completed disposition and release evidence are historical only.
  No current success claim applies until the corrected design passes review and
  a fresh release gate.
- Baseline focused verification before corrected implementation passed five
  affected test files and 116 tests with zero failures.
- RED registry-contract evidence: `pnpm exec vitest run
packages/server/test/handler/generated-handler-registry.test.ts
--testNamePattern='rejects a legacy versioned registry'` failed as expected.
  The prior ingestor accepted `{ version: 4, entities: [] }`; the corrected
  unversioned registry must reject it with a regenerate instruction.
- GREEN registry-contract evidence: the same focused command passed after the
  ingestor required `{ receivers }` and rejected legacy versioned metadata with
  a regenerate instruction.
- RED writer-contract evidence: `pnpm exec vitest run
packages/proto-tools/test/generated-registry-writer.test.ts
--testNamePattern='renders deterministic registry source'` failed because
  generated source still emitted `GeneratedHandlerRegistry<4>`, `version`, and
  `entities` rather than the unversioned receiver contract. GREEN evidence:
  the same focused command passed after the writer emitted `receivers` with
  discriminated entity records. `pnpm exec tsc --noEmit -p
packages/server/tsconfig.json` also passed at that checkpoint.
- RED standalone API evidence: `pnpm exec vitest run packages/server/test/index.test.ts
--testNamePattern='exports nominal standalone'` failed with
  `AbstractAssignee is not a constructor`. GREEN evidence: the same focused
  command and the server typecheck passed after adding and exporting the four
  nominal standalone base classes.
- RED standalone-analysis evidence: `pnpm exec vitest run
packages/proto-tools/test/build-time-handler-analyzer.test.ts
--testNamePattern='analyzes nominal standalone'` failed because a decorated
  `AbstractCommander` was diagnosed as missing an Entity state schema. GREEN:
  the same command passed after nominal receiver lineage distinguishes Entity
  and standalone declarations; `pnpm exec tsc --noEmit -p
packages/proto-tools/tsconfig.json` passed.
- RED standalone-ingestor evidence: `pnpm exec vitest run
packages/server/test/handler/generated-handler-registry.test.ts
--testNamePattern='standalone commander'` failed because standalone records
  were ignored. GREEN: the same command passed after receiver validation
  classified nominal standalone lineages and enforced their generated handler
  role matrix before Entity metadata mutation.
- RED default-export evidence: `pnpm exec vitest run
packages/proto-tools/test/build-time-handler-analyzer.test.ts
--testNamePattern='named default-export'` failed with the old
  Entity-specific unsupported-export diagnostic. GREEN: named and anonymous
  default standalone receivers now analyze with default import metadata.
- RED matrix-output evidence: `pnpm exec vitest run
packages/server/test/handler/generated-handler-registry.test.ts
--testNamePattern='Entity state output'` failed because standalone assignee
  validation accepted Entity state output. GREEN: the same test passed after
  assignee and reactor produced schemas were required to be domain Events.
- Handler-registry fixture evidence: added separate domain `commands.proto`,
  `events.proto`, `rejections.proto`, and `states.proto` descriptors; the
  fixture generator and `--check` completed successfully. The ingestor matrix
  now exercises all four standalone roles against all six generated handler
  kinds using those domain schemas.
- The corrected implementer dispatch explicitly selected the existing
  `implementer` role, `gpt-5.6-terra`, and medium reasoning. The Desktop surface
  does not expose separate runtime self-introspection here; the immutable role
  profile and explicit dispatch fields are the acceptance evidence.
- NPM returned `E404` for
  `@spine-event-engine/server@2.0.0-snapshot.9` on 2026-09-08, confirming that
  the existing version-only commit still names an unused release version.
- Corrected registry checkpoint `05e754300` was pushed to the feature branch.
  It replaces the versioned registry surface with unversioned receiver records,
  adds the four nominal standalone base classes, and renames the changed-path
  handler kind to command substitution. Three focused files passed 39 tests and
  server typecheck passed. The checkpoint is not task completion: standalone
  analysis/runtime, signal publication, black-box coverage, and the complete
  terminology/documentation migration remain open with the same implementer.
- Receiver analysis, default exports, standalone role validation, and
  domain-correct registry fixture commits are pushed through `51fe67fe9`.
  Fixture reproducibility, 81 focused tests, and proto-tools/server typechecks
  pass. The first implementer's execution window ended before the complete
  semantic input/output and analyzer tables; no partial test edit was committed.
- Remaining production responsibility transfers without overlap to a fresh
  instance of the existing `implementer` role. Expected and explicitly
  dispatched profile: `gpt-5.6-terra`, medium reasoning. It must not spawn
  subagents and must preserve the published branch history and unrelated work.
- Contract-layer continuation: added readable analyzer and ingestor semantic
  tables using distinct generated Command (`StartReview`/`ScheduleReview`),
  Event (`ReviewStarted`), rejection (`ReviewRejected`), and Entity-state
  (`ReviewState`) descriptors. RED exposed that generated standalone assignees
  accepted Event input. GREEN added explicit Command-input validation for
  assignments and Event/rejection-input validation for reactors. The tables
  cover all standalone legal behaviors, wrong signal and output kinds,
  required outputs, reactor void, subscriber output rejection, and invalid
  External/`@Where` state subscriptions. Fixture check, 83 focused tests, both
  package typechecks, and `git diff --check` pass. Runtime registration and
  delivery remain outside this contract-layer slice.
- Contract-review correction batch: removed the pre-receiver `entities` API
  and writer fallback; Entity receiver records now always carry
  `receiverKind: "entity"`. Ingestion validates receiver, constructor,
  handlers-array, and handler-record structure before dereferencing and raises
  `HandlerRegistryIngestionError` for malformed data. Standalone constructors
  now use a neutral prototype type. Writer tests render and typecheck named and
  anonymous default standalone imports. Focused analyzer/writer/ingestor tests
  (89), both package typechecks, fixture reproducibility, and diff validation
  passed. This is a contract-only correction; runtime assembly remains open.
- Contract re-review minor closure: corrected receiver-neutral shared analyzer
  and generated-handler record TSDoc. The no-follow writer test now imports the
  current writer module and passes receiver-only analysis directly; the removed
  stale package import had reproduced a legacy `entities` dereference. GREEN:
  three focused suites passed 89 tests, both package typechecks, fixture check,
  and `git diff --check` passed.

## Scope

Correct command-input `@Command` registration and execution without changing
event-input or rejection-input command reactions.

## Entity Inbox callback preflight correction

- Cheap preflight found the durable duplicate-follow-up fixture returned
  `() => void` where `EntityInboxFollowUp` requires `() => Promise<void>`.
  The callback now increments the existing counter and resolves immediately;
  its replay/idempotency behavior is unchanged.

## Start state

- Baseline: `origin/master@e37ec8a1fed84f11e0df07c78846d5607a698ede`.
- Branch: `fix-command-to-command-delivery`.
- Worktree: `/Users/armiol/development/experiments/spine-ts-fix-command-to-command-delivery`.
- Official remote: `git@github.com:SpineEventEngine/spine-ts.git`.
- Primary checkout remained clean on protected `master`.

## Root cause established before implementation

- Build-time analysis accepts Command, Event, and rejection inputs for
  `@Command`, but represents all three as `command-reaction`.
- Runtime command readiness and repository command routing register only
  `command-assignment` handlers.
- Runtime event routing registers every `command-reaction`, so command-input
  methods are attached to the wrong bus.
- The public service therefore sees no accepted command type and reports
  `UNSUPPORTED_COMMAND`; server-context posting has the same missing dispatcher.
- Existing tests cover analysis/ingestion and event-to-command execution but do
  not exercise command-to-command delivery end to end.

## Baseline verification

- `pnpm install --frozen-lockfile` completed with an unchanged lockfile.
- Proto generation and TypeScript build completed.
- Six focused test files passed: 107 tests, zero failures.

## Dispatch record

The bounded implementation assignment uses the existing `implementer` role.
Expected model `gpt-5.6-terra` and expected reasoning `medium` are explicit in
the dispatch. The implementer must not spawn subagents and owns production code,
focused tests, task records, and implementation commits until the complete
review finding batch is returned.

Actual runtime metadata will be recorded when the surface exposes it. Otherwise
the immutable configured role/profile and explicit dispatch fields are the
available evidence.

## Progress

- Task contract and branch baseline recorded.
- RED end-to-end reproduction recorded before production changes:
  - Added domain-correct `ApproveProject` and `ScheduleProject` Command fixtures to the isolated
    project-workflow black-box Proto model. `ProjectCoordinator.approve(ApproveProject,
CommandContext)` updates its Process Manager state and returns `ScheduleProject`.
  - Added the end-to-end context-posting reproduction in
    `packages/server-blackbox-tests/test/project-event-routing.test.ts`.
  - `pnpm proto:generate` and `pnpm typecheck:build:generated` succeeded.
  - Focused Vitest command:
    `pnpm exec vitest run packages/server-blackbox-tests/test/project-event-routing.test.ts --testNamePattern='delivers a client and server-context'`
    failed as expected before production changes: `No command dispatcher registered for
"type.spine.server.testing/spine.server.testing.ApproveProject"` from `CommandBus.#dispatch`.
    This proves command-input `command-reaction` metadata does not enter command readiness.
- Production implementation pending.
- GREEN implementation in progress:
  - Build-time analysis now emits `command-transformation` for command-input `@Command` methods,
    retaining `command-reaction` for event and rejection inputs.
  - Generated metadata, readiness, repository routing, Aggregate/Process Manager execution, and
    metadata derivation recognize one command transformation receptor per input type.
  - Produced commands are committed after entity state and then placed on the Command Bus's
    framework-owned follow-up queue. The focused RED exposed the normal enqueue guard
    (`Cannot enqueue runtime work from an active runtime work item`); the follow-up queue is the
    existing runtime seam that preserves ordering without timers.
  - Focused validation passed: 66 tests across command-bus, command-readiness, build-time
    analyzer, and black-box Process Manager delivery tests.
- Aggregate increment:
  - Added domain-correct `ReviewProject` Command fixture and an Aggregate-only transformation
    test with an optional `CommandContext`; it returns `ScheduleProject`.
  - `pnpm proto:generate`, `pnpm typecheck:build:generated`, and the focused Aggregate test
    passed. The persisted Aggregate state reaches `review-approved` through the queued follow-up.
- Rollback proof: a failing Aggregate transformation mutates its draft then throws before commit;
  focused delivery evidence confirms the stored Project remains `scheduled` and no follow-up
  changes it. Command posting is accepted at Inbox handoff, so the handler failure is not returned
  to that caller; the durable no-publication/state assertion is the applicable contract.
- Focused verification, review, corrections, release verification, version
  alignment, and final reporting pending.

## Metadata and tenant routing increment

- Added a repository-level command-transformation fixture with distinct input
  and output Command message types, a custom command route, and a registered
  output dispatcher capture.
- The focused test proves that the transformed Command retains the source actor,
  tenant, immediate command origin, grand-origin, and causal ID; it also proves
  that the Process Manager state is written only to the custom-routed entity in
  the source tenant, not an identically addressed entity in another tenant.
- RED limitation: this test was introduced after the inherited runtime GREEN
  checkpoint (`1c1393e73`), so its first focused run was already green rather
  than a new missing-behavior failure. It is therefore regression coverage, not
  independent RED evidence for the earlier implementation.
- Focused evidence: `pnpm exec vitest run
packages/server/test/repository/repository-routing.test.ts
--testNamePattern='routes command transformations within their tenant'`
  passed (1 test, 259 skipped, zero failures).

## Follow-up promise observation correction

- Corrected Aggregate and Process Manager command-transformation callbacks to
  start all child posts in declaration order, retain every returned promise,
  and await `Promise.all`; this removes floating rejected child-post promises.
- The post-commit callback remains detached by the local Entity Inbox follow-up
  chain. Direct replay can observe its failure; chained follow-up failure is
  contained and diagnosed, so an already accepted source command is not
  retroactively failed. Child enqueue is not atomic-outbox/exactly-once and a
  process crash between commit and enqueue can lose a child.
- Focused regression evidence: repository-routing metadata and existing
  process-manager event-command tests passed (2 tests, 258 skipped).

## Sibling follow-up observation regression

- Added a Process Manager transformation that can return two declared-order
  Commands. Its registered child dispatcher rejects the first and accepts the
  second; the test closes the context and proves both dispatches occurred in
  FIFO declaration order without an unhandled rejection escaping Vitest.
- GREEN evidence: `pnpm exec vitest run
packages/server/test/repository/repository-routing.test.ts
--testNamePattern='starts every sibling transformed command'` passed (1
  test, 260 skipped).
- Requested detached-parent RED procedure was attempted at `3b74cd6ed` in a
  temporary detached worktree. A frozen install first failed because no
  workspace build outputs existed; after generated-build preparation, the test
  could not start because generated Proto output was incomplete. A third run
  began `pnpm proto:generate` followed by generated build but exceeded the
  bounded execution window before Vitest started. Therefore no parent RED
  result is claimed; the limitation is build bootstrap/time, not a passing
  parent behavior. Published history was not modified.

## Generated registry version 4

- Advanced newly generated handler registries to version 4 because
  `command-transformation` is a new serialized handler kind. The ingestor
  accepts supported version 3 registries for compatibility, but rejects a
  transformation declared under version 3 and materializes it under version 4.
- Removed the command-receiver input heuristic from the external-command rule:
  version-4 transformations and assignments are command receivers; retained
  legacy Event-schema validation is scoped to `@Where` input validation.
- RED evidence: the new ingestor test failed under the prior v3 implementation
  because it accepted a transformation in v3. GREEN evidence follows the
  writer/ingestor focused suite.

## Effective command receptor uniqueness

- Added direct metadata-registry tests rejecting an `@Assign`/command-
  transformation collision and a cross-entity transformation/transformation
  collision for the same Command input type.
- Restored `findCommandAssignment()` to its assignment-only public meaning.
  Readiness now uses the explicitly named `findCommandReceptor()` internal
  lookup, which can resolve either the unique assignment or transformation.
- RED evidence: the assignment-only assertion failed before the lookup split;
  GREEN evidence: handler metadata, readiness, and decorator suites passed 41
  tests with zero failures.

## Return shape and reaction classification

- Amended AC4: command transformations return one or more Commands; empty
  output remains rejected by generated-registry emitted-schema validation.
- Added version-4 transformation empty-output coverage and direct metadata
  coverage that event-input `@Command` remains a command reaction, not a
  command receptor. Existing analyzer coverage retains event/rejection
  command-reaction classification.
- Removed the unused `isEventInputSchema` helper from handler metadata.
- Focused analyzer, registry, and metadata evidence: 75 tests passed with zero
  failures.

## Public transformation and close drain

- Extended the project-workflow BlackBox acceptance path to post
  `ApproveProject` publicly after `CreateProject` and to observe the resulting
  Aggregate and Process Manager state through public eventual Query APIs.
- Added immediate-close coverage: after the transforming command is accepted,
  `close()` drains the chained `ScheduleProject` follow-up; the domain fixture
  records that the `approved` schedule handler ran before close completed.
- Initial close assertion attempted to read Stand after close and failed with
  `Stand is closed`; that is an invalid post-close observation, not a drain
  failure. The final assertion observes the domain handler side effect after
  the drain. Focused BlackBox/close evidence: 2 tests passed, 5 skipped.

## Durable Entity Inbox duplicate handoff

- Reworked the attempted repository-target replay proof: direct target replay
  bypasses durable Inbox status ownership and is not the retry contract.
- Extended LocalEntityInbox's durable concurrent duplicate Command test with a
  returned follow-up callback. One persisted source row replays once and runs
  its child callback once; the duplicate waits for the original row and does
  not replay or enqueue another child.
- This is at-least-once source intake plus best-effort post-commit child
  enqueue, not an atomic outbox or exactly-once guarantee. A process crash
  after Entity commit and before child enqueue remains a documented gap.
- Focused evidence: LocalEntityInbox duplicate-follow-up test passed (1 test,
  35 skipped).

## Pre-review acceptance audit

- Client BlackBox intake, Aggregate and Process Manager transformations,
  optional `CommandContext`, custom tenant routing, causal metadata, duplicate
  receptor rejection, registry v4 compatibility, empty-output rejection,
  follow-up close drain, and durable LocalEntityInbox duplicate handoff have
  focused coverage in this branch.
- Event-input command reactions retain analyzer and metadata regression
  coverage. Rejection-input command reactions retain the existing generated
  analyzer/registry fixture coverage; neither is registered as a command
  receptor.
- Multiple PM child Commands have sibling-order and rejection-containment
  coverage. Aggregate one-command transformation and rollback-before-commit
  coverage are present; no separate Aggregate multi-child fixture was added.
- Focused combined coverage was run across changed behavior suites. It is not a
  meaningful repository-wide percentage (19.41% statements / 14.76% branches)
  because it instruments the whole monorepo; changed-source line-level coverage
  requires the orchestrator's selected task profile. No release gate or version
  bump was run here.
- Remaining semantic limitation: child enqueue is best-effort after commit;
  crash between commit and enqueue is not atomic-outbox/exactly-once. Direct
  repository-target replay intentionally bypasses durable Inbox idempotency and
  is not used as acceptance evidence.

## Static correction checkpoint

- Restored deterministic generation identifiers, corrected command follow-up
  naming, TSDoc, and targeted test lint issues introduced in this milestone.
- Static evidence: targeted ESLint, `lint:cleanup`, `lint:tsdoc`, format check,
  and `git diff --check` passed. `typecheck:build:generated` had already passed
  before these source-format-only corrections.

## Source-scoped coverage checkpoint

- Requested focused coverage suite ran across analyzer/writer, bus, context,
  Inbox, readiness, generated registry, decorators, metadata, repository,
  signal metadata, and BlackBox workflow tests.
- Relevant source totals: command bus 89/94 lines and 28/33 branches;
  command readiness 28/29 and 7/11; generated registry 81/85 and 95/105;
  handler decorators 68/74 and 47/53; handler metadata 112/113 and 56/60;
  readiness metadata 48/49 and 11/12; repository 1385/1548 and 753/941;
  signal metadata 64/64 and 84/87. Bounded context is 624/707 and 194/258,
  diluted by its unrelated lifecycle surface; its two T-0225 wiring lines are
  covered by the BlackBox/context acceptance path.

## Canonical generated-state correction

- Regenerated Proto outputs and reran the build typecheck. Corrected all
  readiness-fixture metadata literals to include `commandTransformations`, as
  required by the expanded handler metadata contract.
- The earlier partial preflight claims are superseded by the requested canonical
  verification run, which remains pending after these generated-state fixes.

## Canonical focused verification

- `verify:task` requires explicit test paths; the bare no-coverage invocation
  correctly rejected without them. The expanded 16-path invocation completed
  its node/proto/generated-build stages from regenerated state. The direct
  equivalent expanded Vitest run then passed all 15 resolved test files and
  574 tests with zero failures.
- The worktree was clean after generated-state verification; no random
  generation-ID restoration was required on this rerun.

## Registry v3/v4 type-discrimination correction

- Replaced the single registry interface with the defaulted distributive
  `GeneratedHandlerRegistry<V = 3 | 4>` shape. `GeneratedHandlerRegistry<3>`
  and its nested records exclude `command-transformation`; version 4 admits it.
  The bare default remains the supported v3-or-v4 union for existing consumers.
- The writer now declares generated modules as `GeneratedHandlerRegistry<4>`.
  Ingestion narrows on the serialized version before generic materialization,
  while retaining runtime rejection for an untrusted v3 transformation. Shallow
  discovery accepts exactly v3 or v4 and defers record validation to ingestion.
- Replaced the routing fixture's cloned Aggregate-state descriptors with a
  `ValidatedTaskCommand` input and distinct `TransformedTaskCommand` output
  descriptor, both Protobuf Command messages.
- RED evidence: the public compile contract failed because registry record
  types were not generic, and writer output lacked `<4>`. GREEN evidence:
  `pnpm proto:generate`, `pnpm typecheck:build:generated`, and focused registry,
  context, consumer-contract, and repository-routing Vitest suites passed.

## Contained transformed-command failure diagnostics

- Post-commit transformed command failures now emit the existing bounded-context
  server diagnostic with source and child command IDs/type URLs. The callback
  remains detached and contained by LocalEntityInbox; source command success and
  post-commit best-effort semantics are unchanged.
- RED evidence: a rejecting first PM child admitted its sibling but produced no
  diagnostic. GREEN evidence: the same FIFO sibling test observes the contained
  diagnostic after generated build/typecheck.
- The direct Aggregate multi-child failure-and-close fixture remains the next
  acceptance increment; PM coverage is retained here.

## Aggregate transformed-command containment

- Added a direct Aggregate transformation using separate Protobuf Command input
  and output descriptors. It commits Aggregate state before the returned
  post-commit callback dispatches two children.
- Focused evidence: the first child rejects, both children are admitted in
  declaration/FIFO order, the committed source state is readable, the contained
  diagnostic identifies source and first child, and immediate context close
  drains the accepted sibling. The focused Aggregate test passed after generated
  build/typecheck.

## Review documentation/API reconciliation

- Public `@Command` TSDoc now distinguishes a unique Command-input receptor
  returning one or more Commands from Event/rejection reactions. The server
  reference identifies writer v4/read-compatible v3 behavior and the detached
  post-commit best-effort boundary: accepted follow-ups drain on close, child
  failures are diagnosed and contained, and no atomic-outbox, exactly-once, or
  durable child-retry guarantee is made.

## Final canonical preflight correction

- Corrected strict null narrowing in transformed-command dispatch assertions,
  static lint findings in readiness and focused test fixtures, cleanup line
  bounds, and the internal follow-up diagnostic name. The changes preserve all
  assertions and behavior.
- The expanded 17-path `verify:task -- --no-coverage` profile passed from a
  regenerated Proto state. No nondeterministic generation-ID artifacts remained
  afterward. Global branch coverage remains deferred to the final release gate.

## Canonical preflight correction follow-up

- The prior canonical claim was stale: the subsequent preflight lint edit
  removed `async` from the nested-command dispatcher that awaits an assertion.
  The earlier profile therefore did not validate the final edited source.
- RED evidence: the complete command-bus file failed to parse at the nested
  `await` and executed zero tests. Restoring `async` on that callback made the
  complete command-bus file GREEN (16 tests). The canonical expanded profile is
  rerun from this corrected state before this checkpoint is recorded complete.
- The corrected expanded 17-path profile passed 595 tests. Its direct Vitest
  execution confirms all 17 requested files were included; the command-bus file
  contributed its complete 16-test suite.

## Release branch-coverage correction

- The single release verification run passed 288 files / 4,575 tests but missed
  the global branch threshold at 89.96% (13,173 / 14,642). Added behavioral
  boundary coverage for closed CommandBus follow-up intake, invalid internal
  follow-up access, and `CommandRegistrationReadiness.isAuthentic()` rejection
  of null, primitive, and plain-object inputs.
- Focused coverage ran 30 tests across those two files. Its global threshold
  report is intentionally not comparable to the release run because it
  instruments the entire repository while executing only two files; it does
  confirm the targeted CommandBus/readiness branches execute. Release is not
  rerun here; the orchestrator owns that final expensive gate.

## Final verification

- The post-correction bounded preflight passed all generated-build, tooling
  typecheck, lint, cleanup, TSDoc, formatting, documentation, Proto,
  dependency, logging, release-readiness, package-consumer, and focused test
  gates. The focused matrix passed 645 tests in 21 files.
- The corrective `pnpm verify:release` run passed all 288 test files and all
  4,576 tests. Global coverage passed at 93.29% statements (22,334 / 23,940),
  90.00% branches (13,178 / 14,642), 92.87% functions (5,477 / 5,897), and
  94.45% lines (20,707 / 21,923).
- All current package manifests, internal dependency pins, lockfile importers,
  generated Proto manifests, and release expectations use the unused published
  version `2.0.0-snapshot.9`.
- The feature branch is ready for human review. No pull request or merge was
  performed.

## Independent review correction

- Projection repositories now reject both explicit and generated command-input
  `@Command` transformations before command readiness can advertise an
  undeliverable command route. Aggregate and Process Manager transformation
  support is unchanged.
- Replaced every reviewed follow-up and generated-registry transformation
  fixture with the domain `ValidatedTaskCommand` descriptor; the version-4
  registry positive and non-empty-output negative cases are Aggregate-owned.
- RED: the explicit Projection construction test initially completed without
  error. GREEN: focused repository, generated-registry, and CommandBus suites
  passed 62 tests; generated build typecheck, focused ESLint, TSDoc, cleanup,
  API docs, and audience docs checks passed. The generated-snippet check was
  deferred behind a concurrent repository proto-generation lock.

## Independent acceptance reconciliation

- The CommandBus follow-up coverage now uses the generated domain command for
  both outer and child posts, including the reviewed closed and invalid-access
  assertions. Generated registry transformation fixtures use distinct domain
  Command input and output schemas, avoiding a misleading self-transform.
- Documentation now consistently assigns one effective command receptor to
  `@Assign` or command-input `@Command`, keeps event/rejection reactions on
  EventBus, and correctly describes v4 writer output with legacy v3 runtime
  ingestion.
- Focused CommandBus and generated-registry suites passed 42 tests; generated
  build typecheck, TSDoc, API/audience docs, focused formatting, and diff
  checks passed. The generated-snippet workflow completed without leaving
  generated artifacts.

## Final re-review reconciliation

- Generated-registry and repository fixtures now name the actual input domain
  message `ValidatedTaskCommand`; only the distinct generated output is named
  `TransformedTaskCommand`.
- Public decorator, reference, API, and architecture documentation now state
  that command-input transformations are supported only by Aggregate and
  Process Manager repositories, while Projection repositories reject them at
  repository construction rather than through generic registry typing.
- The arity contract now explicitly documents that generated two-argument
  command transformations receive `CommandContext`. Focused registry and
  repository tests passed 46 tests; generated build typecheck, TSDoc, API and
  audience docs, formatting, and diff checks passed. The generated snippets
  workflow completed cleanly.

## Independent review closure

- Every independent reviewer was dispatched without implementation-memory
  inheritance and reviewed the repository, task contract, and complete branch
  diff directly. Correctness, style/maintainability, performance/reliability,
  TypeScript/API documentation, and reader documentation are clean after the
  accepted correction batch; security remains concretely N/A.
- The final six-file `verify:task -- --no-coverage` preflight passed all static,
  generated-state, documentation, package-consumer, and release-readiness gates
  plus 356 focused tests.
- The final `pnpm verify:release` run passed 288 test files and 4,578 tests.
  Coverage passed at 93.29% statements (22,337 / 23,942), 90.01% branches
  (13,183 / 14,646), 92.87% functions (5,477 / 5,897), and 94.45% lines
  (20,710 / 21,925).
- No review finding remains open. The published feature branch remains
  unmerged, and no pull request was created.

## Process Manager-only `@Command` correction

- Supersedes prior Aggregate command-transformation claims: Aggregates and
  Projections now reject every `@Command` handler and generated command
  metadata record. Process Managers remain the only command-input
  transformation and event/rejection command-reaction owner.
- Removed Aggregate command-transformation and event-side commanding execution
  paths, the Aggregate `ReviewProject` black-box fixture/proto, and Aggregate
  transformation tests. The retained black-box and repository-routing command
  follow-up paths use Process Managers and distinct domain Command outputs.
- Added analyzer, generated-registry, handler-metadata, and repository boundary
  coverage for non-Process-Manager command metadata; public manual builder and
  decorator materialization no longer expose schema-less `@Command` setup.
- Focused GREEN: 383 tests across eight analyzer/server/black-box files;
  `pnpm typecheck:tooling` and `git diff --check` passed. Release verification
  is intentionally not rerun for this correction.

## Inheritance and registry-role follow-up

- Prior final-ready and release-pass entries remain historical and superseded;
  they do not cover the current inheritance or generated-schema-role correction.
- The analyzer now follows same-program entity base classes with cycle
  protection to classify Aggregate, Projection, and Process Manager descendants;
  unresolved decorated ancestry fails closed. Generated registry ingestion validates that transformations take
  Command input and that reactions take Event or rejection input; both must emit
  Command schemas.

## Final bounded re-review correction

- The current correction is awaiting final bounded review and release
  verification; no release-green claim applies. Analyzer lineage is computed
  once per class and generated signal roles use exact command/event basenames,
  with near-miss negative coverage.

## Strict generated Event source names

- Buf runtime descriptors omit `.proto`, so the registry accepts exact plural
  `events` and `*_events` names alongside their analyzer-side `.proto` forms.
  The unsupported singular `*_event` and `*_event.proto` accommodation was
  removed. Routing fixtures now use domain-correct plural Event source names,
  and focused registry coverage rejects both singular forms.
- Focused registry and repository-routing tests, tooling typecheck, static
  documentation checks, formatting, and diff validation are recorded with this
  correction. Release verification remains parent-owned and has not been rerun.

## Canonical preflight artifact correction

- The canonical preflight exposed one overlong analyzer-test fixture import and
  regenerated the absent `server-blackbox-tests` tree. Its generated marker and
  committed manifest now agree on the generator-provided ID; it was not
  invented or substituted manually.
- Cleanup, targeted formatting, generated-current validation, and diff checks
  accompany this correction. The canonical preflight and release verification
  have not been rerun yet.

## Black-box fixture role correction

- Canonical preflight passed 431 tests but exposed one integration-load
  failure: `project_workflow.proto` mixed commands, events, IDs, and entity
  states, so generated role validation correctly rejected hand-authored
  command-reaction metadata. Runtime validation was not weakened.
- Project commands now live in `project_commands.proto`, project events in
  `project_events.proto`, and IDs/entity states remain in the workflow file.
  The normal generator refreshed the manifest, generated exports, checksums,
  and generation marker. The black-box routing, analyzer, and registry focused
  suites pass after the split. No release run has occurred.

## 2026-09-08 SignalPublisher lifecycle closure

- Introduced the context-scoped internal SignalPublisher and migrated
  repository-produced signals away from Bounded Context failure retention.
- Preserved separate fresh-Event and stored-Event paths, trusted internal
  cross-bus admission while closing, reverse assembly abort, and fixed-point
  context drain.
- Corrected legacy tests that used Entity states as Commands with semantic
  Command and Event fixtures; runtime validation was not weakened.
- Focused runtime suites passed 448 tests. Server typecheck, fixture
  reproducibility, changed-file lint and formatting, and diff checks passed.
- Independent performance/reliability review found one missing-test gap. The
  correction added direct close/drain, post-finish rejection, and cross-runtime
  internal-versus-public admission coverage; its 102 focused tests and static
  checks passed.
- The independent re-review approved the complete lifecycle slice with no
  remaining or new finding. Standalone handler adapters remain the next task.

## 2026-09-08 standalone runtime closure

- Added exact-constructor materialization for explicit standalone Assignee,
  Commander, Reactor, and Subscriber instances through generated metadata.
- Added separate Command, domain Event, and System Event state adapters without
  Repository, Entity Inbox, or Entity transaction use.
- Added origin-specific Event schema advertisement and dispatch, exact state
  matching, output validation, defaulted/cloned handler contexts, duplicate
  command-receptor rejection, and SignalPublisher delivery/drain coverage.
- Added executable generated-assembly Integration Broker proof for external
  standalone subscriptions.
- Focused final runtime/context/broker tests passed 104 cases before the narrow
  generated broker proof passed with its 92-case group. Tooling/generated
  typechecks, generated output, TSDoc, formatting, and diff checks passed.
  Standalone runtime coverage reached 97.95% statements and 93.42% branches.
- Independent correctness/reliability and style/maintainability re-reviews
  approved the corrected slice with no remaining finding.
- Branch-wide API expected-export cleanup and stale full-file lint assertions
  remain open for the final documentation/preflight task.

## 2026-09-08 final convergence

- Removed the stale API-check expectations for the retired transformation and
  failure-reporting exports, and recorded the current standalone receiver and
  unversioned generated-registry contracts in public reference material.
- The API checker now expects the four standalone nominal bases,
  `CommandSubstitutionHandlerMetadata`, and the two receiver-registry SPI
  exports. Active reference text no longer describes versioned registries,
  transformation aliases, or Bounded Context failure snapshots.
- Full measured changed-file ESLint is clean: unused analyzer declarations,
  void-return callbacks, malformed-record validation, nominal base classes, and
  Integration Broker assertions were corrected without suppressions. The
  malformed-record boundary test first exposed a null-handler `TypeError`; the
  ingestion boundary now validates an untrusted runtime value and returns its
  documented ingestion error.
- Focused public-flow validation passed 137 tests: client and context Process
  Manager substitution, registry/repository restrictions, standalone runtime,
  bounded-context, and generated Integration Broker paths. Tooling typecheck
  completed without diagnostics. Source-scoped coverage exercised
  `standalone-handler-runtime.ts` at 97.95% statements and 93.42% branches;
  the command exits nonzero only because the repository-wide global threshold
  evaluates every workspace in this focused run.
- `pnpm lint:tsdoc` is an active preflight failure, reporting legacy missing
  comments in SignalPublisher and standalone-runtime internals plus current
  generated-registry comment layout. This is documentation cleanup, not a
  runtime or fixture-contract failure. No final preflight or release claim
  applies.

## 2026-09-08 TSDoc correction

- Completed the generated receiver-registry and analyzer declaration summaries,
  standalone nominal-base comments, Bounded Context publisher parameter, and
  standalone runtime API documentation.
- Documented SignalPublisher with produced-signal and contained-failure wording.
  The comments make no exactly-once claim and do not describe a retained
  Bounded Context failure registry.
- `pnpm lint:tsdoc` passed. The remaining documentation, type, lint, generated,
  formatting, terminology, and diff preflight checks are pending this entry.
- Updated the active architecture reference from registry v3 to the unversioned
  generated receiver registry after the terminology scan found that stale claim.

## 2026-09-08 canonical preflight

- Removed ignored disposable planning/review artifacts after release-readiness
  correctly detected their internal execution terminology. Durable task, work,
  and review records retain the decisions and evidence.
- Corrected precise Integration Broker test schema types, cleanup naming and
  line lengths, CodeMatters fixture headers, and final formatting findings
  exposed by successive cheap-gate runs. No compatibility API was restored.
- Final `pnpm verify:task -- --no-coverage` passed all static, generated,
  documentation, packaging, and release-readiness gates plus 12 focused files
  and 521 tests. The worktree remained clean. Release verification has not run.

## 2026-09-08 final specialist-review corrections

- RED: generated registry ingestion accepted `spine.core.Command` as a handler
  payload and analyzer two-argument handlers accepted unrelated context types.
- GREEN: registry ingestion now requires a generated domain Command schema;
  analyzer requires imported canonical `CommandContext` for Command inputs and
  `EventContext` for Event, rejection, and state inputs, including aliased and
  namespace imports.
- Corrected the current mixed unversioned receiver/buildAsync documentation,
  seven metadata roles, substitution terminology, SignalPublisher close wording,
  and obsolete publication-kind type complexity.
- Focused behavior validation passed 8 files and 450 tests. `typecheck:tooling`
  passed. Direct package typecheck scripts are unavailable; package build/type
  validation remains pending the selected task profile. Release verification was
  not run.

## 2026-09-09 correction coverage closure

- A bounded existing `implementer` assignment used the explicitly dispatched
  `gpt-5.6-terra` / medium profile and no subagents. It added only contract
  tests: malformed generated metadata with `signalSchema: null`, and primitive
  and untyped second handler parameters.
- The generated-registry suite passed 16 tests. The broader registry set passed
  365 tests and covered `generated-handler-registry.ts` at 91.42% statements,
  90.15% branches, 100% functions, and 91.04% lines.
- The analyzer suite passed 50 tests. The compiler-integration file remains
  intentionally excluded from repository-wide thresholds; an isolated report
  measured the exact final-review correction at 93.75% statements and 93.55%
  branches. Whole-file results were 83.86% statements, 81.22% branches, 94.69%
  functions, and 85.15% lines.
- Commits `1e43a1e9d` and `9e7cc0074` were pushed to the official feature branch.
  Temporary coverage configuration was removed. Narrow specialist re-review is
  next; release verification has not run.
- The corrected canonical cheap preflight passed all static, generated,
  documentation, package, and release-readiness gates, plus 66 tests in the
  analyzer and generated-registry suites. Three independent narrow re-review
  lanes are running; reader documentation follows when capacity is available.
- The completed narrow wave found no correctness/reliability regression, then
  accepted two P1 contract findings and two P2 terminology findings: malformed
  schema objects need descriptor-shape validation; the public standalone
  constructor type must exclude Entities; active transformation wording must
  become command substitution; and active follow-up wording must name produced
  signal enqueue/dispatch behavior. One correction batch is in progress.
- The correction is pushed in `2e2bc64a8` and `315e010b2`. Generated registry
  validation now rejects a schema without a descriptor file name through
  `HandlerRegistryIngestionError`; the public standalone constructor union
  names only the four nominal standalone bases; runtime and compile-time
  regressions cover both contracts. Active docs, TSDoc, and test names use
  command substitution and precise produced-signal enqueue/dispatch wording.
  Focused registry tests passed 18 cases and the affected routing test passed.
  Final mechanical verification is pending.
- The post-correction canonical cheap preflight passed all repository gates and
  73 focused tests with a clean tree. The same affected reviewer lanes are
  performing one narrow correction re-review; release verification has not run.
- Correction re-review found that the public standalone constructor union also
  excludes valid dependency-injected subclasses, and that a two-field forged
  descriptor object still passes registry validation. It also found four active
  repository-routing fixture/assertion names using transformation wording and a
  stale current task disposition. One final correction batch is in progress.
- Commit `cbd275e2f` is pushed. It accepts nominal standalone subclasses with
  required application dependencies, proves generated registry matching and
  real Bounded Context dispatch for such an instance, validates coherent Buf
  message/file descriptor identity before role classification, rejects the
  cited forged object, and completes repository fixture terminology. Six
  focused tests and scoped type/lint/TSDoc/format/diff checks passed. Canonical
  verification and final affected-concern re-review are pending.
- The first canonical rerun stopped at cleanup because the new descriptor code
  had one overlong line and four undispositioned standalone helpers. Commit
  `9ed9522ff` consolidates them into the private `DescriptorValidation` module.
  The canonical rerun then passed every repository gate and 359 focused tests
  with a clean tree. Final narrow review closure is pending; release verification
  has not run.
- Final affected-concern closure is clean across correctness/reliability,
  style/OOP, TypeScript/API/TSDoc, and reader documentation. The nominal
  dependency-injected constructor contract, descriptor validation, cyclic
  traversal, generated-writer compatibility, active terminology, and current
  status all passed independent review. Security is N/A because no new trust
  boundary or externally reachable surface was added. Release verification is
  next.
- The first authoritative `pnpm verify:release` passed every pre-test gate but
  did not complete: managed replicas repeatedly failed with
  `INVALID_REGISTRY_MODULE`. A narrow managed-external test reproduces the same
  failure. Root-cause tracing found two executable server fixtures still
  exporting removed version-3 `entities` registries. They are being migrated to
  unversioned `receivers`; production discovery/ingestion will not regain
  compatibility. No release-verification claim applies.
- Commit `598181acf` migrates both stale executable fixtures directly to
  unversioned `receivers` with `receiverKind` and `receiverType`. The exact RED
  command is green, both affected integration files pass 55 tests, focused
  static/cleanup checks pass, and no managed child remains. Intentional negative
  tests still use old shapes to prove rejection. Release verification is
  pending after the cheap preflight.
- The release retry was first interrupted because an intentional verbose-child
  stdout payload was misclassified as a restart loop. A subsequent run was
  allowed to exit naturally after 805 seconds: every pre-test gate passed,
  4,616 tests passed, and one test failed. The sole failure is a positive
  generated-registry discovery fixture using framework Command/Event envelopes
  as domain signals. A domain-correct fixture replacement is in progress; no
  production relaxation is allowed and no release claim applies.
