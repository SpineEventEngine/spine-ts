# T-0225 Work Log

## Scope

Correct command-input `@Command` registration and execution without changing
event-input or rejection-input command reactions.

## Start state

- Baseline: `origin/master@e37ec8a1fed84f11e0df07c78846d5607a698ede`.
- Branch: `fix-command-to-command-delivery`.
- Worktree: `/Users/armiol/development/experiments/spine-ts-fix-command-to-command-delivery`.
- Official remote: `git@github.com:SpineEventEngine/spine-ts.git`.
- Primary checkout remained clean on protected `master`.

## Root cause established before implementation

- Build-time analysis accepts Command, Event, and rejection inputs for
  `@Command`, but represents all three as `command-reaction`.
- Runtime command readiness and repository command routing register only
  `command-assignment` handlers.
- Runtime event routing registers every `command-reaction`, so command-input
  methods are attached to the wrong bus.
- The public service therefore sees no accepted command type and reports
  `UNSUPPORTED_COMMAND`; server-context posting has the same missing dispatcher.
- Existing tests cover analysis/ingestion and event-to-command execution but do
  not exercise command-to-command delivery end to end.

## Baseline verification

- `pnpm install --frozen-lockfile` completed with an unchanged lockfile.
- Proto generation and TypeScript build completed.
- Six focused test files passed: 107 tests, zero failures.

## Dispatch record

The bounded implementation assignment uses the existing `implementer` role.
Expected model `gpt-5.6-terra` and expected reasoning `medium` are explicit in
the dispatch. The implementer must not spawn subagents and owns production code,
focused tests, task records, and implementation commits until the complete
review finding batch is returned.

Actual runtime metadata will be recorded when the surface exposes it. Otherwise
the immutable configured role/profile and explicit dispatch fields are the
available evidence.

## Progress

- Task contract and branch baseline recorded.
- RED end-to-end reproduction recorded before production changes:
  - Added domain-correct `ApproveProject` and `ScheduleProject` Command fixtures to the isolated
    project-workflow black-box Proto model. `ProjectCoordinator.approve(ApproveProject,
    CommandContext)` updates its Process Manager state and returns `ScheduleProject`.
  - Added the end-to-end context-posting reproduction in
    `packages/server-blackbox-tests/test/project-event-routing.test.ts`.
  - `pnpm proto:generate` and `pnpm typecheck:build:generated` succeeded.
  - Focused Vitest command:
    `pnpm exec vitest run packages/server-blackbox-tests/test/project-event-routing.test.ts --testNamePattern='delivers a client and server-context'`
    failed as expected before production changes: `No command dispatcher registered for
    "type.spine.server.testing/spine.server.testing.ApproveProject"` from `CommandBus.#dispatch`.
    This proves command-input `command-reaction` metadata does not enter command readiness.
- Production implementation pending.
- GREEN implementation in progress:
  - Build-time analysis now emits `command-transformation` for command-input `@Command` methods,
    retaining `command-reaction` for event and rejection inputs.
  - Generated metadata, readiness, repository routing, Aggregate/Process Manager execution, and
    metadata derivation recognize one command transformation receptor per input type.
  - Produced commands are committed after entity state and then placed on the Command Bus's
    framework-owned follow-up queue. The focused RED exposed the normal enqueue guard
    (`Cannot enqueue runtime work from an active runtime work item`); the follow-up queue is the
    existing runtime seam that preserves ordering without timers.
  - Focused validation passed: 66 tests across command-bus, command-readiness, build-time
    analyzer, and black-box Process Manager delivery tests.
- Aggregate increment:
  - Added domain-correct `ReviewProject` Command fixture and an Aggregate-only transformation
    test with an optional `CommandContext`; it returns `ScheduleProject`.
  - `pnpm proto:generate`, `pnpm typecheck:build:generated`, and the focused Aggregate test
    passed. The persisted Aggregate state reaches `review-approved` through the queued follow-up.
- Rollback proof: a failing Aggregate transformation mutates its draft then throws before commit;
  focused delivery evidence confirms the stored Project remains `scheduled` and no follow-up
  changes it. Command posting is accepted at Inbox handoff, so the handler failure is not returned
  to that caller; the durable no-publication/state assertion is the applicable contract.
- Focused verification, review, corrections, release verification, version
  alignment, and final reporting pending.

## Metadata and tenant routing increment

- Added a repository-level command-transformation fixture with distinct input
  and output Command message types, a custom command route, and a registered
  output dispatcher capture.
- The focused test proves that the transformed Command retains the source actor,
  tenant, immediate command origin, grand-origin, and causal ID; it also proves
  that the Process Manager state is written only to the custom-routed entity in
  the source tenant, not an identically addressed entity in another tenant.
- RED limitation: this test was introduced after the inherited runtime GREEN
  checkpoint (`1c1393e73`), so its first focused run was already green rather
  than a new missing-behavior failure. It is therefore regression coverage, not
  independent RED evidence for the earlier implementation.
- Focused evidence: `pnpm exec vitest run
  packages/server/test/repository/repository-routing.test.ts
  --testNamePattern='routes command transformations within their tenant'`
  passed (1 test, 259 skipped, zero failures).

## Follow-up promise observation correction

- Corrected Aggregate and Process Manager command-transformation callbacks to
  start all child posts in declaration order, retain every returned promise,
  and await `Promise.all`; this removes floating rejected child-post promises.
- The post-commit callback remains detached by the local Entity Inbox follow-up
  chain. Direct replay can observe its failure; chained follow-up failure is
  contained and diagnosed, so source-command success does not promise child
  delivery or exactly-once semantics.
- Focused regression evidence: repository-routing metadata and existing
  process-manager event-command tests passed (2 tests, 258 skipped).

## Sibling follow-up observation regression

- Added a Process Manager transformation that can return two declared-order
  Commands. Its registered child dispatcher rejects the first and accepts the
  second; the test closes the context and proves both dispatches occurred in
  FIFO declaration order without an unhandled rejection escaping Vitest.
- GREEN evidence: `pnpm exec vitest run
  packages/server/test/repository/repository-routing.test.ts
  --testNamePattern='starts every sibling transformed command'` passed (1
  test, 260 skipped).
- Requested detached-parent RED procedure was attempted at `3b74cd6ed` in a
  temporary detached worktree. A frozen install first failed because no
  workspace build outputs existed; after generated-build preparation, the test
  could not start because generated Proto output was incomplete. A third run
  began `pnpm proto:generate` followed by generated build but exceeded the
  bounded execution window before Vitest started. Therefore no parent RED
  result is claimed; the limitation is build bootstrap/time, not a passing
  parent behavior. Published history was not modified.

## Generated registry version 4

- Advanced newly generated handler registries to version 4 because
  `command-transformation` is a new serialized handler kind. The ingestor
  accepts supported version 3 registries for compatibility, but rejects a
  transformation declared under version 3 and materializes it under version 4.
- Removed the command-receiver input heuristic from the external-command rule:
  version-4 transformations and assignments are command receivers; retained
  legacy Event-schema validation is scoped to `@Where` input validation.
- RED evidence: the new ingestor test failed under the prior v3 implementation
  because it accepted a transformation in v3. GREEN evidence follows the
  writer/ingestor focused suite.

## Effective command receptor uniqueness

- Added direct metadata-registry tests rejecting an `@Assign`/command-
  transformation collision and a cross-entity transformation/transformation
  collision for the same Command input type.
- Restored `findCommandAssignment()` to its assignment-only public meaning.
  Readiness now uses the explicitly named `findCommandReceptor()` internal
  lookup, which can resolve either the unique assignment or transformation.
- RED evidence: the assignment-only assertion failed before the lookup split;
  GREEN evidence: handler metadata, readiness, and decorator suites passed 41
  tests with zero failures.

## Return shape and reaction classification

- Amended AC4: command transformations return one or more Commands; empty
  output remains rejected by generated-registry emitted-schema validation.
- Added version-4 transformation empty-output coverage and direct metadata
  coverage that event-input `@Command` remains a command reaction, not a
  command receptor. Existing analyzer coverage retains event/rejection
  command-reaction classification.
- Removed the unused `isEventInputSchema` helper from handler metadata.
- Focused analyzer, registry, and metadata evidence: 75 tests passed with zero
  failures.

## Public transformation and close drain

- Extended the project-workflow BlackBox acceptance path to post
  `ApproveProject` publicly after `CreateProject` and to observe the resulting
  Aggregate and Process Manager state through public eventual Query APIs.
- Added immediate-close coverage: after the transforming command is accepted,
  `close()` drains the chained `ScheduleProject` follow-up; the domain fixture
  records that the `approved` schedule handler ran before close completed.
- Initial close assertion attempted to read Stand after close and failed with
  `Stand is closed`; that is an invalid post-close observation, not a drain
  failure. The final assertion observes the domain handler side effect after
  the drain. Focused BlackBox/close evidence: 2 tests passed, 5 skipped.
