# T-0094 Work Log

## 2026-08-02 — Task framing

- Created `task/T-0094-remote-delivery` from verified `main@c618e6da` in
  `.worktrees/T-0094-remote-delivery`.
- Classified D1 as high-risk because it changes public configuration,
  cross-package lifecycle ownership, failure cleanup, and retry semantics.
- Recent baseline evidence is the T-0093 post-merge generated build, 420 server
  tests, four Envoy tests, 11 frozen JVM fixture tests, and four static wire
  tests. The build protocol permits this fresh evidence instead of repeating a
  full baseline gate.
- Completed the canonical skill inventory check. Selected isolated-worktree,
  subagent-driven, and TDD guidance subject to repository protocol precedence;
  no `.superpowers` directory or ledger is used.
- Next: requirements split and task-relevant source inspection, followed by one
  Terra Medium implementation owner. Work continues.

## 2026-08-02 — Splitter redispatch

- First dispatch used the existing `requirements_splitter` role with explicit
  `gpt-5.6-sol` / high fields. Runtime self-introspection was unavailable.
- The dispatch exceeded its bounded planning interval without an artifact,
  checkpoint, question, or blocker and was interrupted. It made no repository
  change, so no work was lost or accepted.
- Redispatch keeps the same required role/profile and narrows output to the
  cycle-free contract choice, exact RED matrix, owned files, and gates already
  frozen by D1. Broader Wave research is excluded.

## 2026-08-02 — Requirements split accepted

- The existing `requirements_splitter` role completed the bounded D1 split at
  `build-protocol/planning/T-0094_REMOTE_DELIVERY_SPLIT.md`.
- Dispatch explicitly selected `gpt-5.6-sol` with high reasoning. Runtime
  self-introspection was unavailable, so the immutable configured role/profile
  and explicit dispatch are the recorded acceptance evidence.
- Froze the smallest cycle-free contract: `server` exposes only a generic
  openable environment-delivery lifecycle port; `delivery-client` owns endpoint
  configuration, the client/adapters/builder bundle, readiness, rollback, and
  quarantine ownership.
- Froze open-before-attachment/listener admission, reverse failed-open rollback,
  and final closure order: attachments, delivery, client, quarantine, transport,
  tracing, storage. Concurrent/repeated close shares work and retries only
  unfinished phases.
- Defined the exact RED test names, one-owner path list, relevant four-lane
  review wave, cheap preflight, and one final `verify:release`.
- No current architecture blocker. Next: one Terra Medium implementation owner
  creates and pushes the strict RED commit. Work continues.

## 2026-08-02 — Split acceptance correction

- Corrected two protocol conflicts before production work: tests now use the
  mirrored package `test/` trees, and the stateful remote facility is exposed
  through `RemoteDelivery.connectTo(RemoteDeliveryConfig)` instead of an
  exported standalone function.
- The cycle-free dependency direction, open/rollback/close semantics, exact RED
  behaviors, owned production paths, and gates are unchanged.
- Recorded the existing implementer assignment with explicit expected
  `gpt-5.6-terra` / medium profile. Next: commit/push this accepted correction,
  then dispatch the implementation owner. Work continues.

## 2026-08-02 — RED checkpoint

- Implementer: existing `implementer` role, dispatched with explicit
  `gpt-5.6-terra` / medium reasoning. This execution surface does not expose
  runtime model/reasoning self-introspection; the configured role/profile and
  explicit dispatch are the available metadata evidence.
- Inspected task-relevant JVM notes in
  `spine-jvm-docs/spine-server-runtime-and-bounded-context.md` and the current
  TypeScript environment, attachment, client, adapter, and builder sources.
  The JVM reference supports a small environment-owned delivery facility;
  implementation will keep endpoint/client/adapter details in delivery-client.
- Added only RED tests at
  `packages/delivery-client/test/remote-delivery.test.ts` and
  `packages/server/test/server/server-environment.test.ts`.
- RED evidence: `pnpm exec vitest run packages/delivery-client/test/remote-delivery.test.ts`
  failed all seven cases because `remote/remote-delivery.js` does not exist.
  `pnpm exec vitest run packages/server/test/server/server-environment.test.ts`
  failed the intended missing open-rejection/coalescing behavior and the
  required transport-before-tracer close order. Existing close-only delivery
  compatibility and retry checkpoints are already green baseline behavior.
- `pnpm test -- <both paths>` regenerated checked artifacts and completed the
  build entrypoint; focused RED failure classification used the direct Vitest
  runs above. Next: commit and push this tests-and-records-only checkpoint;
  work continues.

## 2026-08-02 — GREEN implementation checkpoint

- Implemented the frozen cycle-free seam: `server` exports the generic
  `ServerEnvironmentDelivery` port and admits attachments only after an
  openable delivery has opened. Close-only local deliveries remain compatible.
- Added `RemoteDelivery.connectTo()` in delivery-client. It owns the endpoint,
  client/options snapshot, remote adapters, readiness, failed-open cleanup,
  and transferred durable quarantine without adding a server dependency cycle,
  health route, provider selector, worker, or delivery-server mode.
- Updated package README/reference material and public TSDoc. The documented
  order is attachment retirement followed by delivery, transport, tracer, and
  storage; remote delivery then closes client/session before its quarantine.
- The orchestrator authorized one bounded ownership expansion to
  `packages/server/test/server/environment-close.test.ts` for expectation-only
  replacement of the superseded tracer-before-transport order. No production
  scope expanded.
- GREEN evidence: `pnpm exec vitest run packages/delivery-client/test/remote-delivery.test.ts packages/server/test/server/server-environment.test.ts packages/server/test/server/environment-close.test.ts`
  passed 29 tests in 3 files. Earlier `pnpm typecheck:build:generated` passed
  after the source implementation. Next: commit/push this GREEN checkpoint,
  then run the remaining focused mechanical checks and changed-source coverage;
  work continues.

## 2026-08-02 — Focused mechanical validation

- `pnpm typecheck:build:generated`, `pnpm typecheck:tooling`, focused ESLint,
  formatting, and `git diff --check` are green after mechanically formatting
  the changed README and new tests. The three focused suites remain green at
  29 tests.
- A raw `vitest --coverage` run reports 10.98% global coverage because this
  repository enforces global thresholds even for a focused invocation; it is
  not changed-source evidence. The mandated `verify:task --coverage` profile
  initially stopped at two bounded test typing defects (mock quarantine shape
  and attachment result narrowing). Both were fixed and the affected tooling,
  lint, and tests are green. The profile will be rerun once from this clean
  preflight state; `verify:release` remains intentionally deferred to the
  orchestrated post-review release gate.
- The rerun task profile completed its generated-gate process and removed its
  workflow lock and temporary generated directories. This execution surface
  detached its final output after the initial command yield, so its successful
  process cleanup and clean worktree are recorded as evidence; the exact final
  changed-source percentage was not surfaced. The raw global percentage above
  remains a known measurement limitation, not task-scoped coverage evidence.

## 2026-08-02 — Documentation inventory correction

- The canonical scoped gate reported `docs:check:generated` failure because
  its delivery-client public-export inventory did not yet list
  `RemoteDelivery` and `RemoteDeliveryConfig`.
- Added those two delivery-client exports and the matching generic server port
  `ServerEnvironmentDelivery` to `scripts/check-api-docs.mjs`; the second
  entry was surfaced by the same targeted API check. These match already
  documented and exported public APIs. No runtime behavior, test behavior, or
  verification profile was broadened. Next: targeted API, TSDoc, formatting,
  and diff checks, then immediate push.

## 2026-08-02 — Pre-review lifecycle coverage correction

- The broader lifecycle run found one stale expectation in the authorized
  `packages/server/test/server/server-environment-singleton.test.ts`; ownership
  expands only to replace its superseded tracer-before-transport close order.
- Added behavior-focused retry coverage for failed remote delivery, client, and
  quarantine close phases, plus cached successful environment-delivery opening.
  These cover only branches introduced by T-0094. Next: run the requested
  lifecycle set and source-scoped coverage without invoking `verify:release`.
- The seven-file lifecycle run passed 195 tests. Source-scoped coverage remains
  below the requested 90% floor: remote delivery is 98.03% statements, 81.81%
  branches, 100% functions, and 100% lines; server environment is 82.28%,
  73.61%, 90.47%, and 82.45%. The remaining server percentage is dominated by
  broad pre-existing environment branches, so further tests would exceed the
  instruction to cover only T-0094-affected behavior.

## 2026-08-02 — Functional wiring GREEN

- Remote delivery now publishes fail-closed inbox/work-registry ports after
  readiness. Environment workers receive those exact ports through the existing
  attachment create-worker seam for finite and supervisor delivery paths.
- Focused remote and attachment tests passed 86/86 before final typecheck and
  mechanical preflight.

## 2026-08-02 — Functional wiring RED checkpoint

- Restored the prior accidental production edits before this test-first turn;
  the three protected production paths were clean against `4cfa44fa`.
- Added RED-only behaviors for fail-closed remote adapter ports and configured
  environment delivery ports. The next step is to run and classify the missing
  public members/wiring failures before any production edit.
- RED evidence: focused Vitest ran 17 tests; the remote-port behavior failed
  because `delivery.inbox` returned `undefined` rather than throwing before
  readiness. The environment configuration assertion passes structurally but
  has no current worker-path observation; GREEN must add the accepted minimal
  production wiring and an observation assertion in the mirrored lifecycle
  test.
- Added the direct `EnvironmentDeliveryWorker` RED behavior using delegating
  real ports around a local `Delivery`. Its focused suite ran 77 tests, with
  the new case failing as intended: the narrow-cast `ports` option is ignored
  and wrapper operation count remains zero after finite delivery.

## 2026-08-02 — Functional wiring gap

- Orchestrator acceptance inspection traced `ServerEnvironment.delivery` into
  attachment generation. The configured `RemoteDelivery` opens and builds the
  correct remote adapters, but `EnvironmentAttachments` still constructs
  `EnvironmentDeliveryWorker` with its default local delivery factories.
- Consequently, the current implementation proves resource lifecycle but not
  that application delivery uses the remote inbox/work registry. This is an
  accepted P1 contract gap discovered before specialist review.
- The requirements boundary is reopened for the smallest internal worker
  factory seam and one RED functional-wiring test. No JVM work or product-scope
  expansion is authorized. Work continues.

## 2026-08-02 — Functional wiring resolution

- The focused corrective requirements-splitter dispatch used the explicit
  existing role and `gpt-5.6-sol` / high profile. It exceeded the bounded
  interval without an artifact or blocker and was interrupted; no result or
  repository change is accepted from it.
- The first implementation handoff correctly reported that the finite path
  constructs internal `Delivery` directly while only the supervisor path uses
  `DeliveryBuilder`; no files were changed.
- The corrected minimum seam exposes existing `DeliveryInbox` and
  `DeliveryWorkRegistry` ports from `ServerEnvironmentDelivery` after open.
  The current finite constructor and supervisor builder accept the same ports,
  preserving both execution models and environment lifecycle ownership.
- `RemoteDelivery` retains a client plus adapters only. The built `Delivery`
  and reflective close helper are removed because `Delivery` exposes no close
  contract. Closure is client/session then quarantine after attachments stop.
- Next: the existing implementer adds the two RED wiring behaviors, applies the
  minimum correction, and reruns focused lifecycle/coverage checks. Work
  continues.

## 2026-08-02 — Pre-review convergence

- Functional RED/GREEN is complete: the remote owner publishes generic ports
  only after readiness, and both existing finite-worker and supervisor paths
  use those exact ports. The speculative built-delivery reflection path is
  removed.
- Canonical task preflight passes every deterministic gate and 174 focused
  tests. Later deterministic TSDoc, formatting, and factual-doc corrections
  pass their focused checks and changed no runtime behavior.
- All four relevant existing reviewer concerns are recorded with explicit
  required profiles in the review log. Three runtime/API concerns launch at
  current capacity; documentation launches as soon as one slot frees. Work
  continues.

## 2026-08-02 — Deterministic preflight correction

- Focused ESLint found only a nullish-coalescing preference in the new worker
  port spread and two non-null assertions in the new server-environment test.
  Replaced them without changing runtime behavior. Latest wiring coverage is
  recorded in the preceding focused lifecycle evidence; no verification profile
  is rerun for this mechanical correction.

## 2026-08-02 — Deterministic cleanup correction

- The cleanup checker found three overlong lines in the new remote getters,
  environment port helper, and focused attachment test. Reflowed them only;
  behavior is unchanged. Next: focused cleanup, formatting, diff, and affected
  test checks before immediate push.

## 2026-08-02 — Deterministic TSDoc correction

- Added semantic documentation for the generic environment delivery ports and
  the fail-closed remote adapter getters. No lifecycle behavior changed. Next:
  focused TSDoc/API/format/diff checks and affected tests before immediate push.

## 2026-08-02 — Final deterministic formatting correction

- Preflight stopped only because Prettier required the remote-delivery test's
  mechanical layout. Formatting is restricted to that one test file; no
  behavior or verification profile changes are made.

## 2026-08-02 — Deterministic documentation correction

- Documentation/status lint found stale facility build/close wording in the
  delivery-client reference and omitted port-wiring detail in the server docs.
  Corrected documentation only: remote open now records its client plus generic
  ports and readiness publication, failed attempts close only the client, and
  shutdown closes client/session then quarantine. Server documentation now
  records that an openable environment delivery supplies both existing finite
  and supervisor paths after readiness while close-only local delivery remains
  valid.
- Next: docs audience, API, TSDoc, formatting, and diff checks before the
  immediate documentation-only push. Work continues.

## 2026-08-02 — Review correction RED checkpoint

- Added focused regression coverage before production edits. The RED run proves
  pending-readiness close leaves the opening client unmanaged, permits terminal
  reopening, admits an incomplete callable delivery setting, and still exposes
  `ServerEnvironment.openDelivery()` publicly.
- The remote test now records exact client and adapter publication without the
  obsolete delivery-builder mock. Next: serialize the remote lifecycle, route
  opening through internal access, and validate the complete delivery shape.

## 2026-08-02 — Review correction GREEN checkpoint

- `RemoteDelivery` now becomes terminal when close begins, closes the active
  client before awaiting pending readiness, suppresses adapter publication after
  closure, and closes the transferred quarantine only after the client/session
  phase. Failed close phases retain their existing retry checkpoints.
- `ServerEnvironment` now uses one complete, non-evaluating structural guard:
  callable `open`, inherited close, and both port properties identify openable
  delivery. Non-callable legacy `open` properties remain close-only; incomplete
  callable settings reject before attachment admission. Opening is retained in
  the existing package-private access seam rather than the public class shape.
- The delivery-client README now declares real storage and transport
  collaborators in its beginner assembly snippet. Next: focused tests,
  package typechecks, and deterministic documentation/API checks before GREEN
  push. Work continues.
