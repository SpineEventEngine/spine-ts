# T-0039b Work Log

Status: Review round 3 assigned

## Start

- Started `2026-07-14T11:46:19Z` from pushed `main` baseline `0868ecca` after
  T-0039a canonical reconciliation closed.
- Branch/worktree: `task/T-0039b-package-api-docs` at
  `.worktrees/T-0039b-package-api-docs`.
- Offline frozen install reused the existing lockfile/store with no dependency
  or lockfile change.

## Milestone

- Reconcile root and six package READMEs, API overview, public TSDoc, and TypeDoc
  against the final public surface.
- Keep the framework user guide and example closure in T-0039c/T-0040.
- No runtime, export-set, Protobuf, generated, dependency, or policy expansion.

## Inventory

- Required docs total about 2,374 lines: root 56; core 153; proto 44; server
  1,197; storage 80; transport 51; testing 69; API overview 724.
- Current stale candidates include package/API `future`/`not yet` policy claims,
  pre-final delivery/lifecycle wording, and older slice-oriented limitations.
- Existing docs/API checker verifies expected export counts
  `100/28/205/19/17/3`; package metadata tests cover generated Proto exports.

## Assignment

- Existing implementer, explicit `gpt-5.6-terra` / medium, no subagents.
- Author owns required public docs/TSDoc and these records only. Coordinator owns
  review, commits, integration, push, and cleanup.

## Verification Baseline

- T-0039a post-merge native full verify passed `71` files / `1,642` tests in
  ordinary and coverage phases at `95.38/90.12/98.22/95.4` percent coverage,
  with every static/docs/API/Proto/generated-clean gate green.

## Author Handback

- Reconciled the required root/package/API documents only. No runtime, export,
  Protobuf, generated, dependency, user-guide, or example path changed.
- The docs now state package responsibilities and public subpaths, observable
  lifecycle ownership/retry behavior, adapter-neutral same-host ZeroMQ scope,
  delivery limitations, release exclusions without future-policy commitments,
  and narrow legacy/manual-transaction compatibility wording.
- `pnpm typecheck:build` passed. The first `pnpm docs:check` attempt exposed
  missing local workspace build declarations in this fresh worktree; rerunning
  it after that required build passed with TypeDoc counts `100/28/205/19/17/3`.
  Focused package metadata/API export tests passed `7` files / `71` tests, and
  `pnpm typecheck:generated` passed.
- Exact changed paths are recorded in the task handback. Exact-path Prettier,
  phrase/import/end-user API scans, Markdown-target scan, scope/generated scan,
  status equality, and `git diff --check` all passed.
- Actual immutable profile: existing implementer, `gpt-5.6-terra` / medium; no
  subagents. Full `pnpm verify` is deliberately not run in this task.

## Coordinator Pre-Review Round 1

- Focused local checks confirmed the 11-path scope, equal durable statuses,
  clean diff whitespace, bounded release-exclusion wording, and no private
  source/generated imports.
- Runtime evidence rejected the empty-`Command` `packAny()` snippet:
  `packAny(CommandSchema, create(CommandSchema, {}))` throws
  `ValidationException` with three violations.
- Canonical API/TSDoc comparison found stale close-order prose in
  `docs/api/README.md`: it skips context transport drain and delivery
  detach/quiescence and overstates cleanup continuation across the network/
  context-intake hard gate.
- The author concern about `@example/tasks-proto` is accepted as a clarity
  defect because the package is illustrative and absent from this workspace;
  the docs must identify it as a consumer-generated package to substitute.
- The complete batch was returned to the existing implementer, immutable
  `gpt-5.6-terra` / medium, with docs/records-only ownership and no subagents.

## Implementation Fix Handback Round 1

- `packages/core/README.md`: replaced the empty `Command` with public
  `FieldPathSchema` and a valid `task.title` field path. A built-package probe
  completed the default-validation `packAny()` / `unpackAny()` round trip and
  printed `type.spine.io/spine.base.FieldPath task.title`.
- `docs/api/README.md`: restored every observable `RunningServer.close()` phase
  and the network/context-intake hard-gate retry behavior from
  `DEVELOPER_API.md` and public TSDoc.
- `packages/server/README.md`: marked `@example/tasks-proto` as a consumer
  substitution before its first use while preserving package-only imports.
- Focused verification: the built-package snippet probe printed
  `type.spine.io/spine.base.FieldPath task.title`; `pnpm docs:check` passed and
  reported TypeDoc exports `100/28/205/19/17/3`.
- Scope remains the three corrected docs and these three durable records. Full
  `pnpm verify` remains reserved for the final gate; coordinator verification
  and independent review remain pending.
- Actual immutable profile: existing implementer, `gpt-5.6-terra` / medium; no
  subagents.

## Coordinator Verification Round 1

- Built public-package `FieldPathSchema` packing/unpacking probe passed and
  printed `type.spine.io/spine.base.FieldPath task.title`.
- `docs:check` passed with expected TypeDoc counts `100/28/205/19/17/3`.
- Focused package root-export/API suites passed `6` files / `69` tests.
- Prettier over all 11 milestone paths, status equality, private/generated
  import scan, scope scan, conflict scan, and both endpoint/baseline
  `git diff --check` passed.
- Future-policy lint false positives were adjudicated as observable current
  lifecycle wording, not future commitments. The pre-review fixes are accepted
  for independent review.

## Independent Review Round 1 Assignment

- Immutable review endpoint: `0868ecca..fcef6d35`.
- Existing style, API-docs, and performance/reliability roles are explicitly
  assigned `gpt-5.6-terra` / high; the existing documentation role is
  explicitly assigned `gpt-5.6-luna` / medium.
- Each lane is read-only, bounded to its canonical concern and this milestone,
  cannot spawn subagents, and must ignore superseded historical text unless a
  current task record or changed public document makes it active.
- The coordinator will collect the complete wave before accepting or assigning
  any finding batch. Security remains the T-0041 release-readiness gate.

## Independent Review Round 1 Results

- Complete wave collected and every reviewer closed.
- Clean: documentation completeness, TypeScript/API docs, and
  performance/reliability.
- Accepted maintainability findings: root README falsely excludes implemented
  semantic-tag routing consumption; server README duplicates adjacent
  production exclusion lists.
- Technical adjudication confirmed D-0069, server public docs, tests, and
  `runtime-routing.ts` consume command-assignee/event-receiver tags, and
  confirmed the duplicate exclusion vocabulary at server README lines
  1095-1100.
- Explicit dispatch fields and the Desktop runtime's immutable role metadata
  match the required profiles: docs Luna/medium; style, API, and reliability
  Terra/high. The batch is assigned to the existing Terra/medium implementer;
  no subagents and no code/example/export/dependency expansion.

## Review Round 1 Fix Handback

- Corrected the root release list to acknowledge implemented D-0069
  descriptor-derived semantic-tag routing consumption while retaining only the
  excluded application-owned registration/materialization policy.
- Consolidated the adjacent server production exclusions into one statement,
  preserving every unique boundary and the explicit no-future-policy qualifier.
- Changed only `README.md`, `packages/server/README.md`, and the three T-0039b
  durable records in this fix batch. Full `pnpm verify` remains final-only;
  coordinator verification is pending.
- Exact-path Prettier, semantic-tag/future-policy/duplicate/status scans, and
  `git diff --check` passed. `pnpm docs:check` passed with TypeDoc exports
  `100/28/205/19/17/3`.
- Actual immutable profile: existing implementer, `gpt-5.6-terra` / medium; no
  subagents.

## Coordinator Verification After Review Round 1

- Independently accepted both wording fixes against D-0069, current server
  routing documentation, and the exact changed paragraphs.
- `docs:check` passed with expected counts `100/28/205/19/17/3`.
- Exact-path formatting, focused wording/duplicate/status scans, conflict scan,
  and endpoint/baseline diff checks passed.
- No source, export, example, dependency, generated, or user-guide path changed.
  A fresh complete reviewer wave is next; full verify remains final-only.

## Independent Review Round 2 Assignment

- Immutable full-milestone range: `0868ecca..769176d4`.
- Explicit immutable profiles: documentation Luna/medium; style, API docs, and
  reliability Terra/high.
- All four concerns are read-only and bounded, no subagents, with superseded
  historical text ignored unless current records/docs activate it. Results will
  be collected as one complete wave before adjudication.

## Independent Review Round 2 Results

- Complete wave collected and all four reviewers closed. Style is clean.
- Documentation and API lanes independently found the same testing README
  issue: missing consumer-generated schema imports and undeclared fixture setup
  values. It is one deduplicated finding covering both snippet blocks.
- Reliability found that server README conflates environment selection,
  server-level environment ownership, and per-facility ownership. Public source
  confirms production `ownsStorageFactory`, `ownsTransport`, `ownsDelivery`,
  and `ownsTracerFactory` all default false, while `ownsEnvironment` controls
  whether the server closes the environment object.
- Explicit dispatch/runtime profiles matched: docs Luna/medium; style, API, and
  reliability Terra/high. The two-item batch is assigned to the existing
  Terra/medium implementer, no subagents, docs/records only.

## Review Round 2 Fix Handback

- Made both testing fixture snippets self-contained in imports and declarations:
  consumer schemas come from the identified `@example/tasks-proto` stand-in;
  the built context and repository/topic/query/command setup are explicit, with
  the latter values imported from an identified consumer test-support stand-in.
- Corrected server environment ownership wording: facility selection does not
  imply closure; the four production `owns*` flags default false and are
  distinct from server-level `ownsEnvironment` closure of the environment
  object.
- Changed only the two assigned package READMEs and these three T-0039b records.
  Full `pnpm verify` remains final-only; coordinator verification is pending.
- Actual immutable profile: existing implementer, `gpt-5.6-terra` / medium; no
  subagents.
- Verification passed: exact-path Prettier over the two assigned READMEs and
  three records; focused package-import/snippet, stale ownership,
  future-policy, identical-status, and prohibited-scope scans.
- `git diff --check` and `pnpm docs:check` passed. The documentation gate
  verified copied proto checksums and TypeDoc export counts `100/28/205/19/17/3`
  for proto/core/server/storage/transport/testing.
- Full `pnpm verify` was not run; it remains reserved for the final gate.
  Coordinator verification remains pending.

## Coordinator Verification After Review Round 2

- Multiline import-block validation confirmed all eight formerly undeclared
  testing symbols are imported, with both illustrative package substitutions
  stated before use.
- Source/TSDoc comparison confirmed the four facility flags default false in
  production and are independent of server-level `ownsEnvironment`; the new
  server README wording matches that contract.
- `docs:check` passed at `100/28/205/19/17/3`; exact-path formatting,
  equal-status, stale-wording, conflict, scope, and baseline/current diff checks
  passed. A fresh full reviewer wave is next; full verify remains final-only.

## Independent Review Round 3 Assignment

- Immutable range `0868ecca..716aba30`; complete milestone, not only the latest
  fixes.
- Explicit immutable profiles: docs Luna/medium; style, API, and reliability
  Terra/high. Every lane is read-only, no subagents, distinct concern, with the
  full wave collected before adjudication.
