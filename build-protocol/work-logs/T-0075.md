# T-0075 Work Log

## 2026-07-27 — Wave 4 implementation start

- Reviewed base: pushed `origin/main@f8a59883`.
- Branch/worktree: `task/T-0075-wave4-browser-interoperability` /
  `.worktrees/T-0075-wave4-browser-interoperability`.
- The primary checkout is dirty with unrelated work and remains untouched.
- Classification: high-risk due to public packages, browser/runtime and TS/JVM
  protocol boundaries, auth/session security, serialized context rewriting,
  and long-lived concurrent streams.
- Approved plan:
  `build-protocol/planning/WAVE_4_BROWSER_CLIENT_INTEROPERABILITY_PLAN.md`.
- Desktop supports explicit child model/reasoning dispatch. Independent child
  self-introspection is unavailable; immutable configured role/profile and
  explicit dispatch are metadata evidence unless a mismatch appears.

## Planning assignment

- Existing role: `requirements_splitter`.
- Bounded scope: read-only decomposition of Preflight and Slices A-F into small
  dependency-ordered tasks, ownership, behavior-first acceptance, security/API
  seams, and required reviews.
- Expected model: `gpt-5.6-sol`.
- Expected reasoning: `high`.
- Both fields are explicit. The child may not edit, commit, push, merge, or
  spawn children.

## Planning result

- The existing `requirements_splitter` returned a dependency-ordered P1,
  A1-A4, B1-B4, C1-C5, D1-D5, E1-E4, and F1-F4 split with no product or
  architectural blocker.
- Expected and explicitly dispatched model: `gpt-5.6-sol`.
- Expected and explicitly dispatched reasoning: `high`.
- The immutable configured role/profile matches. Independent runtime
  self-introspection is unavailable; no mismatch or inherited fallback was
  exposed.
- Frozen public/security seams and executable acceptance are persisted in
  `build-protocol/planning/WAVE_4_EXECUTION_SPLIT.md`.
- The orchestrator accepted the split after checking it against the approved
  plan. The browser-safe kernel residing in `client-web` avoids a third public
  package; `client-node` may depend only on its explicitly exported neutral
  seam, never an internal subpath.
- P1 begins with official
  `SpineEventEngine/core-java@461a8281e484c12636d8cf660a1d6c929fbbd7ec`,
  whose source contains the required native Command, Query, Subscription,
  Server, and port-based `GrpcContainer` APIs. Capability/build probing remains
  required before the fixture is frozen.

## Baseline verification

- `pnpm install --frozen-lockfile` restored all 309 lockfile packages. Expected
  pre-build workspace-bin warnings disappeared as generated/build artifacts
  became available.
- An initial generated-only test invocation failed because a fresh worktree had
  not generated Proto sources. It is invocation-error evidence only and is not
  treated as a repository failure.
- Corrected baseline:
  `pnpm --config.verify-deps-before-run=false test -- --maxWorkers=4`.
- Result: 140 test files and 2,638 tests passed; 3 files and 25 tests were
  intentionally skipped.
- Official upstream revisions observed before the fixture decision:
  `SpineEventEngine/core-java@461a8281e484c12636d8cf660a1d6c929fbbd7ec`
  and
  `SpineEventEngine/delivery-server@21f2901f393e552208b97166f4eaeb942f9f5172`.
  The core revision remains provisional until its exact immutable fixture
  inputs are confirmed against the requirements split.

## P1 implementation assignment

- Existing role: `implementer`.
- Ownership: new `interop/jvm/**` lock manifest, fetch/verify/build/launch-probe
  tooling, its focused tests, and
  `build-protocol/tasks/T-0075-wave4-browser-interoperability/P1_IMPLEMENTATION_REPORT.md`.
  It may make only the smallest root tooling/config changes needed to run the
  focused tests.
- Behavior boundary: freeze official
  `SpineEventEngine/core-java@461a8281e484c12636d8cf660a1d6c929fbbd7ec`
  only after checksum, wrapper/toolchain/lock, clean-source, and native
  Command/Query/Projection-subscription/exposed-event-subscription capability
  evidence passes. The fixture must be fetched to ignored cache, verified
  before extraction, built detached, and never patched, vendored, or added as a
  submodule.
- Expected model: `gpt-5.6-terra`.
- Expected reasoning: `medium`.
- Both fields are explicit in dispatch. The owner uses behavior-first tests,
  preserves concurrent/unrelated work, may not spawn children, and may not
  commit, push, merge, or edit other protocol records.
- Runtime metadata limitation: explicit dispatch and immutable configured
  role/profile are available; independent self-introspection is unavailable.

## P1 coordinator verification and correction

- Existing role/result metadata matched the explicitly dispatched
  `gpt-5.6-terra` / `medium` implementation profile. Independent
  self-introspection remained unavailable.
- Coordinator focused verification passed 1 file / 5 tests, module syntax,
  supported-file Prettier, and `git diff --check`.
- A checksum-pinned archive-only download succeeded. Running the workflow under
  the restricted sandbox then reproduced a real defect before Gradle execution:
  extraction created the locked archive-root directory, while subsequent code
  looked under a nonexistent SHA-only directory and failed to find the wrapper
  JAR.
- Source inspection also rejected the event-capability proxy:
  `Server.Builder.include(BindableService)` proves extension support, not native
  event subscriptions. The pinned upstream actually proves event subscriptions
  through `SubscriptionService`, `TopicValidator` accepting `EventMessage`,
  and `EventUpdateHandler`; the lock and capability test must use that evidence.
- One focused correction returns to the same existing `implementer`, expected
  and explicitly dispatched `gpt-5.6-terra` / `medium`. It must add a failing
  end-to-end prepare regression for the archive-root path, correct extraction,
  replace the false event proxy with native source evidence, and rerun focused
  checks. It may not broaden P1, commit, push, merge, or spawn children.

## P1 corrected implementation result

- Existing role: `implementer`; expected and explicitly dispatched profile:
  `gpt-5.6-terra` / `medium`. The immutable configured profile matches;
  independent self-introspection remains unavailable.
- RED: an end-to-end prepare regression reproduced the locked archive-root path
  defect. The native event capability test also failed after removing the
  invalid `BindableService` proxy.
- GREEN: locked-root extraction and native
  `SubscriptionService`/`TopicValidator`/`EventUpdateHandler` evidence pass 1
  file / 6 focused tests.
- Coordinator verification also passed module syntax, Prettier, `git diff
--check`, and provisional/frozen-claim lint.
- The safe offline upstream Gradle probe started wrapper 9.6.1 under Java 21 but
  stopped because `org.jetbrains.kotlinx:kover-gradle-plugin:0.9.9` was not
  cached. This is recorded evidence, not a successful build/launch probe.
- The revision remains provisional. P1 review proceeds over the safe workflow
  and exact limitation; P1 cannot close until a permitted real build/launch
  probe passes.

## P1 review correction assignment

- The complete P1 review wave is recorded in
  `build-protocol/reviews/T-0075-wave4-browser-interoperability.md`.
- All nine deduplicated P1/P2 findings are accepted as one batch. No correction
  began before style, documentation, and reliability results were collected.
- The same existing `implementer` context is reused, expected and explicitly
  dispatched `gpt-5.6-terra` / `medium`. It must add RED regressions first,
  correct only P1 tooling/tests/manifest/runbook/report, and rerun focused
  validation.
- It may fetch checksum text/artifacts without executing them, but the real
  networked Gradle build remains a separate evidence gate subject to the
  environment policy. It may not mark the fixture frozen without actual
  Command/Query/Projection/Event launch evidence.
- It may not edit unrelated package/runtime code, commit, push, merge, or spawn
  children.

## P1 correction-context replacement

- The original implementation context returned twice while explicitly
  acknowledging accepted reliability work remained incomplete. Neither return
  is accepted as P1 completion.
- Preserved useful work includes file-specific capabilities, native event
  evidence, expanded-size metadata, Gradle distribution lock inputs, Java
  target wording, exact upstream test selection, runbook, revision lock
  skeleton, and source staging.
- A fresh existing `implementer` receives only the remaining P1 reliability
  batch. Expected model `gpt-5.6-terra`; expected reasoning `medium`; both are
  explicit in dispatch. It must test-first finish atomic archive publication,
  safe internal-symlink handling for the pinned archive, stale/failed lock
  recovery, per-run source/build staging, mutation/failure cleanup, concurrent
  prepare serialization, and a checksum-backed Gradle distribution path that
  does not patch upstream.
- The replacement may modify only assigned P1 paths and the P1 report; it may
  not change client/auth runtime code, commit, push, merge, or spawn children.

## P1 real-probe correction

- Replacement coordinator verification passed 1 file / 10 tests and module
  syntax. A combined formatting check found only the orchestrator-edited work
  log still needed Prettier.
- The first authorized online probe reached Gradle configuration after 2m12s,
  then Gradle Doctor rejected the inherited `JAVA_HOME` because it pointed to
  the JDK `bin` directory instead of the JDK root. No fixture capability result
  was claimed.
- Retrying with the exact JDK root exposed a production retry defect before
  Gradle: distribution readiness checked for `gradle-9.6.1-bin`, while the
  distribution extracts `gradle-9.6.1`. The workflow then tried to rename
  staging over an existing nonempty wrapper cache and failed with `ENOTEMPTY`.
- This exact retry failure returns to the replacement existing `implementer`,
  expected and explicitly dispatched `gpt-5.6-terra` / `medium`. It must add a
  failing regression for an already prepared/partially wrapper-used cache,
  correct idempotent publication without deleting a live verified cache, append
  RED/GREEN evidence, and rerun focused checks. The fixture remains
  provisional.

## P1 upstream KSP diagnostic assignment

- Coordinator verification after the cache correction passed 1 file / 11
  tests, module syntax, Prettier, and `git diff --check`.
- The corrected-JDK real probe then reached the unmodified upstream Gradle
  build. It failed after 3m03s in `:server:kspTestFixturesKotlin` with
  `KSP failed with exit code: PROCESSING_ERROR`, before the selected
  compatibility tests ran.
- The same replacement existing `implementer` owns the bounded diagnostic and
  fixture/toolchain correction. Expected model `gpt-5.6-terra`; expected
  reasoning `medium`; both are explicit in dispatch. It must reproduce with
  useful Gradle diagnostics, preserve the pinned source, add RED evidence for
  any fixture defect, and rerun focused checks. It may adjust only P1 paths and
  its report, may not claim a frozen fixture without a successful real probe,
  and may not commit, push, merge, or spawn children.

## P1 exact Java 17 correction

- Independent coordinator verification accepted the improved failure
  diagnostics: 1 file / 12 tests, syntax, Prettier, and `git diff --check`
  passed.
- The diagnostic conclusion that Java 21 was sufficient is rejected.
  The pinned commit's official `Build on Ubuntu` GitHub Actions check succeeded
  and its workflow explicitly installs Zulu Java 17. The local failing probe
  launched Gradle with Homebrew Java 21, while no Java 17 was installed.
- Azul's official metadata API identifies the current macOS AArch64 Zulu 17
  package as `fa19a050-71b2-4d90-b496-a69aa94ec33d`, version `17.0.20+8`, URL
  `https://cdn.azul.com/zulu/bin/zulu17.68.17-ca-jdk17.0.20-macosx_aarch64.tar.gz`,
  size `191677000`, and SHA-256
  `0da52534760b74ba8a42660384b2f4e44311a47ae52faa45fcbd829b2797b244`.
- The same replacement existing `implementer` receives a correction to pin
  and checksum-stage this exact CI-aligned launcher in the ignored cache and
  run the unmodified pinned source under Java 17. Expected model
  `gpt-5.6-terra`; expected reasoning `medium`; both are explicit in dispatch.
  It must add RED evidence for rejecting/avoiding the Java 21 launcher, preserve
  source and wrapper integrity, and rerun focused checks. It may change only P1
  paths/report and may not commit, push, merge, or spawn children.

## P1 Zulu metadata-size correction

- Independent verification of the exact-Java correction passed 1 file / 14
  tests, syntax, Prettier, and `git diff --check`.
- The authoritative probe stopped before checksum/extraction because it treated
  Azul's metadata `size` field (`191677000`) as an exact byte count. The
  official CDN's response for the same immutable URL declares
  `Content-Length: 191676948`, 52 bytes less. The metadata value remains a
  valid upper bound, but not an exact equality assertion.
- The same replacement existing `implementer` receives the bounded correction.
  Expected model `gpt-5.6-terra`; expected reasoning `medium`; both are
  explicit in dispatch. It must add RED evidence for accepting content at or
  below the locked maximum while still rejecting larger content, preserve
  checksum verification as the identity guard, update wording/evidence, and
  rerun focused checks. It may change only P1 paths/report and may not commit,
  push, merge, or spawn children.

## Human scope correction: no Spine JVM builds in Wave 4

- The user explicitly prohibited further Wave 4 work that builds the Spine JVM
  project because of its execution cost. The active disposable Linux test run
  was interrupted immediately with exit 130; no upstream source was modified.
- This instruction supersedes P1/E3/E4 runtime-build and launch-probe criteria.
  Wave 4 retains only immutable source/archive verification, static service
  capability inspection, shared Protobuf/service-descriptor compatibility, and
  TS-owned wire-contract fixtures. JVM runtime compatibility execution is
  deferred and must be documented as a limitation.
- The existing P1 `implementer` receives a scope-reduction correction. Expected
  model `gpt-5.6-terra`; expected reasoning `medium`; both are explicit in
  dispatch. It must make the default/only P1 workflow incapable of invoking
  Gradle or downloading/executing a JDK, remove obsolete build/launcher code and
  claims, retain checksum/atomic/safe-extraction/static-capability behavior and
  focused tests, and update the runbook/report. It may change only P1
  paths/report and may not commit, push, merge, or spawn children.

## P1 static-only review correction assignment

- The complete reopened P1 review wave is recorded in
  `build-protocol/reviews/T-0075-wave4-browser-interoperability.md`.
  Documentation is clean; style and reliability returned one deduplicated
  six-item correction batch covering real ZIP metadata integration, pinned
  source digest, lease-safe locking, in-progress transfer bounds, normalized
  root containment, and `.ready` cleanup.
- The same existing `implementer` owns the correction. Expected model
  `gpt-5.6-terra`; expected reasoning `medium`; both are explicit in dispatch.
  It must add RED regressions first, preserve the static-only/no-JVM-build
  boundary, update deterministic report evidence, and rerun focused checks.
  It may change only P1 paths/report and may not commit, push, merge, or spawn
  children.

## P1 coordinator lock correction

- Independent focused verification passed 1 file / 12 tests, syntax, Prettier,
  and `git diff --check`. The real cached static verifier passed archive
  checksum/metadata/extraction/tree-digest checks and all four source-surface
  predicates without executing JVM code.
- Coordinator inspection rejected the lock correction as incomplete:
  `100 * 10ms` still times out a legitimate concurrent caller after about one
  second, shorter than the observed 1.7-second cached verifier and far shorter
  than a network fetch. The asynchronous interval renewal can also race
  release/removal.
- This ordinary verification defect returns to the same existing
  `implementer`, expected and explicitly dispatched `gpt-5.6-terra` /
  `medium`. It must use a separately bounded realistic wait deadline and
  race-safe live-owner detection/cleanup, add a regression holding the lock
  beyond one second, update evidence, and preserve the no-JVM-build boundary.

## P1 final review correction assignment

- Final style/reliability re-review produced the accepted batch recorded in the
  review log: true streaming download bounds, fail-closed one-to-one ZIP
  metadata, staging validation before publication, atomic stale-generation
  takeover, end-to-end digest/malicious-archive tests, and one README wording
  fix.
- The same existing `implementer` owns the batch. Expected model
  `gpt-5.6-terra`; expected reasoning `medium`; both are explicit in dispatch.
  It must use RED-first tests, preserve the static-only/no-JVM-build boundary,
  update deterministic evidence, and rerun focused checks. It may change only
  P1 paths/report and may not commit, push, merge, or spawn children.

## P1 final correction incomplete result

- The owner implemented streaming bounded download, fail-closed metadata
  matching, staging validation, atomic stale-lock quarantine, and README
  wording; its focused suite passed 13/13.
- The result is not accepted because the assigned end-to-end malicious ZIP,
  two-competing-waiter, and bad-pinned-digest/preserved-prior-source regressions
  were explicitly left incomplete.
- Those exact missing behaviors return to the same existing `implementer`,
  expected and explicitly dispatched `gpt-5.6-terra` / `medium`. It must finish
  them without broadening scope or running a JVM build.

## P1 acceptance re-review correction assignment

- Acceptance re-review found four remaining items recorded in the review log:
  duplicate metadata multiplicity, generation-serialized stale takeover,
  stalled-download timeout cleanup, and capability-failure publication
  preservation.
- The same existing `implementer` owns the correction. Expected model
  `gpt-5.6-terra`; expected reasoning `medium`; both are explicit in dispatch.
  It must add the exact forced-interleaving/integration regressions, preserve
  the no-JVM-build boundary, and rerun focused checks. It may change only P1
  paths/report and may not commit, push, merge, or spawn children.

## P1 acceptance correction partial result

- Exact metadata multiplicity and an injected unit-level streaming timeout were
  added; 17/17 focused tests passed.
- The result is not accepted. The owner explicitly left the forced stale
  observer/replacement-generation regression and timeout-through-
  `prepareFixture()` cleanup regression incomplete, and did not report the
  assigned capability-rejection/preserved-publication integration case.
- These three exact requirements return to the same existing `implementer`,
  expected and explicitly dispatched `gpt-5.6-terra` / `medium`. It may not
  return until all three are present and passing.

## P1 final integration-context replacement

- The existing correction owner completed exact metadata multiplicity,
  generation-safe stale takeover, and abortable timeout behavior; 18/18 focused
  tests passed.
- The same owner returned incomplete three times and still omitted the two
  assigned `prepareFixture()` integration cases: stalled-download cleanup/retry
  and missing-capability preservation/retry. That context is no longer accepted
  for completion.
- A fresh existing `implementer` receives only those two missing tests/fixes.
  Expected model `gpt-5.6-terra`; expected reasoning `medium`; both are explicit
  in dispatch. It must preserve all existing work, own only P1 files/report,
  add RED/GREEN integration evidence, and may not build JVM, spawn children,
  commit, push, or merge.

## P1 shared-source/lock simplification assignment

- Final integration review showed that cross-process stale takeover and atomic
  replacement were unnecessary complexity for the now static-only reference.
- The orchestrator accepted a shallower design: cache only the immutable
  checksum-verified archive; use unique per-run extraction, validate digest and
  capabilities before returning evidence, and remove the extraction in
  `finally`. Remove revision locks, shared extracted-source publication,
  `.ready` recovery, and their preservation/takeover abstractions/tests.
- A fresh existing `implementer` owns this simplification. Expected model
  `gpt-5.6-terra`; expected reasoning `medium`; both are explicit in dispatch.
  It must retain streaming byte/time bounds and fail-closed archive safety, add
  concurrent independent-run cleanup plus real partial-timeout cleanup tests,
  update the current report/runbook, and may not build JVM, spawn children,
  commit, push, or merge.

## P1 per-run staging cleanup correction

- Independent simplification verification passed 8/8 tests, syntax, Prettier,
  `git diff --check`, and the real static archive/digest/capability command.
- Coordinator inspection found that each invocation recursively removed the
  shared `staging` parent in `finally`, allowing one completed caller to delete
  another caller's active unique extraction.
- This returns to the same simplification `implementer`, expected and explicitly
  dispatched `gpt-5.6-terra` / `medium`. It must remove only caller-owned
  staging and remove the parent only if empty, with a forced-overlap regression.
  No JVM build, commit, push, merge, or child dispatch is allowed.

## P1 incremental checksum correction

- Final archive-only style review is clean. Reliability found that archive
  checksum verification still loads the allowed 256 MiB file into memory and
  multiplies that cost across concurrent callers.
- The same simplification `implementer` receives the single correction,
  expected and explicitly dispatched `gpt-5.6-terra` / `medium`. It must add a
  RED regression proving incremental stream hashing, preserve failure cleanup
  and bounds, update evidence, and may not build JVM, spawn children, commit,
  push, or merge.

## P1 acceptance

- Final focused evidence: 1 file / 11 tests, module syntax, supported-file
  Prettier, `git diff --check`, and the real cached static source-reference
  command all pass.
- The real command verifies the pinned archive SHA-256, fail-closed ZIP
  metadata/path/link/expanded-size constraints, pinned symlink-aware source
  digest, and static Command/Query/Projection-subscription/Event-subscription
  surfaces. It does not download or execute a JDK, Gradle, or Spine JVM.
- Final reviewer dispositions are recorded in the P1 review record: style
  clean, documentation clean, TypeScript/API N/A, reliability clean.
- P1 is accepted subject to the change-sensitive full repository test gate,
  commit, and immediate push. The next feature slice is A1, the behavior-
  preserving `client` to `client-node` migration.

## P1 full verification

- The first sandboxed full-suite attempt exposed an environmental limitation:
  ZeroMQ IPC socket binds under `/private/tmp` failed with `EPERM`. A focused
  reproduction confirmed the same operating-system denial before any changed
  P1 behavior was involved.
- The identical Spine TS gate was rerun with local IPC access and passed:
  141 test files / 2,649 tests passed; 3 files / 25 tests were skipped.
- Proto generation verified 39 copied Spine source checksums and 48 frozen
  descriptors at normalized digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`;
  the generated TypeScript build also passed.
- No Gradle, Java, JDK setup, Spine JVM build, or Spine JVM execution occurred.
  Wave 4 JVM work remains limited to the pinned static source reference.
- P1 is ready for commit and immediate task-branch push. Work continues with
  A1, the behavior-preserving `client` to `client-node` migration.

## A1 implementation assignment

- Classification: standard, public package migration with broad workspace
  consumer impact but intentionally no behavioral API change.
- Existing role: `implementer`.
- Expected model: `gpt-5.6-terra`.
- Expected reasoning: `medium`.
- Both model and reasoning are explicit in the child dispatch. The execution
  surface exposes the immutable configured role/profile but not independent
  runtime self-introspection; this limitation will be recorded when accepting
  the result.
- Ownership: `packages/client/**` to `packages/client-node/**`, the package's
  tests and code-generation configuration, and every root/package/example
  dependency or import required for the atomic workspace migration. The owner
  must preserve unrelated changes and coordinate with the shared worktree.
- Acceptance:
  - the existing package is renamed to
    `@spine-event-engine/client-node` with its current behavior preserved;
  - all workspace consumers, build references, documentation inventories, and
    lockfile entries use the new name/path;
  - the old `@spine-event-engine/client` package name and `packages/client`
    path are absent except in intentional historical planning records;
  - Node-only transport and Entity-column generation remain owned only by
    `client-node`;
  - focused package tests, typechecking/build, formatting, and repository
    searches pass.
- The owner may not commit, push, merge, spawn children, or build/run Spine
  JVM. Public verb redesign belongs to A2, not this slice.

## A1 implementation progress

- The assigned existing `implementer` started the standard, behavior-preserving
  package migration. Its immutable configured profile is
  `gpt-5.6-terra` / `medium`, explicitly dispatched by the coordinator;
  independent runtime metadata introspection is unavailable on this surface.
- RED: the focused package-export test was extended to require the
  `@spine-event-engine/client-node` manifest identity. Before the migration it
  failed exactly because the manifest declared `@spine-event-engine/client`.
- GREEN: `packages/client` was moved atomically to `packages/client-node`; the
  manifest, workspace dependencies/imports, codegen entry points, root build
  inventories, docs/snippets, TypeDoc/API checks, and lockfile links were
  migrated without changing the client implementation or public verbs.
- Focused GREEN evidence: `pnpm --config.verify-deps-before-run=false exec
vitest run packages/client-node/test/package-exports.test.ts
packages/client-node/test/index.test.ts scripts/generate-entity-columns.test.mjs`
  passed 3 files / 15 tests. Broader package and build verification remains in
  progress; no Spine JVM build or execution has been run.

## A1 implementation evidence

- Final focused test evidence: `pnpm
--config.verify-deps-before-run=false exec vitest run packages/client-node/test
scripts/generate-entity-columns.test.mjs` passed 9 files / 126 tests. This
  includes the three real native loopback transport cases, rerun outside the
  sandbox because its localhost bind is denied there with `EPERM`.
- `pnpm typecheck:build` passed after fresh frozen-lockfile workspace linking;
  Proto generation also verified 39 copied-source checksums and 48 frozen
  descriptors at normalized digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`.
- `pnpm format:check`, `node docs/check-typescript-snippets.mjs`, `pnpm
docs:check`, and `git diff --check` passed. The documentation/API inventory
  now names `client-node`.
- A precise non-historical repository scan found no
  `@spine-event-engine/client` package name or `packages/client` path; the old
  directory is absent. Historical planning, work-log, decision, and review
  records were intentionally preserved.
- Node-native `@connectrpc/connect-node` transport ownership remains only in
  `packages/client-node` for this client boundary. No client behavior or public
  verbs were redesigned; that remains A2 scope.
- `pnpm install --offline --frozen-lockfile` could not complete because this
  worktree lacked cached dependencies and sandbox DNS is restricted. A
  subsequent authorized `pnpm install --frozen-lockfile` reused all 309 locked
  packages with zero downloads and confirmed the lockfile was current. No
  generated Proto temporary locks/staging directories remain.
- No Spine JVM build or execution, commit, push, merge, or child dispatch was
  performed. The execution surface exposes the immutable configured
  `implementer` profile (`gpt-5.6-terra` / `medium`) but not independent runtime
  metadata introspection.

## A1 correction batch

- The complete review wave is recorded in the A1 review record: style,
  documentation, and TypeScript/API are clean; reliability is N/A because
  runtime behavior did not change.
- Independent verification reproduced one mechanical omission:
  `pnpm docs:check` fails because `scripts/check-api-docs.mjs` still constructs
  `clientIndexPath` for `packages/client/src/index.ts`.
- An existing `implementer` receives only this path/evidence correction.
  Expected model `gpt-5.6-terra`; expected reasoning `medium`; both are explicit
  in dispatch. It must rerun docs/API checks and stale-reference scans, and may
  not build/run Spine JVM, spawn children, commit, push, or merge.

## A1 final verification

- The mechanical API-checker correction now targets
  `packages/client-node/src/index.ts`. TypeDoc/API checks verified all 40
  expected `@spine-event-engine/client-node` exports; snippet checks,
  repository formatting, and `git diff --check` passed.
- The first full-suite attempt passed 140 test files / 2,649 tests but had one
  transient failure in the unchanged delivery-server executable signal test:
  a child reported `exitCode=null` after `SIGINT` rather than graceful zero.
- The isolated delivery-server executable suite then passed all 4/4 signal and
  configuration cases.
- A fresh complete Spine TS gate passed: 141 test files / 2,650 tests passed;
  3 files / 25 tests were skipped. Proto generation verified 39 copied-source
  checksums and 48 frozen descriptors at normalized digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`;
  the generated TypeScript build passed.
- No Spine JVM build or execution occurred. A1 is accepted for commit and
  immediate task-branch push. The next feature slice is A2, the browser-safe
  `client-web` kernel and approved public verbs.

## A2 implementation assignment

- Classification: high-risk public-contract and package-boundary slice. Its
  architecture and verbs were frozen in the approved Wave 4 plan/split, so no
  new requirements-splitting round is required.
- Existing role: `implementer`.
- Expected model: `gpt-5.6-terra`.
- Expected reasoning: `medium`.
- Both fields are explicit in dispatch. Independent runtime self-introspection
  is unavailable; immutable configured role/profile and absence of visible
  mismatch will be the acceptance metadata.
- Ownership: introduce `packages/client-web/**` with a browser-safe common
  kernel and public transport-injection seam; adapt `packages/client-node/**`
  narrowly to reuse that seam while retaining native Node transport and
  Entity-column code generation. Update only the root/workspace dependencies,
  tests, and current docs necessary for this contract.
- Frozen acceptance:
  - public operations use `post`, `send`, `createSubscription`, `activate`, and
    `cancel`;
  - old `query`, `subscribeToState`, and `subscribeToEvents` public verbs are
    removed without deprecation aliases;
  - `client-web` contains no `node:*`, `@connectrpc/connect-node`, React,
    filesystem/process, or Entity-column code-generation imports;
  - `client-node` reuses the exported transport-neutral seam rather than
    duplicating the common client lifecycle;
  - compile/package tests freeze the approved declarations and browser
    dependency scan; existing Node behavior remains covered.
- Explicit gRPC-Web/Connect factories, secure browser IDs, reconnect semantics,
  authentication, and React belong to later slices A3-A4/C5/D1.
- The owner must use focused RED/GREEN evidence and may not commit, push, merge,
  spawn children, or build/run Spine JVM.

## A2 completion-context replacement

- The original A2 owner completed the browser-safe kernel, Node adapter,
  lockfile, BlackBox API, primary docs, API inventory, and focused checks, but
  returned incomplete three times.
- The transient dependency problem is resolved: the coordinator ran
  `pnpm install --frozen-lockfile`; all 309 packages were reused with zero
  downloads, the lock was current, and ZeroMQ setup completed.
- The only implementation gap is the deliberate migration of existing
  Chat/Todo fixtures and any remaining client-facing snippets from typed legacy
  `query`/`subscribeToState`/`subscribeToEvents` helpers to raw `send`
  `QueryResponse` decoding and `createSubscription`/`activate`
  `SubscriptionUpdate` decoding.
- A fresh existing `implementer` owns only that completion and full A2 evidence.
  Expected model `gpt-5.6-terra`; expected reasoning `medium`; both are explicit
  in dispatch. It must preserve the existing A2 design, must not restore
  compatibility aliases, and may not commit, push, merge, spawn children, or
  build/run Spine JVM.

## A2 implementation evidence

- Existing role: `implementer`; expected model `gpt-5.6-terra`; expected
  reasoning `medium`. Both were explicit in dispatch. This execution surface
  exposes no independent runtime metadata introspection, so the immutable
  configured role/profile is the available metadata evidence.
- RED: `packages/client-web/test/client.test.ts` first failed because the
  browser package and its public kernel did not exist. After the package
  manifest was added, the focused test then failed for the intentionally
  missing implementation. The initial package-link failure was environmental:
  a workspace-wide install cannot resolve pre-existing private example package
  metadata from the registry. Focused validation used restored untracked local
  dependency links; no tracked dependency lock change was made.
- GREEN: the focused web test passes 2/2. It proves injected transport and ID
  source behavior, explicit inactive subscription creation, activation, and
  idempotent remote cancellation. `client-node` now provides only the native
  HTTP/2/UUID adapter and delegates to the exported `client-web` kernel.
  The legacy Node client lifecycle, old subscription tests, and exported
  `query`, `subscribeToState`, and `subscribeToEvents` surface were removed;
  Entity-column codegen remains in `client-node`.
- Focused evidence: `tsc -b packages/client-web packages/client-node` passed;
  `vitest run packages/client-web/test/client.test.ts
packages/client-node/test/index.test.ts packages/client-node/test/package-exports.test.ts`
  passed 3 files / 8 tests; `pnpm --filter @spine-event-engine/client-web
check:dependencies` passed; Prettier and `git diff --check` passed.
- No Spine JVM build/execution, child dispatch, commit, push, or merge occurred.
  Concrete gRPC-Web/Connect factories, browser secure-ID generation, reconnect,
  authentication, and React remain later owned slices.

## A2 continuation evidence

- `pnpm install --frozen-lockfile` first proved the expected missing
  `client-web` lock importer mismatch. A subsequent pnpm-managed regeneration
  produced the `packages/client-web` importer and `client-node` workspace link.
  With the temporary diagnostic workspace-link setting removed, frozen install
  reports that the lockfile is current; its local node_modules reconstruction
  then encountered sandbox DNS `ENOTFOUND` retries for uncached artifacts.
- Before that reconstruction, full `pnpm typecheck:build` passed after
  migrating the compiled `@spine-event-engine/testing` BlackBox scope from the
  removed verbs to `send` and `createSubscription`. TypeDoc now includes the
  browser package and the API inventory freezes its new public exports.
- The user guide and testing README now show `send`, explicit subscription
  creation, `activate`, and `cancel`. Remaining test-fixture migrations are
  tracked separately because their old decoded state/event helpers require
  explicit raw `QueryResponse` decoding and `Topic` construction; they are not
  retained as public aliases.

## A1 correction evidence

- Corrected `scripts/check-api-docs.mjs` so `clientIndexPath` resolves the
  migrated `packages/client-node/src/index.ts` root, matching the already
  migrated TypeDoc module path and package identity checks.
- `pnpm docs:check` passed: Proto generation verified 39 copied-source
  checksums and 48 frozen descriptors at normalized digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`,
  then TypeDoc and the API inventory completed successfully.
- `node docs/check-typescript-snippets.mjs` and `git diff --check` passed.
  A precise non-historical scan across live roots/configuration found no
  `packages/client` path or `@spine-event-engine/client` package identity;
  historical records were excluded intentionally.
- No public or runtime behavior changed. No Spine JVM build/execution, child
  dispatch, commit, push, or merge was performed. The execution surface exposes
  the immutable configured `implementer` profile (`gpt-5.6-terra` / `medium`)
  but not independent runtime metadata introspection.

## A2 fixture-completion evidence

- Existing role: `implementer`; expected model `gpt-5.6-terra`; expected
  reasoning `medium`. Both were explicit in dispatch. Independent runtime
  metadata introspection remains unavailable; the immutable configured role and
  profile are the available acceptance metadata.
- Chat, Todo, and runner-neutral BlackBox client-facing tests now construct raw
  `Topic` messages, use `createSubscription()` then `activate()`, and decode
  `QueryResponse` / `SubscriptionUpdate` payloads with their generated schemas.
  The public test/docs surfaces now use `post`, `send`,
  `createSubscription`, `activate`, and `cancel`; no compatibility alias was
  restored. Storage and query-builder `.query()` APIs were not changed.
- RED/GREEN: the new raw-subscription fixture calls first failed in the elevated
  focused run with `TypeError: subscription.activate is not a function`.
  Root cause: BlackBox's ownership `Tracked` wrapper delegated `cancel` and
  iteration but not activation. The minimal wrapper delegation was added; the
  same type/build plus focused loopback run then passed 3 files / 51 tests.
- Fresh deterministic evidence: `pnpm
--config.verify-deps-before-run=false typecheck:build` passed and reverified
  39 copied Proto source checksums plus 48 frozen descriptors at normalized
  digest `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`.
  `vitest run packages/client-web/test packages/client-node/test` passed 5
  files / 32 tests. `pnpm --filter @spine-event-engine/client-web run
check:dependencies` passed. The BlackBox/Chat/Todo run required elevated
  localhost sockets because sandbox listeners fail with `EPERM`; after
  elevation, all 51 focused tests passed.
- `pnpm docs:check`, `pnpm format:check`, and `git diff --check` passed. A
  targeted live-surface scan found no `.query(`, `.subscribeToState(`, or
  `.subscribeToEvents(` calls under client packages, testing, Chat/Todo,
  current docs, or the root README. The only excluded API-document occurrence
  is the unrelated `RecordStorage.query()` documentation.
- Worktree cleanliness: no generated lock, staging, or temporary artifacts are
  untracked. The listed untracked paths are the intended A2 new
  `client-web`, Node adapter, and dependency-scan sources; all other changes
  remain the existing A2 work. No Spine JVM build/execution, child dispatch,
  commit, push, or merge occurred.

## A2 pre-review API-documentation correction

- RED: direct `node scripts/check-api-docs.mjs` reproduced the pre-review
  failure. TypeDoc's direct `packages/client-node/src` module omitted
  `ClientKernel`, `ClientOperationOptions`, `ClientOptions`, `ClientOutcome`,
  `ClientProtocolError`, `ClientRequest`, `ClientTransport`, and `Subscription`.
  The TypeScript re-exports themselves resolved for consumers, but TypeDoc
  followed the external clauses into the `client-web` module and therefore did
  not create direct client-node reflections.
- GREEN: the client-node root now declares local aliases for the seven shared
  types and exports a local constant referencing the same `ClientProtocolError`
  constructor. This preserves the `client-web` seam and Node root import
  contract while giving TypeDoc direct reflections. A compile-time package test
  names each re-exported client-web contract so declaration drift is tested.
- Fresh evidence: `node scripts/check-api-docs.mjs` exits successfully and
  reports all 31 expected client-node exports. `pnpm docs:check` regenerated
  the docs after verifying the 39 copied source checksums and 48 frozen
  descriptors. Focused client-web/client-node tests pass 5 files / 33 tests;
  `pnpm --filter @spine-event-engine/client-web run check:dependencies`,
  `pnpm format:check`, and `git diff --check` pass.
- No compatibility alias, JVM operation, child dispatch, commit, push, or merge
  was performed.

## A2 pre-review correction

- Independent coordinator verification confirmed the full TypeScript build,
  5 focused client files / 32 tests, and the browser dependency scan.
- `pnpm docs:check` then failed: TypeDoc/API verification could not find eight
  intended `client-node` re-exports (`ClientKernel`, `ClientOperationOptions`,
  `ClientOptions`, `ClientOutcome`, `ClientProtocolError`, `ClientRequest`,
  `ClientTransport`, and `Subscription`).
- The current fixture-completion `implementer` receives this exact
  declaration/export-documentation correction. Expected model
  `gpt-5.6-terra`; expected reasoning `medium`; both remain explicit. It must
  preserve the approved package seam, add executable declaration/API evidence,
  rerun docs/API plus affected client checks, and may not build/run Spine JVM,
  spawn children, commit, push, or merge.

## A2 consolidated correction assignment

- The complete first A2 review wave found terminal ownership, activation/
  cancellation race, protocol validation, public API inventory/factory,
  behavioral coverage, and documentation defects. Exact findings and the
  aggregated eight-part correction are recorded in the A2 review record.
- A fresh existing `implementer` owns the complete correction across
  `client-web`, the narrow Node adapter/declarations, affected tests/checkers,
  and current documentation.
- Expected model `gpt-5.6-terra`; expected reasoning `medium`; both are explicit
  in dispatch. Independent runtime introspection is unavailable.
- Reconnect, retry, resync/gap, bounded update queues, and durable signal
  composition remain A4. A2 must restore terminal close, race-safe finite
  cancellation, protocol identity validation, and accurate deferment claims.
- No compatibility aliases, child dispatch, commit, push, merge, or Spine JVM
  build/execution are allowed.

## A2 correction verification-context replacement

- The correction owner implemented all five exact reliability regressions
  requested after review. Focused `client-web` evidence is now 9/9 tests,
  including stalled remote cleanup bounds, source close on cancellation
  failure, invalid-activation no-double-cancel, natural stream owner release,
  and pending iterator termination.
- The owner returned before the required full verification three times and is
  no longer accepted as the completion context.
- A fresh existing `implementer` owns final A2 verification and only
  failure-driven fixes. Expected model `gpt-5.6-terra`; expected reasoning
  `medium`; both are explicit in dispatch.
- It must run all client tests, the exact 51 BlackBox/Chat/Todo fixtures, root
  type/build, both API inventories, browser dependency scan, docs/snippets,
  formatting, diff/legacy scans, and targeted client coverage. It may not
  commit, push, merge, spawn children, or build/run Spine JVM.

## A2 coverage correction assignment

- Final functional verification passed, including 5 client files / 40 tests and
  the exact 3 loopback files / 51 tests.
- Native branch coverage is not accepted: `client-web` is 51.80% (43/83),
  although `client-node` is 93.33% (196/210). The task requires at least 90%
  native branch coverage for changed behavior.
- A fresh existing `implementer` owns test-only coverage expansion plus
  failure-driven kernel fixes. Expected model `gpt-5.6-terra`; expected
  reasoning `medium`; both are explicit in dispatch.
- Tests must cover real outcome/error/context/input/abort/close/subscription
  branches and may not use ignore directives, implementation-only exports, or
  vacuous assertions to inflate coverage. It may not commit, push, merge,
  spawn children, or build/run Spine JVM.

## A2 final correction assignment

- Focused re-review resolved the package seam, exports, Node factory, identity
  validation, primary close/activation races, coverage, package READMEs, and
  testing guide.
- Remaining findings are limited to genuine non-cooperative cleanup bounds,
  failure-safe owner/source cleanup, early iterator remote cancellation,
  broken/stale user/API guide content, semantic snippet checking, and
  member-level web-interface TypeDoc.
- Exact findings and the seven-part correction are recorded in the A2 review
  record.
- A fresh existing `implementer` owns this final batch. Expected model
  `gpt-5.6-terra`; expected reasoning `medium`; both are explicit in dispatch.
  It may not add compatibility aliases or A3/A4 behavior, commit, push, merge,
  spawn children, or build/run Spine JVM.

## A2 consolidated correction evidence

- Existing role: `implementer`; expected model `gpt-5.6-terra`; expected
  reasoning `medium`. Both were explicit in dispatch. Independent runtime
  metadata introspection is unavailable on this surface; no visible profile
  mismatch occurred.
- RED/GREEN: focused shared-kernel regressions initially exposed that client
  close admitted no owned work and accepted invalid subscriptions. The green
  kernel now tracks unary abort controllers and every inactive/activating/active
  subscription, closes them terminally, serializes activation/cancellation,
  cancels a late accepted subscription once, bounds remote cancellation, and
  closes the owned source even if cleanup fails. It validates accepted and
  delivered subscription identities/topics before exposure and removes terminal
  owners after invalid or naturally completed streams.
- The Node factory is a nonconstructible frozen factory object with explicit
  shared-kernel return types. Local TypeDoc aliases document the shared error
  constructor identity. `check-api-docs` independently checks all eight
  declared and TypeDoc `client-web` root exports.
- Current docs/snippets now state the A3 transport-factory and A4 reconnect,
  signal-lifetime, bounded-queue, overflow, and gap/resync deferrals. The
  testing README again states public `postEvent`, `eventually`, tenant, zone,
  and timeout constraints. No compatibility alias or concrete browser transport
  was added.
- Focused evidence: `vitest run packages/client-web/test packages/client-node/test`
  passed 5 files / 36 tests; web and Node package typechecks passed;
  `pnpm --filter @spine-event-engine/client-web run check:dependencies`,
  `pnpm format:check`, and `git diff --check` passed. Full TypeScript build
  passed with the 39 copied-source checksum and 48 frozen-descriptor checks.
  Direct API verification passed with 31 client-node and 8 client-web expected
  exports. No Spine JVM build/execution, commit, push, merge, or child dispatch
  occurred.
- Elevated localhost loopback evidence: the exact BlackBox/Chat/Todo fixture
  selection passed 3 files / 51 tests after a rebuilt kernel made a canceled
  iterator terminal without unobserved stream-abort rejections.

## A2 final verification evidence

- Existing role: `implementer`; expected model `gpt-5.6-terra`; expected
  reasoning `medium`. Both were explicit in the final-verifier dispatch.
  This surface does not expose independent runtime self-introspection; no
  visible profile mismatch occurred.
- Fresh root build: `pnpm --config.verify-deps-before-run=false typecheck:build`
  passed, after verifying all 39 copied-source checksums and 48 frozen
  descriptors at normalized digest
  `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`.
- Fresh focused suites: `vitest run packages/client-web/test
packages/client-node/test` passed 5 files / 40 tests. The elevated localhost
  selection `packages/testing/test/index.test.ts`,
  `examples/chat/test/model-registry.test.ts`, and
  `examples/todo/test/black-box.test.ts` passed 3 files / 51 tests.
- The nine `client-web` tests include each requested reliability proof: bounded
  stalled remote cancellation, serialized activation with a late accepted
  subscription cancelled once, pending iterator termination, source cleanup
  after remote cancellation failure, and no second cancellation after natural
  stream completion.
- `pnpm --filter @spine-event-engine/client-web run check:dependencies`, direct
  API inventory (31 `client-node`, 8 `client-web` exports), `pnpm docs:check`,
  TypeScript-snippet validation, `pnpm format:check`, and `git diff --check`
  all passed. The browser-source forbidden-import scan found none.
- Scoped coverage was run without the repository-wide global threshold:
  `client-web` passed 9 tests with 51.80% branch coverage (43/83); the complete
  `client-node` package passed 31 tests with 93.33% branch coverage (196/210).
  A first parallel coverage attempt failed only because two Vitest processes
  removed the same `coverage/.tmp`; the isolated sequential Node rerun passed.
- The legacy client-verb scan found no client `.query()`,
  `.subscribeToState()`, or `.subscribeToEvents()` calls. Its sole match is the
  unrelated documented `RecordStorage.query()` API. Status/configuration drift
  remains limited to the intended A2 files; no generated staging/configuration
  artifact is untracked. No runtime change, Spine JVM build/execution, child
  dispatch, commit, push, or merge occurred during final verification.

## A2 client-web coverage correction evidence

- Existing role: `implementer`; expected model `gpt-5.6-terra`; expected
  reasoning `medium`. Both were explicit in dispatch. Independent runtime
  metadata introspection remains unavailable on this surface; no mismatch was
  visible.
- RED/GREEN: new public-contract tests first demonstrated that
  `activate({ signal })` still invoked `Subscribe` when the signal had already
  been aborted, and that a delivered subscription identity violation could be
  observed as normal iterator completion. The minimal kernel correction rejects
  pre-aborted activation before transport use and performs failure cleanup
  without converting the protocol error into terminal completion.
- Test coverage now exercises `post` ok/error/rejection/malformed/mismatched
  acknowledgements; tenant/zone/actor validation and cloning; send builders,
  context, abort and close; subscription activation, topic/identity validation,
  iteration, cancel, close, natural completion, failure and timeout paths.
  No ignore directives, implementation-only exports, compatibility APIs, or
  A3/A4 behavior were added.
- Fresh isolated sequential native coverage passed 17 tests at 91.01% branch
  coverage (81/89), exceeding the required 90% threshold. The complete client
  suite passed 5 files / 48 tests, and both `client-web` and `client-node`
  TypeScript project checks passed. Fresh root `typecheck:build` passed after
  verifying 39 copied Proto source checksums and 48 frozen descriptors at
  normalized digest `e52be2d85355d13701055a4df3fed5eab8ad17ebe3bcc89482cc44b5cc02737f`;
  `pnpm format:check` and `git diff --check` also passed. No Spine JVM
  build/execution, child dispatch, commit, push, or merge occurred.

## A2 final correction completion evidence

- Existing role: `implementer`; explicitly dispatched profile `gpt-5.6-terra`
  / `medium`. Independent runtime self-introspection is unavailable; no visible
  profile mismatch occurred.
- RED/GREEN: a Cancel transport that ignores abort originally left public
  cleanup pending; iterator `return()` issued no remote Cancel; and an invalid
  accepted subscription whose compensating Cancel failed could skip terminal
  owner release. The green kernel races Cancel against the finite cleanup
  timeout while observing late rejection, releases ownership in `finally`,
  settles every close cleanup before reporting failures, closes its owned
  source exactly once, and makes iterator return use the same bounded Cancel.
  A failed validation and failed compensating cleanup reports both errors in
  original-then-cleanup order without leaving a later close to recancel it.
- Public client TSDoc now covers `ClientRequest.post/send/createSubscription`,
  `Subscription.activate/cancel`, and `ClientTransport.createRequestId/close`.
  The guide shows raw `QueryResponse`/`Any` unpacking and raw
  `SubscriptionUpdate` discrimination, fixes local symbols/imports, and states
  that signal-lifetime composition, decoded queues, overflow, reconnect, and
  gap/resynchronization remain A4. The API overview now has the correct
  fifteen-entry-point status.
- Snippet validation retains syntax diagnostics and semantically checks client
  snippets for unresolved local symbols while preserving explicit public-export
  validation; its declaration inventory resolves re-exports independently.
- Fresh evidence: focused client suite passed 5 files / 51 tests; the exact
  loopback BlackBox/Chat/Todo selection passed 3 files / 51 tests under the
  required unrestricted localhost listener context after sandboxed execution
  correctly reported `listen EPERM`. Root `typecheck:build`, docs/snippet/API,
  and browser dependency checks passed. Isolated client-web coverage passed 20
  tests with 91.91% branches (91/99), 97.15% statements, 96.07% functions,
  and 99.45% lines.
- No Spine JVM build/execution, compatibility alias, A3/A4 behavior, child
  dispatch, commit, push, or merge occurred.

## A2 final coordinator acceptance

- The coordinator independently inspected activation, cancellation, iterator,
  owner, and source-close paths after the final correction. Every accepted
  second-wave finding is resolved and no P0/P1 risk remains; the review record
  therefore closes A2 without an automatic third complete review wave.
- Fresh focused verification passed: root `typecheck:build`; client suite
  5 files / 51 tests; native BlackBox/Chat/Todo loopback suite 3 files / 51
  tests; `docs:check`; semantic snippet checking; browser dependency checking;
  formatting; and `git diff --check`.
- Fresh isolated `client-web` coverage passed 20 tests with 91.91% branches
  (91/99), 97.15% statements, 96.07% functions, and 99.45% lines.
- Spine JVM remained a read-only static reference. No Java, JDK, Gradle, JVM
  build, JVM generation, or JVM execution was used. Runtime JVM compatibility
  remains an explicitly deferred limitation.
- A2 is accepted subject to the full TypeScript repository gate, commit, and
  immediate task-branch push. The next feature slice is A3: explicit browser
  transport factories, credential metadata injection, and secure browser
  request IDs.
- The fresh full TypeScript repository gate then passed 138 test files / 2,581
  tests, with 3 files / 25 tests intentionally skipped. This closes the A2
  verification condition without any Spine JVM build or execution.

## A3 implementation assignment

- Classification remains high-risk because this slice adds public browser
  transport factories, credential metadata handling, and the secure
  request-identity boundary. The Sol/high Wave 4 split already froze the
  architecture, so no new requirements-splitter dispatch is needed.
- Existing role: `implementer`; bounded ownership is
  `packages/client-web/**`, the minimum required package/project/lock files,
  focused public API/docs records, and this work log. Existing Node behavior is
  read-only unless a compile-safe shared type correction is strictly required.
- Expected model `gpt-5.6-terra`; expected reasoning `medium`; both fields are
  explicit in dispatch. The Desktop role profile is immutable, while
  independent runtime self-introspection may remain unavailable and will be
  recorded before acceptance.
- Behavior-first acceptance:
  1. expose separate, explicit gRPC-Web and Connect browser factories using
     `@connectrpc/connect-web`, with no probing or fallback;
  2. permit application-supplied credential/request metadata without retaining,
     logging, or putting secrets into request IDs;
  3. generate browser request IDs through `crypto.randomUUID()` when available,
     otherwise through `crypto.getRandomValues()` with correct UUID v4
     version/variant bits;
  4. fail before transport invocation when secure randomness is unavailable;
  5. prove both explicitly selected transports execute the same post/query
     contract, prove selection never falls back, and cover metadata plus secure
     randomness paths with RED/GREEN tests;
  6. preserve browser dependency isolation and all A2 lifecycle behavior.
- The owner may not implement reconnect/A4, auth-gateway/session policy,
  React, Envoy, compatibility aliases, or broad documentation. It may not
  build/run Spine JVM, spawn children, commit, push, or merge.

## A3 implementation evidence

- Existing role: `implementer`; expected model `gpt-5.6-terra`; expected
  reasoning `medium`. Both fields were explicit in dispatch. This surface does
  not expose independent runtime self-introspection; the immutable configured
  profile is the available metadata and no visible mismatch occurred.
- RED: before production changes, the three new focused public-contract tests
  failed because `Client.forGrpcWeb` and `Client.forConnect` did not exist.
  The failures were `TypeError: factories.forGrpcWeb is not a function`, not
  test setup errors.
- GREEN: `client-web` now has explicit `forGrpcWeb()` and `forConnect()`
  factories backed only by their respective `@connectrpc/connect-web` factory.
  There is no probing or fallback. `onRequestMetadata` is deliberately a
  synchronous per-call provider; it supplies fresh headers through a Connect
  interceptor, and the client neither logs nor incorporates those values into
  request IDs. Cookie/session fetch behavior remains C5 work.
- Browser IDs use `crypto.randomUUID()` first, then `crypto.getRandomValues()`
  with UUID v4 version/variant bits, and reject before the first transport call
  if neither secure API exists. The exact `@connectrpc/connect-web@2.1.2`
  registry dependency and normal integrity lock entry were added. The regular
  scoped pnpm add path exposed unrelated existing workspace resolution for
  unpublished `@spine-event-engine/server@0.0.0` from `proto-tools`; the
  dependency metadata was therefore added from verified registry metadata,
  without committing a tarball or path dependency.
- Focused GREEN evidence: `vitest run packages/client-web/test/client.test.ts`
  passed 24/24 tests; the browser client and Node adapter suite passed 5 files
  / 55 tests; `tsc --noEmit -p packages/client-web/tsconfig.json`, browser
  dependency isolation, TypeDoc/API inventory (10 browser exports), repository
  formatting, and `git diff --check` all passed. No Spine JVM build, launch,
  generation, test, or other execution occurred. A4 reconnect/resync/gap,
  auth gateway/session integration, React, Envoy, and aliases remain out of
  scope.
- Scoped native coverage (`vitest run --coverage
--coverage.include='packages/client-web/src/**'
packages/client-web/test/client.test.ts`) passed at 90.99% branches
  (101/111), 97.41% statements, 96.66% functions, and 99.50% lines. The
  unscoped focused coverage invocation correctly failed only because Vitest
  applied the repository-wide 90% threshold to every unexecuted package; it is
  not used as slice evidence.

## A3 consolidated-review correction evidence

- Existing role/profile remains the explicitly dispatched `implementer`:
  `gpt-5.6-terra` / `medium`. Independent runtime self-introspection is not
  exposed on this surface; no visible mismatch occurred.
- RED: the interceptor-aware replacement test initially failed because its
  first test-only harness imported Connect's type-only `applyInterceptors` as a
  runtime function (`TypeError: applyInterceptors is not a function`). The
  harness now implements the same minimal reverse interceptor composition and
  normalizes call headers before invoking it. This is test code only; browser
  factory production behavior was not changed.
- GREEN: direct, typed calls to `Client.forGrpcWeb()` and `Client.forConnect()`
  now prove each protocol runs both `Post` and `Read`, with a fresh metadata
  value on every call. A thrown `onRequestMetadata` provider rejects before the
  harness's next/transport path. The tests import the real
  `BrowserClientOptions` and use the real static factory members—no
  hand-authored `unknown` factory shape remains.
- The practical user guide now has a compile-checked `client-web` workflow:
  explicit gRPC-Web versus Connect choice, synchronous fresh-header hook,
  secure Web Crypto request-ID behavior, and clear C5 auth/session/provider
  and A4 reconnect/queue/gap deferrals. It does not claim cookies, sessions,
  OIDC, or reconnect behavior exists in A3.
- Fresh correction evidence: focused `client-web` test 25/25; client-web plus
  client-node suite 5 files / 56 tests; root `typecheck:build`; semantic
  TypeScript snippets; TypeDoc/API inventory (10 web exports); dependency
  isolation; formatting; and diff hygiene all passed. Scoped client-web
  coverage remains 101/111 branches (90.99%), 97.41% statements, 96.66%
  functions, and 99.50% lines. No JVM build, launch, generation, test, or
  other execution occurred.

## A3 review convergence

- Targeted re-review returned clean style/maintainability, documentation,
  TypeScript/API, and performance/reliability dispositions. The reliability
  reviewer independently reran the web suite at 25/25.
- No P0-P2 finding remains. A3 is accepted subject to the full TypeScript
  repository gate, commit, and immediate task-branch push.
- Spine JVM remained a read-only static reference; no Java, JDK, Gradle, JVM
  build, generation, test, or execution occurred.
- The fresh full TypeScript repository gate passed 138 test files / 2,586
  tests, with 3 files / 25 tests intentionally skipped. This closes A3
  verification without any Spine JVM operation.

## A4 public-lifecycle planning assignment

- A4 remains high-risk because it changes the public subscription contract and
  implements retry timing, bounded queues, generation ownership, terminal
  cancellation, and reconnect behavior.
- A demonstrated contract ambiguity requires the existing
  `requirements_splitter`: a raw Spine `Topic` contains a target type/criteria
  but no entity-versus-event discriminator, while the approved behavior must
  re-query entity state and emit `gapPossible` for events after reconnect.
  The planner must freeze the smallest caller-facing way to distinguish those
  cases without restoring the removed typed facade or inventing a generic
  cursor/cache layer.
- Existing role: `requirements_splitter`; expected `gpt-5.6-sol` / `high`.
  The original role dispatch explicitly set both fields; this bounded follow-up
  reuses that immutable planning context. Independent runtime introspection is
  unavailable unless the surface reports otherwise.
- The plan must also freeze the smallest public shape for separate update and
  lifecycle streams, finite buffer capacities/overflow behavior, retry policy
  and injected scheduler, entity resync result delivery, and terminal
  cancellation/close. It must preserve the already approved defaults: five
  attempts, exponential 250 ms to 5 s delay with jitter, 30 s total retry
  limit, one generation/pipeline/timer, no command retry, and
  notify-and-continue `gapPossible`.
- The planner is read-only and may not edit, implement, spawn children, commit,
  push, merge, or build/run Spine JVM.

## A4 public-lifecycle contract freeze

- The existing `requirements_splitter` completed the bounded follow-up under
  the original explicit `gpt-5.6-sol` / `high` assignment. Independent runtime
  self-introspection is unavailable; no visible profile mismatch occurred. No
  files changed and no Spine JVM work occurred.
- `createSubscription(topic, options)` now requires an explicit discriminated
  kind. Event options are `{ kind: "event", signal? }`; Entity options are
  `{ kind: "entity", authoritativeQuery: () => Query | { build(): Query },
signal? }`. The built Query target must be byte-equivalent to the Topic
  target, while the request scope still replaces its context. This avoids
  inventing Query IDs, ordering, formats, or limits from a Topic.
- `Subscription.updates` is a single-consumer
  `AsyncIterable<SubscriptionDelivery>` with raw update and Entity
  resynchronization variants. `Subscription.lifecycle` is an independent
  single-consumer stream of `connecting`, `connected`, `resynchronizing`,
  `gapPossible`, `failed`, and `closed` notifications. There is no cross-stream
  observation-order guarantee.
- Runtime options are nested under `ClientOptions.subscriptions` and expose
  update count/byte capacities, lifecycle capacity, retry policy, and scheduler.
  Defaults are 64 deliveries, 1,048,576 encoded bytes, 32 lifecycle notices,
  five retries after the initial attempt, and a 30,000 ms total retry window.
  Default delay is exponential from 250 ms through 5,000 ms with ±20% jitter,
  capped at 5,000 ms. Invalid non-positive, non-finite, or unsafe configuration
  fails during client construction. Remote cleanup remains bounded at 1,000 ms.
- One logical subscription owns one serialized activation/reconnect pipeline,
  at most one retry timer, and monotonically increasing generations. Initial
  activation emits `connecting → connected`. Event reconnect emits
  `connecting → gapPossible → connected`; Entity reconnect emits
  `connecting → resynchronizing`, attaches the new stream, sends the
  authoritative Query, enqueues its successful response before held wire
  updates, then emits `connected`.
- Unexpected EOF/transient transport failure retries. Protocol validation,
  query/Topic mismatch, non-OK authoritative Query, buffer overflow,
  cancellation, and client close do not. Overflow terminates both streams and
  bounded cleanup; no data is silently dropped while reporting health.
  `failed` and `closed` are mutually exclusive terminal states.
- Terminal cancel/close invalidates the generation and settles local streams
  before bounded remote cleanup, prevents future work, disposes late wires,
  shares one cleanup promise, aggregates client-close failures, and closes an
  owned transport once. Commands are never retried.
- Implementation is serialized as A4.1 public contracts/bounded channels,
  A4.2 generation-owned activation/cleanup, A4.3 finite reconnect and
  kind-specific recovery, and A4.4 public documentation/regression closure.

## A4.1 implementation assignment

- Existing role/profile: `implementer`, explicitly dispatched
  `gpt-5.6-terra` / `medium`; the follow-up reuses that immutable context.
- Ownership: `packages/client-web/**`, its focused API/dependency checks, and
  A4.1 work/review evidence only. Production lifecycle/reconnect wiring beyond
  the bounded-channel contract remains A4.2/A4.3.
- RED-first acceptance freezes the exact public types/options above and covers
  omitted kind, forbidden Event query, missing Entity query, invalid capacities,
  second iterator, count overflow, byte overflow, independent lifecycle
  overflow, cloned immutable deliveries, and deterministic terminal errors.
- Review concerns are TypeScript/API, style/maintainability, and
  performance/reliability. Documentation receives a justified N/A for A4.1
  because A4.4 owns public guidance after runtime behavior exists.
- The owner may not spawn children, commit, push, merge, add auth/React/Envoy,
  or build/run Spine JVM.

### A4.1 compile-consumer migration clarification

- Because the required explicit subscription kind is an intentional breaking
  public signature, A4.1 may make the smallest mechanical migration in existing
  tests/examples/snippets so the repository compiles. Those callers must select
  the behavior they already exercise (`event` or `entity`) and may not add
  reconnect claims, redesign examples, or perform the broader public
  documentation work reserved for A4.4.

### A4.1 migration completion-context replacement

- The original implementation context completed the production contract and
  bounded-channel core at 31/31 focused tests but repeatedly exhausted its
  continuation turn before applying the now-resolved caller migration. It
  remains accepted as the production author; a fresh existing `implementer`
  owns only migration and final evidence.
- Exact migration: Todo task-list topics are Entity with
  `createTaskListQuery()`; Todo rejection topics are Event; Chat state is Entity
  with the existing by-ID EntityQuery factored as the authoritative factory;
  Chat MessagePosted is Event; BlackBox ProjectionState uses a new matching
  include-all Query; BlackBox EventState is Event. Update consumers read
  `subscription.updates` and unwrap the raw update delivery.
- README/guide compile snippets that demonstrate Entity state must use matching
  Query/Topic targets and may not be mislabeled Event merely to compile.
- Existing role/profile: `implementer`, expected `gpt-5.6-terra` / `medium`,
  both explicit in the new dispatch. Ownership is only the listed callers,
  required public re-exports/API inventory, work/review evidence, and mechanical
  fixes proven by root checks. Production channel/runtime code is read-only
  unless a concrete compilation defect is reported first.

### A4.1 migration completion evidence

- Existing role/profile: `implementer`, explicitly dispatched
  `gpt-5.6-terra` / `medium`. The Desktop surface exposes the immutable role
  profile but not independent runtime self-introspection; no visible profile
  mismatch occurred. No child was spawned and no Spine JVM project was built or
  executed.
- Todo task-list subscriptions now declare `{ kind: "entity" }` with matching
  all-row or by-ID authoritative `createTaskListQuery()` values; Todo rejection
  subscriptions declare `{ kind: "event" }`. Chat factors its existing Chat
  by-ID `EntityQuery` as the state subscription's authoritative factory, while
  `MessagePosted` is an Event subscription. All migrated consumers iterate
  `subscription.updates` and unwrap only raw update deliveries.
- BlackBox now wraps the non-iterable `Subscription` shape, forwards its
  `updates` and `lifecycle` streams, and releases the tracked handle after a
  terminal stream or cancellation. Its contract uses an include-all
  `ProjectionState` query for Entity topics and `{ kind: "event" }` for
  `EventState`. `client-node` additionally re-exports `SubscriptionDelivery`
  and `SubscriptionLifecycle`, the compile defect found by the first root
  build.
- README/user-guide examples now use matching TaskList Entity query/topic
  targets and explicit Entity subscription options; no Entity example is
  mislabeled as an Event merely to satisfy the new signature.
- Fresh evidence: root `typecheck:build` passed after the public type
  re-export correction (39 frozen Proto source checksums and 48 descriptor
  files verified); migrated loopback suites passed (3 files / 51 tests);
  TypeDoc/API inventory, semantic snippets, browser dependency isolation,
  formatting, and diff hygiene passed. Scoped `client-web` coverage passed:
  31/31 tests, 92.25% branches (143/155), 97.03% statements. An initial
  unfiltered coverage command failed only because it applied the repository-wide
  90% threshold to unrelated sources; the required package-scoped include
  filter passed.

## A4.1 coordinator contract-audit correction assignment

- The coordinator rejected A4.1 acceptance after comparing the combined diff
  against the frozen requirements-splitter contract. The production source has
  a duplicate `ClientOwner.transport` declaration; exposes a different
  optional-field retry-policy shape instead of `maxAttempts`,
  `maxElapsedMs`, and `delayMs(attempt)`; models lifecycle as one broad
  interface instead of the frozen discriminated union; and discards the
  supplied retry policy, scheduler, and subscription-kind/query information.
- One bounded correction batch owns only the A4.1 contract/scaffolding and
  focused tests. It must use the exact frozen public names and shapes
  (`CreateSubscriptionOptions`, `SubscriptionLifecycleState`, discriminated
  `SubscriptionLifecycle`, `SubscriptionRetryPolicy`), retain validated retry
  policy/scheduler and Entity recovery metadata for A4.2/A4.3, validate finite
  positive safe retry bounds/scheduler time/delays during client construction
  without starting runtime retry behavior, remove the duplicate declaration,
  and preserve deterministic two-stream overflow failure.
- Existing role: `implementer`; expected model `gpt-5.6-terra`; expected
  reasoning `medium`. Both fields are explicit in the dispatch. The Desktop
  surface exposes the immutable role profile but not independent runtime
  self-introspection; the result is unacceptable on any visible mismatch.
- The implementer may edit only `packages/client-web/src/client/client.ts`,
  its public re-export/API inventory surfaces, focused client tests, and this
  work/review evidence. Caller migrations are otherwise frozen. It must not
  commit, push, merge, spawn children, or build/run Spine JVM.

### A4.1 coordinator contract-audit correction result

- The bounded correction restored the frozen public names and shapes:
  `CreateSubscriptionOptions`, `SubscriptionLifecycleState`, discriminated
  `SubscriptionLifecycle`, and `SubscriptionRetryPolicy` with required
  `maxAttempts`, `maxElapsedMs`, and `delayMs(attempt)`.
- Client construction now validates and retains finite, positive safe retry
  limits and delay values, a finite non-negative scheduler time, and the
  supplied scheduler. The default delay is an integer, uses ±20% jitter, and
  is finally capped at 5,000 ms. Topic subscriptions retain their explicit
  kind/recovery metadata and validated runtime for the later A4.2/A4.3 work.
- The legacy `SubscriptionOptions` public alias was removed; the minimal
  client-node and BlackBox type forwarding now uses `CreateSubscriptionOptions`.
  No caller behavior was otherwise changed.
- Focused evidence: `pnpm --config.verify-deps-before-run=false exec vitest run
packages/client-web/test/client.test.ts` passed (1 file / 32 tests); root
  `typecheck:build` passed with the frozen Proto checksum/digest checks;
  `docs:check`, semantic TypeScript snippets, client-web dependency isolation,
  and `git diff --check` passed. `format:check` reports only the concurrently
  dirty `build-protocol/reviews/T-0075-wave4-browser-interoperability.md`; the
  owned source and focused test files were formatted and are clean. No Spine
  JVM build, run, generation, or test occurred.
- Pre-review documentation lint then found stale A2/A3 wording in the client
  and BlackBox guide sections. The smallest correction now documents bounded,
  independent `updates` and `lifecycle` streams, overflow termination and
  bounded cleanup, and raw updates inside `{ kind: "update" }` deliveries;
  it explicitly leaves reconnect, entity resynchronization, and gap behavior
  to A4.3/A4.4. The existing review record was formatted. Fresh `docs:check`,
  semantic snippet verification, `format:check`, and `git diff --check` all
  passed. No Spine JVM execution occurred.
- The documentation inventory then correctly rejected the newly public
  client-web exports until its deterministic expected-export list was updated.
  It now lists exactly `CreateSubscriptionOptions`, `SubscriptionDelivery`,
  `SubscriptionLifecycle`, `SubscriptionLifecycleState`,
  `SubscriptionRetryPolicy`, `SubscriptionRuntimeOptions`, and
  `SubscriptionScheduler`; fresh `docs:check`, semantic snippets,
  `format:check`, and `git diff --check` passed. No Spine JVM execution
  occurred.
- The same deterministic inventory required the matching client-node surface:
  `CreateSubscriptionOptions`, `SubscriptionDelivery`, and
  `SubscriptionLifecycle`. Its expected-export list now contains exactly those
  three additions. A complete `pnpm docs:check` reached successful final exit;
  `format:check` and `git diff --check` passed after formatting this record.
  No Spine JVM execution occurred.

## A4.1 full-gate coverage correction assignment

- The unrestricted full TypeScript gate passed 138 runnable files / 2,596
  tests, with 3 files / 25 tests skipped, but failed the global branch threshold
  at 89.87% (8,315/9,252) against the required 90%. The preceding sandboxed
  run's 187 failures were environmental loopback/IPC/process `EPERM`/`EAGAIN`
  results and are superseded by the unrestricted all-test pass.
- This is a deterministic A4.1 verification defect: at least 12 additional
  existing branches must be behaviorally covered without excluding sources,
  lowering thresholds, adding ignore directives, or testing unrelated
  implementation details merely for a number.
- The existing implementer owns one focused coverage batch in changed
  client-web/testing behavior and its tests. Prefer meaningful public lifecycle,
  validation, cleanup, scheduler, and BlackBox wrapper branches introduced or
  exposed by A4.1. Production changes are allowed only for a concrete defect
  proven by those tests; otherwise add tests only.
- Expected existing role/profile remains explicitly dispatched
  `implementer`, `gpt-5.6-terra` / `medium`. It may not change coverage
  configuration, commit, push, merge, spawn children, or build/run/generate/test
  Spine JVM. It must run focused coverage first and report the projected global
  branch delta before the expensive unrestricted full gate.
- The accepted A4.1 review correction batch now preserves graceful buffered
  delivery drain while cancellation discards locally, rejects a second pending
  `next()` deterministically, serializes `connecting`, shares one overflow
  error object across both streams, contains detached cleanup failures, and
  preserves terminal `done` during cancellation. It adds focused regressions
  for those behaviors and exact one-delivery byte reclamation; the
  authoritative-query factory remains unevaluated and Topic comparison stays
  deferred to A4.3. TSDoc and the browser guide/README now state the actual
  bounded-queue behavior while retaining only the allowed A4.3/A4.4 deferrals.
  Evidence: web focused 35/35, web+Node focused 5 files / 66 tests, scoped
  client-web coverage 91.62% branches, root `typecheck:build`, `docs:check`,
  semantic snippets, dependency isolation, formatting, and diff hygiene all
  passed. No Spine JVM execution occurred.
- The follow-up A4.1 coverage batch adds public client validation, abort,
  cleanup, aggregate-close, terminal-stream, binary-delivery, and Todo
  lifecycle behavior, plus the existing BlackBox lifecycle cleanup seam. The
  public Todo assertion consumes `connecting` and `connected` and observes
  lifecycle completion after cancellation. The focused BlackBox loopback test
  requires an elevated local bind; sandbox-only execution fails with
  `listen EPERM: operation not permitted 127.0.0.1`, while its elevated exact
  run passed. The elevated full TypeScript coverage gate measured 8,331/9,252
  covered branches (90.0454%), a +16 improvement over the 8,315 baseline and
  above the 90% threshold. No production behavior, coverage threshold, include
  list, or ignore directive changed, and no Spine JVM execution occurred.

## A4.1 final coordinator verification

- The exact final focused BlackBox/Todo lifecycle rerun passed 2 files / 36
  tests with the required local loopback permission.
- The exact final unrestricted TypeScript coverage gate passed 139 runnable
  files / 2,605 tests, with 3 files / 25 tests intentionally skipped. Global
  coverage is 94.10% statements, 90.04% branches (8,331/9,252), 94.24%
  functions, and 94.71% lines.
- Formatting, diff hygiene, root TypeScript build, generated Proto
  checksum/digest validation, documentation/API inventory, semantic snippets,
  and browser dependency isolation are clean. The earlier sandbox-only
  loopback/IPC/process failures are superseded by the unrestricted all-test
  pass.
- All four canonical reviewer concerns are converged. No Spine JVM build, run,
  generation, or test occurred. A4.1 is accepted for commit and immediate
  task-branch push; the next slice is A4.2 generation-owned activation and
  terminal cleanup.

## A4.2 implementation assignment

- Classification remains high-risk: this slice changes long-lived public
  subscription lifecycle, abort/cancel races, generation ownership, remote
  cleanup, and client-close resource behavior. The existing Sol/high A4
  contract freeze is sufficient; no new architecture split is required.
- Behavior acceptance:
  1. one logical subscription owns one serialized activation pipeline and a
     monotonically increasing generation for each Subscribe/Activate attempt;
  2. terminal cancel/client close invalidates the generation before awaiting
     cleanup, aborts pending work, prevents future activation, and disposes any
     late accepted wire exactly once;
  3. concurrent/repeated `activate()` shares one first-connection result;
     repeated `cancel()` shares one cleanup promise;
  4. each accepted wire subscription receives at most one bounded remote
     `Cancel`, including cancel-during-Subscribe and cancel-between-Subscribe-
     and-Activate attachment;
  5. terminal local streams settle before remote cleanup, emit exactly one
     `closed` on cancellation or exactly one `failed` on non-cancellation
     failure, never both, and admit no later transition;
  6. a stalled or rejecting remote cleanup remains bounded by 1,000 ms and is
     observable from `cancel()`/client close without reopening local state;
  7. late stream values or errors from an invalidated generation cannot reach
     consumers or retain the owner.
- A4.2 does not add retry timers, reconnect, `gapPossible`, Entity
  authoritative re-query, or command retry; those remain A4.3. It preserves
  A4.1 queue/default/export behavior and the explicit no-Spine-JVM-execution
  rule.
- Existing role: `implementer`; expected `gpt-5.6-terra` / `medium`, both
  explicit in dispatch. The existing implementation context is reused because
  it owns this runtime and the accepted A4.1 race tests. Independent runtime
  self-introspection remains unavailable; any visible mismatch is
  unacceptable.
- Ownership: `packages/client-web/src/client/client.ts`, focused client tests,
  minimal public TSDoc/API inventory only if the frozen contract requires it,
  and A4.2 work/review evidence. Caller examples and A4.1 migrations are
  otherwise frozen. The owner may not commit, push, merge, spawn children, or
  build/run/generate/test Spine JVM.

### A4.2 implementation evidence

- One subscription activation now owns a monotonically increasing generation.
  Cancellation invalidates it before cleanup, shares the exact memoized
  `cancel()` promise, and remote cancellation is memoized per accepted wire.
  Late activation/stream work checks its generation before delivery.
- Terminal lifecycle capacity is explicitly the configured number of admitted
  non-terminal notices plus one bounded terminal slot: `closed` or `failed`
  follows queued notices in order and is then followed by completion. Terminal
  cancellation emits `closed`; non-cancellation protocol failures normalize one
  Error object shared by the update rejection and lifecycle `failed` notice.
  A4.1 overflow retains its established shared terminal error behavior.
- Unexpected stream EOF now follows the A4.2 non-cancellation terminal path:
  it rejects updates, appends one `failed` notification, and performs the
  at-most-once bounded remote Cancel. Reconnect classification remains A4.3.
- Focused client evidence is 42/42 tests and scoped browser coverage 94.09%
  branches (207/220), 95.58% statements. Root `typecheck:build` passed with
  frozen Proto checksum/digest checks; documentation/API, semantic snippets,
  browser dependency isolation, formatting, and diff hygiene were run before
  the final identity-only `cancel()` correction, whose focused test and
  formatting/diff checks also passed. No Spine JVM execution occurred.
- Pre-review lint split the unchanged lifecycle union documentation so only
  `connected`/`resynchronizing`/`gapPossible` are described as non-terminal,
  while `closed` is explicitly terminal. Fresh focused client tests,
  `docs:check`/API inventory, semantic snippets, formatting, and diff hygiene
  passed. No Spine JVM execution occurred.

### A4.2 review-correction evidence

- Terminal cancellation now races raw `Subscribe` and stream `next()` work
  against the local abort signal. Local activation/consumption therefore
  settles and releases the owner even if a transport ignores abort. A late
  accepted Subscribe wire retains only a transport-level continuation, which
  sends one bounded Cancel without retaining the `TopicSubscription` or client
  owner. The optional iterator `return()` call remains best effort and never
  delays terminal cleanup.
- Overflow selection uses the private `SubscriptionBufferOverflowError` marker
  rather than matching English error text. The existing shared terminal-error
  behavior is preserved. The race helper also observes a pending promise when
  called after abort, preventing a later raw rejection from becoming
  unhandled.
- Focused regressions prove that client close settles and closes its source
  when Subscribe never resolves, that a post-close accepted wire is Cancelled
  exactly once, and that a non-cooperative stream `next()` cannot retain local
  stream completion or source close. Connect 2.1 deliberately omits
  `return()` from its generated public stream iterator, so the transport
  fixture cannot observe a raw `return()` spy; the runtime still invokes it
  whenever the iterator supplies one.
- Fresh evidence: `packages/client-web/test/client.test.ts` passed 45/45;
  scoped client-web coverage is 93.39% statements, 91.92% branches (205/223),
  92.70% functions, and 94.67% lines. Root `typecheck:build`, `docs:check`,
  semantic snippets, client-web dependency isolation, `format:check`, and
  `git diff --check` all passed. The permanent Wave 4 rule permits Spine JVM
  references only as static, read-only material: no Spine JVM build, test,
  generation, dependency resolution, or project execution occurred.

### A4.2 final deterministic iterator-disposal regression

- A test-only mocked Connect client supplies an iterator whose `next()` never
  settles and whose optional `return()` is invoked once then rejects. It proves
  `cancel()` still settles promptly, preserves local update completion, sends
  one remote Cancel, and lets `client.close()` close its source. This directly
  covers the best-effort return branch that Connect 2.1 deliberately hides
  from public generated stream iterators; no production behavior changed.
- Focused client-web evidence passed 2 files / 46 tests. The test uses no
  Spine JVM project operation; the phase-wide static-reference-only constraint
  remains in effect.

### A4.2 full-gate coverage correction assignment

- The unrestricted full TypeScript gate passed all 140 runnable files / 2,610
  tests, with 3 files / 25 tests intentionally skipped. Global branch coverage
  measured 8,354/9,284 (89.98%), two covered branches below the unchanged 90%
  requirement after A4.2 introduced new terminal-race behavior.
- This is a deterministic coverage correction owned by the existing
  `implementer`, expected explicit `gpt-5.6-terra` / `medium`. Add the smallest
  behavior-focused tests for currently uncovered A4.2 client lifecycle/race
  paths sufficient to restore the global gate. Do not lower or exclude
  coverage, add ignore directives, change production merely for coverage,
  reopen A4.3, commit, push, merge, or spawn children.
- The permanent Wave 4 constraint remains absolute: static read-only Spine JVM
  references only; no JVM build, test, generation, dependency resolution, or
  other project execution.

### A4.2 final coordinator verification

- The deterministic correction covered non-Error stream-failure normalization
  and cancellation requested synchronously during Subscribe dispatch. Scoped
  client-web coverage rose from 205/223 to 207/223 branches without changing
  production behavior or coverage configuration.
- The final unrestricted TypeScript gate passed 140 runnable files / 2,612
  tests, with 3 files / 25 tests intentionally skipped. Global coverage is
  94.05% statements, 90.00% branches (8,356/9,284), 94.24% functions, and
  94.67% lines.
- Focused cancellation, iterator-disposal, Todo BlackBox, and Node BlackBox
  contract tests passed. Root generated typechecking, formatting, and diff
  hygiene are clean. All A4.2 review findings are resolved and the public A4.3
  boundary remains intact.
- No Spine JVM build, test, generation, dependency resolution, launch, or
  other project execution occurred. A4.2 is accepted for commit and immediate
  task-branch push; A4.3 finite reconnect and kind-specific recovery follows.

## A4.3 implementation assignment

- Classification remains high-risk because this slice introduces finite retry
  timing, reconnect generations, failure classification, ordered Entity
  recovery, and event-gap notification. The Sol/high A4 contract freeze is
  already complete; no new requirements split is needed.
- Behavior acceptance:
  1. unexpected stream EOF and retryable transport failures enter one
     serialized reconnect pipeline with at most one timer and monotonically
     increasing generations;
  2. retry policy applies to retries after the initial attempt, stops at
     `maxAttempts` or `maxElapsedMs`, validates every computed delay, and uses
     only the injected scheduler/clock;
  3. each abandoned accepted wire receives at most one bounded Cancel, while
     cancellation/client close aborts any wait and remains terminal;
  4. event reconnect emits
     `connecting → gapPossible → connected`, notifying and continuing without
     claiming replay completeness;
  5. Entity reconnect evaluates `authoritativeQuery` for the first time,
     accepts a Query or builder, requires its target to be byte-equivalent to
     the Topic target, replaces only its context, attaches the new stream,
     emits `resynchronizing`, enqueues a successful authoritative
     `QueryResponse` before held wire updates, then emits `connected`;
  6. protocol validation, Query/Topic mismatch, non-OK authoritative query,
     queue overflow, explicit cancellation, and client close are terminal and
     never retried; final retry exhaustion emits exactly one `failed`;
  7. no command retry, silent update drop, generic cursor/cache layer, auth,
     React, Envoy, or A4.4 documentation closure is introduced.
- Existing implementation owner: `implementer`; expected explicit
  `gpt-5.6-terra` / `medium`. Ownership is
  `packages/client-web/src/client/client.ts`, focused client-web tests, and
  A4.3 work/review evidence. The owner must use behavior-first tests, preserve
  A4.2 terminal/cleanup invariants, and may not commit, push, merge, spawn
  children, or edit unrelated callers.
- Runtime self-introspection remains unavailable; the immutable configured
  role/profile is the acceptance metadata unless a visible mismatch occurs.
  The permanent Wave 4 rule permits static read-only Spine JVM references only:
  no JVM build, test, generation, dependency resolution, launch, or other
  project execution.

### A4.3 retry-slice implementation-context replacement

- The original A4 implementation context produced a green event-EOF
  checkpoint and incorporated coordinator fixes for final-attempt admission,
  elapsed expiry, stale iterator release, and accepted-wire cleanup, but ended
  three continuation turns without adding the bounded retry/elapsed/cancel
  regressions required for acceptance. No product blocker was established.
- A fresh existing `implementer` role owns only completion of A4.3 retry slice
  1 in the current diff. Expected model `gpt-5.6-terra` and reasoning `medium`
  are explicit in dispatch. It must add exact retry Subscribe/Activate,
  exhaustion/elapsed, and cancel-during-wait tests, fix only proven defects,
  run focused coverage and typechecking, and hand back a complete green slice.
- The owner may not start Entity recovery, commit, push, merge, spawn children,
  or execute the Spine JVM project. The permanent static-read-only JVM rule
  remains in force.

### A4.3 retry slice 1 implementation evidence

- Existing role/profile: `implementer`, explicitly dispatched `gpt-5.6-terra`
  / `medium`; immutable configured profile is the available metadata. It owns
  only the client runtime, focused client-web tests, and this A4.3 evidence.
- RED: focused regressions proved that a retryable initial `Subscribe` failure
  rejected `activate()` instead of entering reconnect, and that an accepted
  initial wire whose activation setup failed did not recover. A rejected
  `Subscribe` was also confirmed to have no wire to cancel; the test expects
  only the previously accepted wire's cleanup.
- GREEN: retryable initial and streaming failures now use one generation-owned
  retry pipeline. Regressions cover initial and reconnect `Subscribe` failure
  then success; accepted-wire activation/setup failure then retry; exact
  once-per-accepted-wire cleanup; retry exhaustion; elapsed expiry after the
  scheduler wait; and explicit cancellation or client close during a scheduler
  wait with only `closed` and no later RPC. Event generations retain ordered
  `connecting`, `gapPossible`, and `connected` notices. Entity recovery remains
  deliberately unstarted.
- Evidence: scoped `client.test.ts` coverage passed 56/56 tests, 95.78%
  statements, 92.30% branches, 94.17% functions, and 97.59% lines for
  `client.ts`; root `typecheck:build` passed after frozen Proto checksum/digest
  verification. No Spine JVM build, test, generation, dependency resolution,
  launch, or other project execution occurred.

### A4.3 Entity-recovery slice assignment

- The same existing `implementer` context now owns A4.3 slice 2, explicitly
  `gpt-5.6-terra` / `medium`. It must implement Entity reconnect only:
  deferred first evaluation of raw/builder authoritative Query, exact
  byte-equivalent Topic target validation, request-context replacement,
  stream attachment before Query Read, `resynchronizing`, successful
  resynchronization delivery before held wire updates, then `connected`.
- Query factory/build failure, target mismatch, non-OK response, protocol
  failure, overflow, cancellation, and client close are terminal/nonretryable;
  accepted wire cleanup remains bounded and exactly once. Tests must cover
  ordering under an update arriving during the query, immutability, raw and
  builder inputs, and terminal paths.
- The owner may not add a cursor/cache layer, alter Event recovery, start A4.4,
  edit unrelated callers, commit, push, merge, spawn children, or execute any
  Spine JVM project operation.

### A4.3 Entity completion-context replacement

- The Entity slice owner established the green nonempty-target ordering path
  but repeatedly ended before implementing its bounded contract-failure batch.
  No product blocker was established and the partial result is not accepted.
- A fresh existing `implementer` role, explicitly `gpt-5.6-terra` / `medium`,
  inherits the current diff and owns only Entity Batch A: builder/raw contract
  preservation, exact-target mismatch, non-OK response, one-call
  factory/build failure, and immutable delivery regressions with minimal fixes.
- It may not alter Event recovery, begin the remaining cancellation/retry batch,
  commit, push, merge, spawn children, or perform any Spine JVM project
  operation.

### A4.3 Entity Batch B assignment

- The productive Entity Batch A `implementer` context continues under its
  explicit `gpt-5.6-terra` / `medium` profile. It owns only the remaining
  runtime-lifecycle cases: retryable authoritative Read failure, cancellation
  and client close during pending Read, decoded update overflow after
  resynchronization, and stale-generation fencing.
- Batch B must preserve the accepted Batch A contract behavior, exact
  once-per-accepted-wire cleanup, one recovery pipeline/timer, mutually
  exclusive terminal states, and prompt source close. It must finish focused
  coverage, root typechecking, formatting, and diff hygiene.
- It may not begin A4.4, auth, React, or Envoy work; commit, push, merge, spawn
  children; or execute any Spine JVM project operation.

### A4.3 review-regression completion-context replacement

- The Entity implementation context applied the two consolidated production
  corrections and preserved 63/63 existing tests, but ended before adding the
  reviewer-required abort-ignoring-scheduler and iterator-return regressions or
  rerunning coverage/typechecking. The batch is not accepted.
- A fresh existing `implementer`, explicitly `gpt-5.6-terra` / `medium`, owns
  only those focused regressions, any defect they prove in the already scoped
  correction, and final mechanical evidence. It may not expand behavior,
  begin A4.4, commit, push, merge, spawn children, or execute Spine JVM.
- Follow-up RED/GREEN: cancellation during an initial `Subscribe` retry wait
  originally caused pending `activate()` to resolve after emitting `closed`.
  The recovery path now rethrows its cancellation reason to its awaiting
  activation caller (detached stream recovery already observes the rejection).
  The exact regression and the focused suite passed 57/57; Prettier and diff
  hygiene remain clean. No Entity recovery or JVM project operation occurred.

### A4.3 Entity recovery checkpoint

- RED: an Entity reconnect did not evaluate or dispatch its authoritative
  query. The first recovery regression also exposed the generated response
  status shape as `response.status.status`, not a direct status oneof.
- GREEN checkpoint: Entity reconnect attaches its replacement iterator before
  the authoritative read, defers iterator consumption until the successful
  immutable resynchronization delivery is queued, then emits `connected` and
  delivers wire updates. The raw-query regression uses a nonempty exact target
  and proves `connecting → resynchronizing → connected` plus resynchronization
  before the next update. Factory/build/clone preparation failures are wrapped
  as nonretryable protocol errors; target byte comparison and context
  replacement are implemented. Focused Entity regression and the prior full
  client suite (58/58) plus root `typecheck:build` passed. No JVM operation
  occurred. Remaining required terminal/builder coverage is still in progress.

### A4.3 Entity Batch A completion evidence

- Existing role/profile: `implementer`, explicitly dispatched `gpt-5.6-terra`
  / `medium`; immutable configured profile remains the available metadata. This
  bounded completion context owned only `packages/client-web/src/client/client.ts`,
  `packages/client-web/test/client.test.ts`, and this evidence record.
- RED: the new builder-input contract test initially failed by comparing the
  outgoing Read context with the caller-owned Topic, which deliberately has no
  request context. The accepted subscription Topic is prepared before Subscribe;
  the assertion was corrected to its captured prepared context. This establishes
  that replacement means the prepared Topic context, not the original caller
  value, without changing runtime behavior.
- GREEN: the Entity recovery regression now proves a builder is evaluated once
  only on recovery; Read receives a distinct cloned query retaining its id,
  byte-equivalent target, and format while replacing its context from the
  prepared Topic. It also proves the delivered resynchronization and nested
  response/status messages are deeply frozen. A table-driven terminal regression
  proves byte-different targets perform zero Reads and no further retry; non-OK
  responses perform one Read and no further retry; factory and builder failures
  are each invoked once with zero Reads. Each accepted wire is cancelled exactly
  once (the abandoned first wire and the terminal replacement wire), with client
  close issuing no duplicate Cancel.
- Evidence: focused `pnpm exec vitest run packages/client-web/test/client.test.ts`
  passed 1 file / 59 tests. `git diff --check` passed and Prettier was applied to
  the two owned runtime/test files. No Spine JVM build, test, generation,
  dependency resolution, launch, or other project execution occurred. Entity
  Batch B cancellation, Read-retry, and overflow behavior remains unstarted.

### A4.3 Entity Batch B completion evidence

- Existing `implementer` role/profile remains explicitly `gpt-5.6-terra` /
  `medium`; immutable configured profile is the available metadata. Ownership
  remained the client runtime, focused client-web tests, and this evidence.
- RED/GREEN: focused tests first exposed harness errors (replacement streams
  unintentionally ending and an unprepared recovery path), then proved the
  inherited runtime already performs the bounded behavior: a plain transient
  authoritative Read failure cancels its accepted wire, retries through the
  scheduler on a fresh generation/wire, reevaluates the query, and succeeds.
  Cancel and client close during pending Read fence late success, emit closed
  only, consume no iterator update, and clean each accepted wire once; close
  also closes the source. Post-resynchronization decoded update count and byte
  overflow yield the same typed terminal error on both channels with no
  duplicate cleanup.
- Evidence: focused suite passed 1 file / 63 tests. Focused coverage executed
  those tests and measured `client.ts` at 95.41% statements, 91.84% branches,
  94.39% functions, and 97.71% lines; the command exits nonzero only because
  its workspace-wide global threshold includes unexecuted packages. Root
  `pnpm typecheck:build`, Proto checksum/digest verification, `git diff --check`,
  and Prettier passed. No Spine JVM project operation occurred.

### A4.3 consolidated review correction evidence

- The retry path now fences scheduler implementations that ignore abort: after
  every wait it checks the controller signal before any later RPC admission.
  Abandonment also clears the retained iterator before invoking optional
  `return()` best-effort, so repeated terminal/retry paths cannot double-return
  or wait on a rejecting/non-settling iterator cleanup.
- Focused client suite passed 1 file / 63 tests; Prettier and `git diff --check`
  passed. No JVM project operation occurred.

### A4.2 full-gate stale lifecycle-contract correction

- The full TypeScript gate initially reported 138 passed files, 2 failed files,
  2,608 passed tests, 2 failed tests, and 25 skipped tests. Both failures were
  stale callers that expected lifecycle iteration to end immediately after
  cancellation. A4.2 intentionally emits its one `{ state: "closed" }`
  terminal notice before `done`.
- Updated only the Todo BlackBox assertion and the shared Node BlackBox
  contract to consume and validate `closed`, then validate completion. Focused
  elevated loopback evidence passed Todo 33/33 and Node contract 15/15. The
  sandbox-only versions fail before assertions with `listen EPERM` and are
  superseded by the exact elevated runs. No production behavior changed.
- The permanent Wave 4 constraint remains: any Spine JVM reference is static
  and read-only; no build, test, generation, dependency resolution, or project
  execution occurred.

### A4.2 deterministic coverage correction

- After all full TypeScript tests passed (140 files / 2,610 tests, with 3 files
  / 25 tests skipped), the global branch result was 8,354/9,284 = 89.98%, two
  covered branches below the unchanged threshold. No coverage configuration,
  threshold, source include, or ignore setting changed.
- Added two behavior-focused client lifecycle regressions: a stream rejection
  whose value is not an `Error` normalizes into the same `Error` carried by the
  failed lifecycle notice, and cancellation requested synchronously while
  Subscribe dispatches settles local activation/updates without leaving the
  raw promise unobserved. Neither changes production behavior or begins A4.3.
- Scoped client-web coverage passed 2 files / 48 tests and rose from 205/223
  to 207/223 covered branches (+2); it measured 94.37% statements, 92.82%
  branches, 93.75% functions, and 95.51% lines. The permanent Wave 4
  static-read-only JVM-reference constraint remains in effect; no JVM project
  operation occurred.

### A4.3 review-regression completion evidence

- The retrying stream owner now retains its exact active iterator until
  recovery disposes it. The clear-before-best-effort-return helper prevents a
  later cancellation or client close from returning that iterator twice.
- The existing mocked Connect seam directly supplies the iterator owned by
  `TopicSubscription`. It proves a retryable Event stream failure invokes a
  rejecting `return()` once before reconnect; later cancellation/close invokes
  only the replacement iterator once. It also proves an Entity sequence of
  stream failure, transient authoritative Read failure/retry, then non-OK Read
  terminal failure returns each of the three abandoned iterators exactly once,
  including a rejecting return, with no duplicate after close.
- A caller activation AbortSignal test uses a scheduler wait that ignores its
  signal and resolves only after abort. The pending activation rejects with the
  abort reason, emits `connecting` then one terminal `failed` lifecycle notice,
  and makes no later Subscribe attempt.
- Evidence: `pnpm exec vitest run packages/client-web/test/client.test.ts
packages/client-web/test/client.iterator-return.test.ts --coverage` ran
  67/67 passing tests. It measured `client.ts` at 95.64% statements, 92.55%
  branches, 95.32% functions, and 97.73% lines. The command's nonzero status is
  solely the unchanged workspace-global 90% threshold applied to unexecuted
  packages (global 2.92% coverage). Root `pnpm typecheck:build` passed after
  frozen Proto checksum/digest verification. `git diff --check` and Prettier
  passed. No Spine JVM build, test, generation, dependency resolution, launch,
  or other project execution occurred.

### A4.3 final coordinator verification

- The final scheduler-wait correction races even a never-settling injected
  wait against cancellation and retains raw-promise rejection observation.
  Focused client evidence passed 68/68, and all targeted specialist lanes are
  clean.
- The unrestricted full TypeScript gate passed 140 runnable files / 2,632
  tests, with 3 files / 25 tests intentionally skipped. Global coverage is
  94.09% statements, 90.00% branches (8,409/9,343), 94.30% functions, and
  94.74% lines.
- Root generated typechecking/Proto checks, formatting, and diff hygiene are
  clean. No Spine JVM build, test, generation, dependency resolution, launch,
  or other project execution occurred.
- A4.3 finite reconnect, Event gap notification, and Entity authoritative
  resynchronization are accepted for commit and immediate task-branch push.
  A4.4 public documentation and regression closure follows automatically.

## A4.4 documentation/regression closure assignment

- Classification is standard documentation/API closure over the already
  reviewed A4 runtime. No architecture planning is required.
- Acceptance:
  1. `packages/client-web/README.md`, the end-user guide, and API overview use
     current package names and executable inline TypeScript snippets for
     browser transport creation, commands, queries, Event subscriptions, and
     Entity authoritative recovery;
  2. docs state actual defaults, finite retry/elapsed bounds, lifecycle
     generations, bounded count/byte queues, terminal overflow, cancellation,
     and exact `closed`/`failed` behavior;
  3. limitations are explicit: no cluster-complete delivery guarantee, Event
     gaps remain possible and are only notified, Entity reconnect re-queries
     authoritative state, lifecycle/update streams have no cross-stream order,
     and commands are never retried;
  4. docs distinguish required gRPC-Web compatibility from optional Connect
     protocol optimization without claiming JVM execution evidence;
  5. semantic snippets, API/export inventory, dependency isolation, focused
     client regressions, formatting, and diff hygiene pass.
- Existing role: `implementer`; expected explicit `gpt-5.6-terra` / `medium`.
  Ownership is browser-client README/guide/API docs, snippet fixtures/checkers
  only if needed, focused regression titles/assertions, and A4.4 evidence. The
  owner may not change runtime semantics, begin auth/React/Envoy work, commit,
  push, merge, or spawn children.
- Permanent Wave 4 rule: Spine JVM is a static read-only reference only; no
  JVM build, test, generation, dependency resolution, launch, or other project
  execution.

### A4.3 bounded scheduler-wait correction evidence

- RED: a custom scheduler whose initial retry wait never settled and ignored a
  caller activation AbortSignal left `activate()` pending until a test deadline.
- GREEN: recovery now races the scheduler wait with the existing controller
  terminal race, while retaining its post-wait controller and elapsed fences.
  The raw wait remains part of the race, so its later rejection is observed.
  The regression proves caller abort promptly rejects activation, starts no
  later Subscribe, emits `connecting` then one `failed` lifecycle terminal and
  done, and allows client close to settle.
- Evidence: focused `client.test.ts` plus mocked iterator-return seam passed
  2 files / 68 tests. Root `pnpm typecheck:build` passed after frozen Proto
  checksum/digest verification. Prettier and `git diff --check` passed. No
  Spine JVM build, test, generation, dependency resolution, launch, or other
  project execution occurred.

### A4.4 documentation/regression closure evidence

- The browser-client README, end-user guide, and API overview now document the
  current public `Client.forGrpcWeb()`/`Client.forConnect()` factories with
  executable TypeScript composition helpers named `createGrpcWebClient` and
  `createConnectClient`. The helpers are explicitly local names, not invented
  package exports. They demonstrate gRPC-Web as the universal browser protocol
  and Connect as an optional known-compatible endpoint optimization, with no
  probing or fallback.
- The browser README includes executable command/raw-query, Entity
  authoritative recovery, and Event create/activate/lifecycle/updates/cancel
  snippets. All docs now state the 64-delivery/1,048,576-byte update and
  32-notice lifecycle defaults; five retry attempts after initial activation;
  the 30,000 ms total elapsed bound; positive-safe-integer validation; and the
  250 ms exponential delay base, ±20% jitter, 1 ms minimum, and 5,000 ms cap.
  They state direct shared-error overflow, pre-terminal `closed`, non-overflow
  `failed`, monotonically increasing recovery generations, per-wire bounded
  cancellation, and independent streams.
- The explicit limitations are current: commands are never retried; Event
  recovery reports `gapPossible` then continues with possible gaps and no
  cluster-complete/replay guarantee; Entity recovery re-evaluates its
  raw/builder authoritative query, validates its byte-equivalent target, and
  delivers the authoritative raw response before held updates; lifecycle and
  updates have no cross-stream order; signals/schedulers do not establish a
  cursor, cache, replay, auth/session, or durability policy. Stale A4.3/A4.4
  deferral claims were removed. No runtime semantics changed.
- Fresh evidence: `pnpm docs:check` passed after TypeDoc plus API inventory and
  static Proto checksum/descriptor verification; `node docs/check-typescript-snippets.mjs`,
  `node scripts/check-client-web-dependencies.mjs`, targeted Prettier, and
  `git diff --check` passed; focused `pnpm exec vitest run
packages/client-web/test/client.test.ts
packages/client-web/test/client.iterator-return.test.ts` passed 2 files / 68
  tests. No Spine JVM build, test, generation, dependency resolution, launch,
  or project execution occurred.

### A4.4 consolidated review correction evidence

- The accepted prose findings were verified directly against the reviewed
  runtime before correction. Remote cancellation is deduplicated at most once
  per accepted wire, not once for a logical subscription across reconnects, and
  `cancelWire()` enforces a 1,000 ms bound for each wire. Browser factory
  sources select/create a Connect-Web transport but supply no platform close
  hook; `Client.close()` closes owned subscription work, while an injected
  source invokes its optional `close()` hook.
- Docs now distinguish overflow from other terminal failures: update or
  lifecycle overflow directly fails both streams with the same error and does
  not enqueue `failed`; non-overflow terminal failures do enqueue one failed
  lifecycle notice. `closed` is explicitly pre-terminal only. Default retry
  delay wording is now exact: a 250 ms exponential base, ±20% jitter, a
  5,000 ms final cap, and a 1 ms minimum; custom returned delays are positive
  safe integers. The Event snippet now creates a valid `includeAll` target for
  a generated event schema and identifies its first lifecycle notice as
  `connecting`.
- Fresh correction evidence passed: `pnpm docs:check` verified TypeDoc and the
  API inventory; `node docs/check-typescript-snippets.mjs` and
  `node scripts/check-client-web-dependencies.mjs` passed; targeted Prettier
  and `git diff --check` passed; focused `pnpm exec vitest run
packages/client-web/test/client.test.ts
packages/client-web/test/client.iterator-return.test.ts` passed 2 files / 68
  tests. No runtime semantics, later Wave 4 feature, commit/push/merge, or
  Spine JVM project operation occurred.

### A4.4 final coordinator verification

- Documentation and TypeScript/API targeted re-reviews are clean; every
  accepted wording/snippet finding is resolved.
- Direct `pnpm docs:check` passed frozen 39-source checksum and 48-descriptor
  digest validation, TypeDoc generation, and exact API inventories including
  17 client-web exports. Semantic TypeScript snippets and browser dependency
  isolation passed.
- Focused browser client evidence remains 68/68, formatting and diff hygiene
  are clean, and A4.4 changes no runtime behavior. The immediately preceding
  A4.3 unrestricted gate passed all TypeScript tests at 90.00% branches, so the
  change-sensitive docs gate is sufficient here.
- No Spine JVM build, test, generation, dependency resolution, launch, or
  other project execution occurred. A4.4 is accepted for commit and immediate
  task-branch push.

## 2026-07-27 — B1 auth contract foundation

- Classification: high-risk because B1 introduces a public package, serialized
  authentication service contract, authorization request model, and security
  extension seams. The already accepted Wave 4 requirements split freezes the
  architecture, so no additional deep-planning dispatch is needed.
- Behavior-focused acceptance:
  1. add `@spine-event-engine/auth` with an exhaustive discriminated
     `IncomingRequest` model for Command, Query, Subscribe, Activate, and
     Cancel, preserving the known decoded envelope and safe optional packed
     message decoding;
  2. expose only documented allowlisted transport facts to policy and
     diagnostics, keeping raw credentials outside ordinary request facts;
  3. define provider-neutral authenticator, session, authorization, trusted
     Actor/tenant resolution, clock, and request-decoding seams without
     implementing B2 forwarding or C-series session/provider behavior;
  4. add the shared auth-owned Protobuf
     `AuthenticationService.ResolveContext` request/response contract whose
     actor, tenant, and expiry output is informational rather than a credential;
  5. compose Wave 3 model registries for optional packed command decoding,
     handle unknown/malformed `Any` safely, and preserve type-URL-only policy;
  6. focused contract, exhaustive-kind, malformed/unknown decoding,
     dependency-boundary, generation/checksum, typecheck, formatting, and diff
     checks pass.
- Existing role: `implementer`.
- Ownership: `packages/auth/**`, the minimum shared auth Proto source/generated
  export/checksum changes, B1-focused tests and package/workspace configuration,
  and a bounded B1 implementation report. Existing browser runtime semantics
  and B2+ gateway/session/React/Chat/Envoy behavior are out of scope.
- Expected model: `gpt-5.6-terra`.
- Expected reasoning: `medium`.
- Both fields must be explicit in dispatch. The owner uses behavior-first TDD,
  preserves concurrent and unrelated work, may not commit, push, merge, or
  spawn children.
- Runtime metadata limitation: Desktop exposes the immutable configured
  implementer profile and explicit dispatch, but not independent child
  self-introspection. A visible mismatch or inherited fallback would require
  rejection and redispatch; missing introspection alone does not.
- Permanent Wave 4 rule: Spine JVM is static read-only reference material only.
  No JVM build, test, generation, dependency resolution, launch, or any other
  project execution is permitted.

### B2 implementation-context replacement

- The first B2 implementation context returned twice while explicitly
  acknowledging required acceptance remained incomplete. Neither return is
  accepted as B2 completion.
- Preserved useful work is limited to a minimal injected unary command path
  whose focused test proves one authenticated/authorized forward with a fresh
  trusted context and the injected trusted clock.
- A fresh existing `implementer` receives the remaining B2 slice plus
  responsibility to inspect and correct the preserved command path as needed.
  Expected model `gpt-5.6-terra`, expected reasoning `medium`, both explicit.
- Remaining acceptance includes Query.Read, ResolveContext, byte-bound and
  malformed no-work ordering, unauthenticated/denied/stale no-forward behavior,
  complete fresh context replacement, preservation of every other envelope
  field/byte, no retry, focused verification, and B2 evidence.
- The replacement owns only `packages/auth/**`, B2 tests/report and required
  auth API inventory. It may not begin B3+, commit, push, merge, spawn children,
  or execute any Spine JVM project operation.

### B2 final acceptance

- All style, documentation, TypeScript/API, and reliability findings converged
  after canonical operation binding, immutable independent transport facts,
  private request-byte ownership, collaborator isolation, one authoritative
  timestamp, shared decoding, unknown-field preservation, rejection/no-retry
  coverage, public TSDoc, and semantic module separation.
- Final unrestricted gate passed 143 files / 2,652 tests; 3 files / 25 tests
  were intentionally skipped. Coverage: 94.09% statements, 90.02% branches
  (8,486/9,426), 94.33% functions, and 94.76% lines.
- Expected explicit profiles matched every immutable implementation/reviewer
  role; independent runtime self-introspection was unavailable with no visible
  mismatch or fallback.
- B2 is accepted for commit and immediate origin push. No Spine JVM build,
  test, generation, dependency resolution, launch, or project execution
  occurred.

## 2026-07-28 — B3 atomic subscription ownership

- Base and origin task branch are synchronized at
  `6c08218b6a9f1cb9c99ca7c0c5ecc0f9f2be68a5`.
- Classification: high-risk concurrency/security lifecycle. The approved Wave
  4 split freezes the design; no new architecture dispatch is required.
- Acceptance: gateway-generated IDs hide backend subscriptions; an injectable
  atomic binding store records backend envelope, principal fingerprint, tenant,
  expiry, and inactive/active/cancelling/closed state; Subscribe, Activate, and
  Cancel each authenticate and authorize independently; cross-owner/stale
  access never reaches the backend; activation/cancellation races transition
  atomically; cleanup is idempotent and leaves no retained binding/payload;
  Wave 4 includes an in-memory reference store. B4 streaming/native forwarding
  and C-series sessions remain out of scope.
- Existing role: `implementer`; ownership `packages/auth/**`, B3 tests/API/docs
  and report. Expected explicit `gpt-5.6-terra` / `medium`. No commit, push,
  merge, children, B4/C+, or Spine JVM execution.

### B3 implementation-context replacement

- The initial B3 context returned twice while explicitly acknowledging the
  assigned slice remained incomplete. Neither return is accepted as B3
  completion.
- Preserved work is limited to one green in-memory binding-store test and a
  minimal store that generates public IDs, privately copies backend envelopes,
  and denies cross-owner activation before exposing backend bytes.
- A fresh existing `implementer` receives the full remaining B3 acceptance and
  must inspect/correct the preserved checkpoint. Expected model
  `gpt-5.6-terra`, expected reasoning `medium`, both explicit.
- Ownership remains `packages/auth/**`, B3 tests/API/docs/report only. No B4/C+,
  commit, push, merge, child dispatch, or Spine JVM execution.

### B3 correction-context replacement

- The replacement B3 context returned twice while explicitly acknowledging the
  accepted review correction remained incomplete. Neither return is accepted.
- Preserve verified work: injectable store contract, lazy purge, explicit
  zeroing close, one clock snapshot, expired-session rejection, defensive
  backend callback copies, and callback/store failure cleanup.
- A fresh `implementer`, expected explicit `gpt-5.6-terra` / `medium`, receives
  the remaining immutable collaborator views, stale identity checks, trusted
  context rewrites, optional-tenant behavior, complete regressions/docs/report,
  and responsibility to audit the preserved correction as a whole.
- Ownership remains auth B3 only; no B4 native transport, C+, commit, push,
  merge, child dispatch, or Spine JVM execution.

### B3 bounded-coordination architecture clarification

- The second correction review demonstrated an architectural blocker in the
  attempted gateway-wide/store-wide promise-chain design: one hung callback
  can block unrelated bindings and retain an unbounded queue. This triggers
  one bounded deep-planning clarification rather than another speculative
  implementation retry.
- Existing role: `requirements_splitter`.
- Scope: define the smallest B3-compatible per-binding coordination and finite
  admission/retention limits, including shutdown, overflow, activation/cancel
  ordering, pre-binding compensation, and the exact separation from B4 relay
  work. No production edits.
- Expected model: explicit `gpt-5.6-sol`.
- Expected reasoning: explicit `high`.
- Read-only; no commit, push, merge, child dispatch, or Spine JVM build, test,
  generation, dependency resolution, launch, or project execution.

#### Accepted B3 bounded design

- The requirements splitter ran under the explicitly dispatched existing role,
  model `gpt-5.6-sol`, and reasoning `high`. Independent runtime
  self-introspection was unavailable; the immutable configured role/profile
  matched with no visible fallback. It made no edits and performed no JVM
  execution.
- Remove both global promise tails. Each retained binding owns its finite
  coordinator so unrelated IDs never block each other. Ordering begins at the
  atomic post-auth transition.
- Defaults: `maxRequestBytes = 1_048_576`,
  `maxBackendEnvelopeBytes = 1_048_576`, `bindingLimit = 100`,
  `pendingOperationLimit = 1`, `operationTimeoutMs = 30_000`, and
  `shutdownTimeoutMs = 1_000`. Every value is a positive safe integer.
- `handle()` snapshots route, bounded/copied wire, copied credential, and one
  frozen allowlisted transport view synchronously before asynchronous work.
- Add deterministic `request-too-large`, `backend-envelope-too-large`,
  `binding-capacity-exceeded`, and `binding-busy` rejections.
- Activate-first serializes backend activation before queued Cancel cleanup;
  Cancel-first closes and denies later Activate. Owner/tenant denials do not
  consume a queue slot. Failed/expired cancellation remains retry-safe as
  specified; absent Cancel is idempotent.
- Backend callbacks are mandatory and receive an `AbortSignal`. Private
  `dispose(envelope, signal)` compensates every post-Subscribe/pre-binding
  failure. Dual failure is an ordered `AggregateError`.
- Close marks terminal synchronously, rejects queued work, aborts running
  effects, bounds cleanup to 1 second, zeroes every owned byte copy, and clears
  all store/coordinator retention. A callback retaining its own copy while
  ignoring abort is outside B3's memory guarantee; B4 adapters must honor the
  signal.
- B4 remains responsible for native gRPC interpretation/cancellation,
  bidirectional relay, relay byte/message queues and backpressure, disconnect
  and backend-error propagation. Those transport responsibilities must not be
  pulled into B3.
- The next fresh existing `implementer` owns this complete B3 correction,
  focused tests/docs/API inventory/evidence only. Expected explicit
  `gpt-5.6-terra` / `medium`; no B4/C+, commit, push, merge, children, or Spine
  JVM execution.

#### B3 bounded-correction context replacement

- The bounded-correction context returned twice without completing the frozen
  design. Preserve its verified synchronous admission, named exports, initial
  per-binding coordinator, timer clearing, mirrored test move, and API
  inventory work; neither return is B3 acceptance.
- Remaining deterministic work includes mandatory store disposal without a
  compatibility no-op, expiry handling that cannot bypass cleanup or gate an
  unrelated binding, one globally bounded close that collects failures and
  clears every binding, atomic concurrent capacity enforcement, real
  security-pipeline decomposition, and the complete required regression
  matrix/evidence.
- A fresh existing `implementer` receives that exact bounded remainder and
  audits the accepted design as a whole. Expected explicit model
  `gpt-5.6-terra`, reasoning `medium`; ownership remains B3 auth
  runtime/tests/docs/inventory/evidence only. No B4/C+, commit, push, merge,
  children, or Spine JVM execution.

### B3 implementation evidence

- Correction continuation: moved the B3 subsystem beneath
  `packages/auth/src/subscriptions/` with the root as a public re-export. The
  gateway now holds one private request-byte snapshot, independently decodes
  authorization and context-resolver views, preserves an untouched requested
  actor/tenant identity snapshot, and builds a fresh trusted `ActorContext` for
  Subscribe/Activate/Cancel backend callbacks and the public Subscription topic.
  Tenant-less contexts are valid when both requested and trusted contexts omit
  TenantId. Stale/fabricated actor or tenant never reaches a backend callback.
- Correction RED/GREEN evidence: the focused subscription test first failed for
  unchanged forwarded context, stale Subscribe acceptance, and tenant-less
  rejection; after the minimal gateway correction it passes 1 file / 8 tests.
  The suite also proves collaborator mutation isolation, stale Activate/Cancel
  no-callback behavior, exact activate-callback rejection and zero retention.

- RED/green behavior-first evidence: before B3 implementation, the focused
  suite failed because `SubscriptionGateway`, cancellation lifecycle, and
  binding observability did not exist. The green suite now proves a generated
  public ID, no backend-envelope bytes in a public response, separate session
  resolution and `subscribe`/`activate`/`cancel` authorization, owner/tenant/
  expiry denials, idempotent cancel cleanup, and both ordered Activate/Cancel
  race outcomes with zero retained bindings.
- Delivered an injectable `InMemorySubscriptionBindings` reference store with
  privately copied backend envelopes, principal fingerprint, serialized-tenant
  fingerprint,
  expiry, and inactive/active/cancelling/closed transition state. Its public
  operations expose no backend envelope. `SubscriptionGateway` is deliberately
  transport-neutral so B4 can add native stream mapping without reopening B3
  ownership logic.
- Focused evidence passed: `vitest` over the three auth test files (3 files / 21
  tests), `tsc --noEmit -p packages/auth/tsconfig.json`, the TypeDoc/API
  inventory check (35 auth exports), Prettier on all B3 paths, and `git diff
--check`.
- Limits: no B4 native gRPC/stream relay, concrete sessions/OIDC, React, Chat,
  Envoy, commit/push/merge, or Spine JVM build, test, generation, dependency
  resolution, launch, or project execution occurred.

### B3 second correction evidence

- RED was observed on `subscription-bindings.test.ts`: a post-close create
  resolved and Cancel reached its backend callback while Activate was delayed.
  The added retry and activation-failure cleanup regressions then drove the
  smallest serialized store/gateway correction.
- Focused verification on 2026-07-28: `vitest` over the three auth files passed
  3 files / 25 tests; `tsc --noEmit -p packages/auth/tsconfig.json` passed;
  `docs:check:generated` passed with the exact 35-export auth inventory;
  Prettier and `git diff --check` passed. No Spine JVM command ran.

### B3 bounded-correction implementation evidence

- RED: focused B3 regressions showed that oversized input reached asynchronous
  session work and that a delayed Activate could block an unrelated binding.
- GREEN: `handle()` now copies route, bounded wire, credential, and allowlisted
  transport facts synchronously; B3 defaults are 1 MiB request/backend bytes,
  100 bindings, one pending operation, 30-second effect timeout, and one-second
  shutdown cleanup. Store coordination is finite per binding; close aborts,
  bounds cleanup, zeroes private bytes, and removes retention. Backend callbacks
  and Subscribe compensation are mandatory and signal-aware. B4 relay/native
  transport remains out of scope. Fresh focused verification follows this entry.
- Superseded by the final bounded correction below: the earlier 3-file/28-test
  evidence did not cover mandatory expiry/close disposal and atomic capacity.

### B3 historical initial final bounded-correction evidence (superseded)

- RED: simultaneous creates exceeded the binding limit and shutdown timed out
  on the first never-settling disposal, leaving the second cleanup unattempted.
  GREEN: creation reserves capacity synchronously; expiry atomically detaches a
  binding before starting required disposal; close marks terminal, aborts active
  effects, starts every disposal concurrently under the shutdown bound, clears
  all bindings, and aggregates cleanup failures.
- Focused verification passed: 3 auth files / 33 tests; `tsc --noEmit -p
packages/auth/tsconfig.json`; generated TypeDoc/API inventory with exactly 41
  auth exports; Prettier on changed B3 paths; and `git diff --check`. No Spine
  JVM build, test, generation, dependency resolution, launch, or execution ran.

### B3 final review-correction evidence

- RED/GREEN: the focused callback regression first received the former custom
  abort view rather than the platform `AbortSignal`; after replacement it proves
  the event-capable standard signal. The same final focused suite proves foreign
  cancellation is denied before any queue reservation, pre-Subscribe capacity
  is leased while backend Subscribe is pending, and callback copies are zeroed
  after their effect finishes.
- The correction bounds Subscribe and pre-binding compensation with the
  operation timeout, clears timers, aborts timeout effects, releases every
  failed capacity lease, and schedules expiry disposal behind an existing
  binding tail. It exports the transition and capacity-reservation signature
  types, and documents cooperative standard-AbortSignal limits and serialized
  tenant fingerprints.
- Fresh focused verification: 3 auth files / 36 tests passed; `tsc --noEmit -p
packages/auth/tsconfig.json` passed; generated TypeDoc/API checking passed
  with 43 auth exports; Prettier and `git diff --check` were rerun after the
  correction. No Spine JVM command ran.

### B3 targeted final-correction evidence

- RED/GREEN: gateway close was absent while a pre-binding Subscribe could retain
  a capacity lease. The new regression holds Subscribe, calls gateway close,
  and proves the standard abort event fires and terminal closure prevents
  another lease. The implementation tracks pending controllers/leases,
  synchronously aborts and releases them at close, then closes retained
  bindings.
- The auth TypeScript configuration now consumes Node's platform web globals;
  no module-private AbortSignal declaration remains. The actual perform and
  subscribe phases are decomposed into bounded activate/cancel/receive/retain/
  failed-creation methods. Documentation now consistently calls tenant matching
  a serialized-tenant-fingerprint comparison, and the earlier 33-test/41-export
  report is explicitly superseded.
- Fresh focused verification: 3 auth files / 37 tests passed; `tsc --noEmit -p
packages/auth/tsconfig.json`, generated TypeDoc/API checking, Prettier, and
  `git diff --check` passed. No Spine JVM command ran.

### B3 last targeted correction evidence

- RED/GREEN: a pending gateway Subscribe previously remained unresolved after
  close because its effect raced only the timeout. The regression now holds the
  callback, closes the gateway, and proves both the abort event and prompt
  public-handle rejection. `withTimeout()` races the same controller's abort
  event, clears its timeout timer, and the admitted gateway wire is zeroed in
  the handle-finally path.
- Pre-binding compensation now uses the tracked Subscribe controller, so close
  aborts the creator operation and any mandatory compensation through one
  gateway-owned lifecycle. The forwarding-only `#handle` method is removed.
- Fresh focused verification remained 3 auth files / 37 tests with auth
  typecheck, generated TypeDoc/API inventory, Prettier, and diff hygiene all
  passing. No Spine JVM command ran.

### B3 final interleaving evidence

- The interrupted final-test context left eight complete regressions, which the
  coordinator preserved and verified: synchronous gateway admission under a
  delayed session; stale Actor/tenant Activate and Cancel; queued foreign
  owner/tenant denial without slot consumption; Subscribe timeout/abort/lease
  release; oversize and create-failure compensation timeout/abort; true
  cancel-first ordering; expiry across active/queued work; and close across an
  active effect plus bounded cleanup.
- Coordinator verification passed 3 auth files / 45 tests, auth typecheck,
  exact 43-export TypeDoc/API inventory, formatting, and diff hygiene. No Spine
  JVM command ran.

### B3 terminal-race correction evidence

- The initial 33-test/41-export evidence block is historical and superseded by
  the later 45-test/43-export checkpoint. RED held binding creation after a
  backend Subscribe completed, then closed the gateway: the original controller
  was no longer tracked, so the race did not prove safe compensation.
- GREEN retains the pending Subscribe controller and capacity reservation until
  pre-binding retention settles. If close has already aborted that controller,
  compensation receives a fresh live controller bounded by `shutdownTimeoutMs`.
  The regressions prove disposal runs without an orphaned binding and that a
  timed-out compensation releases a capacity-one lease for a successful follow-up
  Subscribe.
- Fresh verification: three focused auth files / 47 tests passed; `tsc --noEmit
-p packages/auth/tsconfig.json` and generated TypeDoc/API checking passed;
  Prettier and `git diff --check` passed. No Spine JVM command ran.

### B3 terminal-race P2 regression closure

- The accepted reliability P2 regression holds disposal after close races a
  completed Subscribe but before retention. It proves the fresh compensation
  controller starts live, is aborted at `shutdownTimeoutMs`, settles the public
  handle, releases the capacity-one reservation, and leaves no retained binding.
- No runtime change was needed: RED exposed only an initially incomplete test
  seam; the corrected behavior test passed against the existing implementation.
- Fresh verification: three focused auth files / 47 tests passed; auth
  typecheck, generated TypeDoc/API checking, Prettier, and diff hygiene passed.
  No Spine JVM command ran.

### B3 coverage test expansion

- Four behavior-focused B3 tests cover terminal/unknown/malformed requests,
  invalid timestamp, unauthenticated/forbidden/expired security outcomes,
  tenant-less trusted context with present allowlisted transport facts, invalid
  backend/ID storage inputs, and capacity-failure mapping. Production behavior,
  thresholds, and ignore configuration are unchanged.
- Focused coverage passed 3 auth files / 51 tests and raised
  `subscriptions/index.ts` branch coverage from 77.43% to 88.71% (+22
  branches). Against the preceding unrestricted 8,637/9,621 total, the result
  is exactly 8,659/9,621 (90.00%). The local unrestricted rerun exited cleanly
  but left no final coverage summary or LCOV artifact; the exact post-change
  global report therefore remains pending coordinator confirmation.

### B3 final-test context replacement

- The targeted correction context returned twice without implementing the
  explicitly assigned gateway/interleaving evidence and left the report's
  canonical-tenant wording unchanged. Preserve its verified standard signal,
  abort-race, pending Subscribe settlement, capacity lease, and method-split
  runtime changes; neither return closes B3.
- A fresh existing `implementer`, expected explicit `gpt-5.6-terra` /
  `medium`, owns only the finite missing B3 regression matrix, any defect those
  tests expose, the already-aborted signal guard, and truthful report evidence.
  No B4/C+, commit, push, merge, children, or Spine JVM execution.

### B1 implementation evidence

- RED/GREEN TDD evidence: the absent auth package made the first focused
  request-contract test fail; package-local decoding then passed for exhaustive
  Command/Query/Subscribe/Activate/Cancel kinds, allowlisted transport facts,
  registry-backed decoding, and safe malformed/unknown `Any` type-URL-only
  fallback. A focused source-verifier test first rejected the auth-owned Proto
  as lacking copied-source provenance; the verifier now accepts a separately
  checksum-pinned owned source without fabricating upstream provenance.
- Delivered `@spine-event-engine/auth`, `spine.auth.AuthenticationService`
  `ResolveContext`, source/generated manifest and frozen descriptor checksum
  updates, plus provider-neutral authenticator/session/authorization/context,
  clock, decoder, and optional TypeRegistry seams. ResolveContext returns only
  informational actor, tenant, and expiry values; it carries no credential.
- Focused validation passed: `pnpm --config.link-workspace-packages=true
proto:generate` (40 source checksums, 49 descriptors),
  `typecheck:build:generated`, focused Vitest (3 files, 10 tests),
  `format:check`, and `git diff --check`.
- No B2 forwarding/context replacement, C-series sessions/OIDC/providers,
  React/Chat/Envoy behavior, commit/push/merge, or Spine JVM build, test,
  generation, dependency resolution, launch, or other project execution
  occurred.

### B1 review and final acceptance

- Complete review wave dispositions:
  - style/maintainability: clean after adding malformed-envelope, per-kind
    target/context, fallback, and header-normalization regressions;
  - documentation: one accepted wording precision finding corrected; final
    claims are accurate;
  - TypeScript/API: canonical auth Proto subpath and exact 22-export auth
    TypeDoc inventory are enforced; targeted re-review is clean;
  - performance/reliability: N/A because B1 adds contracts and bounded
    synchronous decoding only, without concurrency, persistence, lifecycle,
    retry, streaming, cancellation, or resource ownership;
  - security: deferred to the final Wave 4 release-readiness gate as required
    by the protocol.
- The first full gate exposed three deterministic frozen-inventory omissions;
  focused corrections added the auth package export, 40-source manifest count,
  and two auth message schemas. The next full gate passed every test but reached
  only 89.96% branches.
- Meaningful fallback coverage then exposed and corrected one real defect:
  explicitly undefined allowlisted headers are omitted rather than represented
  as empty request/correlation IDs. Targeted style and API re-reviews are
  clean.
- Final unrestricted evidence:
  `pnpm --config.link-workspace-packages=true
--config.verify-deps-before-run=false test:coverage:generated` passed 142
  files / 2,639 tests; 3 files / 25 tests were intentionally skipped. Coverage:
  94.10% statements, 90.02% branches (8,434/9,368), 94.31% functions, and
  94.74% lines.
- Every implementation/reviewer dispatch used the expected explicit role,
  model, and reasoning. Desktop does not expose independent child runtime
  self-introspection; immutable configured profiles matched and no fallback or
  mismatch was visible.
- B1 is accepted for one task-branch commit and immediate origin push. No Spine
  JVM build, test, generation, dependency resolution, launch, or other project
  execution occurred.

## 2026-07-28 — B2 unary authentication pipeline

- Base and remote task branch are synchronized at
  `232e1f0598c17d2a2ffdde4ae8be13f0558b74ee`.
- Classification: high-risk because B2 authenticates and authorizes unary
  requests, compares untrusted serialized context, constructs the trusted
  security context, and controls whether backend work may begin. The approved
  Wave 4 plan and execution split already freeze this architecture, so no new
  splitter dispatch is required.
- Behavior-focused acceptance:
  1. add one small injected unary gateway pipeline for CommandService.Post,
     QueryService.Read, and AuthenticationService.ResolveContext; native gRPC
     transport remains B4;
  2. enforce a finite request-byte bound before decoding; reject malformed or
     unknown envelopes without authenticating, authorizing, resolving, or
     forwarding;
  3. for Post/Read, resolve the application session credential, construct the
     typed `IncomingRequest`, authorize it, resolve trusted Actor/tenant/time
     and validated hints, reject unauthenticated/denied/stale actor-or-tenant
     requests before forwarding, then replace the entire client-supplied
     `ActorContext` with a freshly constructed gateway-owned value;
  4. preserve every non-ActorContext Command/Query field byte-equivalently and
     prove the backend receives no caller credential or original context
     object; commands are never retried;
  5. ResolveContext authenticates the application session and returns only
     informational actor, tenant, and session expiry through the B1 Proto
     response; it does not forward to a Spine backend or treat returned values
     as credentials;
  6. expose a minimal transport-neutral unary forwarding/error seam suitable
     for B4 mapping, without implementing subscription binding, native gRPC,
     cookies, signed sessions, OIDC/providers, React, Chat, or Envoy;
  7. test exact pipeline order, no-forward failures, stale/fabricated context,
     fresh context replacement, byte preservation, size/malformed input,
     ResolveContext, and dependency cleanup; focused checks and later full
     coverage pass.
- Existing role: `implementer`.
- Ownership: `packages/auth/**`, B2-focused tests and package/API inventory,
  bounded B2 implementation report, and only minimal B1 contract adjustment
  demonstrably required by ResolveContext. Shared Proto changes are out of
  scope unless an actual B2 blocker is first reported.
- Expected model: `gpt-5.6-terra`.
- Expected reasoning: `medium`.
- Both fields must be explicit in dispatch. The owner uses behavior-first TDD,
  preserves unrelated/concurrent edits, may not commit, push, merge, or spawn
  children.
- Runtime metadata limitation: immutable configured role/profile and explicit
  dispatch are available; independent child self-introspection is not. Reject
  only a visible mismatch, omitted field, wrong role, or actual inherited
  fallback.
- Permanent Wave 4 rule: Spine JVM is static read-only reference material only.
  No JVM build, test, generation, dependency resolution, launch, or any other
  project execution is permitted.

### B2 implementation evidence

- RED: six focused `UnaryGateway` tests failed against the preserved
  command-only path. The failures demonstrated missing decode-before-session
  ordering, distinct rejection outcomes, stale actor/tenant checks,
  injected-clock context construction, Query.Read, and ResolveContext.
- GREEN: `UnaryGateway` requires a finite non-negative bound and bounds bytes
  before known-envelope decoding;
  malformed/unknown requests perform no session/policy/context/forward work.
  Post and Read authenticate, authorize, resolve trusted context, reject stale
  actor or tenant hints, replace the complete context, and forward once without
  a credential. ResolveContext validates the session and returns informational
  actor, tenant, and expiry without policy or backend forwarding.
- Focused verification passed: `packages/auth/test/unary-gateway.test.ts`
  (1 file / 7 tests) and `typecheck:build:generated`. The initial generated
  typecheck caught narrow-union and exact-optional construction defects; both
  were corrected before the passing rerun.
- The API inventory now includes the six B2 public gateway exports, and
  `packages/auth/README.md` describes the implemented boundary and B4/C-series
  limits. No Spine JVM operation occurred.

### B2 review-correction evidence

- RED: canonical-route, mutable-collaborator, changing-clock, and unknown-field
  regressions failed against the reviewed gateway path.
- GREEN: the semantic `src/gateway/` module now owns B2 contracts and runtime;
  root re-exports it. It snapshots canonical allowlisted transport facts before
  awaits, isolates authorization/context views, retains the untouched envelope
  and identity snapshot, uses the resolver timestamp, and preserves unknown
  fields without retrying forwarding.

### B1 review-correction evidence

- The canonical Proto entrypoint test now freezes and resolves `./auth` /
  `@spine-event-engine/proto/auth`. TypeDoc now includes `packages/auth`, and
  the API-doc checker freezes its exact public export inventory.
- New regressions reject malformed outer Query/Subscribe/Activate/Cancel bytes
  and prove decoded target/context facts for each relevant envelope. README
  wording now correctly identifies the optional registry as a property of the
  command `IncomingRequestInput` variant.
- Fresh validation passed: `pnpm --config.link-workspace-packages=true
docs:check`, `typecheck:build:generated`, focused Vitest (4 files / 15
  tests), `format:check`, and `git diff --check`. No B2+ work or Spine JVM
  project operation occurred.

### B1 full-gate mechanical correction

- Frozen package metadata now includes `./auth`, and the Proto module test now
  counts 40 owned sources including `spine.auth.ResolveContextRequest` and
  `spine.auth.ResolveContextResponse` through the auth schema inventory.
- Focused evidence passed: Vitest (6 files / 21 tests), generated build
  typecheck, docs/API checks, formatting, and `git diff --check`. No runtime
  behavior, B2+ scope, or Spine JVM project operation occurred.

### B1 coverage correction

- Added meaningful fallback coverage for missing command/query/topic/
  subscription facts and optional transport fields. It exposed one real bug:
  an explicitly undefined allowlisted header became an empty correlation fact.
  Header normalization now omits undefined values.
- Focused auth tests (5/5), generated build typecheck, formatting, and diff
  hygiene pass. A single-file coverage invocation reports global repository
  thresholds, so its threshold failure is not a focused-test failure. No B2+
  or Spine JVM project operation occurred.

### B3 final full-gate closure

- Four behavior-focused subscription tests raised the auth suite to 3 files /
  51 tests and the new subscription module to 88.71% branch coverage without
  production, threshold, or ignore changes.
- Independent focused auth tests, auth typecheck, generated TypeDoc/API
  checking, formatting, and diff hygiene passed.
- The repeated unrestricted TypeScript gate passed 144 runnable files / 2,685
  tests with 25 skipped. Coverage is 94.07% statements, 90.00% branches
  (8,659/9,621), 94.30% functions, and 94.79% lines.
- Every B3 reviewer concern is clean or resolved. No Spine JVM build, test,
  generation, dependency resolution, launch, or other project execution ran.

## B4 architecture assignment

- B4 is high-risk because it changes a public native-forwarding seam,
  server-stream lifecycle, concurrency, cancellation, and resource ownership.
- The existing `requirements_splitter` receives the bounded
  `B4_TASK_BRIEF.md`. Expected model is explicit `gpt-5.6-sol`; expected
  reasoning is explicit `high`. The selected Desktop surface supports both
  fields.
- Scope is the smallest B3-compatible contract for native unary forwarding and
  bounded server-stream relay, including exact limits, ownership, error
  propagation, and TDD slices. It is read-only: no code edits, commit, push,
  merge, children, or Spine JVM project execution.
- Runtime self-introspection may be unavailable; immutable role/profile and
  explicit dispatch fields are the acceptance evidence unless a visible
  mismatch or fallback appears.

### B4 architecture result and implementation assignment

- The initial requirements-splitter context produced the core public-update
  sink and 64-message/1-MiB limit decision but failed to finish two bounded
  turns. It was interrupted without edits. A fresh existing
  `requirements_splitter`, explicitly `gpt-5.6-sol` / `high`, completed the
  exact relay contract. Runtime self-introspection was unavailable; the
  immutable role/profile showed no mismatch or fallback.
- Coordinator inspection corrected one direction error in the returned
  pseudocode: the Connect-facing Activate adapter wraps
  `SubscriptionGateway`, not `SubscriptionCreator`, so B3 security and opaque
  ownership cannot be bypassed. The injected native backend remains behind
  B2/B3.
- The implementation reuses installed `@connectrpc/connect` 2.1.2 and shared
  generated service descriptors; no new infrastructure library is required.
- One existing `implementer` owns B4 production code, behavior-focused tests,
  root exports/API inventory, package metadata/docs, `B4_IMPLEMENTATION_REPORT`,
  and B4 work/review evidence. Expected model is explicit `gpt-5.6-terra`;
  expected reasoning is explicit `medium`.
- The owner must read and follow the test-driven-development skill, preserve
  unrelated files and the local progress ledger, and may not commit, push,
  merge, spawn children, begin C+, or execute any Spine JVM project command.

### B4 focused implementation and pre-review rejection

- The implementer completed the first focused B4 pass under its explicit
  `gpt-5.6-terra` / `medium` assignment. Independent runtime
  self-introspection was unavailable; the immutable role/profile showed no
  mismatch or fallback.
- A failed offline install damaged only generated worktree dependency links.
  The coordinator preserved the broken directories under `/private/tmp`, then
  restored all root and package-local links canonically with the frozen
  lockfile and `link-workspace-packages=true`.
- Independent focused verification now passes 4 auth files / 55 tests, auth
  typecheck, generated TypeDoc/API checking, formatting, and diff hygiene.
- Pre-review lint rejects the pass as incomplete before specialist dispatch:
  1. downstream abort closes only the public relay; a quiet native Activate
     stream is not immediately aborted through B3;
  2. relay failure resolves a pending iterator waiter with a rejected Promise
     cast as an iterator result instead of rejecting that waiter;
  3. native creator and gateway-service behavior plus the required terminal
     convergence matrix lack direct tests;
  4. generic handler helpers use `as never` and erase method-contract errors;
  5. the report calls B4 complete while explicitly deferring required terminal
     acceptance to the coordinator.
- The same existing implementer owns this complete deterministic correction
  batch under its original explicit `gpt-5.6-terra` / `medium` assignment. It
  must use RED/GREEN tests, may change the smallest B3 cancellation seam needed,
  and may not commit, push, merge, begin C+, spawn children, or execute any
  Spine JVM project command.

### B4 handler-test context replacement

- The original implementer completed immediate external-abort propagation,
  true relay-waiter rejection, typed native handlers, and the direct
  NativeSubscriptionCreator transport matrix. Focused auth evidence is 4 files
  / 58 tests plus auth typecheck and diff hygiene.
- That context ended four bounded turns while repeatedly identifying the same
  remaining direct `createNativeGatewayServices` handler/terminal matrix. It is
  closed without acceptance; its verified work is preserved.
- A fresh existing `implementer` owns only the missing handler/terminal tests,
  any concrete defect those tests expose, and final B4 evidence. Expected model
  is explicit `gpt-5.6-terra`; expected reasoning is explicit `medium`.
  Runtime self-introspection may be unavailable; immutable role/profile and
  explicit dispatch fields are the acceptance evidence.
- No installs, B4 redesign, C+, commit, push, merge, children, or Spine JVM
  project execution are permitted.

### B4 implementation evidence

- RED/GREEN added the copied asynchronous `SubscriptionUpdateSink` to the B3
  activation lifetime and the native relay FIFO/overflow boundary.
- `NativeSubscriptionCreator` uses the installed Connect 2.1.2 transport with
  shared descriptors. Public service handlers delegate to `UnaryGateway` or
  `SubscriptionGateway`; Activate does not bypass B3 by calling the creator.
- Focused authentication validation passed 4 files / 55 tests, auth TypeScript
  typecheck, Prettier, and diff hygiene. The generated TypeDoc/API script could
  not run because the earlier offline package repair left this worktree without
  its local `node_modules/.bin/typedoc`; existing primary-worktree binaries and
  explicitly repaired local workspace links were used for the focused checks.
- No Spine JVM command, commit, push, merge, C+ work, or network retry ran.

### B4 direct handler and terminal matrix closure

- The replacement existing `implementer` completed the remaining direct
  `createNativeGatewayServices` handler tests under the explicit assigned
  `gpt-5.6-terra` / `medium` profile. Independent runtime self-introspection
  remains unavailable; immutable role/profile and the explicit dispatch have
  no visible mismatch or inherited fallback.
- RED: a quiet fake B3 Activate held its backend effect after public iterator
  `return()` or `throw()`; the async-generator adapter could close only its
  relay and did not abort the B3 effect. GREEN: a small linked controller and
  iterator wrapper preserve external-context abort propagation while making
  downstream terminal operations abort the quiet B3 effect exactly once.
- Direct typed fake-gateway evidence covers Post/Read/Subscribe/Cancel success,
  all unary/subscription rejection status mappings, Activate's B3-only call
  path, FIFO updates, abort-before-update, iterator return/throw,
  backend/gateway rejection, normal completion, message/byte overflow, and
  explicit Cancel. Tests do not access private relay state; terminal behavior
  proves no pending consumer survives a terminal path.
- Validation passed: focused handler matrix 1 file / 27 tests; full auth suite
  5 files / 85 tests; auth typecheck; generated TypeDoc/API checking; Prettier;
  and `git diff --check`. The full repository gate and reviewer wave remain
  coordinator work. No install, commit, push, merge, C+, or Spine JVM command
  ran.

### B4 complete review correction assignment

- The complete style, TypeScript/API, performance/reliability, and
  documentation wave produced one deduplicated correction batch recorded in
  `build-protocol/reviews/T-0075-wave4-browser-interoperability.md`.
- One existing `implementer` owns all B4 correction code, focused RED/GREEN
  tests, public TSDoc/README, API inventory, and evidence. Expected model is
  explicitly `gpt-5.6-terra`; expected reasoning is explicitly `medium`.
- Runtime self-introspection may be unavailable; the immutable configured
  role/profile and explicit dispatch fields are the acceptance metadata unless
  a visible mismatch or fallback appears.
- The owner must preserve B2/B3 security and opaque-envelope ownership, use no
  new dependency without a demonstrated need, and may not install, commit,
  push, merge, begin C+, spawn children, or execute any Spine JVM project
  command.

### B4 targeted re-review correction assignment

- The complete targeted re-review accepted one remaining P1 queued-expiry race
  and deterministic P2 formatting, API-inventory, README-accuracy, and TSDoc
  corrections. The exact batch is recorded in the review log.
- One existing `implementer` owns the forced-interleaving RED/GREEN regression,
  minimal signal re-check, deterministic documentation/API fixes, and evidence.
  Expected model is explicitly `gpt-5.6-terra`; expected reasoning is
  explicitly `medium`.
- Runtime self-introspection may be unavailable; the immutable configured
  profile and explicit dispatch fields are the acceptance metadata. No install,
  commit, push, merge, C+, child dispatch, or Spine JVM project execution is
  permitted.

### B4 final API documentation correction

- Final style, reliability, and documentation acceptance are clean.
- TypeScript/API acceptance requested one deterministic TSDoc correction:
  enumerate the existing rejection-to-Connect status mapping on the native
  gateway factory. The orchestrator owns this record/documentation-only change;
  runtime behavior is unchanged and only TypeScript/API docs reopens.

### B4 full-gate branch coverage assignment

- Final specialist review is converged. A canonical full run passed 146
  runnable files / 2,728 tests with 25 skipped but missed branch coverage at
  89.99% (8,756/9,729) against 90%.
- One existing `implementer`, explicitly `gpt-5.6-terra` / `medium`, owns only
  behavior-focused B4 test expansion for real uncovered outcomes. Production,
  thresholds, ignore directives, C+, commits, pushes, merges, child dispatch,
  and Spine JVM execution are prohibited.

### B4 full-gate acceptance

- Three behavior-only native relay/gateway tests raised the auth suite to 97
  tests without production, threshold, or ignore-directive changes.
- Independent focused checks passed. The final canonical full gate passed 146
  runnable files / 2,731 tests with 25 skipped and 90.04% branch coverage
  (8,760/9,729); statements are 94.08%, functions 94.26%, and lines 94.80%.
- B4 is review-converged and mechanically complete. No Spine JVM project
  command ran.

### B4 full-gate branch coverage correction evidence

- The existing `implementer` completed the test-only correction under explicit
  `gpt-5.6-terra` / `medium` assignment. Runtime self-introspection is
  unavailable; explicit dispatch and immutable configured profile are the
  metadata evidence, with no visible mismatch or inherited fallback.
- Added real B4 behavior coverage for immediate waiter delivery, post-close
  backend update rejection, and unexpected Activate acknowledgement mapping to
  Connect `Internal`. No production source, limits, ignore directives, or
  dependencies changed.
- Direct focused Vitest validation passed 2 files / 38 tests; the full focused
  auth suite passed 5 files / 97 tests. Focused V8 coverage exercised the
  intended native branches; its project-wide threshold result is necessarily
  non-passing at 84/9,729 branches because only two test files ran. Canonical
  full coverage remains required. Prettier for the added relay test and durable
  records, plus `git diff --check`, pass; standalone ESLint reports existing
  errors elsewhere in the already-untracked B4 test files.
- `pnpm exec vitest` is blocked before execution by an installed-workspace
  `linkWorkspacePackages` mismatch. No install was authorized or run; the
  installed Vitest binary completed focused validation. No commit, push,
  merge, C+, child dispatch, or Spine JVM command ran.

### B4 complete-review correction implementation

- The existing `implementer` completed the single accepted correction batch
  under explicit `gpt-5.6-terra` / `medium` assignment. Independent runtime
  self-introspection remains unavailable; immutable role/profile and dispatch
  fields show no visible mismatch or inherited fallback.
- RED/GREEN proves pre-aborted real B3 activation makes zero native calls,
  the active-effect signal is required by SubscriptionBindings, natural
  completion removes the binding through bounded B3 cancellation, live session
  expiry aborts without another request, malformed updates reject/purge/abort,
  and later iterator/context termination supersedes graceful relay drain.
- Unary HandlerContext cancellation reaches B2 forwarding and native Post/Read;
  B2 cancels a non-cooperative forward effect without exposing credentials or
  changing copy/context-rewrite behavior. Relay and handler tests use distinct
  non-empty payloads for copied FIFO assertions.
- Focused validation passed: auth suite 5 files / 92 tests, auth typecheck,
  generated TypeDoc/API checks, Prettier, and diff hygiene. No install, commit,
  push, merge, C+, or Spine JVM command ran. Targeted re-review and full
  repository verification remain coordinator work.

### B4 targeted re-review final correction implementation

- Existing `implementer` assignment remained explicitly `gpt-5.6-terra` /
  `medium`; independent runtime self-introspection is unavailable, while the
  immutable configured profile and dispatch show no visible mismatch or
  inherited fallback.
- RED/GREEN forced a real in-memory binding's queued Activate signal to abort
  behind a first failing Activate. The pre-fix queued effect incorrectly
  returned `activated`; the corrected post-await check returns `denied` before
  state/native invocation, with zero queued native calls.
- Reflowed B4-authored overlength lines, froze `NativeGatewayServicesOptions`
  in the auth API inventory, qualified README cleanup success versus retryable
  cancellation retention, and completed the required public native TSDoc.
- Focused subscription validation passed 1 file / 40 tests. Full auth
  validation passed 5 files / 94 tests, auth typecheck, and generated
  TypeDoc/API checking, Prettier, and diff hygiene. No install, commit, push,
  merge, C+, child dispatch, or Spine JVM command ran.
