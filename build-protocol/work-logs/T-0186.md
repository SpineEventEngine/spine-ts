# T-0186 Work Log

## 2026-08-14 - Start

- Created `task/T-0186-wave11-convergence` from integrated Wave 11 baseline
  `f128af42` in isolated worktree `.worktrees/T-0186-wave11-convergence`.
- Classified the milestone as high-risk release closure because it converges
  shared generated-source publication, compiler-input confinement, public
  routing, durable replay, examples, documentation, security, and release
  evidence.
- Recorded the existing implementation owner as `implementer`, explicit
  `gpt-5.6-terra` / medium. Desktop runtime telemetry does not expose
  independent child metadata; the immutable configured profile is the
  available evidence.
- Recorded the complete required specialist and final-security profiles before
  dispatch. No review or release profile has started.

## 2026-08-14 - Audit correction and fixture convergence

- Ran the required one-time generated-output/prohibited-name audit as an
  inline terminal command only; no broad ordinary test gate was introduced.
  The initial `169`-file TS/d.ts inventory found two unnoticed generated Todo
  interface declarations:
  `examples/todo/dist/generated/interfaces/task-event.d.ts` and
  `task-assignment-event.d.ts`.
- Added the focused behavior test first. Its command and expected RED were:
  `pnpm exec vitest run scripts/normalize-generated-declarations.test.mjs -t
'normalizes generated interface declarations with their Proto provenance'`;
  it failed because the generated notice was absent.
- Applied the minimal fix in `scripts/normalize-generated-declarations.mjs`:
  include generated `interfaces/*.d.ts` in the bounded declaration inventory.
  The focused test then passed; `pnpm typecheck:build:generated` regenerated
  the declarations. The repeated one-time audit output was exactly
  `{ "generatedFamilyFiles": 169, "violations": [] }`.
- Prohibited active-name scan found no `TaskReassignmentEvent`,
  `routeSemantic`, or `@Route`. Deployment matches document the established
  unsupported Cloud Run/multiple-Gateway boundary; no hidden support claim was
  found.
- Focused evidence passed:
  - `pnpm proto:generate && pnpm typecheck:build:generated` — 47 Proto
    checksums and 52 frozen descriptors (digest
    `8e2be5dd99bf7865e9fc65b5bf615a55db4109c024eb57873064c8301bc5d5bd`);
  - `pnpm exec vitest run packages/proto-tools/test/proto-tools.test.ts -t
'<publication matrix>'` — 5 passed;
  - `pnpm exec vitest run scripts/proto-workflow.test.mjs -t
'<publication matrix>'` — 8 passed;
  - focused declaration RED/GREEN and generated-policy/server-fixture suite —
    passed after the correction.
- Cheap preflight and changed-source coverage passed at
  `a4ce5fa88b886e12003c9a6a9d0710c094d0a002`:
  `pnpm verify:task -- --coverage scripts/normalize-generated-declarations.test.mjs
--source scripts/normalize-generated-declarations.mjs`. It regenerated the
  expected outputs and completed with exit `0`. `verify:release` and all
  specialist review dispatches remain deliberately unrun.
- Direct final pre-review checks passed: `pnpm docs:audience:check`,
  `pnpm lint:copyright`, `pnpm docs:api:check`, owned-record Prettier check,
  and `git diff --check`. Pre-review records head
  `6a41476834bd459adf428bd1a09bdd415e16f4d5` was pushed cleanly.
- Cleared the last repository-wide formatter failures mechanically, limited to
  `examples/todo/REFERENCE.md`, `examples/todo/USER_GUIDE.md`, and
  `scripts/check-typescript-snippets.test.mjs`. The diff contains only table,
  wrapping, and quote formatting. Because a runnable TypeScript fence wrapped,
  reran `scripts/check-typescript-snippets.test.mjs` with the Todo reader
  contract (`18` passed) and `pnpm docs:snippets:check`; both passed.
- `pnpm format:check`, `git diff --check`, and
  `pnpm verify:task -- --no-coverage scripts/check-typescript-snippets.test.mjs`
  passed. This formatting-only correction does not require changed-production
  coverage or a new review/release dispatch.

## 2026-08-14 - Accepted review-correction intake

- Copied the accepted requirements-splitter correction plan before runtime
  edits. Splitter assignment: existing `requirements_splitter`, explicit
  `gpt-5.6-sol` / high. Implementation remains existing `implementer`,
  explicit `gpt-5.6-terra` / medium. Desktop runtime telemetry does not expose
  independent execution metadata, so immutable configured profiles are
  recorded instead.
- Ordered work: forged descriptor validation; manifest v2 generation-ID and
  commit-point protocol; retained recovery evidence/aggregate failures;
  bounded tree traversal and declaration idempotency; then documentation and
  record wording. Each runtime slice begins with focused behavior RED/GREEN.

## 2026-08-14 - Correction convergence

- Extended declaration normalization coverage to prove generated interface
  declarations are byte-identical on a second run. The bounded inventories now
  prove depth 65 and 1,001 entries reject while exactly 1,000 entries accept.
- The focused v2 operational-reader run initially exposed stale v1 dependency
  fixtures. Migrated only the affected fixture publisher to write format-2
  manifests, nonempty fixture generation IDs, matching generated-root markers,
  and required generated exports. Graph generation and application composition
  now exercise the same manifest reader seam without bypassing marker checks.
- Corrected reader documentation: a drain is page-bounded but repeated pages
  may process an unbounded total backlog; the 30-second Inbox deduplication
  window is admission control, not replay retention. Proto publication now
  describes tree/marker installation before the manifest commit point and
  retained journal evidence with fail-closed readers if restoration fails.
- Focused evidence: `pnpm exec vitest run packages/proto-tools/test/proto-tools.test.ts
--testNamePattern='links a cross-model|uses each direct dependency|composes through each installed'`
  passed 3; `pnpm exec vitest run scripts/normalize-generated-declarations.test.mjs
scripts/proto-workflow.test.mjs --testNamePattern='<bounds/declaration/recovery matrix>'`
  passed 7. `pnpm docs:audience:check`, `pnpm lint:copyright`,
  `pnpm docs:api:check`, targeted Prettier, and `git diff --check` passed.

## Historical Next (superseded)

- Run the converged cheap task profile with changed-source coverage, record its
  exact result, then commit and push this correction-complete implementation.
  No specialist review, security review, release profile, merge, or tag has
  started.

## 2026-08-14 - Final fixture convergence handoff

- The preceding implementation owner exhausted its context; final convergence
  remains with the existing `implementer`, explicitly configured
  `gpt-5.6-terra` / medium. Desktop exposes no independent runtime profile
  telemetry, so the immutable configured role/profile is recorded as evidence.
- Preserved the dirty v2 fixture migration and repaired only its stale routing
  assumptions. State feedback tests now create a bounded second
  descriptor-valid Projection schema (not an Aggregate schema), preserving the
  shared scalar identity shape. The catch-up test now delivers generated
  `TaskCreated.taskListId` through a compatible `TaskListId` route and reads
  the message-valued state ID with that generated schema.
- RED evidence inherited at handoff: focused routing had 241 passing and three
  failing tests. After the fixture corrections, the same command reported 244
  passing tests; `git diff --check` was clean.

## 2026-08-14 - Clean-checkout marker correction

- Added the five matching v2 generated-root markers required by clean-checkout
  model composition (`084eb6bb`, `e89f180f`). The generated-output checker now
  permits only that exact root marker; all generated source files remain
  ignored and untracked. Focused checker evidence: 10 tests passed; repeated
  generation retained the 47-source/52-descriptor digest and current-output
  check passed.

## 2026-08-14 - Coverage closure in progress

- Final implementation owner remains the existing `implementer`, explicit
  `gpt-5.6-terra` / medium; Desktop exposes no independent runtime-profile
  telemetry.
- Added public generation behavior coverage: changed output receives a fresh
  generation ID, and null, v1, tree-mismatched, or manifest-mismatched committed
  state never reuses the previous ID. Focused test passed; the full proto-tools
  suite passed 109 tests under one V8 worker. Its exact changed-source result is
  `generator.ts` 32/32 executable lines and 20/20 branches; `index.ts` remains
  13/14 lines and 6/7 branches (`manifest requires model mode`).
- Added adversarial public MessageInterfaces membership inputs for primitive,
  reflection-trapped, wide/deep, cyclic, and malformed descriptor trees. The
  full core suite passed 57 tests. Isolated V8 coverage is below its repository
  global threshold by design, but exact changed-source mapping remains 27/28
  lines and 34/38 branches; uncovered mapping is line 157 and branches
  146:2:0, 150:5:0, 153:7:0, and 154:8:0. This is not accepted as closure.

## 2026-08-14 - Changed-source coverage closed

- The continuing implementation owner remains the existing `implementer`,
  explicitly configured as `gpt-5.6-terra` / medium. Desktop exposes no
  independent child runtime-profile telemetry; the immutable configured
  role/profile remains the assignment evidence.
- Fresh Prettier validation covered `scripts/proto-workflow.mjs` and its test,
  followed by a fresh workflow run of `77/77` tests. The single-worker
  workflow-only V8 profile also passed `77/77` and retained
  `/tmp/t0186-workflow-coverage-current/lcov.info`.
- A fresh serial affected-source V8 profile passed seven files and `379/379`
  tests. Intersecting its retained
  `/tmp/t0186-changed-source-coverage-current/lcov.info` with executable points
  on added lines in `origin/main` through the current worktree gives **150/154
  executable lines (97.40%)** and **120/128 branches (93.75%)** across all nine
  changed production sources.
- Exact changed slices: Core `28/28` lines and `38/38` branches; generator
  `32/32` and `20/20`; Proto Tools index `14/14` and `7/7`; cleanup `2/2` and
  `2/2`; generated-clean `3/4` and `3/3`; artifact writer `8/8` and `5/5`;
  declaration normalizer `2/2` and `4/4`; workflow `61/64` and `41/49`.
  `check-api-docs.mjs` changed only a non-executable inventory line, so its
  changed executable denominator is zero.
- The remaining misses are generated-clean CLI exit line `359` and workflow
  fallback lines `283`, `313`, and `462`, plus workflow branches at lines
  `279`, `282`, `309`, `447`, `449`, `452`, `456`, and `461`. The required
  aggregate gate is already above 90% in both metrics, and the misses are
  defensive/CLI fallbacks rather than missing normal-transaction behavior.
- The first diagnostic coverage invocation rejected unsupported Vitest option
  `--minWorkers` before tests started; the corrected `--maxWorkers=1` invocation
  produced the retained report. A first read-only LCOV parser attempt assumed
  an absolute `SF:` path; accepting Vitest's actual repository-relative `SF:`
  record produced the exact results above. Neither diagnostic failure changed
  repository state.

## 2026-08-14 - Root generation-ID reuse correction assignment

- Cheap-preflight generation reproduced a deterministic root-only defect: the
  first and second unchanged canonical generations issued different
  `packages/proto` generation IDs (`f34c12f4-...` then `32aa4024-...`). All
  four example model IDs/hashes remained stable, current-output validation
  passed, and no stage, backup, claim, journal, or lock residue remained.
- The bounded correction is assigned to the existing `implementer` role with
  explicit `gpt-5.6-terra` / medium dispatch fields. It owns root-cause
  investigation, one behavior RED, the minimal production correction in the
  root artifact/workflow publication path, directly affected tests, and this
  work-log evidence. It must not spawn subagents or revert concurrent/user
  work. Desktop exposes no independent runtime telemetry; the immutable
  configured role/profile will be the acceptance evidence unless the surface
  reports a visible mismatch.

## 2026-08-14 - Root generation-ID reuse correction

- Root-cause trace confirmed the staged/live root manifests were canonically
  equal and the root generated trees matched `48/48` files. The defect was not
  artifact content: `reuseStagedGenerationId()` ran for model stages only and
  was omitted from the root staging path, so a fresh staged UUID published.
- Behavior-first regression added: `pnpm exec vitest run
scripts/proto-workflow.test.mjs --testNamePattern='reuses a v2 root generation
ID'` was RED with `root-staged` published instead of `root-live`; it was GREEN
  after root staging invokes the existing reuse gate after either root manifest
  producer. The full workflow suite then passed `78/78`.
- Formatted `scripts/proto-workflow.mjs` and its test; `git diff --check`
  passed. Two serial canonical `pnpm proto:generate` runs both passed 47
  source checks and the 52-descriptor digest
  `8e2be5dd99bf7865e9fc65b5bf615a55db4109c024eb57873064c8301bc5d5bd`.
  Their five manifest/marker pairs were byte-identical; root manifest and
  marker hashes were respectively
  `789377150677556cae18d6573ea3431d9eea91758e242224728092cbfcd59d7a`
  and `0c1f44ee7cee2b15b46e98093b877a3c51318e7c6138d09c35131dd71929f7f1`,
  with retained root ID `32aa4024-1052-4da2-ae47-f2ec6b8b431d`.
- `pnpm proto:check-generated:current` passed. The stage, backup, claim,
  publication-journal, and workflow-lock residue scan was empty. The owner
  remains the existing `implementer`, explicitly configured `gpt-5.6-terra` /
  medium; Desktop runtime telemetry remains unavailable, so immutable configured
  role/profile is the acceptance evidence.

## 2026-08-14 - Tooling typecheck correction assignment

- After independently accepting the root reuse correction, the orchestrator
  reran the preflight. Generated build/typecheck completed, but
  `pnpm typecheck:tooling` failed with three test-fixture diagnostics:
  duplicate object property at `packages/proto-tools/test/proto-tools.test.ts:769`,
  `EventRouting<TaskListId>` assigned to `EventRouting<string>` at
  `packages/server/test/repository/repository-routing.test.ts:9452`, and an
  unsupported `taskListId` property on a `Task` fixture at line `10589`.
- The complete deterministic correction batch returns to the existing
  `implementer`, explicitly dispatched as `gpt-5.6-terra` / medium. Ownership
  is limited to those two test files, their directly affected tests, and this
  record; production behavior is unchanged. The implementer must not spawn
  subagents or revert other work. Desktop runtime telemetry remains
  unavailable, so the immutable configured role/profile is the acceptance
  evidence unless a visible mismatch is reported.

## 2026-08-14 - Tooling typecheck fixture correction

- Reproduced the RED batch with `pnpm typecheck:tooling`: duplicate
  `./generated/*.js` fixture export; `AlternateCatchUpProjection` retained
  scalar `string` identity though its generated `TaskList` state has message
  `TaskListId`; and the local Todo `Task` fixture omitted Proto `taskListId`.
- Removed only the duplicate key, changed the projection fixture generic to
  `TaskListId`, and added optional message-valued `taskListId` to the local
  structural `Task` fixture. No production behavior changed.
- GREEN: `pnpm typecheck:tooling` passed; Proto Tools passed `109/109`
  (`/tmp/t0186-tooling-proto-tools.json`); repository routing passed `244/244`
  (`/tmp/t0186-tooling-routing.json`). The existing `implementer` is explicitly
  configured `gpt-5.6-terra` / medium; Desktop exposes no independent runtime
  telemetry, so immutable configured role/profile is the acceptance evidence.

## 2026-08-14 - Proto Tools typed-fixture lint assignment

- After the tooling fixture correction passed independent typecheck and
  `353/353` focused tests, the complete generated-gates preflight restarted.
  Both generated and tooling typechecks passed; full ESLint then reported ten
  `@typescript-eslint/no-unsafe-*` errors at lines `1800` through `1839` of
  `packages/proto-tools/test/proto-tools.test.ts`. They are confined to the new
  generation-ID JSON assertions.
- This complete deterministic lint batch returns to the same existing
  `implementer`, retaining its explicit `gpt-5.6-terra` / medium profile.
  Ownership is limited to typed test parsing/assertions in that file, its
  focused suite, and T-0186 records; production behavior remains unchanged.
  Desktop runtime telemetry is unavailable, so the immutable configured
  role/profile remains the acceptance evidence.

## 2026-08-14 - Proto Tools typed-fixture lint correction

- The generation-ID tests parsed manifest JSON directly, so ESLint correctly
  treated their member reads and downstream values as `any`. The established
  fixture convention is a narrow typed `JSON.parse()` assertion at the file
  boundary; applying `{ generationId: string }` to these three reads preserves
  the existing behavior assertions without production changes.
- GREEN evidence: exact-file ESLint passed; `pnpm typecheck:tooling` passed;
  full Proto Tools passed `109/109` (`/tmp/t0186-proto-tools-typed-lint.json`).
  The owner remains the existing `implementer`, explicitly configured
  `gpt-5.6-terra` / medium. Desktop exposes no independent runtime telemetry,
  so the immutable configured role/profile remains the acceptance evidence.

## 2026-08-14 - Complete pre-review mechanical convergence

- Independent acceptance after the final fixture correction passed exact-file
  ESLint, tooling typecheck, and Proto Tools `109/109`. The complete
  `pnpm verify:generated-gates` restart then passed generated/tooling
  typechecks, full ESLint, cleanup, TSDoc, copyright, repository formatting,
  API inventory, audience, snippets, generated Proto lint/current-output,
  logging containment, and release readiness.
- Exact affected suites are green: Core `57/57`; Proto Tools `109/109`;
  workflow `78/78`; normalizer `6/6`; generated-clean `10/10`; cleanup
  `109/109`; repository routing `244/244`; five routing/runtime `270/270`;
  Todo short contracts `17/17`; Todo black-box `41/41`; Todo local
  multi-process `19/19`. The multi-process log is retained at
  `/tmp/t0186-preflight-todo-local-multi-process.log`.
- Two independent canonical generations retained byte-identical IDs and
  manifest/marker hashes for all five models. Root ID remains
  `32aa4024-1052-4da2-ae47-f2ec6b8b431d`; current-output passed and the
  stage/backup/claim/journal/lock scan was empty.
- Fresh serial V8 coverage passed seven files and `380/380` tests. Intersecting
  `/tmp/t0186-changed-source-coverage-pre-review/lcov.info` with added
  executable points from `origin/main` through current HEAD gives **155/159
  lines (97.48%)** and **126/136 branches (92.65%)** across the nine changed
  production sources. Remaining misses are bounded CLI/defensive fallbacks.
- The one-time audit found exactly `169` generated-family files and zero
  notice/provenance/copyright/absolute-path violations. Active
  `TaskReassignmentEvent`, `routeSemantic()`, and `@Route` are absent. Every
  Cloud Run/multiple-Gateway result is an explicit unsupported/out-of-scope
  statement. `git diff --check` and the lightweight status/API/overclaim scan
  are clean.
- Status is correction complete; targeted specialist re-review ready. The four
  concern-specific assignments and explicit immutable profiles are recorded in
  the review log. No specialist, final security, or release profile has run in
  this convergence pass.

## 2026-08-14 - Complete specialist wave accepted

- The complete concern-specific wave returned before correction began.
  TypeScript/API (`gpt-5.6-terra` / high) reported two P2s; style/
  maintainability (`gpt-5.6-terra` / high) reported one P2; performance/
  reliability (`gpt-5.6-terra` / high) reported four P1s; documentation
  (`gpt-5.6-luna` / medium) reported one P2. Every dispatch field was explicit,
  no reviewer reported a visible mismatch, and Desktop runtime telemetry is
  unavailable.
- The style marker-policy P2 duplicates the first reliability P1. One accepted
  correction batch contains seven independent concerns: canonical marker-aware
  reuse; bounded symlink-rejecting direct tree comparison; bounded claim-aware
  reader retry; retained stage evidence on rollback failure; internalize the
  marker filename export; correct generation-ID factory TSDoc; and remove the
  30-second replay-retention contradiction.
- All four P1 and all three independent P2 findings are accepted; no P0 was
  reported. The existing `implementer`, retaining its explicit
  `gpt-5.6-terra` / medium profile, owns the complete batch, behavior-first
  regressions, focused checks, and durable correction evidence. It must not
  spawn subagents or revert other work. Final security and release verification
  remain unstarted.

## 2026-08-14 - Remaining correction handoff

- The original correction context pushed partial checkpoints through
  `c1107a1d`: API/docs/internal marker cleanup, marker-aware bounded direct
  traversal, retained recovery stages, one canonical reuse module, and bounded
  claim-aware reader retry. It then reported that it could not complete the
  remaining adversarial/interleaving/recovery tests in that context. This is
  context exhaustion, not a project blocker; no accepted finding is waived.
- Remaining ownership is reassigned to a fresh existing `implementer`, with
  explicit `gpt-5.6-terra` / medium dispatch fields and subagent spawning
  prohibited. Its bounded scope is: canonical reuse marker/bounds/symlink
  parity tests and any directly exposed fix; strict reader no-claim,
  interleaving-success, and exhaustion tests; wrapper aggregate-failure retained
  journal/backup/stage plus later-recovery test; full affected suites/static
  gates; and durable correction evidence. It must preserve every pushed
  checkpoint and unrelated/user work. Desktop runtime telemetry remains
  unavailable, so the immutable configured profile is the acceptance evidence.

## 2026-08-14 - Post-correction coverage assignment

- Independent correction acceptance passed Proto Tools `126/126`, workflow
  `88/88`, tooling typecheck, affected ESLint, current-output, diff hygiene,
  and zero residue. The refreshed serial coverage profile passed `407/407`.
- Exact changed-source lines pass at **180/186 (96.77%)**. Branches are RED by
  one covered point at **139/155 (89.68%)** across the expanded eleven-source
  production set. Exact misses are retained in
  `/tmp/t0186-changed-source-coverage-correction/lcov.info`; the new reader,
  direct reuse fallback, and wrapper recovery paths contain meaningful public
  behavior candidates.
- The current existing `implementer`, explicitly configured
  `gpt-5.6-terra` / medium, owns one minimal behavior-first coverage correction
  in affected tests only unless the regression exposes a real production
  defect. It must not spawn subagents or revert other work. Desktop runtime
  telemetry remains unavailable; immutable configured profile is the
  assignment evidence.

## 2026-08-14 - Post-correction coverage closure

- Added one test-only public fail-closed behavior regression: a dead
  generation claim must not be treated as an in-progress coherent commit, so
  malformed manifest state fails immediately. The focused regression passed
  `1/1`; full Proto Tools coverage rerun passed `127/127`.
- The focused V8 profile at
  `/tmp/t0186-dead-claim-coverage-20260814/lcov.info` records the formerly
  unhit `index.ts:55` liveness outcome. Unioning that non-overlapping covered
  changed point with the retained serial `407/407` correction profile changes
  the exact diff-scoped result to **181/186 executable lines (97.31%)** and
  **140/155 branches (90.32%)**. Both required changed-source thresholds now
  pass. No production behavior changed and no specialist lane reopens.
- The existing `implementer` remains explicitly configured `gpt-5.6-terra` /
  medium; Desktop exposes no independent runtime-profile telemetry, so the
  immutable configured role/profile remains the assignment evidence.

## 2026-08-14 - Accepted correction batch complete

- The accepted specialist batch is fully corrected through `660d35bf`.
  Checkpoints internalized the marker API/docs policy, made direct reuse
  marker-aware and traversal bounded, retained recovery-owned stages, shared
  one reuse policy, added bounded claim-aware reader retry, completed
  adversarial marker/bounds/symlink and aggregate recovery cases, contained the
  packaged runtime helper, and closed stale-claim coverage.
- The original implementation context became unavailable after partial pushed
  checkpoints. A fresh existing `implementer`, explicitly configured
  `gpt-5.6-terra` / medium, completed the recorded handoff without broadening
  production scope. Desktop exposes no independent runtime telemetry; no
  visible mismatch was reported.
- Independent GREEN: tooling typecheck; affected ESLint; Proto Tools `127/127`;
  workflow `88/88`; current-output; Prettier/diff; zero stage/backup/claim/
  journal/lock residue. The independent Proto Tools LCOV at
  `/tmp/t0186-proto-tools-coverage-independent/lcov.info`, unioned per source
  with `/tmp/t0186-changed-source-coverage-correction/lcov.info`, gives exact
  changed coverage **181/186 lines (97.31%)** and **140/155 branches (90.32%)**.
- Status is specialist corrections complete; targeted re-review ready. All four
  substantively affected lanes reopen; final security and release verification
  remain unstarted.

## 2026-08-14 - Targeted re-review residual batch

- TypeScript/API and documentation targeted re-reviews are CLEAN under their
  recorded Terra/high and Luna/medium profiles. Style (Terra/high) reported one
  residual P2 dead root wrapper. Reliability (Terra/high) reported P1 direct
  staged symlink publication, P1 Message Board registry stage loss during
  retained recovery, and P2 unbounded per-attempt claim scanning. No reviewer
  reported a visible profile mismatch; Desktop telemetry is unavailable.
- All four residuals are accepted. The existing fresh `implementer`, explicitly
  configured `gpt-5.6-terra` / medium, owns one batch: delete unused wrappers;
  reject unsafe direct staged trees before publication and preserve prior
  output; preserve registry stages while a journal remains and clean them after
  later recovery; cap claim scan entries and fail closed on overflow. It must
  use behavior-first tests, must not spawn subagents, and must preserve all
  pushed/unrelated/user work.

## 2026-08-14 - Residual cleanup regression assignment

- Independent post-residual serial coverage was RED at `409/411`: both
  generated-clean stale-output cases left `.generated-*` stages, contradicting
  ordinary failure cleanup. This is an exposed production regression in the
  new Message Board recovery-stage retention boundary, not a coverage-only
  issue.
- The existing fresh `implementer`, explicitly configured
  `gpt-5.6-terra` / medium, owns root-cause diagnosis and one minimal correction
  distinguishing journal-owned failed-recovery evidence from ordinary
  generated-clean failure cleanup. It owns the generated-clean/workflow tests
  and T-0186 evidence only, must not spawn subagents, and must preserve all
  pushed/unrelated/user work. Desktop runtime telemetry remains unavailable.

## 2026-08-14 - Final residual coverage assignment

- After cleanup correction `dcc7ee55`, the fresh full serial profile passed
  `411/411`. Exact changed lines pass at **193/197 (97.97%)**; branches are RED
  by one covered point at **152/169 (89.94%)** because the residual claim/
  recovery logic expanded the denominator.
- The existing fresh `implementer`, explicitly configured
  `gpt-5.6-terra` / medium, owns one meaningful public-path test-only coverage
  correction unless it exposes a production defect. Candidate unhit behavior
  includes public manifest reads when the package directory cannot be scanned
  and successful Message Board registry file-stage cleanup. Synthetic private
  branch probing is prohibited. Desktop telemetry remains unavailable.

## 2026-08-14 - Final residual coverage closure

- Added one public fail-closed manifest-read behavior test: malformed
  publication state still fails when its package directory cannot be scanned
  for a generation claim. The focused regression passed `1/1`; the full Proto
  Tools V8 profile passed `131/131` and is retained at
  `/tmp/t0186-unscannable-coverage/lcov.info`.
- The profile covers the former claim-scan catch-return-false outcome. Unioned
  with the retained fresh serial `411/411` LCOV, the exact
  `origin/main...HEAD` changed-source intersection is **194/197 executable
  lines (98.48%)** and **153/169 branches (90.53%)**. Both thresholds pass.
- This is test-only: no production defect was exposed. The existing
  `implementer` remains explicitly configured `gpt-5.6-terra` / medium;
  Desktop runtime telemetry remains unavailable.

## 2026-08-14 - Post-review correction coverage assignment

- Checkpoint `73964b2a` changes the manifest-reader claim boundary and adds the
  final wrapper recovery proof, so the exact multi-suite changed-source profile
  must be refreshed before re-review acceptance.
- Mechanical verification is dispatched as an orchestrator function with
  explicit `gpt-5.6-luna` / low configuration. It owns no files and must run
  the six extant affected suites serially with one worker, then intersect fresh
  LCOV executable line and branch points with added production ranges in
  `origin/main...HEAD`. The earlier historical “seven-file” label named a
  nonexistent standalone artifact-writer test; normal workflow tests exercise
  that writer and fresh LCOV must contain every changed production source.
  Desktop exposes no independent runtime telemetry; the immutable configured
  profile is the assignment evidence.

## 2026-08-14 - Post-review correction coverage result

- The first inclusive measurement omitted the canonical `.mjs` reuse policy
  and was rejected. The corrected explicit-source profile passed all six
  extant affected test files and `404/404` tests with one worker; all ten
  changed production sources have LCOV records at
  `/tmp/t0186-final-changed-coverage-20260814-exact/lcov.info`.
- Exact `origin/main...HEAD` changed executable coverage is **201/204 lines
  (98.53%)** and **159/175 distinct branches (90.86%)**. Both thresholds pass.
  The three missed lines are bounded defensive/CLI paths in the generator,
  generated-clean, and workflow sources. The historical “seven-file” wording
  is superseded by this complete six-suite evidence.

## 2026-08-14 - Targeted re-review residual correction

- Removed the unused root generation-reuse wrappers; the canonical policy is
  now reachable only through the Proto Tools generation source.
- Direct staged-symlink behavior was tested first and was already GREEN: the
  existing generated-tree normalization rejects the symlink before publication
  and preserves the prior output and manifest, so no speculative production
  change was made for that accepted concern.
- TDD RED/GREEN: manifest-reader claim scans accepted 1,001 regular claim
  files and returned a malformed-manifest error. The reader now caps each scan
  at 1,000 claims and fails closed on overflow; boundary and overflow tests
  pass.
- Message Board composition-only stages now clean immediately after their
  staged registry file is copied, while the journal-referenced file-stage root
  is retained whenever recovery retains a publication journal. Later recovery
  removes that journal-owned stage normally.
- Focused evidence passed `5/5`; full Proto Tools passed `130/130`; workflow
  passed `88/88` in 32.82 seconds. The existing `implementer` remains explicit
  `gpt-5.6-terra` / medium; Desktop runtime telemetry remains unavailable.

## 2026-08-14 - Generated-clean retention correction

- Independent serial coverage exposed two stale-output cleanup failures in
  `scripts/check-generated-clean.test.mjs`. Root cause: Message Board's
  composition-only stage root now cleans immediately after copying the staged
  file, but generated-clean's finally block still attempted to remove that
  removed property. That exception stopped cleanup before the journal-unowned
  file-stage root could be removed.
- Removed only the obsolete composition-root cleanup from generated-clean. Its
  finally block continues to remove the file-stage root for ordinary stale
  output; workflow retains that root only while an actual publication journal
  owns it after failed recovery.
- GREEN: generated-clean `10/10`; Proto Tools `130/130`; workflow `88/88` in
  33.86 seconds; tooling typecheck, affected ESLint, current-output, Prettier,
  and diff checks passed. The existing `implementer` remains explicitly
  `gpt-5.6-terra` / medium; Desktop runtime telemetry is unavailable.

## 2026-08-14 - Specialist API and documentation correction checkpoint

- Internalized the generated-root marker filename, removed its accidental root
  package API inventory entry, and clarified `ProtoManifest.create()` that an
  omitted generation ID is freshly random for a new marker lifecycle.
- Replaced the stale API README claim tying exact-row replay to a 30-second
  retention window with the Inbox delivery lifecycle. Tooling typecheck, API
  inventory, audience check, formatting, and diff check passed. The remaining
  accepted reliability findings are still in progress.

## 2026-08-14 - Direct generation marker and traversal checkpoint

- Direct generation now writes its staged marker before reuse comparison and
  requires valid nonempty live/staged marker IDs to agree with their manifests.
  Its generated-tree walk now rejects symlinks and bounds depth and entries.
- Focused generation-ID and bounded-generation tests passed `3/3`; tooling
  typecheck passed. Claim-aware strict reader retry and retained workflow stage
  evidence remain in progress as separate reliability slices.

## 2026-08-14 - Workflow recovery-stage retention checkpoint

- The wrapper cleanup previously removed every staged target even when
  `recoverPublication()` failed and retained the journal as recovery authority.
  It now preserves journal-owned stages until a later recovery removes the
  journal.
- Focused workflow rollback/recovery cases passed `5/5`. Dedicated
  primary-plus-recovery aggregate and later-recovery behavior coverage remains
  to be added with the reader/canonical-policy completion slice.

## 2026-08-14 - Canonical generation reuse policy checkpoint

- Direct generator and workflow root/model staging now call one internal
  runtime-neutral policy for marker, manifest, and bounded generated-tree
  comparison. Existing focused direct generation-ID cases passed `3/3` and
  workflow root/model reuse cases passed `2/2`; tooling typecheck passed.
- Full adversarial marker/tree parity and claim-aware reader retry coverage
  remain in progress.

## 2026-08-14 - Remaining reliability correction closure in progress

- The continuing existing `implementer` is explicitly configured
  `gpt-5.6-terra` / medium. Desktop exposes no independent runtime-profile
  telemetry; the immutable configured role/profile is the assignment evidence.
- Added direct and workflow normal-transaction parity coverage for missing,
  malformed, empty, and mismatched live/staged markers; exact depth `64/65`
  and entry `1,000/1,001` bounds; and live/staged symlink handling. The direct
  and focused workflow cases passed `16/16` and `10/10` respectively.
- TDD RED: a live writer that began with malformed state could not complete a
  coherent marker/manifest commit because `ProtoManifest.read()` performed its
  three retries without yielding. The reader now waits a bounded 25 ms only
  between failed attempts while a verified live generation claim exists. The
  no-claim immediate failure, completed-only interleaving read, and bounded
  three-attempt exhaustion regressions are GREEN.
- Added wrapper-level primary-plus-recovery failure behavior. It proves the
  thrown `AggregateError` retains both failures and preserves the journal,
  backup, and stage until a later successful recovery restores the prior tree
  and removes all recovery evidence.
- Full Proto Tools passed `126/126`. The first full workflow run then exposed
  a bootstrap-only module-resolution defect in the shared policy: cache-built
  Proto Tools resolved the root-relative policy import below a nonexistent
  `packages/proto-tools/scripts/` path. The canonical policy now lives beside
  generator sources, bootstrap copies that one module into its cache output,
  and package prepack copies it beside compiled output. The clean-checkout
  bootstrap test is RED until this checkpoint is committed because it creates
  a detached worktree from `HEAD`; it is the next verification after push.
- The first bootstrap-fix checkout run then exposed TypeScript's missing
  declaration lookup for the canonical `.mjs` module. Added its exact `.d.mts`
  contract and made the bootstrap copy conditional only for deliberately
  minimal non-executing fixture roots. Local tooling typecheck is GREEN; the
  detached-checkout bootstrap test again requires this checkpoint to be pushed.
- Confirmed the committed-head workflow suite is GREEN at `88/88` after a
  32.50-second run (non-interactive 30-second collection windows had cut off
  its summary, so no incomplete output was accepted as evidence). The full
  affected static/current-output gate is GREEN: `pnpm typecheck:tooling`,
  affected ESLint, `pnpm proto:check-generated:current`, Prettier, and
  `git diff --check`. The residue scan found zero stage, backup, claim,
  publication-journal, or workflow-lock entries outside ignored dependencies.
- Affected ESLint found only dead pre-extraction workflow helpers, an
  array-style annotation, and three test syntax violations. Removed or
  normalized those mechanical items without changing the tested behavior.
- Final affected reruns are GREEN: Proto Tools `126/126` in 24.70 seconds and
  workflow `88/88` in 32.37 seconds. The latter was collected through an
  interactive session because the execution surface caps a single ordinary
  collection window at 30 seconds; its final exit code was `0`.

## 2026-08-14 - Final claim-reader and MessageBoard recovery correction

- Existing `implementer` assignment: explicit `gpt-5.6-terra` / medium.
  Desktop provides no independent runtime-profile telemetry; the configured
  profile and absence of a visible mismatch are recorded as the available
  model evidence.
- TDD RED: the `1,001` non-regular lock-candidate reader regression returned
  the malformed-manifest error after inspecting its first symlink. The defect
  was that the cap was applied as candidates were validated. The scan now
  collects and bounds every lock-named entry first, then opens each candidate
  `O_RDONLY | O_NOFOLLOW | O_NONBLOCK`, fstats the descriptor as regular,
  reads via that descriptor, and closes it in `finally`.
- GREEN: regular and symlinked `1,000/1,001` public reader boundaries pass;
  Proto Tools `133/133`; wrapper-level MessageBoard primary plus recovery
  failure/later recovery passes and proves the registry file stage is retained
  only while journal-owned; workflow `89/89`; generated-clean `10/10`.
- Tooling typecheck, affected ESLint, Prettier/diff, current-output, cleanup,
  and residue checks are green. The targeted style/reliability correction is
  ready for re-review; security and release verification remain pending.

## 2026-08-14 - Final security correction checkpoint

- Existing `implementer` assignment: explicit `gpt-5.6-terra` / medium;
  Desktop provides no independent runtime-profile telemetry.
- TDD RED/GREEN: descriptor membership invoked an attacker-controlled array
  iterator before applying its bounds. It now traverses only bounded indexed
  own data properties; a genuine descriptor with a throwing iterator remains
  accepted. A direct staged FIFO also previously published; generated-tree
  collection now rejects every entry that is neither a directory nor a regular
  file, preserving the prior live output and manifest.
- Focused Core and Proto Tools regressions are green. Claim ownership/release,
  marker descriptor reads, and workflow non-regular-tree coverage remain in
  progress under the accepted final security batch.
