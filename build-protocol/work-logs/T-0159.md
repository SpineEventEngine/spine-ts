# T-0159 Work Log

## 2026-08-11 - Startup

- T-0158 completed, merged to `origin/main` at `3168135d`, passed post-merge
  generated build and 5 focused files / 234 tests, and its task branches and
  worktrees were removed.
- T-0159 starts from clean `origin/main@3168135d` in its dedicated worktree.
- Classification: high-risk public/multicast routing and durable replay change.
- Implementation assignment: existing implementer, explicit
  `gpt-5.6-terra` / medium, no subagents. Runtime metadata is recorded if the
  surface exposes it; otherwise the immutable configured role/profile is
  evidence.
- Frozen assumptions: producer ID is required; compatible malformed producer
  fails; valid incompatible producer falls back to the declaration-first Event
  field; zero targets is valid; results are bounded, validated atomically,
  stable-deduplicated, copied, and frozen; replay consumes the stored typed
  target without rerouting.

Status: implementation starting.

## 2026-08-11 - Public Declarations Checkpoint

- RED first observed the absent root `EventRouting` export after generated
  package outputs were restored in the fresh worktree.
- GREEN adds factory-only `EventRouting.create()`, exact and canonical semantic
  declarations, replacement default, and a copy-safe immutable construction
  snapshot. `EventRoute` returns `readonly Id[]` and documents deterministic,
  side-effect-free once-per-admission behavior with no replay invocation.
- Focused declaration tests pass 1 file / 2 tests and the generated build
  passes. Repository construction and runtime selection remain next.

## 2026-08-11 - Repository Selection Checkpoint

- `RepositoryOptions.eventRouting` is typed by the repository Entity ID and is
  snapshotted at construction.
- Exact schema > direct descriptor `(is)` > file descriptor `(every_is)` >
  replacement/default selection is resolved at construction. Unregistered
  exact or semantic declarations fail there; caller compatibility tags remain
  outside the lookup.
- Custom Event results accept zero through 1,000 IDs, validate the complete raw
  array before returning a route, copy IDs, stable-deduplicate them, and freeze
  the plan. Focused declaration/selection tests pass 2 files / 8 selected tests.
- Default producer routing and one-plan Inbox handoff/replay remain next.

## 2026-08-11 - Default Event Route Checkpoint

- Default routing now requires a producer ID. A producer whose canonical typed
  identity matches the repository ID type is decoded, validated, and used
  without comparing it to the Event declaration-first field.
- A producer of a different valid type falls back to the declaration-first
  Event field. A producer that claims the compatible type but cannot be decoded
  fails closed instead of falling back.
- Removed the former producer/first-field equality requirement and the
  projection-only first-field special case. String and message-valued target
  tests cover compatible precedence, incompatible fallback, absent producer,
  and malformed compatible producer behavior.
- The complete repository-routing suite passes 1 file / 207 tests and the
  generated build passes. One-plan Inbox handoff and replay remain next.

## 2026-08-11 - Admission And Replay Checkpoint

- EventBus now retains one dispatcher-local Event snapshot from successful
  acceptance through dispatch. Repository dispatchers associate the immutable
  route plan with that snapshot, so custom routing is not called again after
  Event Store append.
- Aggregate, Process Manager, and Projection execution accept the captured
  route plan. Public custom routing tests cover zero-target suppression,
  one-target PM/Projection work, and multi-target Aggregate/PM work.
- Process Manager and Projection replay decode and validate the persisted typed
  target ID and target repository type, rebuild a one-target plan from the
  stored Event schema, and never call application routing. Persisted route
  selection is trusted; payload, tenant, target type, and typed ID compatibility
  remain fail-closed.
- Canonical zero-valued `int32` and `int64` producer IDs route successfully.
  Generated build and focused EventBus/EventRouting/repository suites pass 3
  files / 258 tests.

## 2026-08-11 - Server Fixture Convergence

- The first full server run passed 1,683 tests and exposed eight stale
  expectations: one root export inventory, two descriptor semantic-tag
  expectations, and five Events without the now-required producer identity.
- Updated only those inventories and valid Event fixtures. Producer values use
  canonical StringValue identity and preserve each fixture's intended target.
  The cross-process transport fixture now proves producer metadata survives the
  wire path.
- The five formerly failing files pass 154 / 154 tests. The full server rerun
  and changed-source coverage remain next.

## 2026-08-11 - Coverage Convergence

- The full server suite passes 71 files / 1,691 tests.
- Removed unreachable optional route-plan and empty replay-plan branches from
  private execution code. Added real behavior tests for duplicate semantic
  declarations, a non-array route result, and malformed compatible message-ID
  producer bytes.
- Fresh focused coverage passes 3 files / 259 tests. Exact changed-production
  metrics from the V8 map and `origin/main` changed lines are 157 / 161
  statements (97.52%), 55 / 59 branches (93.22%), 31 / 31 functions (100%),
  and 146 / 150 lines (97.33%).

Status: implementation mechanically converged; cheap preflight and one review
wave remain.

## 2026-08-11 - Cheap Preflight

- Generated build, tooling typecheck, repository-wide ESLint, cleanup rules,
  TSDoc, logging containment, API docs, formatting, documentation audience,
  release-readiness links, and `git diff --check` pass.
- The compatibility scan finds no routing from caller `semanticTags`, retired
  producer/first-field equality, or public constructor use. Its sole
  `new EventRouting` match is the class-private `create()` factory.

Status: review-ready. One complete specialist wave is next.

## 2026-08-11 - Review Correction

- One complete specialist wave accepted four related corrections: stale public
  default-route TSDoc; incomplete custom-result/EventBus acceptance wording;
  and JSON-based deduplication failing for valid message IDs with `bigint`
  fields. Security remains N/A because no trust boundary changed.
- Event and Entity ID keys now use descriptor-aware packed `Any` bytes. This
  keeps scalar and message identities type-distinct and handles `int64` message
  fields without JSON serialization. Invalid runtime ID shapes fail at the
  descriptor boundary instead of receiving an unrelated fallback key.
- A generated int64-message-ID Projection test proves stable duplicate removal,
  real EventBus admission/handoff, delivered-row replay, and no application
  routing call during replay.
- Generated build and the full server suite pass 71 files / 1,693 tests.
  Focused coverage passes 4 files / 275 tests. Exact changed-production LCOV
  from `/tmp/t0159-correction-cov/lcov.info` is 136 / 140 statements and lines
  (97.14%), 81 / 86 branches (94.19%), and 33 / 33 functions (100%).
- Tooling typecheck, TSDoc, API docs, exact changed-source ESLint, Prettier, and
  `git diff --check` pass.

Status: correction-complete; targeted style, API, documentation, and
performance/reliability re-review is next.

## 2026-08-11 - Targeted Re-review

- Documentation/TSDoc, TypeScript/API documentation, style/maintainability,
  and performance/reliability all returned CLEAN on the correction range.
- The reliability reviewer independently reran the 4 correction-focused files
  and observed 275 / 275 tests passing.

Status: accepted; one final `verify:task` remains before merge.

## 2026-08-11 - Final Verification

- The one final `verify:task --no-coverage` completed successfully. It passed
  Proto generation/lint/current-output checks, generated build, tooling,
  repository ESLint, cleanup, TSDoc, logging containment, formatting,
  documentation audience/API checks, release readiness, and 9 focused files /
  429 tests.
- Changed-range coverage remains the fresh 97.14% statements/lines, 94.19%
  branches, and 100% functions correction measurement.

Status: complete and merge-ready.
