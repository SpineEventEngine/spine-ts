# T-0158 Work Log

## 2026-08-11 - Startup

- Created the isolated task worktree from `origin/main@e3bd0dfa` and installed
  frozen workspace dependencies.
- Frozen scope comes from the approved Wave 9 plan: public Command routing,
  precedence, repository injection/snapshot, declaration-first default,
  validated single target, and replay-safe target reuse.
- Implementation assignment: existing implementer, explicit
  `gpt-5.6-terra` / medium, no subagents. Runtime metadata is recorded when the
  surface exposes it; otherwise the immutable configured profile is evidence.
- Verification profile: bounded `verify:task` after one converged specialist
  review wave.

## 2026-08-11 - Demonstrated Durable-Target Blocker

- Command routing and persisted-target replay expose a pre-existing serialized
  mismatch: JVM `InboxId.entity_id` is a typed `spine.client.EntityId` wrapping
  `Any`, while the TS delivery model stores `targetId: string` and persists it
  as `google.protobuf.StringValue` for every repository ID type.
- The existing TS form cannot reversibly preserve supported int32, int64, or
  general message-valued IDs. No canonical field stringifier exists until the
  later T-0162 slice, and inventing a private tagged string is prohibited.
- Architecture assignment: existing requirements splitter, explicit
  `gpt-5.6-sol` / high, no subagents. It owns only the bounded decision on the
  smallest JVM-compatible durable target representation and dependency/task
  boundary. Runtime metadata is recorded if exposed; otherwise the configured
  immutable role/profile is evidence.
- Production implementation is paused at the pushed routing checkpoints until
  that correction boundary is frozen.

## 2026-08-11 - Public Builder Checkpoint

- Implementer `t0158_command_routing`: configured `gpt-5.6-terra` / medium;
  runtime metadata is unavailable on this surface.
- Focused RED established that the public `CommandRouting` export was absent.
  GREEN adds the frozen API shape's `create()`, exact route, semantic route,
  and default replacement declarations with duplicate and malformed
  registration rejection.
- `pnpm typecheck:build:generated` then
  `pnpm exec vitest run packages/server/test/repository/command-routing.test.ts`
  passed. Repository selection, snapshot, dispatch, replay,
  and descriptor semantic precedence remain in progress.

## 2026-08-11 - Routing Integration Checkpoints

- Implementer `t0158_command_routing`: configured `gpt-5.6-terra` / medium;
  runtime metadata remains unavailable on this surface.
- `4ff3e746` adds a copy-safe immutable route-declaration snapshot; a focused
  RED observed the absent internal snapshot surface, then GREEN verified that
  later builder mutation does not alter its captured declarations.
- `9d84ecd7` adds the typed `RepositoryOptions.commandRouting` injection and
  routes exact-schema commands through the callback once before existing ID
  validation. `d7beb918` adds construction-time descriptor provenance lookup
  and exact > `(is)` > `(every_is)` > default selection, rejecting route
  declarations that do not match a registered command schema.
- `2098bb7a` changes Entity Inbox command replay to construct the accepted
  target from its persisted Inbox row rather than re-running application
  routing. A legacy mismatch assertion was deliberately updated: persisted
  target IDs are trusted by the frozen task contract, while target type remains
  validated.
- Focused evidence after the latest checkpoint:
  `pnpm exec vitest run packages/server/test/repository/repository-routing.test.ts packages/server/test/repository/command-routing.test.ts`
  passed 2 files / 192 tests; `pnpm typecheck:build:generated` passed.
- Remaining implementation evidence includes true one-call dispatch/replay,
  aggregate and process-manager custom routing, default descriptor constraints,
  malformed/ambiguous construction, and full mechanical pre-review gates.
