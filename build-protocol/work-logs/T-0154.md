# T-0154 Work Log

## 2026-08-10 - Framing

- Started from `origin/main@e30c2aa4` in
  `.worktrees/T-0154-loglayer-foundation` on
  `task/T-0154-loglayer-foundation`.
- Classified the task as high-risk because it changes shared dependencies,
  public server-side options, default logging behavior, secret handling, and a
  shared build checker.
- Confirmed the Desktop surface supports the required explicit child role,
  model, and reasoning dispatch.
- Read the governing protocol, completion plan, approved Wave 9 plan, T-0153
  task, and T-0153 work log. The dependency-ordered plan has no unresolved
  product decision for this slice.
- Recorded the skill applicability result in the task file. The selected
  implementation, TDD, subagent, worktree, review, and verification skills are
  fully read; the prohibited `.superpowers` ledger is not used.
- Inspected the existing `ServerEnvironmentSettings.warn` seam, the exact
  independent option declarations, package manifests, server exports, and
  environment attachment paths.
- Verified against the npm registry that current stable `loglayer` is `9.4.0`.
  An attempted invented `@loglayer/transport-structured` package returned 404;
  the approved `StructuredTransport` comes from LogLayer itself.
- No production or test file has been edited. The next action is the explicit
  Terra Medium implementation assignment and observed RED tests.

## Current Status

Task framing is complete. Implementation has not started.

## 2026-08-10 - JVM Server Guardrail

- Before any `@spine-event-engine/server` runtime/API change, inspected
  `spine-jvm-docs/spine-server-runtime-and-bounded-context.md` (environment
  assembly and lifecycle sections),
  `spine-jvm-docs/spine-validation-storage-observability-and-support.md`
  (the `ServerEnvironment` summary), and the local reference source
  `/private/tmp/spine-research/core-jvm/server/src/main/java/io/spine/server/ServerEnvironment.java`.
- The JVM model owns server facilities and closes only those facilities; it
  does not establish a framework-owned application logging lifecycle. T-0154
  therefore keeps the TypeScript logger caller-owned, snapshots a child at
  environment construction, and propagates that child explicitly without a
  singleton/facade/lifecycle wrapper.
- JVM source was inspected only as a guardrail; it was neither changed nor
  built. The next action is a focused RED test for logger injection and child
  ownership.

## 2026-08-10 - Foundation RED/GREEN Checkpoint

- Added the focused `ServerEnvironment` logger-injection and legacy-warning
  behavior tests before the corresponding runtime change. The observed RED was
  `expected "vi.fn()" to be called 1 times, but got 0 times` because the
  environment did not snapshot the supplied logger child.
- Added direct `loglayer@9.4.0` dependencies to every affected runtime package
  and regenerated `pnpm-lock.yaml`. This uses LogLayer's own
  `StructuredTransport`; no invented transport package or Spine facade was
  added.
- Implemented the minimal green foundation: `ServerEnvironmentSettings.logger`
  replaces `warn`, `ServerEnvironment` snapshots a single child immediately,
  existing environment warning emission uses that child, and no logger lifecycle
  is included in owned closeables. The no-logger path constructs the frozen
  structured-console configuration.
- Evidence: after `pnpm proto:generate` restored the ignored generated TypeScript
  Proto outputs required by this fresh worktree, `pnpm typecheck:build:generated`
  and `pnpm exec vitest run packages/server/test/server/server-environment.test.ts`
  passed (21/21). An earlier direct test run and typecheck were blocked by the
  absent generated outputs; this was resolved without building or modifying JVM.
- Next action: add the exact independent public logger option declarations and
  their focused type/API checks, then establish RED tests for private emission
  safety and the containment checker.

## 2026-08-10 - Public Option Contract Checkpoint

- Added direct `ILogLayer` option fields to `DynamicUnaryOptions`,
  `SubscriptionGatewayOptions`, `DeliveryServerOptions`,
  `ScheduledNodeDiscoveryOptions`, `GkeNodeDiscoveryOptions`,
  `GceNodeDiscoveryOptions`, and both `GceRegistrarOptions` variants. No frozen
  excluded option type was changed.
- `pnpm typecheck:build:generated` and `git diff --check` passed. These are
  declaration-only additions; the components do not yet emit operational logs.
- Next action remains private fault-contained emission and the AST containment
  manifest/checker harness, with their RED tests first.

## 2026-08-10 - Private Emission RED/GREEN Checkpoint

- Observed RED with `packages/server/test/server/server-log.test.ts`: the
  private module did not exist. The test specifies positive field allowlisting,
  UTF-8 oversize omission, and fault containment for both a synchronous throw
  and a promise-like rejection.
- Added the package-private `emitServerWarning` seam. It accepts only the frozen
  field names, validates operation/reason codes and count bounds, omits dynamic
  strings over 256 UTF-8 bytes, never accepts arbitrary objects, and catches or
  observes logger failures without recursive logging. The existing volatile
  registry path uses it with fixed constants.
- GREEN evidence: `pnpm typecheck:build:generated` followed by focused Vitest
  files `server-log.test.ts` and `server-environment.test.ts` passed 23/23.
- Next action: pass the environment child through the package-private attachment
  seam, then implement the containment checker/manifest fixtures.
