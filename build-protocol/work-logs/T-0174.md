# T-0174 Work Log

## 2026-08-12 - Startup

- Started from integrated `origin/main@bef11ad9`, which contains T-0171,
  T-0172, and T-0173.
- Scope is the two canonical reader references and T-0174 records. Shared
  snippet tooling is conditional ownership only if convergence requires it.
- Existing `implementer` assignment explicitly uses `gpt-5.6-terra` / medium;
  runtime metadata limitation will be recorded if the surface exposes none.

Status: implementation starting.

## 2026-08-12 - Canonical reference reconciliation

- Assignment acceptance: existing `implementer`, explicitly `gpt-5.6-terra` /
  medium. The execution surface exposes no runtime model self-introspection;
  the configured immutable profile is the available evidence, with no visible
  mismatch or fallback.
- This remains high-risk documentation because it states cross-package public,
  routing, persistence, delivery, and topology contracts. No runtime, public
  API, generated output, or shared snippet-checker policy change is required.
- Skill applicability: the session catalog and `implement` skill were checked;
  `implement` was read and used for the bounded documentation workflow. TDD is
  not selected because no runtime behavior or checker policy changes. No
  subagents are permitted or used.
- Both assigned documents are `changed`. The API page now provides canonical
  handoffs and a concise exact-routing contract; architecture now identifies
  its canonical role, links each detailed topic once, and separates preserved
  Proto wire definitions from TypeScript repository routing.
- Source evidence: `CommandRouting`, `EventRouting`, and
  `StateUpdateRouting` each expose exact `route(schema, via)` and
  `replaceDefault(via)` APIs. Routes are deterministic during admission and
  replay uses stored targets. No `@Route` or `routeSemantic()` API exists.

Status: focused validation pending.

## 2026-08-12 - Deterministic validation

- Passed generated build: `pnpm typecheck:build`; the Proto workflow verified
  47 source checksums and 52 frozen descriptor files against normalized digest
  `2a0db288787ac436c190ed86d72d78922deeb0fba4a64b2094b36b2525859db0`.
- Passed strict TypeScript snippets for both assigned paths, API inventory, and
  documentation audience checks. Shared snippet checker policy and path lists
  needed no change.
- Passed the canonical-target and retired-routing scan: both documents name
  exact `CommandRouting`, `EventRouting`, and `StateUpdateRouting` with
  `route(schema, via)` and `replaceDefault(via)`; no active Java semantic or
  retired TypeScript routing claim remains.
- Passed copyright, release readiness (82 package imports, 51 package assets,
  350 relative Markdown links), Prettier, and diff hygiene.
- Passed `pnpm verify:task -- --no-tests` once after convergence. It rebuilt
  generated TypeScript and tooling successfully. Runtime tests and coverage are
  N/A because this is a documentation-only change.

Status: implementation verified and ready for the orchestrator review wave.

## 2026-08-12 - Accepted review correction

- Accepted the complete review batch in the existing implementation context.
  The assignment remains existing `implementer`, explicitly `gpt-5.6-terra` /
  medium. Runtime self-introspection is still unavailable; configured profile
  remains the metadata limitation with no visible mismatch or fallback.
- Documentation/API P2: split both canonical handoff maps into one reference
  each for Node, browser, and React clients, and one each for common deployment,
  GCE operation, and GKE operation.
- Documentation/API P2: corrected `@Where`. One equality filter follows type
  routing on event- or rejection-consuming `@Subscribe`, `@React`, or
  `@Command`; `eventField` and `equals` are typed string literals and invalid or
  repeated declarations fail closed. It remains a filter, not routing.
- Source verification preceded the correction: the build-time analyzer permits
  `@Where` once on each listed event-consuming handler and rejects invalid
  inputs with `INVALID_WHERE`. No runtime behavior, test classification, or
  shared checker policy change is warranted.

Status: correction validation pending.

## 2026-08-12 - Correction validation

- Passed generated build and strict TypeScript snippets for both owned paths,
  plus API and audience checks.
- Passed canonical-handoff/retired-routing scan: each client and deployment
  subject has a distinct reference target; `@Where` carries its corrected
  event/rejection handler scope, literal inputs, one-filter limit, and
  fail-closed behavior; no Java semantic or retired TypeScript routing claim
  remains.
- Passed copyright, release readiness (82 package imports, 51 package assets,
  358 relative Markdown links), Prettier, and diff hygiene.
- Passed `pnpm verify:task -- --no-tests` once after convergence. It rebuilt
  generated TypeScript and tooling successfully. Runtime tests and coverage
  remain N/A for this documentation-only correction.

Status: accepted review batch corrected, verified, and pushed.

## 2026-08-12 - Final delivery handoff correction

- Split the final combined delivery handoff in both canonical maps: remote
  client/coordination points to `packages/delivery-client/REFERENCE.md`, while
  local in-memory server limits and lifecycle point to
  `packages/delivery-server/REFERENCE.md`.
- This is a narrow documentation correction only. The existing explicit
  implementation profile remains `gpt-5.6-terra` / medium; runtime metadata is
  still unavailable, with no mismatch or fallback exposed.

Status: final mapping correction implemented.

## 2026-08-12 - Final mapping validation

- Passed generated build, strict snippets for both canonical documents, API and
  audience checks, and the exact delivery mapping scan. Neither document keeps
  a combined local/remote delivery subject.
- Passed copyright, release readiness (82 package imports, 51 package assets,
  360 relative Markdown links), Prettier, and diff hygiene.

Status: final T-0174 mapping correction verified and pushed.

## 2026-08-12 - Review closure

- Documentation re-review is CLEAN after the final delivery-client/server
  canonical-target split.
- TypeScript/API documentation and performance/reliability are CLEAN.
- All task commits are pushed to `origin/task/T-0174-api-architecture-docs`.

Status: review complete; integration ready.
