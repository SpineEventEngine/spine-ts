# T-0146 Work Log

## Start

- Base: verified Wave 8 guide-correction main commit `9a35e84b`.
- Trigger: human correction that MySQL `_scope` and `_revision` are invented,
  Datastore and MySQL use different tenant isolation, and Bounded Context names
  must not partition domain records.
- Classification: high-risk persistence architecture analysis; planning only.
- Local JVM evidence sources are read-only and already dirty outside this task:
  - `/Users/armiol/development/Spine/jdbc-1.x` at `834c140a`;
  - `/Users/armiol/development/Spine/gcloud-1.x` at `634e46fb`;
  - `/Users/armiol/development/temp/tmp-core-java` at `0a4b2fbdeb`.
  Existing JVM modifications and untracked files will not be touched.

## Analysis Dispatch

- JDBC/core storage analysis: read-only `explorer`, explicitly
  `gpt-5.6-terra` / `high`; trace tenant-specific database/data-source routing,
  table identity, write conflict/atomicity behavior, and absence or presence of
  Bounded Context/revision columns. No subagents.
- Datastore analysis: read-only `explorer`, explicitly `gpt-5.6-terra` /
  `high`; trace single- and multitenant namespace selection, keys/kinds,
  queries/transactions, and absence or presence of Bounded Context/revision
  properties. No subagents.
- Spine TS impact inventory: read-only `explorer`, explicitly
  `gpt-5.6-terra` / `high`; enumerate production/tests/docs coupled to `_scope`,
  `_revision`, canonical context scope, tenant namespaces, table/kind identity,
  and CAS behavior. No subagents.
- The orchestrator performs cross-source synthesis and writes the correction
  plan. Runtime profile metadata will be recorded if exposed; otherwise the
  explicit configured profiles and metadata limitation are the evidence.

