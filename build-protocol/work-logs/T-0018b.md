# Work Log: T-0018b Runtime Metadata Adoption

Status: integrated on main

Task:
`build-protocol/tasks/T-0018b-runtime-metadata-adoption/TASK.md`

Review log:
`build-protocol/reviews/T-0018b-runtime-metadata-adoption.md`

Branch: `task/T-0018b-runtime-metadata-adoption`

Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0018b-runtime-metadata-adoption`

## Activity

- `2026-07-09`: User explicitly allowed native `corepack`, `pnpm install`,
  `pnpm --config.verify-deps-before-run=false verify`, and local IPC/loopback
  listener tests.
- `2026-07-09`: Requirements splitter
  `019f4796-6039-73d0-9745-fbaa61501725` selected this task as the next
  non-blocked implementation slice after T-0018a and was closed.
- `2026-07-09`: Created durable task, review, and work-log records before
  branch/worktree creation and implementation.
- `2026-07-09`: Committed the planning checkpoint on `main` as `9c3ba8a`
  before creating the implementation worktree.
- `2026-07-09`: Corrected the implementation base to `e62de1a` and created
  branch/worktree `task/T-0018b-runtime-metadata-adoption` at
  `.worktrees/T-0018b-runtime-metadata-adoption`. The first sandboxed
  `git worktree add` attempt failed because `.git/refs/...lock` could not be
  created under sandbox restrictions; the rerun with explicit escalation
  succeeded.
- `2026-07-09`: Implementation agent began focused adoption pass. Initial read
  found hand-rolled routine command metadata in the to-do command helpers,
  to-do public docs, and `@spine-ts/testing` fixture tests/docs; no handler
  envelope or transaction changes are needed.
- `2026-07-09`: Updated the to-do example command helpers and public command
  snippets to use `SignalMetadata` for command IDs and command/actor context.
  Query and subscription actor contexts in the same examples now use
  `SignalMetadata.actorContext()` as well.
- `2026-07-09`: Updated `@spine-ts/testing` fixture tests and README to show
  `SignalMetadata` for routine command metadata while keeping
  `packCommand()`/`packEvent()` as low-level envelope helpers.
- `2026-07-09`: Ran required review lanes. Code style/maintainability,
  documentation, TypeScript/API docs, security, and performance/reliability
  reviewers all returned clean results with no findings; each reviewer was
  closed after its result was captured.
- `2026-07-09`: Root verification found two process issues before final gates:
  sandboxed focused tests failed only on local loopback `EPERM` and passed with
  explicit loopback/native approval; running `typecheck:build` and
  `docs:check` in parallel collided in generated proto output, so write-heavy
  proto-generation checks must run sequentially.
- `2026-07-09`: Full verify initially failed in `eslint` because interrupted
  generated staging directories from the earlier proto collision
  (`packages/proto/.generated-QwUD1W` and `examples/todo/.generated-vlWYsX`)
  were left untracked and linted outside the TypeScript project. Root removed
  only those two untracked generated temp directories before rerunning verify.
- `2026-07-09`: Full verification passed after cleanup.
- `2026-07-09`: Committed task branch
  `task/T-0018b-runtime-metadata-adoption` as `9647963`
  (`Adopt runtime metadata in examples`) and merged it into `main` as
  `348ff95` (`Merge T-0018b runtime metadata adoption`).
- `2026-07-09`: Post-merge full verification passed on `main`.

## Decisions

- Choose `T-0018b` before deeper registry-discovery hardening because T-0018a
  explicitly left example/testing adoption as follow-up work, and this slice
  fixes public drift without reopening runtime design.
- Keep the task narrow: use the existing `SignalMetadata` seam in example and
  testing paths, and avoid adding a broad client DSL or new framework runtime
  surface unless a tiny helper is demonstrably clearer.
- Use direct `SignalMetadata` calls plus local short constants/helpers where
  they make call sites smaller. Do not add an exported helper because the
  adoption sites are examples/tests and do not need a new public surface.

## Verification

- `pnpm install`: first sandboxed attempt failed after npm registry
  `ENOTFOUND` retries with `TypeError: fetch failed`; rerun with network
  approval succeeded and installed 199 packages.
- `pnpm --config.verify-deps-before-run=false exec vitest run examples/todo/src/index.test.ts packages/testing/test/index.test.ts packages/server/test/runtime/signal-metadata.test.ts`:
  first attempt before build failed to resolve `@spine-ts/core`; reran after
  `typecheck:build`. Sandboxed rerun then passed 38 tests and failed only the
  standalone to-do smoke test with `listen EPERM: operation not permitted
127.0.0.1`; rerun with loopback/native approval passed 3 test files and
  39 tests.
- `pnpm --config.verify-deps-before-run=false typecheck:build`: passed.
- `pnpm --config.verify-deps-before-run=false docs:check`: passed; TypeDoc
  reported only the existing invalid `origin` remote warning.
- `pnpm --config.verify-deps-before-run=false format:check`: initially failed
  on the already-modified task Markdown; after applying Prettier's wrapping for
  that file, passed.
- `git diff --check`: passed.
- Root focused test rerun:
  `pnpm --config.verify-deps-before-run=false exec vitest run examples/todo/src/index.test.ts packages/testing/test/index.test.ts packages/server/test/runtime/signal-metadata.test.ts`.
  The sandboxed run passed 38 tests and failed only the standalone server test
  with `listen EPERM: operation not permitted 127.0.0.1`; the escalated
  loopback rerun passed 3 files and 39 tests.
- Root parallel static-check attempt:
  `typecheck:build` failed with a generated-directory `filesystem_error` while
  `docs:check` was also running `proto:generate`; `docs:check` itself passed
  with the existing invalid-`origin` TypeDoc warning. Remaining generated
  checks are being rerun sequentially.
- Root formatting fix:
  `pnpm --config.verify-deps-before-run=false format` reformatted
  `build-protocol/reviews/T-0018b-runtime-metadata-adoption.md` after
  `format:check` flagged it.
- Root full verify first attempt:
  `pnpm --config.verify-deps-before-run=false verify` failed during eslint
  because stale untracked `.generated-*` staging directories were linted. The
  generated temp directories were removed and the full gate is being rerun.
- Root full verify rerun:
  `pnpm --config.verify-deps-before-run=false verify` passed with native
  loopback/IPC approval. It ran node checks, proto generation, typecheck, lint,
  cleanup enforcement, format check, 57 Vitest files with 1,077 tests, coverage
  at 95.05% statements / 90.11% branches / 98.19% functions / 95.07% lines,
  docs check with the known invalid-`origin` TypeDoc warning, proto lint, and
  generated-clean checks.
- Post-merge full verify on `main`:
  `pnpm --config.verify-deps-before-run=false verify` passed with native
  loopback/IPC approval. It ran node checks, proto generation, typecheck, lint,
  cleanup enforcement, format check, 57 Vitest files with 1,077 tests, coverage
  at 95.05% statements / 90.11% branches / 98.19% functions / 95.07% lines,
  docs check with the known invalid-`origin` TypeDoc warning, proto lint, and
  generated-clean checks.

## Participants

- Requirements splitter:
  `019f4796-6039-73d0-9745-fbaa61501725`; completed and closed by root.
- Implementation agent:
  `019f479d-07bf-7be0-beaa-90f12649c2db`; completed focused implementation
  and was closed by root before review.
- Code style/maintainability reviewer:
  `019f47a7-38f1-7680-8e0c-2b26cadab89b`; clean result captured and closed.
- Documentation reviewer:
  `019f47a7-399a-7940-8363-75230e2a3e98`; clean result captured and closed.
- TypeScript/API reviewer:
  `019f47a7-3a19-7061-8137-fadfda2920b8`; clean result captured and closed.
- Security reviewer:
  `019f47a7-3aac-7cf1-874c-98e90f564ca2`; clean result captured and closed.
- Performance/reliability reviewer:
  `019f47a7-3b49-7840-accd-2092d494d9e3`; clean result captured and closed.
