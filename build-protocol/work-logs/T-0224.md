# T-0224 Work Log

## 2026-09-03

- Started from fresh `origin/master@5b7c6d1e5` in the isolated
  `general-protobuf-entity-identifiers` worktree.
- Installed the frozen workspace. A forced materialization was required because
  the first pnpm invocation left no worktree-local dependency metadata.
- Generated and built prerequisites, then established an unchanged baseline:
  `primitive-id.test.ts` and `repository.test.ts`, 22 tests passed.
- Proto generation changed ten location-sensitive generation IDs. These are
  setup artifacts and must be restored before the first commit.
- Recorded the task as high-risk public/serialized-contract work.
- Extended the clean affected baseline to six additional routing, descriptor,
  Inbox, persistence, and duplicate-delivery files: 334 tests passed.
- Confirmed `2.0.0-snapshot.8` is unused for all 18 public packages.
- Requirements review accepted the architecture with no blocker. It confirmed
  that schema-aware route admission and replay are the production seam, while
  storage, packed Inbox targets, canonical keys, and guards already preserve
  complete message IDs.
- A read-only mechanical scan identified the existing fixture/test seams and
  the obsolete test that requires exactly `$typeName` plus `value`.

## Assignment ledger

| Function                         | Scope                                                | Model           | Reasoning   | Status                                                                                         |
| -------------------------------- | ---------------------------------------------------- | --------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| Requirements splitter            | Validate contract boundaries and test decomposition  | `gpt-5.6-sol`   | high        | Complete; explicit dispatch, configured role confirmed; runtime self-introspection unavailable |
| Implementation owner             | TDD, runtime, public type, TSDoc, records            | `gpt-5.6-terra` | medium/high | Complete; explicit dispatch/profile confirmed; runtime self-introspection unavailable          |
| Mechanical verification          | Focused tests/checks and output classification       | `gpt-5.6-luna`  | low/medium  | Complete; 1,895 server tests passed and changed branches received focused proof                |
| Correctness/compatibility review | Routing, persistence, canonical identity, JVM parity | `gpt-5.6-terra` | high        | Clean after correction                                                                         |
| Style/maintainability review     | Changed implementation and tests                     | `gpt-5.6-terra` | high        | Clean after correction                                                                         |
| TypeScript/API docs review       | Exported declaration and TSDoc                       | `gpt-5.6-terra` | high        | Clean after correction                                                                         |
| Performance/reliability review   | Key collision/stability and lifecycle paths          | `gpt-5.6-terra` | high        | Clean after correction                                                                         |

## Current next action

Run the final cheap preflight and one complete `verify:release`, then record the
feature-branch endpoint for human review.

## 2026-09-03 implementation slice

- Observed RED before production edits: `pnpm exec vitest run
packages/server/test/repository/primitive-id.test.ts` failed
  `reads message IDs with arbitrary declared fields` because a one-field
  `{ $typeName, uuid }` ID was rejected by the exact `$typeName` + `value`
  shape check.
- Replaced the public `MessageId` declaration with the general Buf `Message`
  contract and corrected its TSDoc. The package-root type import is exercised
  by the focused test.
- General message admission now retains the whole message; route admission
  checks the target descriptor type name and packs with `Identifiers` against
  that schema. The legacy exact `{ $typeName, value }` scalar adapter remains
  solely in `readValue()` / scalar routing. Generic blank-ID logic no longer
  inspects message fields. Removed the unused JSON `MessageIds.key()` path.
- GREEN: focused primitive-ID and repository-routing suite passed, 249 tests.
  `pnpm typecheck` passed after generated prerequisite checks.
- Changed-source coverage run completed. Relevant files: `primitive-id.ts`
  96.42% lines and `repository.ts` 89.49% lines. The command exits non-zero
  only because Vitest applies the workspace-wide 90% global threshold to this
  focused subset (19.85% global lines), not because a focused test failed.
- Remaining scope is deliberately not claimed complete: the required generated
  composite routing, Inbox/replay, persistence, canonical-key/guard tests and
  review wave still need implementation/verification.

## 2026-09-03 generated composite routing slice

- Added an in-test generated descriptor fixture for `CompositeRouteId` with a
  nested `spine.core.UserId reader` and an `int32 number` discriminator, plus
  compatible Entity state/event/source-state schemas. No authored/generated
  proto files changed.
- Focused routing tests now prove full-ID preservation for default Command,
  compatible Event producer, incompatible-producer Event fallback, and built-in
  state-update routing. Custom Event and state-update routes deduplicate a
  generated clone of `idA` while retaining `idB` that differs only by `number`.
- Canonical descriptor keys match a generated clone and distinguish `idA` and
  `idB`. The obsolete `RoutableId.value` member was removed.
- GREEN: `repository-routing.test.ts` passed 248 tests. Focused coverage with
  explicit zero global thresholds passed 252 tests across routing and
  primitive-ID files; relevant line coverage remained `primitive-id.ts` 96.42%
  and `repository.ts` 89.49%.
- Still outstanding: durable Inbox replay/persistence/reload, produced-event
  complete producer ID, malformed stored-target rejection, guard isolation,
  and specialist review.
- `pnpm typecheck` passed after the generated composite fixture addition.

## 2026-09-03 escalated integration proof

- Existing senior TypeScript implementation function
  `/root/t0224_integration_tests` was explicitly dispatched for this bounded
  integration blocker as `gpt-5.6-terra` with `high` reasoning. The configured
  implementation function/profile is the accepted runtime metadata because the
  execution surface exposes no independent child self-introspection. No child
  agents were used.
- Added in-test composite Process Manager coverage by deriving a state
  descriptor from the existing Process Manager fixture. It uses the accepted
  nested `UserId reader` and scalar `number` Entity ID shape without authored
  or generated Proto changes.
- Proved Entity Inbox handoff/replay holds full `idA`/`idB` target messages,
  replay uses the stored target without rerunning custom routing, and a fresh
  context rehydrates the two persisted states independently.
- Proved complete composite produced-event `context.producerId`, rejection of
  wrong-ID-type and malformed-byte stored targets before handler invocation,
  and guarded multi-target isolation: cloned `idA` is deduplicated, `idB` is
  retained, repeated source events are suppressed per target, and a distinct
  source event is processed.
- The initial guard test fixture omitted required event-history producer ID,
  version, and timestamp metadata. Guard instrumentation showed the commit
  rejected before completion; restoring valid persisted-event metadata made
  the proof pass. The temporary probe was removed, leaving no production diff.
- Mechanical evidence: `pnpm exec vitest run
packages/server/test/repository/repository-routing.test.ts --testTimeout=15000
--maxWorkers=1` passed 1 file / 252 tests. `pnpm typecheck` exited 0 after
  frozen descriptor validation. Formatting completed with `pnpm exec prettier
--write packages/server/test/repository/repository-routing.test.ts`.
- Remaining task scope: deterministic documentation/coverage checks and the
  complete relevant specialist review wave; this integration proof alone does
  not close T-0224.

## 2026-09-03 mechanical typecheck correction

- The earlier explicitly dispatched mechanical verifier (`gpt-5.6-luna`, low
  reasoning) found `TS2339` at `primitive-id.ts:193`: the scalar-target legacy
  wrapper reader accessed `.value` after only general `MessageId` narrowing.
- Existing senior TypeScript implementation function
  `/root/t0224_integration_tests` was explicitly dispatched as
  `gpt-5.6-terra` / high to repair this bounded gate. Runtime self-metadata is
  unavailable; explicit dispatch/profile is the accepted metadata.
- Added a private exact legacy `{ $typeName, value }` wrapper narrowing for
  `MessageIds.readValue()` only. Public `MessageId` remains the general Buf
  `Message` contract with no `value` member. The focused test retains valid and
  non-finite wrapper coverage and now rejects an extra-field lookalike.
- `pnpm exec prettier --write packages/server/src/repository/primitive-id.ts
build-protocol/work-logs/T-0224.md` completed. Narrow checks passed:
  `primitive-id.test.ts` 1 file / 4 tests and `pnpm exec tsc --noEmit -p
packages/server/tsconfig.json` exit 0. Final mechanical gate passed:
  `pnpm exec vitest run packages/server/test/repository/primitive-id.test.ts
packages/server/test/repository/repository-routing.test.ts --testTimeout=15000
--maxWorkers=1` passed 2 files / 256 tests, and `pnpm typecheck` exited 0
  after frozen descriptor validation.

## 2026-09-03 tooling test-type correction

- The second explicit mechanical verifier result (`gpt-5.6-luna`, low
  reasoning) found test-only `typecheck:tooling` failures: excess-property
  checks on direct general-`MessageId` literals and a missing required command
  `context` in the composite routing fixture.
- Existing senior TypeScript implementation function
  `/root/t0224_integration_tests` was explicitly dispatched as
  `gpt-5.6-terra` / high. It now infers each concrete arbitrary-field message
  object first, then proves assignment to imported `MessageId`; the public
  type remains unbroadened. The composite command fixture uses the existing
  `create(CommandContextSchema)` helper value.
- Final evidence: `pnpm typecheck:tooling` exited 0 and `pnpm exec vitest run
packages/server/test/repository/primitive-id.test.ts
packages/server/test/repository/repository-routing.test.ts --testTimeout=15000
--maxWorkers=1` passed 2 files / 256 tests. Prettier completed for both test
  files and this work log; `git diff --check` is clean.

## 2026-09-03 lint declaration correction

- The third explicit mechanical verifier result (`gpt-5.6-luna`, low
  reasoning) identified the repository's consistent-interface rule in
  `primitive-id.ts`.
- Existing senior TypeScript implementation function
  `/root/t0224_integration_tests` was explicitly dispatched as
  `gpt-5.6-terra` / high. `MessageId` is now an interface extending Buf
  `Message` with an explicit required `$typeName`; it does not add `value` or
  weaken the general message contract. The private legacy wrapper declaration
  also uses the required interface form.
- `pnpm exec eslint` over the affected identifier/routing source and focused
  tests exited 0; `pnpm typecheck:tooling` exited 0; and the two focused test
  files passed 2 files / 256 tests. Formatting and diff checks are the final
  pre-commit steps for this declaration-only correction.

## 2026-09-03 cleanup naming correction

- The fourth explicit mechanical verifier result (`gpt-5.6-luna`, low
  reasoning) rejected the five-component private helper name
  `isLegacyScalarMessageWrapper` under cleanup policy.
- Existing senior TypeScript implementation function
  `/root/t0224_integration_tests` was explicitly dispatched as
  `gpt-5.6-terra` / high and renamed it to the compliant
  `isLegacyScalarWrapper`, with no behavior or public-contract change.
- `pnpm lint:cleanup` reported “Cleanup enforcement checks passed”; ESLint and
  `pnpm typecheck:tooling` exited 0; focused primitive-ID and repository-routing
  tests passed 2 files / 256 tests. Formatting and diff checks are the final
  pre-commit steps for this mechanical-only correction.

## 2026-09-03 TSDoc property correction

- The fifth explicit mechanical verifier result (`gpt-5.6-luna`, low reasoning)
  required summary documentation for public `MessageId.$typeName`.
- Existing senior TypeScript implementation function
  `/root/t0224_integration_tests` was explicitly dispatched as
  `gpt-5.6-terra` / high. It added concise TSDoc identifying `$typeName` as the
  fully qualified Protobuf message type name, using the established
  `prettier-ignore` blank-line layout required by the repository checker. No
  public behavior or contract changed.
- Complete cheap preflight passed: Prettier and `git diff --check`; affected
  ESLint; `pnpm lint:cleanup`; `pnpm lint:tsdoc`; `pnpm typecheck:tooling`; and
  focused primitive-ID/repository-routing tests (2 files / 256 tests).

## 2026-09-03 convergence, review, and version alignment

- The complete server test inventory passed 1,895 tests. Its two-file coverage
  projection reported 86.63% branches because the repository-wide 90% threshold
  was applied to two large source files containing historical branches. The
  changed undefined message-route branch then received a direct regression and
  exact lcov hit evidence; the focused suite passed 257 tests.
- The complete specialist review wave produced one consolidated correction:
  authoritative generated validation before packing, own `$typeName` admission,
  direct validated-ID returns, name-resolved descriptor fixtures, and accurate
  codec documentation. TDD reproduced inherited-property admission and invalid
  generated-constraint admission before the fix. After correction, the focused
  suite and coverage passed 259 tests; ESLint, cleanup, TSDoc, tooling typecheck,
  and full typecheck passed.
- Correctness/compatibility, reliability, and API-documentation re-reviews were
  clean. Maintainability's one deterministic duplicate-guard cleanup passed its
  narrow checks and required no semantic re-review.
- Commit `956e570b4` changes exactly the 26 top-level workspace versions to
  `2.0.0-snapshot.8` with the required message. Commit `a17d1a4f2` aligns 46
  concrete internal pins while preserving the external validation dependency;
  commit `2043352f0` updates only the corresponding lockfile importers.

## 2026-09-03 final release verification

- The final task preflight passed every generated, TypeScript, lint, cleanup,
  TSDoc, copyright, formatting, documentation, Proto, logging, and release-
  readiness gate plus 276 focused tests.
- The first `pnpm verify:release` run passed 4,550 tests and exposed only two
  stale snapshot.7 assertions in release-policy tests. The actual prepared and
  externally installed artifacts were already snapshot.8. Commit `53b4f44f1`
  updates those exact expectations; their focused suite passed 23 tests.
- The complete `pnpm verify:release` rerun passed 287 test files and 4,552 tests.
  Coverage passed at 93.29% statements, 90.01% branches, 92.83% functions, and
  94.45% lines. It also rebuilt and inspected all 18 snapshot.8 tarballs and
  installed them together in the isolated external consumer.
- The feature branch is ready for human review. No pull request or `master`
  change was made by the agent.
