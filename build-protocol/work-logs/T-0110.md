# T-0110 Work Log

## 2026-08-04 - Start

- Classified as high-risk from the approved Wave 6 plan because it changes
  public configuration, authentication forwarding, persistence identity, and
  concurrent subscription cleanup.
- Baseline is `origin/main@451906b7`; isolated branch and worktree are
  `task/T-0110-gateway-fan-in` and `.worktrees/T-0110-gateway-fan-in`.
- One read-only `explorer` is dispatched explicitly with
  `gpt-5.6-terra` / `medium` to map the current one-backend BrowserServer,
  UnaryGateway, SubscriptionGateway, native relay, durable binding, and test
  seams. It may not edit or spawn children.
- Runtime self-introspection may be unavailable. The explicit immutable
  dispatch is accepted absent a visible mismatch; omitted fields, a wrong role,
  or a visible fallback require redispatch.
- After exploration, one existing `implementer` owns the bounded production and
  behavior-test slice under explicit `gpt-5.6-terra` / `medium`. Review profiles
  follow `AGENTS.md`; no new role is introduced.

## Architecture Map

- The explorer completed under the explicit Terra/medium profile; runtime
  self-introspection was unavailable and no mismatch was visible.
- The minimum dependency order is: opaque composite subscription envelope and
  topology identity; durable binding persistence/fencing; bounded native
  fan-in and unary round robin; BrowserServer wiring; client lifecycle handling;
  then Message Board deployment configuration.
- Existing authentication termination, Actor/Tenant rewriting, public result
  shapes, byte ownership, relay bounds, binding leases, and combined/singular
  deployment behavior must remain intact.
- Partial fan-out cleanup, topology identity versus principal identity,
  aggregate envelope bounds, non-terminal child loss, and cancellation/close
  races are the principal risks.
- Frozen `SubscriptionUpdate.response` already permits a non-terminal error
  notice with no Entity/Event payload. This can represent a generic backend
  loss without exposing endpoints or modifying frozen JVM Protos; client-web can
  emit `gapPossible` and continue consuming healthy streams.

## Contract Freeze Assignment

- This public/durable/concurrency boundary qualifies for the existing
  `requirements_splitter` role once at the milestone boundary. Dispatch is
  explicit `gpt-5.6-sol` / `high`. It owns a concise implementation brief that
  freezes the smallest compatible topology, composite-envelope, loss-notice,
  cleanup, and test contracts. It may not edit production code or spawn agents.
- Runtime self-introspection may be unavailable; the explicit immutable role
  profile is accepted absent a visible mismatch. The implementation owner
  remains Terra/medium after this one contract pass.

## Contract Freeze Disposition

- The explicitly dispatched Sol/high requirements splitter did not converge
  within the bounded planning window and produced no file or production edit.
  It was interrupted to avoid delaying implementation. Runtime introspection
  was unavailable; no profile mismatch was visible.
- The orchestrator froze the concise implementation brief directly from the
  accepted explorer map and inspected public client/Proto seams. It uses the
  existing `SubscriptionUpdate.response` and client lifecycle `gapPossible`,
  avoids frozen Proto changes, and introduces no separate Gateway subsystem.
- The existing `implementer` role now receives sole production ownership under
  explicit `gpt-5.6-terra` / `medium`. Runtime metadata handling remains the
  same. The owner may not spawn agents and must push checkpoints immediately.

## Core Fan-In Checkpoints

- The implementer completed under its explicit Terra/medium profile; runtime
  self-introspection was unavailable and no mismatch was visible.
- `7516dd6b` adds the bounded 1–32 round-robin unary and versioned,
  length-delimited composite subscription collaborators. Unary failures are not
  retried; partial creation disposes every successful child. Four focused auth
  tests pass.
- `25c24d72` preserves singular `backend.baseUrl`, adds ordered validated
  `backend.baseUrls`, and wires standalone BrowserServer through the fan-in
  collaborators. The focused auth/server profile passes 102 tests.
- Generated build TypeScript and diff checks pass. Both commits were pushed
  immediately. The same implementer retains ownership for durable topology
  fencing, non-terminal loss/client lifecycle, and Message Board configuration.

## 2026-08-05 - Native fan-in checkpoint

- Implementer assignment is the existing `implementer` role, explicitly
  configured `gpt-5.6-terra` / `medium`; runtime metadata is not exposed by
  this surface and no visible profile mismatch occurred.
- TDD RED exposed the missing fan-in exports; the focused test was initially
  blocked by generated workspace package links, then the normal generated
  TypeScript build made that dependency resolvable.
- Added a bounded (1–32) round-robin unary forwarder that invokes one selected
  backend exactly once, plus a versioned length-delimited fan-in subscription
  envelope. Create rolls successful children back with `allSettled`; activate,
  cancel, and dispose attempt every child and preserve failures as an aggregate.
- Evidence: `pnpm typecheck:build:generated`, then
  `pnpm exec vitest run packages/auth/test/gateway-fan-in.test.ts` (4 passed),
  and `git diff --check` passed. Remaining work: topology identity/durable
  fencing, BrowserServer wiring/configuration, non-terminal loss notice,
  client-web behavior, and Message Board deployment coverage.

## 2026-08-05 - Browser topology configuration checkpoint

- Added the compatible `browser.backend.baseUrls` form while preserving the
  singular `baseUrl` form. It validates ordered, canonical, unique HTTP(S)
  origins with a 1–32 bound.
- Standalone BrowserServer now creates one native creator per configured URL,
  uses the bounded round-robin forwarder for unary calls, and uses the composite
  creator for subscription lifecycle fan-in. Combined mode continues to use its
  one local backend path.
- TDD RED: the topology tests failed because `BrowserServer.backendUrls` did
  not exist. GREEN followed with configuration validation and wiring. A focused
  regression exposed array handling in owned-server cleanup; it now treats URL
  arrays as standalone endpoints rather than a local RunningServer.
- Evidence: `pnpm typecheck:build:generated`; focused auth/server tests: 102
  passed; `git diff --check` passed. Remaining: durable topology identity,
  non-terminal backend-loss notice/client lifecycle, Message Board manifests,
  and broader integration cases.

## 2026-08-05 - Message Board gateway environment checkpoint

- `BACKEND_URLS` is now an ordered comma-separated fixed topology and takes
  precedence when set; legacy `BACKEND_URL` remains the single-node fallback.
- The Message Board gateway passes the resulting list to the compatible server
  `baseUrls` configuration.
- TDD evidence: configuration tests were RED for the absent list contract, then
  16 focused tests passed after implementation; generated TypeScript build and
  `git diff --check` passed. Compose/Kubernetes topology declarations and
  durable topology fencing remain outstanding.

## 2026-08-05 - Deployment topology declaration checkpoint

- Compose now declares each standalone Gateway's ordered two-application
  `BACKEND_URLS` topology and focused Compose/Kubernetes manifest checks pass
  (8 tests). The Kubernetes reference records the `BACKEND_URLS` contract;
  durable topology identity and stream-loss lifecycle behavior remain pending.

## 2026-08-05 - Durable topology fencing checkpoint

- BrowserServer derives a SHA-256 identity from length-delimited ordered
  canonical backend URLs and forwards it through SubscriptionGateway lifecycle
  calls. Durable records persist it, use a v3 storage key/record shape, and
  ownership checks reject different or missing identities before callbacks.
- Existing direct/in-memory collaborators retain the explicit `legacy` default;
  old durable record shapes are not accepted as valid active ownership records.
- Evidence: generated TypeScript build and 122 durable-binding tests passed.

## 2026-08-05 - Non-terminal fan-in loss checkpoint

- A child subscription ending while another child remains active emits a generic
  `SubscriptionUpdate` error response without an entity/event payload. The
  notice contains no backend endpoint or credential details; final completion
  remains when the last child ends.
- Browser client treats this exact protocol notice as `gapPossible`, does not
  publish it as a domain delivery, and continues the existing iterator/retry
  lifecycle.
- Evidence: generated TypeScript build, focused auth/client suites (79 tests),
  and `git diff --check` passed.
- Follow-up `be67ea38` keeps the aggregate activation joined to healthy child
  streams after one child fails; that failure is non-terminal and only the
  generic notice is emitted. The same focused suites remain green.

## Mechanical Preflight Correction

- Focused `verify:task --no-coverage` passes Node, Proto, checksum, example
  Proto quality, generated build, and root tooling TypeScript, then ESLint
  reports 15 deterministic issues in the new slice: forbidden non-null
  assertions/one unsafe aggregate decode return and async test doubles without
  awaits.
- The existing implementer retains explicit Terra/medium ownership for one
  correction batch over `auth/gateway/fan-in.ts`, its focused test, and
  BrowserServer. Runtime metadata handling is unchanged. No review begins until
  the same focused mechanical preflight passes.
- `ec7d9320` completes the lint correction under the retained Terra/medium
  profile; tooling TypeScript, focused ESLint, 79 tests, formatting, and diff
  checks pass. The full focused preflight then passes lint/cleanup and reports
  three deterministic TSDoc-format defects: two callable summaries in
  `fan-in.ts` and one block opener in `subscriptions/index.ts`. The same owner
  receives this documentation-only correction before review.
- `18583b2b` closes the TSDoc defects. The next full preflight passes lint,
  cleanup, TSDoc, formatting, and documentation audience, then API-doc checking
  reports that the two intentional auth exports (`FanInSubscriptionCreator` and
  `RoundRobinUnaryForwarder`) are absent from the expected public inventory.
  The same owner receives this deterministic API-ledger correction.
- `5669501a` closes the public API inventory. Fresh focused
  `verify:task --no-coverage` passes every deterministic gate and 368 focused
  tests across six active files. The endpoint enters one complete relevant
  specialist review wave; security is N/A because no trust boundary or
  credential flow changes.
- The complete review wave returns eight consolidated correction areas:
  aborted-signal compensation, pre-allocation aggregate bounds, exact loss
  discrimination, valid Kubernetes fixed endpoints, exclusive backend config,
  topology-fencing capability, missing high-risk proofs, and user/agent/TSDoc
  updates. No frozen Proto or trust-boundary defect exists. One batch returns to
  the existing Terra/medium implementation context.
- Replaced every reported assertion with a checked local value and converted
  test doubles to direct promises. Root tooling TypeScript, focused ESLint,
  79 focused auth/client tests, formatting, and `git diff --check` now pass.

## Deterministic TSDoc Correction

- Corrected callable summaries for the fan-in `forward` and `dispose` methods
  and normalized the topology property comment block opener.
- Evidence: `pnpm lint:tsdoc`, root tooling TypeScript, focused ESLint,
  Prettier, and `git diff --check` passed.

## Deterministic API Inventory Correction

- Recorded `FanInSubscriptionCreator` and `RoundRobinUnaryForwarder` as the
  intended public auth exports in the TypeDoc inventory.
- Evidence: API inventory, documentation audience, tooling TypeScript,
  Prettier, and `git diff --check` passed.

## Review Correction - Fan-in Safety and API

- Fan-in rollback now uses fresh cleanup signals, and aggregate envelope sizing
  is checked with safe arithmetic before allocation. Client suppression is
  limited to the exact generic `backend-unavailable` error-only notice.
- Browser backend configuration now requires exactly one of `baseUrl` or
  `baseUrls` in both TypeScript and runtime validation.
- Evidence: generated build and 177 focused auth/client/server tests passed.

## Review Correction - Durable Capability and Deployment

- Built-in durable bindings declare topology fencing; standalone fan-in rejects
  custom stores without that capability. Kubernetes now uses stateful application
  nodes and a headless service for two distinct stable gateway endpoints.
- Updated server and Message Board documentation with preferred multi-backend
  configuration, fallback, and best-effort recovery behavior.
- Evidence: Kubernetes manifest tests, generated build, and 102 focused tests passed.

## Review Correction - Runtime Configuration Regressions

- Added both/neither backend-form runtime rejection coverage and completed
  public fan-in/backend/loss/re-query contract wording in server and browser
  extension documentation.
- Evidence: generated build, 179 focused auth/client/server tests, TSDoc,
  Prettier, and `git diff --check` passed.

## Review Regression Proofs

- Added fan-in regressions proving aborted child creation uses a fresh disposal
  signal and aggregate envelope caps compensate successful children before a
  composite is allocated.
- Evidence: focused gateway fan-in tests (6) and `git diff --check` passed.

## Durable Topology Regression Proof

- Added matching ordered-topology activation coverage and verified reordered,
  different, and missing topology cancellation paths deny before callbacks.
- Evidence: durable subscription binding suite (123 tests) passed.
