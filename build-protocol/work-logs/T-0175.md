# T-0175 Work Log

## 2026-08-12 - Startup

- Started from integrated `origin/main@de2f5a6a`, after canonical API and
  architecture references converged.
- Scope is `docs/USER_GUIDE.md` plus T-0175 records. The current guide is 2,019
  lines and will be replaced under the approved ten-section structure.
- Existing `implementer` assignment explicitly uses `gpt-5.6-terra` / medium;
  runtime metadata limitation will be recorded if unavailable.

Status: implementation starting.

## 2026-08-12 - Guide rewrite

- Assignment acceptance: existing `implementer`, explicitly `gpt-5.6-terra` /
  medium. This surface does not expose runtime model self-introspection; the
  immutable configured profile is the available metadata, with no mismatch or
  fallback exposed.
- The guide is high-risk documentation only. It changes no runtime API,
  generated output, package metadata, or shared snippet policy. The
  documentation co-authoring skill was selected for reader structure; its
  interactive drafting steps are superseded by the approved ten-section plan
  and autonomous task authorization. No subagents were used.
- Replaced the prior 2,019-line guide with the approved ten-section journey.
  Every section begins with a useful beginner step and ends at its exact primary
  canonical handoff. Message Board is continuous; To-Do, Orders, Projects, and
  Distributed Message Board are used only where they clarify a next step.
- The guide preserves current contracts: implicit first-field IDs, exact
  `route()`/`replaceDefault()` routing, `@Where` scope, generated rejections,
  LogLayer, typed/provider-native storage tenancy, idempotent durable replay,
  one Gateway, and Cloud Run exclusion.

Status: focused validation pending.

## 2026-08-12 - Deterministic validation

- Passed generated build: `pnpm typecheck:build`; the Proto workflow verified
  47 source checksums and 52 frozen descriptor files against normalized digest
  `2a0db288787ac436c190ed86d72d78922deeb0fba4a64b2094b36b2525859db0`.
- Passed strict snippets both for `docs/USER_GUIDE.md` and the default admitted
  set, plus API inventory and documentation audience checks. No shared snippet
  checker policy or path expectation changed.
- Passed ten-section order, exact primary-handoff, routing/current-scope, and
  natural-prose scans. The scan deliberately rejects affirmative retired API
  claims while allowing the guide's clear statement that `@Route` and semantic
  routing are absent.
- Passed copyright, release readiness (82 package imports, 51 package assets,
  359 relative Markdown links), Prettier, and diff hygiene.
- Passed `pnpm verify:task -- --no-tests` once after convergence. It rebuilt
  generated TypeScript and tooling successfully. Runtime tests and coverage are
  N/A for this documentation-only task.

Status: implementation verified and ready for the orchestrator review wave.

## 2026-08-12 - Accepted Review Corrections

- Continued with the existing `implementer`, explicitly configured
  `gpt-5.6-terra` / medium. The surface still exposes no runtime model
  self-introspection, so the immutable configured profile is the available
  metadata; no mismatch or fallback is visible.
- Documentation P2: first changed the focused exact-inventory expectation to
  include `docs/USER_GUIDE.md`. The focused test failed while the guide was
  absent from the default list, then passed after registration. The shared
  checker behavior is unchanged; only its documented default inventory gained
  the guide.
- TypeScript/API P2: replaced the inaccurate "server reference logging
  section" wording with the approved unanchored server-reference handoff for
  framework and server contracts. The guide itself remains the introductory
  logging explanation and its canonical API links remain available where
  needed.
- Performance/reliability is CLEAN: the correction makes no runtime, delivery,
  retry, or topology change. No reviewer dispatch is authorized for this
  accepted deterministic batch.

Status: correction validation in progress.

## 2026-08-12 - Correction Validation

- Passed generated build, all 11 focused snippet-checker tests, and the default
  strict snippet checker. The admitted guide's two TypeScript fences compile
  through their real declared source contexts; no stub declarations were added.
- Passed API documentation and audience checks; the ten-section, exact-handoff,
  retired-routing, current-scope, and natural-prose scans; release-readiness
  link checking; focused ESLint; cleanup, TSDoc, and logging-containment checks;
  copyright, Prettier, and diff hygiene.
- Passed `pnpm verify:task -- --no-tests` after convergence. Runtime behavior
  remains unchanged, so runtime tests and coverage remain N/A for this
  documentation/default-inventory correction.

Status: accepted review corrections verified and ready for orchestrator
continuation.
