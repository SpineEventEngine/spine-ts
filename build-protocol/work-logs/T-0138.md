# T-0138 Work Log

## 2026-08-08 Baseline

- Created isolated branch/worktree from pushed T-0137 commit `258435c0`.
- Frozen offline dependency installation passed for all workspace projects.

## Architecture Dispatch

- Existing role: `requirements_splitter`.
- Expected and explicitly dispatched profile: `gpt-5.6-sol` / `high`.
- Runtime self-introspection will be recorded if exposed; otherwise the
  immutable configured role/profile and limitation are accepted unless a
  visible mismatch or fallback occurs.

## 2026-08-08 Architecture Result

- Existing `requirements_splitter` completed under explicit `gpt-5.6-sol` /
  `high`. Runtime self-introspection was unavailable; the immutable configured
  role/profile is the available metadata and no fallback was visible.
- It froze one direct approved record family, exact CAS lifecycle and uncertain
  write handling, complete single-Gateway restart recovery, finite expiry work,
  provider selector identities, public option removals, and a complete
  invention-deletion inventory.
- No human decision blocker remains. One overlapping Auth/Server implementer
  owns the ordered codec, lifecycle, Gateway cutover, restart, provider, and
  public-contract slices.

## Implementation Dispatch

- Existing role: `implementer`.
- Expected and explicitly dispatched profile: `gpt-5.6-terra` / `medium`.
- Runtime self-introspection will be recorded if exposed; otherwise the
  immutable configured role/profile and limitation are accepted unless a
  visible mismatch or fallback occurs.

## 2026-08-08 Implementation Start

- The dispatched existing `implementer` owns the listed overlapping Auth and
  Server files under explicit `gpt-5.6-terra` / `medium`; runtime
  self-introspection is unavailable on this surface, and no fallback is
  visible. The configured role/profile is therefore the available metadata.
- Classified as high-risk for direct persisted authentication records, expiry,
  restart lifecycle, and public-contract removals. The frozen acceptance
  criteria are direct approved-record retention, CAS lifecycle, retained
  Actor/Tenant authorization, one-Gateway recovery, provider selection, and
  removal of every legacy coordination seam.
- Skills checked: session inventory, expected-skill manifest, bounded
  `/Users/armiol/.agents/skills` scan, and lock manifest. Selected and fully
  read `test-driven-development` and `tdd`; the already-provided worktree
  makes `using-git-worktrees` N/A, and review/delegation skills are outside
  this implementer assignment.
- JVM guardrail read: `spine-jvm-docs/spine-server-runtime-and-bounded-context.md`
  (SubscriptionService lifecycle) and
  `spine-jvm-docs/spine-client-api-queries-subscriptions-and-tests.md`
  (opaque ID, create/activate/cancel, close). The local JVM source paths cited
  by those notes are unavailable, so notes are the task-relevant evidence.
  This supports preserving the simple three-operation lifecycle and opaque ID,
  not adding coordination concepts.

## 2026-08-08 Direct Record Slice

- RED: added a focused durable-store behavior test requiring the approved
  `GatewayAuthenticatedSubscriptionSchema` and exactly the `when_expires`
  materialized column. It first failed because the old registry required its
  prohibited coordination limits.
- GREEN: replaced the durable Any/JSON binding, quota, and cleanup record
  implementation with a direct Protobuf RecordSpec keyed by `SubscriptionId`.
  The focused test passes.
- Focused `tsc --noEmit -p packages/server/tsconfig.json` has no T-0138
  durable-store diagnostics. It remains blocked by pre-existing unowned
  `schema`-versus-`recordType` RecordSpec diagnostics in tenant/delivery
  sources; the earlier workspace build also has unrelated Datastore API
  errors in examples. These are expected downstream baseline failures, not
  changed by this slice.
- Replaced the legacy durable-registry test inventory with direct-record
  behavior tests for schema/column selection, reopened recovery, and cleanup
  failure retaining the durable row. Focused Vitest evidence: one file and
  three passing tests.

## 2026-08-08 Review Wave One Corrections

- Durable ownership now compares retained and request contexts by Actor and
  Tenant only. Record reads and recovery fail closed for missing/mismatched
  IDs, missing trusted context, and unsafe expiry timestamps. Expiry cleanup
  aborts active work and enters the same per-ID operation serialization as
  Activate and Cancel; recovery also serializes every retained ID.
- Focused durable behavior evidence: 24 passing tests. `git diff --check`
  passed. `pnpm lint:tsdoc` is externally blocked by existing unowned
  `packages/storage-rdbms/src/mysql/{errors,scope,testing}.ts` TSDoc findings;
  it did not report an owned T-0138 path.
- Fresh Subscribe compensation now calls backend cleanup before durable deletion;
  when that cleanup fails, the retained row remains retryable. Scheduled expiry
  cleanup catches rejected promises. Focused direct-subscribe evidence: four
  passing tests.

## 2026-08-08 Replacement-Owner Handoff

- The direct durable-record test remains green after updating its trusted
  Actor/Tenant fixture: `packages/server/test/server/durable-subscription-bindings.test.ts`
  reports 3/3 passing.
- The Auth cutover is incomplete and not reviewable. The old Subscribe flow
  derives the public subscription ID from the now-prohibited reservation before
  it can persist the canonical definition. Its replacement must make the
  bindings `create` operation construct and return the canonical subscription
  from the trusted Topic (or otherwise provide the same direct-record atomic
  transition); simply deleting reservation fields produces an invalid empty
  retained definition.
- Current evidence: `packages/auth` source typechecks after the partial public
  type removals; the legacy Auth subscription suite fails 36/54 because it
  still asserts removed capacity, fingerprint, and callback-guard behavior.
  Server typechecking has the pre-existing unrelated RecordSpec `schema`
  diagnostics in tenant/delivery sources, and the partial Auth contract must
  not be accepted until the full test replacement and Browser fixtures land.

## 2026-08-08 Micro-batch A — Direct Subscribe

- Replaced reservation-derived Subscribe IDs with `SubscriptionBindings.create`
  from the trusted rewritten Topic. Both memory and durable bindings allocate
  with `nextId`, build the canonical public Subscription, retain it, and return
  its public wire before backend creation. Backend creation failure compensates
  through the normal context-checked cancel path.
- Removed capacity reservation, binding-limit, principal-fingerprint, tenant
  fingerprint, and callback-guard types and production-path usages from the
  Auth/Browser subscription surface. Actor/Tenant comparison remains byte-based
  through the trusted ActorContext.
- Focused evidence: Auth source `tsc --noEmit` passes; direct Subscribe and
  durable bindings suites pass 5/5; `git diff --check` is clean; an exact
  prohibited-symbol scan has no production matches. The old broad Auth
  subscription inventory is deliberately not a Micro-batch-A gate because it
  contains later lifecycle assertions and removed reservation expectations.

## 2026-08-08 Micro-batch B — Direct Lifecycle and Restart

- Durable activation now owns a local per-ID abort controller. Cancel aborts an
  active callback before joining the same per-ID queue, invokes backend cleanup
  against the stored canonical definition, then performs exact CAS deletion.
  A failed cleanup retains the row; an unsuccessful CAS is accepted only when
  a reread proves absence.
- Expiry remains bounded to the 25 earliest durable records and removes only
  after cleanup succeeds. Recovery scans all rows, rehydrates unexpired
  definitions, and preserves a row if expiry cleanup or recovery fails.
  Durable close aborts and joins local work while retaining rows; memory close
  discards retained bindings.
- Focused evidence: Auth source typecheck passes; focused direct Subscribe and
  durable lifecycle/restart suites pass 9/9; `git diff --check` passes.

## 2026-08-08 Micro-batch C — Browser and Public API Cutover

- Removed BrowserServerOptions fingerprint plumbing and standalone startup
  validation, obsolete capacity rejection mapping, and direct durable
  constructor coordination options. The durable cleanup collaborator is now
  explicitly named `cleanup`; browser startup/recovery and shutdown continue
  to route through the bindings contract.
- Focused Auth and Server suite evidence: 165/165 passing across server
  integration, native gateway status mapping, direct Subscribe, and durable
  bindings tests. Auth source typecheck passes. Server typecheck has no owned
  T-0138 diagnostic; the only remaining diagnostics are the known pre-existing
  five RecordSpec `schema` errors in tenant/delivery sources.

## 2026-08-08 Micro-batch D — Provider and Invention Proof

- Added mocked MySQL table-selection and recording Datastore creator proofs for
  the direct authenticated-subscription family. The Datastore exercise proves
  create, reopen, recovery, and cancel use only
  `GatewayAuthenticatedSubscriptionSchema`; the MySQL recording factory proves
  the configured table and no legacy binding/quota SQL family are selected.
- Added a direct durable-source contract test rejecting Any/type URL/JSON and
  persisted coordination vocabulary. The RecordSpec test now asserts both
  sourceType and recordType are exactly the approved schema.
- Focused provider/public contract evidence: 3/3 passing; Auth source
  typecheck and `git diff --check` pass. No API-documentation script change was
  required because it has no stale subscription-symbol expectation.

## 2026-08-08 Mechanical Closure Attempt

- Narrow Server README, reference, user guide, and browser-auth limit guidance
  now describe the direct approved record, trusted Topic context, finite expiry
  cleanup/restart, single-Gateway limitation, and MySQL/Datastore selection.
  They no longer advertise durable quotas, reservations, fingerprints, leases,
  fences, or the removed browser fingerprint option.
- Focused runtime suite remains green at 168/168. A repository-wide Vitest
  coverage invocation is not a changed-source measurement: it applies the
  global 90% threshold to all workspace sources and reports 13.22% overall.
- `pnpm verify:task -- --coverage …` reached its shared `tsc -b` gate but is
  blocked before task coverage by five known later Server RecordSpec `schema`
  diagnostics and unowned message-board/orders example consumers still using
  removed `dispose`/`fingerprint` APIs. These paths are outside T-0138's
  assigned no-examples boundary. No owned Auth/Server source diagnostic was
  reported by the gate before those failures.

## 2026-08-08 Micro-batch F Gate Evidence

- Explicit changed-source coverage over all six requested production files ran
  168 passing focused tests but measured 75.09% statements, 68.52% branches,
  79.92% functions, and 78.11% lines. This is below the required 90% in every
  metric; the largest uncovered surface is the still-legacy Auth subscription
  lifecycle inventory.
- Exact ESLint also found owned callback-style failures in the newly authored
  focused tests. Direct production callback and non-null findings were corrected,
  but the complete test-fixture cleanup and coverage expansion remain required
  before a green mechanical handoff can be claimed.

## 2026-08-08 Micro-batch F2D — Legacy Auth Lifecycle Migration

- Migrated the retained `packages/auth/test/subscriptions/index.test.ts`
  lifecycle coverage to direct `create({ topic, whenExpires })` inputs and
  trusted `ActorContext` activation/cancel inputs. The test fixture now derives
  canonical Topic bytes rather than constructing obsolete public-definition,
  fingerprint, tenant, or expiry fields.
- Deleted only capacity/reservation/lease admission tests and removed their
  binding-limit and reservation helpers. Retained activation failures,
  cancellation retry, expiry, close, byte copying, and authorization behavior
  now assert direct-record retention: normal or failed activation is retained
  until cancel or expiry; failed direct persistence propagates before any
  backend subscribe/cancel operation.
- Exact inventory has no legacy `definition`, `expiresAtMs`,
  `principalFingerprint`, `reserveCapacity`, `reservation`, `guard`,
  `binding-capacity`, `lease`, or `bindingLimit` contract matches. Remaining
  `release` identifiers are ordinary local Promise test synchronizers.
- Focused evidence: `pnpm exec vitest run packages/auth/test/subscriptions/index.test.ts`
  passes 46/46; `pnpm --filter @spine-event-engine/auth exec tsc --noEmit -p tsconfig.json`
  exits cleanly. Coverage and complete ESLint gates remain outstanding.

## 2026-08-08 Micro-batch F2E — Expanded Coverage and Fixture Hygiene

- Expanded the direct durable behavior suite with public tests for successful
  expiry cleanup, foreign and expired transition denial, missing cancel
  idempotency, trusted-topic/direct-ID creation invariants, repeated identical
  CAS creation, unique-ID rejection, close, and the exported durable type guard.
  These raised standalone durable coverage to 85.83% statements, 70.76%
  branches, 96% functions, and 94.23% lines (9/9 tests passing).
- Removed all owned ESLint errors from changed Auth and durable production/test
  paths without suppressions. The exact changed-path ESLint invocation passes.
- Auth's green direct lifecycle, direct Subscribe, and native suites measure
  89.65% statements, 83.84% branches, 87.78% functions, and 92.37% lines;
  native adapter and relay exceptional paths remain below the 90% aggregate.
- The requested lifecycle/environment additions cannot currently join a shared
  coverage run: `environment-attachment`, `environment-close`,
  `environment-generation-stop`, and `server-lifecycle-integration` fail or
  hang in `InMemoryStorageFactory.tenantRecords` while reading an undefined
  RecordSpec schema from unowned T-0134 delivery/inbox paths. This is the same
  known RecordSpec-schema limitation recorded for Server typecheck, not an
  Authenticated Subscription API assertion. `server.test.ts` remains green
  (124/124) and by itself measures browser-server 94.66/90.20/91.93/95.61 and
  server.ts 69.14/66.45/84.84/69.09 (statements/branches/functions/lines).
- Consequently the required combined changed-source >=90% gate is still not
  met; no threshold was weakened. Further native relay/adapter and server
  lifecycle behavior coverage is required once the unrelated RecordSpec
  blocker permits the requested combined run.

## 2026-08-08 Micro-batch F3 — Corrected Runtime Coverage Scope

- Runtime measurement now excludes type-only T-0138 diffs in Auth `index.ts`
  and Server `server.ts`; those remain covered by Auth typecheck and public API
  contract tests. The runtime set is the materially changed subscription
  gateway, browser server, and durable bindings implementations.
- Added direct durable public behavior for blank namespace/ID rejection,
  trusted-topic validation, duplicate/CAS-read reconciliation, close, aborted
  activation, foreign/expired transitions, expiry cleanup, callback failure,
  idempotent missing cancel, and active-callback abort/serialization.
- Exact scoped run (220/220) is 92.64% statements, 87.10% branches, 92.12%
  functions, and 95.80% lines. Browser-server is above 90% in all metrics;
  subscription branches are 86.59% and durable branches 76.92%, leaving the
  branch threshold unmet. Exact owned ESLint passes; no coverage threshold or
  production-only instrumentation was changed.

## 2026-08-08 Micro-batch F4 — Durable Storage-Seam Branches

- Added a public StorageFactory seam proof that durable bindings rejects a
  storage implementation without atomic compare-and-set, alongside the F3
  direct create/CAS and lifecycle cases. Durable focused behavior is now 13/13
  passing and exact fixture ESLint passes.
- The last aggregate measurement before this small seam addition was 220/220
  with only branches below target (87.10%); a complete remeasurement remains
  required after further reachable direct gateway failure-path tests.

## 2026-08-08 Micro-batch F4A — Create CAS Throw Reconciliation

- Corrected durable direct create so a CAS transport error is reconciled by an
  exact reread: an identical persisted record returns the public wire, while
  an absent row propagates the original error. Public StorageFactory seam tests
  cover both outcomes; durable suite is 14/14 and exact lint passes.
- Remaining requested F4A seam matrix cases (divergent throw, false-CAS retry,
  cancellation loss/retry, mixed cleanup, malformed recovery, repeated close)
  and the complete scoped aggregate rerun are still outstanding.

## 2026-08-08 Micro-batch F4B — Expiry and Close Matrix

- Added direct mixed-expiry behavior: a failed cleanup preserves its row while
  a succeeding sibling cleanup deletes its row, then recovery proves only the
  failed row remains. Also proves repeated close is safe and post-close purge
  is rejected. Durable focused suite is 15/15 and exact lint passes.
- Divergent reread, false-CAS cancellation retry, and malformed recovery seam
  cases still require completion before the scoped aggregate can be rerun.

## 2026-08-08 Micro-batch F4C — Remaining Durable Seams

- Added public seam tests for divergent reread after a create CAS error,
  false-CAS cancel denial followed by retry closure, and malformed stored
  record recovery failure. Durable focused behavior is 18/18 and exact lint
  passes.
- Scoped runtime coverage now passes 226/226: 93.18% statements, 87.99%
  branches, 92.12% functions, and 96.37% lines. The branch-only deficit remains
  below the requested 90%; no threshold was weakened.

## 2026-08-08 Final Branch-Coverage Gate

- The existing `implementer` assignment remains explicitly configured as
  `gpt-5.6-terra` / `medium`. Runtime self-introspection is not exposed by this
  surface, so the immutable configured role/profile is the available metadata;
  no fallback or visible mismatch was observed.
- Added only public `StorageFactory`-seam behaviors for the final durable
  branches: expiry stops at the first unexpired raw row, a false cancel CAS
  with a present reread remains denied and retained, a raw missing-ID record is
  rejected by cancel and recovery, and a raw Topic without trusted context is
  denied without deleting the row.
- TDD evidence: the missing-ID recovery test was RED because recovery accepted
  a malformed row; the minimal GREEN guard now validates the durable record ID
  before expiry or rehydration. Focused durable suite passes 24/24.
- Exact scoped runtime coverage command over `subscriptions/index.ts`,
  `browser-server.ts`, and `durable-subscription-bindings.ts` passes 236/236:
  94.17% statements, 90.00% branches, 92.12% functions, and 96.64% lines.
  Exact owned-file ESLint, Prettier check, and `git diff --check` also pass.
- No coverage threshold, suppression, compatibility shim, commit, push, or
  merge was introduced. The only residual uncertainty is the established
  shared task verification limitation from later unowned RecordSpec/example
  consumers; it is outside this bounded final coverage gate.

## 2026-08-08 Pre-review Deterministic Corrections

- Replaced all 13 provider-conformance no-await callback implementations with
  explicit `Promise.resolve()` returns and replaced the optional ID non-null
  assertion with an explicit validation guard. No lint suppression or runtime
  behavior change was introduced.
- Corrected durable binding TSDoc summary verbs, block-tag gaps, and complete
  callable `@param`/`@returns` documentation. The repository-standard
  formatter guard preserves required leading member-documentation gaps.
- Evidence: targeted provider conformance suite passes 2/2; exact provider and
  durable-path ESLint passes; targeted TSDoc scan reports no T-0138 durable
  binding findings; Prettier and `git diff --check` pass. The inherited
  T-0134 MySQL TSDoc findings remain untouched.

## 2026-08-08 Lifecycle and Documentation Correction Closure

- TDD RED evidence: the new close regressions failed before the correction
  because cooperative create closed storage early and noncooperative admitted
  create let `close()` resolve. GREEN behavior tracks outer create/purge/recover
  work and per-ID work, aborts active callbacks, joins cooperators, times out
  noncooperators, closes once, and rejects later admission.
- Added only behavior coverage for callback abort forwarding, recovered expiry,
  durable Tenant ownership, bounded same-ID queuing, uncertain cancel deletion,
  trusted Zone/Language propagation, and invalid lifecycle wire rejection.
  No threshold, suppression, production-only seam, commit, push, merge, JVM,
  or later-consumer edit was introduced.
- Exact scoped coverage command over Auth subscriptions, browser server, and
  durable bindings: 247/247 passing; 94.07% statements, 90.14% branches,
  92.55% functions, and 96.94% lines. The final focused suite including the
  API-checker metadata test reports 256/256 passing.
- Auth source typecheck, exact ESLint, Prettier, audience-doc check, and
  `git diff --check` pass. TSDoc no longer reports owned Auth/durable paths;
  remaining failures are storage-rdbms debt only. API-doc checking and Server
  typecheck remain blocked by five later RecordSpec `schema` errors plus two
  unowned browser-server/API declaration mismatches. These paths were not
  changed by this correction owner.
- Documentation and API inventory now remove dangling cleaner-fence/takeover,
  private binding-envelope, returned-ID-only, 100-binding quota, and
  `SubscriptionCapacityReservation` claims while documenting direct approved
  records, trusted Actor/Tenant checks, and single-Gateway semantics.

## 2026-08-08 Final Reliability Correction

- RED: direct read accepted a record whose slot differed from its retained ID;
  purge could clean a mismatched query slot; recovery could rehydrate it; and
  direct create did not run the canonical retained-record validator before CAS.
- GREEN: direct read passes the requested ID to canonical validation; purge and
  recovery pass each query slot; create validates its constructed record before
  CAS. Mismatches now fail closed before cleanup, deletion, or rehydration.
- New behavior tests cover read/activate, purge, recovery slot mismatch and
  actor-less/unsafe-expiry direct creation. Focused durable suite: 36/36.
  Exact scoped aggregate: 251/251; 94.40% statements, 90.53% branches, 93.04%
  functions, and 97.07% lines.
- Exact lint and Prettier pass; Auth source typecheck and `git diff --check`
  pass. TSDoc still reports only the inherited storage-rdbms inventory; no
  owned Auth or durable binding finding is present. No commit, push, merge,
  JVM, later-consumer edit, suppression, or threshold change was introduced.

## 2026-08-08 Final Review Corrections

- Corrected durable limit validation and removed an accidental Auth helper
  export; focused API/durable checks passed 42/42 with Auth typecheck clean.
- Corrected the remaining User Guide, GKE, Server Reference, and recovery
  callback documentation. Final documentation re-review reported no P0-P2.
- Corrected per-ID queue accounting so capacity represents waiting work, not
  active work, and narrowed durable options to the three used process limits.
  The focused durable suite passed 37/37; final style re-review reported no
  P0-P2.
- Final reliability re-review reported no P0-P2 and passed 88 targeted tests.
  Final API re-review findings were documentation/validation-only and are
  resolved above.

## 2026-08-08 Final Independent Verification

- Fresh scoped runtime verification passed 252/252 tests with 94.31%
  statements, 90.39% branches, 93.08% functions, and 96.96% lines across Auth
  subscriptions, Browser Server, and durable authenticated bindings.
- Package metadata/public-contract tests passed 10/10. Auth typecheck,
  changed-file ESLint, documentation audience and snippet checks, Prettier, and
  `git diff --check` passed. Rebuilding Auth declarations removed the stale
  local declaration diagnostics from Browser Server without a source shim.
- TSDoc reports only the inherited T-0134 MySQL files. Server typecheck reports
  only five later `RecordSpec.schema` consumers owned by T-0139: TenantIndex,
  delivery attempts, two Inbox specifications, and shard work.
- The required single `verify:task` attempt passed Node and Proto generation,
  authored/example Proto policy, 47 source checksums, and 52 frozen descriptor
  checks. Its shared TypeScript build then stopped on the same five T-0139
  consumers plus the planned T-0142 example migrations: removed Datastore
  creation APIs, fingerprint/durable option cutover, and forbidden Message
  Board quarantine/revocation record sources.
- No T-0138 source appears in the final blocker inventory. No threshold,
  suppression, compatibility shim, JVM edit/build, or unrelated consumer fix
  was introduced.
