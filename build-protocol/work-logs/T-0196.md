# T-0196 Work Log

## Assignment Gate

| Assignment       | Existing role | Scope                                                                                                     | Explicit model  | Explicit reasoning | Child spawning | Runtime telemetry                                                                                         |
| ---------------- | ------------- | --------------------------------------------------------------------------------------------------------- | --------------- | ------------------ | -------------- | --------------------------------------------------------------------------------------------------------- |
| Wave 13 RED gate | `implementer` | New test/test-support/child-fixture and RED evidence files only; all 22 accepted designs; no product code | `gpt-5.6-terra` | medium             | Prohibited     | Immutable configured role/profile and explicit dispatch are visible; child self-telemetry is unavailable. |

## State

T-0196 is active from accepted `origin/main@c1b45018`. The protected primary
checkout remains read-only.

## RED Review And Plan Refinement

The first test-only draft was rejected because most cases asserted only module,
export, or source-token existence and RED-22 treated a broker import as success.
Those tests would not retain their behavioral assertions as implementation
landed.

The same implementer then identified one exact structural omission in T-0195:
the plan had not named the additive dispatcher-origin method or the existing
environment configuration property. The orchestrator froze the smallest
approved refinement before product work:

- `messageSchemas()` remains the complete dispatcher set;
- optional `externalEventSchemas()` declares the validated external subset and
  omission preserves domestic compatibility;
- `ServerEnvironmentSettings.transportFactory` and resolved
  `ServerEnvironment.transportFactory` use the existing `when(...).use(...)`
  lifecycle;
- `BoundedContextBuilder.addEventDispatcher()` and recording transport frames
  are the behavior-test seams; no broker accessor or test DSL is added.

The TypeScript/API and style/maintainability reviewers re-review this material
contract refinement before the RED gate is accepted.

Style review passed. TypeScript/API review accepted the additive shapes but
found one P1: broker interest derivation must consume external subsets from the
domain EventBus only. The plan now explicitly keeps system/state external
classification local and requires a RED proving it emits no wanted document.

RED-22 then exposed one final exact construction-name omission. The plan now
freezes `new InMemoryTransportFactory()` at the transport root and
`createZeroMqTransportFactory(config: ZeroMqConfig)` at the existing ZeroMQ
entry point. The latter accepts no new options and remains distinct from
SignalTransport's existing `createZeroMqTransport()`.

The cross-process fixture then exposed the generated registry record detail
needed to prove a generated external receptor. The plan now freezes registry
version 3 as the current shape plus required
`origin: "domestic" | "external"` on generated and canonical records. Fixtures
load only through existing `withGeneratedRegistryRoot(root)` and the established
generated module path; no injection overload or runtime registration seam is
added.

## Ownership Handoff

The first implementer completed the common behavioral scaffolding, downstream
Proto assertions, and partial lifecycle/transport cases but exhausted its
execution window before RED-20 and RED-22 could be implemented. Execution time
is not a protocol blocker. Ownership of all T-0196 test/evidence files transfers
exclusively to one replacement existing `implementer`, explicitly
`gpt-5.6-terra` / medium, with no child spawning. It must preserve the existing
work and finish only the remaining behavior/verification; the prior owner is no
longer active.

The orchestrator rejected its first replacement dispatch before accepting work
because model/reasoning fields were omitted, despite the role default matching.
The corrected replacement dispatch explicitly sets existing `implementer`,
`gpt-5.6-terra`, and medium reasoning; runtime self-telemetry remains
unavailable.

The second owner replaced the shared one-delivery placeholder with distinct
broker scenarios, executable origin checks, and a generated-handler-only
cross-process consumer. It exhausted its execution window with RED-14/18/20/21
still incomplete, and final audit rejected an invented
`testOnlyApplyWantedTransition` broker hook in RED-07–12. Ownership transfers to
one final explicit implementer for only those five bounded corrections. The
recording `TransportFactory` remains the approved observation seam; no broker
test API is authorized.

RED-21 exposed one genuine private-format testability gap: the plan required
malformed/symlink/oversize/stale-manifest behavior without fixing a manifest
layout. The plan now freezes an internal v1 JSON manifest, directory/socket
layout, 4096-byte/1024-entry bounds, 1-second heartbeat/5-second expiry,
PID-liveness rules, atomic permissions, cache key/reconciliation, and sanitized
attempt-all aggregation. It remains unexported and introduces no public option,
wire field, broker policy, or persistence concept.

Reliability review found that `adapterIdentity` needed an exact namespace rule.
Scanning now accepts only the local validated identity; a valid foreign identity
is ignored without connection or deletion. RED-21 proves same-directory foreign
identity isolation, and RED-22 peers use one shared mesh identity rather than
per-process identities.

## Final RED Correction

The final owner completed all 22 cases and the native child/manifest fixtures.
Independent orchestration verification then rejected one remaining contract
mistake: RED-17/19 expected a runtime `External` value although the frozen API
is a transparent type-only alias. The orchestrator replaced that check with an
executable virtual TypeScript program passed through the established
`BuildHandlerAnalyzer`; it now proves the canonical imported first-parameter
marker, domestic default, invalid shapes, external event command acceptance,
and external command rejection without requiring a runtime export. The
nonexistent `BuildTimeHandlerAnalyzer` shortcut was also removed from RED-20;
state-origin ingestion remains exercised through the real generated-registry
root and Bounded Context builder.

## Specialist Review Gate

| Concern                 | Existing role                      | Scope                                                                                          | Explicit model  | Explicit reasoning | Child spawning | Runtime telemetry                                                                                         |
| ----------------------- | ---------------------------------- | ---------------------------------------------------------------------------------------------- | --------------- | ------------------ | -------------- | --------------------------------------------------------------------------------------------------------- |
| Style/maintainability   | `style_maintainability_reviewer`   | T-0196 test/fixture and plan-refinement diff only                                              | `gpt-5.6-terra` | high               | Prohibited     | Immutable configured role/profile and explicit dispatch are visible; child self-telemetry is unavailable. |
| Performance/reliability | `performance_reliability_reviewer` | Lifecycle, transport conformance, process harness, bounds, and deterministic cleanup in T-0196 | `gpt-5.6-terra` | high               | Prohibited     | Immutable configured role/profile and explicit dispatch are visible; child self-telemetry is unavailable. |
| TypeScript/API docs     | `typescript_api_docs_reviewer`     | Frozen public/type/generated contracts expressed by T-0196                                     | `gpt-5.6-terra` | high               | Prohibited     | Immutable configured role/profile and explicit dispatch are visible; child self-telemetry is unavailable. |

Documentation review is N/A for this test-only gate because no application
documentation or established product claim changes; documentation ownership is
reserved for T-0202 after behavior stabilizes. Final security review remains
deferred to the one mandatory T-0202 release-readiness pass; T-0196 only freezes
the malicious-manifest and wrapper validation acceptance cases it will review.

### Review wave result

All three lanes requested changes. The accepted consolidated batch is:

- make RED-06 a schema-correct bidirectional topology and observe that imported
  events are not re-exported;
- drive real complete-set requester replacement, duplicate, withdrawal,
  rollback, unchanged, and peer-online transitions in RED-07–12, with creation
  counts distinct from active/closed publication behavior;
- exercise actual EventBus/repository mixed-origin delivery in RED-03/04 and
  exact external-message tenant/import mutation behavior in RED-15/16;
- freeze every integration/transport Proto field, reservation, canonical type
  URL, source provenance, Event/non-event IDs, and no-JSON/V8 frame behavior;
- add compile-time public contract fixtures for transparent type-only
  `External<T>`, additive `EventDispatcher`, required registry-v3 origin
  literals/validation, typed transport root exports, and exact
  `ThirdPartyContext` overloads without requiring a runtime `External` value;
- complete RED-18 two-context ownership, wrapped withdrawal, close ordering,
  failure aggregation, stale-resource retry proof;
- complete RED-20 generated analyzer/writer/ingestor coverage for every allowed
  external receptor plus ThirdParty actor/tenant/producer/external-only
  behavior and fixture cleanup;
- complete RED-21 live/malformed/symlink/oversize/dead/expired/missing-socket,
  bounded-scan, aggregate-attempt, restart-cache, close/error, and exact typed
  message/channel conformance;
- make RED-22 readiness explicit, assert full received Event/payload/context and
  derived producer identity, await clean child exits, verify adapter cleanup,
  and retain bounded fallback cleanup;
- remove the unused duplicate scenario helper and repair child/temp/global
  cleanup plus the malformed copyright header.

This is the single correction batch returned to the existing T-0196 owner.

The existing owner completed only the unused-helper removal and part of RED-20,
then again treated already-frozen contracts as unavailable. That blocker was
rejected with exact plan and repository API evidence. Because the owner still
did not implement the remaining accepted review batch, its ownership ends. One
replacement existing `implementer`, explicitly `gpt-5.6-terra` / medium and
prohibited from spawning children, receives exclusive ownership of the same
test-only files to finish the already-recorded batch without rediscovery or
product changes. Runtime self-telemetry remains unavailable.

The replacement owner reported the batch complete, but orchestrator inspection
found most accepted findings unchanged. The orchestrator therefore took direct
test-only ownership and completed the compile-consumer, transition, lifecycle,
typed-frame, manifest, ThirdParty, and process-readiness corrections. The exact
RED command again reports 22 named failures; root TypeScript, child syntax,
Prettier, and whitespace checks pass. Only the three substantively affected
review lanes are re-running before acceptance.

The first focused re-review still found under-specified exact types, generated
mixed-origin routing, requester replacement/rollback, complete tenant/context
intake, native attempt-all/generation bounds, control wrapper IDs, and bounded
failure cleanup. The orchestrator tightened those test-only contracts. Root
TypeScript, child syntax, Prettier, and whitespace checks pass, and the complete
acceptance command again reports exactly 22 intended missing-feature failures.
The same three lanes are performing one final focused re-review of only these
corrections. The TypeScript/API lane passes the exact registry and transport
surface after this correction.

## Final Review Result

TypeScript/API, style/maintainability, and performance/reliability all pass
after focused re-review. Documentation remains N/A for this test-only task, and
final security remains assigned to T-0202. No product or generated file changed
in T-0196.
