# T-0038a Work Log

Status: Affected re-review assigned

## Start

- `2026-07-14T01:50:36Z`: created from verified `main` at `75340852` in branch
  `task/T-0038a-canonical-fallback-type-urls` and its isolated worktree.
- Parent T-0038 audit confirmed the public malformed-fallback defect; accepted
  Sol High plan is checkpointed at parent commit `9addd3b0`.
- The child is independent from the unmerged parent. It will merge into `main`
  first; updated `main` returns to the parent only after child integration.

## Author Assignment

- Existing implementer at explicit expected `gpt-5.6-terra` / medium, no
  subagents.
- TDD source/test plus narrow core README/TSDoc and this record set only.
- Direct custom-fallback selection/derivation is the defect. Packing and
  implicit registry APIs expose no custom fallback and remain regression checks.

## Planned Gates

- Core focused RED/GREEN and compatibility tests.
- Generated build typecheck, scoped ESLint, docs/API, Prettier, generated-clean,
  and diff/status lint.
- Relevant style, documentation, TypeScript/API docs, and reliability reviews.
- Final full verify only after clean review; post-merge verify on `main`.

## Implementer Record

- `2026-07-14`: Existing implementer is executing at the assigned expected
  profile `gpt-5.6-terra` / medium; no subagents were spawned. Canonical skill
  check selected and read `test-driven-development`, `implement`, and
  `verification-before-completion` before RED. `typescript-advanced-types` is
  N/A because this is no advanced type-level change; server/JVM inspection is
  N/A because no server files are in scope.
- RED test table added through public `getTypeUrlPrefix()` and `deriveTypeUrl()`
  custom fallback inputs for `""`, whitespace-only, `"/"`, and `"///"` on
  no-option `AnySchema`. Expected deterministic error is
  `TypeError: Fallback type URL prefix must be non-empty and contain no whitespace.`
- RED evidence: after the normal generated build prerequisite,
  `corepack pnpm exec vitest run packages/core/test/index.test.ts --passWithNoTests`
  failed all four table cases with
  `AssertionError: expected function to throw an error, but it didn't` at
  `getTypeUrlPrefix`; 26 of 30 tests passed. Earlier attempts before the build
  prerequisite could not resolve ignored
  `@spine-ts/proto` entrypoints and were environment failures, not RED evidence.
- GREEN evidence: following the single-owner runtime change,
  `corepack pnpm exec vitest run packages/core/test/index.test.ts --passWithNoTests`
  reported `1 passed`, `34 passed (34)`. The implementation strips trailing
  separators, rejects an empty or whitespace-containing remaining fallback
  with the documented deterministic `TypeError`, and retains file-option
  precedence before evaluating a fallback.
- Added compatibility regressions keep valid bare/trailing/repeated trailing
  custom forms exact, validate a malformed unused fallback is ignored for a
  Spine-option schema, retain default derivation/packing and implicit registry
  registration, and retain explicit valid registry URLs. No custom fallback
  surface was added to packing or registry APIs.

## Focused Gate Evidence And Handoff

- `corepack pnpm typecheck:build:generated`: exit 0 (`tsc -b`).
- `corepack pnpm exec vitest run packages/core/test/index.test.ts --passWithNoTests`:
  exit 0; `1 passed`, `34 passed (34)`.
- `corepack pnpm exec eslint packages/core/src/index.ts packages/core/test/index.test.ts`:
  exit 0.
- `corepack pnpm docs:check:generated`: exit 0; TypeDoc/API check retained 28
  expected `@spine-ts/core` exports.
- Exact six-file `corepack pnpm exec prettier --check ...`: exit 0; all matched
  files use Prettier style. `corepack pnpm proto:check-generated`: exit 0;
  generated outputs are ignored, untracked, and freshly regenerated.
- `git diff --check`: exit 0. Final status/diff list exactly the six assigned
  paths, and the source public-export diff has no added/removed export
  declaration.
- Handoff is `DONE_WITH_CONCERNS`: no full verify, commit, push, merge, or
  reviewer dispatch occurred by design. No runtime behavior uncertainty remains
  under the focused evidence. The expected author profile is
  `gpt-5.6-terra` / medium and no subagents were spawned, but immutable actual
  runtime metadata is unavailable in this context; orchestrator acceptance and
  pending reviewer dispositions remain.

## Coordinator Pre-review Finding

- `2026-07-14T01:59:03Z`: immutable implementer-role metadata confirms actual
  Terra Medium matching explicit dispatch; author closed, no subagents.
- Fresh coordinator focused gate passes 34/34 tests, build, lint, docs/API,
  generated-clean, format, and diff checks.
- Resume the same author only to synchronize headers, document the
  `deriveTypeUrl()` throw contract directly, and clean broken inline-code wraps
  in these records. No runtime/test/README behavior change is assigned.

## Independent Review Assignment

- `2026-07-14T02:02:08Z`: implementer accepted/closed at actual Terra Medium
  from explicit immutable-role dispatch, no subagents. Coordinator rerun:
  34/34 core tests; generated build; docs/API with 28 core exports; exact-file
  Prettier; synchronized status; clean diff.
- Dispatch docs Luna Medium and style/API/reliability Terra High, all explicit,
  bounded, read-only, and no subagents. Security deferred to T-0041.

## Independent Review Result And Fix Assignment

- `2026-07-14T02:06:30Z`: actual docs Luna Medium and style/API/reliability
  Terra High; explicit immutable-role dispatch matched; no subagents; all
  reviewers closed. API is CLEAN.
- Complete batch: direct success-path getter normalization/precedence tests;
  concise README valid-result and file-option precedence wording; current-action
  status correction. Same Terra Medium author, no runtime change, focused gates
  only, then affected-lane re-review.

## Review Fix Implementation

- Converted the three existing valid custom derive cases into a compact
  parameterized table that preserves each exact derivation assertion and adds
  direct normalized getter assertions. Added a direct getter assertion to the
  existing malformed-unused-fallback Spine-option precedence case.
- Tightened only the core README registry bullet with explicit file-option
  precedence and `type.example.test///` to
  `type.example.test/<full.type.Name>`. Replaced the task's stale Immediate Next
  Action with focused verification and affected re-review direction.
- Focused evidence: `corepack pnpm exec vitest run
packages/core/test/index.test.ts --passWithNoTests` exited 0 with
  `36 passed (36)`; `corepack pnpm docs:check:generated` exited 0 with 28
  expected core exports; exact-six-file Prettier, generated-clean, synchronized
  status, and `git diff --check` all exited 0.
- No runtime, TSDoc, export, generated tracked-file, full verify, or Git action;
  no subagents. Affected re-review is pending.

## Affected Re-review Assignment

- `2026-07-14T02:10:41Z`: same actual Terra Medium author accepted/closed,
  explicit role metadata matched, no subagents. Fresh coordinator evidence:
  36/36 tests, docs/API 28 exports, generated-clean, format, status, diff.
- Assign style/reliability Terra High and docs Luna Medium. API N/A after prior
  CLEAN because fix changed only tests, README, and records. Security deferred.
