# T-0025 Work Log

Status: implemented; review pending

Branch: `task/T-0025-delivery-label-contract-cleanup`

Worktree:
`.worktrees/T-0025-delivery-label-contract-cleanup`

Started: `2026-07-10T03:08:00Z`

Baseline commit: `1a9804c0`

## Entries

- `2026-07-10T03:08:00Z`: Started T-0025 after T-0024 merged and root
  verification passed. Root `main` was clean except for user-owned untracked
  `human-review-1-jul.md`.
- `2026-07-10T03:08:00Z`: Requirements splitter
  `019f49f9-528f-7bd3-a111-68619d826f44` recommended removing `IMPORT_EVENT`
  from supported public write/input labels, rejecting new `IMPORT_EVENT` writes,
  preserving legacy stored-row recognition, and leaving `CATCH_UP` /
  `TO_CATCH_UP` unchanged.
- `2026-07-10T03:08:00Z`: Created the T-0025 task brief, D-0076, and this work
  log before runtime or docs implementation edits. A coordination mistake placed
  the first copy in the root checkout; the records were ported into this task
  worktree before review.
- `2026-07-10T03:24:00Z`: Implementation worker
  `019f49fd-3f5d-7a71-b761-da2e0d0a5c1e` implemented the delivery-label
  contract cleanup. `DeliveryLabel` now excludes `IMPORT_EVENT`, new
  `IMPORT_EVENT` writes fail before durable storage mutation, and legacy stored
  `IMPORT_EVENT` rows are recognized as deprecated compatibility data that fail
  closed before delivery.
- `2026-07-10T03:24:00Z`: Updated delivery API and architecture docs to describe
  supported labels and the legacy fail-closed behavior. `CATCH_UP` and
  `TO_CATCH_UP` remain unchanged.

## TDD Evidence

- Initial focused test command could not run before dependency setup:
  `pnpm --config.verify-deps-before-run=false exec vitest run`
  `packages/server/test/delivery/inbox.test.ts` failed with
  `Command "vitest" not found`.
- After `pnpm install --frozen-lockfile`, focused test setup still required
  generated/build output. `typecheck:build:generated` first failed because
  generated proto files were absent; `proto:generate` fixed the setup gap.
- Red receive test:
  `pnpm --config.verify-deps-before-run=false exec vitest run`
  `packages/server/test/delivery/inbox.test.ts -t "rejects IMPORT_EVENT receives"`
  failed because `IMPORT_EVENT` flowed into the dedup/write path and produced
  `Inbox dedup guard could not be completed due to concurrent changes.`
- Red legacy tests:
  `pnpm --config.verify-deps-before-run=false exec vitest run`
  `packages/server/test/delivery/inbox.test.ts -t "IMPORT_EVENT"` failed because
  a stored legacy row read back as an ordinary `InboxMessage`, and drain invoked
  the endpoint before failing during mark-delivered.
- Green focused contract tests:
  `pnpm --config.verify-deps-before-run=false exec vitest run`
  `packages/server/test/delivery/inbox.test.ts -t "IMPORT_EVENT"` passed with 4
  tests.
- Green full focused delivery file:
  `pnpm --config.verify-deps-before-run=false exec vitest run`
  `packages/server/test/delivery/inbox.test.ts` passed with 90 tests.

## Files Changed

- `packages/server/src/delivery/inbox.ts`
- `packages/server/src/delivery/inbox-records.ts`
- `packages/server/test/delivery/inbox.test.ts`
- `build-protocol/DECISION_LOG.md`
- `build-protocol/DEVELOPER_API.md`
- `build-protocol/RUNTIME_ARCHITECTURE.md`
- `build-protocol/TECHNICAL_SPEC.md`
- `build-protocol/tasks/T-0025-delivery-label-contract-cleanup/TASK.md`
- `build-protocol/work-logs/T-0025.md`
- `docs/api/README.md`
- `packages/server/README.md`

## Verification Evidence

- Stale scan:
  `rg -n "IMPORT_EVENT|DeliveryLabel|delivery label" build-protocol/DEVELOPER_API.md`
  `docs/api/README.md packages/server/README.md build-protocol/RUNTIME_ARCHITECTURE.md`
  `build-protocol/TECHNICAL_SPEC.md packages/server/src`
  `packages/server/test/delivery/inbox.test.ts` shows only tests, internal
  compatibility handling, and docs describing `IMPORT_EVENT` as
  unsupported/deprecated fail-closed data.
- `pnpm --config.verify-deps-before-run=false exec vitest run`
  `packages/server/test/delivery/inbox.test.ts` passed with 90 tests.
- `pnpm --config.verify-deps-before-run=false typecheck:build:generated`
  passed.
- `git diff --check` passed.
- `pnpm --config.verify-deps-before-run=false format:check` passed.
