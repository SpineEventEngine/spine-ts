# T-0045 Work Log

Status: Complete; committed and remotely synchronized

Branch/worktree: to be created for the sole implementation owner
Baseline: `main` at task start

## 2026-07-17 - Start

- P2 authorized by the human; P3 explicitly deferred.
- Build protocol and completion plan reread before task action.
- Functional acceptance: real gRPC command/query/subscription workload over a
  project-management domain with exactly 3 aggregates, 20 projections, 10
  process managers, and 10/25/50/100 independent users.
- High-risk assumptions are recorded in the task brief.
- No implementation child has been accepted yet.

## 2026-07-17 - Current implementer session

- Existing role: `implementer`; bounded P2 ownership only.
- Expected immutable execution metadata: `gpt-5.6-terra` / `medium`.
- Actual immutable execution metadata: unavailable to this in-workspace role;
  no child was spawned or accepted, and the supplied execution surface does
  not expose immutable model/reasoning metadata to repository code. This is a
  recorded limitation rather than inferred acceptance evidence.
- Skills applicability checked against
  `build-protocol/skills/EXPECTED_SKILLS.md`: the listed installed entrypoints
  for `planning-with-files`, `domain-modeling`, and
  `verification-before-completion` are readable, but are not selected for this
  bounded existing domain slice. `test-driven-development` and
  `javascript-testing-patterns` are selected and read for runtime behavior
  tests. No installed-skill lock manifest was found; the expected-skills
  manifest is the durable inventory fallback.
- Acceptance criteria retained: generated domain with exactly 3 aggregates,
  20 projections, and 10 process managers; public-API-only real local gRPC
  command/query/subscription smoke/load scenarios at 10/25/50/100 independent
  asynchronous users; bounded cleanup and documented result limits.
- TDD red evidence: `pnpm vitest run
examples/project-management/test/topology.test.ts` failed as expected
  because `examples/project-management/dist/src/index.js` does not yet exist.
- Generation prerequisite evidence: after the new P2 package was linked with
  `pnpm install`, `pnpm proto:generate` failed because the root Buf workspace
  does not contain `examples/project-management/proto`; therefore its generated
  module cannot resolve the canonical `spine/options.proto` imports. The new
  source must import those options to describe aggregates, projections, and
  process managers.
- Initial blocker: adding that module to root `buf.yaml` was strictly required
  for generation but outside the original assigned files.
- Continuation authorization: the human explicitly authorized the minimal root
  `buf.yaml` module entry and corresponding P2 generation/TypeScript/package
  configuration on 2026-07-17. The module entry is now in scope.
- Partial P2 changes remain limited to the package/proto scaffold, generation
  workflow target, root TypeScript reference, failing test, and this log. No
  P1/P3, framework package, Firestore, or protected human-review files changed.

## 2026-07-17 - Runtime implementation and focused validation

- The authorized root `buf.yaml` module entry was added, and the generation
  workflow now stages handler registries for both existing example packages.
- `examples/project-management` now contains the generated Proto domain,
  direct bare-decorated public-API-only handlers, local server assembly, and a
  black-box client harness. The topology is explicitly registered as 3
  aggregates, 20 projections, and 10 process managers.
- The harness accepts only 10/25/50/100 independent asynchronous users. Each
  user opens an isolated HTTP/2 session and subscription, posts generated
  `CreateProject`, polls generated `ProjectSummary`, receives its first update,
  then aborts/returns the iterator and session in `finally`. Result metrics are
  success counts, acknowledgement/query/subscription p50/p95/p99, and
  throughput; they are diagnostic rather than an SLO.
- Focused build evidence: `pnpm typecheck:build` exited 0 after generation.
- Focused test evidence: the first sandbox run reached the test body but local
  listener binding failed with managed-sandbox `listen EPERM 127.0.0.1`; the
  identical command with approved local-loopback permission exited 0 with 4/4
  tests passing, including the real command/query/subscription test and a
  10-user asynchronous load scenario.
- Remaining focused validation: run the CLI harness at 25/50/100 users,
  format/lint the P2 files, and record actual immutable metadata limitations at
  final handoff.

## 2026-07-17 - Focused completion evidence

- Actual immutable metadata remains unavailable from this current execution
  context. The human explicitly retained the expected `gpt-5.6-terra` /
  `medium` dispatch. No child was spawned, so no child acceptance or inherited
  metadata was asserted.
- Repeated focused checks after final formatting and lint corrections:
  `pnpm typecheck:build`, `pnpm proto:lint`, `pnpm exec eslint
examples/project-management`, and `pnpm exec prettier --check
examples/project-management scripts/proto-workflow.mjs tsconfig.json
buf.yaml build-protocol/work-logs/T-0045.md` all exited 0.
- Final real loopback suite: `pnpm vitest run
examples/project-management/test/topology.test.ts` exited 0 with 4/4 tests.
  It proves the generated runtime exists, registration counts are exactly 3/20/10,
  a real command/query/subscription path works, and 10 independent asynchronous
  users complete with metric output. The command requires loopback permission;
  the non-escalated managed sandbox failure is recorded above.
- Forbidden end-user API scan found no matches for internal registry/discovery
  or direct repository construction terms under `examples/project-management`
  (generated output excluded).
- Compiled CLI load evidence (all status counts equal the requested users):
  - 10 users: acknowledgement/query/subscription `10/10/10`, throughput
    `23.88/s`.
  - 25 users: `25/25/25`, throughput `28.05/s`.
  - 50 users: `50/50/50`, throughput `28.90/s`.
  - 100 users: `100/100/100`, throughput `29.06/s`.
- The observed values are one local diagnostic run and intentionally are not
  represented as stable performance targets. No P3/Firestore or protected
  human-review file was changed. No commit, worktree, subagent, review child,
  or remote action was performed as directed.
- Review-lane disposition: no reviewer child was spawned because the human
  explicitly prohibited children for this bounded implementation session. The
  style, documentation, TypeScript/API, and reliability concerns instead have
  focused local evidence above; independent review remains pending orchestrator
  authorization if required before merge.

## Child dispatch contract

- Existing role: `requirements_splitter` for bounded-context and workload
  decomposition; expected `gpt-5.6-sol` / high; child returned no usable result
  and was rejected.
- Existing role: `implementer` for the example and tests; expected
  `gpt-5.6-terra` / medium; explicit dispatch was recorded, but the execution
  surface exposed no immutable child runtime metadata, so the result is not
  treated as protocol-grade child acceptance evidence.

## Splitter dispatch outcome

- `requirements_splitter` was explicitly dispatched as
  `gpt-5.6-sol` / high, but returned no usable result or immutable runtime
  metadata after repeated waits and was shut down. Its result is rejected by
  the acceptance gate. The orchestrator retains ownership of the bounded
  outline: one new example package, one domain proto set, public handlers and
  a bounded asynchronous client harness; no runtime/framework API changes.

## Worktree constraint

- `git worktree add` could not create a task ref because this environment
  denies writes to `.git/refs` (`Operation not permitted`). No branch or
  worktree was created. The implementation owner must therefore work in the
  current root only if the orchestrator explicitly accepts this managed-surface
  constraint; unrelated user-owned files remain excluded.

## 2026-07-17 - Review wave 1 findings

- Complete wave collected across style, documentation, TypeScript/API, and
  performance/reliability concerns. TypeScript/API was clean.
- Accepted fix batch: add `scripts/proto-workflow.test.mjs` fixture/assertions;
  make registry generation target metadata-driven; replace placeholder domain
  fan-out with credible named topology behavior or explicitly neutralize names;
  share private client/scenario helpers; add deadlines, all-settled cleanup,
  subscription correlation, and failure/cancellation coverage; reformat protos;
  narrow docs to implemented scope and expose the example from root README.
- Return the complete batch to the existing implementer. Dedicated security
  review remains out of scope under BUILD_PROTOCOL.

## 2026-07-17 - Wave 1 fixes and verification

- Fixed the workflow fixture and metadata-driven registry target configuration.
- Added explicit 3/20/10 topology inventory with event-specific registrations,
  shared client helper work was consolidated by removing the untyped helper,
  and the load runner now uses all-settled users, bounded RPC/iterator cleanup,
  first-update correlation, and independent subscription latency timing.
- Narrowed README claims to the implemented create-project scenario, documented
  unavailable mixed-workload/failure-injection phases, added metric definitions,
  and linked the example from the root README.
- Verification passed: `pnpm typecheck:build`, `pnpm proto:lint`, ESLint,
  TypeScript tooling check, Prettier, `scripts/proto-workflow.test.mjs` (9/9),
  native topology suite (4/4), and native 25/50/100-user scenarios with
  `failedUsers: 0` and 25/25/25, 50/50/50, 100/100/100 completion counts.
- P3/Firestore files were not touched. Generated output remains ignored.
- Reviewer profiles were dispatched explicitly, but this execution surface did
  not expose immutable actual model/reasoning metadata; results are recorded as
  provisional rather than falsely accepted as protocol-grade child evidence.
- Git task branch/worktree creation and remote synchronization remain blocked
  by managed `.git/refs` write denial. No commit was created.

## 2026-07-18 - Protocol closure

- Human explicitly granted permission to modify `.git`; the prior refs-write
  blocker is resolved.
- Sustained native loopback run completed for 331 seconds (5:31): 210 batches,
  rotating 10/25/50/100-user levels, 9,655 users, 9,655 command
  acknowledgements, 9,655 query visibilities, 9,655 subscription deliveries,
  zero failed users, zero launcher failures, and no runtime errors. Raw output
  is retained at `/private/tmp/spine-project-management-load-20260717.log`.
- The subscription metric limitation documented in the example remains an
  accepted diagnostic limitation: its timer can begin after the first update
  promise has been created, so it is not an end-to-end delivery SLO.
- Formal reviewer model metadata remains unavailable on this execution
  surface; the four canonical lanes have explicit local dispositions and no
  unresolved findings. No child result is represented as protocol-grade
  evidence.
- The task is ready for a dedicated branch commit, post-merge verification,
  and remote synchronization.
