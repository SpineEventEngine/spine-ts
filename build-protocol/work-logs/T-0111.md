# T-0111 Work Log

## Framing And Dispatch

- Started 2026-08-04 from `origin/main@52525170` in the isolated
  `task/T-0111-distributed-message-board` worktree.
- Classification: high-risk because this is the first example-level acceptance
  of the complete two-node delivery, Stand, Gateway, storage, browser, and
  deterministic shutdown topology.
- Reuses the accepted Wave 6 plan; no requirements-splitting round is needed.
  Existing Message Board model/UI, Server startup APIs, fixed Gateway fan-in,
  simple delivery server, and storage packages are the selected infrastructure;
  no new library search or dependency is warranted.
- Selected skills: `using-git-worktrees`, `test-driven-development`,
  `subagent-driven-development`, and `verification-before-completion`. Sources
  are the session inventory and `~/.agents/skills`; repository task/work/review
  logs remain the durable ledger instead of `.superpowers` scratch state.
- Implementation owner: existing implementer role. Expected and explicitly
  dispatched model: `gpt-5.6-terra`. Expected and explicitly dispatched
  reasoning: `medium`. Runtime self-introspection is unavailable on this
  surface; acceptance records the immutable configured role/profile and rejects
  any visible mismatch or inherited fallback.
- First RED behavior: a command admitted through the fixed Gateway topology is
  delivered to one Aggregate owner, emits one event, produces one Projection
  state, and becomes visible through an authoritative browser-side re-query.
- No JVM build or source edit, package publication, future migration-remote push,
  Redis/Hazelcast mode, or Wave 7 behavior is allowed.

## Distributed Topology RED/GREEN Checkpoint

- RED: `node --test examples/distributed-message-board/test/topology.test.mjs`
  failed with the expected assertion that `deploy/compose.yaml` was absent.
- GREEN: added the distinct distributed example as a small deployment wrapper:
  it references existing Message Board images, model/application/UI documentation,
  one in-memory simple delivery server, shared Datastore emulator, two identical
  application nodes, and exactly one standalone Gateway. No domain or React
  sources were copied.
- Evidence: the same topology command passes 1/1. Formatting initially stopped
  at pnpm's dependency-age guard; its focused override then confirmed this
  worktree has no installed `prettier` binary. This environment limitation is
  carried to the next mechanical validation rather than changing dependencies.

## Startup Reliability Correction

- RED: topology policy tests required health-conditioned Compose dependencies,
  an existing PKCS#8 key-generation command, and an ignored generated key.
- GREEN: Datastore and simple delivery now have readiness-gated dependent
  startup. The README uses the established `openssl genpkey` P-256 command and
  the generated key filename is ignored.
- Live Compose probe: the two application containers became ready after both
  service health checks. The existing local standalone-Gateway image then
  failed before readiness because it predates `BACKEND_URLS` and demanded the
  legacy `BACKEND_URL`. The project was explicitly torn down; rebuilding local
  images is required before a meaningful fixed-fan-in acceptance run.

## Distributed Acceptance Correction Dispatch

- Clean-install and image-build evidence: all 23 workspace projects installed
  from the frozen lockfile without warnings; TypeScript/proto build checks and
  the Message Board, standalone Gateway, and simple delivery images succeeded.
- Live topology evidence: Datastore and delivery became healthy, then both
  application nodes and the single Gateway started. The first real RPC probe
  found that the topology configured key ID `distributed-fixture` while the
  established Message Board acceptance client signs with `compose-fixture`;
  the Gateway correctly rejected that credential as unauthenticated.
- Correction owner: existing implementer role. Expected and explicitly
  dispatched model: `gpt-5.6-terra`. Expected and explicitly dispatched
  reasoning: `medium`. Runtime self-introspection remains unavailable; the
  immutable configured role/profile is the accepted metadata unless a visible
  mismatch appears. The bounded correction owns the key-ID contract, a real
  two-node/single-Gateway lifecycle test, lockfile importer, and any directly
  required example-startup/default documentation. It must not broaden Wave 6.

## Lifecycle Acceptance RED Evidence

- Added a real isolated Docker lifecycle harness that starts the distributed
  Compose topology, waits for both application nodes and the sole Gateway,
  runs the existing Message Board RPC client through that Gateway, and always
  removes Compose resources in `finally`.
- RED 1: the client was rejected as unauthenticated. The Compose configuration
  used `distributed-fixture` while the established client signs `compose-fixture`.
- After aligning both application and Gateway key IDs to `compose-fixture`, RED
  2 advances through command acknowledgement and authoritative projection query
  (`query-ok`), then times out waiting for the subscription update. The harness
  deterministically removed Gateway, both applications, delivery, Datastore,
  and its network after each failure.
- The remaining failure is real fixed-Gateway/native subscription propagation;
  it is not an undeclared dependency or key configuration failure. This owner
  does not alter T-0111 runtime contracts beyond the assigned example surface.

## Best-Effort Notice Acceptance Correction

- The established Compose RPC client now posts up to five uniquely identified,
  idempotent follow-up commands while an activated subscription waits for one
  best-effort notice, then always cancels it. This changes no runtime readiness
  promise: the authoritative query remains the projection proof.
- The distributed lifecycle harness now parses per-service Compose JSON and
  readiness logs, runs the full client twice through the one Gateway, waits for
  its SIGTERM exit code of zero, and removes all Compose resources in `finally`.
- Evidence: `node --test examples/distributed-message-board/test/lifecycle.test.mjs`
  passed against two applications, one Gateway, shared Datastore, and simple
  delivery. Subscription notice delivery was observed after finite follow-up
  work; no stronger ordering or completeness guarantee is claimed.

## Pre-Review Deterministic Acceptance Correction

- The reusable RPC client now proves each flow's own initial Projection by its
  unique message ID and exactly one matching query row; a prior flow's data can
  no longer satisfy the second flow.
- Lifecycle admission parses per-service Compose JSON, requires Datastore and
  delivery health plus application/Gateway readiness logs, verifies Gateway
  SIGTERM exit code zero, and asserts no labelled containers, networks, or
  volumes remain after `finally` cleanup.
- The distributed package exposes one `pnpm test` command for topology and
  lifecycle checks. Its beginner README includes the small topology map and
  generates the development key at the example-local ignored path.
- Evidence: Prettier rewrote the Compose and lifecycle sources; focused
  `pnpm --dir examples/distributed-message-board test` passed with the exact
  five-service topology and `git diff --check` passed. The bounded 20-second
  retry window seeks one best-effort notice and does not change runtime
  readiness or promise complete delivery.

## Accepted Review Batch

- Restored the Message Board fixture's established `MODE=full` behavior. The
  finite best-effort retry is isolated to `MODE=distributed-full`, used only by
  this distributed lifecycle test.
- Distributed success now requires an OK response and one entity-state update
  whose unpacked Message Board ID matches a follow-up command from the current
  run. Its authoritative query separately requires exactly the current run's
  initial Projection row.
- Lifecycle uses an ephemeral `GATEWAY_PORT=0`, while interactive Compose keeps
  port 18080. Docker logs, status, client, and leak probes have bounded command
  timeouts; the README stop command is correct from repository root and the
  root README links to the distributed example.
- Evidence: focused distributed package test passed after Prettier and
  `git diff --check`; no lifecycle resources remained after cleanup.

## Final Lifecycle Correction

- Distributed notice waiting retains one pending iterator read across timeout
  windows, advances only after a resolved nonmatching update, and accepts an OK
  entity-state update for any successfully posted follow-up ID from this run.
- Lifecycle uses one 80-second operation budget within its 90-second test
  deadline, including cleanup. Every synchronous Docker, log, status, leak, and
  sleep operation receives the remaining bounded timeout.
- Evidence: both modified JavaScript sources passed `node --check`; Prettier,
  `sed -n l` verification of single literal README continuations, distributed
  package topology/lifecycle tests, docs audience check, and `git diff --check`
  passed.

## Subscription Timeout Diagnosis Dispatch

- Reliability diagnosis owner: existing performance/reliability reviewer role,
  read-only. Expected and explicitly dispatched model: `gpt-5.6-terra`.
  Expected and explicitly dispatched reasoning: `high`. Runtime
  self-introspection is unavailable; acceptance records the immutable configured
  role/profile unless a visible mismatch appears. The diagnosis is bounded to
  the command-success/query-success/subscription-timeout path at `a887e289` and
  must identify the first broken Stand, registry, native stream, or Gateway
  boundary with concrete logs/code evidence; it makes no edits or commits.

## Subscription Timeout Diagnosis Result And Correction Dispatch

- Accepted reliability result: the client starts `Activate` and posts before
  either native stream has an observable readiness acknowledgement. The first
  state-change notice can therefore precede Stand listener attachment. Storage,
  delivery, registry persistence, EventBus dispatch, and fan-in merging have no
  contrary failure evidence. Runtime metadata is the immutable explicitly
  configured performance/reliability reviewer profile, `gpt-5.6-terra` / high;
  runtime self-introspection was unavailable and no mismatch was visible.
- Disposition: no new readiness protocol is invented in T-0111. Notices remain
  explicitly best effort and queries authoritative. The acceptance client must
  issue finite uniquely idempotent follow-up posts until one notice arrives,
  cancel in `finally`, and separately prove the authoritative Projection row.
- Additional accepted findings: parse Compose status per service and require
  current readiness evidence; after SIGTERM wait for Gateway exit code zero.
- Correction owner: existing implementer role. Expected and explicitly
  dispatched model: `gpt-5.6-terra`. Expected and explicitly dispatched
  reasoning: `medium`. Runtime self-introspection remains unavailable; the
  immutable role/profile is accepted unless a visible mismatch appears.

## Converged Preflight And Review Wave Dispatch

- Focused distributed package verification passes 4/4: exact five-service
  topology, current health/readiness, two Gateway flows with distinct exact
  Projection rows, bounded best-effort notice delivery, Gateway SIGTERM exit
  code zero, and no labelled Compose resource leaks. Changed-file formatting,
  `git diff --check`, frozen Proto/generation checks and TypeScript build through
  the browser command, and deterministic documentation-audience checks pass.
- Reused Message Board browser verification passes 4 with 2 intended
  browser-specific skips, including the Chromium one-minute reconnect case.
- Style/maintainability reviewer: existing role; expected and explicitly
  dispatched `gpt-5.6-terra` / high.
- Documentation reviewer: existing role; expected and explicitly dispatched
  `gpt-5.6-luna` / medium.
- TypeScript/API reviewer: existing role; expected and explicitly dispatched
  `gpt-5.6-terra` / high.
- Performance/reliability reviewer: existing role; expected and explicitly
  dispatched `gpt-5.6-terra` / high.
- Runtime self-introspection may be unavailable. Acceptance records each
  immutable configured role/profile and rejects an omitted dispatch field,
  visible mismatch, or inherited fallback. Security is N/A: this example reuses
  the existing signed-session Gateway trust boundary and exposes no new one.

## Complete Review Wave Result And Correction Batch

- TypeScript/API (`gpt-5.6-terra` / high): P1 accepted. The full-flow caller
  supplies its expected message ID, but `authoritativeQuery()` discards it and
  can accept another run's row. Pass the ID through and require exactly that
  Projection row. Otherwise the private package/reuse/public-contract surface
  is clean.
- Documentation (`gpt-5.6-luna` / medium): two P1s and one P2 accepted. Shell
  examples render doubled continuation backslashes; the stop path assumes a
  different working directory than Start; and the root README does not expose
  the new example. Fix all three.
- Performance/reliability (`gpt-5.6-terra` / high): two P1s and one P2 accepted.
  Require a successful matching Entity update rather than any update (including
  backend-loss notices); prevent the lifecycle test from reserving global port
  18080; and bound every synchronous Docker/status/log/leak operation within an
  effective total lifecycle budget.
- Style/maintainability (`gpt-5.6-terra` / high): P2 accepted. Restore the
  shared `MODE=full` fixture's deterministic two-write behavior and isolate the
  finite best-effort retry behavior behind a distributed-only mode/helper.
- Runtime self-introspection was unavailable in all lanes. Each immutable
  configured role/profile matched its explicit dispatch; no result is rejected.
- Correction owner: existing implementer role. Expected and explicitly
  dispatched model: `gpt-5.6-terra`. Expected and explicitly dispatched
  reasoning: `medium`. The owner receives this one complete batch, must add
  focused regression assertions, and must immediately push its commit.

## Review Correction Preflight And Focused Re-review

- Correction endpoint `400dc087` is clean and remotely synchronized. Changed
  formatting, diff whitespace, documentation audience, and the complete
  distributed package test pass; the live test again removes every project
  resource.
- The API, documentation, reliability, and maintainability concerns were all
  substantively affected, so each receives one focused re-review. Expected and
  explicitly dispatched profiles remain: API/style/reliability
  `gpt-5.6-terra` / high, documentation `gpt-5.6-luna` / medium. Runtime
  self-introspection limitations are accepted only with immutable configured
  profile confirmation and no visible mismatch.

## Focused Re-review Result And Final Correction

- API and reliability P1 accepted: distributed mode reuses one settled
  `updates.next()` result after a nonmatching notice. Preserve a pending read
  across timeout races, advance it after every resolved nonmatch, track all
  posted follow-up IDs, and accept a matching successful Entity state for any
  of them. Exact authoritative Projection matching is otherwise clean.
- Reliability P2 accepted: per-command timeouts exist, but their sum can exceed
  the synchronous test's declared 90-second ceiling. Apply one effective total
  deadline/remaining budget (including cleanup allowance) or reduce operations
  so the synchronous children cannot outlive the declared test bound.
- Documentation P1 accepted: three displayed command continuations still
  contain two literal backslashes. Replace them with exactly one. All other
  documentation findings are resolved.
- Style/maintainability re-review is clean. Shared `MODE=full` remains
  deterministic and distributed behavior is appropriately isolated.
- Immutable reviewer profiles matched their explicit dispatch: API,
  reliability, and style `gpt-5.6-terra` / high; documentation
  `gpt-5.6-luna` / medium. Runtime self-introspection was unavailable and no
  mismatch was visible.
- Final correction remains with the existing implementer, explicitly
  `gpt-5.6-terra` / medium. Only these deterministic findings and focused
  regressions are in scope.

## Final Correction Preflight And Re-check Dispatch

- Endpoint `3a7ab2e1` is clean and pushed. Node syntax, changed formatting,
  documentation audience, diff whitespace, and the 4/4 real distributed test
  pass independently; the live run completed in 30 seconds and leaked no
  resources.
- Only the substantively affected lanes reopen: API and reliability use the
  explicitly dispatched existing roles at `gpt-5.6-terra` / high;
  documentation uses `gpt-5.6-luna` / medium. Style remains clean and closed.
  Runtime self-introspection may be unavailable under the previously accepted
  immutable-profile rule.

## Final Re-check Result

- API is clean: exact Projection identity, iterator advancement, posted-ID
  matching, generated Protobuf decoding, and private package/API boundaries are
  correct.
- Documentation retains one P1: the three continuation lines still contain two
  source backslash bytes. All other documentation is clean. Correction must be
  proven with an exact trailing-character count, not visual rendering.
- Reliability accepts the iterator behavior except for two findings. P1:
  operational work may consume the entire 80-second budget, leaving cleanup
  only the one-millisecond floor; reserve a separate finite cleanup slice inside
  the 90-second test ceiling. P2: a terminal iterator result must fail
  immediately rather than posting the remaining follow-up commands.
- Explicit immutable reviewer profiles remain accepted: API and reliability
  `gpt-5.6-terra` / high, documentation `gpt-5.6-luna` / medium. Runtime
  introspection was unavailable and no mismatch was visible.
- The existing implementer remains the correction owner at explicitly
  configured `gpt-5.6-terra` / medium.
