# T-0085 Work Log

## 2026-07-31 — Framing and first review

- Created isolated branch/worktree from `main` at `9d8c2c76`.
- Added the eleven approved efficiency rules to `BUILD_PROTOCOL.md` and the
  execution summary in `AGENTS.md`.
- Changed-file Prettier and `git diff --check` passed.
- Release-readiness was not acceptance evidence in the fresh worktree because
  package `dist` outputs were absent; its diagnostics named only those missing
  artifacts.
- Documentation review found that named verification profiles and single-pass
  behavior must exist before the present-tense protocol claims can be accepted.
- Promoted the task from micro to standard shared-tooling scope.

## Assignment

- Implementer: `/root/t0085_implement`; existing `implementer` role,
  `gpt-5.6-terra` / medium. Runtime self-metadata was unavailable; the immutable
  configured profile is the available evidence.
- Expected ownership: `package.json`, `scripts/check-api-docs.mjs`, focused
  tooling tests, and task records. Protocol prose remains orchestrator-owned.

## Implementation and preflight

- Added executable task and release verification profiles; `pnpm verify`
  aliases the release profile.
- Release verification now reuses one Proto generation, one TypeScript build,
  one coverage-backed test run, and one TypeDoc generation.
- Added current-output generated policy checking so the release profile does
  not stage a second Proto generation; standalone freshness comparison remains.
- Made Buf resolution honor an explicitly supplied repository root, keeping
  generated-cleanliness fixtures isolated after a real dependency install.
- Independent preflight passed: build typecheck, tooling typecheck, 72 focused
  tests across three files, changed-file Prettier, and `git diff --check`.

## 2026-07-31 — Verification-profile implementation

- Implementer: existing `implementer` role. The dispatcher explicitly assigned
  `gpt-5.6-terra` / medium; runtime self-metadata is unavailable, so the
  immutable configured profile is the available acceptance evidence.
- Added `verify:task` and `verify:release`; `verify` now aliases release.
  Profiles generate Proto once and reuse those outputs. Release runs one
  TypeScript build, one coverage-enabled Vitest invocation, and one TypeDoc/API
  generation/check. Generated-output Buf lint preserves the existing Proto lint
  gate without repeating the source/checksum validation performed by generation.
- `verify:task` runs the shared deterministic gates and requires the caller to
  supply focused tests plus an explicit coverage choice for the affected scope.
- Behavior-first test: `scripts/package-metadata.test.mjs` failed red on the
  old `verify` command, then passed green (9 tests) after the profile change.
  `git diff --check` passed. `pnpm exec` could not run from this isolated
  worktree because it has no local dependencies; the parent-workspace Vitest
  executable was used for the focused test.
- Status: implementation is ready for the remaining mandatory preflight and
  concern-specific review wave. No release profile has been run yet, by design;
  it is reserved for one post-convergence invocation.

## 2026-07-31 — Single-pass review corrections

- Confirmed and corrected two command-composition defects: the profile used a
  generated-cleanliness command that staged a second generation, and the API
  checker redirected TypeDoc HTML into a temporary directory.
- Added `--current-output` to `check-generated-clean.mjs` and the corresponding
  `proto:check-generated:current` script. It preserves generated-directory
  safety and ignored/untracked checks for the outputs produced earlier in the
  same `&&` chain, without a second staging generation. The standalone checker
  still regenerates for independently invoked freshness comparison.
- Removed TypeDoc's temporary `--out` override from `check-api-docs.mjs`; its
  one invocation now writes `docs/api/reference` from `typedoc.json` while JSON
  remains temporary for assertions.
- Focused tests passed: `../../node_modules/.bin/vitest run
scripts/package-metadata.test.mjs scripts/check-generated-clean.test.mjs
--maxWorkers=1 --no-file-parallelism` (18 tests). Changed-file Prettier and
  `git diff --check` passed.
- Direct TypeDoc validation using `--options typedoc.json --json
/private/tmp/t0085-api.json` was blocked before output creation by 683
  pre-existing missing generated-module/dependency and build-resolution errors
  in the fresh worktree. This confirms the end-to-end check must follow the
  profile's generation/build steps; it does not contradict the focused command
  composition tests.

## 2026-07-31 — Fixture-isolation correction

- A real build installed worktree-local Buf and exposed a nondeterministic
  fixture boundary: `stageGeneratedTargets({ repoRoot })` resolved Buf and ran
  its default command from the source worktree rather than the supplied fixture
  root. The stale Chat-registry test then compared an ambient staged Proto
  output before the intended registry mismatch.
- `stageGeneratedTargets()` now resolves and runs Buf using the provided root.
  The default production root is unchanged, while fixture generation is fully
  isolated from ambient worktree tooling.
- Focused suite passed after the fix: 18 tests across package-metadata and
  generated-cleanliness tests.

## 2026-07-31 — Accepted review batch

- Added machine-enforced task verification choices: focused paths require an
  explicit coverage decision; only explicit `--no-tests` skips them for
  documentation/record-only work. Shared generated gates are now composed once
  by both profiles, with release adding one full coverage run.
- Passed the supplied root to Todo companion Buf resolution and added its
  fixture regression. Fake Buf fixtures now use platform-specific launchers and
  `node:path` delimiters.
- Focused validation passed: 75 tests across verification-profile,
  generated-cleanliness, and Proto-workflow tooling tests.
- Coverage-mode review correction: task coverage now requires explicit changed
  source paths after `--source` and passes them as Vitest `--coverage.include`
  arguments, so focused coverage does not evaluate unrelated production files.

## 2026-07-31 — Task-profile invocation correction

- A real `pnpm verify:task -- --no-coverage ...` invocation proved that pnpm
  forwards the conventional separator to the Node runner.
- The runner now accepts one leading separator while retaining strict mode,
  focused-path, and changed-source validation.
- The exact corrected invocation passed all shared deterministic gates and 75
  declared focused tests. TypeDoc generated the configured API reference once;
  Proto generation/checksum validation, Buf lint, current-output policy, and
  release-readiness all passed.
