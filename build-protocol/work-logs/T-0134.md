# T-0134 Work Log

## 2026-08-07 - Architecture And Contract Freeze

- Started from reviewed and pushed T-0133 commit `62fa1dc9` in the isolated
  `task/T-0134-mysql-layout` worktree.
- Existing requirements-splitter was explicitly dispatched at
  `gpt-5.6-sol` / `high`. Independent runtime self-introspection was unavailable;
  the immutable configured profile is the accepted metadata and no mismatch or
  fallback was visible.
- Froze the public builder/create-operation generics, JVM-style default/grouped
  naming, per-source table structure, compatibility inspection rules, query
  pushdown, engine matrix, and transactional/nontransactional commit semantics.
- No human decision or architecture blocker remains. No implementation or JVM
  build/edit was performed in this planning slice.

## 2026-08-07 - Implementation Dispatch

- One existing `implementer` owns all overlapping T-0134 production and test
  files. Its expected profile is explicitly `gpt-5.6-terra` / `medium`; both
  fields are present in the dispatch.
- The owner may create checkpoint commits only after a coherent slice passes
  its focused tests. The orchestrator pushes every such commit immediately.
- The owner must not spawn subagents, build or edit JVM code, touch Datastore or
  later Wave tasks, commit, push, or merge directly.

## 2026-08-07 - Provider Rewrite Redispatch

- The original implementer completed and checkpointed the public builder slice,
  then stopped after only confirming that the remaining provider is the legacy
  shared-table implementation. This is the accepted rewrite scope, not a
  technical blocker.
- A fresh existing `implementer` now owns the remaining one-table adapter,
  history/commit, live-matrix, test, and package-documentation work. Its
  expected profile remains explicitly `gpt-5.6-terra` / `medium`; both fields
  are present in the dispatch.

## 2026-08-07 - Cohesive Completion Redispatch

- The replacement owner established compiling per-family storage, generated
  history adapters, query evaluation, fresh tests, and a transaction
  coordinator, but repeatedly stopped after individual seams without a
  technical blocker.
- A fresh existing `implementer` owns the entire remaining cohesive integration:
  wiring all Entity/Event families through transactional and nontransactional
  commit paths, native SQL pushdown, complete focused/live tests, and package
  documentation. The expected profile is explicitly `gpt-5.6-terra` /
  `medium`; both fields are present in the dispatch.

## 2026-08-07 - MyISAM Key-Length Architecture Block

- Live MySQL 8.4.10 rejected the frozen composite primary key because its
  declared widths total 1,535 bytes while MyISAM permits at most 1,000 bytes
  (`ERROR 1071`). InnoDB live creation/query and one-connection Entity commit
  binding pass.
- No hash, prefix, or silent truncation workaround is accepted without checking
  current Spine JDBC record-key behavior and preserving exact row identity.
- The existing requirements-splitter is explicitly redispatched at
  `gpt-5.6-sol` / `high` for this demonstrated architecture block. It is
  read-only and may inspect current JVM source without building or modifying it.

## 2026-08-08 - Accepted Cross-Engine Primary Key

- Current Spine JDBC uses direct record IDs and no digest/hash/fingerprint key.
  The prior TS three-part key was therefore replaced, not hashed.
- The accepted table key is `_scope VARBINARY(224), ID VARBINARY(768)` with
  `PRIMARY KEY (_scope, ID)`. Its declared width is 992 bytes and its
  variable-value storage accounting is 996 bytes, below MyISAM's 1,000-byte
  limit.
- `_scope` is an exact length-delimited context/tenancy/tenant byte tuple.
  Scope or ID overflow fails before connection acquisition; no truncation,
  prefix, digest, normalization, or surrogate is permitted.
- The architecture recheck used the existing requirements-splitter explicitly
  at `gpt-5.6-sol` / `high`. Runtime self-introspection was unavailable; no
  visible mismatch or fallback occurred. No JVM build or edit was performed.

## 2026-08-08 - Failure-Injection Completion Dispatch

- The cohesive owner completed per-family storage, immutable collision rules,
  advisory locking, and basic live MySQL/MariaDB coverage, but stopped before
  trigger-injected Entity-commit rollback/prefix/retry conformance.
- A fresh existing `implementer` owns only this bounded completion plus any
  minimal provider-private test seam. The expected profile is explicitly
  `gpt-5.6-terra` / `medium`; both fields are present in the dispatch. No
  public fault API is permitted.

## 2026-08-08 - Coverage Completion Dispatch

- Final deterministic behavior and live-engine gates pass, but focused source
  coverage with live InnoDB is 78.75% statements and 69.29% branches. The
  shortfall is concentrated in Entity history/retention, resolver validation,
  and factory error/precedence behavior.
- One existing `implementer` is explicitly dispatched at
  `gpt-5.6-terra` / `medium` to add behavior tests without excluding sources,
  lowering thresholds, or changing public semantics. Both dispatch fields are
  explicit.

## 2026-08-08 - Coverage Completion Progress

- Branch-coverage completion continuation is owned by the existing `implementer`,
  explicitly dispatched at `gpt-5.6-terra` / `medium`. The execution surface
  does not expose independent runtime-model introspection; the immutable
  dispatch profile is the available evidence, with no visible mismatch or
  fallback. Scope is limited to `storage-rdbms` behavior tests and this log;
  no JVM, commit, push, merge, or production refactor is authorized.
- Acceptance criterion: live InnoDB package coverage reaches at least 90% for
  statements, branches, functions, and lines without exclusions, threshold
  changes, or assertion-free tests. The exact baseline is 90.05% statements,
  81.88% branches, 90.96% functions, and 92.46% lines (37 tests). The lcov map
  requires at least 31 more covered branch arms across the package.
- Added behavior-focused cases for nested Entity-family connection binding,
  disabled-history permutations, malformed records, retention boundaries,
  record CAS rollback, query SQL windows, oversized keys, builder overloads,
  optional connection configuration, and closed/cross-scope/conflicting/
  identifier-less Entity commits. The nontransactional test runs a real MyISAM
  commit with both populated and absent optional immutable families, and always
  restores all affected shared tables to InnoDB in `finally`.
- Evidence: the live InnoDB focused suite passes 6 files / 46 tests with
  `--coverage --coverage.include='packages/storage-rdbms/src/**'`: 93.76%
  statements (556/593), 90.55% branches (345/381), 92.16% functions (153/166),
  and 95.85% lines (509/531). No source exclusions, threshold changes,
  assertion-free tests, or production changes were used. Focused validation
  and task preflight remain in progress.
- Final focused validation: `tsc -p packages/storage-rdbms/tsconfig.json
--noEmit`, exact ESLint for changed tests, changed-file Prettier, `git diff
--check`, and repository TSDoc enforcement all exit clean. The final live
  InnoDB coverage rerun after formatting remains 6 files / 46 tests and
  93.76% statements, 90.55% branches, 92.16% functions, and 95.85% lines.
- Limitation: the full `verify:task` profile remains blocked before focused
  test execution by pre-existing, out-of-scope root build errors in Datastore,
  server, deployment, and Message Board packages. This slice did not alter
  those packages. No commit, push, or merge was performed by this implementer.

- Runtime metadata limitation: the execution surface does not expose
  independent model introspection. The immutable dispatched `implementer`
  profile is `gpt-5.6-terra` / `medium`; both fields were explicit and no
  visible fallback or mismatch occurred.
- Added behavior coverage for resolver precedence/default/last-wins/invalid
  names and for disabled Entity histories, backward version filtering,
  `stateAt`, trim, event truncate, and handle close. No production behavior
  changed and therefore no production bug fix was needed.
- Focused checks pass: resolver suite 4/4; Entity coordinator/history suite
  6/6; `tsc -p packages/storage-rdbms/tsconfig.json --noEmit` exits 0.
- Live-InnoDB package suite with trigger administration (`root:root` on the
  task Docker fixture) passes 6 files / 30 tests. Its package-source coverage
  is currently 82.12% statements, 75.85% branches, 83.13% functions, and
  84.93% lines. Coverage remains below the required 90% gate, concentrated in
  `entity-history.ts`, `record-storage.ts`, and `storage-factory.ts`; further
  behavior tests are still required. No source exclusions or threshold changes
  were made.
- The selected `verify:task -- --coverage ... --source ...` profile is blocked
  before its focused test phase by unrelated root `tsc -b` errors in
  `packages/storage-datastore`, `packages/server`, `packages/deployment`, and
  Message Board sources that still use removed `RecordSpec.schema` and legacy
  Entity-history contracts. The assigned `storage-rdbms` typecheck remains
  green; no out-of-scope repair was made.
- Continued behavior coverage adds public record read/delete/query/CAS/batch
  operations, Entity prepare/bind/preflight, disabled trim/truncate, missing
  historical state, parsed factory connection/failure/close behavior, and
  coordinator rollback/lock-failure behavior. The latest live package run
  passes 6 files / 36 tests at 89.20% statements, 80.57% branches, 90.96%
  functions, and 91.71% lines. Branch coverage remains below the 90% gate;
  no exclusions or thresholds were changed.
- A coverage-map-guided native-column test now proves BOOLEAN, DOUBLE, BIGINT,
  MEDIUMBLOB, and JSON-safe object parameter binding through the family table.
  The latest live focused run passes 6 files / 37 tests and reaches 90.05%
  statements, 81.88% branches, 90.96% functions, and 92.46% lines. The only
  remaining aggregate gate failure is branch coverage; no source exclusions,
  threshold changes, or production changes were made.

## 2026-08-07 - Builder Contract RED/GREEN

- Runtime metadata: the existing `implementer` assignment explicitly requires
  `gpt-5.6-terra` / `medium`. This surface does not expose independent runtime
  model introspection; the configured profile is the available evidence and no
  mismatch or inherited-profile fallback is visible.
- RED: `packages/storage-rdbms/test/mysql-builder-contract.test.ts` failed with
  `MysqlStorageFactory.newBuilder is not a function` after generated Proto
  artifacts were restored by the frozen workflow.
- GREEN: the package exports the frozen `MysqlStorageFactoryBuilder`,
  `CreateOperationFactory`, `MysqlCreateOperation`, `MysqlTableSpec`, and
  `MysqlColumnSpec` contracts. `MysqlStorageFactory.newBuilder()` is present
  and the static `create` alias is absent. The builder preserves last-call-wins
  registration state and rejects a missing options value before connection.
- Evidence: `pnpm --config.verify-deps-before-run=false exec vitest run
packages/storage-rdbms/test/mysql-builder-contract.test.ts` passes 1/1.
  `pnpm proto:generate` passed 49 frozen-source checksums and 52 frozen
  descriptor checks.
- Limitation: package TypeScript checking remains red exclusively across the
  legacy RDBMS provider implementation, which still imports T-0133-removed
  history DTOs and accesses removed `RecordSpec.storageKey/schema`, receipt,
  fingerprint, and legacy Entity input members. Replacing that provider layout
  is the remaining T-0134 implementation, not a generated-artifact issue.

## 2026-08-07 - Provider Rewrite Replacement Checkpoint

- Runtime metadata: the replacement existing `implementer` was explicitly
  assigned `gpt-5.6-terra` / `medium`. The execution surface exposes no
  independent runtime-model introspection; the immutable configured assignment
  is the available evidence, with no visible inherited-profile fallback.
- RED/GREEN: `mysql-table-spec.test.ts` first failed because
  `MysqlTableResolver` did not exist, then passed after the resolver was added.
  It proves stable source/group resolution, separation of named groups, and
  required context, tenant, ID, payload, and revision columns.
- Replacement: removed the legacy shared-record/schema-key implementation
  source and introduced separate table-spec, table-resolver, and generic
  record-storage modules. Each family now resolves one `spine_*` table and
  lazily creates/inspects its fixed required columns. The generic adapter
  implements read, delete, write, writeAll, and transactional CAS over that
  family table.
- Evidence: `pnpm --config.verify-deps-before-run=false exec tsc -p
packages/storage-rdbms/tsconfig.json --noEmit` exits 0;
  `pnpm --config.verify-deps-before-run=false exec vitest run
packages/storage-rdbms/test/mysql-table-spec.test.ts` passes 1/1;
  `git diff --check` exits 0.
- Remaining limitation: Entity current/history/commit is intentionally still a
  skeletal replacement seam and query filters/sorts/continuations plus live
  engine-matrix coverage are not implemented. The existing legacy mock and
  integration tests describe deleted shared-table behavior and require their
  own RED-first replacement. No completion, commit, push, merge, or JVM work
  was performed.

## 2026-08-07 - Replacement Continuation

- Generic record reads now decode both primitive and Protobuf IDs, and direct
  `RecordQuery` IDs, filters, sorting, offsets, limits, and continuations are
  evaluated after a tenant/family table read. This preserves behavior while the
  native-column SQL pushdown remains to be added.
- Generated `EntityRecord`, state-history, and diagnostic-event family adapters
  are wired through the new record-family resolver. Current reads/writes and
  enabled history append/read/retention methods use those generated records.
- The provider-owned commit handle now applies current/history/diagnostic work
  through the generated adapters and returns a current-record conflict before
  mutation. It is not yet a single database transaction and does not yet add
  framework delivery events; this is a known correctness gap, not accepted
  behavior.
- Continuation evidence: TypeScript check exits 0; builder-contract and
  table-spec focused tests pass 2/2; `git diff --check` exits 0. Live Docker
  matrix and legacy-test replacement have not run.
- Legacy-suite evidence: `mysql-storage-factory.test.ts` currently fails before
  executing a test because its fixture imports the intentionally removed
  `entityHistorySchema.tables`. That stale shared-schema test must be replaced
  by per-family behavior tests before it can validate this implementation.

## 2026-08-07 - Legacy Fixture Replacement

- Removed the obsolete shared-schema mock suite and replaced it with a focused
  per-family record-storage behavior test. It asserts lazy creation of the
  resolved `spine_*` family table before the first write and then the matching
  parameterized insert path.
- Evidence: builder contract, table resolver, and record storage tests pass
  3/3; `storage-rdbms` TypeScript check exits 0. The former legacy suite is no
  longer an import-time blocker.
- Remaining: RED-first transactional Entity/delivery and nontransactional
  prefix/retry tests, native-column SQL pushdown, live engine matrix, and
  documentation are still required before review.

## 2026-08-07 - Commit Transaction RED/GREEN

- RED: `mysql-entity-commit-contract.test.ts` failed because the transaction
  coordinator module did not exist.
- GREEN: `MysqlEntityCommitCoordinator` owns one acquired connection, begins a
  transaction, commits on success, rolls back on error, and releases exactly
  once. Its contract test passes and asserts no receipt-table SQL.
- Evidence: focused coordinator test passes 1/1 and the package TypeScript
  check exits 0.
- Limitation: the coordinator has not yet been threaded into all Entity
  current/history/delivery writes, so this does not establish atomic Entity
  commit semantics or the nontransactional advisory-lock protocol.

## 2026-08-07 - Cohesive Completion Continuation

- Runtime metadata: this existing implementer assignment was explicitly
  `gpt-5.6-terra` / `medium`; the surface does not expose independent runtime
  model metadata, with no visible fallback or mismatch.
- RED/GREEN: added a record-family test which initially failed at the old
  generic layout boundary. The adapter now creates the frozen `_context`,
  `_tenant`, `ID`, `bytes`, and `_revision` layout, stores declared columns as
  nullable native SQL values, and emits parameterized declared-column equality
  predicates plus SQL ordering/windowing rather than evaluating basic record
  queries after a full scan.
- Default family naming now follows the frozen source-name convention, and
  builder registrations validate names and reject duplicate physical names.
  Package README and REFERENCE now use `newBuilder()` and no longer promise
  shared tables, fingerprints, or receipts.
- Evidence: package typecheck exits 0; four focused suites pass 5/5; the full
  package test directory passes 13/13 with 17 opt-in integration cases skipped;
  `git diff --check` exits 0.
- Remaining limitation: custom creation operations are retained by the builder
  but are not yet executed; schema inspection remains column-presence-only;
  Entity commits have not yet been threaded through one transaction or the
  nontransactional advisory-lock/prefix protocol; live Docker engine matrix is
  not run. These are open T-0134 requirements, not accepted completion.

## 2026-08-07 - Creation, Inspection, And Live Harness Continuation

- RED/GREEN: a supplied create-operation was observed failing against default
  DDL, then passes after the resolved `MysqlTableSpec` callback is carried from
  the builder into lazy family creation. The callback is run through the
  provider's private connection.
- Schema inspection now checks required columns, the exact primary-key order,
  and supported engine names without any `ALTER` path. A focused incompatible
  primary-key test fails before the check and passes after it.
- Replaced the obsolete skipped shared-table integration assertions with a
  per-family live harness. Docker MySQL `8.4.10` was started as
  `spine-t0134-mysql` on localhost port 33316. The live suite initially found
  a mysql2 prepared-statement binding defect in SQL query execution; the bound
  query path now passes 2/2 against the server.
- Evidence: package typecheck passes; record-storage focused tests pass 4/4;
  live command `SPINE_TS_MYSQL_URL=mysql://spine:spine_test@127.0.0.1:33316/spine_t0134 pnpm --config.verify-deps-before-run=false exec vitest run packages/storage-rdbms/test/mysql-integration.test.ts` passes 2/2.
- Remaining limitation: primary-key/engine inspection is not yet the full
  type/nullability/collation/extra-constraint acceptance matrix. Entity commit
  now includes framework delivery records and durable prefix ordering, but is
  still not a one-connection InnoDB transaction and does not implement the
  MyISAM/Aria advisory-lock retry protocol or live matrix.

## 2026-08-07 - Transaction Binding And Engine-Matrix Blocker

- The Entity coordinator now binds current, state-history, diagnostic-history,
  and delivery-event family handles to its one acquired connection while the
  coordinator transaction is active. Focused coordinator/record tests pass
  5/5 and the MySQL InnoDB live suite still passes 2/2.
- Concrete MyISAM blocker: the frozen exact primary key is `_context
VARBINARY(512), _tenant VARBINARY(255), ID VARBINARY(768)`, or 1,535 bytes
  before MyISAM index overhead. MySQL 8.4.10 rejects the actual family table
  conversion with `ERROR 1071 (42000): Specified key was too long; max key
length is 1000 bytes`. A prefix or hash key would violate the frozen exact
  primary-key contract, so MyISAM's required layout cannot be created.
- Attempted MariaDB `11.4` image acquisition did not complete in this Docker
  environment. This is secondary to the MyISAM physical-layout contradiction.
- No MyISAM/Aria advisory-lock/retry claim is accepted, and no final completion
  or commit is made while the frozen requirements conflict.

## 2026-08-08 - Scope-Key Amendment Implementation

- RED/GREEN: a scope alias test failed against the former three-column layout,
  then passed after replacing it with `_scope VARBINARY(224), ID
VARBINARY(768)` and `PRIMARY KEY (_scope, ID)`. The provider encodes the
  amended injective tuple (u32be UTF-8 context length, context bytes,
  single/multi tag, and for multitenancy u32be tenant length and tenant bytes).
  It rejects oversized scope and canonical IDs before record operations reach
  a connection.
- Updated SQL predicates, writes, custom table specifications, inspection, and
  resolver metadata to `_scope`; the former primary-key shape is rejected.
- Live evidence: Docker MySQL 8.4.10 InnoDB suite passes 2/2. After a test-only
  `ALTER TABLE ... ENGINE=MyISAM`, the same suite passes 2/2 against MyISAM,
  confirming the exact 992-byte key is admitted.
- MariaDB 11.4 image acquisition was retried with `docker pull mariadb:11.4`
  and did not produce an installed image, so MariaDB InnoDB/Aria remains an
  external Docker-image availability limitation. Nontransactional advisory
  lock/prefix/retry behavior is still not accepted as complete.

## 2026-08-08 - Advisory Lock Continuation

- RED/GREEN: `MysqlEntityCommitCoordinator.lock()` now uses parameterized
  `GET_LOCK` and always attempts `RELEASE_LOCK`; its focused contract asserts
  the exact server calls. Entity commits take this lock using context, tenant,
  Entity source, and Entity ID before their coordinated write sequence.
- Evidence: package typecheck passes; package tests pass 17/17 with 2
  URL-gated live cases skipped; explicit Docker MySQL live suite passes 2/2.
- Limitation: injected mid-prefix failure, identical retry/lost acknowledgement,
  and divergent immutable-collision cases have not yet been modeled as focused
  Entity-commit conformance tests. The advisory primitive alone is not recorded
  as full nontransactional protocol acceptance.

## 2026-08-08 - Immutable Prefix And MariaDB Matrix

- Added provider-private `MysqlRecordStorage.writeImmutable()`: it probes the
  concrete family key, inserts an absent row, leaves canonical-byte-identical
  rows untouched, and rejects divergent bytes. Entity state history,
  diagnostic history, and Event Store delivery now use this immutable path in
  their required prefix order before current state.
- MariaDB 11.4 was started from digest
  `sha256:67873d30a17f6a9c331f06363b2fa15f38abca415529966d67c84f87f82439fe`.
  The explicit live suite passes 2/2 on MariaDB InnoDB and 2/2 after a
  test-only conversion to Aria. Existing Docker MySQL InnoDB and MyISAM live
  runs also pass 2/2 each.
- Evidence: package typecheck and package tests pass 17/17 with two URL-gated
  cases skipped under the default environment; focused live runs are recorded
  above. No receipt, fingerprint, or marker SQL exists in the provider source.
- Remaining test limitation: there is not yet a failure-injecting Entity commit
  fixture that observes every durable prefix boundary; primitive-level lock and
  immutable-write behavior are covered, while end-to-end injection remains to
  be added before final acceptance.

## 2026-08-08 - Failure-Injection Continuation (Unaccepted)

- Added a URL-gated live Entity-commit fixture using the provider commit handle
  and the actual current, state-history, diagnostic-history, and Event Store
  family tables. It installs a `BEFORE INSERT ... SIGNAL SQLSTATE '45000'`
  trigger at each physical boundary and drops every trigger in `finally`.
- RED exposed a real coordinator defect: unaliased `GET_LOCK(?, ?)` is returned
  by mysql2 under an expression key rather than `acquired`, so every lock was
  rejected. The private coordinator SQL now aliases it as `acquired`; its
  focused contract was updated accordingly.
- The coordinator now has an engine-aware one-connection `commit()` path and
  the record adapter exposes provider-private immutable preflight. This is the
  intended basis for InnoDB rollback and MyISAM/Aria prefix/retry behavior; it
  does not add a public fault API.
- Current blocker: after the lock correction, the MySQL 8.4 InnoDB fixture
  passes the first two existing live cases but the four-boundary commit loop
  exceeds Vitest's 30-second test timeout and leaves the runner active until
  reaped. Therefore no boundary/engine result is accepted and no completion,
  commit, push, merge, or review-ready status is claimed from this slice.

## 2026-08-08 - Failure-Injection Hang Investigation

- Bounded one-boundary MySQL run with `T0134_DEBUG=1` traced coordinator
  acquire/engine decision, lazy table create/inspect, immutable preflight, and
  the first immutable write. It stalls after `immutable write:start
google_protobuf_StringValue_StringValue`, before that insert completes.
- The same trace proves this was not an InnoDB transaction: the coordinator
  reported `transactional=false rows=4`. Direct `information_schema.TABLES`
  inspection showed the shared `google_protobuf_StringValue` current table was
  MyISAM, while the other actual families were InnoDB; no temporary trigger
  remained and post-timeout `SHOW PROCESSLIST` was clean.
- Root-cause hypothesis (one only): global default family table names let a
  prior MyISAM matrix conversion contaminate the later run labelled InnoDB.
  The fixture must assert and establish the requested engine on its actual
  resolved tables before its seed commit. This is not yet fixed or accepted.

## 2026-08-08 - Remaining Correction Batch Progress

- Owner: existing `implementer`, explicitly configured `gpt-5.6-terra` /
  `medium`. The surface does not expose independent runtime model metadata;
  the immutable profile is the available evidence, with no visible fallback.
- RED/GREEN: oversized query IDs and CAS IDs initially acquired three pool
  connections. They now fail before acquisition; focused storage tests pass.
  Entity handles now expose `isOpen()`, close current plus enabled history
  handles once, and deregister from the factory on close. Coordinator-only
  `transaction()` and `lock()` test APIs were removed; tests use production
  `commit()`.
- Public schema/decode/operation failures now cross as the corresponding
  sanitized provider classes. User guide, server conformance factory creation,
  API-export inventory, README, and REFERENCE were updated for builder-only,
  lazy per-family tables, no fingerprints, custom operations, and engine
  semantics.
- Focused evidence: non-live `storage-rdbms` suite passes 66 tests with 6
  URL-gated skips; package `tsc --noEmit`, Prettier, and `git diff --check`
  pass. The first live rerun uncovered resolver reuse after close; resolving
  ungrouped tables by source identity corrected that failure.
- Current blockers: live MySQL rerun still has two failures: the injected
  trigger is installed on a hard-coded table that is no longer necessarily the
  actual resolved family table, and concurrent InnoDB commits can deadlock in
  immutable-prefix writes. Non-live coverage is 81.61% statements, 81.98%
  branches, 75.91% functions, and 82.69% lines, below the required 90% gate.
  The TSDoc gate is also blocked by pre-existing `table-spec.ts` documentation
  defects plus internal-helper documentation. No commit, push, or merge was
  performed.

## 2026-08-08 - Remaining Correction Batch Converged

- Live failure diagnosis found two provider defects. Trigger injection used
  guessed legacy names instead of resolved family handles; the fixture now
  obtains Entity and Event Store table names from provider-created handles.
  Concurrent absent-row InnoDB commits can receive `ER_LOCK_DEADLOCK` after
  MySQL rolls the entire transaction back. The coordinator retries exactly once
  only for that error; a deterministic coordinator test proves rollback then
  successful retry, and the live conflicting-commit test proves one committed
  and one conflict result.
- The canonical table-spec and internal helper TSDoc was completed. Focused
  checks pass: live MySQL suite 73/73; coverage 94.15% statements, 90.62%
  branches, 93.78% functions, and 96.08% lines; storage-rdbms TypeScript;
  exact changed-source ESLint; TSDoc enforcement; TypeDoc/API inventory;
  documentation audience check; Prettier; and `git diff --check`.
- No commit, push, or merge was performed by this implementation owner.

## 2026-08-08 - Wave 2 Correction Progress

- Owner metadata: existing `implementer`, explicitly `gpt-5.6-terra` / medium.
  This surface exposes no independent runtime profile; no fallback is visible.
- Corrected canonical primitive ID storage/recovery through `CanonicalMysqlValues`,
  zero-sort ID continuations, typed malformed payload reads, retryable failed
  readiness, non-InnoDB per-key CAS advisory serialization, private factory
  connection construction, and single table-layout specification.
- Focused TypeScript and live integration checks are in progress. The all-package
  non-live rerun exposed a stale resolver-layout assertion after intentionally
  removing `MysqlResolvedTable.columns`; that assertion was updated. No commit,
  push, or merge occurred.

## 2026-08-08 - Wave 2 Typed Capability Progress

- `MysqlEntityStorage` now holds concrete private MySQL family stores and
  supplies one private `EntityFamilyCommitCapability`; the commit façade no
  longer structurally casts public history ports into provider operations.
  Factory construction carries exact Entity generics rather than the former
  `unknown` constructor casts.
- Live MySQL focused suite passes 73/73 and package TypeScript passes. The
  coverage rerun is not accepted: branches are 88.86% after the capability
  paths were introduced, below the 90% requirement. Exact changed-source lint
  needed only unused-import cleanup and remains to be rerun with the final
  coverage evidence. No commit, push, or merge was performed.

## 2026-08-08 - Wave 2 Coverage And Gate Evidence

- Behavior-focused coverage now passes without exclusions: live MySQL suite
  73/73, 94.22% statements, 90.16% branches, 93.71% functions, and 96.05%
  lines. Added optional Entity-family capability behavior and MyISAM keyed-CAS
  lock success/rejection coverage.
- Package TypeScript, changed-source lint, TSDoc, Prettier, and diff checks
  pass. Repository API/audience execution remains blocked upstream by 89
  unrelated storage-datastore/server/deployment TypeScript errors that use
  removed RecordSpec and Entity-history contracts. No commit, push, or merge.

## 2026-08-08 - T-0134 Cleanup Completion (Awaiting Live Rerun And Review)

- Removed all T-0134 production/test diagnostic tracing and the no-op trace
  helper. No `T0134_DEBUG`, trace, or queue-diagnostic behavior remains in the
  provider sources or tests.
- RED/GREEN: a new advisory-key test first failed because the provider did not
  expose a key derivation seam. `mysqlEntityLockKey()` now derives the exact
  64-character lowercase SHA-256 hexadecimal key from context, optional
  tenant, source type, and entity identity. The focused contract also proves
  different identities differ and that `RELEASE_LOCK` is attempted after a
  nontransactional work failure.
- Added immutable-preflight coverage: a divergent record rejects before an
  `INSERT` prefix write. Existing no-receipt SQL coverage remains explicit;
  provider source contains no receipt or marker table/SQL/symbol.
- Evidence: exact changed TypeScript ESLint is clean; package `tsc --noEmit`
  is clean; focused tests pass 11/11; the non-live package suite passes 20
  tests with 3 URL-gated live tests skipped. `git diff --check` is clean.
- Focused coverage was invoked through `verify:task` with the six changed
  MySQL source paths, but the task profile stops at repository-wide `tsc -b`
  on out-of-scope legacy failures in `packages/deployment`, `packages/server`,
  and `packages/storage-datastore`. `docs:api:check` is blocked by that same
  build. `lint:tsdoc` also reports pre-existing global failures plus missing
  broad TSDoc coverage in the prior T-0134 provider rewrite; this cleanup made
  no public-contract expansion beyond the documented advisory-key test seam.
- Prettier was applied to every assigned T-0134 changed file. No live shell
  matrix was run in this cleanup; the next action is the orchestrator's
  requested live rerun followed by review. No commit, push, merge, JVM, or
  out-of-scope package change was made.

## 2026-08-08 - Public TSDoc Correction

- Runtime metadata: this bounded existing `implementer` assignment was
  explicitly configured as `gpt-5.6-terra` / `medium`. The execution surface
  does not expose independent runtime-model introspection; the configured
  profile is the available evidence, with no visible mismatch or inherited
  fallback.
- Corrected every T-0134 MySQL provider TSDoc finding. Exported options,
  builder contracts, table specifications, error classes, Entity storage
  handles, and provider-private storage/commit helpers now use multiline
  blocks with summaries, callable parameter and return tags, and constructor
  parameter documentation. README and REFERENCE claims remain accurate for
  the current `newBuilder().setOptions(...).build()` API and independent
  record-family tables; no broad prose rewrite was needed.
- The deleted `mysql-storage-factory.test.ts` was retained as a staged
  deletion, not restored. This removes the checker path-confinement report
  caused by its obsolete tracked path and introduces no replacement source or
  configuration change.
- Evidence: exact changed-source ESLint passed; `tsc -p
packages/storage-rdbms/tsconfig.json --noEmit` passed; package Vitest passed
  20 tests with 3 URL-gated live tests skipped; `git diff --check` passed.
  `node scripts/check-tsdoc.mjs packages/storage-rdbms` reports only the
  pre-existing T-0133 baseline `packages/storage/src/memory/canonical-utf8.ts
:: block@5`, outside this package.
- The localized established `// prettier-ignore` pattern preserves required
  first nested TSDoc blank lines. Direct Prettier checks of all six corrected
  MySQL source files and the package TSDoc check now coexist; the latter still
  reports only the inherited T-0133 `canonical-utf8.ts` baseline outside this
  package. No runtime behavior, test behavior, JVM code, commit, push, or
  merge was changed in this documentation correction.

## 2026-08-08 - Review-Wave Correction Continuation

- Runtime metadata: this existing implementation-owner assignment is explicitly
  configured `gpt-5.6-terra` / `medium`. The surface does not expose
  independent runtime-model introspection; the immutable configured profile is
  the available evidence and no fallback is visible.
- RED/GREEN: a record-query continuation contract first demonstrated that the
  emitted SQL lacked a keyset predicate. `MysqlRecordStorage.querySql()` now
  emits lexicographic continuation predicates, direction-aware comparisons,
  and the ID tie breaker inside the existing tenant scope predicate.
- Evidence: `mysql-record-storage.test.ts` passes 12/12 and
  `tsc -p packages/storage-rdbms/tsconfig.json --noEmit` exits 0.
- Remaining accepted findings include cross-handle CAS/commit concurrency,
  full compatibility inspection, immutable collision races, lifecycle/API
  boundary corrections, engine-matrix behavior, and final documentation/API
  verification. No completion claim is made.

## 2026-08-08 - CAS Serialization RED/GREEN

- RED: the new cross-handle CAS unit contract observed an unlocked `SELECT`
  before mutation.
- GREEN: CAS now reads the candidate row with `FOR UPDATE` inside its existing
  transaction before comparing or writing it. The focused record-storage suite
  passes 13/13 and package TypeScript checking exits 0.
- Limitation: this covers an existing row. The absent-row conflict and the
  Entity multi-family commit protocol remain open P0 corrections.

## 2026-08-08 - Immutable Insert-Only Continuation

- Replaced immutable write's read-then-upsert with `INSERT IGNORE`; an
  insertion race now rereads and accepts only canonical-byte-identical content,
  rejecting divergent content without an overwrite update.
- Evidence: record-storage tests remain 13/13 and package TypeScript checking
  exits 0.
- Limitation: the deterministic cross-handle collision race and all-InnoDB
  multi-family Entity-commit concurrency tests are still required before this
  P0 correction can be accepted.

## 2026-08-08 - Immutable Collision Race Coverage

- Added deterministic provider-path coverage for a losing `INSERT IGNORE` race:
  the first immutable probe observes absence, the insert loses, the reread
  observes divergent canonical bytes, and the operation rejects without any
  overwrite clause.
- Evidence: `mysql-record-storage.test.ts` passes 14/14.
- Remaining P0 scope: identical concurrent collision and all-InnoDB Entity
  multi-family serialization, including an absent current row.

## 2026-08-08 - Identical Immutable Collision Coverage

- Added the paired losing-insert race: after `INSERT IGNORE` reports a
  collision, a canonical-byte-identical reread resolves successfully rather
  than rewriting the row.
- Evidence: `mysql-record-storage.test.ts` passes 15/15 and package TypeScript
  checking exits 0.
- Remaining: all-InnoDB Entity multi-family serialization including absent
  current rows, followed by the outstanding P1/P2/P3 review corrections and
  final live verification.

## 2026-08-08 - Coordinator-Bound Current Locking

- Entity commits now obtain current state through a provider-private
  `readCurrentLocked()` path while all participating family handles are bound
  to the coordinator connection. The underlying exact-key read uses `FOR
UPDATE`, covering the current-row/gap lock in the InnoDB transaction.
- Evidence: package TypeScript check exits 0; entity-commit contract tests pass
  11/11.
- Limitation: deterministic cross-handle live concurrent commits (absent and
  shared expected-current cases) still need to prove exactly one conflicting
  writer commits before accepting this P0 item.

## 2026-08-08 - Cross-Handle InnoDB Commit Contract

- Added URL-gated integration coverage with two independently opened Entity
  commit handles racing on an absent current record with divergent next state.
  It asserts one `committed`, one `conflict`, and a readable durable current
  record after the race.
- Evidence: package TypeScript checking exits 0. The integration file compiles,
  but its six URL-gated cases are skipped without `SPINE_TS_MYSQL_URL`; no live
  concurrency acceptance is claimed until the configured engine matrix runs.

## 2026-08-08 - Grouped Default Naming Correction

- Corrected grouped default table resolution to use the record type's simple
  name rather than the source type's simple name, matching the frozen grouped
  storage contract.
- Evidence: resolver tests pass 5/5 and package TypeScript checking exits 0.
- Remaining accepted correction scope includes default-name collision detection
  across resolved families, full schema inspection, lifecycle/API boundaries,
  documentation inventory, and non-live verification.

## 2026-08-08 - Default Resolved-Name Collision Correction

- The resolver now records resolved physical names and rejects a name resolved
  by a distinct default family. Registered record-name fallback remains a
  deliberate shared family identity, so exact record registrations continue to
  work across source callers without a false collision.
- Evidence: resolver tests pass 6/6 and package TypeScript checking exits 0.
- Remaining: full schema inspection matrix and the rest of the accepted
  lifecycle/API/documentation/verification correction batch.

## 2026-08-08 - Canonical Schema Matrix Correction

- Runtime metadata: the existing `implementer` assignment was explicitly
  `gpt-5.6-terra` / `medium`. The surface exposes no independent runtime-model
  inspection; the configured profile is the available evidence, with no
  visible mismatch or inherited-profile fallback.
- RED/GREEN: parameterized storage tests initially exposed missing primary-key
  enforcement, revision defaults, declared native-column validation, harmful
  additions and unique-index validation, and capacity-widening compatibility.
  A binary-collation test was explicitly RED-checked with its guard removed and
  then restored GREEN.
- `MysqlStorageFactory` now resolves one `MysqlTableSpec` and gives it to both
  the custom creation callback and `MysqlRecordStorage`. Default DDL and full
  inspection use that exact canonical specification; the reduced resolver
  output no longer defines either path. The adapter never emits `ALTER TABLE`.
- Inspection enforces every mandatory key/column rule and accepts only the
  frozen harmless column/index additions. InnoDB, MyISAM, and Aria validation
  is preserved.
- Evidence: fresh `vitest run packages/storage-rdbms/test` passed 65 tests
  with 6 URL-gated live tests skipped. Package TypeScript, exact source ESLint,
  Prettier check, and `git diff --check` exited 0.
- The full record-storage test-file ESLint command still reports inherited
  `require-await` fixture failures outside this matrix; the new matrix's two
  local type-style findings were corrected. No lint configuration was changed.
- Limitation: no Docker engine rerun or repository-wide `verify:task` profile
  was run in this bounded correction. No commit, push, merge, JVM, or
  out-of-scope package modification was made.

## 2026-08-08 Wave 2 Completion Evidence

- Runtime metadata remains the configured `implementer` `gpt-5.6-terra` /
  `medium`; the execution surface has no independent runtime model inspection.
- The live fixture resolves canonical physical family names. It proves present
  and absent two-handle CAS races: exactly one writer succeeds on InnoDB,
  MyISAM, and Aria.
- A real absent-key InnoDB deadlock was observed from simultaneous gap locks
  followed by inserts. Record CAS retries once, only for `ER_LOCK_DEADLOCK`.
  MariaDB metadata normalization accepts equivalent nullable `NULL` defaults
  and integer display widths.
- Live evidence: MySQL 8 InnoDB/MyISAM and MariaDB 11.4 InnoDB/Aria are each
  7/7. The dedicated MariaDB fixture schema was reset between variants.
- Coverage: 74/74; statements 94.06% (713/758), branches 90.01% (496/551),
  functions 93.83% (198/211), lines 95.82% (643/671), no config change.
- Package TypeScript, changed ESLint, Prettier, TSDoc, docs audience, and diff
  checks pass. `check-api-docs` remains blocked by unrelated downstream shared-
  train compilation failures. No commit, push, or merge.

## 2026-08-08 Final Correction Evidence

- Entity commits reject supplied states or diagnostics when their respective
  history family is disabled. Canonical-byte-identical Entity replays complete
  immutable suffix work without replacing the current row.
- Advisory lock keys include database identity. The protected factory override
  exposes `RecordStorage`; the private construction seam retains the MySQL
  handle needed by Entity commits.
- Focused typecheck and 21 commit/integration tests pass. Coverage: 76/76,
  94.26% statements, 90.08% branches, 93.86% functions, and 95.87% lines.
- Live MySQL 8 InnoDB/MyISAM and MariaDB 11.4 InnoDB/Aria each pass 9/9.
  Changed lint, Prettier, TSDoc, audience, and diff checks pass.

## 2026-08-08 Final Generic And Coverage Evidence

- Removed the Entity-history query generic-erasure cast by translating IDs in
  normalized predicates to canonical Entity keys. Tests cover ID, comparison,
  nested, absent, and fully populated plan variants.
- Public ID validation is asynchronous and pre-acquisition. The focused record
  suite passes 32/32; focused history/record suite passes 45/45.
- Full provider coverage passes 77/77 with 94.13% statements, 90.25% branches,
  93.98% functions, and 95.68% lines. No threshold/configuration changed.
- Package TypeScript, changed ESLint, Prettier, TSDoc, audience, and diff pass.
  Live matrix: MySQL 8 InnoDB (coverage run), MySQL 8 MyISAM 9/9, MariaDB 11.4
  InnoDB 9/9, and MariaDB 11.4 Aria 9/9. No commit, push, or merge.

## 2026-08-08 Revision Replay Evidence

- Added the non-public `src/mysql/testing.ts` seam for provider integration
  tests. It resolves the canonical current-family table and reads `_revision`
  using the same scope and canonical Entity-ID encodings; it is not exported by
  the package index.
- The live InnoDB replay regression now reads `_revision` after the first
  identical Entity commit and after the retry, asserting exact equality.
- Seam coverage covers single-tenant, multitenant, and missing-row paths.
  Provider coverage is 78/78, 94.25% statements, 90.28% branches, 94.03%
  functions, and 95.77% lines. TypeScript, changed lint, Prettier, TSDoc,
  audience, and diff checks pass. No commit, push, or merge.

## 2026-08-08 Mechanical Closeout

- The generic Entity commit derives a canonical key from its typed input; the
  private Entity capability consumes that string key. This removes Entity-ID
  intersection/erasure casts from the commit path.
- `mysqlScopeKey()` is the shared production/test canonical scope encoder.
- Documentation removes remaining eager/fixed/fingerprint/FK-index claims,
  documents lazy per-family behavior and engine-qualified writes, and links the
  README directly to the MySQL user-guide section.
- Live InnoDB integration passes 9/9. Full provider coverage: 78/78; 93.95%
  statements, 90.28% branches, 93.15% functions, and 95.45% lines. Package
  TypeScript, TSDoc, audience, formatting, and diff checks pass.

## 2026-08-08 Final Acceptance Evidence

- Removed the last live-test private-state casts. Provider-private typed helpers
  resolve default record/Entity tables, read current records and revisions, and
  count retained histories without entering the package root API. Production
  and test helpers share `mysqlScopeKey()`.
- Revision reads use `CAST(_revision AS CHAR)` and `BigInt(...)`, avoiding
  mysql2's default lossy numeric decoding while retaining an exact bigint
  testing contract.
- Final coverage command with live MySQL InnoDB passes 79/79 at 93.96%
  statements, 90.01% branches, 92.98% functions, and 95.52% lines.
- Final focused MySQL InnoDB integration passes 9/9. Earlier accepted final
  matrix evidence remains MySQL InnoDB/MyISAM and MariaDB InnoDB/Aria at 9/9
  each; the post-matrix edits affect only typed test helpers and prose.
- Package TypeScript, strict changed-file ESLint, TSDoc, documentation snippets,
  documentation audience, Prettier, and `git diff --check` pass.
- All four specialist concerns are clean. Security is N/A. The only retained
  limitation is the stacked-train downstream compilation blocker preventing
  repository-wide API/verification profiles from reaching provider inspection.
- The final `verify:task --coverage` profile passed Proto generation, all 49
  source checksums, authored/example Proto checks, and all 52 frozen descriptor
  checks. It then stopped at repository `tsc -b` on the expected downstream
  RecordSpec/entity-history callers in deployment, server, Datastore, and
  Message Board. No provider-scope error was emitted, so T-0134 is accepted on
  the Wave 8 integration train.
