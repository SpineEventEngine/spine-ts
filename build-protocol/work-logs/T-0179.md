# T-0179 Work Log

## 2026-08-13 - Start

- T-0178 planning was reviewed, verification-complete, fast-forwarded to
  `origin/main@8ef4c066`, and tagged `T-0178`.
- Created isolated worktree `.worktrees/T-0179-generated-source-policy` on
  `task/T-0179-generated-source-policy` from that exact baseline.
- `pnpm install --frozen-lockfile` passed for all 26 workspace projects.
- Classified T-0179 as high-risk shared build/release behavior.
- Fully read the `executing-plans`, `subagent-driven-development`,
  `test-driven-development`, `implement`, and `using-git-worktrees` skills.
- Recorded the complete human ledger, acceptance criteria, TDD boundary,
  selected release profile, review plan, and explicit implementer assignment
  before any production edit or child dispatch.
- `pnpm proto:generate` passed 47 source checksums and the 52-descriptor frozen
  digest. The first focused test attempt then had 3 fixture typecheck failures
  because the fresh worktree lacked built workspace declarations; 164 other
  tests passed. After the required `pnpm typecheck:build:generated` setup, the
  identical four-file suite passed 167/167. This is baseline evidence, not the
  task's behavior RED.

## Current Status

Review corrections complete and re-review ready. Earlier “work continues”
entries are historical checkpoints superseded by the accepted review-correction
batch. The orchestrator retains the converged release-profile and re-review
steps.

## 2026-08-13 - Accepted review correction batch

- Reviewer assignments: documentation `gpt-5.6-luna` / medium; API, style, and
  reliability `gpt-5.6-terra` / high. Runtime metadata was unavailable on this
  surface. Accepted findings cover generator provenance, path validation,
  package-import registries, declaration TSDoc, atomic fixtures, and records.
- Security remains concrete N/A: stable Proto-path validation narrows rather
  than expands the configured model-root trust boundary.

## 2026-08-13 - Direct package correction complete

- Direct `spine-proto` generation, composition, and packed handler commands now
  use the publishable shared policy. Package/direct and atomic-fixture tests pass
  90/90; generated build/tooling, TSDoc, copyright, generation/current-output,
  ESLint, cleanup, formatting, and diff checks pass. Ready for re-review.

## 2026-08-13 - Final correction evidence

- The final idempotency RED was
  `scripts/proto-workflow.test.mjs`: a composed multi-source Message Board
  registry received a second, narrower one-file notice and duplicate
  declaration provenance from the outer workflow wrapper. GREEN is
  `pnpm exec vitest run scripts/proto-workflow.test.mjs
  packages/proto-tools/test/proto-tools.test.ts
  scripts/generate-server-test-fixtures.test.mjs`: 147/147. The outer workflow
  now requires and preserves the package-composed notice; package compose emits
  the declaration provenance itself.
- `pnpm proto:generate && pnpm proto:check-generated:current` passes. The
  tracked registry has one sorted five-source notice and one provenance line.
- `pnpm verify:task -- --coverage ...` completed its cheap preflight. The final
  equivalent explicit-source run exercised five focused suites (171/171). Its
  global aggregate is 87.21% lines because it includes legacy workflow bodies;
  LCOV mapped against `origin/main` including the final worktree changes hits
  **81/88 changed executable lines (92.05%)**: package policy 21/22, generator
  5/5, handler generator 1/1, copyright 4/4, fixture 11/12, generated policy
  19/22, and workflow 20/22. This satisfies the task's changed-range route.
- Package-only explicit coverage is 97.46% lines and 92.02% branches; its
  in-process handler regression records `HandlerGeneration.generate` 1/1
  function and 3/3 lines, avoiding the uninstrumented packed-CLI subprocess.
- `pnpm lint:tsdoc`, `pnpm lint:copyright`, `pnpm format:check`, and
  `git diff --check` pass after formatting. No release profile or re-review was
  run, by orchestrator instruction.

## 2026-08-13 - TDD RED/GREEN: generated classification and copied headers

- Implementer assignment: existing `implementer` role, explicitly configured
  `gpt-5.6-terra` / medium. This execution surface does not expose runtime
  model metadata, so the immutable configured profile is the available record.
- RED observed before any production edit: `pnpm exec vitest run
scripts/check-copyright.test.mjs --passWithNoTests` failed exactly because
  `examples/todo/generated/spine/examples/todo/tasks_pb.ts` was treated as
  authored and reported `missing CodeMatters header`.
- Added a focused shared generated-source policy, then the same focused test
  passed (21/21). A second RED proved copied line-comment Proto copyright text
  leaked into generated output; the focused policy and copyright suites then
  passed (22/22).
- A third RED proved the tracked server descriptor fixture still began with a
  CodeMatters header. Its generator and checked-in fixture now use sorted stable
  fixture Proto provenance; its focused suite passes (2/2).
- `pnpm proto:generate` passes against all 47 source checksums and the frozen
  52-descriptor digest after the normalization change.
- Work continues on the remaining writers, declaration provenance, and full
  focused verification. No release profile has run.

## 2026-08-13 - Shared normalization expanded

- Normalization now applies the same deterministic file notice and declaration
  provenance to Buf modules, generated Proto modules, handler registries,
  rejection and entity-column companions. It extracts stable Proto import paths
  from generated metadata/imports, sorts and deduplicates them, and removes
  copied leading generated comments before publication.
- Focused suites pass: `scripts/generated-source-policy.test.mjs`,
  `scripts/check-copyright.test.mjs`,
  `scripts/generate-server-test-fixtures.test.mjs`, and
  `scripts/proto-workflow.test.mjs` (82 tests); `pnpm typecheck:tooling` and
  `pnpm lint:copyright` also pass.

## 2026-08-13 - Repeat generation and coverage limitation

- Ran `pnpm proto:generate` twice consecutively. Both runs passed the 47 source
  checksums and frozen 52-descriptor digest; tracked output remains clean under
  `pnpm proto:check-generated:current`.
- Focused coverage ran the same 82 tests, but the repository's global Vitest
  coverage include configuration does not instrument the changed standalone
  `.mjs` policy scripts. It therefore reported global 0% and failed the global
  90% threshold despite passing tests. This is a verification limitation still
  requiring an explicit changed-range coverage route or orchestrator decision;
  it is not claimed as satisfying the task coverage criterion.

## 2026-08-13 - Explicit changed-range coverage

- `pnpm verify:task -- --coverage scripts/generated-source-policy.test.mjs
scripts/check-copyright.test.mjs scripts/generate-server-test-fixtures.test.mjs
scripts/proto-workflow.test.mjs --source scripts/generated-source-policy.mjs
scripts/check-copyright.mjs scripts/generate-server-test-fixtures.mjs
scripts/generate-spine-proto-artifacts.mjs scripts/proto-workflow.mjs` exited
  successfully after the complete cheap preflight. Its inherited terminal stream
  was truncated after the build phase, so the equivalent final Vitest invocation
  was rerun directly with those exact five `--coverage.include` arguments.
- The direct focused suite passed 82/82. Whole-file coverage is intentionally
  not the changed-range metric for shared legacy workflow scripts (82.24%
  statements, 74.84% branches). LCOV mapped against `origin/main...HEAD` counts
  39 of 41 executable changed lines as hit: **95.12% changed-line coverage**.
  The two unhit lines are callback-only mapping bodies at
  `scripts/proto-workflow.mjs:972` and `:975`; their enclosing normalizer branch
  and fallbacks execute in workflow integration tests. This satisfies the
  task's permitted line-by-line changed-range coverage route.

## 2026-08-13 - TSDoc pre-review correction

- The mechanical pre-review gate found invalid one-line TSDoc openers in the
  generated server fixture and private generator comments. Focused RED tests
  first demonstrated that declaration provenance was emitted as a one-line
  block. The shared policy now emits valid multiline TSDoc and merges the
  provenance line into an existing declaration block when one exists; internal
  script helper comments use ordinary block comments.
- GREEN evidence: the four focused suites pass 82/82. `pnpm typecheck:tooling`,
  `pnpm lint:tsdoc`, `pnpm lint:copyright`,
  `pnpm proto:check-generated:current`, changed-file ESLint,
  `pnpm format:check`, and `git diff --check` pass after `pnpm proto:generate`.
