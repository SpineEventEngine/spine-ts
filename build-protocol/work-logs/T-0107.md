# T-0107 Work Log

Status: In Progress

- 2026-08-04: created the isolated task branch and worktree from
  `origin/main@ce1ef99e` after T-0106 was reviewed, verified, integrated,
  pushed, and cleaned up.
- 2026-08-04: classified the task as high-risk and reused the approved Wave 6
  architecture. No new requirements split is needed because the fan-out,
  lease, reconnect-snapshot, bounded-overflow, and shutdown contracts are
  already explicit.
- 2026-08-04: recorded the existing `implementer` assignment with expected
  explicit `gpt-5.6-terra` / `medium`. Runtime self-introspection is
  unavailable; the immutable role profile and explicit dispatch fields are the
  accepted metadata evidence.
- 2026-08-04: implementation dispatch confirmed as existing `implementer`,
  explicitly configured `gpt-5.6-terra` / `medium`; this surface exposes no
  actual runtime model metadata, so the immutable role profile and explicit
  dispatch fields remain the acceptance evidence.
- 2026-08-04: RED checkpoint added focused environment coverage requiring a
  configured remote Admin source to drive snapshot recovery and observation.
  After generated-output setup, it failed as expected because the environment
  continued to use `LocalDeliverySource`; the direct pre-setup test could not
  resolve workspace package build output. The generated-output setup failure is
  baseline tooling state, not behavior evidence.
- 2026-08-04: GREEN connects a ready `RemoteDelivery` client as the existing
  environment supervisor source, retaining local fallback behavior. Supervisor
  watch reconnect and bounded stream-overflow recovery now rescan the complete
  Admin snapshot before reopening updates; environment close keeps ownership of
  the supervisor and client cancellation paths.
- 2026-08-04: focused GREEN evidence: `61` tests across the server environment,
  supervisor, remote-delivery lifecycle, and client-source contract passed;
  server and delivery-client package typechecks plus `git diff --check` passed.
  RED checkpoint `f43601df`; GREEN commit `015ef122`.
- 2026-08-04: selected `verify:task -- --no-coverage` for the bounded changed
  package paths. It passed its generated Proto, build/type-tooling, lint,
  formatting, documentation, release-readiness, and declared focused-test
  gates. Documentation commit `983a38b0` is pushed. Full release verification
  and the required specialist review wave remain orchestrator-owned next steps.
- 2026-08-04: RED multi-node real-gRPC evidence was added. Its first focused
  run failed before test execution because delivery-client has no test-time
  package edge to delivery-server (`Cannot find package
'@spine-event-engine/delivery-server'`). The test deliberately requires the
  simple server and two real client supervisors; the smallest GREEN is to use
  the existing local delivery-server test source without changing production
  package ownership.
- 2026-08-04: GREEN `remote-supervisor-grpc.integration.test.ts` starts one
  loopback simple delivery server and two independent delivery clients and
  supervisors. Both consume the real Admin stream, only one remote owner
  dispatches each durable row, a second row arriving while the first callback
  is held also drains, and both supervisors close before client/session close.
  The focused run passed `1/1`.
- 2026-08-04: existing deterministic recovery evidence is intentionally not
  duplicated in the process test: `DeliverySupervisor` proves "restarts a
  failed Admin watch with one bounded backoff timer" (including its fresh
  snapshot), "rescans real overflowed work after retained pending capacity
  clears", and "cancels the Admin watch as soon as close stops admission";
  `ShardObservationStream` proves "bounds a slow Admin observation consumer
  with a stable sanitized overflow error", "reconnects a transient Admin stream
  ending only through a fresh ACK", and "cancels an Admin reconnect backoff
  without opening another observation stream"; `delivery-fencing.test.ts`
  proves "fences a builder-created delivery when supervisor close aborts a
  pending endpoint" and "stops a real delivery lease timer on abort before a
  blocked endpoint settles".
- 2026-08-04: focused validation passed: `pnpm exec vitest run
packages/delivery-client/test/remote-supervisor-grpc.integration.test.ts
packages/delivery-client/test/shard-observation.test.ts
packages/delivery-client/test/client-rpc-lifecycle.test.ts
packages/server/test/delivery/delivery-supervisor.test.ts
packages/server/test/delivery/delivery-fencing.test.ts` (`90/90`),
  `pnpm exec tsc --noEmit -p packages/delivery-client/tsconfig.json`, and
  `git diff --check`. Real-GRPC RED commit `edeff3a0`; GREEN commit `c9f9e4e0`.
- 2026-08-04: deterministic lint correction replaced invalid `void` promise
  resolvers, an unnecessary async callback, and a confusing assertion expression
  in the real-gRPC test only. The exact required
  `pnpm --config.verify-deps-before-run=false verify:task -- --no-coverage`
  command over the five delivery-client/server test paths passed all generated
  Proto, type, lint, format, documentation, release-readiness, and focused-test
  gates.
- 2026-08-04: correction-wave RED: `shard-service.test.ts` added same-live-worker
  refresh and delayed-owner release coverage. It failed as expected because a
  repeated pickup returned `alreadyPickedUp`; existing release was also
  worker-agnostic. The correction must preserve a current owner on a delayed
  prior-owner release.
- Next: orchestrator review wave, then release verification.
- 2026-08-04: first review correction slice added owner-matched shard release.
  RED `42c45006` proved a delayed worker release could clear a later owner's
  session; GREEN `31804335` makes release compare the full current worker.
  Focused shard-service evidence passes 17/17. Commit `382164b4` records the
  Human-Imposed Requirements Ledger and the per-acquisition/commit-fence
  architecture decision.
- 2026-08-04: the original correction context ended twice without starting the
  assigned client revalidation slice. Its endpoint is clean and fully pushed;
  no work is lost. Reassigned the next non-overlapping slice to a fresh existing
  `implementer`, explicit `gpt-5.6-terra` / `medium`. Runtime
  self-introspection remains unavailable; immutable role configuration and
  explicit dispatch fields are the metadata evidence.
- 2026-08-04: this correction slice is owned by the existing `implementer`,
  explicitly dispatched `gpt-5.6-terra` / `medium`. The execution surface does
  not expose actual runtime model metadata; the immutable configured role/profile
  and explicit dispatch fields are the accepted metadata evidence, with no
  visible mismatch. RED tests require a unique opaque worker value per remote
  acquisition, package-internal same-generation revalidation, and release of an
  accidental re-acquisition. `pnpm exec vitest run
packages/delivery-client/test/remote-work-registry.test.ts
packages/delivery-server/test/core/shard-service.test.ts` failed as expected:
  consecutive pickups both used `spine-ts:node`, the internal revalidation access
  is absent, and the server omitted the refreshed outcome marker.
- 2026-08-04: GREEN gives every `RemoteWorkRegistry.pickUp()` a UUID-backed
  opaque `WorkerId.value` while preserving `nodeId`. Package-internal
  revalidation retains the exact active wire worker, accepts only the server's
  `refreshed` marker, treats a `lost` response as lost ownership, and releases a
  `picked` accidental re-acquisition before reporting loss. The server marks
  revalidation outcomes as `refreshed`, `lost`, or `picked`; no Protobuf or
  public API changed. Focused evidence passed: `pnpm exec vitest run
packages/delivery-client/test/remote-work-registry.test.ts
packages/delivery-client/test/remote-adapters-quarantine.test.ts
packages/delivery-client/test/shard-observation.test.ts
packages/delivery-server/test/core/shard-service.test.ts` (`62/62`), both
  delivery-client and delivery-server `tsc --noEmit` checks, and `git diff
--check`. Test fixtures now echo the outgoing worker for remote registry
  tests, preserving the protocol's exact-worker validation instead of assuming
  the former deterministic worker token.
- Next: return this pushed client revalidation slice for the repository
  commit-fence correction and its focused review/verification.
- 2026-08-04: commit-fence RED adds delivery ownership-loss/ABA coverage. It
  fails as expected because the internal commit-fence module is absent.
- 2026-08-04: GREEN adds a package-internal AsyncLocalStorage delivery commit
  fence. Delivery scopes each endpoint callback to active-work synchronization;
  all five repository Entity commit paths await that guard immediately before
  their existing synchronous commit. Focused delivery, repository, and context
  evidence passed (`183/183`), along with the server typecheck, formatting, and
  `git diff --check`. No public API or wire contract changed.
- 2026-08-04: reconnect correction makes watch reopening conditional on a
  successful release-plus-complete-snapshot recovery, and rejects malformed
  dynamically supplied environment delivery sources. Focused supervisor tests
  and the server typecheck pass.
- 2026-08-04: corrected the snapshot-gate test to retain its successful
  replacement stream. An immediately completed watch is itself a new stream
  break and therefore retried with bounded backoff; the retained stream proves
  failed recovery opens none and one later successful snapshot opens exactly
  one replacement watch. Focused supervisor evidence passes 38/38 with server
  typecheck, formatter, and diff checks.
- 2026-08-04: assigned the next non-overlapping client-source correction slice
  to a fresh existing `implementer`, explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection is unavailable; immutable role configuration and
  explicit dispatch fields remain the metadata evidence.
- Next: make the `RemoteDelivery` supervisor source expose the first stream
  break, prove public reconnect behavior is unchanged, and complete dynamic
  source-validation evidence.
- 2026-08-04: this client-source correction slice remains owned by the existing
  `implementer`, explicitly dispatched `gpt-5.6-terra` / `medium`. Runtime
  self-introspection is unavailable on this surface; the immutable configured
  role/profile and explicit dispatch fields are the accepted metadata evidence,
  with no visible mismatch.
- 2026-08-04: RED `b3914526` added focused evidence that public client
  observations retain configured reconnects, `RemoteDelivery.source` must
  expose its first stream break, and dynamic `null`, empty, and incomplete
  sources are refused after delivery opens. The focused run failed as intended
  because the remote source used the reconnecting public client stream; the
  already-present environment validator satisfied its matrix cases.
- 2026-08-04: GREEN adds package-internal one-attempt observation access to
  `DeliveryClient` and makes `RemoteDelivery.source` use it while direct public
  observation retains its configured reconnect policy. Source validation keeps
  omitted sources on the local fallback and accepts a structurally valid remote
  source; no public export, Protobuf, JVM, or real-gRPC implementation changed.
- 2026-08-04: GREEN commit `23281b6f` is pushed. Focused evidence passed:
  `pnpm exec vitest run packages/delivery-client/test/shard-observation.test.ts
packages/delivery-client/test/remote-delivery.test.ts
packages/server/test/server/server-environment.test.ts
packages/server/test/delivery/delivery-supervisor.test.ts
packages/server/test/delivery/delivery-fencing.test.ts` (`95/95`), both
  delivery-client and server `tsc --noEmit` checks, Prettier over every changed
  file, and `git diff --check`. No uncertainty remains within this owned slice;
  the broader T-0107 review/release verification remains orchestrator-owned.
- 2026-08-04: assigned the final non-overlapping implementation slice to a
  fresh existing `implementer`, explicit `gpt-5.6-terra` / `medium`. Runtime
  self-introspection remains unavailable; immutable role configuration and
  explicit dispatch fields are the metadata evidence.
- Next: replace the raw two-supervisor fixture with real application-environment
  assembly, add fault/recovery and failure-safe teardown evidence, then finish
  the affected README/reference corrections.
- 2026-08-04: RED replaces the raw two-supervisor real-gRPC test with two
  child application-node processes. It failed as expected (`1/1`) because the
  required remote `ServerEnvironment` fixture does not yet exist; the failure
  was `Fixture process exited before response.` This proves the new acceptance
  path is not accidentally exercising the old raw-supervisor assembly.
