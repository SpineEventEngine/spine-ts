# T-0107 Work Log

Status: In Progress

- 2026-08-04: created the isolated task branch and worktree from
  `origin/main@ce1ef99e` after T-0106 was reviewed, verified, integrated,
  pushed, and cleaned up.
- 2026-08-04: classified the task as high-risk and reused the approved Wave 6
  architecture. No new requirements split is needed because the fan-out,
  lease, reconnect-snapshot, bounded-overflow, and shutdown contracts are
  already explicit.
- 2026-08-04: recorded the existing `implementer` assignment with expected
  explicit `gpt-5.6-terra` / `medium`. Runtime self-introspection is
  unavailable; the immutable role profile and explicit dispatch fields are the
  accepted metadata evidence.
- 2026-08-04: implementation dispatch confirmed as existing `implementer`,
  explicitly configured `gpt-5.6-terra` / `medium`; this surface exposes no
  actual runtime model metadata, so the immutable role profile and explicit
  dispatch fields remain the acceptance evidence.
- 2026-08-04: RED checkpoint added focused environment coverage requiring a
  configured remote Admin source to drive snapshot recovery and observation.
  After generated-output setup, it failed as expected because the environment
  continued to use `LocalDeliverySource`; the direct pre-setup test could not
  resolve workspace package build output. The generated-output setup failure is
  baseline tooling state, not behavior evidence.
- 2026-08-04: GREEN connects a ready `RemoteDelivery` client as the existing
  environment supervisor source, retaining local fallback behavior. Supervisor
  watch reconnect and bounded stream-overflow recovery now rescan the complete
  Admin snapshot before reopening updates; environment close keeps ownership of
  the supervisor and client cancellation paths.
- 2026-08-04: focused GREEN evidence: `61` tests across the server environment,
  supervisor, remote-delivery lifecycle, and client-source contract passed;
  server and delivery-client package typechecks plus `git diff --check` passed.
  RED checkpoint `f43601df`; GREEN commit `015ef122`.
- 2026-08-04: selected `verify:task -- --no-coverage` for the bounded changed
  package paths. It passed its generated Proto, build/type-tooling, lint,
  formatting, documentation, release-readiness, and declared focused-test
  gates. Documentation commit `983a38b0` is pushed. Full release verification
  and the required specialist review wave remain orchestrator-owned next steps.
- 2026-08-04: RED multi-node real-gRPC evidence was added. Its first focused
  run failed before test execution because delivery-client has no test-time
  package edge to delivery-server (`Cannot find package
'@spine-event-engine/delivery-server'`). The test deliberately requires the
  simple server and two real client supervisors; the smallest GREEN is to use
  the existing local delivery-server test source without changing production
  package ownership.
- 2026-08-04: GREEN `remote-supervisor-grpc.integration.test.ts` starts one
  loopback simple delivery server and two independent delivery clients and
  supervisors. Both consume the real Admin stream, only one remote owner
  dispatches each durable row, a second row arriving while the first callback
  is held also drains, and both supervisors close before client/session close.
  The focused run passed `1/1`.
- 2026-08-04: existing deterministic recovery evidence is intentionally not
  duplicated in the process test: `DeliverySupervisor` proves "restarts a
  failed Admin watch with one bounded backoff timer" (including its fresh
  snapshot), "rescans real overflowed work after retained pending capacity
  clears", and "cancels the Admin watch as soon as close stops admission";
  `ShardObservationStream` proves "bounds a slow Admin observation consumer
  with a stable sanitized overflow error", "reconnects a transient Admin stream
  ending only through a fresh ACK", and "cancels an Admin reconnect backoff
  without opening another observation stream"; `delivery-fencing.test.ts`
  proves "fences a builder-created delivery when supervisor close aborts a
  pending endpoint" and "stops a real delivery lease timer on abort before a
  blocked endpoint settles".
- 2026-08-04: focused validation passed: `pnpm exec vitest run
packages/delivery-client/test/remote-supervisor-grpc.integration.test.ts
packages/delivery-client/test/shard-observation.test.ts
packages/delivery-client/test/client-rpc-lifecycle.test.ts
packages/server/test/delivery/delivery-supervisor.test.ts
packages/server/test/delivery/delivery-fencing.test.ts` (`90/90`),
  `pnpm exec tsc --noEmit -p packages/delivery-client/tsconfig.json`, and
  `git diff --check`. Real-GRPC RED commit `edeff3a0`; GREEN commit `c9f9e4e0`.
- 2026-08-04: deterministic lint correction replaced invalid `void` promise
  resolvers, an unnecessary async callback, and a confusing assertion expression
  in the real-gRPC test only. The exact required
  `pnpm --config.verify-deps-before-run=false verify:task -- --no-coverage`
  command over the five delivery-client/server test paths passed all generated
  Proto, type, lint, format, documentation, release-readiness, and focused-test
  gates.
- 2026-08-04: correction-wave RED: `shard-service.test.ts` added same-live-worker
  refresh and delayed-owner release coverage. It failed as expected because a
  repeated pickup returned `alreadyPickedUp`; existing release was also
  worker-agnostic. The correction must preserve a current owner on a delayed
  prior-owner release.
- Next: orchestrator review wave, then release verification.
- 2026-08-04: first review correction slice added owner-matched shard release.
  RED `42c45006` proved a delayed worker release could clear a later owner's
  session; GREEN `31804335` makes release compare the full current worker.
  Focused shard-service evidence passes 17/17. Commit `382164b4` records the
  Human-Imposed Requirements Ledger and the per-acquisition/commit-fence
  architecture decision.
- 2026-08-04: the original correction context ended twice without starting the
  assigned client revalidation slice. Its endpoint is clean and fully pushed;
  no work is lost. Reassigned the next non-overlapping slice to a fresh existing
  `implementer`, explicit `gpt-5.6-terra` / `medium`. Runtime
  self-introspection remains unavailable; immutable role configuration and
  explicit dispatch fields are the metadata evidence.
- 2026-08-04: this correction slice is owned by the existing `implementer`,
  explicitly dispatched `gpt-5.6-terra` / `medium`. The execution surface does
  not expose actual runtime model metadata; the immutable configured role/profile
  and explicit dispatch fields are the accepted metadata evidence, with no
  visible mismatch. RED tests require a unique opaque worker value per remote
  acquisition, package-internal same-generation revalidation, and release of an
  accidental re-acquisition. `pnpm exec vitest run
packages/delivery-client/test/remote-work-registry.test.ts
packages/delivery-server/test/core/shard-service.test.ts` failed as expected:
  consecutive pickups both used `spine-ts:node`, the internal revalidation access
  is absent, and the server omitted the refreshed outcome marker.
- 2026-08-04: GREEN gives every `RemoteWorkRegistry.pickUp()` a UUID-backed
  opaque `WorkerId.value` while preserving `nodeId`. Package-internal
  revalidation retains the exact active wire worker, accepts only the server's
  `refreshed` marker, treats a `lost` response as lost ownership, and releases a
  `picked` accidental re-acquisition before reporting loss. The server marks
  revalidation outcomes as `refreshed`, `lost`, or `picked`; no Protobuf or
  public API changed. Focused evidence passed: `pnpm exec vitest run
packages/delivery-client/test/remote-work-registry.test.ts
packages/delivery-client/test/remote-adapters-quarantine.test.ts
packages/delivery-client/test/shard-observation.test.ts
packages/delivery-server/test/core/shard-service.test.ts` (`62/62`), both
  delivery-client and delivery-server `tsc --noEmit` checks, and `git diff
--check`. Test fixtures now echo the outgoing worker for remote registry
  tests, preserving the protocol's exact-worker validation instead of assuming
  the former deterministic worker token.
- Next: return this pushed client revalidation slice for the repository
  commit-fence correction and its focused review/verification.
- 2026-08-04: commit-fence RED adds delivery ownership-loss/ABA coverage. It
  fails as expected because the internal commit-fence module is absent.
- 2026-08-04: GREEN adds a package-internal AsyncLocalStorage delivery commit
  fence. Delivery scopes each endpoint callback to active-work synchronization;
  all five repository Entity commit paths await that guard immediately before
  their existing synchronous commit. Focused delivery, repository, and context
  evidence passed (`183/183`), along with the server typecheck, formatting, and
  `git diff --check`. No public API or wire contract changed.
- 2026-08-04: reconnect correction makes watch reopening conditional on a
  successful release-plus-complete-snapshot recovery, and rejects malformed
  dynamically supplied environment delivery sources. Focused supervisor tests
  and the server typecheck pass.
- 2026-08-04: corrected the snapshot-gate test to retain its successful
  replacement stream. An immediately completed watch is itself a new stream
  break and therefore retried with bounded backoff; the retained stream proves
  failed recovery opens none and one later successful snapshot opens exactly
  one replacement watch. Focused supervisor evidence passes 38/38 with server
  typecheck, formatter, and diff checks.
- 2026-08-04: assigned the next non-overlapping client-source correction slice
  to a fresh existing `implementer`, explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection is unavailable; immutable role configuration and
  explicit dispatch fields remain the metadata evidence.
- Next: make the `RemoteDelivery` supervisor source expose the first stream
  break, prove public reconnect behavior is unchanged, and complete dynamic
  source-validation evidence.
- 2026-08-04: this client-source correction slice remains owned by the existing
  `implementer`, explicitly dispatched `gpt-5.6-terra` / `medium`. Runtime
  self-introspection is unavailable on this surface; the immutable configured
  role/profile and explicit dispatch fields are the accepted metadata evidence,
  with no visible mismatch.
- 2026-08-04: RED `b3914526` added focused evidence that public client
  observations retain configured reconnects, `RemoteDelivery.source` must
  expose its first stream break, and dynamic `null`, empty, and incomplete
  sources are refused after delivery opens. The focused run failed as intended
  because the remote source used the reconnecting public client stream; the
  already-present environment validator satisfied its matrix cases.
- 2026-08-04: GREEN adds package-internal one-attempt observation access to
  `DeliveryClient` and makes `RemoteDelivery.source` use it while direct public
  observation retains its configured reconnect policy. Source validation keeps
  omitted sources on the local fallback and accepts a structurally valid remote
  source; no public export, Protobuf, JVM, or real-gRPC implementation changed.
- 2026-08-04: GREEN commit `23281b6f` is pushed. Focused evidence passed:
  `pnpm exec vitest run packages/delivery-client/test/shard-observation.test.ts
packages/delivery-client/test/remote-delivery.test.ts
packages/server/test/server/server-environment.test.ts
packages/server/test/delivery/delivery-supervisor.test.ts
packages/server/test/delivery/delivery-fencing.test.ts` (`95/95`), both
  delivery-client and server `tsc --noEmit` checks, Prettier over every changed
  file, and `git diff --check`. No uncertainty remains within this owned slice;
  the broader T-0107 review/release verification remains orchestrator-owned.
- 2026-08-04: assigned the final non-overlapping implementation slice to a
  fresh existing `implementer`, explicit `gpt-5.6-terra` / `medium`. Runtime
  self-introspection remains unavailable; immutable role configuration and
  explicit dispatch fields are the metadata evidence.
- Next: replace the raw two-supervisor fixture with real application-environment
  assembly, add fault/recovery and failure-safe teardown evidence, then finish
  the affected README/reference corrections.
- 2026-08-04: real environment RED `15bab59e` proved the delivery commit fence
  did not reach remote worker revalidation because `RemoteInboxWork.synchronize`
  performed only a local shard check. The first owner preserved the uncommitted
  mapping/fixture correction but ended before a conclusive GREEN run.
- 2026-08-04: systematic debugging found the child fixture imports built package
  output. Its earlier rerun used stale delivery-client `dist`; after rebuilding
  server and delivery-client, the exact real lease-loss/ABA test passed 1/1 in
  1.34 seconds. The source fix is therefore retained for finalization rather
  than replaced with another runtime design.
- 2026-08-04: reassigned only final cleanup/verification of the preserved
  changes to a fresh existing `implementer`, explicit `gpt-5.6-terra` /
  `medium`. Runtime self-introspection remains unavailable; immutable role
  configuration and explicit dispatch fields are the metadata evidence.
- Next: remove temporary diagnostics if any, stabilize the real test, commit and
  push GREEN, then add remaining real reconnect/overflow evidence.
- 2026-08-04: RED replaces the raw two-supervisor real-gRPC test with two
  child application-node processes. It failed as expected (`1/1`) because the
  required remote `ServerEnvironment` fixture does not yet exist; the failure
  was `Fixture process exited before response.` This proves the new acceptance
  path is not accidentally exercising the old raw-supervisor assembly.
- 2026-08-04: GREEN adds the minimal child-only remote-environment fixture.
  Each child configures `ServerEnvironment` with `RemoteDelivery`, attaches a
  real descriptor through the environment lifecycle, and closes the attachment
  and facility on SIGTERM. The real gRPC test starts two such singleton-isolated
  nodes against one simple delivery server, observes one current dispatcher,
  holds that callback, writes a second row mid-drain, then releases it and
  observes exactly one completed replay per row. Focused evidence passed
  (`1/1`); the existing focused supervisor, observation, and fencing suites
  retain the deterministic stream-break, bounded-overflow, snapshot-gate, and
  ownership-fence evidence. Delivery-client README/reference now state the
  all-node attempt, one-owner, snapshot-recovery, and no-cross-shard-ordering
  contract; server REFERENCE records optional `source`, local fallback, and
  facility-owned supervisor lifecycle.
- 2026-08-04: ownership-loss RED adds a real `releaseExpired(1)` race after a
  blocked environment replay. The integration test failed as expected: no
  replacement `committed` frame exists because the fixture has not yet invoked
  package-internal `commitFenced`; the blocked fixture also made teardown time
  out, identifying the required SIGTERM barrier release.
- 2026-08-04: final ownership-fence GREEN retains the existing module-private
  exact-session mapping and removes its unsafe by-shard fallback. `RemoteInboxWork`
  now revalidates the retained exact remote worker before the commit fence can
  commit; registry release, loss, and Admin reconciliation clear that mapping.
  The real two-environment fixture reports resumed/fenced states, releases a
  blocked callback before SIGTERM teardown, and rethrows after a fence. Its
  real gRPC assertion proves replacement-only commit, stale-owner non-commit,
  ABA rejection, and delayed-release safety without a fixed lease-expiry sleep.
- 2026-08-04: stale built package output was the earlier inconclusive result:
  the child fixture imports `delivery-client` and `server` `dist`, so source
  changes are invisible until `pnpm typecheck:build:generated` refreshes them.
  After that build, the exact real test passed three consecutive runs (`2/2`
  each; 1.79 s, 1.68 s, 1.68 s). The final focused set passed `134/134`:
  real gRPC, remote work registry, remote adapters quarantine, shard service,
  delivery fencing, and delivery worker. Delivery-client and server `tsc --noEmit`, ESLint,
  Prettier, and `git diff --check` also passed. The shared
  transport fixture now returns the real server's `refreshed` marker for
  revalidation requests, so the builder adapter regression uses the same wire
  acknowledgement as production. No public API, Protobuf, JVM, or documentation
  surface changed in this finalization slice.
- 2026-08-04: assigned the remaining real reconnect/overflow acceptance to a
  fresh existing `implementer`, explicit `gpt-5.6-terra` / `medium`. Runtime
  self-introspection remains unavailable; immutable role configuration and
  explicit dispatch fields are the metadata evidence.
- Next: prove restart snapshot recovery and bounded overflow convergence through
  the real environment/simple-server path, then run deterministic preflight and
  affected re-review.
- 2026-08-04: final fault-recovery evidence is owned by the existing
  `implementer`, explicitly dispatched `gpt-5.6-terra` / `medium`. Runtime
  self-introspection is unavailable; immutable role configuration and the
  explicit dispatch fields are the accepted metadata evidence, with no visible
  mismatch.
- 2026-08-04: added real-gRPC environment evidence for same-endpoint simple
  server restart and a separate actual `RemoteDelivery.source` overflow. The
  restart closes the first Admin listener, observes the failed write while the
  endpoint is unavailable, starts a fresh listener on the exact endpoint, then
  writes before the supervisors' bounded reconnect can be observed and proves
  one delivery. The overflow case starts a real tiny-buffer remote source,
  pauses its consumer after acknowledgement, proves the stable overflow error,
  reads a fresh complete snapshot, then releases the real environment drain and
  proves both retained rows complete once. The added tests passed immediately
  because the prior correction commits already implement the recovery gates;
  therefore there is no honest new runtime RED failure or production GREEN in
  this final evidence-only slice.
- 2026-08-04: focused lint then rejected an unsafe Vitest asymmetric matcher
  in the new complete-snapshot assertion. The minimal test-only correction
  reads the real snapshot and checks its typed observations directly; the
  real-gRPC file, ESLint, Prettier, and `git diff --check` pass.
- 2026-08-04: final focused validation from `7f5d954e` passed `108/108` across
  the real-gRPC, shard-observation, remote-delivery, client-lifecycle,
  supervisor, and fencing suites. Delivery-client/server `tsc --noEmit`, ESLint,
  Prettier, and committed-diff checks passed. The branch is pushed; broader
  T-0107 review and release verification remain orchestrator-owned.
- 2026-08-04: final task preflight initially found that a test-only transport
  stub did not satisfy the complete Connect `Transport` type and treated
  `HeadersInit` as `Headers`. Commit `897deab8` corrected the fixture without a
  production change; strict TypeScript, focused ESLint, Prettier, and diff
  checks passed, and the correction was pushed immediately.
- 2026-08-04: the complete T-0107 preflight then passed Proto integrity, the
  workspace build, and strict TypeScript checking at pushed endpoint
  `897deab8`. The four substantively affected specialist concerns are assigned
  for one focused correction re-review wave using the recorded immutable roles
  and explicit model/reasoning profiles.
- 2026-08-04: the complete correction re-review returned two P1 and four P2
  findings: undocumented revalidation metadata breaks frozen-wire servers;
  aborted shutdown can skip prompt remote lease release; environment overflow
  recovery is not exercised end to end; fixture cleanup is failure-unsafe and
  parent process waiting is unbounded; revalidation state/test seams are
  duplicated; and docs omit drain-until-empty behavior. All results used the
  recorded roles and expected profiles. Runtime self-introspection remained
  unavailable with no visible mismatch.
- 2026-08-04: the wire-compatibility P1 is assigned to the existing
  `requirements_splitter`, explicit `gpt-5.6-sol` / `high`, before the complete
  finding batch returns once to implementation. The architecture pass is
  read-only and must prefer the smallest compatible contract without inventing
  a new protocol unless existing frozen semantics cannot satisfy the fence.
- 2026-08-04: the Sol/high architecture disposition requires no Proto/public
  change or human decision. It selects an ordinary frozen `PickShard` probe
  with a fresh challenger worker: only `already_picked_up` for the original
  exact worker and `whenPicked` authorizes the immediate commit; a challenger
  pickup or any mismatch fences the old generation and receives bounded,
  uncancelled cleanup. It also removes required private pickup metadata and
  consolidates duplicated per-client session state. The residual boundary is
  documented honestly: probe and Entity commit are not one distributed
  transaction, and transactional fencing tokens are outside this task.
- Next: return the complete six-finding batch plus this disposition once to an
  existing Terra/medium implementation owner, then run only the affected
  re-review concerns and the release gate after convergence.
- 2026-08-04: correction implementation is owned by the existing `implementer`,
  explicitly dispatched `gpt-5.6-terra` / `medium`. Runtime self-introspection
  remains unavailable; immutable configured role/profile and explicit dispatch
  fields are the actual-metadata evidence, with no visible mismatch.
- 2026-08-04: RED/GREEN commit `e7aafa6e` removes client-required private
  pickup/revalidation headers. The production `RemoteInboxWork.synchronize`
  path now performs an ordinary frozen `PickShard` probe using a fresh
  challenger and admits a commit only for the original exact worker and
  `whenPicked`; a challenger acquisition is released with an uncancelled,
  bounded cleanup and fences the old session. Header-ignorant scripted evidence
  passes without a JVM build.
- 2026-08-04: correction commit `c950cf02` runs bounded supervisor release
  cleanup after abort but before an unwinding blocked drain, makes fixture
  resource cleanup failure-safe with a bounded child wait, and records finite
  drain-until-empty (including mid-drain rows) without claiming linearizable
  distributed fencing. `pnpm typecheck:build:generated` refreshed child-fixture
  package output; focused real remote environment plus supervisor evidence then
  passed 42/42.
- 2026-08-04: the final focused correction run passed 64 tests with one skipped
  legacy adapter test. That skipped test encoded the removed private renewal
  protocol and needs replacement with a complete builder-path frozen-probe
  fixture; it is a known closure gap. The constrained-source-in-environment
  overflow acceptance was not completed in this implementation context, so the
  full six-finding batch remains pending re-review despite the passing focused
  wire, shutdown, fixture, and documentation evidence.
- 2026-08-04: correction closure resumes from `5dedca57` under the existing
  `implementer`, explicitly dispatched `gpt-5.6-terra` / `medium`. Runtime
  self-introspection remains unavailable; immutable configured role/profile and
  explicit dispatch fields are the actual-metadata evidence, with no visible
  mismatch.
- 2026-08-04: enabled the former skipped builder regression. It uses the
  shared ordinary frozen `PickShard` fixture to return `already_picked_up` for
  the original exact worker/generation and exercises the real
  `RemoteInboxWork.synchronize` path through `DeliveryBuilder`; no skipped
  focused test remains.
- 2026-08-04: `RemoteSessionOwner` is the package-private per-`DeliveryClient`
  canonical owner for exact session mapping, shard index/reconciliation,
  quarantine/release state, ordinary challenger acquisition, synchronization,
  invalidation, and release. `RemoteWorkRegistry` is now a thin port adapter
  and `RemoteInboxWork` resolves the same owner directly; the test-only
  revalidation seam remains deleted.
- 2026-08-04: rebuilt package dist before child fixtures with
  `pnpm typecheck:build:generated`. The real child `ServerEnvironment`
  fixture configures its actual `RemoteDelivery` with a deliberately tiny
  observation buffer, holds the first drain, floods retained rows, releases the
  drain, and converges each retained row once. Focused ESLint, delivery-client
  and server typechecks, Prettier, diff checks, and the real/focused suite pass
  65/65. The test observes end-to-end convergence; it does not expose a private
  supervisor counter for snapshot/watch-reopen internals.
- 2026-08-04: the same existing implementation context is redispatched for the
  three explicit closure gaps, with immutable `implementer`, explicit
  `gpt-5.6-terra` / `medium`: replace the skipped builder-path test with a real
  frozen-probe fixture, install constrained `RemoteDelivery.source` in the
  actual environment supervisor and prove overflow recovery, and finish the
  single package-private per-client session owner. Runtime self-introspection
  remains unavailable; the role/profile and explicit redispatch are the
  metadata evidence. No other scope is reopened.
