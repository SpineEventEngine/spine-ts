# T-0128 Work Log

## 2026-08-07 - Milestone start

- Classified T-0128 as high-risk because it closes cross-platform discovery,
  subscription recovery, replacement, capacity, and public deployment claims.
- Started from `origin/main@3ca436b1` in isolated branch
  `task/T-0128-wave7-closure`; the dirty primary checkout remains protected.
- Re-read the authoritative T-0128 plan after T-0126 and T-0127 integration.
  No unresolved product decision or architectural blocker remains.
- The implementation owner must be the existing implementer role with explicit
  `gpt-5.6-terra` / `medium`. Runtime metadata is recorded when exposed;
  otherwise the immutable configured role/profile and absence of a visible
  mismatch are the acceptance evidence.
- The first implementation action is RED-first deterministic acceptance for
  GKE/GCE scale, replacement, Gateway restart, and 32/40-node capacity. No
  runtime refactor is authorized without a demonstrated test-level gap.

## 2026-08-07 - RED/green cross-platform closure evidence

- Implementer: existing `implementer` profile, explicitly dispatched as
  `gpt-5.6-terra` / `medium`; this surface does not expose runtime-model
  introspection, and no visible profile mismatch occurred. No subagents used.
- RED: `pnpm exec vitest run packages/deployment-gke/test/terraform-policy.test.ts`
  failed as intended. The new deterministic guard found the obsolete fixed
  `1–32` Gateway claims in the architecture and browser guides.
- GREEN: corrected root, architecture, and browser-Gateway guidance to describe
  one standalone Gateway, GKE DNS/GCE leased discovery, bounded all-node
  reconciliation, platform-owned scaling, and the Cloud Run/multiple-Gateway
  exclusions. The guard then passed.
- Added the reproducible deterministic 32/40-node profile at
  `build-protocol/reports/T-0128_CAPACITY_PROFILE.md`. It records the local
  Node 24/pnpm 11.9.0 environment, a two-start bound, all-node use, and its
  non-benchmark limitation. The existing GKE/GCE suites supply controlled DNS
  and lease scale-to-zero/return behavior; dynamic-subscription coverage proves
  retained definitions reactivate after membership returns.
- Focused evidence after `pnpm typecheck:build`:
  `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts packages/deployment-gke/test/terraform-policy.test.ts packages/deployment-gke/test/discovery/gke-node-discovery.test.ts packages/deployment-gce/test/registry/registry-reader.test.ts`
  passed 54/54. The build was required so Vitest could resolve workspace package
  `dist` entry points; it did not report a source failure.
- No runtime API or production-code gap was demonstrated, so no runtime refactor
  was made. Remaining uncertainty: this deterministic profile is not a real-cloud
  latency or throughput benchmark; operator-selected autoscaling and storage
  sizing remain deployment responsibilities.

## 2026-08-07 - Acceptance-audit correction

- RED: the added capacity-profile policy assertion failed because the recorded
  command omitted dynamic-subscription, durable-restart, and client recovery
  suites, and the report had no executable replacement evidence.
- GREEN: the profile command now runs dynamic-subscription reactivation,
  durable binding restart recovery, React authoritative re-query, and both GKE
  DNS/GCE leased-discovery suites. `DynamicUnaryForwarder` now has a focused
  deterministic scenario for compatible overlap and incompatible stop-all/new
  cutover. It changes no runtime production code or API.
- The resulting composition proves one shared Gateway behavior against both
  platform discovery mechanisms: scale-to-zero/return, retained-definition
  reactivation, durable-definition restart recovery, client reconnect/re-query
  contract, compatible overlap, and incompatible zero-node cutover. It makes no
  framework compatibility handshake or real-cloud benchmark claim.
- Expanded focused gate:
  `pnpm exec vitest run packages/auth/test/dynamic-unary-forwarder.test.ts packages/auth/test/dynamic-subscription-creator.test.ts packages/client-react/test/client-react.test.ts packages/server/test/server/durable-subscription-bindings.test.ts packages/deployment-gke/test/discovery/gke-node-discovery.test.ts packages/deployment-gce/test/registry/registry-reader.test.ts packages/deployment-gke/test/terraform-policy.test.ts`
  passed 260/260 after the already-recorded `pnpm typecheck:build` prerequisite.

## 2026-08-07 - Example-topology completeness correction

- A deterministic pre-review scan found an acceptance gap in the existing
  Message Board deployment example: its standalone Compose and Kubernetes
  material still described or launched two Gateway replicas with a fixed
  backend list. Wave 7 permits exactly one standalone Gateway; multiple
  Gateways are deferred to Wave 8.
- The correction returns to the existing `implementer` profile, explicitly
  dispatched as `gpt-5.6-terra` / `medium`. The selected surface does not
  expose runtime-model introspection; the immutable configured profile and no
  visible mismatch are the available acceptance evidence. No subagents may be
  used.
- The owner must make local Compose's one-Gateway static discovery explicitly
  local-only, connect the Kubernetes example to `GkeNodeDiscovery`, reconcile
  affected beginner/reference documentation, add deterministic RED-first
  coverage, and push the correction immediately. The public runtime contract
  remains unchanged unless this concrete example integration demonstrates a
  blocker.

## 2026-08-07 - Message Board topology correction

- RED: the Compose/Kubernetes source-policy tests failed against the old two
  Gateway names/replica count and fixed Kubernetes backend list.
- GREEN: standalone Compose now has one `gateway` service and labels its two
  `BACKEND_URLS` values as a local static fixture. Kubernetes has one Gateway,
  sets `BACKEND_DISCOVERY_SERVICE`/`BACKEND_DISCOVERY_PORT`, and the example
  gateway constructs `GkeNodeDiscovery` from those values. The application
  image gains only the existing deployment-GKE package dependency; framework
  public API remains unchanged.
- The Compose lifecycle fixture now records the intended client interruption
  and single-Gateway restart recovery instead of a Wave-8-style failover.

## 2026-08-07 - Mechanical preflight

- Formatting, `git diff --check`, API documentation, and documentation-audience
  checks passed.
- `pnpm verify:task -- --no-coverage` passed 279/279 affected tests after the
  shared build, tooling typecheck, lint/TSDoc, documentation, generated-code,
  and release-readiness gates.
- The real standalone Docker lifecycle test failed after the intentional
  single-Gateway restart: its fresh client completed the authoritative query
  but timed out waiting for a subscription update. Compose cleanup completed.
  This is a pre-review implementation failure, not an accepted limitation.
- The focused correction returns to the existing `implementer`, explicitly
  dispatched as `gpt-5.6-terra` / `medium`; runtime-model introspection remains
  unavailable and no visible configured-profile mismatch exists. No subagents
  may be used. The owner must preserve the one-Gateway boundary, diagnose the
  lifecycle assumption, correct behavior or the scenario without weakening
  acceptance, rerun the real Docker test, then commit and push immediately.
- The first diagnostic pass proved the failure occurs in the initial
  single-shot `full` flow before Gateway restart. The authoritative query is
  green, while a command posted immediately after opening native activation can
  precede attachment readiness because the streaming protocol has no separate
  ready acknowledgement. This is consistent with the documented best-effort
  notification contract, not a durable reactivation loss.
- The same implementer is redispatched with explicit `gpt-5.6-terra` /
  `medium` to replace that single-shot probe with the existing bounded
  `distributed-full` scenario, which still requires a matching real update but
  tolerates activation startup by posting distinct commands for a finite
  interval. It must retain restart, durable cancellation, and authoritative
  query assertions, keep the package-image correction, and publish only after
  fresh-image Docker evidence is green.
- Fresh-image combined and standalone runs using the bounded matching-update
  probe remained red: authoritative queries observe every posted state, but no
  matching entity subscription update arrives across 20 finite attempts. This
  rules out the original activation-start race as the complete cause and
  demonstrates a runtime delivery defect.
- A read-only diagnosis is dispatched to the existing
  `performance/reliability reviewer` with explicit `gpt-5.6-terra` / `high`.
  Runtime-model introspection is unavailable; the immutable configured role
  profile and absence of a visible mismatch are recorded. The reviewer must
  trace the fresh-image domain/System event, Stand runtime, durable registry,
  and Gateway fan-in path and report a minimal evidence-backed correction; it
  may not edit files or spawn subagents.
- Reliability diagnosis found the exact deterministic defect in
  `DynamicUnaryForwarder.#activateChild`: native Subscribe returns a distinct
  private envelope for every application node, but activation forwarded the
  public Gateway definition instead of `child.backend`. Native services found
  no such ID and ended cleanly; the Gateway then silently removed each child.
  The System EventBus and Stand path are not the cause.
- The diagnostic reviewer was the existing performance/reliability role,
  explicitly configured as `gpt-5.6-terra` / `high`; runtime introspection was
  unavailable and no visible mismatch occurred. Its focused existing suite
  passed 47/47, exposing that fixtures reused opaque envelopes and did not
  assert per-node activation identity.
- The existing implementer is redispatched with explicit `gpt-5.6-terra` /
  `medium` to add a failing per-node-envelope test, activate with a defensive
  copy of each child backend envelope, retain the public-boundary rewrite, and
  rerun focused plus fresh-image Docker evidence. No persistence or public API
  change is authorized.
- RED/GREEN confirmed the private-envelope defect: the new two-node test first
  observed public bytes at both native activations, then passed after
  `#activateChild` defensively copied `child.backend`. The focused auth suite
  passed 58/58 and `pnpm typecheck:build` passed.
- The orchestrator rebuilt all local images successfully with the corrected
  source and deployment package tarballs. The fresh real suite then passed the
  combined topology, but standalone failed earlier during public Subscribe
  with gRPC `internal`: at least one of its two native Subscribe calls rejected
  before activation. This is distinct from the now-corrected ID mismatch.
- The same implementer is redispatched with explicit `gpt-5.6-terra` /
  `medium` to capture per-service diagnostics and isolate the two-node native
  creation failure. It must not weaken all-node subscription creation or hide
  the backend error; it may improve failure diagnostics and must preserve the
  already-green combined behavior.

## 2026-08-07 - One-Gateway lifecycle diagnosis

- The prior restart assertion was stale: its former second Gateway performed
  cancellation before `assert-cancelled`. The one-Gateway sequence must restart,
  reconnect, cancel through that restarted Gateway, verify closure, then
  authoritative-query. That ordering correction is staged.
- Rebuilding local images exposed and eliminated an image-packaging gap caused
  by the new deployment-GKE dependency: the offline image installer now packs
  both `@spine-event-engine/deployment` and `deployment-gke` rather than
  fetching the private workspace package from npm.
- The rebuilt real lifecycle still fails _before_ restart: the first public
  `full` flow completes its query but times out waiting for a subscription
  update with one static Gateway and two application nodes. This is a concrete
  one-Gateway static-fixture runtime integration failure, not a stale image or
  restart-order failure. The correction does not reintroduce the prohibited
  second Gateway or weaken the behavior assertion. Further runtime diagnosis is
  required before this task can claim the Docker lifecycle gate green.

## 2026-08-07 - Wave 7 runtime and lifecycle closure

- RED/GREEN runtime correction: a two-node native-subscription test first
  observed the public envelope at both native `Activate` calls. Each native
  backend now receives its own defensively copied private envelope, while the
  public Gateway envelope remains the durable binding definition. The focused
  auth suites passed 58/58 and `pnpm typecheck:build` passed.
- Fresh local images then passed both real Docker Compose paths: the combined
  two-node distributed subscription relayed a matching update; standalone
  performed the same flow, killed and restarted its one Gateway, cancelled
  through the restarted process, observed the expected closed public boundary,
  and completed an authoritative query.
- The public cancellation probe accepts only the two documented closure shapes:
  native `NotFound`, or the public Gateway's `PermissionDenied` with exact
  `gateway rejected denied` after a retired durable binding. It does not hide
  other backend failures.
- A deterministic image-contract RED exposed shutdown order inversion: the
  test started Delivery before Application, Combined, and Gateway but reversed
  a return list beginning with Application, killing Delivery before Combined.
  The list now preserves startup ownership order and the image identity checks
  name the Application and Combined positions explicitly. The focused SIGTERM
  matrix then passed.
- The staged image builder includes `deployment` and `deployment-gke` package
  tarballs for its offline install, preserving the new Kubernetes discovery
  dependency without a private-registry fetch. No public API, persistence
  schema, or runtime deployment topology was changed beyond the approved
  one-Gateway example integration.
- Final focused evidence: `pnpm exec vitest run
  packages/auth/test/dynamic-subscription-creator.test.ts
  packages/auth/test/dynamic-unary-forwarder.test.ts` passed 58/58;
  `pnpm test:images:local` passed 8/8; the fresh `node --test
  examples/message-board/deploy/compose/lifecycle.test.mjs` completed both
  Compose topologies successfully; and `pnpm verify:task -- --no-coverage
  packages/auth/test/dynamic-subscription-creator.test.ts
  packages/auth/test/dynamic-unary-forwarder.test.ts` passed its shared
  generated-build, tooling, lint, documentation, and focused-test profile.
