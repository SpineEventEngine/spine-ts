# T-0124 Work Log

## 2026-08-07 - Task start and implementation dispatch

- T-0123 is durably complete at `origin/main@92caa9fc`; T-0124 starts in its
  isolated worktree from that exact baseline.
- Classification: high-risk. The accepted Wave 7 plan already resolves the
  architecture and human decisions, so no new requirements-splitter invocation
  is required.
- Existing role: implementer. Expected explicit profile:
  `gpt-5.6-terra` / `medium`. Ownership covers the new
  `packages/deployment-gce/**` package and the minimum listener lifecycle seam
  in `packages/server/**`; no other production writer may overlap.
- Both model and reasoning are explicit in dispatch. Runtime self-introspection
  may be unavailable; the immutable configured role/profile is acceptable only
  when no visible mismatch or fallback occurs.
- The implementation must use strict RED/GREEN checkpoints for metadata and
  endpoint derivation, 20/60 timing, lost-response confirmation, serialized
  renewal/cleanup, shutdown quiescence, crash expiry, zero/return recovery, and
  40-node complete discovery. Every commit is pushed immediately.

## 2026-08-07 - Runtime acceptance convergence

- Existing role: implementer. The orchestrator explicitly dispatched
  `gpt-5.6-terra` / `medium`; this surface does not expose runtime
  self-introspection, and no visible profile mismatch or fallback occurred.
- `b16b0920` closed two bounded-operation findings: the unconfirmed ownership
  lookup now receives its own deadline-composed abort signal, and the default
  renewal timer is unref'ed. Its focused tests prove deadline abort propagation
  and that both deadline and scheduler handles are unref'ed.
- The original GCE forty-node test only proved snapshot delivery. The added
  composed coverage feeds `GceRegistryReader` through `ScheduledNodeDiscovery`
  into the existing dynamic Gateway; all forty nodes reconcile, route, and
  receive native subscription children. No Gateway production correction was
  required because its reconciliation/routing/subscription implementation was
  already complete.
- `81040ea3` adds table-driven metadata 400/503, empty/malformed project,
  zone, numeric-ID, and private-IP handling, external cancellation propagation,
  exact fake-clock 20-second renewal/60-second expiry evidence, and RED-first
  operation-timeout validation for nonpositive, fractional, and unsafe values.
- Deterministic evidence before the documentation pass: the focused GCE suite
  passes 34/34, deployment-GCE `tsc --noEmit` passes, and `git diff --check`
  passes. These are focused checks only; task/release preflight and specialist
  review remain pending.

## 2026-08-07 - Documentation and acceptance disposition

- Public GCE API TSDoc now documents metadata inputs, endpoint derivation,
  scheduler/deadline seams, registrar defaults, retry/close semantics, and
  discovery reader cancellation. The listener lifecycle seam documents its
  readiness, rollback, close, and network-gating contract.
- The package README and reference record metadata permissions, explicit
  registry storage/namespace ownership, private networking, identity and TLS
  rules, 20/60 timing, deadline behavior, scale-zero/crash recovery, shutdown
  order, and the T-0127 Terraform/beginner-guide boundary.

## 2026-08-07 - Metadata documentation verification

- Official metadata guidance was checked at
  <https://docs.cloud.google.com/compute/docs/metadata/querying-metadata> and
  <https://docs.cloud.google.com/compute/docs/metadata/predefined-metadata-keys>.
  The implementation uses the documented base URL
  `http://metadata.google.internal/computeMetadata/v1`, required
  `Metadata-Flavor: Google` header, and the `project/project-id`,
  `instance/zone`, `instance/id`, and `instance/network-interfaces/0/ip` paths.
- End-user documentation describes metadata as a VM-local trust boundary rather
  than claiming IAM permission is required. It contains no task or wave jargon;
  infrastructure guidance is described generically as a separate deployment
  guide.

## 2026-08-07 - Default assembly ergonomics

- `ScheduledNodeDiscovery` now supplies its own unref'ed production scheduler,
  `GceRegistryReader` defaults its clock to `Date.now`, and a registrar with a
  registry plus port defaults to `GceMetadataService`. The public seams remain
  injectable for deterministic testing.
- Focused evidence for this correction: deployment discovery plus GCE tests
  pass 62/62; deployment and deployment-GCE no-emit typechecks pass; formatting
  and diff checks pass. A new full task preflight remains required after the
  correction.

## 2026-08-07 - Coverage continuation

- Continuation remains with the existing implementer role, explicitly
  configured as `gpt-5.6-terra` / `medium`; runtime self-introspection is not
  exposed and no profile mismatch is visible.
- Stable task preflight completed every generation, build, tooling, cleanup,
  TSDoc, formatting, documentation, API, Buf, generated-cleanliness, and
  release-readiness gate. The expanded affected-suite run passed 249 tests but
  exited nonzero only because global branch coverage was 88.36% against the
  90% threshold (statements 90.83%, functions 93.05%, lines 92.44%).
- Follow-up scope is limited to focused behavioral tests for uncovered branches
  in the already affected deployment registry and server lifecycle code; it
  does not broaden runtime sources or alter coverage thresholds.
- The registry test now proves owner renewal and distinguishes live, expired,
  and absent leases. The server test proves a malformed non-string backend URL
  is rejected by the public `Server.start()` boundary before any standalone
  listener opens.
- The unchanged expanded affected-suite coverage command passed 251/251 tests
  with 92.67% statements, 90.25% branches, 94.44% functions, and 94.25% lines.
  `verify:task` was also invoked, reaching generation, generated build, and
  tooling typecheck; this execution surface ended its command session before
  later silent gates could report. Those later deterministic gates had passed
  in the immediately preceding 249-test preflight and no runtime source was
  changed by this coverage correction.

## 2026-08-07 - Accepted P1/P2 correction batch

- The existing T-0124 implementation owner is correcting the accepted review
  batch with the explicit `implementer` role profile `gpt-5.6-terra` /
  `medium`. This surface does not expose independent runtime metadata; no
  profile mismatch is visible.
- The correction scope is limited to the accepted metadata cancellation,
  registrar shutdown, leased-registry cancellation, listener lifecycle,
  public API, GCE source/test structure, beginner guide, and mechanical
  findings. Each runtime correction will use a focused RED/GREEN regression
  before its smallest implementation change.
- RED/GREEN: a metadata-service regression first failed because sibling fetch
  signals stayed live after one HTTP failure. `GceMetadataService` now combines
  caller cancellation with an internal request group and aborts that group on
  the first failure; the focused GCE suite passes 37/37 and the package
  no-emit typecheck, formatting, and diff checks pass.
- RED/GREEN: a registrar regression first observed a renewal timer scheduled
  after `close()` fenced a stalled initial registration. The scheduling boundary
  now rejects closed registrars; the focused GCE suite passes 38/38 with the
  same typecheck, formatting, and diff evidence.
- RED/GREEN: a paginated registry read first made a second storage query after
  cancellation arrived between pages. Pagination now checks the caller signal
  before and after every page; the focused registry suite passes 21/21 with
  the deployment no-emit typecheck, formatting, and diff checks clean.
- RED/GREEN: listener lifecycle regressions first proved FIFO rollback and a
  cleanly rolled-back listener-start failure could restart. Admitted lifecycles
  now close in LIFO order, and a fully rolled-back listener-start failure marks
  its builder terminal; the focused lifecycle suite passes 49/49 with server
  no-emit typecheck, formatting, and diff checks clean.
- Public-contract correction: `ListenerLifecycle` is now exported from the
  server root, and registrar options are a truthful union that accepts an
  explicit node or a required metadata port. Tooling typechecking first
  rejected the missing root export and now passes, as do 89 focused GCE/server
  lifecycle tests. The beginner README now explicitly directs Terraform
  modules and deployment procedures to the separate deployment guide.
- Structural correction was not complete in the original implementation
  context. Its entrypoint scaffold existed, but registrar production and tests
  remained monolithic; no structural verification result was accepted then.

## 2026-08-07 - Structural correction handoff

- The original implementation context reached its execution-window limit. Its
  uncommitted scaffold does not yet satisfy the accepted semantic split because
  registrar production and test files remain monolithic; no structural commit
  was accepted.
- A fresh existing `implementer` role receives only this unfinished structural
  correction. Expected explicit profile: `gpt-5.6-terra` / `medium`. Both
  fields are explicit in dispatch. Runtime self-introspection may be
  unavailable; accept the immutable configured role/profile only when no
  mismatch or fallback is visible.

## 2026-08-07 - Structural correction completion

- The existing `implementer` role completed the accepted P2 with the explicit
  configured profile `gpt-5.6-terra` / `medium`. This execution surface does
  not expose independent runtime metadata; no visible profile mismatch or
  fallback occurred.
- The package root now only re-exports public API. Production ownership is
  separated into application-node endpoint derivation, metadata transport and
  parsing, operation deadline/signal/timer helpers, registrar lease lifecycle,
  and registry reading. Public exports and behavior remain unchanged.
- Focused tests now separately own node/endpoint, metadata, registrar
  lifecycle, registrar recovery, and registry-reader/discovery plus the
  forty-node Gateway composition. No production or test file is a retained
  500-line or approximately 1,000-line monolith.
- The first focused split run exposed one omitted metadata-test delimiter and
  one dropped defensive constructor check; both were corrected before the
  passing run. Focused evidence after correction: 39/39 GCE tests pass,
  deployment-GCE `tsc --noEmit` exits 0, Prettier check, focused ESLint,
  TSDoc enforcement, API-documentation and audience checks, and `git diff
--check` all pass. The API-documentation expectation now includes the
  previously exported `ListenerLifecycle` contract.
- The selected stable profile was source-scoped coverage over the five GCE
  test files and five split production files. It reached Node validation, Proto
  generation/checksums, generated build, and tooling typecheck. This execution
  surface then detached its command output before later gates could report a
  final status; process inspection later confirmed no verifier process or
  generated-worktree residue. The already-observed focused lint, TSDoc,
  formatting, API/audience documentation, diff, test, and package typecheck
  evidence remains the available deterministic result; no unobserved later
  gate is claimed as passed.

## 2026-08-07 - Deterministic preflight correction

- The stable task preflight reported one mechanical failure at
  `packages/deployment/test/leased-node-registry.test.ts:117`:
  `@typescript-eslint/no-confusing-void-expression` rejected the concise
  callback that returned `controller.abort()`.
- The callback now uses a braced body and returns no expression, preserving
  the cancellation-between-pages behavior. The correction remains with the
  existing `implementer` role configured as `gpt-5.6-terra` / `medium`.
  Runtime self-introspection is unavailable; no visible profile mismatch or
  fallback occurred.

## 2026-08-07 - Cleanup-gate structural correction

- The deterministic cleanup gate rejected the flat deployment-GCE source
  layout, two over-composed internal registrar option names, and the standalone
  operation helper. The correction retains the package root as exports only.
- Production is now organized by metadata, node derivation, registrar
  lifecycle/operation ownership, and registry reading; focused tests mirror
  those semantic areas. `ExplicitRegistrarOptions` and
  `MetadataRegistrarOptions` preserve the existing truthful public union.
- `GceOperationRunner.run()` now owns deadline/signal execution for each
  registrar lifecycle, preserving deadline release and shutdown behavior while
  eliminating the standalone operation function. This remains a structural
  correction with no intended public behavior change.
- Staging the semantic moves exposed shortened public comments from the prior
  scaffold; required detailed TSDoc was restored in the moved modules. The
  cleanup checker and TSDoc enforcement pass. Focused evidence also passes:
  39/39 deployment-GCE tests, package `tsc --noEmit`, focused ESLint,
  API-documentation and audience checks, Prettier, and both working-tree and
  staged diff checks.

## 2026-08-07 - Listener lifecycle deferred-cleanup P1

- The remaining focused reliability finding uses the existing `implementer`
  role configured as `gpt-5.6-terra` / `medium`. Runtime self-introspection is
  unavailable; no visible profile mismatch or fallback occurred.
- RED: a listener start failure admitted one lifecycle, whose rollback
  succeeded, followed by a one-time network-close failure. The next
  `Server.start()` incorrectly rejected as terminal instead of first retrying
  the unfinished network cleanup.
- GREEN: `RunningHttp2Server` now retains deferred cleanup whenever its close
  attempt remains pending, rather than basing retryability only on admitted
  listener lifecycles. The next `start()` completes cleanup and rejects with
  the existing deferred-cleanup terminal result; a later start remains
  terminal. Focused evidence: the new regression passes, server lifecycle
  integration passes 51/51, server `tsc --noEmit`, focused ESLint, TSDoc,
  Prettier, and `git diff --check` pass.
