# T-0109 Work Log

Status: In Progress

## 2026-08-05 — task start

- Created the isolated task branch/worktree from `origin/main@cceb72ca`.
- Classified the task high-risk because it changes commit/event ordering,
  subscription persistence and convergence, timers, lifecycle, and multi-node
  listener behavior.
- Inherited a clean exact-baseline post-merge `verify:release` from T-0108.
- Recorded three independent read-only Explorer assignments. Each dispatch uses
  explicit `gpt-5.6-terra` / `medium`; runtime metadata acceptance will be
  recorded when results arrive.

## Initial Stand/service code map

- The first Explorer completed read-only mapping with the explicitly dispatched
  `gpt-5.6-terra` / `medium` profile. Runtime self-introspection was not exposed;
  explicit dispatch and the configured function are the available metadata,
  with no visible mismatch.
- Accepted result: Stand currently stores state and owns process-local schema/
  tenant callbacks; EventBus already supports internal type-URL observers after
  persistence and dispatcher completion. SubscriptionService independently owns
  durable inactive records, claims, timers, local maps, activation attachments,
  and cancellation instead of using `StandSubscriptionRegistry`.
- Matching/encoding for event and Entity updates currently lives in
  `ServiceValues`. Aggregate persistence has a deferred post-persistence Stand
  notification seam, while Projection and Process Manager paths update Stand
  separately. No TS producer currently emits the frozen `EntityStateChanged`.

## Entity commit-path code map

- The second Explorer completed with explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection was not exposed; the explicit dispatch and
  configured function are accepted metadata with no visible mismatch.
- `TransactionalEntity.commit()` is too early for publication because durable
  Stand/history/event writes can still fail. The implementation must capture an
  independent accepted-transition snapshot there and publish only at the later
  family-specific durable seams.
- Aggregate publication belongs after aggregate Stand/history/EventStore
  persistence and before stored-event dispatch. Projection publication belongs
  after Stand and optional history persistence. Process Manager publication
  belongs after state/history/diagnostic persistence and before follow-up
  events/commands.
- Command/event source IDs and tenant causality should reuse `SignalMetadata`
  conventions. Rollback, pre-durable failure, retry, and replay tests must prove
  that only actual durable commits produce the frozen system event.

## Registry reconciliation code map

- The third Explorer completed with explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection was not exposed; the explicit dispatch and
  configured function are accepted metadata with no visible mismatch.
- Complete storage snapshots are already stabilized. `get(id)` plus exact
  revision is the required pre-attachment fence. No current component schedules
  registry cleanup or snapshot reconciliation.
- Bounded Context has no public start phase. Stand therefore needs an internal
  start/readiness tail kicked after repository registration; service activation
  waits that tail. Context close must stop the timer/reconciler before closing
  EventBus, Stand state handles, or the registry.
- Reuse the DeliverySupervisor pattern: one unref'ed timer, serialized work,
  close-state gates, and awaited active work. Existing custom registry and fake
  timer fixtures cover snapshot races and timer-free shutdown.

## Implementation assignment

- One existing `implementer` owns the complete bounded production/test slice to
  avoid overlapping runtime edits. Expected profile is explicitly
  `gpt-5.6-terra` / `medium`; the dispatch must include both fields. Runtime
  metadata or its limitation is recorded before acceptance.

## Implementation checkpoint 1

- Existing implementer dispatch used explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection was not exposed; the immutable configured role and
  explicit dispatch are accepted metadata with no visible mismatch.
- Commit `44be23f6` was pushed. Subscribe creates the context registry
  definition, Activate transitions it to active, and Cancel physically deletes
  it. Multitenant contexts use a shared non-tenant subscription storage scope.
- The focused lifecycle test and two multitenant event tests pass. Five legacy
  durable-claim service tests still fail because the obsolete service-owned
  persistence system temporarily coexists with the registry. This checkpoint is
  not accepted as task convergence; the implementer continues with Stand-owned
  reconciliation and removal of the second definition system.

## Implementation checkpoint 2

- Commit `03a25630` was pushed by the existing explicitly configured
  `gpt-5.6-terra` / `medium` implementer. Runtime self-introspection remains
  unavailable; no visible profile mismatch occurred.
- Stand now begins registry cleanup and complete-snapshot work after repository
  registration, and close awaits admitted reconciliation. The focused context
  RED/GREEN test and server TypeScript build pass.
- This is lifecycle scaffolding only. Timer cadence, revision-fenced local
  attachments/sweep, EventBus observation, `EntityStateChanged` publication,
  and legacy service-persistence removal remain open.

## Implementation checkpoint 3

- Commit `52ee2f0f` was pushed. Stand now runs one immediate and then unref'ed
  10-second cleanup/snapshot cadence, serializes cycles, and clears/awaits the
  active cycle during close. Focused lifecycle and server typecheck pass.
- The implementer turn ended after this scaffolding checkpoint. Its explicit
  configured profile was `gpt-5.6-terra` / `medium`; runtime introspection was
  unavailable and no visible mismatch occurred. The result is accepted only as
  partial progress.
- The same implementation context was immediately redispatched to finish
  revision-fenced attachments/sweeps, EventBus observation and matching,
  post-durable system-event publication, and removal of the obsolete service
  persistence system before specialist review.

## Implementation-context handoff

- The original implementer identified the Stand-owned attachment adapter as the
  next seam but could not execute that bounded slice within its remaining
  runtime window. It made no further edit; the branch remained clean at the
  pushed checkpoints. This is not an accepted result or repository blocker.
- A fresh existing `implementer` is assigned revision-fenced local attachment
  and monotonic sweep behavior only. Expected dispatch is explicitly
  `gpt-5.6-terra` / `medium`; runtime metadata or its limitation must be recorded
  before acceptance. It owns the affected Stand/service production and focused
  tests for this serialized slice and must not overlap another writer.

## Implementation checkpoint 4

- Commit `0433204f` was pushed by the fresh existing implementer. Dispatch was
  explicit `gpt-5.6-terra` / `medium`; runtime self-introspection was not
  exposed and no visible mismatch occurred.
- Stand now serializes local attachment reconciliation, revalidates exact active
  registry ID/revision before attachment, removes listeners absent from a
  completed snapshot, and unsubscribes on local removal and close. Service
  activation delegates local attachment ownership through the internal Stand
  seam.
- Focused Stand tests pass 32/32 and server TypeScript passes. Seven legacy
  service-persistence/claim tests and two stale context storage-order
  assumptions remain outside this bounded slice. The checkpoint is accepted as
  partial progress; post-durable system-event publication and full service-store
  removal remain required.

## System-event publication assignment

- A fresh existing `implementer` owns the non-overlapping post-durable
  `EntityStateChanged` publisher and repository-family integration/tests.
  Expected dispatch is explicitly `gpt-5.6-terra` / `medium`; runtime metadata
  or its limitation must be recorded before acceptance. No service persistence
  removal is included in this ownership slice.

## Implementation checkpoint 5

- Commit `d6ddc51c` was pushed by the explicitly configured
  `gpt-5.6-terra` / `medium` implementer. Runtime self-introspection was not
  exposed and no visible mismatch occurred.
- A shared internal publisher now constructs the frozen `EntityStateChanged`
  envelope and is invoked from Aggregate, Projection, and Process Manager
  post-durable paths. It preserves causal context, packs entity/source IDs,
  includes old/new state, time, and version, keeps the internal schema out of
  public event routes, and treats follow-up publication failure as a bounded
  diagnostic rather than mutation rollback.
- The Aggregate focused RED/GREEN test and server TypeScript pass. The result is
  partial: family-specific Projection/Process Manager and rollback/failure/
  retry proofs remain assigned to the same implementation context before review.
