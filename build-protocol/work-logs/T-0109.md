# T-0109 Work Log

Status: In Progress

## 2026-08-05 — task start

- Created the isolated task branch/worktree from `origin/main@cceb72ca`.
- Classified the task high-risk because it changes commit/event ordering,
  subscription persistence and convergence, timers, lifecycle, and multi-node
  listener behavior.
- Inherited a clean exact-baseline post-merge `verify:release` from T-0108.
- Recorded three independent read-only Explorer assignments. Each dispatch uses
  explicit `gpt-5.6-terra` / `medium`; runtime metadata acceptance will be
  recorded when results arrive.

## Initial Stand/service code map

- The first Explorer completed read-only mapping with the explicitly dispatched
  `gpt-5.6-terra` / `medium` profile. Runtime self-introspection was not exposed;
  explicit dispatch and the configured function are the available metadata,
  with no visible mismatch.
- Accepted result: Stand currently stores state and owns process-local schema/
  tenant callbacks; EventBus already supports internal type-URL observers after
  persistence and dispatcher completion. SubscriptionService independently owns
  durable inactive records, claims, timers, local maps, activation attachments,
  and cancellation instead of using `StandSubscriptionRegistry`.
- Matching/encoding for event and Entity updates currently lives in
  `ServiceValues`. Aggregate persistence has a deferred post-persistence Stand
  notification seam, while Projection and Process Manager paths update Stand
  separately. No TS producer currently emits the frozen `EntityStateChanged`.

## Entity commit-path code map

- The second Explorer completed with explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection was not exposed; the explicit dispatch and
  configured function are accepted metadata with no visible mismatch.
- `TransactionalEntity.commit()` is too early for publication because durable
  Stand/history/event writes can still fail. The implementation must capture an
  independent accepted-transition snapshot there and publish only at the later
  family-specific durable seams.
- Aggregate publication belongs after aggregate Stand/history/EventStore
  persistence and before stored-event dispatch. Projection publication belongs
  after Stand and optional history persistence. Process Manager publication
  belongs after state/history/diagnostic persistence and before follow-up
  events/commands.
- Command/event source IDs and tenant causality should reuse `SignalMetadata`
  conventions. Rollback, pre-durable failure, retry, and replay tests must prove
  that only actual durable commits produce the frozen system event.

## Registry reconciliation code map

- The third Explorer completed with explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection was not exposed; the explicit dispatch and
  configured function are accepted metadata with no visible mismatch.
- Complete storage snapshots are already stabilized. `get(id)` plus exact
  revision is the required pre-attachment fence. No current component schedules
  registry cleanup or snapshot reconciliation.
- Bounded Context has no public start phase. Stand therefore needs an internal
  start/readiness tail kicked after repository registration; service activation
  waits that tail. Context close must stop the timer/reconciler before closing
  EventBus, Stand state handles, or the registry.
- Reuse the DeliverySupervisor pattern: one unref'ed timer, serialized work,
  close-state gates, and awaited active work. Existing custom registry and fake
  timer fixtures cover snapshot races and timer-free shutdown.

## Implementation assignment

- One existing `implementer` owns the complete bounded production/test slice to
  avoid overlapping runtime edits. Expected profile is explicitly
  `gpt-5.6-terra` / `medium`; the dispatch must include both fields. Runtime
  metadata or its limitation is recorded before acceptance.

## Implementation checkpoint 1

- Existing implementer dispatch used explicit `gpt-5.6-terra` / `medium`.
  Runtime self-introspection was not exposed; the immutable configured role and
  explicit dispatch are accepted metadata with no visible mismatch.
- Commit `44be23f6` was pushed. Subscribe creates the context registry
  definition, Activate transitions it to active, and Cancel physically deletes
  it. Multitenant contexts use a shared non-tenant subscription storage scope.
- The focused lifecycle test and two multitenant event tests pass. Five legacy
  durable-claim service tests still fail because the obsolete service-owned
  persistence system temporarily coexists with the registry. This checkpoint is
  not accepted as task convergence; the implementer continues with Stand-owned
  reconciliation and removal of the second definition system.

## Implementation checkpoint 2

- Commit `03a25630` was pushed by the existing explicitly configured
  `gpt-5.6-terra` / `medium` implementer. Runtime self-introspection remains
  unavailable; no visible profile mismatch occurred.
- Stand now begins registry cleanup and complete-snapshot work after repository
  registration, and close awaits admitted reconciliation. The focused context
  RED/GREEN test and server TypeScript build pass.
- This is lifecycle scaffolding only. Timer cadence, revision-fenced local
  attachments/sweep, EventBus observation, `EntityStateChanged` publication,
  and legacy service-persistence removal remain open.

## Implementation checkpoint 3

- Commit `52ee2f0f` was pushed. Stand now runs one immediate and then unref'ed
  10-second cleanup/snapshot cadence, serializes cycles, and clears/awaits the
  active cycle during close. Focused lifecycle and server typecheck pass.
- The implementer turn ended after this scaffolding checkpoint. Its explicit
  configured profile was `gpt-5.6-terra` / `medium`; runtime introspection was
  unavailable and no visible mismatch occurred. The result is accepted only as
  partial progress.
- The same implementation context was immediately redispatched to finish
  revision-fenced attachments/sweeps, EventBus observation and matching,
  post-durable system-event publication, and removal of the obsolete service
  persistence system before specialist review.

## Implementation-context handoff

- The original implementer identified the Stand-owned attachment adapter as the
  next seam but could not execute that bounded slice within its remaining
  runtime window. It made no further edit; the branch remained clean at the
  pushed checkpoints. This is not an accepted result or repository blocker.
- A fresh existing `implementer` is assigned revision-fenced local attachment
  and monotonic sweep behavior only. Expected dispatch is explicitly
  `gpt-5.6-terra` / `medium`; runtime metadata or its limitation must be recorded
  before acceptance. It owns the affected Stand/service production and focused
  tests for this serialized slice and must not overlap another writer.

## Implementation checkpoint 4

- Commit `0433204f` was pushed by the fresh existing implementer. Dispatch was
  explicit `gpt-5.6-terra` / `medium`; runtime self-introspection was not
  exposed and no visible mismatch occurred.
- Stand now serializes local attachment reconciliation, revalidates exact active
  registry ID/revision before attachment, removes listeners absent from a
  completed snapshot, and unsubscribes on local removal and close. Service
  activation delegates local attachment ownership through the internal Stand
  seam.
- Focused Stand tests pass 32/32 and server TypeScript passes. Seven legacy
  service-persistence/claim tests and two stale context storage-order
  assumptions remain outside this bounded slice. The checkpoint is accepted as
  partial progress; post-durable system-event publication and full service-store
  removal remain required.

## System-event publication assignment

- A fresh existing `implementer` owns the non-overlapping post-durable
  `EntityStateChanged` publisher and repository-family integration/tests.
  Expected dispatch is explicitly `gpt-5.6-terra` / `medium`; runtime metadata
  or its limitation must be recorded before acceptance. No service persistence
  removal is included in this ownership slice.

## Implementation checkpoint 5

- Commit `d6ddc51c` was pushed by the explicitly configured
  `gpt-5.6-terra` / `medium` implementer. Runtime self-introspection was not
  exposed and no visible mismatch occurred.
- A shared internal publisher now constructs the frozen `EntityStateChanged`
  envelope and is invoked from Aggregate, Projection, and Process Manager
  post-durable paths. It preserves causal context, packs entity/source IDs,
  includes old/new state, time, and version, keeps the internal schema out of
  public event routes, and treats follow-up publication failure as a bounded
  diagnostic rather than mutation rollback.
- The Aggregate focused RED/GREEN test and server TypeScript pass. The result is
  partial: family-specific Projection/Process Manager and rollback/failure/
  retry proofs remain assigned to the same implementation context before review.

## Implementation checkpoint 6

- Commit `47cdf62f` was pushed by the explicitly configured
  `gpt-5.6-terra` / `medium` implementer. Runtime self-introspection remained
  unavailable and no visible profile mismatch occurred.
- Focused repository tests now prove exact schema payload URLs, causal context,
  source signal types, version, one committed publication, and no-change
  suppression for Aggregate command/reactor, Projection, and Process Manager
  command/event paths. Three focused tests and server TypeScript pass.
- Rejection/rollback, pre-durable persistence failure, retry, and replay
  at-most-once proofs remain incomplete. The same implementation context retains
  this bounded proof-only ownership; expected dispatch remains explicitly
  `gpt-5.6-terra` / `medium`.

## Implementation checkpoint 7

- Commit `6b9f59ae` was pushed by the explicitly configured
  `gpt-5.6-terra` / `medium` implementer. Runtime self-introspection remained
  unavailable and no visible profile mismatch occurred.
- Four focused repository tests, server TypeScript, and `git diff --check` pass.
  Aggregate transition rejection now proves that rolled-back state publishes no
  `EntityStateChanged` event.
- A fail-once aggregate-history fixture suppressed the failed attempt correctly,
  but a later command produced no publication. The fixture was removed instead
  of weakening the proof. Pre-durable failure, retry, and replay remain an
  explicit correctness investigation before review.

## Publication retry investigation assignment

- A read-only `explorer` is assigned to determine whether the missing second
  publication is a production retry defect or a fixture/causal-observation
  mistake, and to identify the smallest deterministic proof seam. Expected
  dispatch is explicitly `gpt-5.6-terra` / `medium`; runtime metadata or its
  limitation must be recorded before accepting the result.

## Service lifecycle simplification assignment

- One fresh existing `implementer` owns `SpineServices` subscription lifecycle,
  its obsolete durable-record helper, and directly corresponding service tests.
  It must make `StandSubscriptionRegistry` the sole definition store and leave
  only bounded active transport streams/queues in the service. It must not edit
  repository state-change publication files owned by the retry investigation.
- Expected dispatch is explicitly `gpt-5.6-terra` / `medium`; runtime metadata
  or its limitation must be recorded before acceptance. The seven currently
  failing service tests are confirmed obsolete claim/TTL/cancel expectations;
  replacement tests must prove registry create/activate/physical-delete and
  local stream cleanup rather than preserve the superseded system.

## Stand observer refinement assignment

- A read-only `explorer` is assigned to map the remaining architectural gap:
  current reconciliation attaches only when a process-local service callback is
  present, so a second node that discovers the same durable active definition
  cannot yet observe its EventBus independently. The explorer must identify the
  smallest deep-module design that lets Stand build event and Entity-state
  observers from the canonical registry entry while leaving only transport
  consumers in `SpineServices`.
- Expected dispatch is explicitly `gpt-5.6-terra` / `medium`; runtime metadata
  or its limitation must be recorded before accepting the result. This is
  read-only and must not overlap the active service writer.

## Stand observer refinement result

- The existing implementer completed the read-only refinement with explicit
  `gpt-5.6-terra` / `medium`. Runtime self-introspection was unavailable and no
  visible mismatch occurred.
- Stand must be the sole owner of canonical topic validation, registry-entry
  interpretation, per-node EventBus observers, and conversion to native
  updates. `SpineServices` retains only bounded local transport consumers.
- A new internal observer module will deepen the existing event/entity matcher,
  tenant/filter/mask, `no_longer_matching`, and redaction logic rather than
  duplicate it. Shared-registry tests must prove two Stands independently
  observe their local EventBuses, revision replacement, physical-delete
  convergence, and close isolation. No public or Proto change is required.

## Publication retry investigation result

- The explicitly configured `gpt-5.6-terra` / `medium` explorer completed its
  read-only investigation. Runtime self-introspection was unavailable and no
  visible profile mismatch occurred.
- The missing retry publication exposes a real Aggregate persistence atomicity
  defect when a later history/event write fails after Stand current state was
  already written. `deferred.cancel()` settles admission but does not restore
  the earlier current record. The correction must compensate conditionally or
  provide an equivalent atomic boundary before publication can be accepted.
- Reusing the same command ID is separately suppressed by intended Inbox
  deduplication. Deterministic proof must fail a durable write, assert no visible
  partial state or system event, retry with a new command ID, and observe exactly
  one committed update. Successful replay must prove no extra committed update.
  Asynchronous observation must use a bounded condition, not a fixed delay.

## Aggregate durability correction planning

- The defect changes transaction/concurrency semantics and the storage-adapter
  contract, so the existing `requirements_splitter` is invoked at this
  demonstrated architectural block. It owns a bounded correction design only;
  it must not edit production files.
- Expected dispatch is explicitly `gpt-5.6-sol` / `high`; runtime metadata or
  its limitation must be recorded before accepting the design. The design must
  preserve adapter implementability across in-memory, Datastore, and RDBMS
  storage without claiming cross-store atomicity the APIs cannot provide.

## Aggregate durability correction design

- The existing `requirements_splitter` completed with explicit
  `gpt-5.6-sol` / `high`. Runtime self-introspection was unavailable and no
  visible mismatch occurred.
- Compensating restore/delete, current-last reordering, and a broad public
  transaction API are rejected. The accepted bounded design is recorded in
  `ATOMIC_ENTITY_COMMIT_BRIEF.md`: one provider-internal atomic Entity commit
  port with scoped idempotency receipt, expected-current conflict detection,
  and complete current/history/EventStore mutation.
- This is a separate prerequisite slice inside T-0109 because it changes the
  storage SPI, all three current adapters, and repository sequencing. It covers
  Aggregate, Projection, and Process Manager because all share the defect. A
  durable notification outbox remains excluded.

## Atomic Entity commit implementation assignment

- One existing `implementer` owns the storage SPI, in-memory/Datastore/MySQL
  provider implementations, Stand notification-only deferral, repository-family
  sequencing, and the conformance/failure tests named in the accepted brief.
  The active service owner retains exclusive ownership of `SpineServices` and
  its tests; the Stand observer refinement is read-only until this slice lands.
- Expected dispatch is explicitly `gpt-5.6-terra` / `medium`; runtime metadata
  or its limitation must be recorded before acceptance. Current official
  Datastore transaction limits must be verified from primary documentation and
  the exact bound/assumption recorded. No JVM build or source change is allowed.

## Atomic Entity commit implementation decomposition

- The first implementation owner stopped without edits because the assignment
  combined a new SPI, three providers, Stand/repository migration, and shared
  conformance infrastructure beyond one bounded child slice. Its explicit
  profile was `gpt-5.6-terra` / `medium`; runtime self-introspection was
  unavailable and no mismatch occurred.
- The work is decomposed serially at the contract boundary: first the internal
  SPI, in-memory provider, Stand/repository integration, and common conformance
  harness; then independent Datastore and MySQL providers in isolated
  worktrees. Google primary documentation confirms a 10 MiB transaction-size
  limit, 270-second maximum, ambiguous commit acknowledgement, and a 25
  entity-group limit specifically for legacy/Optimistic-with-entity-groups
  mode.

## Atomic commit core assignment

- The first bounded implementation slice owns only the internal Entity commit
  SPI and receipt semantics, the in-memory provider, the shared conformance
  harness, notification-only Stand deferral, and Aggregate/Projection/Process
  Manager repository sequencing. Datastore and MySQL implementations remain
  subsequent isolated provider slices after this contract compiles and passes.
- Expected dispatch is explicitly `gpt-5.6-terra` / `medium`; runtime metadata
  or its limitation must be recorded before acceptance. The completed service
  lifecycle files are outside this ownership.

## Atomic commit core contract refinement

- The bounded owner stopped without edits after confirming that an
  Entity-handle receipt alone cannot atomically include the separately opened
  framework EventStore. Its explicit profile was `gpt-5.6-terra` / `medium`;
  runtime self-introspection was unavailable and no mismatch occurred.
- The provider-only seam is now concrete: the structural storage factory creates
  an Entity commit handle that directly owns current/history and canonical
  EventStore record mutations in one provider transaction. The common storage
  package factors EventStore record materialization for provider use; repository
  commit flow no longer opens a separate EventStore. Missing capability fails
  repository assembly, so incremental implementation introduces no fallback.

## Service lifecycle simplification checkpoint

- The explicitly configured `gpt-5.6-terra` / `medium` implementer removed
  service-owned subscription-record persistence, recovery scans, claim and
  cancellation markers, inactive timers, and storage access from
  `SpineServices`. The configured `StandSubscriptionRegistry` now creates,
  activates, and physically deletes canonical definitions.
- The service retains only active local delivery queues and attachments. It
  reconstructs an activation from the canonical registry entry, including an
  activation served by another service instance, and closes/removes its local
  stream before idempotent registry deletion.
- Focused service evidence: 105 passing tests and 55 deliberately skipped
  legacy service-owned persistence tests; server TypeScript, ESLint for changed
  files, and `git diff --check` pass. The legacy test bodies remain to be
  physically removed or replaced before final review; they are not acceptance
  evidence. Runtime self-introspection is unavailable on this surface; the
  immutable configured role/profile is the available metadata and no mismatch
  was visible.

## Service lifecycle test convergence

- The obsolete service-owned record/TTL/claim/CAS/recovery test blocks and
  their faulting storage fixtures were physically removed. No skipped service
  tests or obsolete `subscription-records` imports remain. The obsolete
  production helper was deleted.
- The retained 105 focused service tests cover the current service contract,
  including custom registry create/activate/delete, cross-service activation,
  local iterator cleanup, duplicate activation, queue bounds, topic matching,
  and bounded unknown cancellation.
- The full focused service suite passes with 105 tests, zero skips. Server
  TypeScript, ESLint for the changed files, and `git diff --check` pass.
  Runtime self-introspection remains unavailable; the immutable explicitly
  configured `gpt-5.6-terra` / `medium` profile is the available metadata and
  no mismatch was visible.

## Atomic commit storage checkpoint

- Added the provider-only `EntityCommitStorage` SPI and the in-memory handle
  skeleton, including backend-scoped serialization, scoped receipts, replay and
  divergent-ID detection, expected-current conflict handling, and direct
  current/history/framework-event writes using the canonical EventStore record
  specification. This is not exported from the storage root.
- Storage TypeScript, changed-file ESLint, and `git diff --check` pass. The
  repository migration and conformance tests remain the next required slice;
  no final atomicity claim is made by this checkpoint.

## In-memory atomic commit checkpoint

- The in-memory provider now stages independent Entity current/history and
  canonical EventStore maps under the backend commit lock, performs all
  validation and cloning against the stage, and replaces live maps plus the
  receipt synchronously in a no-throw apply phase. It does not use compensating
  rollback.
- Focused evidence passes: 11 storage test files / 118 tests, including complete
  visibility, replay, conflict, divergent receipt reuse, compatible-handle race,
  closed handles, and rejected preflight leaving every store unchanged. Storage
  TypeScript, changed-file ESLint, and `git diff --check` pass.
- This checkpoint was completed by the orchestrator after two explicitly
  configured `gpt-5.6-terra` / `medium` implementation handoffs established the
  SPI and receipt foundation. Repository/Stand migration and adapter providers
  remain required before final acceptance.

## Atomic provider assignments

- After the provider-only contract and in-memory conformance endpoint
  `921c83e1`, Datastore and MySQL implementations are independent write-heavy
  slices. Each receives an isolated branch/worktree based on that endpoint and
  must not edit common SPI, server, or the other adapter. Accepted provider
  commits will be merged into the T-0109 feature branch without rewriting their
  published history.
- The Datastore implementer is explicitly `gpt-5.6-terra` / `medium` and owns
  only `packages/storage-datastore` plus provider-specific tests/docs. The MySQL
  implementer is explicitly `gpt-5.6-terra` / `medium` and owns only
  `packages/storage-rdbms` plus provider-specific tests/docs. Runtime metadata
  or its limitation must be recorded before accepting either result.

## Repository integration refinement

- The repository implementer stopped without edits after identifying that
  `Stand.update()` currently delegates to the same deferred path used by
  repositories. The accepted split preserves public low-level `Stand.update()`
  as one direct current-record write plus notification, while the internal
  repository deferral becomes capture/notification-only after provider commit.
- The explicit dispatch was `gpt-5.6-terra` / `medium`; runtime
  self-introspection was unavailable and no visible mismatch occurred.

## Stand notification split checkpoint

- Public low-level `Stand.update()` now prepares, writes one current record, and
  notifies. The repository-only deferred seam prepares the same independent
  values and subscribers but performs no storage write; cancellation only
  settles admitted work.
- The focused Stand suite passes 32 tests, server TypeScript passes, and
  `git diff --check` passes. Repository commit-handle integration remains next.

## Aggregate atomic commit checkpoint

- Aggregate execution now requires the provider commit handle and submits one
  scoped unit containing expected/next current state, configured state history,
  diagnostic history, and delivery EventStore events. Only `committed` clears
  caches, notifies Stand, publishes `EntityStateChanged`, and enables domain
  dispatch; replay and conflict do not emit follow-up work.
- Internal `EntityStateChanged` now uses the direct stored-follow-up EventBus
  seam. It remains best effort and no longer enters the durable domain
  EventStore or awaits its own EventBus execution lane.
- Focused repository evidence passes 146 tests. Server TypeScript, focused
  ESLint, and `git diff --check` pass. Projection and Process Manager still use
  their prior independent persistence calls and remain the next core slice.

## Datastore dispatch correction

- The first Datastore dispatch reached an immutable requirements-splitting role,
  which correctly refused production edits. No files or repository state were
  changed. The result is rejected for implementation and must be redispatched
  to an existing `implementer` with explicit `gpt-5.6-terra` / `medium`.

## Projection and Process Manager atomic commit checkpoint

- Projection state and configured state history now use one provider-owned
  atomic commit. Process Manager state, configured state history, and
  diagnostic events use the same unit. Stand notification,
  `EntityStateChanged`, and produced-signal dispatch happen only after a new
  durable commit; replay and conflict do not repeat follow-up work.
- Read-side catch-up keeps the source event unchanged while assigning an
  internal per-run receipt scope. This allows a deliberately cleared
  Projection to be rebuilt repeatedly without confusing an earlier live commit
  receipt for the rebuild attempt.
- Focused evidence passes: 148 repository-routing tests, including forced
  atomic failures that expose no Projection or Process Manager state and emit
  no follow-up events. Server TypeScript, changed-file ESLint, and
  `git diff --check` pass.

## Stand-owned subscription observer checkpoint

- Stand now owns reconciliation of durable subscription definitions into local
  state and EventBus observers. The RPC adapter no longer supplies an observer
  factory: it attaches only a local stream consumer and translates an observed
  signal to the public wire update. The observer lifecycle is revision-fenced,
  runs immediately plus on the existing unref'd ten-second cadence, and is
  detached when a definition disappears or the Stand closes.
- The explicit implementer dispatch was `gpt-5.6-terra` / `medium`. Runtime
  self-introspection was unavailable in this surface; the immutable configured
  role/profile is the available metadata and no visible mismatch occurred.
- Focused evidence passes: 138 Stand and service tests, including two Stands
  and separate EventBuses sharing one registry while retaining local delivery;
  server TypeScript, changed-file ESLint, and `git diff --check` pass.

## Stand observer boundary correction

- The Stand observer now renders client-safe `SubscriptionUpdate` values from
  the canonical stored Topic before handing them to local stream consumers. It
  owns state filtering, field masking, `no_longer_matching`, tenant filtering
  for events, entity-ID packing, and rejection redaction. Services push the
  already-rendered value and retain admission validation, RPC lifecycle, and
  bounded queue ownership only. The retained service rendering helpers are
  isolated to structural test-double fallback contexts which cannot expose the
  built Stand seam.
- The explicit implementer dispatch remains `gpt-5.6-terra` / `medium`.
  Runtime self-introspection is unavailable; the immutable configured profile
  is the available metadata and no mismatch was visible.

## Stand observer maintainability correction

- The observer operations are now grouped in the documented internal
  `SubscriptionObservers` object. The exported delegate preserves the narrow
  internal seam without leaving semantic rendering helpers as free-standing
  production functions.
- Focused proof now posts event-target subscriptions to two separate local
  EventBuses after both Stands reconcile the same registry definition. Each
  update reaches only the consumer on the bus that posted it.
- Focused evidence passes: 139 Stand and service tests, server TypeScript,
  changed-file ESLint, Prettier, and `git diff --check`. The repository-wide
  TSDoc checker reports no issue in this slice; its remaining failures are in
  unrelated in-progress atomic storage files owned by the concurrent core
  integration work.

## MySQL atomic commit checkpoint

- The MySQL factory now supplies a provider-owned Entity commit handle backed
  by one InnoDB transaction. The transaction fences source-signal receipts,
  compares expected current state, replaces current state, appends configured
  state and diagnostic histories, and inserts canonical EventStore records and
  materialized columns before committing.
- Concurrent absent-row attempts use bounded deadlock retries. Commit failures
  release or destroy their first connection lease before reconciling an
  ambiguous acknowledgement through a fresh receipt read, so a pool with one
  connection cannot self-deadlock.
- MySQL package TypeScript, focused ESLint, formatting, and diff checks pass.
  The non-container suite passes 111 tests. A disposable MySQL 8.4 container
  passed all 17 live integration tests, including concurrent identical commit,
  replay, divergent receipt reuse, optimistic conflict, complete visibility,
  and rollback after a late canonical EventStore collision. Five stale test
  probes were corrected to address their explicitly configured storage key
  rather than the unrelated state schema type name.

## Datastore emulator convergence

- A local Firestore emulator in Datastore mode exposed symmetric independent
  clients repeatedly colliding after `ABORTED` transaction lock timeouts. The
  provider's existing three-attempt bound is preserved, with exponential
  jittered delay before retries so competing nodes do not repeat the same
  schedule.
- The isolated two-client history race passes, followed by the complete
  Datastore emulator profile at 8/8 tests. The emulator used the isolated local
  project `spine-ts-t0109`; no cloud project or live data was accessed.

## Combined preflight correction

- The first combined task preflight stopped during the repository TypeScript
  check on stale test-only callback, fixture, and removed-option typings. No
  runtime test had started and no production behavior failed.
- The focused correction makes test dispatchers return their promised
  `Promise<void>`, narrows optional event and fixture values explicitly, and
  removes the obsolete inactive-subscription option from its service test.
  Repository TypeScript compilation, focused ESLint, formatting, and diff
  checks pass before the complete preflight retry.

## Cleanup convergence assignment

- The existing implementer resumes ownership of
  `packages/server/src/stand/subscription-observer.ts` and the exact server
  standalone-function disposition ledger. The bounded assignment is to replace
  nested standalone helpers with named-object methods, remove the redundant
  exported delegate, update its Stand caller, and delete stale service
  dispositions without changing behavior.
- The expected and explicitly retained dispatch profile is the implementer role
  on `gpt-5.6-terra` / `medium`; the Desktop surface does not expose runtime
  self-introspection, so the immutable configured role/profile is the actual
  metadata available for acceptance. The owner is instructed not to spawn
  children and not to touch concurrent naming corrections.
- The accepted endpoint is `7685c46d`. It passes 139 focused Stand/service
  tests, server typecheck, ESLint, Prettier, cleanup enforcement, and diff
  checks. The combined cleanup checkpoint also passes repository TypeScript,
  focused ESLint, formatting, and cleanup enforcement after shortening the
  catch-up scope and in-memory commit names and the generated-event import.
- The deterministic TSDoc gate then identified only authored-comment layout
  debt in the new internal atomic-commit and observer declarations. The
  corrected declarations pass the complete repository TSDoc policy, focused
  ESLint, formatting, and diff checks.

## Bounded-context preflight regression

- The focused task gate exposed two test selectors that mistook the new durable
  subscription storage for the tenant index. They now select the exact internal
  tenant-index namespace.
- The same run exposed a replay fixture whose Process Manager handled and
  re-emitted `ProjectionState`, producing a self-feeding follow-up chain during
  bus drain. The fixture now emits an unrelated declared event shape while
  retaining the Aggregate and Process Manager replay assertions. The complete
  bounded-context suite passes 60/60 in 1.18 seconds; repository TypeScript,
  focused ESLint, formatting, and diff checks also pass.

## Storage receipt correction in progress

- Existing implementer assignment: storage-provider correction scope, explicitly
  configured `gpt-5.6-terra` / `medium`. Runtime self-introspection is not
  exposed by this surface; the immutable configured role/profile is the
  available metadata, with no visible mismatch.
- Datastore and MySQL receipts now persist a per-invocation owner token. The
  internal `EntityCommitStorageFactories.create(factory, input)` accessor is
  registered by provider factories, keeping commit construction off public root
  factory class surfaces. A lost Datastore acknowledgement is covered by a
  focused deterministic test and must return `committed` to its receipt owner.

## Final storage re-review correction

- Existing implementer assignment: strict exact-one provider proof and internal
  reference correction, explicitly configured `gpt-5.6-terra` / `medium`.
  Runtime self-introspection remains unavailable; the immutable configured
  profile is the available metadata with no visible mismatch.
- MySQL concurrent identical callers now assert exactly one `committed` and one
  `replayed` result. Datastore adds the equivalent competing-client assertion,
  alongside the existing lost-acknowledgement receipt-owner proof. The storage
  reference distinguishes `internal/entity-history` from
  `internal/entity-commit` and names `EntityCommitStorageFactories`.

## Final confirmation convergence

- The API confirmation found one contradictory storage-reference sentence. It
  now attributes atomic commits exclusively to `internal/entity-commit`; the
  documentation confirmation is clean, including relative links and formatting.
- The style confirmation required a real attached EventBus observer in the
  close proof and four omitted approved requirements in the ledger. Both are
  corrected.
- The reliability confirmation exposed an unguarded Process Manager post-commit
  Stand notification. `5abacea5` applies the shared failure boundary.
- The replacement implementer was explicitly dispatched as `gpt-5.6-terra` /
  `medium`. Runtime self-introspection was unavailable and no visible mismatch
  occurred. `6ada7260` proves Process Manager command and event transitions
  remain committed through throwing subscribers, later transitions continue,
  diagnostics are recorded, and close detaches a real active observer. The
  focused profile passes 187 tests plus server TypeScript, ESLint, formatting,
  and diff checks.
- `973e0ba2` strengthens shutdown proof by capturing the actual EventBus
  subscription handle and asserting that `Stand.close()` closes it. The final
  reliability confirmation is clean with 37 focused tests. All review concerns
  are closed; security remains N/A because no trust boundary or credential/
  authorization surface changed.

## Release-profile catch-up regression

- The first `verify:release` run passes every deterministic gate and 3,592
  tests, but deterministically fails the Todo read-side catch-up test: replay
  retains the deleted `Wrong` state and appends the rebuilt `Original` task.
- Root-cause tracing shows that T-0109 changed Projection and Process Manager
  loading from public `Stand.readVersioned()`, which hides deleted records, to
  internal `readCurrent()`, which retains tombstone metadata for atomic
  compare-and-set. The loaders incorrectly use the deleted record's state to
  instantiate the entity.
- The correct boundary keeps the tombstone as the atomic `expected` record but
  starts replay/continued read-side handling from the entity's default state.
  Aggregate lifecycle loading is separate and remains unchanged.
- A bounded implementer is explicitly dispatched as `gpt-5.6-terra` /
  `medium`. It must preserve the existing failing Todo test as RED, add a
  Process Manager tombstone-state RED proof, apply the smallest shared loader
  correction, and run focused server/Todo gates. Runtime self-introspection may
  be unavailable; the immutable configured profile is accepted absent a visible
  mismatch. It may not spawn children or modify unrelated paths.
- The implementer completed `c0bb470b` under the explicit Terra/medium profile;
  runtime self-introspection was unavailable and no mismatch was visible. The
  real Process Manager regression failed before the fix with deleted state
  applied twice, then passed after Projection and Process Manager initialization
  began from default state while retaining the complete tombstone for atomic
  compare-and-set. The original Todo catch-up passes 1/1, repository routing
  passes 151/151, and generated build, ESLint, formatting, and diff checks pass.

## Release branch-coverage diagnosis

- The corrected release run passes 184 test files and 3,594 tests but reports
  88.46% branch coverage against the 90% threshold. The deficit is concentrated
  in new provider atomic-commit code and subscription reconciliation; provider
  live profiles pass but are intentionally unavailable to standalone release
  verification. Production files will not be excluded merely to raise coverage.
- Two read-only explorers are explicitly dispatched as `gpt-5.6-terra` /
  `medium` because the Desktop surface offers no Luna child override. One owns
  MySQL/Datastore standalone-test seams; the other owns Stand/subscription
  observer branch gaps. Runtime self-introspection may be unavailable; the
  immutable dispatched profile is accepted absent a visible mismatch. They may
  not edit or spawn children.
- Both explorations completed under the explicit Terra/medium assignments;
  runtime self-introspection was unavailable and no mismatch was visible. They
  identify honest standalone tests capable of recovering the 201 branch hits
  needed without excluding production files.
- Three disjoint implementation owners are explicitly dispatched as the
  existing `implementer` role on `gpt-5.6-terra` / `medium`:
  1. provider atomic commits owns only the MySQL mock test and Datastore entity-
     history test/fixture;
  2. observer reconciliation owns a new observer test and the existing Stand
     test;
  3. repository/service outcomes owns only repository-routing and SpineServices
     tests.
     Each owner must exercise real behavior, run focused gates, commit once, push
     immediately, preserve concurrent edits, and not spawn children. Runtime self-
     introspection may be unavailable; the immutable configured profile is
     accepted absent a visible mismatch.
- All three owners completed under their explicit Terra/medium profiles; runtime
  self-introspection was unavailable and no mismatch was visible.
  `0cc54e59` adds 260 passing server replay/conflict and inert-activation tests.
  `e85f47cd` adds 40 passing real-EventBus observer/Stand tests for masked state,
  no-longer-matching, suppression, and rejection redaction. `6fd71681` adds 152
  passing standalone MySQL/Datastore atomic-commit tests for full commits,
  replay, conflict, preflight, retry, and ambiguous acknowledgement. TypeScript,
  ESLint, formatting, and diff gates pass in every lane; no production file or
  coverage exclusion changed.
- Combined coverage passes 185 test files and 3,606 tests and improves branch
  coverage from 88.46% to 89.44%. Seventy-three additional branch hits remain
  necessary. The same three implementers retain their explicit Terra/medium
  profiles for one bounded second slice over their existing files only:
  provider error/preflight/lifecycle outcomes; observer predicate/ID/mask
  variants; and Stand/service/repository cleanup/failure outcomes. No production
  edit, guard-only assertion, or coverage exclusion is authorized. Runtime
  metadata handling remains unchanged.
- The second slice completes under the retained Terra/medium profiles with no
  runtime metadata exposure or visible mismatch. `ecd6b3ec` adds 41 passing
  observer predicate/mask/tenant/type tests. `8845b020` adds 263 passing atomic-
  throw, state-follow-up failure, and cleanup-aggregation tests. `117464bd` adds
  162 passing provider preflight, lifecycle, retry-exhaustion, and failure-
  sanitation tests. All focused TypeScript, ESLint, formatting, and diff gates
  pass; changes remain test-only.
- A stable combined rerun passes 185 files and 3,620 tests at 89.59% branch
  coverage, leaving 53 required hits. The same three owners receive one final
  micro slice over remaining meaningful public outcomes only: provider divergent
  retry/identity/size/history cases; observer missing-target, invalid-filter,
  tenant-shape, and primitive-ID cases; and Stand/repository optional/lifecycle
  branches. Profiles, file ownership, and metadata limitations remain unchanged.
- The final micro slice completes under the retained Terra/medium profiles with
  no runtime introspection or visible mismatch. `5091fc6f` adds 43 passing
  observer fallback/filter tests; `5715b201` adds 264 passing unchanged-Process-
  Manager diagnostic tests; `fccc6001` adds 166 passing MySQL receipt/current/
  compatibility edge tests. The outcome commit's first push raced the other
  lanes, but the concurrent provider tip already contained it as an ancestor;
  no rewrite or work loss occurred. All changes are test-only and all focused
  gates pass.
- The capped combined run passes 185 files and 3,627 tests at 89.66% branch
  coverage, leaving 44 hits. This is a genuine release blocker. One exact final
  correction uses existing public schemas/fakes only: observer scalar ID/tenant
  variants, provider divergent-history/root/receipt cases, and in-memory commit
  lifecycle/current-comparison cases. The observer and provider implementers
  retain Terra/medium; a third disjoint implementer receives only the in-memory
  commit test under explicit Terra/medium. No production edit, custom synthetic
  descriptor, threshold change, or exclusion is authorized. Metadata handling
  remains unchanged.
- The exact final coverage correction completed under three explicitly
  dispatched existing `implementer` roles using `gpt-5.6-terra` / `medium`.
  Runtime self-introspection was unavailable and no visible profile mismatch
  occurred. Commit `fd830226` covers Datastore commit validation and divergent
  retries, `6c2b135b` covers in-memory commit comparison and lifecycle
  boundaries, and `1afa2cb4` covers observer scalar identifiers and tenant
  origins. Commit `643fbf39` corrects the optional expected-current fixture for
  `exactOptionalPropertyTypes`. The combined TypeScript check, 7/7 in-memory
  commit tests, 45/45 observer/Stand tests, 168/168 provider tests, ESLint,
  formatting, and diff checks pass. No production file, coverage threshold, or
  coverage exclusion changed.
- The combined suite passes 185 active files and 3,635 tests at 89.85% branch
  coverage, leaving 19 branch hits before the mandatory 90% gate. Two disjoint
  final test-only assignments are dispatched to the existing `implementer`
  role with explicit `gpt-5.6-terra` / `medium`: one owns only Stand observer
  byte/`Any` comparison behavior; the second owns only in-memory record query
  ordering/path behavior and the atomic-factory missing-provider outcome; the
  third owns only MySQL current-record mismatch behavior.
  Runtime self-introspection may be unavailable; the immutable configured
  profile is accepted absent a visible mismatch. Production edits, exclusions,
  threshold changes, and synthetic guard-only assertions are forbidden.
- All three assignments completed under their explicit Terra/medium profiles;
  runtime self-introspection was unavailable and no mismatch was visible.
  `98ac2875` covers equal and unequal raw `Any` and byte-array observer IDs
  (46/46 focused tests), `504c3dc0` covers MySQL absent/current and version
  conflicts (118/118), and `9376b615` covers primitive/null record paths,
  boolean ordering, and missing atomic-commit providers (39/39). Their focused
  TypeScript, ESLint, formatting, and diff checks pass. Every commit was pushed
  immediately and all changes remain test-only.
- The rerun passes 185 active files and 3,640 tests at 89.95%, six branch hits
  short. The in-memory owner retains the explicit Terra/medium implementer
  assignment for one last public record-query slice covering descending keyset
  continuation, multi-value filters, and deterministic list/object comparison
  boundaries. The same metadata and no-production-change constraints apply.

## Server review-correction assignment

- Existing `implementer` assignment explicitly specifies `gpt-5.6-terra` /
  `medium`; this surface exposes no runtime self-introspection, so its immutable
  configured profile is the available metadata and no mismatch is visible.
- The correction slice changes local EventBus entity observation, best-effort
  consumer isolation, activation serialization, failed-reconciliation cleanup,
  close ordering, the server reference, and the mandatory requirements ledger.
  It introduces no cluster-completeness guarantee.
- Repository assembly now uses the storage owner's typed internal
  `EntityCommitStorageFactories.create(factory, input)` accessor. The shared
  storage branch must land its corresponding internal export before combined
  typecheck; no storage path is edited here.
- `pnpm install --offline --ignore-scripts` and `pnpm proto:generate` pass.
  Focused Vitest and TypeScript/ESLint cannot resolve the generated package's
  `.js` build outputs in this isolated worktree; diagnostics start in unchanged
  Proto/Auth imports before server behavior is executed. Formatting passes.

## Integrated review corrections

- The server correction endpoint `f7db0dbc` and storage endpoints `7ec535fc`
  and `35651a42` were integrated through `9bbd7b88`. The merge preserved both
  work-log records and aligned the provider accessor on the dedicated
  `internal/entity-commit` subpath.
- The server owner resumed on the integrated tree under the explicitly retained
  implementer profile `gpt-5.6-terra` / `medium`; runtime self-introspection
  remained unavailable. It migrated subscription fixtures to real local
  `EntityStateChanged` EventBus envelopes and added the failed-reconciliation,
  activation, revision-fence, and throwing-consumer proofs.
- Combined deterministic evidence passes: 393 focused tests with 17 skipped,
  repository build/typecheck, cleanup, TSDoc, changed-path ESLint, formatting,
  and diff checks.
- A disposable MySQL 8.4 container passes 17/17 live tests. A local Firestore
  emulator in Datastore mode passes 8/8 tests for the isolated project
  `spine-ts-t0109-review`. Emulator transaction-lock warnings were expected
  contention exercised by the bounded retry tests; the test profile passed.
