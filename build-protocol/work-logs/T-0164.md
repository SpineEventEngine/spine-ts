# T-0164 Work Log

## 2026-08-11 - Startup

- Starts from `origin/main@c993fdf7` after T-0163 integration.
- Classification is high-risk because validation precedes Command routing,
  durable replay, and all Entity-family commits.
- The frozen Wave 9 contract controls the implementation; no new public API or
  Proto option is introduced.
- TDD begins with one server policy fixture covering descriptor declaration
  order, explicit-option precedence, supported values, and exclusions.
- Product Markdown remains deferred to Wave 10.

Status: mapping descriptor options and existing validation result seams before
the first RED.

## 2026-08-11 - Runtime Integration Checkpoint

- The first RED was the missing package-private policy module. The policy now
  compiles and caches the declaration-first field per schema, observes explicit
  `(required)` presence, and returns the existing sanitized validation shape.
- Supported empty string, bytes, enum, message, repeated, and map values fail;
  non-empty values pass. Numeric/boolean primitive IDs, Events, rejections,
  ordinary messages, and misleading filenames are excluded.
- Fresh CommandBus admission rejects before dispatcher side effects. Durable
  Aggregate replay rejects before handler invocation.
- Entity transition validation now performs ordinary message validation before
  cached implicit-ID and set-once rules. Explicit true remains authoritative;
  explicit false suppresses the implicit rule.
- Real Aggregate, Process Manager, and Projection handlers can run, but a blank
  state-ID mutation is rejected before any state commit.
- The focused four-file suite passes 294/294 tests, including the exact
  `commands.proto` basename and the boolean primitive exclusion.

Status: runtime behavior converged; broad mechanical/coverage gates, concern
review, corrections, final verification, and integration remain.

## 2026-08-11 - Wave Integration Fixture Convergence

- The first full-server run exposed already-integrated Wave 9 fixture debt,
  not an implicit-ID runtime failure: generated registry fixtures still wrote
  v1 metadata, the root export inventory omitted `StateUpdateRouting` and
  `Where`, and two Projection fixtures represented Entity state as an Event or
  subscribed to their own state.
- Registry fixtures now use v2. Projection tests use a real Event schema or a
  foreign Entity-state subscription, preserving the self-state recursion guard.
- The affected context, lifecycle, native subscription, SpineServices, and root
  export suites pass 249/249 tests.
- The last broad-suite failure was the equivalent cross-process fixture. Its
  child and parent now exchange the real `TaskCreated` Event schema rather than
  labelling a Projection state as an Event; all 7 cross-process tests pass.

Status: integration fixtures converged; rerunning the complete server suite.

## 2026-08-11 - Review Convergence

- Full server verification passes 74 files / 1,758 tests.
- The focused 294-test coverage run gives the new policy 97.61% statements,
  100% branches/functions, and 97.5% lines. Other directly changed validation
  modules also exceed 90% statements/branches/functions/lines.
- Generated build, tooling typecheck, cleanup enforcement, TSDoc, TypeDoc/API,
  ESLint, Prettier, and diff checks pass.
- Concern-specific performance/reliability, style/maintainability,
  TypeScript/API documentation, and documentation passes are clean. No public
  API, unbounded resource, competing parser, or unsanitized failure seam was
  introduced. Security is N/A for this validation-only slice and remains a
  final T-0167 gate.

Status: review-complete; final parameterized `verify:task`, integration, and
post-main verification remain.
