# T-0164 Work Log

## 2026-08-11 - Startup

- Starts from `origin/main@c993fdf7` after T-0163 integration.
- Classification is high-risk because validation precedes Command routing,
  durable replay, and all Entity-family commits.
- The frozen Wave 9 contract controls the implementation; no new public API or
  Proto option is introduced.
- TDD begins with one server policy fixture covering descriptor declaration
  order, explicit-option precedence, supported values, and exclusions.
- Product Markdown remains deferred to Wave 10.

Status: mapping descriptor options and existing validation result seams before
the first RED.

## 2026-08-11 - Runtime Integration Checkpoint

- The first RED was the missing package-private policy module. The policy now
  compiles and caches the declaration-first field per schema, observes explicit
  `(required)` presence, and returns the existing sanitized validation shape.
- Supported empty string, bytes, enum, message, repeated, and map values fail;
  non-empty values pass. Numeric/boolean primitive IDs, Events, rejections,
  ordinary messages, and misleading filenames are excluded.
- Fresh CommandBus admission rejects before dispatcher side effects. Durable
  Aggregate replay rejects before handler invocation.
- Entity transition validation now performs ordinary message validation before
  cached implicit-ID and set-once rules. Explicit true remains authoritative;
  explicit false suppresses the implicit rule.
- Real Aggregate, Process Manager, and Projection handlers can run, but a blank
  state-ID mutation is rejected before any state commit.
- The focused four-file suite passes 294/294 tests, including the exact
  `commands.proto` basename and the boolean primitive exclusion.

Status: runtime behavior converged; broad mechanical/coverage gates, concern
review, corrections, final verification, and integration remain.
