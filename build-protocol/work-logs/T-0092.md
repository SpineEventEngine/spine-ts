# T-0092 Work Log

Task: `build-protocol/tasks/T-0092-subscription-coordination/TASK.md`

## 2026-08-02 — Framing

- Created the isolated task from pushed `main@229b610e` after T-0091 merged,
  passed the generated build and 299 post-merge tests, and was pushed.
- Classified B2 as high-risk concurrency/persistence work and selected the
  existing requirements splitter before implementation because durable
  state-machine, lease/fence, ambiguous-outcome, and cleanup invariants require
  one precise dependency split.
- Froze the approved B2 behavior and exclusions without adding hosting,
  deployment, Wave 6 notification propagation, or another persistence model.
- Recorded explicit Sol/high splitter, Terra/medium implementer, and four
  specialist reviewer profiles before dispatch.
- Next: push framing, establish the focused T-0091 baseline, and dispatch the
  requirements splitter for a bounded implementation map.

## 2026-08-02 — Baseline

- Proto generation passed 40 source checksums, example quality checks, and 49
  frozen descriptor digests. The generated TypeScript build completed cleanly.
- The inherited auth bindings, durable registry, production server admission,
  Datastore, and MySQL focused baseline passed 5 files and 271 tests.
- No existing failure or provider capability blocker was found.
- Next: push this evidence and dispatch the recorded Sol/high requirements
  splitter to define exact durable transitions and deterministic RED packets.

## 2026-08-02 — Requirements split

- Existing role `requirements_splitter` refined only frozen Wave 5 B2. The
  dispatch explicitly selected expected `gpt-5.6-sol` with high reasoning.
  Runtime self-introspection was not exposed to the child, so acceptance uses
  the immutable configured role/profile and records that metadata limitation
  honestly; no omitted field, fallback, or visible mismatch occurred.
- Added
  `build-protocol/planning/T-0092_SUBSCRIPTION_COORDINATION_SPLIT.md` with the
  exact version-2 binding lifecycle, version-1 quota and cleanup control rows,
  CAS/fence/accounting invariants, transition table, deterministic race/fault
  RED packets, five dependency-ordered implementation checkpoints, ownership,
  review concerns, release gate, and exclusions.
- The map reuses one T-0091 namespaced `RecordStorage`, its `StorageFactory`,
  atomic `compareAndSet()`, and bounded keyset queries. A capacity reservation
  allocates its final binding slot and converts that same row to an inactive
  binding, avoiding a cross-row count gap. Recoverable quota operations and a
  separate cleanup control row remain metadata inside the same registry.
- Confirmed that awaitable reservation release and a guarded activation cleanup
  callback are the minimum contract changes required by the approved global
  capacity and stale-owner guarantees. No host, scheduler abstraction,
  deployment policy, generic cursor, second registry, or Wave 6 behavior was
  introduced.
- No governing conflict or blocker was found.
- Next: push the accepted split, dispatch the existing Terra/medium implementer
  as the one writer, and execute the checkpoints with immediate tested pushes.

## 2026-08-02 — Checkpoint 1: codec and deterministic validation

- Implementer assignment uses the existing `implementer` role with explicit
  `gpt-5.6-terra` / medium. Runtime self-introspection is not exposed in this
  surface; the immutable configured role/profile is the available metadata.
- RED: the inherited version-1-only codec tests reject the version-2 binding
  format before storage admission; reserved public-ID and invalid-record paths
  were exercised while replacing that development format.
- GREEN: the registry now uses a closed version-2 binding family and closed
  version-1 quota/cleanup families. Their type URLs, identifiers, revision,
  fence, canonical byte accounting, private-field combinations, and encoded
  size limit are validated before effect execution.
- Evidence: `tsc -p packages/server/tsconfig.json --noEmit` and focused durable
  bindings tests passed (27 tests). Formatting was applied to changed files.
- Next: commit and push this codec foundation, then extend deterministic
  cross-registry admission and repair tests. Work continues.

## 2026-08-02 — Checkpoint 2: global admission

- RED: two independently opened registries share a capacity-one namespace;
  a second reservation is rejected while the first durable slot is reserved.
  A conversion test proves creation replaces the preallocated slot rather than
  taking a second slot.
- GREEN: quota-backed reserve/release now increments only on durable slot
  admission and decrements only after the exact reserved/retired slot leaves.
  A failed quota completion invokes a bounded physical-slot accounting repair.
- Evidence: focused durable bindings tests passed (29 tests), auth typecheck
  passed, `git diff --check` and changed-file Prettier checks passed.
- Next: add two-handle lease/fence/stale-owner and cancellation regressions.
  Work continues.

## 2026-08-02 — Checkpoint 3: lease and fencing

- RED: one gateway holds an activation callback open while a second gateway
  attempts an unexpired claim and then an expired claim.
- GREEN: only the first durable lease owner is admitted before expiry; the
  second owner receives a new fence after expiry, and the resumed former owner
  receives `denied` rather than finalizing as current. Local activation state
  has one controller and one finite renewal timer per admitted binding.
- Evidence: focused durable bindings tests passed (30 tests).
- Next: complete cleanup/restart bounds and public contract reconciliation.
  Work continues.

## 2026-08-02 — Checkpoint 4: bounded cleanup

- GREEN: cleanup reads no more than `cleanupBatchSize` records, ignores the
  reserved quota/control IDs, releases expired reservations, and removes
  expired inactive bindings through the same recoverable quota-release path.
  Registry close aborts local controllers and clears timers without closing the
  application-owned storage factory.
- Existing restart and expiry coverage remains green alongside the new finite
  admission and fencing paths. No background scheduler or second registry was
  introduced.
- Evidence: auth/server typechecks passed; focused auth subscription and
  durable registry tests passed (81 tests); formatting and `git diff --check`
  passed.
- Next: commit and push cleanup/documentation reconciliation, then run the
  complete cheap pre-review gate. Work continues.

## 2026-08-02 — Checkpoint 5: contract reconciliation

- Updated the human guide and agent reference to describe namespace-global
  limits, preallocated IDs, asynchronous exactly-once release, finite leases,
  fencing, bounded cleanup, restart behavior, and the explicit update-delivery
  limitations. Removed the stale claim that two-gateway coordination was absent.
- Public contract remains minimal: the existing reservation release is now
  awaitable; no coordination row, cursor, scheduler, or host API is exported.
- The first cheap preflight exposed two test-double callers that still declared
  synchronous release. They were corrected to the public asynchronous contract;
  no production semantics changed.
- The complete rerun passed `verify:task -- --no-tests`: node/proto checks,
  generated build and tooling typechecks, ESLint, cleanup/TSDoc, formatting,
  documentation policy, Buf lint, generated cleanliness, and release-readiness.
- Next: push the pre-review reconciliation and return the task tree for one
  complete specialist review wave. Work continues.

## 2026-08-02 — Mechanical pre-review completeness scan

- The deterministic cheap gate is clean, but a direct comparison against the
  accepted split found the implementation checkpoint incomplete. The focused
  test file covers headline capacity and stale-owner paths only.
- Missing required RED packets: applied-then-thrown CAS reconciliation at
  quota/slot/claim/renew/cancel/retire/delete stages; paged quota repair and
  restart; per-update durable guards; cancellation lease takeover and
  callback concurrency; cleanup continuation, backoff, competing cleaners,
  crash points, and restart resumption.
- The current `repair()` materializes one bounded page and writes a count
  directly; it does not implement the accepted durable paged repair operation.
  Cleanup-control continuation/backoff fields exist in the codec but are not
  exercised by transition behavior or deterministic tests.
- This is a mechanical specification gap, not a specialist review result. No
  review wave is dispatched against known-incomplete behavior.
- Next: push this scan and return the complete missing acceptance packet to the
  same implementer before specialist review.

## 2026-08-02 — Guarded forwarding packet

- RED: after gateway B claims the expired lease, gateway A's still-open backend
  callback asks its durable guard whether it remains current.
- GREEN: `OnBackendSubscription` now supplies an optional async guard. The
  durable registry passes one only after claiming its owner/fence; it rereads
  durable state before forwarding. The stale A regression proves that guard is
  false after B takeover.
- Evidence: generated build and focused durable binding tests pass (30 tests).
- Remaining: the accepted fault/reconciliation, paged repair, cancellation
  takeover, and cleanup-control matrix is still incomplete; do not dispatch
  review yet.

## 2026-08-02 — Repair foundation follow-up

- Added an internal durable quota repair operation with an ID continuation and
  cumulative count. Reserve and release help an interrupted repair before
  changing namespace capacity. Each repair read is bounded by
  `cleanupBatchSize` and excludes quota/cleanup metadata.
- This is only the production foundation. The required fault-injecting storage
  fixture and deterministic applied-then-thrown/restart proof matrix are still
  absent, so this packet is not accepted as review-ready.
- Evidence: generated build and focused durable bindings tests pass (30 tests);
  formatting and `git diff --check` pass.

## 2026-08-02 — Reconciliation ownership transfer

- The prior implementation context completed without a writer overlap. The
  existing `implementer` ownership transferred to the configured
  `gpt-5.6-terra` / medium implementation profile. Runtime self-inspection is
  unavailable on this surface; the immutable configured role/profile is the
  available metadata.
- RED: an apply-then-throw shared `StorageFactory` fixture exposed that a
  completed quota stage was treated as a capacity failure and that concurrent
  callers could both report one reservation conversion as successful.
- GREEN: quota mutations now stage a durable operation before the slot change,
  reread exact durable operation facts after an ambiguous CAS, and complete the
  same operation idempotently. Reservation conversion is locally consumed once.
  The deterministic fixture covers quota-stage, slot insert, conversion, slot
  deletion, and quota-completion applied-then-thrown outcomes.
- Evidence: focused durable bindings test passed 35 tests; server typecheck and
  `git diff --check` passed. Changed-source coverage is currently 83.33% lines
  and 72.86% branches, below the required 90%; broader paged-repair and
  restart/fault coverage remains required before review.
- Next: extend the bounded repair/restart fixture and coverage matrix before
  accepting the reconciliation checkpoint or dispatching specialist review.

## 2026-08-02 — Paged repair continuation

- RED: a registry reopened with a persisted quota repair cursor either decoded
  malformed binding slots or could not resume from a saved page boundary.
- GREEN: repair first durably stages its operation, retains operation ID,
  cursor, count, and revision through finite ID-ordered pages, and treats every
  non-metadata row as occupied without decoding it. New admission validates a
  clean namespace but allows an in-progress repair to count corrupt slots
  conservatively.
- Deterministic tests exercise a three-page repair, a malformed row, and fresh
  registry resumes from the initial cursor and two later page cursors. The
  existing apply-then-throw fixture remains green for quota stage, slot insert,
  conversion, deletion, and completion.
- Evidence: focused test `packages/server/test/server/durable-subscription-bindings.test.ts`
  passed 39 tests; server typecheck, affected ESLint, Prettier, and
  `git diff --check` passed. The focused changed-source coverage command still
  reports 87.27% lines and 77.65% branches, below 90%. More fault/reconciliation
  tests are required before review. Cancellation and cleanup are intentionally
  still outside this packet.

## 2026-08-02 — Repair ambiguous finalization

- RED: an applied-then-thrown final quota correction left the helper retrying a
  completed repair until its attempt bound expired.
- GREEN: repair rereads the quota after any failed page/final CAS and accepts
  the cleared operation as authoritative. Page and final applied-then-thrown
  faults now reopen a separate registry and converge on the one durable count.
- Evidence: focused test passed 41 tests. The exact focused coverage command
  remains below the 90% branch gate (77.03% branches); this packet cannot be
  accepted or reviewed yet.

## 2026-08-02 — Lease, fence, and cancellation reconciliation

- Ownership remained with the recorded existing `implementer` profile
  (`gpt-5.6-terra` / medium); runtime self-inspection is unavailable, so the
  immutable configured role/profile remains the recorded metadata limitation.
- RED/GREEN: deterministic two-handle tests now prove an applied-then-thrown
  activation claim is reread before one backend callback, an unexpired lease
  cannot be stolen, an expired cancellation fences the former active owner,
  and concurrent same-handle cancellation calls coalesce to one callback.
- Renewal now reschedules after a successful durable renewal and aborts its
  local controller after a durable ownership loss. Cancellation retains its
  durable fence across a callback failure and retries only through that fenced
  state; retirement rereads an ambiguous CAS before quota release.
- Evidence: focused durable registry test passed 45 tests, the server typecheck
  and `git diff --check` passed, and changed files were formatted. Remaining
  accepted scope is cleanup-control/backoff/restart proof plus full fault and
  coverage matrix; no specialist review is due yet.

## 2026-08-02 — Durable cleanup control foundation

- RED: cleanup had no durable owner, continuation, backoff, or restart fact;
  expiry directly deleted inactive rows and could bypass the cancellation fence.
- GREEN: cleanup now claims a durable owner/fence/lease row, reads one ordered
  finite page after its durable continuation, advances the continuation after
  each safe row, and resets it after the terminal empty page. Expired private
  sessions transition through a new fenced `cancelling` lease before the
  configured disposal callback and retirement/recoverable quota release.
- A foreign cleanup owner retains its lease across close. A reopened registry
  can take over only after controlled expiry. Callback failure clears ownership
  and persists an overflow-safe capped exponential retry deadline. Two same
  namespace handles race through the same CAS control fact and invoke one
  cleanup callback.
- Evidence: focused durable bindings test passed 49 tests; server typecheck,
  Prettier, and `git diff --check` passed. This is a foundation checkpoint;
  the full crash/fault, cancellation-takeover, repair-barrier, and 90% branch
  coverage matrix remains mandatory before review.

## 2026-08-02 — Cleanup failure and cancellation fault reconciliation

- Corrected cleanup failure persistence to reread the latest owned control row
  before installing backoff; a row-level continuation advance can no longer
  make the failure update silently lose its CAS.
- RED/GREEN tests prove applied-then-thrown cancellation and retirement CASes
  reconcile before a duplicate backend callback. They also prove a second
  registry takes over only an expired cancelling lease under a new fence.
- Evidence: focused durable bindings test passed 52 tests and server typecheck
  passed. Further cleanup crash points, repair concurrency, and coverage work
  remain before review.

## 2026-08-02 — Expired active cleanup

- RED/GREEN: cleanup now treats an expired active session as an expiry
  transition, fences it to `cancelling`, disposes under that fence, and prevents
  the former active callback from forwarding or finalizing when it resumes.
- Evidence: focused durable bindings test passed 53 tests; server typecheck,
  affected ESLint, Prettier, and `git diff --check` passed.

## 2026-08-02 — Cleanup claim ambiguity

- RED/GREEN: an apply-then-throw cleanup ownership CAS is reread as the
  authoritative durable claim before one disposal callback starts.
- Evidence: focused durable bindings test passed 54 tests and server typecheck
  passed. Work remains before review.

## 2026-08-02 — Guarded activation finalization

- RED/GREEN: a normally settled backend activation now rereads its active
  owner/fence/lease, transitions under a new `activation-end` cancellation
  fence, disposes once, retires, and performs recoverable capacity release.
  A stale settled callback performs no finalization mutation.
- Evidence: focused durable bindings test passed 55 tests; server typecheck,
  affected ESLint, Prettier, and `git diff --check` passed. Further complete
  fault/coverage proof remains before review.

## 2026-08-02 — Renewal ambiguity

- RED/GREEN: a renewal CAS that applies and then reports failure is reread as
  the current active owner/fence before the callback continues or another
  renewal timer is scheduled.
- Evidence: focused durable bindings test passed 56 tests. The required branch
  coverage and remaining crash/race packets are still incomplete.

## 2026-08-02 — Reservation expiry and time bounds

- RED/GREEN: a durable reservation now has a finite lease and an expired slot
  is returned through the cleanup/recoverable quota-release path. Lease-end
  arithmetic saturates at the safe-integer bound instead of persisting an
  overflowed timestamp.
- Evidence: focused durable bindings test passed 57 tests; server typecheck,
  affected ESLint, Prettier, and `git diff --check` passed. Work continues.

## 2026-08-02 — Local expiration reconciliation

- An expired reservation now removes its local reservation handle before the
  durable quota release. A delayed `release()` on that handle is a no-op and
  cannot decrement capacity occupied by a replacement reservation.
- Evidence: focused durable bindings test passed 57 tests; server typecheck,
  affected ESLint, Prettier, and `git diff --check` passed.

## 2026-08-02 — Concurrent repair helping

- RED/GREEN: two independently opened registry handles helping the same
  persisted paged quota repair converge before one final capacity slot is
  admitted; the other request fails at the durable namespace limit.
- Evidence: focused durable bindings test passed 58 tests. The broader required
  controlled-barrier/crash matrix and coverage gate remain open.

## 2026-08-02 — Deterministic rejected-CAS and codec hardening

- RED: negative cleanup-control values were accepted by the durable codec, and
  no regression proved the fail-closed behavior of rejected admission,
  activation, cancellation, retirement, cleanup-continuation, and release
  compare-and-set operations.
- GREEN: cleanup fence, failure count, and retry deadline now reject negative
  durable values. The shared deterministic fault storage proves that rejected
  CAS operations remain finite and that an activation race does not invoke its
  backend callback. Codec tests cover every record family, valid lifecycle, and
  incompatible private field combination without exposing private payloads.
- Evidence: focused durable bindings test passed 84 tests. Exact changed-source
  coverage is 91.81% lines and 83.25% branches (343/412), so the mandatory 90%
  branch gate is still open; no specialist review has been dispatched.

## 2026-08-02 — Coverage fault matrix expansion

- RED/GREEN: deterministic registry fixtures now exercise rejected conversion,
  quota, slot, release, cleanup, expiry, and activation-finalization CAS paths;
  foreign cleanup ownership; malformed/live/retired cleanup rows; pending quota
  operations; safe integer limits; and each durable record family.
- Evidence: the focused suite passed 96 tests. The measured source coverage
  improved to 94.04% lines and 86.89% branches (358/412), but still does not
  satisfy the required 90% branch gate. The task remains outside review.

## 2026-08-02 — Vanishing-row terminal races

- RED/GREEN: deterministic storage now removes an owned row at a selected read
  boundary. Cancellation converges to `closed` after its callback sees a
  vanished row, while an activation denied by a vanished current row cannot
  finalize or resurrect the binding.
- Evidence: focused durable bindings suite passed 98 tests. The branch gate
  remains open and no specialist review has been dispatched.

## 2026-08-02 — Final durable-registry coverage gate

- Ownership remained with the existing `implementer` role, explicitly
  dispatched as `gpt-5.6-terra` with medium reasoning. This surface does not
  expose runtime self-introspection, so the immutable configured role/profile
  is the recorded actual-metadata limitation; no fallback or overlap occurred.
- RED/GREEN: deterministic vanishing-row tests now prove that conversion never
  accepts a reservation whose durable slot disappeared, and that a post-admission
  disappearance cannot return a usable reservation. The focused matrix also
  proves a stale local activation cannot clear a newer local activation, a
  foreign registry cannot take an unexpired failed cancellation lease, and
  incomplete persisted repair/expiry facts are handled conservatively.
- Simplified internal optional-field serialization only where JSON already
  omits `undefined` values. Validated active/cancelling records always contain
  their backend and lease, so current-owner checks retain fail-closed behavior
  without defensive fallback branches that cannot affect a valid durable row.
- Evidence: `packages/server/test/server/durable-subscription-bindings.test.ts`
  passed 104 tests. Exact changed-source coverage is **90.00% branches
  (360/400)**, 94.78% lines (382/403), 92.06% statements (429/466), and 94.54%
  functions (52/55). Server and auth typechecks, affected ESLint, Prettier, and
  `git diff --check` passed.
- Next: commit and push this satisfied final gate, then return the task to the
  orchestrator for the pre-recorded specialist review wave. Work continues.

## 2026-08-02 — Preflight TypeScript correction

- The parent preflight found `TS2554` at durable bindings test line 1157:
  Vitest's `toThrow()` accepts one matcher argument, while the test supplied a
  second assertion-description argument. No production behavior or durable
  state-machine evidence was affected.
- Corrected the assertion to its supported matcher form and removed the now
  unused loop index. The existing `implementer` assignment remains explicitly
  configured as `gpt-5.6-terra` / medium; runtime self-introspection remains
  unavailable on this surface.
- Next: run the narrow server typecheck and focused durable registry suite,
  then commit and immediately push the correction. Work continues.

- Evidence: `tsc -p packages/server/tsconfig.json --noEmit` passed; the focused
  durable registry suite passed 104 tests; Prettier and `git diff --check`
  passed. The correction is ready for immediate push.

## 2026-08-02 — Auth subscription preflight correction

- The parent preflight found four test-only ESLint failures in
  `packages/auth/test/subscriptions/index.test.ts`: two unnecessary `async`
  wrappers, a nested unnecessary `async` release function, and an unawaited
  asynchronous capacity release. No production contract or durable behavior
  is affected.
- The existing `implementer` assignment remains explicitly configured as
  `gpt-5.6-terra` / medium. Runtime self-introspection is not exposed on this
  surface; immutable configured-role evidence remains the recorded limitation.
- Corrected the test doubles to return explicit promises and awaited the final
  release. Next: affected lint, focused auth test/typecheck, formatting, and
  diff verification before immediate push. Work continues.

- Evidence: affected ESLint and `tsc -p packages/auth/tsconfig.json --noEmit`
  passed; the focused auth subscription suite passed 51 tests; Prettier and
  `git diff --check` passed. The correction is ready for immediate push.
