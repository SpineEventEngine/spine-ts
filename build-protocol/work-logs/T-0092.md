# T-0092 Work Log

Task: `build-protocol/tasks/T-0092-subscription-coordination/TASK.md`

## 2026-08-02 — Framing

- Created the isolated task from pushed `main@229b610e` after T-0091 merged,
  passed the generated build and 299 post-merge tests, and was pushed.
- Classified B2 as high-risk concurrency/persistence work and selected the
  existing requirements splitter before implementation because durable
  state-machine, lease/fence, ambiguous-outcome, and cleanup invariants require
  one precise dependency split.
- Froze the approved B2 behavior and exclusions without adding hosting,
  deployment, Wave 6 notification propagation, or another persistence model.
- Recorded explicit Sol/high splitter, Terra/medium implementer, and four
  specialist reviewer profiles before dispatch.
- Next: push framing, establish the focused T-0091 baseline, and dispatch the
  requirements splitter for a bounded implementation map.

## 2026-08-02 — Baseline

- Proto generation passed 40 source checksums, example quality checks, and 49
  frozen descriptor digests. The generated TypeScript build completed cleanly.
- The inherited auth bindings, durable registry, production server admission,
  Datastore, and MySQL focused baseline passed 5 files and 271 tests.
- No existing failure or provider capability blocker was found.
- Next: push this evidence and dispatch the recorded Sol/high requirements
  splitter to define exact durable transitions and deterministic RED packets.

## 2026-08-02 — Requirements split

- Existing role `requirements_splitter` refined only frozen Wave 5 B2. The
  dispatch explicitly selected expected `gpt-5.6-sol` with high reasoning.
  Runtime self-introspection was not exposed to the child, so acceptance uses
  the immutable configured role/profile and records that metadata limitation
  honestly; no omitted field, fallback, or visible mismatch occurred.
- Added
  `build-protocol/planning/T-0092_SUBSCRIPTION_COORDINATION_SPLIT.md` with the
  exact version-2 binding lifecycle, version-1 quota and cleanup control rows,
  CAS/fence/accounting invariants, transition table, deterministic race/fault
  RED packets, five dependency-ordered implementation checkpoints, ownership,
  review concerns, release gate, and exclusions.
- The map reuses one T-0091 namespaced `RecordStorage`, its `StorageFactory`,
  atomic `compareAndSet()`, and bounded keyset queries. A capacity reservation
  allocates its final binding slot and converts that same row to an inactive
  binding, avoiding a cross-row count gap. Recoverable quota operations and a
  separate cleanup control row remain metadata inside the same registry.
- Confirmed that awaitable reservation release and a guarded activation cleanup
  callback are the minimum contract changes required by the approved global
  capacity and stale-owner guarantees. No host, scheduler abstraction,
  deployment policy, generic cursor, second registry, or Wave 6 behavior was
  introduced.
- No governing conflict or blocker was found.
- Next: push the accepted split, dispatch the existing Terra/medium implementer
  as the one writer, and execute the checkpoints with immediate tested pushes.

## 2026-08-02 — Checkpoint 1: codec and deterministic validation

- Implementer assignment uses the existing `implementer` role with explicit
  `gpt-5.6-terra` / medium. Runtime self-introspection is not exposed in this
  surface; the immutable configured role/profile is the available metadata.
- RED: the inherited version-1-only codec tests reject the version-2 binding
  format before storage admission; reserved public-ID and invalid-record paths
  were exercised while replacing that development format.
- GREEN: the registry now uses a closed version-2 binding family and closed
  version-1 quota/cleanup families. Their type URLs, identifiers, revision,
  fence, canonical byte accounting, private-field combinations, and encoded
  size limit are validated before effect execution.
- Evidence: `tsc -p packages/server/tsconfig.json --noEmit` and focused durable
  bindings tests passed (27 tests). Formatting was applied to changed files.
- Next: commit and push this codec foundation, then extend deterministic
  cross-registry admission and repair tests. Work continues.

## 2026-08-02 — Checkpoint 2: global admission

- RED: two independently opened registries share a capacity-one namespace;
  a second reservation is rejected while the first durable slot is reserved.
  A conversion test proves creation replaces the preallocated slot rather than
  taking a second slot.
- GREEN: quota-backed reserve/release now increments only on durable slot
  admission and decrements only after the exact reserved/retired slot leaves.
  A failed quota completion invokes a bounded physical-slot accounting repair.
- Evidence: focused durable bindings tests passed (29 tests), auth typecheck
  passed, `git diff --check` and changed-file Prettier checks passed.
- Next: add two-handle lease/fence/stale-owner and cancellation regressions.
  Work continues.

## 2026-08-02 — Checkpoint 3: lease and fencing

- RED: one gateway holds an activation callback open while a second gateway
  attempts an unexpired claim and then an expired claim.
- GREEN: only the first durable lease owner is admitted before expiry; the
  second owner receives a new fence after expiry, and the resumed former owner
  receives `denied` rather than finalizing as current. Local activation state
  has one controller and one finite renewal timer per admitted binding.
- Evidence: focused durable bindings tests passed (30 tests).
- Next: complete cleanup/restart bounds and public contract reconciliation.
  Work continues.

## 2026-08-02 — Checkpoint 4: bounded cleanup

- GREEN: cleanup reads no more than `cleanupBatchSize` records, ignores the
  reserved quota/control IDs, releases expired reservations, and removes
  expired inactive bindings through the same recoverable quota-release path.
  Registry close aborts local controllers and clears timers without closing the
  application-owned storage factory.
- Existing restart and expiry coverage remains green alongside the new finite
  admission and fencing paths. No background scheduler or second registry was
  introduced.
- Evidence: auth/server typechecks passed; focused auth subscription and
  durable registry tests passed (81 tests); formatting and `git diff --check`
  passed.
- Next: commit and push cleanup/documentation reconciliation, then run the
  complete cheap pre-review gate. Work continues.

## 2026-08-02 — Checkpoint 5: contract reconciliation

- Updated the human guide and agent reference to describe namespace-global
  limits, preallocated IDs, asynchronous exactly-once release, finite leases,
  fencing, bounded cleanup, restart behavior, and the explicit update-delivery
  limitations. Removed the stale claim that two-gateway coordination was absent.
- Public contract remains minimal: the existing reservation release is now
  awaitable; no coordination row, cursor, scheduler, or host API is exported.
- The first cheap preflight exposed two test-double callers that still declared
  synchronous release. They were corrected to the public asynchronous contract;
  no production semantics changed.
- The complete rerun passed `verify:task -- --no-tests`: node/proto checks,
  generated build and tooling typechecks, ESLint, cleanup/TSDoc, formatting,
  documentation policy, Buf lint, generated cleanliness, and release-readiness.
- Next: push the pre-review reconciliation and return the task tree for one
  complete specialist review wave. Work continues.

## 2026-08-02 — Mechanical pre-review completeness scan

- The deterministic cheap gate is clean, but a direct comparison against the
  accepted split found the implementation checkpoint incomplete. The focused
  test file covers headline capacity and stale-owner paths only.
- Missing required RED packets: applied-then-thrown CAS reconciliation at
  quota/slot/claim/renew/cancel/retire/delete stages; paged quota repair and
  restart; per-update durable guards; cancellation lease takeover and
  callback concurrency; cleanup continuation, backoff, competing cleaners,
  crash points, and restart resumption.
- The current `repair()` materializes one bounded page and writes a count
  directly; it does not implement the accepted durable paged repair operation.
  Cleanup-control continuation/backoff fields exist in the codec but are not
  exercised by transition behavior or deterministic tests.
- This is a mechanical specification gap, not a specialist review result. No
  review wave is dispatched against known-incomplete behavior.
- Next: push this scan and return the complete missing acceptance packet to the
  same implementer before specialist review.

## 2026-08-02 — Guarded forwarding packet

- RED: after gateway B claims the expired lease, gateway A's still-open backend
  callback asks its durable guard whether it remains current.
- GREEN: `OnBackendSubscription` now supplies an optional async guard. The
  durable registry passes one only after claiming its owner/fence; it rereads
  durable state before forwarding. The stale A regression proves that guard is
  false after B takeover.
- Evidence: generated build and focused durable binding tests pass (30 tests).
- Remaining: the accepted fault/reconciliation, paged repair, cancellation
  takeover, and cleanup-control matrix is still incomplete; do not dispatch
  review yet.
