# T-0066 Work Log

Status: closed; merged and post-merge verified on `main`.

## 2026-07-22 — Milestone Start

- Created isolated branch `task/T-0066-multi-machine-delivery` from pushed
  `main` at `93ebf880`.
- Classified the packet high-risk because it exercises cross-process ownership,
  stale takeover, public network streaming, and cleanup under failure.
- Requirements-splitter dispatch is the existing configured role with explicit
  expected profile `gpt-5.6-sol` / `high`. Runtime metadata acceptance evidence
  will be recorded when the result returns.
- No production implementation, review, verification, commit, or push has
  started for this packet.
- Requirements splitter found that the existing public server, client, remote
  adapters, builder, and supervisor compose without a production/API defect.
  Runtime self-introspection was unavailable; the immutable configured role and
  explicit dispatch match `gpt-5.6-sol` / `high`, with no visible fallback.
- Accepted scope is fixture-only under `packages/delivery-client/test/e2e` and
  `packages/delivery-client/test-fixtures`, plus task records. Transient shutdown
  `NOT_SERVING` is covered by T-0065's internal order test; this suite asserts
  live health, unknown-service `NOT_SERVING`, and terminal unavailability.

## 2026-07-23 — Implementation Start

- Implementer: existing `implementer` role, explicitly dispatched as
  `gpt-5.6-terra` with `medium` reasoning for the bounded fixture-only scope.
  Runtime self-introspection is not exposed in this surface; the immutable
  configured role/profile and explicit dispatch are the available evidence, and
  no fallback or mismatch is visible.
- Skill applicability was checked against the session inventory, the repository
  `build-protocol/skills/EXPECTED_SKILLS.md`, `find ~/.agents/skills -maxdepth 2
-type f -name SKILL.md -print`, and `~/.agents/.skill-lock.json`. Selected:
  `test-driven-development` (runtime integration behavior). Its full local
  instructions were read before the first test. The expected worktree,
  subagent-development, review-request, verification, planning, ADR, advanced
  types, and Node patterns skills are installed but not selected: this worktree
  already exists, no subagent/review dispatch or architectural/API decision is
  owned here, no advanced public type is changed, and project protocol supplies
  the Node process-lifecycle requirements.
- Accepted behavior slices: parent-owned standalone loopback server/Admin-health
  observation; two public-package-root child applications with bounded IPC;
  exclusive pickup/release; ordinary alpha/beta exact-once delivery; stale-owner
  takeover and replacement-supervisor final delivery; deterministic cleanup and
  port reuse. The deliberate 500 ms quiet window is the sole allowed timed wait.
- No server runtime/API or generated descriptor change is planned. If the RED
  suite exposes one, implementation stops for separately owned correction.

## 2026-07-23 — Initial TDD Evidence

- RED: the new focused suite failed first because the multi-machine fixture was
  absent (`Cannot find module .../test-fixtures/multi-machine-app.mjs`).
- GREEN: added a local child fixture using package-root `@spine-ts/server` and
  `@spine-ts/delivery-client` APIs, generated command/Admin/health descriptors,
  loopback standalone server, bounded IPC, exclusive pickup/release, two-child
  startup, and alpha/beta exact-once dispatch assertions.
- `pnpm install --frozen-lockfile` needed approved network access because the
  sandbox could not resolve the registry. `pnpm proto:generate` restored ignored
  generated sources; generated build typecheck then passed. The sandbox also
  denies loopback binds (`listen EPERM`), so the focused real-process suite was
  run with approved loopback permission and passed. No runtime/public defect was
  observed in this slice.

## 2026-07-23 — Focused Verification Checkpoint

- Generated build typecheck passed after `pnpm proto:generate` restored ignored
  generated sources in this new worktree.
- The native loopback focused suite passed twice independently. It proves
  exclusive pickup/release, alpha/beta delivery, a 500 ms quiet window before a
  killed owner, stale takeover by the other child within five seconds, and a
  replacement supervisor's final dispatch.
- Touched ESLint, Prettier, and `git diff --check` passed. The real-loopback
  command requires approval because the sandbox itself returns `EPERM` on a
  local `listen()`; that is environmental, not a framework failure.

## 2026-07-23 — Admin Defect And Back-Merge

- Exact raw Admin observation found a deterministic surplus empty
  `PICKED/0 -> NOT_PICKED/0` cycle after stale takeover. The accepted 20-update
  assertion remained RED, and this fixture packet stopped before production
  expansion.
- A separately owned and reviewed correction prevents controlled delivery from
  starting when the public source reports zero messages. Implementation commit:
  `d6a5921b`; integrated correction closure: `6d35cc98`.
- Corrected `main` was back-merged into
  `task/T-0066-multi-machine-delivery` without conflicts using the preserved
  topology stash. The integrity reviewer reported all eight checks clean.
  Reviewer runtime self-introspection was unavailable; the orchestrator's
  explicit configured integrity-review profile and metadata limitation are the
  durable acceptance evidence.
- The unrelated unstaged
  `packages/client/codegen/generate-projection-columns.mjs` change remained
  untouched throughout the back-merge and resumed implementation.

## 2026-07-23 — Implementation Endpoint

- Added event-driven terminal Admin barriers around explicit release, each
  ordinary command, takeover, and final delivery. The public generated stream
  now proves exactly 20 post-ACK updates in the accepted
  `2 + 4 + 4 + 6 + 4` model and a final `NOT_PICKED/0` snapshot.
- Live generated health checks prove overall and known-service `SERVING`,
  unknown-service `NOT_SERVING`, and terminal unavailability after standalone
  shutdown.
- Added a deliberate post-readiness failure path. Its ordered `finally`
  cleanup terminates both applications, aborts and settles the Admin stream,
  terminates the server, rebinds the same port, preserves the primary error,
  and aggregates cleanup failures.
- Child IPC is runtime-validated to bounded commands and identifiers. Child
  failures return a stable sanitized diagnostic without payload or stack data.
- No production, public API, generated descriptor, dependency, lockfile,
  example, or public-guide file changed in this packet. The separate supervisor
  correction remains the only runtime change exposed by the suite.

## 2026-07-23 — Consolidated Review Corrections

- Accepted and resolved every finding from the complete
  performance/reliability, style/maintainability, and documentation wave in one
  batch.
- Added a generated health Check for `spine.delivery.InboxService`, distinct
  from overall and unknown-service checks, plus an empty Admin snapshot before
  child activity.
- Stale timing now begins at owner dispatch admission. The suite proves no
  survivor dispatch during the deliberate 500 ms quiet window, no takeover
  earlier than 900 ms for the configured one-second expiry, and takeover before
  five seconds.
- Child readiness and request IPC now use one-second event-driven gates. Every
  settlement removes message/exit/disconnect/error listeners and timers;
  timeout, exit, disconnect, process error, and send-callback failure all reject
  promptly. Runtime fixture validation requires a bounded signal ID only for
  `write` and rejects it for all other commands.
- The principal topology is cleanup-owned through `finally` in children,
  Admin-stream/session, server order. Stream rejection is handled immediately.
  Primary assertion errors remain first and cleanup failures aggregate.
- Admin collection is capped at one ACK plus 20 updates and fails on overflow.
  The initial public snapshot is asserted empty.
- The deliberate failure case now injects one deterministic cleanup error,
  asserts `[primary, cleanup]` ordering, proves later actions still run, and
  retains process/stream/port-reuse evidence.
- The e2e README now gives the exact generated-build prerequisite and documents
  the 500 ms quiet window, one-second expiry, 100 ms tolerance, and sub-five
  second takeover bound.
- Final native flake evidence passed twice independently: 1 file / 3 tests in
  each run (5.18 seconds and 5.09 seconds total duration respectively).

## 2026-07-23 — Final Re-Review Corrections

- Replaced `Promise.race()` exit settlement with one owned promise whose
  timeout, exit, and error branches all cancel the timer and remove their
  listeners. Tests prove both timeout and eventual-exit paths retain no
  coordination state.
- Parent command calls now pass a discriminated `FixtureCommand` object
  directly. Child control validation requires the exact key set
  `id,command,signalId` for `write` and `id,command` for every other command;
  extra-key and invalid signal-shape frames are rejected.
- Added deterministic child-exit, child-error, and send-callback-failure
  settlement coverage alongside timeout and disconnect. Every branch rejects
  promptly and leaves zero message/exit/disconnect/error listeners and zero
  tracked coordination timers.
- The clean-checkout README prerequisite is now the composite
  `typecheck:build`, which generates Proto sources before checking generated
  build inputs.
- Final exact-source focused topology verification passed twice independently:
  1 file / 3 tests in 5.07 seconds and 5.00 seconds. The affected focused
  re-review set remains performance/reliability, style/maintainability, and
  documentation.

## 2026-07-23 — Final Reliability Registration-Race Correction

- Closed the remaining exit-registration window by rechecking terminal child
  state immediately after `settledExit` installs its exit/error listeners. A
  child that exits between the initial check and registration now settles
  successfully through the same timer/listener cleanup path instead of timing
  out falsely.
- Added a deterministic induced-race test whose exit code changes between the
  two state checks. It proves successful settlement with zero retained
  exit/error listeners and zero tracked coordination timers.
- Corrected native verification passed twice independently: 1 file / 3 tests
  in 5.12 seconds and 5.07 seconds.
- Only performance/reliability is affected by this final correction.
  Style/maintainability and documentation remain accepted and do not reopen.

## 2026-07-23 — Acceptance Verification

- All required specialist concerns are clean or have a recorded N/A
  disposition. No accepted finding remains open.
- `pnpm --config.verify-deps-before-run=false verify` passed: 127 test files
  passed and 3 skipped; 2,317 tests passed and 21 skipped.
- Coverage passed at 94.16% statements, 90.09% branches, 95.03% functions, and
  94.67% lines.
- API documentation, Proto integrity, generated-output cleanliness, and release
  readiness all passed.

## 2026-07-23 — Integration Closure

- Committed the accepted topology as `2ac308c4` and pushed
  `task/T-0066-multi-machine-delivery` to `origin` immediately.
- Fast-forwarded `main` to `2ac308c4` while preserving every unrelated local
  change.
- Post-merge `typecheck:build` passed, and the real loopback multi-process
  topology suite passed all 3 tests in 5.02 seconds.
