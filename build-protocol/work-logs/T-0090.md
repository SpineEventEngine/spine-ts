# T-0090 Work Log

Task: `build-protocol/tasks/T-0090-run-lifecycle/TASK.md`

## 2026-08-02 — Framing

- Created the isolated task branch from pushed Wave 5 split `29dd6a3c`.
- Classified the slice as high-risk lifecycle/concurrency work.
- Existing generated build passed and the exact two-file server/environment
  baseline passed 126 tests before task implementation.
- Docker client/server `20.10.24` and Compose `v2.17.2` are ready for later
  Wave 5 slices.
- Recorded the Terra/medium implementation ownership and the relevant
  specialist reviewer profiles before dispatch.
- Next: implementation owner writes the first failing mixed-ownership tests,
  confirms RED, and implements the minimum lifecycle change.

## 2026-08-02 — RED

- Implementer dispatch: existing `implementer` role, configured
  `gpt-5.6-terra` with `medium` reasoning; both fields were explicit in the
  parent assignment. This surface does not expose self-inspectable runtime
  metadata, so the immutable configured role/profile is the available
  evidence.
- Added four public-behavior lifecycle tests in
  `packages/server/test/server/server.test.ts`: mixed caller/run admission,
  sibling run-managed retirement, and retryable final environment close.
- Initial direct Vitest run could not resolve built workspace exports. A first
  generated build also correctly reported absent generated Proto output in the
  fresh worktree. `pnpm proto:generate && pnpm typecheck:build:generated`
  completed successfully before the behavioral run.
- RED command: `pnpm exec vitest run packages/server/test/server/server.test.ts`.
  Result: 4 intended failures, 50 existing tests passing. The first two proved
  `run()` still attached with caller ownership (the forbidden calls resolved);
  the sibling test proved final run retirement left environment facilities
  open; and the retry test proved final run retirement did not attempt
  environment closure. No production source changed before this evidence.
- Next: make `run()` admit server ownership before listener open, allow sibling
  server-owned claims while rejecting mixed ownership, and have the process
  coordinator close the final run-managed environment retry-safely.

## 2026-08-02 — GREEN and focused validation

- Implemented the minimum existing-seam change: `Server.run()` now attaches as
  server-owned before listener open; `start()` remains caller-owned; matching
  ownership claims share a generation while mixed ownership rejects; and the
  existing process coordinator retires the final run record through the
  environment's retryable `close()` path after server cleanup completes.
- GREEN command: `pnpm exec vitest run packages/server/test/server/server.test.ts`.
  Result: 54/54 passing.
- Adjacent lifecycle command:
  `pnpm exec vitest run packages/server/test/server/environment-attachment.test.ts packages/server/test/server/environment-close.test.ts packages/server/test/server/server-lifecycle-integration.test.ts packages/server/test/server/server-context-transport-lifecycle.test.ts`.
  Result: 140/141 passing; the sole failure was the existing
  instrumentation-sensitive `bounds repeated reported failures...` test timeout.
  Its isolated rerun passed (1/1, 75 skipped).
- Changed-source coverage command used the same focused suite with
  `--coverage --testTimeout=15000` to accommodate that test under V8
  instrumentation. Coverage report: `process-server-coordinator.ts` 37/37
  lines and 11/12 branches; `server.ts` 272/282 lines and 102/111 branches;
  `environment-attachment.ts` 894/998 lines and 358/467 branches. This is a
  changed-source inspection, not a repository-wide threshold gate.
- Next: complete deterministic cheap preflight, self-review, commit, and push
  the feature branch immediately if clean.

## 2026-08-02 — Preflight and checkpoint

- Cheap preflight completed before the first checkpoint: generated build and
  tooling typechecks, affected ESLint, cleanup and TSDoc checks, API docs
  checks, generated-output cleanliness, changed-file formatting, and
  `git diff --check` completed without a reported failure. The parent
  mechanical pre-review subsequently ran `verify:task -- --no-tests` and
  located one remaining affected-file ESLint issue.
- Checkpoint commit `e18f8124a1f630b700c3f027b53067841b09a66b`
  (`feat(server): manage run lifecycle`) was pushed immediately to
  `origin/task/T-0090-run-lifecycle`; remote ref confirmation matched that
  SHA. No merge was performed.

## 2026-08-02 — Mechanical pre-review correction

- `verify:task -- --no-tests` reached ESLint and reported only
  `@typescript-eslint/no-confusing-void-expression` at the two ternary arms
  in the new `unusedPort()` close callback.
- Replaced the ternary callback with explicit block-bodied resolve/reject
  branches. The correction changes test cleanup syntax only; lifecycle
  behavior and production source are unchanged.
- Correction validation: `pnpm exec vitest run
packages/server/test/server/server.test.ts` passed 54/54. Affected-file
  ESLint, changed-file formatting, and `git diff --check` completed cleanly.
- Next: commit and push this mechanical correction immediately; the
  orchestrator retains review and release-gate ownership.

## 2026-08-02 — Final formatting correction

- Parent mechanical pre-review confirmed that `verify:task -- --no-tests`
  passes generation, builds, ESLint, cleanup, and TSDoc, and found only
  Prettier drift in this work log, `server.ts`, and `server.test.ts`.
- Ran repository Prettier write exactly on those three files, then checked
  formatting across every changed task file. `git diff --check` passed and
  `pnpm exec vitest run packages/server/test/server/server.test.ts` remained
  green at 54/54.
- This correction is formatting only; no lifecycle behavior or scope changed.
- Next: commit and push the formatting checkpoint immediately; review and
  release verification remain with the orchestrator.

## 2026-08-02 — Wave 1 correction RED

- Resumed as the existing `implementer`, configured `gpt-5.6-terra` with
  `medium` reasoning. Runtime self-introspection remains unavailable; the
  configured role/profile is the available evidence.
- Added focused regressions for same-instance concurrent `run()` coalescing,
  second-signal retry after failed final environment close, and occupied-port
  run rollback with signal-listener/ownership cleanup.
- RED command: `pnpm exec vitest run packages/server/test/server/server.test.ts`.
  Result: 2 intended failures and 55 passing. Concurrent `run()` calls
  returned distinct coordinator handles, and the failed final close removed
  both signal listeners before a second signal could retry it. The raw occupied
  port regression already passes, proving the existing failed-start rollback
  keeps that behavior intact.
- Next: keep retirement module-private, retain the final run record until its
  environment close succeeds, and coalesce run admission before coordinator
  wrapping. Then expand the specified public lifecycle documentation.

## 2026-08-02 — Wave 1 correction GREEN

- `Server.run()` now coalesces same-builder concurrent calls before process
  coordinator admission and returns one managed handle. Coordinator retirement
  mechanics are module-private.
- A final run record remains signal-reachable until environment closure
  succeeds, so a failed final close retries through a later signal without
  repeating completed server cleanup. The occupied-port failed-run regression
  proves no signal-listener leak and later caller-managed startup remains open.
- Expanded public `start()`/`run()` TSDoc and corrected the requested package,
  user-guide, and architecture lifecycle claims.
- GREEN validation: server and attachment suites passed 133/133; affected
  ESLint, Prettier, TSDoc enforcement, and `git diff --check` passed.
- Next: commit and push this accepted Wave 1 correction checkpoint; the
  orchestrator owns the narrow re-review and release gate.

## 2026-08-02 — Final narrow correction

- RED regression proved a later same-builder `run()` returned the cached closed
  managed handle. The cache now clears only after successful managed retirement;
  failed close retains it for retry, while a later run reaches terminal
  environment admission and rejects.
- Qualified the user-guide caller-managed close claim, made the standalone
  README example run-managed, and clarified retries include final environment
  cleanup.
- GREEN: focused server tests passed 58/58; affected ESLint, Prettier, TSDoc,
  and `git diff --check` passed.

## 2026-08-02 — Release-gate cleanup correction

- `verify:release` passed generation, build, tooling, and ESLint before cleanup
  enforcement required standalone private coordinator operations to have an
  exact necessity disposition.
- Grouped private removal and retirement under documented internal
  `ProcessServerCoordinatorValues`; no behavior or public API changed.
- Deterministic correction validation: focused server tests 58/58, cleanup
  enforcement, affected ESLint, Prettier, TSDoc, and `git diff --check` passed.
