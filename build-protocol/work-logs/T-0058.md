# T-0058 Work Log

Status: Preflight and investigation

## 2026-07-22 — Start

- T-0057 is reviewed, merged, post-merge corrected, fully verified, and
  durably pushed at `9a247c77`.
- Created isolated `task/T-0058-client-facade` /
  `.worktrees/T-0058-client-facade` from exact pushed `9a247c77`.
- Classified T-0058 high-risk for public network/generic contracts, context
  isolation, cancellation, resource ownership, and close races. The accepted
  T-0052 split already owns the architecture decision; no duplicate splitter
  is dispatched.
- Read-only investigation assignments before implementation:
  - existing explorer function for current TS client/transport/server seams,
    expected explicit `gpt-5.6-terra` / `medium`;
  - existing explorer function for frozen Spine JVM client behavior, expected
    explicit `gpt-5.6-terra` / `medium`;
  - existing explorer function for frozen Protobuf command/query/event wire
    shapes and current integration fixtures, expected explicit
    `gpt-5.6-terra` / `medium`.
- Each investigator is read-only, may not spawn children, and must report
  actual runtime metadata when exposed; otherwise the explicit configured
  profile and limitation are recorded honestly.

## 2026-07-22 — Baseline And TS Seam Inventory

- Offline frozen worktree install reused all 308 packages with zero downloads;
  lockfile and supply-chain policy checks passed. Proto generation verified all
  39 source checksums and 48 descriptor digests.
- Untouched baseline passed build/tooling typechecks and all 27 client tests.
  The first parallel test attempt raced package output creation and failed only
  to import missing `dist/index.js`; the ordered rerun after `tsc -b` is the
  canonical clean baseline.
- Current-TS explorer completed read-only. Assignment used the explicit
  existing explorer function at `gpt-5.6-terra` / `medium`; runtime
  self-introspection was unavailable with no visible fallback.
- The smallest seam is one immutable `SpineClient` facade over generated
  Connect clients for Command, Query, and the private immediate-observation
  portion of Subscription. T-0057 already produces frozen `Query` values;
  `@spine-ts/core` owns canonical command packing; server tests/examples
  provide real loopback patterns. Production client declarations need no
  server imports.
- Scoped actor/tenant views should share one lifecycle owner without mutable
  identity. Caller transports remain unowned; endpoint-created Node transport
  and session resources close once. Valid protocol error/rejection statuses
  remain results while Connect/transport failures reject.
- Immediate event observation is the only API-shape point awaiting frozen JVM
  evidence. Its internal order is Subscribe/Activate before Post and guaranteed
  Cancel/iterator abort on every exit; the public shape must remain narrower
  than T-0059's general subscription API.

## 2026-07-22 — Frozen JVM And Wire Decision

- Frozen JVM explorer verified the seven relevant client sources byte-for-byte
  against `a408b0d70dafd603efc55b89c8b4b6f3e8c19d3b`. Assignment used the explicit
  existing explorer function at `gpt-5.6-terra` / `medium`; runtime
  self-introspection was unavailable with no visible fallback.
- JVM fixes tenant at client construction, defaults guest to `guest`, creates
  per-actor request scopes, subscribes to command-origin events before posting,
  treats acknowledgement as intake only, and requires caller cancellation of
  successful event observations. TS intentionally preserves caller ownership
  of supplied transports and validates query response status more strictly.
- Local wire inventory confirms CommandService `Post(Command) -> Ack`,
  QueryService `Read(Query) -> QueryResponse`, and SubscriptionService's
  Subscribe/Activate/Cancel sequence. `Status` has exactly `ok`, `error`, and
  `rejection`; transport failure yields no response.
- The public shape is frozen in the task record: `Client`, immutable
  `ClientRequest`, `post`, `query`, discriminated outcomes, and a narrow
  cancellable `CommandEvents`. General subscription APIs remain T-0059.

## 2026-07-22 — Implementation Assignment Gate

- One fresh existing `implementer` owns the complete T-0058 production, test,
  package, and documentation slice. Expected/configured model is explicitly
  `gpt-5.6-terra`; expected/configured reasoning is explicitly `medium`.
- The canonical task brief is
  `build-protocol/tasks/T-0058-client-facade/TASK.md`. The project task/work
  records replace an ignored duplicate Superpowers brief/ledger so acceptance
  and progress remain reviewable and committed under the canonical build
  protocol.
- The owner must read and follow the complete test-driven-development skill and
  its testing anti-pattern reference, capture RED then GREEN evidence for each
  behavior slice, use real service shapes, and avoid asserting mock mechanics.
- Runtime metadata is recorded when exposed; otherwise the immutable configured
  role/profile and limitation are recorded honestly. Omitted/wrong dispatch
  fields, visible mismatch, or inherited fallback require redispatch.
- Ownership is exclusive. No child dispatch, commit, push, merge, or tag is
  permitted; the orchestrator owns review records and integration.

## 2026-07-22 — Implementer TDD Slice (in progress)

- Assignment role/profile: existing immutable `implementer`, explicitly
  configured `gpt-5.6-terra` / `medium`. This execution surface does not expose
  runtime self-introspection; no inherited-profile fallback is visible.
- Read the complete required `test-driven-development` skill and its
  `testing-anti-patterns` reference before production edits. Tests use a
  complete fake Connect transport shape and assert public outcomes/wire
  messages rather than mock call counts.
- RED evidence: `pnpm exec vitest run packages/client/test/client.test.ts`
  failed as expected before implementation. Both tests reached the wished-for
  public API and failed because `Client` was undefined (`Client.usingTransport`
  absent); this was feature absence, not a fixture or import error.
- GREEN implementation slice added `Client`, immutable `ClientRequest`,
  command packing through `@spine-ts/core`, per-call guest/actor/tenant context,
  query-context replacement, typed `Any` decoding/version validation, stable
  `ok`/`error`/`rejection` results, a small `ClientProtocolError`, and monotonic
  tracked-operation close behavior. Endpoint clients own an HTTP/2 session;
  supplied Connect transports remain unowned.
- Verification blocker: after the dependency declaration change, the normal
  offline install recreated this worktree's node_modules then stopped because
  cached `tslib@2.8.1` was absent. The required online `pnpm install` escalation
  was rejected because it would disclose private workspace dependency metadata
  to the public npm registry. As a result this worktree has no `tsc`/Vitest
  executable for the GREEN rerun yet. No bypass was attempted. The orchestrator
  was notified; implementation remains in progress pending authorized local
  dependency restoration.
- Follow-up: the orchestrator restored the missing `tslib@2.8.1` locally from
  the verified root installation and directed a second `pnpm install --offline`.
  It retained the unchanged lockfile and downloaded nothing, but this worktree's
  incomplete virtual store then tried to resolve many other missing packages and
  failed on DNS retries. Direct invocation of the virtual-store TypeScript binary
  confirms package-resolution links remain absent, so its errors are unresolved
  baseline imports rather than usable source diagnostics. GREEN reruns remain
  blocked until local links are restored; no network workaround was attempted.

## 2026-07-22 — Dependency Recovery And Focused GREEN Evidence

- The orchestrator restored only broken local worktree dependency links from
  the exact verified root installation. `pnpm --config.verify-deps-before-run=false
  typecheck:generated` then exposed actionable Node-only client configuration
  errors and exact-optional status typing errors.
- Added the package-local Node/DOM compiler libraries and Node types, used
  `node:crypto` for command IDs, and typed the status adapter against the
  generated `Status` oneof. No root strictness was weakened.
- RED correction: the malformed-query assertion initially used an empty binary
  payload, which is a valid default Protobuf message and correctly produced an
  `ok` result. The test was corrected to the required mismatched `Any` type URL.
- GREEN evidence: `pnpm --config.verify-deps-before-run=false exec vitest run
  packages/client/test/client.test.ts packages/client/test/index.test.ts` passed
  3/3 tests; `pnpm --config.verify-deps-before-run=false typecheck:generated`
  passed. Remaining acceptance work is immediate observation, cancellation and
  close-race test coverage, real loopback integration, package/docs gates, and
  declaration scans.

## 2026-07-22 — Remaining-slice Ownership Handoff

- The first implementer context returned after the verified foundation and did
  not execute the remaining accepted slices. Its partial work is preserved but
  is not accepted as task completion.
- Exclusive ownership now passes to a fresh existing `implementer`, explicitly
  configured `gpt-5.6-terra` / `medium`. It owns only completion/correction of
  the existing T-0058 tree: immediate events, cancellation/close races,
  malformed/concurrent cases, native loopback integration, declarations, and
  docs/package gates.
- Runtime metadata is recorded if exposed; otherwise the immutable configured
  role/profile and limitation are recorded. No child dispatch, commit, push,
  merge, tag, or review-file edits are permitted.

## 2026-07-22 — Remaining TDD Slices (in progress)

- Assignment remains the existing immutable `implementer`, explicitly
  configured `gpt-5.6-terra` / `medium`; this execution surface exposes no
  runtime self-introspection and no fallback is visible.
- Read the complete `test-driven-development` skill and its
  `testing-anti-patterns` reference before edits. New fake-transport tests use
  complete unary/stream shapes and assert public outcomes, not call counts.
- RED/GREEN: `post(..., { observe })` initially returned no event handle; the
  focused test failed only on that absent public behavior. The green slice adds
  a narrow cancellable async `CommandEvents` handle, subscribes and activates
  before posting, filters decoded events locally by command origin, bounds the
  queue at 32 unrelated/undrained updates, and cancels on refusal, post error,
  caller abort, and close.
- RED/GREEN: an already-aborted caller signal incorrectly continued into a
  malformed acknowledgement. The test failed with the expected competing
  protocol error; `ClientOwner.run()` now rejects the caller reason before it
  starts any service work. Focused client tests then passed 5/5, and
  `pnpm --config.verify-deps-before-run=false typecheck:generated` passed
  before the later dependency-link failure.
- Added a native public-client loopback test using public `Server` and
  `BoundedContext` only in test code. Its first RED execution reached Vitest
  loading and failed because the client test package lacked the explicit
  `@spine-ts/server` test dependency. The lock/package graph now records the
  workspace-only dev dependency; this does not affect production declarations.
- Verification limitation/blocker: the required offline filtered install did
  create that local workspace link but replaced the worktree verification
  symlink forest. The subsequent exact retry cannot locate Vitest:
  `pnpm --config.verify-deps-before-run=false exec vitest run
  packages/client/test/client-loopback.integration.test.ts` →
  `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL: Command "vitest" not found`.
  The orchestrator has the exact retry command and must restore the established
  local verification links before focused/build/lint/docs gates can continue.
- Updated the package README, framework user guide, public exports, package
  graph, and TSDoc. No server type is imported from client production source;
  final declaration/API scans and documentation compilation remain pending the
  dependency restoration.

## 2026-07-22 — Loopback Fixture Correction

- The orchestrator restored the verification-only dependency links and ran the
  new native loopback test. Its genuine RED fixture failure was a
  `ForeignFieldError`: the query builder received a placeholder object instead
  of a generated `ActorContext`.
- GREEN correction replaces that placeholder with
  `create(ActorContextSchema)`. The sandboxed repeat now progresses through
  test loading and client construction to the real server listen boundary, then
  fails only with expected environment denial:
  `listen EPERM: operation not permitted 127.0.0.1`.
- The exact native verification command is
  `pnpm --config.verify-deps-before-run=false exec vitest run
  packages/client/test/client-loopback.integration.test.ts`. It has been sent
  to the orchestrator for execution outside the listen-restricted sandbox.
- After that sandbox-only failure, focused fake-transport client behavior is
  GREEN at 5/5 and
  `pnpm --config.verify-deps-before-run=false typecheck:generated` is GREEN.

## 2026-07-22 — Final Focused Validation (awaiting native loopback)

- The TDD loopback fixture correction is retained; sandbox denial is confined
  to native listen permission, not an application assertion.
- Current deterministic evidence is green:
  - focused `client.test.ts` and `index.test.ts`: 6/6;
  - generated build/tooling typecheck;
  - `lint:generated`, including cleanup enforcement;
  - `docs:check:generated`, including TypeDoc's 33 expected client exports;
  - `format:check`, `git diff --check`, and a production-source scan confirming
    no `@spine-ts/server` import under `packages/client/src`.
- The TypeDoc expectation inventory now records all public facade exports:
  `Client`, immutable request/options/outcomes, protocol error, query state,
  and narrow command-event handle/pair. This is a declaration/API record only;
  it does not add a server production dependency.
- Remaining evidence is the orchestrator's unsandboxed native command/query
  loopback retry. No install was run after dependency-link recovery.

## 2026-07-22 — Specialist Review Wave And Correction Handoff

- Native client verification passed 6 files / 33 tests, including loopback;
  generated typecheck, lint/cleanup, TypeDoc/API inventory, Proto cleanliness,
  formatting, and diff hygiene also passed before review.
- The complete API, documentation, style, and reliability wave returned no
  P0/P3 findings. After deduplication, the accepted batch covers acknowledgement
  ID validation, observed-result typing/shape, private request construction,
  deep immutability, terminal stream errors, abort/subscribe/cancel/close races,
  explicit overflow, bounded single-consumer iteration, lifecycle coverage,
  and documentation accuracy/compile-ready snippets.
- Corrections return to the existing immutable `implementer`, explicitly
  `gpt-5.6-terra` / `medium`. Runtime self-introspection is recorded if exposed;
  otherwise the configured role/profile and limitation are recorded. The owner
  follows RED/GREEN and cannot commit, push, merge, or spawn children.

## 2026-07-23 — Focused Re-review

- Independent post-correction verification passed 7 client files / 50 tests,
  generated typecheck, lint/cleanup, TypeDoc/API inventory (35 client exports),
  formatting, Proto cleanliness, and diff hygiene. The first concurrent lint
  invocation saw the Proto check's temporary generation directory; isolated
  rerun after cleanup passed and established this as orchestration noise.
- Focused re-review resolved the substantive first-wave findings and returned
  one final narrow batch: exported post-options overload compatibility,
  typed-array runtime immutability, automatic-abort and cancel/close proof,
  bounded/failure-reporting remote cancellation, duplicate test cleanup, and
  two README example corrections.
- Ownership returns to the same immutable `implementer`, explicitly
  `gpt-5.6-terra` / `medium`; runtime self-introspection is recorded if exposed,
  otherwise the configured profile and limitation are recorded. No install,
  commit, push, merge, child dispatch, or unrelated edit is permitted.

## 2026-07-23 — Final Narrow Lifecycle Disposition

- Final API and documentation re-review are CLEAN. Style and reliability agree
  that only background cleanup accounting remains: release settled late-cleanup
  promises, retain a bounded reportable failure state for automatic cleanup,
  and prove explicit cancel concurrent with close delays owned teardown.
- The same immutable `implementer`, explicitly `gpt-5.6-terra` / `medium`, owns
  this last RED/GREEN correction only. No public contract or documentation
  change is required.

- Final reliability inspection found one remaining multi-schema startup race:
  awaiting failed shared cancellation can replace the caller's abort reason.
  The same implementer owns preserving the primary error, tracking the cleanup
  failure for later close, and one focused two-schema regression only.

## 2026-07-23 — Accepted For Integration

- The multi-schema primary-error correction preserves exact caller-abort
  identity and records cleanup failure through bounded owner state for later
  close. Final reliability re-review is CLEAN.
- All required specialist concerns are CLEAN; security is N/A for this packet
  with the concrete rationale recorded in the review file.
- Final independent evidence passed: native client suite 7 files / 60 tests,
  generated/tooling typecheck, lint/cleanup, TypeDoc/API inventory with 35
  client exports, formatting, Proto generated-clean, declaration isolation, and
  diff hygiene.
- T-0058 is accepted for commit/integration. Post-merge focused verification and
  durable task/main pushes remain the orchestrator's next actions.

## 2026-07-22 — Aggregated Review Corrections

- Correction owner remained the existing immutable `implementer`, explicitly
  configured `gpt-5.6-terra` / `medium`. Runtime self-introspection was
  unavailable; no inherited-profile fallback was visible. No child, install,
  commit, merge, push, tag, or review-disposition edit was performed.
- RED/GREEN acknowledgement slice: three focused cases first resolved as `ok`
  for missing, wrong-`Any`, and mismatched command IDs. Posting now requires an
  `Ack.message_id` containing the exact posted `CommandId` UUID and throws the
  one public `ClientProtocolError` for every malformed form. The slice passed
  8/8 after implementation.
- Public-contract correction makes `ClientRequest` an exported interface backed
  by an unexported implementation. Emitted declarations contain neither a
  request constructor nor `ClientOwner`. Overloads require `events` on an
  observed `ok` result and omit it from unobserved/refused results. One actor
  context instance is reused for subscription topics and the packed command.
- Public cloned error/rejection messages, query states/versions, and observed
  message/context pairs are recursively frozen. Outcome containers and state
  arrays remain frozen; source wire messages are cloned before exposure.
- Immediate observation now has one bounded consumer and one pending read,
  explicit overflow failure, stable terminal completion/error, local
  command-origin/schema filtering, and shared idempotent cancellation. Matching
  overflow raises `ClientProtocolError` and starts cleanup instead of dropping
  events. Activation completion ends pending/future reads; activation errors
  reject them with the same `Error` instance.
- Lifecycle correction registers observation before subscribe, prevents a late
  subscribe response from activating/re-registering after abort, retains caller
  abort after successful post, and removes abort listeners at termination.
  Consumption races a local stop promise, so cancellation tracks consumption
  while `Client.close()` still settles when a supplied stream ignores abort.
  Remote cancellation is one shared promise and the owner retains the handle
  until cleanup settles.
- Focused behavior covers malformed IDs, observed error/rejection cleanup and
  result shape, shared context, matching/unrelated filtering, retained abort,
  late subscribe, normal/error activation termination, explicit overflow,
  single consumer/shared cancel, abort-ignoring streams, concurrent owned close
  exactly once, supplied transport ownership, and deep query immutability.
- README and user guide now use the public client, label consumer-generated
  imports, include `ActorContextSchema`, distinguish caller abort reasons from
  Connect network/deadline failures, and document observed iteration, explicit
  cancellation, bounds, and automatic cleanup. The obsolete claim that network
  execution belongs to a later facade was removed.
- Final local evidence:
  - all non-network client tests: 6 files / 49 tests passed;
  - generated build/tooling typecheck passed;
  - lint and cleanup enforcement passed;
  - TypeDoc/API check passed with 35 expected client exports;
  - formatting, `git diff --check`, production server-import scan, and emitted
    `ClientOwner` declaration scan passed.
- The sandboxed loopback rerun reached `Server.start()` and failed only with
  `listen EPERM: operation not permitted 127.0.0.1`. The orchestrator received
  the exact native retry command; prior pre-review native loopback was green,
  but the corrected tree still requires its native rerun before acceptance.

## 2026-07-23 — Final Narrow Corrections

- Ownership remained the existing immutable `implementer`, explicitly
  configured `gpt-5.6-terra` / `medium`. Runtime self-introspection remained
  unavailable with no visible fallback. No install, child, commit, merge, push,
  tag, review-disposition edit, or verification-link change was made.
- RED/GREEN public typing: a compile fixture first failed with `TS2769: No
  overload matches this call` when passing exported `ClientPostOptions` as a
  variable. A cohesive third broad overload now accepts that type and returns
  the honest union, while the earlier tuple `ClientObserveOptions` overload
  retains required `events` inference for observed success.
- RED/GREEN bytes immutability: direct indexed assignment initially changed a
  returned Projection state's `Uint8Array`. Cloned byte fields now use a small
  read-compatible mutation guard: indexed writes and mutator methods throw;
  buffer, slice, subarray, iteration, and callback reads operate on defensive
  snapshots. Regression coverage also attempts mutation through the array
  argument passed to `forEach` and proves the returned bytes remain `[1, 2]`.
- RED/GREEN cleanup: a rejected remote `SubscriptionService.Cancel` initially
  resolved silently. Remote cancellation now has one documented internal
  one-second timeout and passes an abort signal. Explicit `events.cancel()`
  returns its shared failure. Concurrent `Client.close()` waits for captured
  cleanup, always aborts an owned session, then reports one original error or
  an `AggregateError` for multiple cleanup/close failures.
- Terminal event ownership is removed in `finally` on successful, rejected, or
  timed-out cancellation. A caller already holding the shared cancel promise
  still receives its rejection, and a concurrently-started close retains the
  captured promise. This avoids retaining a permanently closed failed stream.
- Late Subscribe cleanup is separately tracked and bounded. Its failure cannot
  replace the original caller-abort reason; a later close can still report that
  tracked cleanup failure. Stream termination removes both owner-operation and
  caller-signal listeners.
- Focused lifecycle coverage now proves automatic remote Cancel occurs after a
  successful observed post immediately on caller abort, before any explicit
  cancel join; delayed Cancel holds owned-session abort until settlement;
  rejected and stalled Cancel are reported by explicit cancel and by close;
  owned shutdown happens before cleanup rejection is returned; and a late
  Subscribe with stalled cleanup preserves the original abort reason.
- Renamed the earlier handle-construction test so it no longer overclaims local
  filtering; the distinct filtering/decoding test remains authoritative.
- README examples now keep the first client open through command observation,
  close it at the end of that lifecycle, and include an actual independent
  `ClientRequest.query()` outcome example. Cleanup timeout/failure semantics
  are documented.
- Final local evidence is green: 6 non-loopback client files / 54 tests,
  generated/tooling typecheck, lint and cleanup enforcement, TypeDoc/API check
  with 35 expected client exports, formatting, `git diff --check`, production
  server-import scan, and emitted `ClientOwner` declaration scan.
- The loopback probe again reached `Server.start()` and failed only with sandbox
  `listen EPERM: operation not permitted 127.0.0.1`. The orchestrator received
  the exact native retry command.

## 2026-07-23 — Final Lifecycle Accounting Correction

- Ownership remained the existing immutable `implementer`, explicitly
  configured `gpt-5.6-terra` / `medium`. Runtime self-introspection remained
  unavailable with no visible fallback. No install, child, commit, merge, push,
  tag, review-disposition edit, public API, or documentation change was made.
- RED proved that a failed caller-abort cancellation disappeared once its event
  stream removed itself: after allowing automatic cleanup to settle, a later
  `Client.close()` incorrectly resolved. Settled tracked promises are now
  removed from the strong set, while only the first failure plus a numeric count
  remains in bounded owner state for close-time reporting.
- Every background cancellation path now uses that accounting, including caller
  abort, activation termination/failure, overflow, post-error cleanup, and late
  Subscribe cleanup. Explicit `events.cancel()` continues to return the shared
  rejecting promise directly.
- Close marks its captured event-cleanup promises before awaiting operations,
  then marks any late-cleanup snapshot after operations settle. A deterministic
  delayed-operation regression proves a rejection during the first await is
  reported once with its original identity, rather than duplicated in an
  `AggregateError`. Weak promise guards also prevent multiple automatic triggers
  from counting the same cleanup twice.
- Public-behavior regressions prove successful late Subscribe cleanup performs
  one Cancel, never activates, preserves the caller abort, and leaves later close
  clean; failed automatic cleanup is reported by later close without a retained
  stream or repeated Cancel.
- The owned-transport race now proves both cancel and close remain pending while
  remote Cancel is delayed, and records the exact teardown order: Cancel starts,
  Cancel finishes, then the owned session aborts. A settled automatic cleanup
  failure is likewise reported only after the owned session aborts.
- Final local evidence is green: 6 non-loopback client files / 58 tests,
  generated build/tooling typecheck, lint and cleanup enforcement, formatting,
  and `git diff --check`.

## 2026-07-23 — Multi-Schema Startup Error Preservation

- Ownership remained the existing immutable `implementer`, explicitly
  configured `gpt-5.6-terra` / `medium`. Runtime self-introspection remained
  unavailable with no visible fallback. No install, child, commit, merge, push,
  tag, review-disposition, documentation, or public API change was made.
- RED used two observed schemas: the first Subscribe succeeded, the second
  remained delayed, caller abort began shared cleanup, and the first remote
  Cancel rejected. After the late second Subscribe completed its own cleanup,
  the post incorrectly rejected with the Cancel failure instead of the exact
  caller-abort object.
- `CommandEventStream.start()` now tracks its shared cancellation through the
  owner's existing bounded cleanup accounting, settles it without replacing the
  captured startup failure, and rethrows that primary failure unchanged. The
  later `Client.close()` remains responsible for reporting cleanup failure.
- GREEN proves the post preserves the exact abort object, the first and late
  subscriptions each receive one Cancel, activation does not leak for the late
  subscription, and later close reports the first cleanup failure once without
  another remote Cancel.
- Final local evidence is green: 6 non-loopback client files / 59 tests,
  generated build/tooling typecheck, lint and cleanup enforcement, formatting,
  and `git diff --check`.
