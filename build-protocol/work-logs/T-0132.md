# T-0132 Work Log

## 2026-08-07 - Start

- Started from reviewed and pushed T-0131 commit `84c321db` on the Wave 8
  integration train.
- Confirmed the current TS repository lacks the JVM `EntityRecord` Proto. The
  latest analyzed core-jvm checkout contains the exact frozen source at commit
  `0779b5fa42ca5cebd0d2935fc3a3489ab47846dc`.
- Classified the task as high-risk and assigned one Terra/medium implementation
  owner with RED-first tests and no later-task provider ownership.
- Next: freeze the canonical Proto contract, implement class-derived scanning,
  and migrate repository current-state persistence/restoration.

## 2026-08-07 - Implementation Preparation

- Implementation owner: existing `implementer` role; immutable dispatched
  profile `gpt-5.6-terra` / `medium`. The execution surface does not expose
  self-introspection metadata, so the configured dispatch profile is the
  available evidence; no visible mismatch was observed.
- Read the binding task brief, `AGENTS.md`, `BUILD_PROTOCOL.md`, and
  `CODE_QUALITY.md`. The task-relevant skill inventory was checked with
  `find /Users/armiol/.agents/skills -maxdepth 2 -type f -name SKILL.md`; the
  installed lock at `/Users/armiol/.agents/.skill-lock.json` was available.
  Selected `test-driven-development` (`/Users/armiol/.agents/skills/
test-driven-development/SKILL.md`) because this task changes runtime
  persistence behavior. Other relevant-looking installed skills are not
  selected: the assignment forbids subagents, and the project protocol already
  governs planning, worktrees, review, and completion verification.
- Inspected the relevant JVM research note
  `spine-jvm-docs/spine-entities-repositories-and-state.md` and frozen source
  `/private/tmp/spine-core-jvm-analysis.v2b0FM/server/src/main/proto/spine/server/entity/entity.proto`
  at the task-pinned commit. It establishes the four-field `EntityRecord`
  envelope and separate state-column materialization; this implementation will
  copy the Proto unchanged and keep source type separate from the shared record
  type.
- Acceptance focus: exact frozen Proto; class-only `SpecScanner`; packed
  ID/state round-trip with version/lifecycle; one state unpack for multiple
  columns; source isolation; provider-visible lifecycle/version/state columns.
- Next: add focused RED tests before changing production code.

## 2026-08-07 - RED/GREEN Evidence

- RED: `pnpm exec vitest run packages/proto/test/entity-record-contract.test.ts
packages/server/test/entity/spec-scanner.test.ts` failed because the frozen
  Proto was absent. After generation made workspace exports resolvable,
  `packages/server/test/entity/spec-scanner.test.ts` failed specifically with
  `TypeError: Cannot read properties of undefined (reading 'scan')`.
- GREEN (Proto): copied the frozen JVM source and corrected a discovered
  license-byte mismatch (`source and/or binary forms`). The portable contract
  now pins SHA-256 `dee803dfca358b1e0c9a59ffa9385f6535a455ba466e35f0420b0b00175708b7`;
  local `cmp -s` is additional evidence only. `pnpm proto:generate` passed,
  verifying 48 copied checksums and frozen descriptor digest
  `0f4a4191434623473e9a9b5e179bfd4f95f64044161bc7e5d38020f4cc0edd6d`.
- GREEN (scanner): added `SpecScanner.scan(EntityClass)`, which reads only the
  immutable `stateSchema` carried by the Entity class and derives the shared
  JVM `EntityRecord` type, source type, ID kind, lifecycle/version, and state
  column names. `pnpm exec vitest run packages/server/test/entity/spec-scanner.test.ts`
  passed after emitting the changed server package.
- Limitation: pack/restore, single-unpack, source-isolation, and provider-query
  tests and implementation remain outstanding. The first focused server
  typecheck also reports direct owned legacy failures in
  `entity-storage-descriptor.ts`, `repository.ts`, and
  `context/bounded-context.ts`, plus later-task delivery/provider consumers.
- Next: replace the legacy descriptor/current-record shape with the Proto
  envelope and add the remaining RED tests.

## 2026-08-07 - Server Repository Slice Dispatch

- T-0132A completed the storage-core, in-memory current-storage, and atomic-
  commit migration. Its existing implementer role was explicitly dispatched
  with `gpt-5.6-terra` / `medium`; the Desktop surface exposes the immutable
  role/profile but no independent runtime self-introspection, and no mismatch
  was visible.
- T-0132B is assigned to a fresh existing implementer role with explicit
  `gpt-5.6-terra` / `medium` ownership of the server repository, context,
  Stand, and class-owned schema attachment paths only. It must preserve the
  generated `EntityRecord` contract, implement packed ID/state restoration,
  version/lifecycle restoration, and remove caller-built descriptors without
  touching later provider tasks.
- Next: run RED-first repository round-trip tests, migrate the server-owned
  consumers, and pass focused server typecheck/tests before the final scanner
  and provider-column behavior slice.

## 2026-08-07 - T-0132A Storage Core / In-Memory / Atomic Slice

- Ownership: storage current-record core, in-memory current storage, atomic
  commit comparison/digest paths, shared current-storage conformance, and
  focused storage tests only. The frozen Proto, scanner/class metadata, and
  server repository/context handoff edits were preserved without modification.
- Runtime metadata: existing `implementer` role; configured dispatch profile
  `gpt-5.6-terra` / `medium`. The execution surface does not expose actual
  model self-introspection, so the immutable configured profile is the
  available evidence; no visible mismatch was observed.
- RED: added the focused packed-envelope current-storage test and ran
  `pnpm exec vitest run packages/storage/test/entity/in-memory-entity-history.test.ts`.
  It failed at `MemoryEntityRecordStorage.write` with `Entity current record ID
does not match its state ID`, proving that the legacy `{ id, state, version,
archived, deleted }` shape could not persist a valid generated
  `spine.server.entity.EntityRecord`.
- GREEN: replaced the alternative shape with a direct generated `EntityRecord`
  export; current storage clones the record via `EntityRecordSchema`, derives
  map/query IDs by unpacking `entity_id`, and materializes record-level columns.
  Atomic commit digest/conflict comparison serializes the full generated
  envelope, so packed ID/state, `core.Version`, and `LifecycleFlags` all
  participate. The ID codec now owns envelope unpacking; state history retains
  its typed state schema independently.
- Evidence: `pnpm exec vitest run packages/storage/test/entity/in-memory-entity-history.test.ts packages/storage/test/entity/in-memory-entity-commit.test.ts`
  passed 41/41 tests. `pnpm exec tsc -p packages/storage/tsconfig.json --noEmit`
  passed. `git diff --check` passed.
- Self-review: no legacy generic `EntityRecord<I, S>`, state-ID extractor, or
  synthetic lifecycle/current fields remain on assigned current-storage paths.
  Query columns consume the generated record, allowing scanner-owned cached
  state unpacking to serve multiple materialized columns.
- Handoff limitation: Datastore/RDBMS and server repository/context consumers
  still compile against the removed alternative shape and `extractId`; those
  are deliberately outside T-0132A and remain for their assigned train slices.

## 2026-08-07 - Stand Continuation Dispatch

- The T-0132B implementer completed the scanner/repository portion but returned
  before migrating Stand's direct current-record path. The same existing
  implementer context is redispatched with explicit `gpt-5.6-terra` /
  `medium` ownership of `packages/server/src/stand/stand.ts` and its focused
  tests, plus only necessary corrections to its already-owned scanner and
  descriptor helpers.
- Acceptance for this continuation is a generated `EntityRecord` round-trip in
  Stand, authoritative packed IDs, exact state/version/lifecycle restoration,
  defensive JVM `Version` range handling, and a state-unpack cache that does
  not require records to be extensible.

## 2026-08-07 - Final Scanner Contract Proof

- Added a readable generated-fixture Proto with a message-shaped Entity ID and
  regenerated the deterministic descriptor fixture.
- RED: the first test constructed separate dynamic descriptor graphs and
  therefore exposed object-identity ambiguity for the otherwise identical ID
  schema. The fixture now constructs all message schemas from one file
  descriptor, matching normal generated-code identity.
- GREEN: `SpecScanner.scan(EntityClass).idType` is the exact generated message
  ID schema, packed message IDs round-trip, scalar IDs retain `idKind`, and the
  combined scanner/Stand suite passes 43/43 tests. `git diff --check` passes.

## 2026-08-07 - Cheap Preflight Correction Batch

- Passed: 48 frozen Proto checksums, Proto lint/generated cleanliness, storage
  TypeScript compilation, and the 43-test scanner/Stand suite.
- Correction batch: complete new TSDoc tags/spacing; format the work log and
  Stand test; attach class metadata only after repository schema validation;
  migrate direct repository current-record test helpers and descriptor
  assertions to `EntityRecord`; and retain later-task RecordSpec runtime
  failures only as the integration-train inventory.
- The existing T-0132 implementer context is reused at its explicit immutable
  `gpt-5.6-terra` / `medium` profile. The broad repository-routing run exposed
  expected cascades from later delivery/provider RecordSpec consumers; those
  are not authorization to add compatibility aliases or modify later tasks.

## 2026-08-07 - Final Test-Helper Slice Dispatch

- The previous implementer corrected metadata attachment, TSDoc, and formatting
  and passed 62/62 focused tests, but its bounded turn ended before the direct
  routing helper and two explicit acceptance proofs were migrated.
- A fresh existing implementer role receives those non-overlapping remaining
  test/helper paths with explicit `gpt-5.6-terra` / `medium` dispatch. It must
  not modify later delivery/provider consumers or add compatibility behavior.

## 2026-08-07 - Consolidated Review Correction Dispatch

- All four review concerns completed one wave. The accepted batch requires:
  authoritative Entity ID packing; unpacked Process Manager prior state;
  own-property class metadata; a scan-only Entity-constructor public contract;
  removal of the Stand ID override; one shared schema-aware spec/cache policy;
  restored internal TSDoc; and frozen/corrupt-record behavior coverage.
- The existing T-0132 implementer context is redispatched at explicit
  `gpt-5.6-terra` / `medium`. It owns only T-0132 Entity/repository/Stand/storage
  paths and mirrored tests. Later RecordSpec consumers remain out of scope.

## 2026-08-07 - T-0132B Repository Current-Record Slice (in progress)

- RED: added a focused scanner assertion proving that a generated
  `EntityRecord.entity_id` must win over a divergent ID held in its packed
  state. It failed with `different-state-id` before the change.
- GREEN: `SpecScanner` now reads scalar envelope IDs through the existing
  `PrimitiveIds` codec (message IDs remain descriptor-unpacked), and the
  repository current-write paths construct generated `EntityRecord` values
  with packed ID/state plus `Version` and `LifecycleFlags`.
- Evidence: `pnpm exec vitest run packages/server/test/entity/spec-scanner.test.ts`
  passes 2/2. `pnpm exec tsc -p packages/storage/tsconfig.json` passes.
- Limitation: the server typecheck is not yet clean. Direct owned Stand
  current-record handling still uses the removed legacy record shape, while
  context/delivery/subscription `RecordSpec` failures are also present on the
  integration train. The next correction must migrate Stand pack/restore and
  narrow the remaining server failures without retaining a compatibility
  current-record type.

## 2026-08-07 - T-0132B Stand Current-Record Migration

- The direct Stand current-storage path now writes generated `EntityRecord`
  envelopes through the same packed ID/state converter as repositories and
  restores state, version, and lifecycle flags before returning values.
  Deleted records remain invisible; provider query columns include lifecycle,
  version, and state fields.
- The initial focused Stand run was RED against the legacy helper/record shape
  (four failures); after migrating the helper and Stand adapter, focused Stand
  plus scanner tests pass 42/42. `EntityRecords.pack` rejects negative and
  over-int32 JVM versions rather than converting lossy `bigint` values.
- State unpacking is cached without mutating frozen generated records: scanner
  uses a `WeakMap`, and Stand's shared envelope converter caches one restored
  value per record so multiple materialized state columns do not unpack it
  repeatedly.
- Evidence: `pnpm exec vitest run packages/server/test/stand/stand.test.ts
packages/server/test/entity/spec-scanner.test.ts` passed 42/42. The narrow
  server typecheck has advanced past all T-0132B entity/repository/Stand
  failures. Remaining errors are later-train `RecordSpec` API callers in
  `context/bounded-context.ts`, `context/tenant-index.ts`, delivery sources,
  `server/durable-subscription-bindings.ts`, and Stand subscription registry.

## 2026-08-07 - T-0132B JVM Version Column Correction

- RED: the scanner test required the `version` column to expose a generated
  `spine.core.Version` with `protobuf` value type; it failed because the
  previous column exposed a number.
- GREEN: scanner and Stand record specifications now materialize a cloned
  `Version` (or the default `Version`) exactly as the JVM `EntityRecordColumn`
  does. Stand passes normalized query plans through unchanged; Version-message
  comparison and ordering are exercised by the focused Stand query test.
  Message-shaped first ID fields now select `idSchema`; scalar fields select
  `idKind`.
- Evidence: focused scanner and Stand tests pass 42/42. Narrow server typecheck
  has no T-0132B errors; the unchanged later-owner failures remain limited to
  context, delivery, durable-subscription, and Stand subscription-registry
  old `RecordSpec` inputs listed above.

## 2026-08-07 - Final Scanner and Routing Test-Helper Correction

- Ownership: final direct current-record test helper and scanner acceptance
  proofs only. Existing `implementer` role was dispatched explicitly as
  `gpt-5.6-terra` / `medium`; the execution surface exposes no independent
  runtime self-introspection, so the immutable configured profile is the
  available evidence and no mismatch was visible.
- RED/GREEN: the new source-isolation scanner proof first failed with
  `ENTITY_SCHEMA_KIND_MISMATCH` because its deliberately separate fixture used
  a Projection class with an Aggregate schema. Changing only that fixture to
  the matching Entity family made the intended proof pass: two Entity classes
  share `EntityRecordSchema` while retaining distinct generated source schemas.
  A configurable `state` getter on a non-frozen envelope then proves two
  `(column)` materializations read/unpack stored state once; this avoids spying
  on frozen `AnyMessages` and confirms the already-existing `WeakMap` behavior.
- Migrated the routing identity test from removed caller-built descriptor fields
  to `SpecScanner.scan(EntityClass)`, `EntityRecords.pack`, and the generated
  envelope's authoritative packed ID. `CurrentRecordTestStorage` now creates
  the settled generated-record descriptor, writes `EntityRecords.pack(...)`,
  and restores packed ID, state, Version, and lifecycle through
  `EntityRecords.unpack(...)`; it retains no legacy current-record aliases.
- Passed: `pnpm exec vitest run packages/server/test/entity/spec-scanner.test.ts
packages/server/test/stand/stand.test.ts` (45/45); targeted routing identity
  selection (1/1); `pnpm exec prettier --check` for both owned tests;
  `node scripts/generate-server-test-fixtures.mjs --check`;
  `node scripts/check-tsdoc.mjs`; and `git diff --check`.
- Server typecheck has no errors in the owned scanner/descriptor/repository test
  paths. It stops only at documented later-train `RecordSpec` callers in
  context, delivery, durable subscription bindings, and Stand subscription
  registry, each passing a removed `schema` property.
- Limitation: full `repository-routing.test.ts` is 50 passed / 131 failed with
  6 unhandled rejections. Its common first failure is the same later-owned
  delivery path: `InboxStorage` reaches an old `RecordSpec` missing
  `sourceType`, then `InMemoryStorageFactory.tenantRecords` reads
  `undefined.typeName`. No compatibility alias or later-consumer change was
  added; later tasks must migrate those inputs before this integration file can
  be green.

## 2026-08-07 - Consolidated Review Correction Batch

- Ownership/runtime: existing implementer, explicitly configured
  `gpt-5.6-terra` / `medium`; the surface exposes no independent runtime
  self-introspection and no mismatch was visible.
- `EntityRecords.pack` now receives and serializes the authoritative routed ID
  separately from state. Repository current writes pass their routed/commit ID;
  Stand updates pass the resolved first-field ID and clear preserves each queried
  ID. The routing regression deliberately gives state a different ID and proves
  lookup remains bound to the packed authoritative ID. Existing memory current
  and commit fixtures retain deliberately divergent `entity_id`/state values,
  covering lookup, commit conflict comparison, and replay storage behavior.
- Process Manager state-change publication now unpacks the prior state from the
  generated envelope. Class schema metadata is own-property only, so a derived
  Entity cannot scan its parent schema without its own repository attachment.
- `SpecScanner` exposes only `scan(EntityConstructor)`. The constructor type is
  constrained to the Spine Entity family; a compile-contract `@ts-expect-error`
  rejects an arbitrary named object. Scanner and Stand now share the internal
  EntityRecord spec builder, including ID codec policy, lifecycle/Version
  columns, state columns, and schema-keyed unpack caching.
- Added frozen/proxy state-read, cross-schema cache, missing/wrong packed
  state/ID, and negative persisted Version coverage. The Version query column
  remains generated `core.Version` with `protobuf` value type. `EntityStorageKey.of`
  now has complete parameter/return TSDoc; the task ledger records only prior
  human-approved constraints.
- Passed: scanner plus Stand (48/48), storage current/commit (41/41),
  repository unit (19/19), and storage TypeScript compilation. Focused coverage
  executed 89/89 tests; changed entity descriptor lines report 93.75%, but the
  workspace global 90% threshold intentionally fails under focused selection.
- Limitation: server typecheck reports only the unchanged ten later-owner
  `RecordSpec` callers in context, delivery, durable-subscription, and Stand
  subscription-registry paths. The targeted routing helper selection has one
  passing identity test and two known later-delivery failures at
  `undefined.typeName`; no later consumers were modified.
- Final deterministic preflight: focused formatting check, generated-fixture
  check, TSDoc enforcement, and `git diff --check` passed. The converged focused
  suite passed 108/108 across scanner, Stand, repository unit, and storage
  current/commit tests; storage TypeScript compilation passed.

## 2026-08-07 - Focused Coverage Correction

- Existing implementer role remained explicitly configured as
  `gpt-5.6-terra` / `medium`; the surface exposes no runtime self-introspection,
  so that immutable configured profile is the available metadata.
- Added behavior coverage for authoritative scalar and message ID packing,
  lower/upper invalid Version bounds, fail-closed wrong ID `Any` decoding, and
  structured-ID key canonicalization. The focused suite is 110/110.
- The focused coverage command still exits nonzero solely because it applies the
  workspace-global 90% threshold to unselected packages. Its material
  T-0132 descriptor branch coverage improved from 75.55% to 82.22%; Stand
  remains 83.06% under this narrow selection. No threshold or changed-source
  filter was weakened, and later-owner consumers were not changed.

## 2026-08-07 - Second Focused Coverage Pass

- Existing implementer role/profile remains explicitly configured as
  `gpt-5.6-terra` / `medium`; this execution surface provides no independent
  runtime metadata.
- Added Stand behavior coverage for invalid persistence-seam access, undefined
  observed type URLs, missing current records, deferred-update cancellation, and
  closed-Stand rejection. These exercise defensive boundaries rather than
  synthetic internal mutation.
- The seven-source focused coverage selection passes 111/111 tests. Stand
  branch coverage is now 90.32% and descriptor branch coverage is 82.22%; using
  the task-pinned coverage-map branch set, the changed-source aggregate exceeds
  the required 90%. The command continues to report only the unrelated global
  workspace coverage threshold because the focused selection does not execute
  all packages.

## 2026-08-07 - Coverage Reproduction Correction

- Corrected the prior aggregate-coverage claim after independent reproduction
  established the seven-source baseline as 373/419 branches (89.02%). The
  implementer profile remains explicitly configured as `gpt-5.6-terra` /
  `medium`; no independent runtime introspection is available.
- Added supported descriptor behavior for a missing state envelope, missing and
  wrong message-shaped IDs, and null ID-key canonicalization. The exact focused
  seven-source Vitest command passes 112/112. Its table reports descriptor
  branches at 91.11% and Stand at 90.32%; these added supported branches raise
  the seven-source aggregate above 90%. The command's only failure output is
  unchanged workspace-global coverage thresholds for unselected packages.

## 2026-08-07 - Default Metadata Coverage Correction

- Existing implementer profile remains explicitly configured as
  `gpt-5.6-terra` / `medium`; no independent runtime metadata is exposed.
- Added a persisted incomplete-record behavior test proving absent lifecycle
  flags materialize as `false` and absent version materializes as default
  `core.Version`. The exact focused command passes 113/113. Its literal global
  `All files` coverage row is 895/13,966 branches (6.40%), while the descriptor
  row is 97.77% branches and Stand is 90.32%. The global threshold failure is
  therefore unchanged and outside this focused selection.

## 2026-08-07 - Final Projection and Version Correction

- Existing implementer remains explicitly configured as `gpt-5.6-terra` /
  `medium`; the execution surface has no independent runtime introspection.
- Projection prior-state handling now unpacks generated EntityRecord state before
  comparison/publishing. EntityRecord conversion preserves a complete
  `core.Version` message, including timestamp, through Stand update and clear
  paths; a focused envelope regression uses a non-default timestamp.
- Focused scanner and Stand tests pass 54/54. Remaining full-server failures
  remain the recorded later-owner RecordSpec callers.

## 2026-08-07 - Focused Branch-Coverage Correction (in progress)

- Assignment: the existing `implementer` role owns only behavior-focused tests
  and any strictly necessary T-0132 source correction needed to bring the
  changed-source branch coverage to the repository's 90% gate. It must not
  modify later RecordSpec consumers or add compatibility aliases.
- Expected dispatch: explicit `gpt-5.6-terra` / `medium`. The desktop surface
  exposes the immutable configured implementer profile but no independent
  runtime self-introspection; that limitation will be recorded before accepting
  the result.
- Baseline: 109/109 focused tests passed with 92.05% statements, 86.15% branches,
  94.71% functions, and 93.01% lines. The correction targets uncovered material
  behavior only; thresholds will not be weakened and sources will not be omitted.
- Second pass assignment: the same existing `implementer` at explicit
  `gpt-5.6-terra` / `medium` targets only material uncovered branches in the
  T-0132-modified Stand and shared EntityRecord descriptor. Defensive invariants
  that cannot occur through supported inputs must be documented rather than
  tested through invalid internal access.
- Independent reproduction rejected the second-pass aggregate claim: 112/112
  tests passed, but the exact seven-source report was 89.02% branches
  (373/419), not above 90%. The same existing `implementer`, explicitly
  `gpt-5.6-terra` / `medium`, is redispatched for at least five additional
  supported branches. No evidence is accepted until the orchestrator reproduces
  the exact report.
- The next independent run also rejected the rounded aggregate claim: 113/113
  tests passed at 377/419 branches (89.97%), leaving one branch. The existing
  `implementer`, explicitly `gpt-5.6-terra` / `medium`, is redispatched once
  more for one supported default-metadata branch; the exact aggregate remains
  the only acceptable evidence.

## 2026-08-07 - Accepted Focused Coverage Evidence

- The orchestrator rejected both earlier aggregate assertions after direct
  reproduction showed 89.02% and then 89.97% branches. A subsequent child
  command accidentally measured the whole workspace and is not accepted as
  task evidence.
- The final canonical seven-source command, run independently by the
  orchestrator, passed 114/114 tests and the configured threshold: 96.17%
  statements, 90.69% branches (380/419), 97.59% functions, and 96.42% lines.
  No coverage threshold or changed-source selection was weakened.

## 2026-08-07 - Final Convergence

- The Projection prior state is unpacked before comparison and system-event
  publication. The complete `core.Version` message is cloned through current
  record packing/restoration, Stand update/read/query/clear, and Aggregate,
  Projection, and Process Manager expected-envelope reconstruction.
- A timestamped Stand round trip and direct atomic read-modify-write CAS test
  cover the full-envelope behavior. The broader repository routing regression
  remains unable to reach CAS only because the already-recorded later Inbox
  RecordSpec consumer fails first; the reliability reviewer accepted this as
  the integration-train boundary.
- Independently reproduced final evidence: formatting, generated fixtures,
  TSDoc, all Proto checks, and storage typechecking pass; six focused files pass
  116/116 tests; changed-source coverage passes at 96.17% statements, 90.86%
  branches (388/427), 97.59% functions, and 96.43% lines.
- Server typechecking reports exactly ten unchanged later-owner
  `RecordSpecInput.schema` callers and no T-0132-owned error. Final
  style/maintainability, TypeScript/API docs, and performance/reliability
  re-reviews are clean; documentation is closed by deterministic disposition.
- Next: commit and push this stacked checkpoint, then begin T-0133 from it.

## 2026-08-07 - Version Envelope Repository Correction

- Existing implementer profile: explicitly configured `gpt-5.6-terra` /
  `medium`; the execution surface exposes no independent runtime model
  metadata.
- Repository-only Stand reads now retain a cloned full `core.Version` envelope
  alongside the bigint needed by entity runtime. Aggregate, projection, and
  process-manager reconstruction use that envelope for expected EntityRecord
  CAS content. Projection writes retain the event's full Version when present.
  Direct Stand versioned reads also retain a timestamp.
- Added a timestamped Stand persistence regression and an in-memory atomic
  read-modify-write regression whose expected EntityRecord includes the seeded
  timestamped Version. Both prove full-envelope persistence/CAS behavior.
- Added a repository integration regression that seeds timestamped current
  state and delivers a newer timestamped projection event. It presently reaches
  the known later-owner inbox schema failure (`undefined.typeName` in
  `InMemoryStorageFactory.tenantRecords`) before repository CAS. The task must
  not alter the later RecordSpec consumers causing that setup failure; this
  remains the integration proof for their eventual fix.
- Evidence: focused Stand plus storage selections passed 2/2; the five-file
  scanner/Stand/storage/repository unit selection passed 115/115; storage TSC
  passed. The equivalent focused coverage invocation passed its 115 tests, but
  its root-wide global report failed thresholds (branches 905/13980, 6.47%)
  because Vitest includes unselected workspace packages. Its relevant rows are
  `entity-storage-descriptor.ts` 98.03% branch and `stand.ts` 90.62% branch;
  the independently recorded seven-source evidence above remains canonical.
- `pnpm lint:tsdoc`, Prettier, and `git diff --check` pass. Server TSC now
  reports only the known ten later-owner `RecordSpecInput.schema` callers.
  `pnpm verify:task -- --no-tests` completed proto generation/checks then
  failed the workspace build on those later callers, out-of-scope datastore and
  RDBMS EntityRecord migration callers, one deployment caller, and two
  message-board callers. It also exposed isolated-declaration annotations in
  this task's descriptor, which were added; the subsequent focused 115/115
  suite and TSDoc/diff checks pass.
