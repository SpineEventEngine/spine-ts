# T-0194 Work Log

## 2026-08-15 - Start

- Started from clean accepted integration endpoint `a232191c` after T-0188
  through T-0193 were integrated and pushed.
- Classified high-risk. The complete Human-Imposed Requirements Ledger,
  specialist/security profiles, serialized live-resource policy, single final
  release profile, isolated-main integration, and remote-closure contract are
  durable before verification begins.
- Next: combined cheap preflight, Wave-wide review, one correction batch if
  needed, sequential live evidence, one release profile, main/post-merge/remote
  closure.

## 2026-08-15 - Combined preflight finding

- A fresh integration worktree first required the canonical `pnpm
proto:generate` wrapper so workspace plugin binaries were on `PATH`; direct
  script invocation is not a valid generation profile.
- After successful generation/build, tooling typecheck found two browser-owned
  test errors in `packages/auth/test/native-gateway-services.test.ts`: passing a
  `Promise<void>` resolver directly to `AbortSignal.addEventListener` gives it
  an incompatible `Event` argument.
- The deterministic correction returns to the existing browser `implementer`,
  explicitly configured `gpt-5.6-terra` / medium with no subagents. Runtime
  telemetry is unavailable. After correction, the entire cheap preflight
  restarts; no partial pass is retained.

## 2026-08-15 - Combined preflight clean

- Browser test correction `6886340e` is pushed: both abort listeners ignore the
  event argument. Tooling typecheck and 34 native Gateway tests pass.
- The entire preflight restarted from canonical `pnpm proto:generate` and is
  clean: generated build, tooling typecheck, cleanup enforcement, TSDoc,
  audience/API docs, full formatting, and diff check pass.
- Combined deterministic Vitest selection passes 172 tests with four expected
  live-provider skips. Browser runner/Envoy/topology selection passes 14 tests,
  including native three-update continuity and terminal OPTIONS routing.
- Generated manifest/marker residue was restored after verification; the
  integration worktree is clean. Accepted task coverage remains separate:
  Browser 100% changed production lines/branches, Query 96.30%/93.44%, and
  Inbox 93.75%/91.60%.
- Next: collect the four specialist concerns and final security review as one
  Wave-wide result before any release profile.

## 2026-08-15 - Wave-wide review result

- TypeScript/API, style/maintainability, and documentation are clean.
- Accepted correction batch: provider lease currency recheck after awaited
  Inbox read; hard maximum normalized candidate budget; browser failure-log
  credential/URL redaction.
- Security also confirmed production dependency advisories. The binding human
  sequence assigns dependency hygiene to Wave 17, so Wave 12 records and
  defers them rather than changing the lockfile out of order.
- Three isolated, non-overlapping correction worktrees start from `b1b0b023`.
  No release profile or live provider window begins until corrections and
  affected re-review converge.

## 2026-08-15 - Correction convergence

- Browser failure diagnostics were reduced to path-only request locations and
  a non-sensitive header allowlist. Query plans now reject explicit candidate
  budgets above 10,000 before provider access, with both the accepted 10,000
  and rejected 10,001 boundaries exercised. MySQL and Datastore now recheck the
  locked cleanup lease immediately before deletion/commit.
- Separate live correction runs passed two MySQL cases and two Datastore cases,
  including native physical-row assertions; provider windows were serialized
  and released without broad database or emulator cleanup.
- The restarted converged preflight passes 221 deterministic tests with four
  expected live-provider skips and 25 browser runner/Envoy/harness/topology
  tests. Generation, build/tooling typechecks, cleanup, TSDoc, API/audience,
  full formatting, and diff checks are clean.
- Final query-policy scoped coverage is 97.76% lines and 96.85% branches. Both
  new provider lease branches and both query-ceiling branches execute, giving
  the executable correction diff full line and branch coverage. Runtime and
  live-provider evidence remain separate from V8 accounting.
- Next: affected concern re-review, then serialized live Wave acceptance and
  the single converged release profile.

## 2026-08-15 - Final re-review acceptance

- The final correction endpoint is `746b00e8`. Browser diagnostics omit raw
  console-error text and sensitive request details. Query policy enforces the
  inclusive 10,000 maximum, while memory retains the provider-boundary
  `candidateLimit + 1` sentinel so ordering is never applied to a silently
  truncated candidate set. MySQL and Datastore recheck lease currency after
  mutation and roll back rather than commit after expiry.
- Performance/reliability, TypeScript/API, and documentation re-reviews are
  clean. Final security accepts the Wave with production dependency advisories
  explicitly deferred to binding Wave 17. The previously clean style lane was
  not reopened because later corrections did not restructure production code.
- Fresh central preflight passes generation, build/tooling typechecks, cleanup,
  TSDoc, audience/TypeDoc, full formatting, diff checks, 224 focused runtime
  tests with four expected provider skips, and 25 browser topology tests.
- Next: real Chromium/Envoy/Gateway, live MySQL, and live Datastore acceptance
  in serialized windows, followed by the single converged release profile.

## 2026-08-15 - Final live acceptance

- Real Chromium passes 9/9 through the supported Vite -> Envoy -> Gateway ->
  native service topology. The passive viewing tab performs no writes and
  receives three distinct sequential updates from the writer while one Gateway
  binding and native stream remain healthy. Forced disconnect settles both to
  zero; credential, CSRF, origin, ownership, context, and diagnostic-redaction
  cases pass. The harness removes Envoy and releases browser listeners.
- In the exclusive MySQL 8.4 window, the production normalized SQL-plan case
  passes 1/1 and the Inbox cleanup/fencing block passes 2/2 with native `1 -> 0`
  physical-row assertions. The window is released before Datastore begins.
- In the separate Datastore emulator window, normalized plan plus tenant/group
  isolation pass 2/2 and Inbox cleanup/fencing pass 2/2 with native key-count
  assertions. MySQL remains idle, no emulator-wide clear runs, and the window
  is released.
- Live runtime/provider proof remains separate from V8 accounting. Next: the
  one permitted converged `pnpm verify:release` at the accepted integration
  endpoint.

## 2026-08-15 - Release-profile pre-test correction

- The first release-profile invocation stopped before tests because the cheap
  preflight had omitted repository-wide ESLint. Two browser-owned abort-listener
  test callbacks used shorthand bodies that returned a `void` expression,
  violating `@typescript-eslint/no-confusing-void-expression`. No runtime,
  coverage, packaging, or release result was produced by that stopped attempt.
- Existing browser owner `implementer`, explicitly configured
  `gpt-5.6-terra` / medium with no subagents, applied the two block-body fixes in
  `c805aaa3`. Focused ESLint, tooling typecheck, 34 Gateway tests, formatting,
  and diff checks pass. Runtime telemetry remains unavailable.
- The orchestrator then ran the complete canonical generated-gate chain rather
  than another partial preflight. Build/tooling typechecks, repository-wide
  ESLint, cleanup, TSDoc, copyright, full formatting, TypeDoc/audience/snippets,
  Proto lint/current-output, logging containment, and release readiness all
  pass. The release profile may now restart from a genuinely converged tree.

## 2026-08-15 - Release-suite Inbox handoff correction

- The restarted release profile passed all generated, lint, documentation, and
  readiness gates, then reported 42 failures in the combined server suite.
  Reproduction narrowed this to local Inbox handoff completion after bounded
  cleanup removed eligible delivered rows in the same drain.
- Exact direct-drain acknowledgement replaces post-cleanup row rereads as the
  success signal. Bounded in-flight state shares acknowledgement among concurrent
  local handoffs, and Entity/Projection drains accept only the exact received row
  or registered labels and target types owned by that local inbox.
- Typed rejection events were also posted while the command callback still held
  the shard. Their Projection row was persisted but could not reacquire ownership.
  Rejection publication now runs as the existing post-drain follow-up, after shard
  release.
- The four original failing files now pass 359/359. The expanded context,
  repository, and delivery selection passes 636 tests with four expected
  service-gated skips. Server typecheck, focused formatting, and diff checks pass.
  Diff-scoped V8 accounting against `96501a07` is 100% lines (83/83) and 91.80%
  branches (56/61); live provider/runtime evidence remains separately accepted.
- Next: affected performance/reliability and TypeScript/API review, canonical
  preflight, then a fresh release profile from the pushed correction endpoint.

## 2026-08-15 - Release-correction acceptance and preflight

- Performance/reliability and TypeScript/API re-reviews are clean. The
  cleanup-enforcement correction centralizes the unchanged full message ID key
  in internal `InboxHandoff`; both reviewers confirmed pushed endpoint
  `b811d724` remains clean.
- Canonical `verify:task --no-coverage` passes every deterministic gate and the
  expanded 636-test selection with four expected provider-gated skips. The
  worktree is ready for the full release profile.

## 2026-08-15 - Converged release verification

- The single converged `pnpm verify:release` passes at pushed endpoint
  `03d89e76`: 256 test files pass, four service-gated files skip, 4,172 tests
  pass, and 19 skip.
- Repository-wide V8 coverage is 94.14% statements, 90.49% branches, 94.02%
  functions, and 95.21% lines. Every generated-source, build/tooling, lint,
  cleanup, documentation, Proto, logging, and release-readiness gate passes.
- Live Chromium, MySQL, and Datastore proof remains separately recorded and
  accepted. Next: verify the current remote baseline, integrate in an isolated
  worktree, run post-merge checks, push `main`, and reconcile remote/provider
  resources.

## 2026-08-15 - Integration closure

- A fresh fetch confirmed `origin/main` at `e2ab42d2` and proved every remote
  Wave branch contained in the accepted integration endpoint. The primary
  checkout and protected human review folder remained untouched.
- Isolated merge `f06f87d2` has the exact file tree of release-verified endpoint
  `05af3dd1`. After frozen installation and the required generated/build step,
  server typecheck, release-readiness, diff integrity, and the affected runtime
  slice pass with 416 tests and four expected provider-gated skips.
- Wave 12 is complete. The remaining closure operation is to push the final
  documentation commit as `main`, delete only remote branches proven contained
  and all tags, stop the disposable MySQL/emulator resources, and verify the
  final remote shape.

## 2026-08-15 - Remote and provider cleanup

- `main` was pushed through closure commit `126d44ff`. Each of the eight
  remaining Wave 12 remote branches was individually confirmed contained in
  `origin/main` and deleted; no remote tags existed. The resulting origin
  exposed only `refs/heads/main`.
- Dedicated container `spine-wave12-mysql` was stopped and removed. The exact
  gcloud Datastore-mode emulator for project `spine-wave12` was terminated via
  its parent process, its Java child exited, and 127.0.0.1:8081 no longer has a
  listener.
- The primary checkout's human-owned changes and
  `agentic-review-of-main-branch-14-Aug-2026` remain untouched. Wave 12 has no
  remaining implementation, review, release, integration, remote, or provider
  action.
