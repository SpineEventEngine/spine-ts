# T-0148 Work Log

## Start

- Started: 2026-08-09.
- Branch: `task/T-0148-mysql-tenancy`.
- Baseline: `77a41124` (`test(storage): freeze JVM provider values`).
- Classification: high-risk provider tenancy, serialized layout, transaction,
  and physical-compatibility correction.
- Authoring owner: `/root`, single production writer; no child was dispatched
  because the active session instruction reserves subagents unless requested.
- Accepted design and human requirements are frozen in T-0146/T-0147 and are
  not being replanned.

## Current State

- RED/GREEN added `MysqlColumnMapping`: native JVM scalar/enum/bytes/null
  values, compact Proto JSON ordinary messages, epoch-nanosecond Timestamp,
  and numeric Version. DDL selection uses the same generated column type.
- RED/GREEN added `MysqlIdColumn`: native string/int32/int64 IDs and reversible
  compact Proto JSON message IDs. Invented primitive kinds fail closed.
- Canonical table specifications now contain only `ID`, `bytes`, and declared
  columns, with primary key `ID`; `_scope` and `_revision` are absent.
- The common `StorageContext` now preserves a complete generated `TenantId`.
  Memory and EventStore identities use `TenantBoundary`; Bounded Context names
  no longer enter record-family maps or append/commit locks. The former
  `canonical-scope.ts` and MySQL `scope.ts` modules are deleted.
- RED/GREEN added immutable multitenant MySQL configuration. The builder
  accepts complete typed tenant/database entries, rejects duplicate tenants,
  normalized physical-target aliases, empty registries, and mixed tenancy
  modes before connection, owns one pool per tenant, closes partial builds,
  enumerates its configured tenant catalog, and rejects unknown tenants before
  record handles are opened.
- Entity current storage now consumes the descriptor's real typed `RecordSpec`
  instead of inventing an `"entity"` primitive ID kind. MySQL advisory-lock
  identities contain database, record family, and ID only; context and tenant
  duplication is removed.
- Evidence: MySQL mapping/builder/type slices pass 4 files / 27 tests and the
  storage + MySQL package typechecks. Common boundary/memory tests pass 3 files
  / 21 tests, including cross-Context sharing and tenant isolation.
- The current broad MySQL test baseline is intentionally red at 30 failures /
  55 passes because `mysql-record-storage.test.ts` still supplies the deleted
  `_scope`/`_revision` schema and old `RecordColumn` constructor. Production
  restoration is not involved; that fixture migration is the next action.
