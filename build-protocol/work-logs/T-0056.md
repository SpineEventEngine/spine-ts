# T-0056 Work Log

Status: Reviewed and fully verified; ready to commit

## 2026-07-22 — Start And Assignment Record

- T-0055 is reviewed, integrated, post-merge verified, durably recorded, and
  pushed. Local `main` and `origin/main` both resolved to `55b460b6` before the
  T-0056 branch was created.
- Created isolated branch/worktree `task/T-0056-projection-column-model` /
  `.worktrees/T-0056-projection-column-model` from that exact pushed commit.
- Classified T-0056 high-risk because it creates a public package/generic API,
  establishes descriptor semantics, and owns the cross-adapter normalized
  query capability policy. The accepted T-0052 Wave 1 plan already performed
  the required architecture/public-contract split and review correction, so no
  redundant requirements-splitter dispatch is required.
- Current inspection confirms there is no `packages/client`. Server descriptor
  metadata currently admits Projection and Process Manager column annotations,
  rejects only repeated/map fields, and returns untyped field metadata.
  Storage exposes string-based low-level `RecordQuery`/`RecordColumn`; those
  remain low-level and are not promoted as the end-user DSL.
- Implementation assignment uses the existing `implementer` role, explicitly
  `gpt-5.6-terra` / `medium`. It owns behavior-first tests, the bounded package,
  metadata/policy contracts, guide updates, and focused evidence. It may not
  commit, push, merge, modify unrelated files, or spawn children.
- The Desktop collaboration surface supports explicit child model/reasoning
  dispatch. Runtime self-introspection will be recorded when exposed;
  otherwise the immutable configured profile and limitation will be recorded
  honestly before accepting the result.

## Next Action

Dispatch the one complete required specialist review wave from the main
orchestrator, aggregate findings once, and return any accepted correction batch
to the current implementation context.

## 2026-07-22 — RED, Implementation, And Focused Verification

- Accepted implementation owner metadata: existing `implementer`, explicitly
  dispatched as `gpt-5.6-terra` / `medium`, with no child spawning. The Desktop
  surface did not expose child runtime self-introspection, so the immutable
  configured role/profile is the recorded actual metadata limitation.
- Behavior-first RED was established after refreshing missing generated/build
  prerequisites: both client suites failed on the absent root API, and four
  storage-policy behaviors failed because `StorageQueryPolicy` was absent.
- Added the `@spine-ts/client` package with nominal generated definitions,
  immutable descriptor-backed typed Projection columns, system columns,
  value/operator extraction, registration validation, and weak schema/
  definition caching. The package root omits generated constructors and all
  Aggregate/Process Manager factories.
- Added the package-owned/published
  `protoc-gen-spine-projection-columns` executable and
  `@spine-ts/client/codegen` support subpath. All repository Buf templates run
  the plugin after Protobuf-ES. Actual staged generation emits
  `TaskListColumnDefinition`; the real to-do source imports and registers it.
- Added the canonical normalized storage query plan/capability policy without
  provider execution or a public DSL. It validates predicate shape, nesting,
  comparison/order/mask/limit capabilities, and malformed normalized inputs
  before adapter work.
- Added the package README, Projection-column guide/table, real compile-covered
  snippet, TypeDoc/API expectations, root build reference, lockfile entries,
  example dependencies, and storage policy documentation.
- Fresh final runtime GREEN after corrections: 6 files / 28 tests. The final
  focused coverage gate passed 10 tests at 99.18% statements, 97.02% branches,
  100% functions, and 100% lines.
- Mechanical evidence passed: real staged `proto-workflow generate` with 39
  copied-source checksums and 48 frozen descriptors; no residual `.generated-*`
  staging roots; generated-clean reported ignored/untracked/fresh output; full
  `tsc -b --force`; tooling `tsc --noEmit`; focused ESLint; cleanup rules;
  TypeDoc/API expectations; and release readiness over 16 package imports and
  121 Markdown links.
- Final acceptance rerun passed: generation, build, tooling typecheck, and 6
  focused files / 28 tests; focused V8 coverage at 99.18% statements (121/122),
  97.02% branches (98/101), 100% functions (13/13), and 100% lines (109/109);
  ESLint; cleanup rules; local-bin Prettier format check; TypeDoc/API export
  checks; release readiness; generator syntax; generated-clean; and
  `git diff --check`. Final inspection found no `.generated-*` staging roots.
- One infrastructure limitation was resolved during RED setup: the shared
  pnpm dependency tree required a repository-root repair after an interrupted
  offline supply-chain metadata probe. No dependency versions changed.

## 2026-07-22 — Pre-Review Cleanup Correction

- The independent pre-review gate found that `packages/client/src/codegen.ts`
  violated the repository no-flat-`src` layout rule after generation,
  typechecking, and 20 selected tests had passed.
- Moved the source entry to `packages/client/src/codegen/index.ts` and updated
  the published `@spine-ts/client/codegen` export to
  `dist/codegen/index.{js,d.ts}` plus direct source/build test imports. The
  installable generator binary remains at
  `codegen/generate-projection-columns.mjs`, and the client root still omits
  generated construction helpers.

## 2026-07-22 — Review Correction Batch

- Returned the complete deduplicated review batch to the existing
  `implementer`, explicitly `gpt-5.6-terra` / `medium`, with no child spawning.
  Runtime self-introspection remained unavailable; the immutable configured
  role/profile is the recorded metadata limitation.
- RED proved that emitted JavaScript could directly construct a column, cyclic
  predicates overflowed the call stack, malformed plan/capability shapes leaked
  property-access errors, and no shared descriptor classifier existed.
- Added a private runtime construction token; copied and froze every generated
  entry; constrained generated keys to singular, non-map, non-oneof state
  fields; and retained unsafe-cast runtime tests for stable malformed-definition
  errors.
- Replaced recursive predicate validation with a cycle-safe iterative traversal
  bounded to depth 64 and 10,000 nodes. Runtime validation now checks plan,
  capability, discriminant, array, entry, direction, mask, and limit shapes
  before capability admission.
- Replaced custom-option field numbers and partial wire parsing with option
  extension descriptors supplied by plugin requests. Buf now supplies imports
  to the plugin; non-option request partitions exit without output. Generator
  and registration share the package-owned descriptor classifier, with real
  descriptor parity and malformed-option tests.
- Removed `StorageQueryPolicyApi`, kept the frozen structural policy value, and
  clarified that the guide snippet is
  `examples/todo/src/projection-columns.ts` with imports relative to that file.
- GREEN evidence: real generation verified 39 source checksums and 48 frozen
  descriptor digests; full build and tooling typecheck passed; 6 focused files
  / 35 tests passed. Focused correction coverage passed 21 tests at 96.18%
  statements (252/262), 94.17% branches (194/206), 100% functions (31/31), and
  98.71% lines (231/234).
- Final correction verification passed: ESLint, cleanup enforcement, explicit
  Prettier coverage including the new classifier files, TypeDoc/API expectations
  with 27 storage exports, release readiness over 16 imports and 121 links,
  JavaScript syntax, generated-clean, and `git diff --check`.

## 2026-07-22 — Affected-Lane Follow-Up

- Returned the consolidated four-finding follow-up to the existing
  `implementer`, explicitly `gpt-5.6-terra` / `medium`, without child spawning.
  Runtime self-introspection remained unavailable; the immutable configured
  role/profile is the recorded metadata limitation.
- RED proved that generated entry descriptors remained mutable, sparse IDs were
  accepted, a 10,000-child group accessed/enqueued children before its budget
  rejection, and the generator did not expose a descriptor-derived Projection
  enum number. Four regressions failed while 21 existing focused tests passed.
- Generated definitions now freeze each captured field descriptor before
  exposing it through the immutable entry. Query validation performs a complete
  structural pass before capability admission, checks every array slot, and
  rejects a group that exceeds its remaining node budget before reading or
  enqueueing children. The generator derives the Projection number from the
  request-supplied `EntityOption.kind` enum descriptor.
- GREEN evidence: real generation verified 39 source checksums and 48 frozen
  descriptor digests; full build and tooling typecheck passed; 6 focused files
  / 39 tests passed. Affected coverage passed 25 tests at 96.27% statements
  (284/295), 94.24% branches (213/226), 100% functions (32/32), and 97.69%
  lines (254/260).
- Final follow-up gates passed: ESLint, cleanup enforcement, explicit Prettier,
  TypeDoc/API expectations, release readiness, generated-clean, and
  `git diff --check`. Documentation verification retained the corrected guide
  wording that separates real source lines from illustrative property access.

## 2026-07-22 — Final Reliability Correction

- Returned the final descriptor-immutability P2 to the existing `implementer`,
  explicitly `gpt-5.6-terra` / `medium`, without child spawning. Runtime
  self-introspection remained unavailable; the immutable configured
  role/profile is the recorded metadata limitation.
- RED proved that a generated entry's reachable message descriptor remained
  mutable before registration while nine existing projection tests passed.
- Generated definitions now apply a cycle-safe freeze to the reachable
  descriptor graph. Non-freezable binary views are not frozen; instead,
  module-private immutable facts capture schema kind, column annotation, field
  identity/name, and classification before exposure, and registration consumes
  those facts rather than mutable nested option data.
- Focused GREEN passed tooling typecheck and 20 client/generator tests, including
  nested message mutation before registration and nested options mutation after
  registration.
- Final reliability verification passed real generation with 39 source
  checksums and 48 frozen descriptor digests, full build/tooling typecheck, and
  6 focused files / 40 tests. ESLint, cleanup enforcement, explicit Prettier,
  generated-clean, and `git diff --check` also passed.

## 2026-07-22 — Review Closure

- TypeScript/API, documentation, style/maintainability, and
  performance/reliability all completed clean final re-reviews after their
  accepted findings were corrected. Security remains concretely N/A because
  this packet adds no external trust boundary or query executor.
- Reviewer runtime self-introspection was unavailable. The immutable configured
  profiles were recorded in the review file; no visible role/model/reasoning
  mismatch occurred. Desktop cumulative thread limits required the documented
  repository-aware read-only CLI fallback for documentation and style lanes.
- All canonical concerns are closed. The next action is the independent full
  branch verification gate, followed by commit/push, merge/push, post-merge
  verification, durable closure, and automatic continuation to T-0057.

## 2026-07-22 — Final Branch Verification

- `pnpm --config.verify-deps-before-run=false verify` passed from the isolated
  T-0056 worktree with native Node IPC enabled.
- Generation/provenance verified 39 copied source checksums and 48 frozen
  descriptor digests. Typecheck, ESLint, cleanup, and repository formatting
  passed.
- Runtime tests passed 91 files / 1,961 tests, with 3 files / 21 tests skipped
  by their existing environment gates. Coverage passed at 94.49% statements,
  90.16% branches, 94.76% functions, and 94.57% lines.
- TypeDoc/API checks passed for all eight package entry-point groups, including
  10 `@spine-ts/client` and 27 `@spine-ts/storage` exports. Proto lint,
  generated-clean, and release readiness also passed; release readiness checked
  16 package imports and 121 relative Markdown links.
- T-0056 is reviewed and independently verified. Next action: remove the
  verification-only worktree dependency symlink, commit, and immediately push
  the task branch before integration.
