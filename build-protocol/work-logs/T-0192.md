# T-0192 Work Log

## 2026-08-15 - Start

- Blocked on T-0191's exact provider-atomic removal contract. Lifecycle work
  will begin test-first after that contract has passing focused evidence.

## 2026-08-15 - Lifecycle RED/GREEN

- RED: the direct worker test expected a delivered cleanup read after one
  delivery page and again on the following empty owned drain; baseline made
  zero cleanup reads.
- GREEN: owned drains now call the optional persistence capability for one
  bounded delivered page after each delivery page and once at an empty drain.
  Custom/remote ports that omit the optional method retain their own behavior.
- Focused direct Inbox and worker suites passed 30/30. Provider registration,
  live-provider proof, and complete cancellation/failover matrix remain open.

## 2026-08-15 - Scoped coverage status

- Focused direct Inbox and worker suites subsequently passed 32/32, including
  cancellation after a removal, deadline propagation, and custom-port omission.
- The prior task-profile coverage session did not retain a final result. A
  bounded direct V8 run produced current LCOV for `delivery.ts`: 118/133 lines
  and 88/124 branches (88.7%/71.0%), below the required 90% threshold. This is
  incomplete evidence, not an acceptance claim; additional focused lifecycle
  branch coverage and a completed scoped profile remain required.
- Provider-only gap remains Datastore/MySQL coordinator registration and live
  proof, pending the orchestrator's explicit ownership handoff.

## 2026-08-15 - Coverage ownership transfer

- The initial implementer became inactive after pushing `a39d0e3b`; generated
  Proto marker/manifest differences are unstaged command residue and remain
  outside task ownership.
- A replacement existing implementer is explicitly dispatched as
  `gpt-5.6-terra` / medium, without subagents, to close meaningful changed-line
  and changed-branch coverage only. Provider ownership remains with T-0190.

## 2026-08-15 - Lifecycle coverage convergence

- Added focused worker behavior for cancellation after the bounded delivered
  read but before deletion, removal of every row returned by one bounded
  maintenance page, custom-port omission, and ownership loss immediately after
  a delivery page but before its maintenance pass. Existing release-failure and
  deadline-forwarding cases remain part of the same focused suite.
- `pnpm exec vitest run packages/server/test/delivery/delivery-worker.test.ts`
  passed 25/25. A V8 LCOV run covered every changed executable line and both
  outcomes of every changed `delivery.ts` branch: 11/11 lines and 16/16 branch
  outcomes (100%/100%) for lines 288-301, 325, and 379 relative to
  `e2ab42d2`.
- The direct one-file coverage command exits nonzero only because repository
  global 90% thresholds include unexecuted packages; it is not the scoped
  changed-branch gate. Provider registration and live MySQL/Datastore proof
  remain pending the orchestrator's explicit T-0190 handoff.

## 2026-08-15 - Provider cleanup checkpoint

- Provider registration now exists behind the optional direct Inbox removal
  port: MySQL and Datastore own one exact two-record-family cleanup mutation,
  while memory retains its existing critical section. Worker lifecycle behavior
  and page bounds are unchanged in this checkpoint.
- The focused worker suite remains part of the 82 passing local tests. Service
  gated MySQL/Datastore direct-Inbox cleanup tests are present but intentionally
  skipped pending the explicitly requested exclusive live-provider windows.
  Sustained lifecycle, cancellation, restart, duplicate, and two-owner local
  coverage remains to be consolidated with provider live evidence and the
  canonical task profile.

## 2026-08-15 - Mechanical TSDoc and evidence convergence

- T-0191/T-0192 runtime behavior is frozen. The replacement existing
  `implementer` is explicitly configured as `gpt-5.6-terra` / medium with no
  subagents; runtime telemetry is unavailable on this surface.
- The current six-path focused run completed with 40 deterministic passes and
  two service-gated skips. The skipped cases require `SPINE_TS_MYSQL_URL` and
  `DATASTORE_EMULATOR_HOST`, neither of which is configured, so live provider,
  independent-handle, and physical-row-count proof remains a recorded
  limitation.
- The combined changed-runtime coverage from current traces is 112/115 lines
  (97.39%) and 69/75 branches (92.00%); the T-0192 worker-only slice remains
  11/11 lines and 16/16 branches. The next action is mechanical validation and
  the selected `verify:task -- --no-coverage` profile, not a behavior change.

## 2026-08-15 - Final task-profile limitation

- The selected no-coverage task profile passed Node, frozen Proto generation,
  build, tooling typecheck, cleanup, and TSDoc before failing in the shared
  copyright scan. It did not reach the profile format or focused-test stages.
- The seven reported malformed or missing provider/test headers are owned by
  T-0191/T-0192. The earlier out-of-scope classification is corrected: exact
  `copyrightHeader(2026)` repairs are in scope and leave frozen bounded-cleanup
  behavior unchanged. Canonical profile rerun remains pending.

## 2026-08-15 - Header correction and format convergence

- The seven owned headers now match `copyrightHeader(2026)` exactly. The
  focused selection remains 40 passing deterministic tests with two explicit
  live-provider skips.
- The post-header profile cleared copyright and all preceding gates, then found
  one Prettier-only mismatch in the changed shard-registry source. It is now
  normalized; full format passes and canonical profile rerun remains pending.

## 2026-08-15 - Canonical task-profile completion

- The exact six-path no-coverage profile passed all shared deterministic gates
  and focused tests: 40 passed, two expected service-gated skips. It includes
  copyright, full format, TypeDoc/API, generated-output, and release-readiness
  validation after Node/Proto, build, tooling, cleanup, TSDoc, and log checks.
- The task is clean and review-ready. Header template and shard-registry
  Prettier corrections do not alter bounded cleanup behavior; live MySQL and
  Datastore evidence remains unavailable with unset endpoints.

## 2026-08-15 - Accepted review-correction RED/GREEN

- Observed RED demonstrated the protected full-page progress failure and the
  false-delete/ownership-loss `DRAINED` outcome. GREEN uses one bounded ordered
  continuation only when a full cleanup page made no removal, and stops after a
  refused deletion when current ownership cannot be revalidated.
- Direct removal now honors aborted and expired operation preflight and carries
  operation state into provider cleanup coordinators. Five focused deterministic
  suites pass 43/43; serialized live two-owner and row-count proof remains
  pending orchestrator-granted provider windows.

## 2026-08-15 - MySQL exclusive-provider evidence

- The serialized MySQL 8.4.10 focused direct-provider command passed 2/2
  (Datastore skipped). It uses independently opened factories and verifies the
  physical direct-row state before and after replacement-owner cleanup, while
  refusing the stale owner's delete. The MySQL window is released before any
  Datastore request.

## 2026-08-15 - Datastore exclusive-provider evidence

- The separate Datastore emulator acceptance passed 2/2 (two MySQL cases
  skipped) under the granted window. Two independently opened factories prove
  stale-owner refusal plus direct-row presence before, and absence after,
  replacement-owner exact deletion. The window is released before general
  verification.
