# T-0074 Work Log

## 2026-07-27 — Wave 4 Q&A closure

- Baseline: `origin/main@8a627719`, the verified Wave 3 endpoint.
- Branch/worktree:
  `task/T-0074-wave4-browser-interoperability-plan` /
  `.worktrees/T-0074-wave4-browser-interoperability-plan`.
- The primary checkout is dirty with unrelated user work and remains untouched.
- The human completed every product decision needed for Wave 4 planning and
  requested durable documentation of all decisions, diagrams, limitations,
  extension points, and Google/GitHub/third-party authentication flows.
- Classification: high-risk because the plan freezes public package,
  browser/runtime, credential/session, serialized request-rewriting,
  cross-runtime, and deployment-trust contracts.
- No production implementation is authorized by this planning task.

## Baseline verification

- `pnpm install --frozen-lockfile` passed with 309 packages reused and no
  lockfile change. Clean-checkout pre-build warnings identified missing
  generated `proto-tools/dist` CLI links and were resolved by the ordinary
  generation/build path.
- A restricted-sandbox test attempt compiled successfully but failed when
  loopback listeners and ZeroMQ IPC returned `EPERM`; 116 files / 2,447 tests
  passed and 24 network/IPC-dependent files failed. This is not accepted as
  repository evidence.
- The identical elevated native baseline passed: 140 files / 2,638 tests, with
  3 files / 25 tests skipped.

## Planning validation assignment

- Existing role: `requirements_splitter`.
- Bounded scope: read-only validation of the frozen Wave 4 plan against the
  human ledger, package/dependency order, Wave 5/6 boundaries, and
  security-sensitive assumptions. Report only blocking contradictions or
  missing decisions.
- Expected model: `gpt-5.6-sol`.
- Expected reasoning: `high`.
- Both fields will be explicit in dispatch. The child may not edit, commit,
  push, merge, or spawn children.
- Runtime metadata limitation: the Desktop surface exposes the explicit
  dispatch and immutable configured role/profile but not independent child
  self-introspection. This is acceptable under the protocol.

## Reader-test assignment

- Pending after requirements validation and any deterministic corrections.
- Use a fresh-context read-only agent with the document alone and the
  reader-test questions recorded in the task.

## Planning validation result

- Existing role: `requirements_splitter`.
- Expected and explicitly dispatched profile: `gpt-5.6-sol` / `high`.
- Actual immutable configured role/profile matches. Runtime
  self-introspection was unavailable, as anticipated and permitted by the
  recorded metadata limitation.
- Result: three blocking plan contradictions, with no missing human product
  decision, Wave 5/6 scope leakage, or overstated subscription guarantee.
- Corrected deterministically:
  1. reject stale or contradictory client actor/tenant before forwarding, then
     replace a matching object with a fresh trusted `ActorContext`;
  2. authorize `Subscribe`, `Activate`, and `Cancel` independently, with
     activation reauthorizing stream establishment;
  3. freeze the JVM fixture in implementation preflight before Slice A, and
     make Slice E consume the frozen source.
- The corrections only reconcile approved intent and dependency order; they do
  not reopen product scope.
- Focused re-review result: CLEAN. The same existing requirements splitter
  confirmed all three findings resolved. Expected and explicitly dispatched
  `gpt-5.6-sol` / `high` matches the immutable configured profile; independent
  runtime self-introspection remains unavailable.

## Fresh-reader documentation assignment

- Existing role: `documentation_reviewer`.
- Bounded scope: read only
  `build-protocol/planning/WAVE_4_BROWSER_CLIENT_INTEROPERABILITY_PLAN.md` and
  answer the task's reader questions without conversation or other repository
  history; report ambiguity, contradiction, or missing assumed knowledge.
- Expected model: `gpt-5.6-luna`.
- Expected reasoning: `medium`.
- Both fields will be explicit in dispatch. The reader may not edit, commit,
  push, merge, browse other sources, or spawn children.
- Runtime metadata limitation: explicit dispatch and immutable configured
  role/profile are available; independent child self-introspection is not.
- The explicit `documentation_reviewer` / `gpt-5.6-luna` / `medium` dispatch
  was rejected before execution because the current collaboration call accepts
  explicit Sol and Terra model values only. No result was produced or accepted.
- Redispatch is a fresh-context, read-only requirements-comprehension function
  using the existing `requirements_splitter` role, expected
  `gpt-5.6-sol` / `high`, with both fields explicit. This is not the canonical
  documentation review lane; it is the doc-coauthoring reader test for an
  architecture-significant plan. It may read only the Wave 4 plan and may not
  edit or spawn children.

## Fresh-reader result

- Existing role and expected/explicitly dispatched profile:
  `requirements_splitter`, `gpt-5.6-sol` / `high`.
- The immutable configured profile matches; independent runtime
  self-introspection remains unavailable.
- The reader reconstructed all seven requested package, gateway, third-party
  sign-in, session/context, subscription, Chat, wave-boundary, and extension
  answers from the plan alone. It found no contradiction or unresolved product
  decision.
- One documentation seam was confirmed: identity mapping produced an
  application actor/tenant before session creation, but the illustrative
  `SessionManager.create()` accepted only `ExternalIdentity`.
- Corrected the contract so `IdentityMapping` yields
  `ResolvedApplicationIdentity` and `SessionManager.create()` accepts that
  principal/actor/default-tenant result. Clarified that per-request policy
  still checks tenant and stale mapping.
- Also clarified that CSRF behavior, signed-token algorithm/key lifecycle, and
  protected subscription ownership are Slice B/C security designs that must
  freeze before release without reopening approved behavior unnecessarily.
- Focused reader re-review: CLEAN. The same explicit
  `requirements_splitter` / `gpt-5.6-sol` / `high` profile confirmed the
  identity/session seam and security-decision boundary are now unambiguous;
  independent self-introspection remains unavailable.

## Focused review wave

- TypeScript/API concern: existing `typescript_api_docs_reviewer`; expected
  `gpt-5.6-terra` / `high`; both fields explicit. Read-only review of public
  package boundaries, illustrative declarations, naming, compatibility, and
  whether the plan accidentally freezes contradictory API shapes.
- Performance/reliability concern: existing
  `performance_reliability_reviewer`; expected `gpt-5.6-terra` / `high`; both
  fields explicit. Read-only review of reconnect, buffering, session/token,
  gateway streaming, and resource/lifecycle claims.
- Both reviewers may read only the Wave 4 plan plus the T-0074 task and may not
  edit, commit, push, merge, browse, or spawn children. The Desktop surface
  exposes explicit dispatch and immutable configured profiles but not
  independent runtime self-introspection.
- Style/maintainability disposition: N/A for specialist dispatch because this
  planning-only task changes no production code, package graph, or executable
  structure; requirements validation covered plan decomposition and all
  changed Markdown passes Prettier and `git diff --check`.
- Documentation disposition: covered by the doc-coauthoring fresh-reader test.
  The explicit Luna documentation-review dispatch was unavailable and produced
  no result; the clean fresh reader proved agent comprehension from the plan
  alone. Canonical package/end-user documentation review remains required for
  every implementation slice that changes public behavior.
- Final security reviewer: N/A for this planning-only record. The plan makes a
  mandatory final Wave 4 security gate explicit; invoking release-readiness
  security review before code, dependencies, and deployment fixtures exist
  would not establish release evidence.

## Focused review results and disposition

- TypeScript/API result: three accepted findings. Existing role and
  expected/explicitly dispatched profile:
  `typescript_api_docs_reviewer`, `gpt-5.6-terra` / `high`; immutable configured
  profile matches and independent runtime self-introspection is unavailable.
- Performance/reliability result: three accepted findings. Existing role and
  expected/explicitly dispatched profile:
  `performance_reliability_reviewer`, `gpt-5.6-terra` / `high`; immutable
  configured profile matches and independent runtime self-introspection is
  unavailable.
- One combined correction batch:
  1. defined a terminal per-subscription generation, one reconnect pipeline,
     finite retry exhaustion, timer cancellation, and late-result disposal;
  2. required bounded message/byte relay queues, backpressure, bidirectional
     terminal propagation, and idempotent backend cleanup;
  3. replaced render-unsafe TSX with explicitly labelled lifecycle pseudocode
     and required compile-checked Slice D APIs;
  4. made `client-react` explicitly browser-capable, dependent on the public
     `client-web` contract, and free of Node-only imports;
  5. split opaque-session and signed-token strategy contracts so only opaque
     sessions promise intrinsic immediate revocation;
  6. added focused race, exhaustion, blocked-consumer, disconnect, backend
     error, and retained-resource acceptance tests.
- Re-review is required only for the two affected API and reliability concerns.
- TypeScript/API focused re-review: CLEAN. Existing role and
  expected/explicitly dispatched `gpt-5.6-terra` / `high` profile match;
  runtime self-introspection remains unavailable.
- Performance/reliability focused re-review: CLEAN. Existing role and
  expected/explicitly dispatched `gpt-5.6-terra` / `high` profile match;
  runtime self-introspection remains unavailable.
- Complete review wave disposition: no open finding. Requirements,
  TypeScript/API, performance/reliability, and fresh-reader concerns are clean;
  planning-only N/A dispositions remain as recorded above.

## Final verification

- Passed: Prettier check for all six changed protocol/documentation records.
- Passed: `git diff --check`.
- Passed: `pnpm --config.verify-deps-before-run=false docs:check:generated`;
  TypeDoc generation and API inventory are clean.
- The first ordinary full native attempt passed 138 files / 2,636 tests and
  failed two unchanged integration tests: one client subscription assertion
  observed an earlier state update, and one multi-process delivery fixture
  timed out during readiness.
- Both files then passed in isolation: 2 files / 6 tests.
- A second ordinary full attempt passed 139 files / 2,637 tests and failed a
  different test in the same multi-process fixture because readiness timed out
  before its deliberate post-readiness failure. The varying failure plus clean
  isolated result identified cross-suite worker contention against a fixed
  readiness budget; no tracked source was changed.
- Definitive bounded-concurrency native gate passed:
  `pnpm --config.verify-deps-before-run=false test:generated --
--maxWorkers=4` — 140 files / 2,638 tests, with 3 files / 25 tests skipped.
- Only the intended Wave 4 planning, decision, status, question, task, and work
  records remain changed. No production implementation is included.
