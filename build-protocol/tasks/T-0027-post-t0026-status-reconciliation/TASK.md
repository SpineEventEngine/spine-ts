# T-0027: Post-T-0026 Runtime Status Reconciliation

Status: Round 1 wording fix verified; re-review pending
Started: `2026-07-11T04:42:44Z`
Baseline commit: `efbf379a`
Branch: `task/T-0027-post-t0026-status-reconciliation`
Worktree: `.worktrees/T-0027-post-t0026-status-reconciliation`

## Objective

Make active roadmap, runtime, user, and package docs truthful after T-0026
landed a narrow framework-owned delivery worker/loop boundary, without changing
runtime behavior.

## Human-Imposed Requirements Ledger

- Continue autonomously until all tasks are done or a real blocker appears.
- Use one feature branch and worktree per task or sub-task.
- Spawn a requirements-splitting sub-agent before implementation.
- Use one implementation sub-agent for this task.
- Run independent reviewer sub-agents for code style/maintainability,
  documentation, TypeScript/API docs, security, and performance/reliability.
- Feed reviewer comments back to an authoring/fix sub-agent and repeat until
  all lanes are clean.
- Close every participating sub-agent once its role is complete.
- Do not modify or rely on `human-review-1-jul.md`.
- No change may be made without updating the relevant durable log before or in
  the same atomic work step.
- Keep generated output out of VCS and regenerated by build steps.
- Preserve Spine Protobuf contracts, type URLs, options, and modeling
  conventions.
- Keep end-user code free of framework `Event` envelopes, manual transactions,
  `@Apply`, schema-bearing decorators, and app-owned handler materialization.
- Aggregate import/importers, `ImportBus`, and aggregate `@Apply` delivery are
  removed from the active roadmap.
- `IMPORT_EVENT` is not a supported public delivery label for new inbox writes;
  legacy stored rows are deprecated compatibility data that fail closed.
- For server-module work, inspect relevant Spine JVM `core-jvm/server` docs and
  source before design or implementation, and avoid over-engineering.
- T-0027 is docs/status-only: do not change runtime source, tests, generated
  output, storage indexes, retry policy, worker supervision, ZeroMQ topology, or
  catch-up semantics.

## Splitter Result

Requirements splitter `019f4f79-71d3-7a92-8ac3-7e8555bf5980` recommended
`T-0027: Post-T-0026 Runtime Status Reconciliation` as the smallest safe next
slice. T-0026 landed durable inbox rows, shard leases, bounded validated local
replay, and a framework-owned worker/loop boundary for supported labels, but
active docs still contain broad remaining-gap language around transport-backed
workers and production worker execution.

The splitter recommended a docs/status reconciliation before runtime coding so
the next implementation task does not start from stale public status language.
The likely next implementation candidate after reconciliation is
storage-index/keyset continuation for durable scans, but that is out of scope
for this task.

## Scope

- Reconcile active docs after T-0026.
- Distinguish what now exists:
  - durable inbox rows;
  - shard leases and per-message claims;
  - bounded validated local replay;
  - a framework-owned worker/loop boundary for supported labels.
- Keep remaining production gaps explicit:
  - retry monitors/workers;
  - retained attempt history;
  - transport topology;
  - broker/process supervision;
  - production delivery policy;
  - durable catch-up storage and projection catch-up through inbox storage;
  - production storage adapters;
  - broader system/runtime hardening.
- Preserve `CATCH_UP` as valid durable label data that is worker-unsupported for
  replay callbacks and remains pending.
- Preserve `IMPORT_EVENT` as unsupported for new writes and fail-closed for
  legacy stored rows.
- Record a decision-log entry selecting this reconciliation before the next
  implementation slice.

## Out Of Scope

- Runtime/source/test changes.
- Storage index/keyset continuation.
- Retry monitors, retained delivery attempts, and production retry policy.
- ZeroMQ endpoint topology and broker supervision.
- Catch-up semantics beyond existing `BoundedContext.catchUpReadSide()`.
- `ImportBus`, aggregate importers, aggregate `@Apply`, or event import.
- Generated API reference hand edits.

## Likely Files

- `build-protocol/DECISION_LOG.md`
- `build-protocol/tasks/T-0027-post-t0026-status-reconciliation/TASK.md`
- `build-protocol/work-logs/T-0027.md`
- `build-protocol/reviews/T-0027-post-t0026-status-reconciliation.md`
- `docs/architecture/README.md`
- `docs/USER_GUIDE.md`
- `packages/server/README.md`
- `build-protocol/RUNTIME_ARCHITECTURE.md` if active delivery status needs
  tightening.

## JVM Inspection

This task is docs/status-only and does not create new delivery parity claims.
It reuses the T-0026 inspection record for delivery concepts unless edits add
new parity language.

If delivery-parity wording must change, inspect:

- `spine-jvm-docs/spine-routing-dispatch-and-delivery.md`;
- `server/src/main/java/io/spine/server/delivery/Delivery.java`;
- `server/src/main/java/io/spine/server/delivery/Inbox.java`;
- `server/src/main/java/io/spine/server/delivery/ShardedWorkRegistry.java`;
- `server/src/main/java/io/spine/server/delivery/LiveDeliveryStation.java`;
- `server/src/main/java/io/spine/server/delivery/TargetDelivery.java`.

## Skill Applicability

- Selected workflow skills already active in this session:
  `subagent-driven-development`, `using-git-worktrees`,
  `requesting-code-review`, and `verification-before-completion`.
- Additional selected skills for this docs/status task:
  `doc-coauthoring` and `architecture-decision-records`.
- Repo-local expected-skill manifest checked:
  `build-protocol/skills/EXPECTED_SKILLS.md`.
- User-installed skill entrypoints checked with
  `find /Users/armiol/.agents/skills -maxdepth 2 -type f -name SKILL.md -print`.
  Task-relevant entries included the selected workflow skills plus
  `doc-coauthoring`, `architecture-decision-records`, and `codebase-design`.
- Installed-skill lock checked with targeted `rg` over
  `/Users/armiol/.agents/.skill-lock.json`; task-relevant hits included
  `subagent-driven-development`, `using-git-worktrees`,
  `requesting-code-review`, `verification-before-completion`,
  `doc-coauthoring`, `architecture-decision-records`, and `codebase-design`.
- Skipped relevant-looking skills: `codebase-design` because this task changes
  docs/status language only and does not redesign module boundaries;
  `typescript-advanced-types` and `nodejs-backend-patterns` because no
  TypeScript/runtime implementation is in scope.

## Acceptance Criteria

- No active doc says supported delivery workers or equivalent worker execution
  are wholly missing after T-0026.
- Remaining-gap lists separate the local framework-owned worker/loop boundary
  that exists from production supervision/topology/retry policy that remains.
- `CATCH_UP` is described as valid durable label data but worker-unsupported for
  replay callbacks.
- `IMPORT_EVENT` remains unsupported for new writes and legacy stored rows fail
  closed.
- Active roadmap language does not resurrect aggregate import/importers,
  `ImportBus`, or aggregate `@Apply` delivery.
- No source, test, generated output, or `human-review-1-jul.md` changes.

## Verification Plan

- Targeted `rg` checks for stale phrases:
  - supported delivery workers wholly missing;
  - overbroad transport-backed worker gaps;
  - `aggregate import`, `ImportBus`, and aggregate `@Apply` active-roadmap
    regressions.
- `pnpm --config.verify-deps-before-run=false docs:check`.
- `pnpm --config.verify-deps-before-run=false format:check`.
- `git diff --check`.

## Implementation Notes

- Updated `docs/architecture/README.md` to distinguish the existing
  framework-owned local delivery worker/loop boundary, durable inbox rows,
  shard leases, per-message claims, and bounded replay from production
  transport topology, supervision, retry, catch-up, storage-adapter, and
  delivery-policy gaps.
- Updated `docs/USER_GUIDE.md` and `packages/server/README.md` to remove stale
  wording that implied worker execution or supported catch-up work was wholly
  missing, while preserving `CATCH_UP` as valid durable label data that remains
  pending/skipped and `IMPORT_EVENT` as unsupported/fail-closed compatibility
  data.
- Left runtime source, tests, generated API reference output, and
  `human-review-1-jul.md` untouched.

## Verification Results

- `rg -n "supported delivery workers|worker execution missing|supported catch-up work|process-wide transport-backed delivery workers|production transport-backed worker execution|wholly missing" docs/architecture/README.md docs/USER_GUIDE.md packages/server/README.md build-protocol/RUNTIME_ARCHITECTURE.md build-protocol/DEVELOPER_API.md`
  exited `1` with no matches.
- `rg -n "ImportBus.*pending runtime work|aggregate importers? .*future runtime work|aggregate @Apply.*active|@Apply delivery.*active roadmap|aggregate import/importers.*active roadmap" docs/architecture/README.md docs/USER_GUIDE.md packages/server/README.md build-protocol/RUNTIME_ARCHITECTURE.md build-protocol/DEVELOPER_API.md`
  exited `1` with no matches.
- `rg -n 'CATCH_UP.*(remain|stays) pending|IMPORT_EVENT.*(rejected|fail closed|unsupported|corruption)' docs/architecture/README.md docs/USER_GUIDE.md packages/server/README.md build-protocol/RUNTIME_ARCHITECTURE.md build-protocol/DEVELOPER_API.md`
  exited `0` with expected confirming matches.
- `pnpm install --store-dir /Users/armiol/development/experiments/spine-ts/.pnpm-store`
  was needed because this worktree had no `node_modules` and the repo-local
  store was missing `@bufbuild/protoc-gen-es`.
- Initial `pnpm --config.verify-deps-before-run=false docs:check` failed before
  validation because `buf`/`node_modules` were missing; after install, it failed
  once because TypeDoc needed composite package build outputs in the fresh
  worktree.
- `pnpm --config.verify-deps-before-run=false typecheck:build:generated` exited
  `0` and produced the needed build outputs.
- Retried `pnpm --config.verify-deps-before-run=false docs:check` exited `0`;
  TypeDoc reported `0` errors and one invalid-origin source-link warning.
- `pnpm --config.verify-deps-before-run=false format:check` exited `0`.
- `git diff --check` exited `0`.

Coordinator verification on `2026-07-11T04:53:48Z` repeated the targeted
stale-worker and import-roadmap `rg` checks, both of which exited `1` with no
matches; repeated the `CATCH_UP`/`IMPORT_EVENT` confirmation `rg`, which exited
`0` with expected matches; and reran `docs:check`, `format:check`, and
`git diff --check`, all passing. `git status --short` showed no tracked
generated-output or dependency churn.

Round 1 review on `2026-07-11T05:06:12Z`: documentation, TypeScript/API docs,
security, and performance/reliability were clean. Code style/maintainability
found one P3 stale-status wording issue in `docs/USER_GUIDE.md`: the phrase
"transport-backed/background delivery worker orchestration" lacked the
"production" qualifier used elsewhere and could imply the T-0026 local
worker/loop boundary is still absent. The fix will narrow that phrase to
production topology/supervision wording, verify, commit, and rerun all five
review lanes.

Round 1 wording fix on `2026-07-11T05:10:22Z`: updated the reported
`docs/USER_GUIDE.md` phrase to "production transport-backed/background worker
topology and supervision." Verification passed `docs:check`, `format:check`,
and `git diff --check`. Targeted stale wording checks found no unqualified
worker-missing claims in the reported section; the remaining
"production transport-backed/background delivery worker orchestration and
supervision" wording in the earlier production-gap list is intentionally
qualified as production work.

## Review Plan

Run five independent review lanes:

- code style/maintainability: docs structure and no duplicated/conflicting
  status language;
- documentation: all public/runtime docs agree;
- TypeScript/API docs: no API inventory drift or generated-doc hand edits;
- security: no weakening of IPC/trust-boundary language;
- performance/reliability: remaining production gaps and T-0026 worker limits
  are accurately stated.
