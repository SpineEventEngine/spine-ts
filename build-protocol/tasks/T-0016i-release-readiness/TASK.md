# T-0016i: Release Readiness Audit

Status: in progress
Started: `2026-07-08`
Branch: `task/T-0016i-release-readiness`
Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0016i-release-readiness`
Base commit: `4430c9b`

## Objective

Close the T-0016 framework-readiness roadmap with an audit that proves the
framework and to-do example are in a coherent, reviewable state, or records
explicit follow-up tasks for any deferred release-readiness gaps.

## Human-Imposed Requirements Ledger

- Continue autonomously until all tasks are done or a real blocker appears.
- Keep `human-review-1-jul.md` untouched.
- Use one branch and one worktree per task.
- Use one implementation sub-agent for this task and independent reviewer
  sub-agents for code style/maintainability, documentation, TypeScript/API docs,
  security, and performance/reliability.
- Close every participating sub-agent once its role is complete.
- No change may be made without updating the relevant durable log.
- Preserve Protobuf contracts and generated-code policy.
- Generated output must remain ignored and regenerated by builds.
- Ordinary end-user code must not use schema-bearing decorators, `@Apply`,
  framework `Event` envelopes, internal event IDs, manual transactions,
  generated handler materialization, or default command target validation.
- Public docs and TypeScript/API docs must reflect the actual supported API.
- Coverage target is at least 90%.
- Server-module runtime/API code changes require close inspection of the
  relevant Spine JVM `core-jvm/server` code before design or implementation.

## Acceptance Criteria

- `pnpm verify`, generated-clean, cleanup guard, API docs, public docs, and
  forbidden end-user API scans pass.
- Stale deferred wording is removed or linked to explicit follow-up tasks.
- Public legacy compatibility exports such as `@Apply` and
  `defineEntityHandlers()` receive an explicit outcome: remove them, restrict
  them to framework/testing internals, or document their narrow legacy scope in
  TypeScript/API docs and user guides.
- Any discovered release-readiness gaps are either fixed in this task or
  recorded as explicit follow-up tasks with durable rationale.

## Skill Applicability Check

- Session skill inventory exposes relevant workflow and quality skills:
  `subagent-driven-development`, `using-git-worktrees`,
  `verification-before-completion`, `requesting-code-review`,
  `planning-with-files`, `architecture-decision-records`,
  `typescript-advanced-types`, and `nodejs-backend-patterns`.
- Selected skills for orchestration: `subagent-driven-development`,
  `using-git-worktrees`, and `verification-before-completion`; their
  instructions were read earlier in this root session before task actions.
- Repo expected-skill manifest checked:
  `build-protocol/skills/EXPECTED_SKILLS.md`.
- User-installed skill entrypoints checked with
  `find /Users/armiol/.agents/skills -maxdepth 2 -type f -name SKILL.md -print`;
  expected workflow, review, planning, TypeScript, and Node skills were present.
- Installed-skill lock manifest checked with targeted `rg` over
  `/Users/armiol/.agents/.skill-lock.json`; expected workflow, planning,
  architecture, TypeScript, and Node skill entries were present.
- No server runtime/API edits are planned at task start, so Spine JVM inspection
  is not required unless the audit discovers a server-code change.

## Planned Audit Steps

1. Run stale-pattern scans for deferred wording and forbidden end-user API
   references in public docs and example code.
2. Inspect public exports for legacy compatibility symbols, especially `@Apply`
   and `defineEntityHandlers()`.
3. Fix documentation, scripts, or cleanup guards for narrow release-readiness
   gaps found by the audit.
4. Run focused verification and full `pnpm verify`.
5. Complete the required review loop until all lanes are clean.

## Completion Notes

Pending.
