# T-0017b: Query Execution Over Read-Side Columns

Status: complete, integrated
Started: `2026-07-08`
Branch: `task/T-0017b-query-execution`
Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0017b-query-execution`
Base commit: `8dd37ca`

## Objective

Execute supported read-side filters, field masks, ordering, and limits over
`Stand` / query-service routes instead of relying only on example-level query
readiness.

## Scope

- Production parity/runtime completeness task.
- Depends on completed `T-0017a` read-side catch-up.
- Keep implementation small and JVM-familiar.
- Do not introduce broad query-language machinery, durable subscription
  recovery, event subscriptions, Delivery scheduling, process managers, or new
  transport topology.

## Human-Imposed Requirements Ledger

- Continue autonomously until all tasks are done or a real blocker appears.
- Keep `human-review-1-jul.md` untouched.
- Use this branch/worktree for this task.
- Spawn one implementation sub-agent for the task.
- Run independent reviewer sub-agents for code style/maintainability,
  documentation, TypeScript/API docs, security, and performance/reliability.
- Feed reviewer comments back to an authoring/fix sub-agent and repeat until
  all lanes are clean.
- Close every participating sub-agent once its role is complete.
- No change may be made without updating the relevant durable log.
- Preserve Protobuf contracts, generated-code policy, and generated registry
  contracts.
- Keep generated output ignored and regenerated by builds.
- Keep end-user code free of framework `Event` envelopes, manual transactions,
  schema-bearing decorators, `@Apply`, and application-owned handler
  materialization.
- Server-module implementation requires close inspection of local Spine JVM
  docs and corresponding `core-jvm/server` sources before design or code
  changes.

## Required Research Before Implementation

- `spine-jvm-docs/spine-client-api-queries-subscriptions-and-tests.md`
- `spine-jvm-docs/spine-server-runtime-and-bounded-context.md`
- Relevant local `core-jvm/server` sources for `QueryService`, `Stand`,
  entity query handling, record filters, ordering, masks, limits, and lifecycle
  filters.
- Current TS surfaces:
  - `packages/server/src/stand/stand.ts`
  - `packages/server/src/services/spine-services.ts`
  - `packages/storage/src/record/record-query.ts`
  - `packages/storage/src/record/record-column.ts`
  - `packages/storage/src/record/record-mask.ts`
  - existing query service and example query tests.

## Acceptance Criteria

- Read-side queries apply supported filters, masks, ordering, and limits over
  registered read-side routes.
- Query execution keeps strict read-side/write-side segregation.
- Single-tenant and multitenant query behavior remains tenant-scoped.
- Invalid or unsupported queries fail with small deterministic diagnostics.
- Public docs and TypeScript/API docs describe the supported query boundary.
- T-0017b logs record JVM research, implementation decisions, verification, and
  all agent participation.

## Verification Plan

- Focused query-service, `Stand`, and storage query tests.
- Black-box fixture query tests where the existing fixtures support the slice.
- `pnpm format:check`
- `pnpm lint`
- `pnpm docs:check`
- `pnpm proto:check-generated`
- `pnpm --config.verify-deps-before-run=false verify`
- `git diff --check`
