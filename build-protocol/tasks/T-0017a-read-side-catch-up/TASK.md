# T-0017a: Read-Side Catch-Up

Status: complete
Started: `2026-07-08`
Branch: `task/T-0017a-read-side-catch-up`
Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0017a-read-side-catch-up`
Base commit: `5b14845`

## Objective

Add the first read-side catch-up slice: replay already-stored events into
projection subscribers and `Stand` without re-appending those events.

## Scope

- Production parity/runtime completeness task.
- Not required for minimal local in-memory to-do app readiness.
- Keep implementation small and JVM-familiar.
- Do not introduce a broad query engine, scheduler, delivery retry loop,
  durable subscription recovery, process-manager runtime, or ZeroMQ topology.

## Human-Imposed Requirements Ledger

- Continue autonomously until all tasks are done or a real blocker appears.
- Keep `human-review-1-jul.md` untouched.
- Use this branch/worktree for this task.
- Spawn one implementation sub-agent for the task.
- Run independent reviewer sub-agents for code style/maintainability,
  documentation, TypeScript/API docs, security, and performance/reliability.
- Feed reviewer comments back to an authoring/fix sub-agent and repeat until
  all lanes are clean.
- Close every participating sub-agent once its role is complete.
- No change may be made without updating the relevant durable log.
- Preserve Protobuf contracts, generated-code policy, and generated registry
  contracts.
- Keep generated output ignored and regenerated by builds.
- Keep end-user code free of framework `Event` envelopes, manual transactions,
  schema-bearing decorators, `@Apply`, and application-owned handler
  materialization.
- Server-module implementation requires close inspection of local Spine JVM
  docs and corresponding `core-jvm/server` sources before design or code
  changes.

## Required Research Before Implementation

- `spine-jvm-docs/spine-routing-dispatch-and-delivery.md`
- `spine-jvm-docs/spine-client-api-queries-subscriptions-and-tests.md`
- `spine-jvm-docs/spine-server-runtime-and-bounded-context.md`
- Relevant local `core-jvm/server` source paths when available for `Stand`,
  projection catch-up, delivery catch-up, and event replay.
- Current TS surfaces:
  - `packages/storage/src/event/event-store.ts`
  - `packages/server/src/bus/event-bus.ts`
  - `packages/server/src/repository/repository.ts`
  - `packages/server/src/context/bounded-context.ts`
  - `packages/server/src/stand/stand.ts`
  - `examples/todo/src/index.ts`

## Acceptance Criteria

- A built context can run catch-up over stored events for registered projection
  subscribers.
- Catch-up uses already-stored events and does not append duplicates.
- Projection state is rebuilt through framework-owned projection subscriber
  execution and `Stand` writes.
- Tenant scoping is preserved.
- Failures are observable with deterministic, small diagnostics.
- Public docs and TypeScript/API docs describe the supported catch-up boundary.
- T-0017a logs record JVM research, implementation decisions, verification, and
  all agent participation.

## Verification Plan

- Focused catch-up tests for server/repository/stand behavior.
- Focused to-do test proving `TaskList` can be rebuilt from stored events.
- `pnpm format:check`
- `pnpm lint`
- `pnpm docs:check`
- `pnpm proto:check-generated`
- `pnpm test:coverage`
- `git diff --check`
