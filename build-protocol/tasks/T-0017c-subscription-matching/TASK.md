# T-0017c: Rich Entity Subscription Matching

Status: complete
Started: `2026-07-08`
Branch: `task/T-0017c-subscription-matching`
Worktree:
`/Users/armiol/development/experiments/spine-ts/.worktrees/T-0017c-subscription-matching`
Base commit: `1892fef`

## Objective

Match entity subscription updates with filters and masks, including transitions
that no longer match, instead of streaming every update for a subscribed target.

## Scope

- Production parity/runtime completeness task.
- Depends on completed `T-0017b` query/filter semantics.
- Keep implementation small and JVM-familiar.
- Do not introduce event-topic subscriptions, durable subscription recovery,
  delivery schedulers, process managers, or new transport topology in this slice.

## Human-Imposed Requirements Ledger

- Continue autonomously until all tasks are done or a real blocker appears.
- Keep `human-review-1-jul.md` untouched.
- Use this branch/worktree for this task.
- Spawn one implementation sub-agent for the task.
- Run independent reviewer sub-agents for code style/maintainability,
  documentation, TypeScript/API docs, security, and performance/reliability.
- Feed reviewer comments back to an authoring/fix sub-agent and repeat until
  all lanes are clean.
- Close every participating sub-agent once its role is complete.
- No change may be made without updating the relevant durable log.
- Preserve Protobuf contracts, generated-code policy, and generated registry
  contracts.
- Keep generated output ignored and regenerated by builds.
- Keep end-user code free of framework `Event` envelopes, manual transactions,
  schema-bearing decorators, `@Apply`, and application-owned handler
  materialization.
- Server-module implementation requires close inspection of local Spine JVM
  docs and corresponding `core-jvm/server` sources before design or code
  changes.

## Required Research Before Implementation

- `spine-jvm-docs/spine-client-api-queries-subscriptions-and-tests.md`
- `spine-jvm-docs/spine-server-runtime-and-bounded-context.md`
- Relevant local `core-jvm/server` sources for `SubscriptionService`, `Stand`,
  entity-state subscription matching, update kinds, masks, and cancellation.
- Current TS surfaces:
  - `packages/server/src/stand/stand.ts`
  - `packages/server/src/services/spine-services.ts`
  - `packages/testing/src/index.ts`
  - existing subscription service and todo example tests.

## Acceptance Criteria

- Entity subscriptions apply supported filters before delivering updates.
- Response masks are applied to delivered entity states where the proto topic
  requests them.
- Updates that stop matching are reported according to the selected Spine
  client contract for this slice.
- Single-tenant and multitenant subscription behavior remains tenant-scoped.
- Invalid or unsupported subscription criteria fail with small deterministic
  diagnostics before attaching storage/update listeners.
- Public docs and TypeScript/API docs describe the supported subscription
  boundary.
- T-0017c logs record JVM research, implementation decisions, verification, and
  all agent participation.

## Verification Plan

- Focused subscription service and `Stand` tests.
- Black-box todo fixture subscription tests when useful.
- `pnpm format:check`
- `pnpm lint`
- `pnpm docs:check`
- `pnpm proto:check-generated`
- `pnpm --config.verify-deps-before-run=false verify`
- `git diff --check`
