# Session Handoff: T-0026 After Round 24

Created: `2026-07-10`
Reason: current Codex chat is too large; continue in a fresh chat.

## Current Repository State

- Root repository: `/Users/armiol/development/experiments/spine-ts`
- Active coding worktree:
  `/Users/armiol/development/experiments/spine-ts/.worktrees/T-0026-transport-backed-delivery-workers`
- Active branch: `task/T-0026-transport-backed-delivery-workers`
- Current task branch HEAD: the latest handoff commit. Confirm with
  `git log --oneline -4` in the T-0026 worktree.
- Last T-0026 code commit before the handoff-only commit:
  `4f397c9b Fix delivery endpoint accounting and stale claims`
- Task baseline for review packages and final branch diff: `ca8fb2b3`
- Root `main` remains unmodified by T-0026. Its only visible untracked file is
  `human-review-1-jul.md`; do not touch it unless the user explicitly asks.
- T-0026 worktree status at handoff creation: only `.codex-review-packages/`
  is untracked. It is scratch review-package material and should be left
  untouched unless cleanup is explicitly requested.

Recent T-0026 commits:

```text
4f397c9b Fix delivery endpoint accounting and stale claims
36806236 Fix delivery scan and callback snapshots
1fb71b70 Skip unsupported delivery labels
ba61aa0f Fix delivery loop failure budget
d3862137 Fix storage canonical keys
9b3942dc Fix delivery paging scan
a33e2ca2 Fix delivery scan and lease bounds
e7267361 Fix delivery cleanup failure contract
```

## Important Standing Instructions

- Continue autonomously under `build-protocol/BUILD_PROTOCOL.md`.
- Stop only for a real blocker or when the user explicitly asks for a safe stop.
- Use one feature branch and worktree per coding task/sub-task.
- Use subagents where the protocol requires them. Close every subagent once its
  role is complete.
- For every implementation or docs task, run five reviewer lanes:
  code style/maintainability, documentation, TypeScript/API docs, security, and
  performance/reliability.
- Feed reviewer comments back to an implementation/fix worker and repeat until
  all lanes are clean.
- No change may be made without updating the relevant durable task/work/review
  logs.
- Do not modify or rely on `human-review-1-jul.md`.
- Keep generated Protobuf output out of VCS.
- Preserve Spine Protobuf contracts, type URLs, options, and modeling
  conventions.
- Keep end-user code free of framework `Event` envelopes, manual transactions,
  `@Apply`, schema-bearing decorators, and app-owned handler materialization.
- Aggregate import/importers, `ImportBus`, and aggregate `@Apply` delivery are
  removed from the active roadmap.
- `IMPORT_EVENT` is not a supported public delivery label for new inbox writes;
  legacy stored rows are deprecated compatibility data that fail closed.
- For server-module work, inspect relevant Spine JVM `core-jvm/server` docs and
  source before design or implementation, and avoid over-engineering.
- Prefer grouped classes/objects over scattered helpers, short explicit names,
  and small methods. Public names must not exceed four semantic components.
- Callback names start with `on`/`On`, unless the exact generic parameter name
  `callback` is intentional.

## Skills To Re-read In The New Chat

Read these skill instructions before continuing T-0026:

- `/Users/armiol/.agents/skills/subagent-driven-development/SKILL.md`
- `/Users/armiol/.agents/skills/requesting-code-review/SKILL.md`
- `/Users/armiol/.agents/skills/verification-before-completion/SKILL.md`
- `/Users/armiol/.agents/skills/using-git-worktrees/SKILL.md`

Use any additional relevant installed skills when they apply, especially:

- `test-driven-development`
- `implement`
- `javascript-testing-patterns`
- `typescript-advanced-types`
- `nodejs-backend-patterns`
- `codebase-design`
- `security-best-practices`

## Current Task: T-0026

Task file:
`build-protocol/tasks/T-0026-transport-backed-delivery-workers/TASK.md`

Work log:
`build-protocol/work-logs/T-0026.md`

Review log:
`build-protocol/reviews/T-0026-transport-backed-delivery-workers.md`

Round reports:

- `build-protocol/tasks/T-0026-transport-backed-delivery-workers/round-23-fix-report.md`
- `build-protocol/tasks/T-0026-transport-backed-delivery-workers/round-24-fix-report.md`

Current task status: Round 24 fixes are committed and coordinator-verified, and
this handoff-only commit has been added without reviewers by explicit user
request. The mandatory five-lane re-review after the Round 24 code commit
`4f397c9b` has not yet been run.

## What Round 24 Changed

Round 24 fixed findings from the previous review package:

- Added missing Round 23 and Round 24 durable entries to the work log and
  review log.
- Added `round-24-fix-report.md`.
- Exported `DeliveryEndpointMessage`.
- Narrowed `DeliveryEndpoint` and `DeliveryFailure.message` so public callback
  and failure messages only admit supported worker labels:
  `HANDLE_COMMAND`, `UPDATE_SUBSCRIBER`, and `REACT_UPON_EVENT`.
- Changed `InboxStorage` claim CAS so expired per-message claims can be
  atomically reclaimed using the storage clock. Live claims still block.
- Clarified the `InboxClaim` comment: expired claims are not proactively swept,
  but claim CAS may reclaim an expired row.
- Kept pre-callback claim/lease failures visible in `DeliveryRun.failures`
  without incrementing `accepted`.
- Decoupled direct-drain read page size from accepted-work `limit`, so
  `limit: 1` plus skipped head rows does not perform one storage query per
  skipped row.
- Added regression tests for expired-claim reclaim, pre-callback limit
  accounting, bounded page reads, and live-vs-expired claim behavior.
- Updated API docs and API export checks for `DeliveryEndpointMessage`.

Coordinator verification run before commit `4f397c9b`:

```bash
pnpm --config.verify-deps-before-run=false exec vitest run packages/server/test/delivery/delivery-worker.test.ts packages/server/test/delivery/delivery-loop.test.ts packages/server/test/delivery/inbox.test.ts
# PASS: 3 files, 165 tests

pnpm --config.verify-deps-before-run=false typecheck:build:generated
# PASS

pnpm --config.verify-deps-before-run=false docs:check
# PASS, with only the existing invalid-origin TypeDoc source-link warning

pnpm --config.verify-deps-before-run=false format:check
# PASS

git diff --check
# PASS
```

## Immediate Next Steps

1. Work in the existing T-0026 worktree:

   ```bash
   cd /Users/armiol/development/experiments/spine-ts/.worktrees/T-0026-transport-backed-delivery-workers
   git status --short
   git log --oneline -6
   ```

2. Generate a fresh review package from the task baseline to the current HEAD:

   ```bash
   /Users/armiol/.agents/skills/subagent-driven-development/scripts/review-package ca8fb2b3 HEAD
   ```

   Expected new package path pattern:
   `.superpowers/sdd/review-ca8fb2b3..<current-head>.diff`

3. Spawn the five required reviewer lanes against that review package:

   - code style/maintainability;
   - documentation;
   - TypeScript/API docs;
   - security;
   - performance/reliability.

4. Close each reviewer when it completes.

5. If any reviewer returns findings:

   - Update the relevant durable log first.
   - Spawn one fix worker with the complete findings list.
   - Rerun focused verification yourself after the worker returns.
   - Commit verified fixes.
   - Generate a fresh review package and repeat all five reviewer lanes.

6. If all five reviewer lanes are clean:

   - Update task/work/review logs to record clean review closure.
   - Run final T-0026 verification.
   - Merge `task/T-0026-transport-backed-delivery-workers` into root `main`
     without touching `human-review-1-jul.md`.
   - Run post-merge verification.
   - Continue the autonomous roadmap to the next non-blocked task.

## Suggested Reviewer Prompt Content

Use the fresh review package path from step 2 and tailor each prompt to its lane.
The essential context to include:

- Worktree:
  `/Users/armiol/development/experiments/spine-ts/.worktrees/T-0026-transport-backed-delivery-workers`
- Review package:
  the fresh `.superpowers/sdd/review-ca8fb2b3..<current-head>.diff` path
  printed by the review-package script.
- Task log:
  `build-protocol/tasks/T-0026-transport-backed-delivery-workers/TASK.md`
- Work log:
  `build-protocol/work-logs/T-0026.md`
- Review log:
  `build-protocol/reviews/T-0026-transport-backed-delivery-workers.md`
- Intended delivery semantics:
  - Supported worker labels are `HANDLE_COMMAND`, `UPDATE_SUBSCRIBER`, and
    `REACT_UPON_EVENT`.
  - Valid unsupported labels such as `CATCH_UP` remain pending and are skipped
    before callback invocation, acceptance, failure recording, and
    failure-budget consumption.
  - Malformed/deprecated `IMPORT_EVENT` stored rows remain fail-closed legacy
    corruption.
  - `limit` caps endpoint callbacks actually invoked.
  - Scan budget is finite: storage read cap plus `limit`.
  - Callback-visible `DeliveryEndpointMessage` snapshots must not share mutable
    `Date` or `Any.value` state with the internal claimed CAS row.
  - Expired per-message claims are reclaimable during claim CAS; live claims
    still block.
  - Pre-callback claim/lease failures are reported but do not increment
    `accepted`.

## Final Verification To Run Before T-0026 Merge

At minimum:

```bash
pnpm --config.verify-deps-before-run=false exec vitest run packages/storage/test/memory/in-memory-record-storage.test.ts packages/server/test/delivery/inbox.test.ts packages/server/test/delivery/sharded-work-registry.test.ts packages/server/test/delivery/delivery-worker.test.ts packages/server/test/delivery/delivery-loop.test.ts packages/server/test/delivery/delivery-worker-runtime.test.ts packages/server/test/context/process-manager-handoff.test.ts packages/server/test/context/projection-handoff.test.ts packages/server/test/index.test.ts

pnpm --config.verify-deps-before-run=false typecheck:build:generated
pnpm --config.verify-deps-before-run=false docs:check
pnpm --config.verify-deps-before-run=false format:check
git diff --check
```

If the branch is clean after the five reviewer lanes, consider running the full
project gate before merge:

```bash
pnpm --config.verify-deps-before-run=false verify
```

The user has explicitly allowed native execution for:

- `corepack`
- `pnpm install`
- `pnpm --config.verify-deps-before-run=false verify`
- local IPC / loopback listener tests

If sandboxing blocks an important command, request escalation with
`sandbox_permissions: "require_escalated"` and a specific justification.

## Detailed Prompt For The New Chat

Copy this into the new chat:

```text
Continue the autonomous Spine TS development process from the committed T-0026
handoff.

Repository root:
/Users/armiol/development/experiments/spine-ts

Current active worktree:
/Users/armiol/development/experiments/spine-ts/.worktrees/T-0026-transport-backed-delivery-workers

Active branch:
task/T-0026-transport-backed-delivery-workers

Current HEAD:
Use `git log --oneline -4` in the T-0026 worktree. The latest commit should be
the handoff-only commit; the last Round 24 code commit is:
4f397c9b Fix delivery endpoint accounting and stale claims

Read this handoff first:
build-protocol/handoffs/2026-07-10-t0026-round24-handoff.md

Then read:
- build-protocol/BUILD_PROTOCOL.md
- build-protocol/TECHNICAL_SPEC.md
- build-protocol/RUNTIME_ARCHITECTURE.md
- build-protocol/DEVELOPER_API.md
- build-protocol/CODE_QUALITY.md
- build-protocol/tasks/T-0026-transport-backed-delivery-workers/TASK.md
- build-protocol/work-logs/T-0026.md
- build-protocol/reviews/T-0026-transport-backed-delivery-workers.md
- build-protocol/tasks/T-0026-transport-backed-delivery-workers/round-23-fix-report.md
- build-protocol/tasks/T-0026-transport-backed-delivery-workers/round-24-fix-report.md

Also re-read these skills before acting:
- /Users/armiol/.agents/skills/subagent-driven-development/SKILL.md
- /Users/armiol/.agents/skills/requesting-code-review/SKILL.md
- /Users/armiol/.agents/skills/verification-before-completion/SKILL.md
- /Users/armiol/.agents/skills/using-git-worktrees/SKILL.md

Important constraints:
- Continue autonomously. Do not stop unless there is a real blocker or I ask
  for a safe stop.
- Do not ask for confirmation.
- Do not touch /Users/armiol/development/experiments/spine-ts/human-review-1-jul.md.
- Do not spawn duplicate agents. Close every subagent when its role is done.
- Keep following the build protocol: durable logs first, one worker per fix
  batch, five reviewer lanes for every task/docs task, repeat until clean.
- No reviewers are needed for the handoff commit itself, but reviewers are
  required before accepting T-0026 as complete.

Immediate job:
1. In the T-0026 worktree, confirm status. It should have only
   .codex-review-packages/ untracked.
2. Generate a fresh review package:
   /Users/armiol/.agents/skills/subagent-driven-development/scripts/review-package ca8fb2b3 HEAD
3. Run five reviewer subagents against the fresh package:
   code style/maintainability, documentation, TypeScript/API docs, security,
   performance/reliability.
4. Close each reviewer when it returns.
5. If findings appear, update durable logs, spawn one fix worker, verify, commit,
   regenerate the review package, and repeat the five review lanes.
6. If all lanes are clean, update logs, run final T-0026 verification, merge
   task/T-0026-transport-backed-delivery-workers into root main without touching
   human-review-1-jul.md, run post-merge verification, then continue the next
   non-blocked roadmap task autonomously.

Do not summarize and stop after reading. Continue execution.
```

## Notes For The Next Coordinator

- The current chat closed all subagents it spawned in the latest active batch.
- The current root thread may not expose `list_agents`; if the user asks for
  cleanup, use actual multi-agent tools available in that thread and do not
  rely on the Desktop UI list.
- Shell commands often print this harmless warning:
  `/opt/homebrew/Cellar/jenv/0.5.5_2/libexec/libexec/jenv-refresh-plugins:
line 43: /Users/armiol/.jenv/jenv.version: Operation not permitted`.
  Treat it as shell noise when commands exit `0`.
- `docs:check` passes with the existing TypeDoc invalid `origin` warning only.
- Generated Protobuf code under package `generated` directories must remain
  ignored and regenerated by build steps.
