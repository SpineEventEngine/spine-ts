# T-0015g Review Log

Status: clean

Required lanes:

- Code style/maintainability: clean
- Documentation: clean
- TypeScript/API docs: clean
- Security: clean
- Performance/reliability: clean
- JVM alignment / ADR 0001: clean

## Review Rounds

### 2026-07-08 00:38 WEST

Manual two-axis review against baseline `81c325b`.

Reviewer note: the `review` skill expects a separate Agent tool for parallel
sub-agent lanes, but that tool is not available in this session. I performed
the same standards/spec review manually and verified with the required command
suite.

Standards sources checked:

- `build-protocol/tasks/T-0015g-todo-generated-registry/TASK.md`
- `build-protocol/BUILD_PROTOCOL.md`
- `build-protocol/CONTRIBUTOR_WORKFLOW.md`
- `build-protocol/DEVELOPER_API.md`
- `build-protocol/TECHNICAL_SPEC.md`
- `docs/api/README.md`
- `docs/USER_GUIDE.md`
- `scripts/check-cleanup-rules.mjs`

Spec review:

- Clean. The example no longer imports or calls `defineEntityHandlers()` or
  `materializeDecoratedEntityHandlers()`.
- Clean. Example handlers remain bare `@Assign`/`@Subscribe`, return generated
  domain event messages for commands, and `void` for subscriptions.
- Clean. No `@Apply`, app-managed framework transaction calls, app-created
  event IDs, or `packEvent()` calls appear in `examples/todo/src/index.ts`.
- Clean. The generated registry is regenerated by `proto:generate`, remains
  ignored under `examples/todo/generated/`, compiles into `dist/generated/`,
  and is loaded through `GeneratedRegistryDiscovery` during context creation.
- Clean. Default command routing internals were not changed; existing and
  focused tests still cover first-field command routing before handler
  execution.

Standards review:

- Clean. The implementation keeps the API small: one script, one workflow hook,
  async context creation, and no global locator/container/facade.
- Clean. Generated output is ignored and not staged.
- Clean. Documentation covers example build/run behavior and the
  app-package-generated registry workflow.
- Clean with note: `createTodoContext()` is now async to avoid an ESM cycle
  when the generated registry imports entity classes from `src/index.js`. This
  is the smallest API adjustment consistent with the task packet.

Verification reviewed:

- `corepack pnpm typecheck:build`
- `corepack pnpm vitest run examples/todo/src/index.test.ts`
- `corepack pnpm vitest run packages/server/test/handler/build-time-handler-analyzer.test.ts packages/server/test/handler/generated-registry-writer.test.ts packages/server/test/handler/generated-registry-discovery.test.ts packages/server/test/handler/generated-handler-registry.test.ts`
- `corepack pnpm docs:check`
- `corepack pnpm lint`
- `corepack pnpm format:check`
- `git diff --check`
