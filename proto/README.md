# Proto Intake

This directory is reserved for future copied Spine `.proto` files.

T-0002 intentionally does not copy Spine Protobuf definitions. Later proto-intake work must pin the upstream source, copy the required files verbatim, verify drift, run Buf lint, and generate Protobuf-ES TypeScript into `packages/proto/src/generated`.
