# To-Do Example

Standalone server-side to-do example placeholder.

The example domain, Protobuf files, server startup, command handling, queries, subscriptions, validation behavior, and black-box tests are deferred until the framework runtime exists.
