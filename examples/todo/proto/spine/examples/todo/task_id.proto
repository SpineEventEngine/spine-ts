syntax = "proto3";

package spine.examples.todo;

import "spine/options.proto";

option (type_url_prefix) = "type.spine.examples.todo";

// Identifies a task in the to-do list.
message TaskId {
  // Holds the task identifier value.
  string value = 1 [(required) = true];
}
