# Deployment

For operators assembling a Spine Gateway with application-node discovery. See [the reference](REFERENCE.md) for the contract.

`StaticNodeDiscovery` supplies complete local node snapshots. Nodes use stable opaque IDs and canonical HTTP(S) origins.
