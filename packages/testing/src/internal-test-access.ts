/** Internal-only lifecycle test access. This module is intentionally not package exported. */
export { createBlackBoxForTesting } from "./black-box.js";
/** Internal-only tracked-handle test access. This module is intentionally not package exported. */
export { trackBlackBoxHandleForTesting } from "./black-box.js";
/** Internal-only startup test access. This module is intentionally not package exported. */
export { openBlackBoxForTesting } from "./black-box.js";
