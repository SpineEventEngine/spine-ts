# @spine-ts/proto

Package skeleton for copied Spine Protobuf definitions and generated Protobuf-ES TypeScript.

T-0002 intentionally does not copy Spine `.proto` files. Future proto-intake work will populate `proto/`, generate code into `packages/proto/src/generated`, and expose stable schema exports from this package.
